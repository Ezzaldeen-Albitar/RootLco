# Phase 1-28 — Appointment and Vehicle Reception Frontend — canonical plan

**Classification:** Confidential — Commercial Product and Pilot Planning

**Status:** authored on day one of the phase, before any screen, per exit
criterion `U1` of the workshop frontend program
(`docs/product/workshop/frontend-implementation-program.md:152` — contract
archaeology written down before the first screen file is created). This
document is P1-28's executable task authority. Where any other P1-28 statement
disagrees with it, this one is correct.

---

## 0. Why this document exists

P1-27 spent five adversarial rounds paying for a task register that did not
exist when the first screen was built: 33 adjudicated items were counted as 42
canonical tasks, nine tasks appeared in no status table anywhere, and
`RESOLVED / 42 = 42` was written from a table plus a remainder nobody listed.

P1-28 starts the other way round. This register, its derived task matrix
(`task-matrix.json`, generated by `npm run matrix:p1-28`), and the gate that
refuses drift (`validate:p1-28-matrix`) all exist before the first component.
The repository held **no** P1-28 planning document before this one
(`docs/product/workshop/reception-media-checklist.md:476-479` records the
absence); the factual foundation underneath every binding in §5 is
`docs/phase-1/phase-1-28/contract-archaeology.md`, verified file-by-file at
`develop` `d64f259`.

---

## 1. Product, company, tenant, scope

| term        | value                           | note                                                                        |
| ----------- | ------------------------------- | --------------------------------------------------------------------------- |
| **Product** | **CRM**                         | Temporary but decided. No placeholder form is authoritative.                |
| **Company** | **RootLco**                     | Unchanged.                                                                  |
| **Benzene** | a **configurable pilot tenant** | Never hard-coded. `validate:no-fake-data` and the Benzene guard enforce it. |
| **Zoom**    | **outside Phase 1**             | The excluded-scope guard enforces it.                                       |

---

## 2. Dependencies

P1-28 depends on **seven** phases:

| phase     | what P1-28 consumes from it                                                                                                                                                             |
| --------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **P1-16** | CRM / customer Backend capability (search, detail, creation, vehicle link).                                                                                                             |
| **P1-17** | Vehicle Backend capability (search, detail, creation, odometer, relationships).                                                                                                         |
| **P1-18** | Appointment and Reception Backend capability — the 12 write commands this phase consumes, and the owner of every read-surface remediation in §4.                                        |
| **P1-24** | Access-control and route-depth verification; authorization and scope behaviour.                                                                                                         |
| **P1-25** | Frontend architecture and design system.                                                                                                                                                |
| **P1-26** | Authentication, Administration, shared shell, language, notification, navigation, scroll, table, and the Owner-accepted Frontend baseline.                                              |
| **P1-27** | The CRM and Vehicle screens this phase composes (walk-in intake reuses customer/vehicle creation), the shared party/customer libraries, and the API client and idempotency conventions. |

P1-28 composes P1-27's public modules (`@/components/party`, `@/lib/customers`)
and never imports `features/crm` internals.

---

## 3. Visual decision

The **P1-25 / P1-26 design foundations are binding**. P1-28 **composes approved
components**, must not create a competing design system, and may build
feature-specific composition (a check-in wizard layout is composition, not a
new design system). Brand changes require controlled change, not a P1-28
commit. The prohibition list is enforced by `validate:web-boundary`,
`validate:web-brand`, `validate:web-tokens` and
`validate:notification-authority`.

---

## 4. Backend ownership

Permanent rule, carried from P1-27 §4: **no new Backend feature development is
allowed inside the P1-28 Frontend branch.** A real Backend gap goes through
separate protected remediation owned by the Backend phase that owns the domain.

The contract archaeology proved the apt/rec public surface is **12 POST
commands and zero reads**
(`docs/product/workshop/frontend-implementation-program.md:334-336`). The
remediation register — what is missing, who owns it, and the smallest fix — is
`contract-archaeology.md` §4 (R1 through R8). The two highest-leverage items:

| id     | remediation                                                                                                                                                                                                                         | owner |
| ------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----- |
| **R1** | Execute `docs/phase-1/phase-1-27/reception-read-surface-plan.md` — six GETs plus the `rec.reception.read` seed. EXECUTED 2026-08-12 — PR #220; the plan’s own status line records the execution and the deliberate arm-E narrowing. | P1-18 |
| **R2** | Appointment read surface — list, detail, and the `apt.appointment.read` seed (`p1-18-p1-28-boundaries.md:29-43`). EXECUTED 2026-08-12 — PR #220.                                                                                    | P1-18 |

A task in §5 whose operations are marked `[MISSING Rn]` binds the operation
that remediation will create; the binding is honest about not existing yet. **Re-derived 2026-08-12:** R1–R5 are EXECUTED and integrated at this branch’s head — PR #220 (P1-18 read surface, seven catalogues, terminal closes) and PR #221 (P1-16 customer-vehicle read) — and no §5 binding carries the marker any longer. R6–R8 remain open.

---

## 5. Task register

**Canonical total: 35.** Frontend 22 · Security 4 · QA 5 · DevOps 2 ·
Documentation 2.

Every title is **domain-qualified**. Operations are bound from the contract
archaeology: a plain id (for example `apt.appointment-create`) is registered
and routable at this branch’s head (originally recorded at `develop` `d64f259`; re-derived 2026-08-12 after R1–R5 landed); an id marked `[MISSING Rn]` did not exist
at that head and names the operation remediation `Rn` of
`contract-archaeology.md` §4 will create; a bracketed `[kind ...]` or
`[fields ...]` qualifier narrows one registered operation to the part of its
contract the task consumes.

Test references follow the P1-27 §6 rule, mirrored: **appointment tasks take
the appointment catalogue** (`TC-P1-28-APT-###`), **reception tasks the
reception catalogue** (`TC-P1-28-REC-###`), and **genuinely cross-domain tasks
are marked as such** (`TC-P1-28-XD-###`) rather than arbitrarily assigned. Four
tasks are cross-domain: `FE-006` (walk-in intake composes CRM and Vehicle),
`FE-008` (identity confirmation joins CRM, Vehicle and Reception), `FE-013`
(odometer capture is a Vehicle-domain write consumed by Reception), and
`FE-022` (conversion reads the Work Order domain).

### 5.1 Appointment Frontend — 5 tasks

| id             | canonical name                           | primary Backend operation(s)                                                                         | test id            |
| -------------- | ---------------------------------------- | ---------------------------------------------------------------------------------------------------- | ------------------ |
| `P1-28-FE-001` | Appointment calendar and scheduling view | `apt.appointment-list`, `apt.appointment-detail`                                                     | `TC-P1-28-APT-001` |
| `P1-28-FE-002` | Appointment booking form                 | `apt.appointment-create`, `apt.catalogue-appointment-type-list`, `apt.catalogue-source-channel-list` | `TC-P1-28-APT-002` |
| `P1-28-FE-003` | Appointment reschedule and confirmation  | `apt.appointment-reschedule`                                                                         | `TC-P1-28-APT-003` |
| `P1-28-FE-004` | Appointment cancellation                 | `apt.appointment-cancel`, `apt.catalogue-cancellation-reason-list`                                   | `TC-P1-28-APT-004` |
| `P1-28-FE-005` | Appointment no-show recording            | `apt.appointment-no-show`                                                                            | `TC-P1-28-APT-005` |

### 5.2 Reception Frontend — 17 tasks

| id             | canonical name                                  | primary Backend operation(s)                                                                                                         | test id            |
| -------------- | ----------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------ | ------------------ |
| `P1-28-FE-006` | Walk-in customer and vehicle intake             | `crm.customer-search`, `crm.individual-create`, `crm.company-create`, `veh.vehicle-search`, `veh.vehicle-create`, `crm.vehicle-link` | `TC-P1-28-XD-001`  |
| `P1-28-FE-007` | Reception check-in wizard                       | `rec.reception-create`, `iam.user-list`, `rec.reception-list`, `rec.reception-detail`                                                | `TC-P1-28-REC-001` |
| `P1-28-FE-008` | Reception customer and vehicle confirmation     | `crm.customer-read`, `veh.vehicle-read`, `veh.vehicle-relationship-list`, `crm.customer-vehicle-list`                                | `TC-P1-28-XD-002`  |
| `P1-28-FE-009` | Reception party roles and authorization         | `rec.reception-party-role`, `rec.reception-authorization`, `rec.reception-party-role-list`, `rec.reception-authorization-list`       | `TC-P1-28-REC-002` |
| `P1-28-FE-010` | Reception customer complaint capture            | `rec.reception-condition-evidence [kind complaint]`                                                                                  | `TC-P1-28-REC-003` |
| `P1-28-FE-011` | Reception visual inspection and condition items | `rec.reception-condition-evidence [kinds inspection/condition_item/leak]`                                                            | `TC-P1-28-REC-004` |
| `P1-28-FE-012` | Reception damage map and marks                  | `rec.reception-condition-evidence [kinds damage_map/damage_mark]`, `shared.document-read`                                            | `TC-P1-28-REC-005` |
| `P1-28-FE-013` | Reception odometer capture                      | `veh.vehicle-odometer-record`, `veh.vehicle-odometer-history`                                                                        | `TC-P1-28-XD-003`  |
| `P1-28-FE-014` | Reception fuel level and EV state of charge     | `rec.reception-create [fields fuelLevelId/evSocPercent]`, `rec.catalogue-fuel-level-list`                                            | `TC-P1-28-REC-006` |
| `P1-28-FE-015` | Reception warning lights                        | `rec.reception-condition-evidence [kind warning_light]`, `rec.catalogue-warning-light-code-list`                                     | `TC-P1-28-REC-007` |
| `P1-28-FE-016` | Reception vehicle contents                      | `rec.reception-condition-evidence [kind contents]`                                                                                   | `TC-P1-28-REC-008` |
| `P1-28-FE-017` | Reception camera and media upload               | `shared.attachment-upload-authorize`, `shared.attachment-version-register`, `shared.attachment-link-create`, `shared.document-read`  | `TC-P1-28-REC-009` |
| `P1-28-FE-018` | Reception signature capture                     | `rec.reception-signature`                                                                                                            | `TC-P1-28-REC-010` |
| `P1-28-FE-019` | Reception refusal workflow                      | `rec.reception-refusal`, `rec.catalogue-refusal-reason-list`                                                                         | `TC-P1-28-REC-011` |
| `P1-28-FE-020` | Reception summary and approval                  | `rec.reception-approve`, `rec.reception-detail`, `rec.reception-condition-evidence-list`                                             | `TC-P1-28-REC-012` |
| `P1-28-FE-021` | Reception acknowledgement document              | `rec.reception-detail`                                                                                                               | `TC-P1-28-REC-013` |
| `P1-28-FE-022` | Reception conversion to work order              | `rec.reception-convert-to-work-order`, `wo.work-order-detail`                                                                        | `TC-P1-28-XD-004`  |

### 5.3 Security — 4 · QA — 5 · DevOps — 2 · Documentation — 2

| id              | canonical name                                                        |
| --------------- | --------------------------------------------------------------------- |
| `P1-28-SEC-001` | Least-privilege permission and resolved-scope enforcement             |
| `P1-28-SEC-002` | Sensitive-narrative controls for complaint and contents evidence      |
| `P1-28-SEC-003` | Scope hygiene and abuse-case controls                                 |
| `P1-28-SEC-004` | Write-reachability gate for appointment and reception operations      |
| `P1-28-QA-001`  | Contract-mirror unit and component coverage                           |
| `P1-28-QA-002`  | API contract, error-path and replay-shape coverage                    |
| `P1-28-QA-003`  | Tenant / company / branch isolation                                   |
| `P1-28-QA-004`  | Concurrency, idempotency and record-version sourcing                  |
| `P1-28-QA-005`  | Regression and immutable evidence packaging                           |
| `P1-28-DO-001`  | Continuous-integration quality gate and gate-metadata co-maintenance  |
| `P1-28-DO-002`  | Structured logging, monitoring and pipeline wiring                    |
| `P1-28-DOC-001` | Contract archaeology, canonical plan and traceability synchronization |
| `P1-28-DOC-002` | Operator / developer guidance and change-log update                   |

The non-Frontend categories bind no operation and no test id in this table, per
the P1-27 §5.3 convention: their obligations are phase-wide rather than
per-contract, and `P1-27-OD-007` records why a two-column row structurally
carries no per-task test id. Their scope in P1-28 terms:

- `SEC-001` — seed role-to-`apt.*`/`rec.*` grant mapping, an owner/reception
  account, a read-only negative control, a cross-tenant negative control, and
  an explicit disposition for the `iam.user.read` staff-directory overload.
- `SEC-002` — complaint text and contents descriptions render only under
  `iam.sensitive.view`; the WF-27 dual-permission refusal (application check
  passes, database refuses) surfaces to operators as a permissions message,
  never a crash.
- `SEC-003` — no client-asserted scope (P1-27-SEC-001); any `branchId`
  resource selector a future read surface needs enters as a NAMED `query()`
  exemption with a pinning test, never a workaround.
- `SEC-004` — the analogue of `check-p1-27-write-reachability.mjs` for the 12
  `apt.*`/`rec.*` writes: the declared-but-never-wired class (INT-113, P1-27's
  dominant defect) must not recur.
- `QA-002` — the 428/409/422/403 branches per operation and the THREE replay
  shapes (create replay answers 200 with no ETag; approve re-run answers 409
  `ERR-TRN-001`; convert re-run answers 200 `alreadyConverted: true`).
- `QA-004` — every version-guarded write sources its `recordVersion` from a
  command response cached in-session until R1/R2 land; the sourcing discipline
  is the task.
- `DO-001` — `MODULE_DISPOSITION` entries, re-derived doc-count markers, and
  SCAN_ROOTS decisions land in the SAME pull request as the first file that
  changes them.

---

## 6. Scope boundary with P1-29

Quotable, and binding:

> **P1-28 ends where the work order begins.** P1-28 owns appointment
> scheduling, vehicle reception, and the single conversion ACTION
> (`rec.reception-convert-to-work-order`) together with the read-only display
> of its result. **No work-order execution, no technician boards, no
> diagnostics authoring** — no work-order editing, assignment, department
> routing, progress recording, or diagnostic finding of any kind is P1-28
> work. Those are P1-29 (`owner-workflow-requirements.md:220-239`).

There is no `POST /work-orders` anywhere in the platform; the conversion
command is the only way a work order comes to exist, which is exactly why the
boundary sits at that action.

---

## 7. Open decisions inherited

The governing rule, from P1-27 §7: an unresolved decision must **block only the
affected capability** or be **implemented as a decision-neutral foundation**.
It must not block unrelated tasks.

### `P1-OD-025` — document and media file policy · **OPEN**

**Disposition: decision-neutral foundation.**

- **Blocks:** all capture in `FE-017`; the signature-image half of `FE-018`;
  the template-document half of `FE-012`.
- **What ships instead:** the safe foundation with **upload acceptance
  blocked** — no approved file types asserted, no size limit invented, no
  object store assumed, and the open decision named on-screen. The honest
  ceiling of the shipped attachment chain is "registered, pending, never
  downloadable": no storage provider is configured and no scanner exists, so
  no version can reach `accepted`
  (`docs/product/workshop/reception-media-checklist.md:208-210`).
- **Enforced by:** the `no-upload-path` and `no-invented-media-limit` gate
  rules (`scripts/ci/check-p1-27-frontend.mjs:558-711`), which ban any capture
  UI while the decision is open.
- The Owner media rows 12-14 are themselves **Blocked** (`INT-093/094/095`),
  and only the COUNT of seven exterior angles is fixed — the angle set is an
  unconfirmed proposal (`reception-media-checklist.md:102-105`).

### Warning-light catalogue population · **OPEN**

- **Blocks:** `FE-015` only. `rec.warning_light_codes` ships zero rows, no
  read route and no management operation (RMC-11), so the `warning_light`
  evidence kind is unusable in practice.
- **What ships instead:** no warning-light picker; the absence is stated
  on-screen, not papered over with a disabled control. A read route (R3) fixes
  reachability but not emptiness — population needs a management contract or
  an Owner provisioning decision, and the no-fake-data policy forbids
  inventing catalogue rows in a seed.

### Receiving-employee referent — Owner register question A · **OPEN**

- **Blocks:** the receiving-employee field of `FE-007` only.
- **The gap:** `rec.reception_visits.receiving_employee_id` is NOT NULL with
  **no foreign key and no defined referent** (migration `20260721097000:62`);
  the employee master never shipped (`20260718090000:166-196`). An
  `iam.user-list` picker is technically fillable, but nothing defines that an
  `iam.user` id is a legal value, and nothing resolves a stored id to a name
  in any future read.
- **What ships instead:** the field is populated only after the Owner register
  question A (`owner-workflow-requirements.md:199-216`) receives an explicit
  disposition (R6). The UI shows names, never UUIDs.

### Road-test structure — `WF-10` · **OPEN**

- **Blocks:** nothing in this register — deliberately. Road test is **not a
  canonical P1-28 task**: no contract exists anywhere in the platform
  (`INT-054`; `end-to-end-workshop-workflow.md:440-462`), report statuses have
  no not-performed or unsafe outcome, and where a road test lives (reception
  versus job) is an unmade Owner decision.
- **What ships instead:** the only honest subset — free-text
  `notApplicableReason` capture inside `FE-011` (INS-05). **No control may be
  labelled "road test"** until the decision lands and a contract exists.

### Not open decisions — capability gaps

The read surface (`G-APT-READ`, `G-REC-READ`), the catalogue reads
(`G-CAT-APT`, `G-CAT-REC`), the customer-to-vehicles read (`G-CUST-VEH`), the
unreachable terminal visit states (`G-REC-CLOSE`) and the phone-lookup gap
(`G-CRM-PHONE`) are **not decisions awaiting an answer** — they are missing
contracts with named owners and smallest fixes, recorded as R1-R8 in
`contract-archaeology.md` §4 so nobody mistakes a backlog for a blocker that
needs an Owner meeting.

---

## 8. Owner acceptance

**Permanent Frontend rule, binding on every Frontend phase from P1-26 onward:**

> No P1-28 formal closure without real installed-Chrome Owner manual
> acceptance. Automated CI is necessary but **not sufficient**.

Silence is not Pass. The only thing that closes this phase is the Product
Owner manually testing the real application and explicitly returning
`OWNER ACCEPTANCE: PASS`.

Additionally, the Owner register names **reception resumability** a P1-28
readiness blocker (`owner-workflow-requirements.md:182-197`): a phase whose
approve/convert flow works only in a single unbroken browser session may build
behind that limitation, but must not present it for acceptance without the R1
read surface or an explicit Owner waiver recorded in the phase record.

---

## 9. Ownership boundary

Permanent: **`apps/api` is Backend/API only. `apps/web` is Frontend only.**

P1-28 executable work may enter `apps/web/**`, P1-28 Web tests, P1-28
documentation, and approved root CI/tooling required specifically for P1-28.
It must not enter `apps/api/src/**`, `supabase/**`, historical migrations, the
database schema, or Backend route/authorization/OpenAPI implementation.

Frontend work lives in `apps/web/src/features/appointments/**`,
`apps/web/src/features/reception/**` and
`apps/web/src/app/[locale]/(dashboard)/**`.

Required at merge: `APPS_API_EXECUTABLE_DIFF=0`, `SUPABASE_DIFF=0`,
`MIGRATION_DIFF=0`, `UNCLASSIFIED_FILES=0`, `GENERATED_TRACKED_FILES=0`,
`DUPLICATE_FRONTEND_AUTHORITIES=0`, `NESTED_LOCKFILES=0`.

---

## 10. What every Frontend task owes

Arabic · English · RTL · LTR · desktop · tablet · keyboard · accessibility ·
loading · skeleton where appropriate · empty · error · retry · permission-denied ·
conflict where applicable · correlation ID · server-resolved scope · **real API
integration** · evidence.

Mocks are test fixtures only. **Mocks are not production-integration evidence.**

Two truthful-labelling obligations are added for this phase, from the five
riskiest contract facts (`contract-archaeology.md` §5):

- A control is labelled as what it truthfully calls. "Confirm" does not exist
  as an operation — confirmation is a side effect of
  `apt.appointment-reschedule` — so no control may say "Confirm" unless it
  truthfully calls that operation and says so.
- Media state is described as what it is: "registered, pending" — never
  "uploaded" — while `P1-OD-025` is open.
