# Phase 1-28 — Contract archaeology

**Classification:** Confidential — Commercial Product and Pilot Planning

**Source head, and the rule that governs it:** every citation in this document
was first verified file-by-file at `develop` `d64f259` (the P1-27 closure head;
branch `p1-28/foundation` sat on the same commit). **If the tree at a later head
disagrees with a row here, the tree wins and the row is corrected, not
defended.**

**Re-derived at the P1-28 branch head, 2026-08-13 (`P1-28-DOC-001`).** An
earlier addendum said the §1–§3 tables "remain the record AT THE SOURCE HEAD"
while §4 carried the current status. That was an exception to the rule stated
one paragraph above it, and it left this document asserting, in its own tables,
that operations which now exist and which P1-28 screens now call are MISSING —
including its most load-bearing claim of all, that no read operation existed
anywhere in the appointment or reception domain. The exception is withdrawn.
Every row below states what is true AT THIS HEAD; where the source-head fact
explains a decision the phase took, it is kept and labelled as history rather
than left standing as the present tense.

**Re-derived again on the integrated tree, after PR #227
(`P1-28-DOC-001`).** The correction above was made before the intake-catalogue
management surface landed, so the rows it had just corrected went stale again
within the week: three of them still said that no operation anywhere could add a
catalogue row. **Every number this document states about the platform is now a
DERIVED CLAIM** — a `derived:` marker that
`scripts/ci/check-p1-28-traceability.mjs` substitutes the tree's own answer for,
from `docs/phase-1/phase-1-24/evidence/operation-register.json`. Correcting a
hand-written number produces a number that will be wrong again; this is the only
arrangement that survives the next remediation. The same gate refuses the exact
sentences withdrawn here, so none of them can come back quietly.

**What moved.** Remediations R1–R5 of §4 are EXECUTED and merged to `develop` —
PR #220 (R1/R2/R3/R5: the P1-18 read surface, seven catalogue reads under
registered ids `apt.catalogue-*`/`rec.catalogue-*`, and the two terminal
closes) and PR #221 (R4: `crm.customer-vehicle-list`) — and integrated into this
branch. **PR #227** then published the intake-catalogue MANAGEMENT surface:
create, update and status-set across all seven catalogues, plus one
management-projection read per catalogue, every one of them behind
`apt.catalogue.manage` or `rec.catalogue.manage`. Gaps `G-APT-READ`,
`G-REC-READ`, `G-CAT-APT`, `G-REC-CLOSE`, `G-CUST-VEH` and `G-PERM` are CLOSED;
`G-CAT-REC` is closed as a read gap AND as a write gap, and what remains is
neither — the tables still ship no rows because nothing in the product reaches
those writes, which is `P1-28-OD-001` (`canonical-plan.md` §7). `G-EMP`,
`G-MEDIA`, `G-CRM-PHONE`, R6–R8 and every Owner decision remain open.

The apt/rec public surface at this head, derived rather than counted:
<!-- derived: surface operations = 57 --> **57** published operations —
<!-- derived: surface writes = 35 --> **35** writes and
<!-- derived: surface reads = 22 --> **22** reads — carrying
<!-- derived: surface permissions = 14 --> **14** distinct permission codes. Of

those, <!-- derived: surface catalogue-writes = 21 --> **21** writes and
<!-- derived: surface catalogue-reads = 14 --> **14** reads are the intake

catalogue surface — the reads being seven operator-facing pickers and seven
management projections. Every one of the 21 writes is recorded
`DELIBERATELY_ABSENT` in `write-reachability.json` against `P1-28-OD-001`, which
is what
<!-- derived: reachability DELIBERATELY_ABSENT = 21 --> **21** of the manifest's
<!-- derived: reachability total = 35 --> **35** classified writes means.

**What this is:** the factual foundation under `canonical-plan.md` §5. Before
one screen is designed, this records — with file and line — which Backend
contracts the 35 canonical tasks can bind, which are missing, and which are
decision-gated. The governing facts:

- At the source head the entire appointment + reception public surface was
  **12 POST commands and no read of any kind**
  (`docs/product/workshop/frontend-implementation-program.md:334-336`). That is
  history, and it is why every wave in §3 is sequenced the way it is. The
  current figures are the derived ones above, and they are stated in one place
  only: R1/R2/R3 added the reads, R5 added the two terminal commands, and PR
  #227 added the catalogue-management surface.
- The reception read remediation plan existed as **PLANNED, NOT EXECUTED** at
  the source head; it was EXECUTED 2026-08-12 (PR #220) and its status line
  records that (`docs/phase-1/phase-1-27/reception-read-surface-plan.md:4`).
- No canonical P1-28 task register existed anywhere in the repository before
  this phase's `canonical-plan.md`
  (`docs/product/workshop/reception-media-checklist.md:476-479`).

Conventions used below: RV = `recordVersion`. IK = `Idempotency-Key` mandatory
(8-200 chars; missing answers 400 `ERR-INT-002`; key reuse with a different
payload answers 409 `ERR-INT-001`; replay returns the stored body at **200
with no ETag** — `route-handler.ts:369-401`). All operations return
`x-correlation-id` on every response, `Cache-Control: no-store, private`, and
problem+json `{type, title, status, code, correlationId, violations[{path,rule}]}`
(`apps/api/src/server/errors/problem.ts:25-44`). Pagination where it exists:
`{items, nextCursor, hasMore}`, opaque cursor up to 512 chars, limit 1-100
default 50 (`apps/api/src/server/db/pagination.ts:21-66`).

---

## 1. Contract tables

### Table A — Appointments

| #     | capability                                                      | write op                                                                                                                    | route / permission                                                                                                                                                                    | status                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| ----- | --------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| A1    | Book appointment                                                | `apt.appointment-create`                                                                                                    | POST `/api/v1/appointments` (`appointments/route.ts:55-68`); `apt.appointment.manage` (seed `04_iam_permission_catalog.sql:135`), branch scope from body (`appointments/route.ts:97`) | **READY.** Strict body: `companyId`, `branchId`, `vehicleId`, `requesterPartnerId`, `appointmentTypeId` all uuid required; `sourceChannelId` uuid nullable default null; `requestedFrom/To` strings 1-64 that must parse with an explicit UTC offset, To greater than From (`domain/appointment.ts:119-140`). No notes field. 201 `{appointmentId, displayNumber, lifecycleStatus:'requested', recordVersion:1}` + `ETag:"1"`. IK. Audit `apt.appointment.created`.                                                                                                          |
| A2    | Confirm OR reschedule — one operation (WF-02)                   | `apt.appointment-reschedule`                                                                                                | POST `.../{appointmentId}/reschedule` (`reschedule/route.ts:43-57`); `apt.appointment.manage`                                                                                         | **READY** with two traps: `If-Match` REQUIRED (missing 428 `ERR-CON-002`, stale 409 `ERR-CON-001`); any window violation reports path `body.confirmedFrom` even when `confirmedTo` is the bad field (`appointment-service.ts:184`). 200 `{...changeKind:'rescheduled', lifecycleStatus:'confirmed', RV+1}` + ETag. RV came only from a prior write in-session at the source head (G-APT-READ); since R2 it also comes from `apt.appointment-detail`, which is what makes an operator arriving by URL able to reschedule at all.                                              |
| A3    | Cancel appointment                                              | `apt.appointment-cancel`                                                                                                    | POST `.../cancel` (`cancel/route.ts:26-40`); `apt.appointment.lifecycle.manage` (seed `04_iam_permission_catalog.sql:140` — a DIFFERENT code from A1/A2)                              | **READY.** `{cancellationReasonId: uuid}` is mandatory and catalogued, no free text; unknown id answers 422 `unknown_reference`. `If-Match` REQUIRED. IK. The dialog was unbuildable at the source head and is buildable now: R3 shipped `apt.catalogue-cancellation-reason-list`, so the mandatory reason has a picker rather than a guess.                                                                                                                                                                                                                                 |
| A4    | Record no-show                                                  | `apt.appointment-no-show`                                                                                                   | POST `.../no-show` (`no-show/route.ts:33-47`); `apt.appointment.lifecycle.manage`                                                                                                     | **READY.** Body `{}` or null, strict. Legal from `confirmed` ONLY, else 409 `ERR-TRN-001`. `If-Match` REQUIRED. IK.                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| A5    | Appointment list / day queue / branch calendar                  | read: `apt.appointment-list`                                                                                                | GET `/api/v1/appointments` (`appointments/route.ts:149-160`); `apt.appointment.read` (seed `04_iam_permission_catalog.sql:148`)                                                       | **CLOSED by R2 (PR #220).** MISSING at the source head, where no `apt.*.read` permission existed at all — that is why Wave C was sequenced BLOCKED. FE-001 consumes it today.                                                                                                                                                                                                                                                                                                                                                                                                |
| A6    | Appointment detail (resolve vehicle/window/RV from an id)       | read: `apt.appointment-detail`                                                                                              | GET `/api/v1/appointments/{appointmentId}`; `apt.appointment.read`                                                                                                                    | **CLOSED by R2 (PR #220).** MISSING at the source head, where an operator arriving by URL could load nothing. It is now the If-Match source for A2/A3/A4.                                                                                                                                                                                                                                                                                                                                                                                                                    |
| A7-A9 | Appointment-type / source-channel / cancellation-reason pickers | reads: `apt.catalogue-appointment-type-list`, `apt.catalogue-source-channel-list`, `apt.catalogue-cancellation-reason-list` | GET `/api/v1/appointment-catalogue/{appointment-types,source-channels,cancellation-reasons}`; tables `20260721091000_apt_configuration_catalogs.sql:46`, `:105`, `:164`               | **CLOSED as a READ gap by R3 (PR #220);** the registered ids took the `apt.catalogue-*` form rather than the `apt.appointment-type-list` form §4 proposed. **The WRITE half closed too, in PR #227:** create, update and status-set for each of the three, plus a management-projection read, all behind `apt.catalogue.manage`. Neither half populates a table. These three ship rows only where a tenant has provisioned them; no screen in this product reaches the management writes and no seed grants the code, which is `P1-28-OD-001`. No row is ever invented here. |

Lifecycle facts every appointment screen encodes: vocabulary
`requested / pending_confirmation / confirmed / checked_in / cancelled / no_show`;
`pending_confirmation` is reachable by NO operation (render-only);
`checked_in` is reachable ONLY as a side effect of reception create, in the
same transaction, with no version predicate
(`appointment-repository.ts:197-213`); the requested window is immutable
forever; **no appointment edit operation exists at all**
(`domain/appointment.ts:23-53,158-220`).

### Table B — Receptions

| #       | capability                                                                                                                       | write op                                                                                                                                                                                   | route / permission                                                                                                                                                                                                                                  | status                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| ------- | -------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| B1      | Check-in (create visit; appointment XOR walk-in origin)                                                                          | `rec.reception-create`                                                                                                                                                                     | POST `/api/v1/receptions` (`receptions/route.ts:71-98`); `rec.reception.manage` (seed `04_iam_permission_catalog.sql:152`)                                                                                                                          | **READY.** Strict body: `companyId`, `branchId`, `vehicleId`, `receivingEmployeeId`, `serviceRequesterPartnerId` uuid required; `origin` discriminated union `{kind:'appointment', appointmentId}` XOR `{kind:'walk_in', requesterPartnerId?, note? 1-500}`; optional `odometerReadingId`, `fuelLevelId`, `evSocPercent` 0-100. 201 `{receptionVisitId, displayNumber, receptionStatus:'opened', origin, recordVersion:1}` + `ETag:"1"`. Appointment origin: scope+vehicle MUST equal the appointment's (422 `incoherent_reference`); only `confirmed` checks in (409 `ERR-TRN-001`); one open visit per vehicle and one consumption per origin both answer the SAME 409 `ERR-RES-002`. Outbox `vehicle.checked-in`. The fuel picker was blocked by G-CAT-REC at the source head and reads `rec.catalogue-fuel-level-list` today; the receiving-employee field is still blocked by G-EMP, which no remediation has closed.   |
| B2      | Party roles                                                                                                                      | `rec.reception-party-role`                                                                                                                                                                 | POST `.../party-roles`; `rec.reception.party.manage`                                                                                                                                                                                                | **READY** (`{partnerId, relationshipRole enum(7), assignmentSource?, supersede?}`; superseding a same-transaction role answers 422 `out_of_range`); read-back of current roles was MISSING at the source head and is `rec.reception-party-role-list` since R1.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| B3      | Authorization decision                                                                                                           | `rec.reception-authorization`                                                                                                                                                              | POST `.../authorizations`; `rec.reception.authorization.verify` (high risk, approval audit class)                                                                                                                                                   | **READY.** `authorizingRole` enum(4) — `vehicle_user` and `payer` are excluded; role-not-held refusal is the same non-disclosing 409 `ERR-TRN-001` as the authority guard (anti-probing, `domain/reception.ts:275-281`); a standing `declined` blocks approve AND convert until the same party approves later. Standing-decision read-back was MISSING at the source head and is `rec.reception-authorization-list` since R1.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| B4      | Condition evidence — 8-kind union: complaint, inspection, condition_item, damage_map, damage_mark, contents, warning_light, leak | `rec.reception-condition-evidence`                                                                                                                                                         | POST `.../condition-evidence` (`condition-evidence/route.ts:164-188`); `rec.reception.evidence.manage` **plus `iam.sensitive.view` for `complaint` and `contents`** (WF-27: the application check passes, the DATABASE refuses — 403 at write time) | **READY.** All 8 writes READY; read-back was MISSING at the source head and is `rec.reception-condition-evidence-list` since R1. Complaint text 1-4000 restricted. `condition_item` and `damage_mark` vocabularies DIFFER (wear/missing_part/malfunction versus chip/rust/missing) and must not be conflated. Coordinates are 0-1 fractions. `damage_map` needs `documentId` + `documentVersionId`. `warning_light` has a READ route since R3 (`rec.catalogue-warning-light-code-list`) and a MANAGEMENT contract since PR #227 (`rec.catalogue-warning-light-code-create`/`-update`/`-status-set`, `rec.catalogue.manage`), and remains unusable in practice: the table ships no rows, no seed adds one, and nothing in the product reaches the writes. RMC-11 named two things at once — a missing contract and an unpopulated table — and only the first is answered. Who may administer the catalogue is `P1-28-OD-001`. |
| B5      | Signature capture                                                                                                                | `rec.reception-signature`                                                                                                                                                                  | POST `.../signatures`; `rec.reception.signature.manage`                                                                                                                                                                                             | **PARTIAL** — write READY but requires an attachment document (`signatureDocumentId` + `signatureDocumentVersionId` REQUIRED; a `pending` version satisfies the guard), and the media platform is dead-ended (G-MEDIA). Never the drawn bytes. **Read-back does not exist and R1 did not add one** — the six reception GETs do not list signatures, and `rec.signatures` is INSERT/SELECT at the table with no route. This is the one reception read-back gap R1 left open, deliberately (the plan’s arm-E narrowing).                                                                                                                                                                                                                                                                                                                                                                                                       |
| B6      | Refusal record                                                                                                                   | `rec.reception-refusal`                                                                                                                                                                    | POST `.../refusals`; `rec.reception.signature.manage` (same code as B5)                                                                                                                                                                             | **READY.** `refusalType` enum(5); type `authorization` REQUIRES `refusingPartnerId` (422) and the partner must hold an active authorizing role (409 `ERR-TRN-001`) — it becomes a standing block on approve/convert. Never changes `receptionStatus`, and it is NOT the `rec.reception-refuse` exit added by R5 — the two are different operations with the same English word. Refusal-reason catalogue read is `rec.catalogue-refusal-reason-list` since R3; the table itself still ships zero rows.                                                                                                                                                                                                                                                                                                                                                                                                                        |
| B7      | Approve reception                                                                                                                | `rec.reception-approve`                                                                                                                                                                    | POST `.../approve`; `rec.reception.approve` (high, approval audit class)                                                                                                                                                                            | **READY.** Empty body. `If-Match` REQUIRED; at the source head the only RV source was a prior write in the same session (G-REC-READ / INT-015), and since R1 it is `rec.reception-detail`, which is what makes a resumed visit approvable. From `opened` it walks TWO edges in one transaction (`['inspecting','authorized']`), so the returned RV = sent+2. Re-run on an authorized visit with a NEW key answers 409 `ERR-TRN-001` — not idempotent success. The single-unbroken-session limitation the Owner register named a P1-28 readiness blocker (`owner-workflow-requirements.md:182-197`) is LIFTED by R1: a visit can be resumed from its id.                                                                                                                                                                                                                                                                      |
| B8      | Convert to work order                                                                                                            | `rec.reception-convert-to-work-order`                                                                                                                                                      | POST `.../convert-to-work-order`; `rec.reception.convert` (NOT a `wo.*` code — WF-12); result display reads `wo.work-order-detail` under `wo.work_order.read`                                                                                       | **READY**, with the same If-Match source as B7 and the same R1 resolution. 200 `{receptionVisitId, workOrderId, displayNumber, state (opaque catalogue code, NOT an enum), alreadyConverted}` — **NO ETag** (`convert-to-work-order/route.ts:73-83`). Replay on a converted visit answers **200 `alreadyConverted:true` = success**. Deliberately no outbox event. There is no `POST /work-orders` anywhere.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| B9-B10  | Reception list / queue / resume; reception detail (status, RV, origin, roles, standing authorizations)                           | reads: `rec.reception-list`, `rec.reception-detail`, `rec.reception-party-role-list`, `rec.reception-authorization-list`, `rec.reception-condition-evidence-list`, `rec.reception-history` | GET `/api/v1/receptions` plus five per-visit GETs; `rec.reception.read` (seed `04_iam_permission_catalog.sql:184`)                                                                                                                                  | **CLOSED by R1 (PR #220) — six GETs and the seed.** MISSING at the source head, where `rec.reception.read` existed nowhere; that is why Wave G was sequenced FULLY BLOCKED. FE-007 resume, FE-020 and FE-021 consume them today, and `rec.reception-detail` is the If-Match source for approve and convert (INT-010/-011/-015). Signatures are the one read-back R1 deliberately did not add — see B5.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| B11     | Close-without-work / refuse a visit                                                                                              | `rec.reception-close-without-work`, `rec.reception-refuse`                                                                                                                                 | POST `.../close-without-work` and `.../refuse`; `rec.reception.close` (high — seed `04_iam_permission_catalog.sql:192`)                                                                                                                             | **CLOSED by R5 (PR #220).** At the source head both terminal states were unreachable — the only `setStatus` call sites were approve and convert — and an abandoned visit permanently blocked its vehicle through `uq_reception_visits_open_vehicle`, the INT-013 sibling. Both commands take a mandatory reason, REQUIRE `If-Match`, write through `setStatusWithReason` (`reception-service.ts:477-506`), emit no outbox event, and release the open-visit index. `rec.reception-refuse` is the terminal EXIT and is NOT `rec.reception-refusal` (B6), which appends evidence and changes no status.                                                                                                                                                                                                                                                                                                                        |
| B12-B13 | Visit reasons; fuel levels, warning-light codes, refusal reasons                                                                 | reads: `rec.catalogue-visit-reason-list`, `rec.catalogue-fuel-level-list`, `rec.catalogue-warning-light-code-list`, `rec.catalogue-refusal-reason-list`                                    | GET `/api/v1/reception-catalogue/*`; tables `20260721095000:43,102,161,220`                                                                                                                                                                         | **READ gap CLOSED by R3 (PR #220); WRITE gap CLOSED by PR #227; the EMPTINESS is neither closed nor a gap of the same kind.** All four tables ship **no rows at all**. Twelve management writes and four management projections now exist behind `rec.catalogue.manage`, and they changed nothing an operator can see: no screen in this product reaches them, and no seed grants the code to any role. FE-014, FE-015 and FE-019 therefore render a working picker with nothing in it and say so on screen — never a disabled control, never an invented level, code or reason. What population now waits on is a decision about WHO administers these catalogues and THROUGH WHICH SURFACE — `P1-28-OD-001`. The no-fake-data policy forbids a seed and is not the blocker.                                                                                                                                                |

### Table C — Cross-domain reads and writes the wizard composes

| #   | capability                                          | op(s)                                                                                                                                                              | status                                                                                                                                                                                                                                                                                                     |
| --- | --------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| C1  | Customer search                                     | `crm.customer-search` — GET `/customers` (`customers/route.ts:43-54`), `crm.customer.read`                                                                         | **PARTIAL** — no phone/contact-point query field exists (G-CRM-PHONE, allow-list at `customers/route.ts:30-41`); a desk cannot look up a walk-in by phone. Smallest fix: R7 (P1-16).                                                                                                                       |
| C2  | Customer detail                                     | `crm.customer-read` — GET `/customers/{customerId}` + ETag                                                                                                         | **READY.**                                                                                                                                                                                                                                                                                                 |
| C3  | Customer create (walk-in)                           | `crm.individual-create`, `crm.company-create`                                                                                                                      | **READY** — P1-27 screens exist; P1-28 composes `@/components/party` and `@/lib/customers`, never `features/crm` internals.                                                                                                                                                                                |
| C4  | Customer to vehicles                                | `crm.vehicle-link` POST plus `crm.customer-vehicle-list` GET, both on `/customers/{customerId}/vehicles` (`customers/[customerId]/vehicles/route.ts:40` and `:90`) | **CLOSED by R4 (PR #221).** At the source head the POST was the ONLY export of that path and the relationship was write-only platform-wide (INT-012); `GET /vehicles` still has no `customerId` filter, which is why the fix went on the customer path rather than the vehicle one. FE-008 reads it today. |
| C5  | Vehicle search                                      | `veh.vehicle-search` — GET `/vehicles` (filters vin/plate/vehicleNumber/lifecycleStatus/powertrainCategory only; hits carry `hasVin`, never the VIN)               | **READY** — the vehicle-first fallback for C4.                                                                                                                                                                                                                                                             |
| C6  | Vehicle detail and fan-outs                         | `veh.vehicle-read` + five list ops including `veh.vehicle-relationship-list` (partner names included)                                                              | **READY.** Detail carries RV/ETag and `mergedIntoId` (merged is not 404).                                                                                                                                                                                                                                  |
| C7  | Vehicle create + catalogue pickers                  | `veh.vehicle-create` + four catalogue list ops                                                                                                                     | **READY** — lands `draft` always; all descriptive fields optional-nullable.                                                                                                                                                                                                                                |
| C8  | Odometer record (pre-check-in)                      | `veh.vehicle-odometer-record`, `veh.vehicle-odometer-history` — `/vehicles/{vehicleId}/odometer-readings`                                                          | **READY** with the INS-19 trap: the write takes `value` as a JSON **number**, the read returns a decimal **string** — no arithmetic across the divergence. No `road_test` capture method exists (INS-06).                                                                                                  |
| C9  | Receiving-employee / inspector / witness resolution | `iam.user-list`, `iam.user-detail` — GET `/iam/users` (`iam/users/route.ts:23-42`), `iam.user.read`                                                                | **PARTIAL (G-EMP)** — a picker is technically fillable, but `receiving_employee_id` has no FK and no defined referent (`20260721097000:62`); nothing defines an `iam.user` id as a legal value or resolves a stored id to a name later. Owner register question A (R6). UI shows names, never UUIDs.       |
| C10 | Branch/company labels                               | `iam.branch-settings-read` and the company equivalent                                                                                                              | **PARTIAL** — no branches/companies LIST operation; resolve one settings-read per session id (the P1-27 administration pattern).                                                                                                                                                                           |
| C11 | Session bootstrap                                   | `iam.auth-session` — GET `/auth/session`                                                                                                                           | **READY.** `{userId, tenantId, email, displayName, companyIds, branchIds, permissions[]}`; empty scope arrays mean unrestricted-in-tenant.                                                                                                                                                                 |

### Table D — Documents and media

| #   | capability                                                                                                                                                                                                                                                                                                                                                                                            | status                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| --- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| D1  | Attachment chain: `shared.attachment-upload-authorize`, `shared.attachment-version-register`, `shared.attachment-link-create`, `shared.document-read`, `shared.attachment-download-authorize` — all `shared.document.manage`; the link allow-list INCLUDES `apt.appointments` and `rec.reception_visits`; purposes include `inspection_media`, `signature`, `evidence` (`attachment-policy.ts:42-62`) | **PARTIAL, dead-ended in practice:** contracts complete, but no object store (`STORAGE_PROVIDER` defaults `unconfigured`) and no scanner, so no version can reach `accepted` and downloads can NEVER be authorized (`catalog.ts:266-273`). Honest best state: "registered, pending, never downloadable" (`reception-media-checklist.md:208-210`). Any FE capture UI is gate-banned (`no-upload-path`, `no-invented-media-limit` — `scripts/ci/check-p1-27-frontend.mjs:876-895,919-939`) while `P1-OD-025` is open. |
| D2  | Documents-of-this-reception/appointment list                                                                                                                                                                                                                                                                                                                                                          | **MISSING (G-MEDIA).** Vehicles have `GET /vehicles/{id}/documents`; receptions and appointments have nothing. Blocks FE-012/017 read-back. Smallest fix: R8, deferred behind `P1-OD-025`.                                                                                                                                                                                                                                                                                                                          |
| D3  | Document-category enumeration                                                                                                                                                                                                                                                                                                                                                                         | **MISSING (G-MEDIA).** No route serves `shared.document_categories`. Blocks any upload picker. Same deferral.                                                                                                                                                                                                                                                                                                                                                                                                       |
| D4  | Owner media requirement (7 exterior + dashboard + VIN photos)                                                                                                                                                                                                                                                                                                                                         | **BLOCKED:** Owner rows 12-14 explicitly Blocked (`INT-093/094/095`, `owner-workflow-requirements.md:160-162`); only the COUNT of seven is fixed (`reception-media-checklist.md:102-105`); whether missing media blocks approval is an unmade Owner decision (`:525-537`).                                                                                                                                                                                                                                          |

### Table E — Decision-gated / adjacent

| #   | capability                                             | status                                                                                                                                                                                                                                                                                                                         |
| --- | ------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| E1  | Road test (conditional, 5min-1h, unsafe outcome)       | **MISSING + decision-gated (WF-10, INT-054)** — road test exists nowhere in the platform; the only fixture that mentions one is test-only and citing it would be a fabrication (`end-to-end-workshop-workflow.md:440-462`; `inspection-and-diagnostics.md:255-373`). Deliberately NOT a canonical task; see plan §7.           |
| E2  | Lift inspection                                        | **PARTIAL** — expressible only as `kind:'inspection'` plus `condition_item`s; no type column, so indistinguishable from a walk-around (RMC-18); `inspection_status` is created `in_progress` and no operation ever completes it (RMC-12). FE-011 may build the capture but must not label anything "lift inspection complete". |
| E3  | Reception final review                                 | **PARTIAL (INS-10, owned P1-18).** The SUMMARY is buildable since R1 closed G-REC-READ, and FE-020 ships it over those reads. A review RECORD still has no home anywhere in the schema (INS-17), so nothing on that screen may be labelled as a stored review.                                                                 |
| E4  | Concern-to-diagnosis link                              | **MISSING (WF-05/INS-16)** — no column, no operation. The two mandatory labels ("Customer-reported concern" / "Not yet technically verified", `inspection-and-diagnostics.md:548-563`) CAN be rendered at capture time, but the label-removal condition is unimplementable — the second label is permanent in P1-28.           |
| E5  | Work-order screens, departments, technician assignment | **OUT OF SCOPE** — P1-29 (`owner-workflow-requirements.md:220-239`); P1-28 owns only the conversion action and result display (B8). Quotable boundary: plan §6.                                                                                                                                                                |

---

## 2. Gap register

Every row states its status AT THIS HEAD. A gap that has been closed keeps its
row, because the row is what explains why the build order in §3 looks the way
it does — but it does not go on saying "missing" in the present tense.

| gap id        | status at this head                           | what was missing                                                                                                                          | blocked                                                                                  | fix                                                                                                                                        |
| ------------- | --------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------ |
| `G-APT-READ`  | **CLOSED** (R2, PR #220)                      | Any appointment read (list, detail, RV source)                                                                                            | FE-001; If-Match sourcing of FE-003/004/005                                              | R2                                                                                                                                         |
| `G-CAT-APT`   | **CLOSED** (R3, PR #220)                      | Appointment-type, source-channel, cancellation-reason reads                                                                               | FE-002 (own dropdowns); FE-004 (absolute — reason mandatory)                             | R3                                                                                                                                         |
| `G-REC-READ`  | **CLOSED** (R1, PR #220) except signatures    | Any reception read; `rec.reception.read` existed nowhere                                                                                  | FE-020/021; resume path of FE-007; If-Match source of FE-020/022; all evidence read-back | R1; the signature read-back was deliberately out of arm E and has no owner yet                                                             |
| `G-CAT-REC`   | **READ closed** (R3); **WRITE closed** (#227) | Fuel-level, warning-light, refusal-reason and visit-reason reads over tables that ship no rows, and no way to add one                     | FE-014, FE-015, FE-019 — each renders an empty picker and says so                        | R3 closed the read and PR #227 the write; the tables are still empty and who may fill them is `P1-28-OD-001`, a decision rather than a gap |
| `G-REC-CLOSE` | **CLOSED** (R5, PR #220)                      | Terminal visit states unreachable (`closed_without_work`, `refused`)                                                                      | queue actions; product integrity from day one of any reception usage                     | R5                                                                                                                                         |
| `G-EMP`       | **OPEN**                                      | `receiving_employee_id` has no FK and no defined referent                                                                                 | the receiving-employee field of FE-007                                                   | R6 (decision + schema)                                                                                                                     |
| `G-MEDIA`     | **OPEN**                                      | Object store, scanner, per-reception document list, category read                                                                         | FE-017; signature-image half of FE-018; template half of FE-012                          | `P1-OD-025` first, then R8                                                                                                                 |
| `G-CUST-VEH`  | **CLOSED** (R4, PR #221)                      | Customer-to-vehicles read (write-only platform-wide, INT-012)                                                                             | customer-first vehicle pick in FE-002/FE-008                                             | R4                                                                                                                                         |
| `G-CRM-PHONE` | **OPEN**                                      | Customer search by contact point                                                                                                          | walk-in desk lookup in FE-006                                                            | R7                                                                                                                                         |
| `G-PERM`      | **CLOSED** (inside R1/R2)                     | `apt.appointment.read` / `rec.reception.read` unseeded; the navigation entries name both codes and are live (`navigation.ts:163`, `:172`) | navigation `status:'available'` for both trees                                           | seeded inside R1/R2 (seed changes are P1-18-owned)                                                                                         |

---

## 3. Build order

The verdicts below were written at the source head and are kept as the record
of WHY the phase was sequenced this way. Waves A–H are all built at this head;
the blockers named in C, F and G were cleared by R1–R5 rather than worked
around, and H is still blocked because `P1-OD-025` is still open.

| wave                              | content                                                                                                                                                                | verdict                                                                                                                                                                                                                                                                 |
| --------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| A — Foundations                   | This document, `canonical-plan.md`, the task matrix + gate, contract modules written from route source, SEC-004 reachability gate, QA-001 harness                      | **START IMMEDIATELY.** Zero backend dependencies. The register and gates exist before the first screen.                                                                                                                                                                 |
| B — Walk-in composition           | FE-006 with two stated degradations: vehicle lookup is vehicle-first only (G-CUST-VEH) and no phone search (G-CRM-PHONE) — both documented on-screen, not papered over | **START IMMEDIATELY.**                                                                                                                                                                                                                                                  |
| C — Appointment writes            | FE-002/003/004/005 screens; FE-001 shell                                                                                                                               | **BLOCKED** on G-CAT-APT (mandatory pickers) and G-APT-READ + G-PERM (If-Match sourcing; navigation flip). Adapters and contract tests CAN be written now — the writes are frozen.                                                                                      |
| D — Check-in wizard core          | FE-007, FE-008, FE-009                                                                                                                                                 | **PARTIALLY BLOCKED:** the appointment-origin path needs Wave C's read; the walk-in-origin path needs only a G-EMP disposition (fuel is nullable and may ship without G-CAT-REC). A walk-in vertical slice is buildable single-session.                                 |
| E — Evidence capture              | FE-010, FE-011, FE-013, FE-014 (minus fuel picker), FE-016, FE-019 (minus reason picker), FE-018 (UI without media)                                                    | **BUILDABLE single-session** — visit id + RV held from Wave D's create response; every read-back and all resume behaviour blocked on G-REC-READ. FE-015 excluded (RMC-11).                                                                                              |
| F — Approve + convert             | FE-020, FE-022                                                                                                                                                         | **Technically buildable single-session** (create RV 1, approve If-Match 1, convert If-Match 3) but **must not close as done**: resumability is an Owner-named readiness blocker. Ship behind R1, or ship with the single-session limitation stated in the phase record. |
| G — Queues, detail, resume, print | FE-001 full, FE-020 queue, FE-021, resume in FE-007                                                                                                                    | **FULLY BLOCKED** on G-APT-READ + G-REC-READ (+ G-REC-CLOSE for queue actions). Nothing here can start until R1/R2 land.                                                                                                                                                |
| H — Media                         | FE-017, template half of FE-012, signature-image half of FE-018                                                                                                        | **HARD BLOCKED:** `P1-OD-025`, INT-093/094/095, no object store, no scanner, and two CI gates that refuse the code. Do not build; ship the named-open-decision notice per `reception-media-checklist.md:508-517`.                                                       |

SEC-001/002/003, QA-002/003/004, DO-002 and DOC-002 ride the waves they
police; QA-005 is end-of-phase.

---

## 4. Backend remediation candidates

Prove, classify, smallest fix; every one is owned by a Backend phase and none
may be built inside the P1-28 Frontend branch (plan §4).

| id  | remediation                                                                                                                                                                                                                                                                                       | class                                                                                             | owner                                                                        | proof                                                                                                                                                                                                                                                                                                                                                                                                         |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| R1  | Execute `docs/phase-1/phase-1-27/reception-read-surface-plan.md` — six GETs (`rec.reception-list`, `rec.reception-detail`, `rec.reception-party-role-list`, `rec.reception-authorization-list`, `rec.reception-condition-evidence-list`, `rec.reception-history`) + the `rec.reception.read` seed | **EXECUTED 2026-08-12 (#220)**                                                                    | P1-18                                                                        | Plan committed, verified, then executed — the plan’s status line records commit `83c055d` and the deliberate arm-E narrowing. Covers INT-010/-011/-015/-016/-017/-021. The single highest-leverage change in the phase.                                                                                                                                                                                       |
| R2  | Appointment read surface: `apt.appointment-list` (branch + date-range + status), `apt.appointment-detail`, + the `apt.appointment.read` seed                                                                                                                                                      | **EXECUTED 2026-08-12 (#220)**                                                                    | P1-18                                                                        | The total absence of any read was proven by grep and by the seed's own prose at the source head; the seed now carries `apt.appointment.read` and the paragraph explaining the split (`04_iam_permission_catalog.sql:141-148`). The read-model shape was specified in advance (`p1-18-p1-28-boundaries.md:29-43` — GiST-indexed calendar).                                                                     |
| R3  | Seven catalogue reads: `apt.appointment-type-list`, `apt.source-channel-list`, `apt.cancellation-reason-list`, `rec.visit-reason-list`, `rec.fuel-level-list`, `rec.warning-light-code-list`, `rec.refusal-reason-list` — `low-risk-metadata` class                                               | **EXECUTED 2026-08-12 (#220)** (registered ids took the `apt.catalogue-*`/`rec.catalogue-*` form) | P1-18                                                                        | Write bodies REQUIRE the uuids; tables exist with zero routes (migrations `20260721091000` / `20260721095000`). Reads do not populate empty tables — that is a second, separately-classified gap.                                                                                                                                                                                                             |
| R4  | `crm.customer-vehicle-list` — GET `/customers/{customerId}/vehicles`                                                                                                                                                                                                                              | **EXECUTED 2026-08-12 (#221)**                                                                    | P1-16                                                                        | INT-012 closed — adversarially verified (tenant boundary, four-way 404 uniformity, bidirectional cursor refusal);; Owner row 3 Blocked; the shipped FE's own comment (`CustomerProfileScreen.tsx:120-122`). Customer-first mirror of `veh.vehicle-relationship-list`.                                                                                                                                         |
| R5  | Close-without-work / refuse-visit commands                                                                                                                                                                                                                                                        | **EXECUTED 2026-08-12 (#220)**                                                                    | P1-18                                                                        | Terminal states were provably unreachable at the source head — the only two `setStatus` call sites were approve and convert — and an abandoned visit permanently blocked its vehicle (`uq_reception_visits_open_vehicle`), the INT-013 defect shape reproduced and closed there. Closed here by a third writer, `setStatusWithReason` (`reception-service.ts:477-506`), reached only by the two new commands. |
| R6  | `receivingEmployeeId` referent decision (+ FK or a documented iam-user referent + a name-resolution path via the existing `nameActors` pattern)                                                                                                                                                   | MISSING (decision + schema)                                                                       | P1-18 / Owner register question A (`owner-workflow-requirements.md:199-216`) | Column NOT NULL with no FK (`20260721097000:62`); the employee master never shipped (`20260718090000:166-196`).                                                                                                                                                                                                                                                                                               |
| R7  | Customer search by contact point (phone)                                                                                                                                                                                                                                                          | PARTIAL, then fix                                                                                 | P1-16                                                                        | The allow-list demonstrably excludes contact channels (`customers/route.ts:30-41`).                                                                                                                                                                                                                                                                                                                           |
| R8  | Documents-for-reception list + document-categories read                                                                                                                                                                                                                                           | MISSING                                                                                           | P1-15/P1-18, deferred behind `P1-OD-025`                                     | Linking to `rec.reception_visits` is legal but listing from it is not; no route serves categories. No point landing before the media platform exists.                                                                                                                                                                                                                                                         |

---

## 5. The five riskiest contract facts a UI developer would guess wrong

1. **"Confirm" does not exist.** Confirmation is a side effect of
   `apt.appointment-reschedule`; `pending_confirmation` is reachable by NO
   operation; and any window violation on reschedule reports path
   `body.confirmedFrom` even when `confirmedTo` is the bad field
   (`appointment-service.ts:184-207`). Every screen must label the control as
   what it truthfully calls (WFP-04 exit criterion).

2. **The `recordVersion` a version-guarded write demands has TWO sources, and
   which one you get depends on the head.** At the source head there was no GET
   anywhere in apt/rec, so the only source was a previous write's response in
   the same session: create's `ETag:"1"` was load-bearing, and closing the
   browser left the vehicle in custody with no path forward
   (`owner-workflow-requirements.md:182-197`). **R1 and R2 changed that** —
   `rec.reception-detail` and `apt.appointment-detail` publish the version, and
   that is what makes a resumed visit approvable and a bookmarked appointment
   reschedulable. The discipline survives the change: a command response is
   still the freshest truth the client has, and approve returns RV = sent+2, so
   a screen that re-reads after every write and never caches the response is
   also wrong. Cache the RV a command returns; re-read when you arrive by URL.

3. **Three different replay shapes.** Idempotent replay of ANY command returns
   **200** (even create, whose first answer was 201) with **no ETag**, body
   only (`route-handler.ts:369-401`); re-running approve on an authorized
   visit with a NEW key answers 409 `ERR-TRN-001` — and re-reading does NOT
   fix `ERR-TRN-001`; re-running convert on a converted visit answers **200
   with `alreadyConverted:true`, which is success**. Approve also returns
   RV = sent+2 when it walks two edges; convert returns NO ETag at all.

4. **WF-27: complaint and contents writes pass the application's permission
   check with `rec.reception.evidence.manage` and are then refused by the
   DATABASE without `iam.sensitive.view`** — a 403 at write time on a form the
   operator was allowed to open. The adapter must map this honestly, and
   SEC-001's role mapping must grant the pair together.

5. **Check-in couples the two domains invisibly:** `POST /receptions` with
   appointment origin marks the appointment `checked_in` in the same
   transaction with no version predicate; body
   `companyId`/`branchId`/`vehicleId` must EQUAL the appointment's own or the
   request answers 422 `incoherent_reference`; only a `confirmed` appointment
   can check in (409 `ERR-TRN-001`); and one-open-visit-per-vehicle and
   one-consumption-per-origin both surface as the SAME 409 `ERR-RES-002`
   message.

Honourable mentions: odometer `value` is a JSON number in and a decimal string
out (INS-19); the `condition_item` and `damage_mark` vocabularies differ by
three tokens and must not be conflated; 400 and 422 both map to the web
client's `validation` kind; `ERR-RES-001` 404 deliberately hides cross-tenant
existence; scope keys in query strings throw client-side
(`read-operation.ts:89-133`).
