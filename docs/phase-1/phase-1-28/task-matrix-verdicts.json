{
  "FE-001": {
    "BACKEND_READ_CONTRACTS": "MISSING (G-APT-READ). Binds `apt.appointment-list` and `apt.appointment-detail`, the operations remediation R2 will create (contract-archaeology.md Table A rows A5/A6). No `apt.*` read exists at `d64f259` — the seed's own prose says so (`04_iam_permission_catalog.sql:135-136`).",
    "BACKEND_WRITE_CONTRACTS": "N/A with rationale: a calendar and scheduling view performs no mutation of its own; the writes it links to belong to FE-002 through FE-005.",
    "REQUEST_SCHEMA": "Planned under R2, not yet a contract: branch + date-range + status filters over `Page<T>` per the recorded read-model expectations (`docs/phase-1/phase-1-8/p1-18-p1-28-boundaries.md:29-43`, GiST-indexed calendar).",
    "RESPONSE_SCHEMA": "Planned under R2, not yet a contract: list items carrying ids, requested/confirmed windows, `lifecycleStatus` and `recordVersion`; detail with ETag. The lifecycle vocabulary the view renders is fixed today: requested / pending_confirmation / confirmed / checked_in / cancelled / no_show, with `pending_confirmation` reachable by NO operation (render-only).",
    "REQUIRED_PERMISSIONS": "`apt.appointment.read` — UNSEEDED at `d64f259` (G-PERM; `config/navigation.ts:120-124` already names it). The navigation `status` flip to 'available' is forbidden until the seed lands.",
    "OWNER_REQUIREMENT_MAPPING": "Journey step S-1, today's arrivals (`end-to-end-workshop-workflow.md` §5, steps 121-127 block). Decision WF-01 — whether arrival is a business record at all — is undecided and gates the queue half of this view. Owner appointment rows OR-1..5 (`owner-workflow-requirements.md:147-180`).",
    "PROTECTED_REPROOF": "PENDING CANDIDATE OBSERVATION — the authenticated browser tier must be observed at the phase's closing candidate for every Frontend task; on day one no candidate exists and no screen has been built.",
    "FINAL_VERDICT": "PARTIAL",
    "VERDICT_RATIONALE": "Day-one register row. Contracts bound from the archaeology; the screen is not begun and CANNOT be begun — fully blocked on G-APT-READ + G-PERM (remediation R2, owned by P1-18). Wave G of the build order."
  },
  "FE-002": {
    "BACKEND_READ_CONTRACTS": "Pickers: `apt.appointment-type-list` and `apt.source-channel-list` are MISSING (G-CAT-APT, remediation R3) — the tables exist with zero routes (`20260721091000_apt_configuration_catalogs.sql:46,105`). Customer/vehicle resolution rides FE-006/FE-008 contracts (`crm.customer-search`, `veh.vehicle-search`, `veh.vehicle-read`).",
    "BACKEND_WRITE_CONTRACTS": "`apt.appointment-create` — POST /api/v1/appointments (`appointments/route.ts:49-62`). READY and frozen at `d64f259`.",
    "REQUEST_SCHEMA": "Strict: `companyId`, `branchId`, `vehicleId`, `requesterPartnerId`, `appointmentTypeId` all uuid required; `sourceChannelId` uuid nullable default null; `requestedFrom`/`requestedTo` strings 1-64 that must parse with an explicit UTC offset, To greater than From (`domain/appointment.ts:119-140`). No notes/reason field exists. Idempotency-Key mandatory 8-200 chars.",
    "RESPONSE_SCHEMA": "201 `{appointmentId, displayNumber: string|null, lifecycleStatus:'requested', recordVersion:1}` + `ETag:\"1\"`. Echoes nothing else — the ETag is load-bearing because no re-read exists (riskiest-fact 2). Idempotent replay answers 200 with NO ETag (`route-handler.ts:369-401`).",
    "REQUIRED_PERMISSIONS": "`apt.appointment.manage` (seed `04_iam_permission_catalog.sql:137`), branch scope, target resolved from body (`route.ts:91`). Plus `crm.customer.read` / `veh.vehicle.read` for the resolution reads.",
    "OWNER_REQUIREMENT_MAPPING": "Owner rows OR-1..5 (`owner-workflow-requirements.md:147-180`); journey steps S-1/S-4. OR-3 (customer's vehicles) is Blocked on INT-012 / G-CUST-VEH — the customer-first vehicle pick degrades to vehicle-first search until R4.",
    "PROTECTED_REPROOF": "PENDING CANDIDATE OBSERVATION — the authenticated browser tier must be observed at the phase's closing candidate; no candidate exists on day one.",
    "FINAL_VERDICT": "PARTIAL",
    "VERDICT_RATIONALE": "The write contract is READY and the adapter plus contract tests can be written immediately (Wave C allows adapters now). The SCREEN cannot ship: its own mandatory dropdowns have no read (G-CAT-APT) and the customer-first vehicle pick is write-only (G-CUST-VEH)."
  },
  "FE-003": {
    "BACKEND_READ_CONTRACTS": "MISSING (G-APT-READ): `apt.appointment-detail [MISSING R2]` is what would display the appointment being confirmed and source its `recordVersion`. Until R2, the only RV source is a prior write response cached in-session.",
    "BACKEND_WRITE_CONTRACTS": "`apt.appointment-reschedule` — POST /api/v1/appointments/{appointmentId}/reschedule (`reschedule/route.ts:43-57`). Confirmation and reschedule are ONE operation (WF-02); `pending_confirmation` is reachable by no operation.",
    "REQUEST_SCHEMA": "Strict `{confirmedFrom, confirmedTo}` — same explicit-offset and ordering rules as create. TRAP: any window violation reports path `body.confirmedFrom` even when `confirmedTo` is the offending field (`appointment-service.ts:184`). `If-Match` REQUIRED: missing answers 428 `ERR-CON-002`, stale answers 409 `ERR-CON-001`.",
    "RESPONSE_SCHEMA": "200 `{appointmentId, changeKind:'rescheduled', lifecycleStatus:'confirmed', recordVersion+1}` + ETag.",
    "REQUIRED_PERMISSIONS": "`apt.appointment.manage`, re-authorized against the locked row (P1-18-A-01).",
    "OWNER_REQUIREMENT_MAPPING": "Journey step S-4; WFP-04 entry (b) and its exit criterion: the UI must never label a control 'Confirm' unless it truthfully calls this operation (`frontend-implementation-program.md:313-314`).",
    "PROTECTED_REPROOF": "PENDING CANDIDATE OBSERVATION — the authenticated browser tier must be observed at the phase's closing candidate; no candidate exists on day one.",
    "FINAL_VERDICT": "PARTIAL",
    "VERDICT_RATIONALE": "Write contract READY; adapter and contract tests buildable now. The screen's If-Match sourcing and display of what is being confirmed are blocked on G-APT-READ (R2). Truthful-labelling obligation recorded in plan §10."
  },
  "FE-004": {
    "BACKEND_READ_CONTRACTS": "`apt.cancellation-reason-list [MISSING R3]` — an ABSOLUTE blocker: the reason is mandatory and catalogued (no free text), and the table has zero routes (`20260721091000:164`). Plus `apt.appointment-detail [MISSING R2]` for the RV source.",
    "BACKEND_WRITE_CONTRACTS": "`apt.appointment-cancel` — POST /api/v1/appointments/{appointmentId}/cancel (`cancel/route.ts:26-40`).",
    "REQUEST_SCHEMA": "Strict `{cancellationReasonId: uuid}` — mandatory; an unknown id answers 422 `unknown_reference`. `If-Match` REQUIRED. Idempotency-Key mandatory.",
    "RESPONSE_SCHEMA": "200 `{appointmentId, changeKind:'cancelled', lifecycleStatus:'cancelled', recordVersion}` + ETag. Audit `apt.appointment.cancelled` records previous status and reason in details.",
    "REQUIRED_PERMISSIONS": "`apt.appointment.lifecycle.manage` (seed `:142`) — a DIFFERENT code from create/reschedule's `apt.appointment.manage`; the permission gate must not conflate them.",
    "OWNER_REQUIREMENT_MAPPING": "Journey step S-4 lifecycle handling (`end-to-end-workshop-workflow.md` §5).",
    "PROTECTED_REPROOF": "PENDING CANDIDATE OBSERVATION — the authenticated browser tier must be observed at the phase's closing candidate; no candidate exists on day one.",
    "FINAL_VERDICT": "PARTIAL",
    "VERDICT_RATIONALE": "Write contract READY, but the dialog is unbuildable until G-CAT-APT closes (the mandatory reason picker has no read) and If-Match sourcing waits on G-APT-READ. Adapter and contract tests buildable now."
  },
  "FE-005": {
    "BACKEND_READ_CONTRACTS": "`apt.appointment-detail [MISSING R2]` for the RV source and for displaying which appointment is being marked.",
    "BACKEND_WRITE_CONTRACTS": "`apt.appointment-no-show` — POST /api/v1/appointments/{appointmentId}/no-show (`no-show/route.ts:33-47`).",
    "REQUEST_SCHEMA": "Body `{}` or null, strict. `If-Match` REQUIRED. Legal from `confirmed` ONLY — any other state answers 409 `ERR-TRN-001`. Idempotency-Key mandatory.",
    "RESPONSE_SCHEMA": "200 `{appointmentId, changeKind:'no_show_recorded', lifecycleStatus:'no_show', recordVersion}` + ETag.",
    "REQUIRED_PERMISSIONS": "`apt.appointment.lifecycle.manage` (seed `:142`).",
    "OWNER_REQUIREMENT_MAPPING": "Journey step S-4 (`end-to-end-workshop-workflow.md` §5).",
    "PROTECTED_REPROOF": "PENDING CANDIDATE OBSERVATION — the authenticated browser tier must be observed at the phase's closing candidate; no candidate exists on day one.",
    "FINAL_VERDICT": "PARTIAL",
    "VERDICT_RATIONALE": "Write contract READY; screen blocked on G-APT-READ for If-Match sourcing (Wave C). The 409 `ERR-TRN-001` state-refusal branch is a QA-002 obligation."
  },
  "FE-006": {
    "BACKEND_READ_CONTRACTS": "`crm.customer-search` (GET /customers — PARTIAL: no phone/contact-point query, G-CRM-PHONE, allow-list `customers/route.ts:30-41`), `crm.customer-read`, `veh.vehicle-search` (the vehicle-first fallback), `veh.vehicle-read`, plus the four vehicle catalogue list reads for creation pickers.",
    "BACKEND_WRITE_CONTRACTS": "`crm.individual-create`, `crm.company-create` (POST /customers/individuals, /customers/companies), `veh.vehicle-create` (lands `draft` always), `crm.vehicle-link` (POST /customers/{customerId}/vehicles — the ONLY export of that route, `vehicles/route.ts:43`).",
    "REQUEST_SCHEMA": "Per the P1-27 creation contracts (`features/crm/customers/creation-contract.ts`); vehicle link takes `{vehicleId, relationshipRole enum(7)}`. All idempotent commands carry Idempotency-Key.",
    "RESPONSE_SCHEMA": "201 per P1-27 contracts; `possibleDuplicates` arrives on the CREATION RESPONSE (no separate scan — P1-27 §7.1); link answers 201 with audit `crm.customer.vehicle_linked`.",
    "REQUIRED_PERMISSIONS": "`crm.customer.read`, `crm.customer.create`, `crm.customer.vehicle.manage`, `veh.vehicle.read`, `veh.vehicle.manage`.",
    "OWNER_REQUIREMENT_MAPPING": "Owner rows OR-1..5; journey step S-5. OR-3 is Blocked (INT-012): which vehicles belong to this customer cannot be read back — the shipped FE's own comment records it (`CustomerProfileScreen.tsx:120-122`).",
    "PROTECTED_REPROOF": "PENDING CANDIDATE OBSERVATION — the authenticated browser tier must be observed at the phase's closing candidate; no candidate exists on day one.",
    "FINAL_VERDICT": "PARTIAL",
    "VERDICT_RATIONALE": "Wave B — buildable IMMEDIATELY by composing P1-27 modules (`@/components/party`, `@/lib/customers`; never `features/crm` internals), with two degradations stated on-screen: vehicle lookup is vehicle-first only (G-CUST-VEH) and there is no phone search (G-CRM-PHONE)."
  },
  "FE-007": {
    "BACKEND_READ_CONTRACTS": "`iam.user-list` (GET /iam/users, `iam/users/route.ts:23-42`) for the receiving-employee picker — but G-EMP: `receiving_employee_id` has NO FK and no defined referent (`20260721097000:62`). Resume path: `rec.reception-list [MISSING R1]`, `rec.reception-detail [MISSING R1]`.",
    "BACKEND_WRITE_CONTRACTS": "`rec.reception-create` — POST /api/v1/receptions (`receptions/route.ts:37-98`).",
    "REQUEST_SCHEMA": "Strict: `companyId`, `branchId`, `vehicleId`, `receivingEmployeeId`, `serviceRequesterPartnerId` uuid required; `origin` discriminated union `{kind:'appointment', appointmentId}` XOR `{kind:'walk_in', requesterPartnerId?|null, note? 1-500|null}`; `odometerReadingId?|null`, `fuelLevelId?|null`, `evSocPercent? 0-100|null` (fractional accepted). Appointment origin: body scope+vehicle MUST equal the appointment's (422 `incoherent_reference` at `body.branchId`/`body.vehicleId`); only a `confirmed` appointment checks in (409 `ERR-TRN-001`).",
    "RESPONSE_SCHEMA": "201 `{receptionVisitId, displayNumber|null, receptionStatus:'opened', origin, recordVersion:1}` + `ETag:\"1\"`. One open visit per vehicle AND one consumption per origin both answer the SAME 409 `ERR-RES-002` message. Side effect: the appointment is marked `checked_in` in the same transaction with no version predicate (`appointment-repository.ts:197-213`). Outbox `vehicle.checked-in`.",
    "REQUIRED_PERMISSIONS": "`rec.reception.manage` (seed `:146-168`), branch scope from body; `iam.user.read` for the picker (its tenant-wide overload needs the SEC-001 disposition).",
    "OWNER_REQUIREMENT_MAPPING": "Owner rows OR-6..9; journey steps S-5/S-6. The receiving-employee referent is Owner register question A (`owner-workflow-requirements.md:199-216`) and the resumability mandate (`:182-197`) governs the resume path.",
    "PROTECTED_REPROOF": "PENDING CANDIDATE OBSERVATION — the authenticated browser tier must be observed at the phase's closing candidate; no candidate exists on day one.",
    "FINAL_VERDICT": "PARTIAL",
    "VERDICT_RATIONALE": "Wave D — the walk-in-origin vertical slice is buildable single-session once G-EMP receives a disposition; the appointment-origin path needs Wave C's read; the resume path is blocked on G-REC-READ (R1)."
  },
  "FE-008": {
    "BACKEND_READ_CONTRACTS": "`crm.customer-read` (detail + ETag), `veh.vehicle-read` (detail incl. `recordVersion`, `mergedIntoId` — merged is NOT 404), `veh.vehicle-relationship-list` (partner names included: `partnerName|null`, `partnerNumber|null`, `partnerType|null`). Customer-first direction MISSING: `crm.customer-vehicle-list [MISSING R4]` (G-CUST-VEH / INT-012 — write-only platform-wide).",
    "BACKEND_WRITE_CONTRACTS": "N/A with rationale: identity confirmation is a read-and-confirm step; any correction routes to FE-006's creation/link writes.",
    "REQUEST_SCHEMA": "Path uuid for the two details; cursor/limit for the relationship list. Scope keys in query strings THROW client-side (`read-operation.ts:89-133`) — the tenant is resolved server-side.",
    "RESPONSE_SCHEMA": "Customer detail `{id, displayNumber|null, displayName, partyType, lifecycleStatus, commercialStatus, recordVersion, ...}` + ETag; vehicle detail with RV/ETag; relationship page items with partner names.",
    "REQUIRED_PERMISSIONS": "`crm.customer.read`, `veh.vehicle.read`.",
    "OWNER_REQUIREMENT_MAPPING": "Owner rows OR-1..5 confirmation half; journey step S-5/S-6 ('which of their vehicles is this' — OR-3 Blocked on INT-012 until R4).",
    "PROTECTED_REPROOF": "PENDING CANDIDATE OBSERVATION — the authenticated browser tier must be observed at the phase's closing candidate; no candidate exists on day one.",
    "FINAL_VERDICT": "PARTIAL",
    "VERDICT_RATIONALE": "Reads for the vehicle-first direction are READY (Wave D); the customer-first direction is blocked on G-CUST-VEH (R4, owned P1-16). The degradation is stated, not papered over."
  },
  "FE-009": {
    "BACKEND_READ_CONTRACTS": "MISSING (G-REC-READ): `rec.reception-party-role-list [MISSING R1]` and `rec.reception-authorization-list [MISSING R1]` — current roles and standing decisions cannot be read back; capture is in-session only until R1.",
    "BACKEND_WRITE_CONTRACTS": "`rec.reception-party-role` (POST .../party-roles), `rec.reception-authorization` (POST .../authorizations).",
    "REQUEST_SCHEMA": "Party role: `{partnerId uuid, relationshipRole enum(7: service_requester/vehicle_owner/vehicle_user/payer/billing_party/approving_party/authorized_receiver), assignmentSource? <=120|null, supersede? bool}`; superseding a same-transaction role answers 422 `out_of_range`. Authorization: `{authorizingRole enum(4: approving_party/service_requester/vehicle_owner/authorized_receiver — vehicle_user and payer EXCLUDED), partnerId, decision:'approved'|'declined', channel? default in_person, authorizedScope? JSON|null, evidenceDocumentId?|null}`.",
    "RESPONSE_SCHEMA": "201 `{receptionVisitId, partyRoleId, relationshipRole, superseded}` / 201 `{receptionVisitId, authorizationId, decision}`. Role-not-held refusal is the same non-disclosing 409 `ERR-TRN-001` as the authority guard (anti-probing, `domain/reception.ts:275-281`). A standing `declined` blocks approve AND convert until the same party approves later.",
    "REQUIRED_PERMISSIONS": "`rec.reception.party.manage`; `rec.reception.authorization.verify` (high risk, approval audit class).",
    "OWNER_REQUIREMENT_MAPPING": "Journey step S-6 and the S-11 approval preconditions; the authorization roles are the Owner's narrow authorizing set.",
    "PROTECTED_REPROOF": "PENDING CANDIDATE OBSERVATION — the authenticated browser tier must be observed at the phase's closing candidate; no candidate exists on day one.",
    "FINAL_VERDICT": "PARTIAL",
    "VERDICT_RATIONALE": "Both writes READY — capture UI buildable in Wave D. The standing-decision consequences (a declined block) can be explained on-screen but not read back until G-REC-READ closes (R1)."
  },
  "FE-010": {
    "BACKEND_READ_CONTRACTS": "MISSING (G-REC-READ): no re-display of captured complaints until R1's `rec.reception-condition-evidence-list`.",
    "BACKEND_WRITE_CONTRACTS": "`rec.reception-condition-evidence` with `kind:'complaint'` (POST .../condition-evidence, `condition-evidence/route.ts:51-182`).",
    "REQUEST_SCHEMA": "Complaint text 1-4000, restricted narrative. Appendable in any non-terminal visit state. Idempotency-Key mandatory.",
    "RESPONSE_SCHEMA": "201 `{receptionVisitId, kind:'complaint', evidenceId}`. Audit `rec.reception.evidence_recorded` — the visit is the audit entity.",
    "REQUIRED_PERMISSIONS": "`rec.reception.evidence.manage` PLUS `iam.sensitive.view` — WF-27: the application check passes on the first alone and the DATABASE then refuses (403 at write time on a form the operator was allowed to open). SEC-002 owns surfacing this honestly; SEC-001 grants the pair together.",
    "OWNER_REQUIREMENT_MAPPING": "Owner rows OR-10 and OR-26; journey step S-7 reception half. The two mandatory labels — 'Customer-reported concern' / 'Not yet technically verified' (`inspection-and-diagnostics.md:548-563`) — render at capture time; the label-REMOVAL condition is unimplementable (WF-05/INS-16), so the second label is permanent in P1-28.",
    "PROTECTED_REPROOF": "PENDING CANDIDATE OBSERVATION — the authenticated browser tier must be observed at the phase's closing candidate; no candidate exists on day one.",
    "FINAL_VERDICT": "PARTIAL",
    "VERDICT_RATIONALE": "Write READY — Wave E, buildable single-session with the visit id and RV held from Wave D's create response. Read-back blocked on G-REC-READ. The WF-27 dual-permission trap is this task's sharpest edge."
  },
  "FE-011": {
    "BACKEND_READ_CONTRACTS": "MISSING (G-REC-READ) for all read-back until R1.",
    "BACKEND_WRITE_CONTRACTS": "`rec.reception-condition-evidence` with kinds `inspection`, `condition_item`, `leak`.",
    "REQUEST_SCHEMA": "Per-kind schemas. TRAP: the `condition_item` vocabulary (wear/missing_part/malfunction) and the `damage_mark` vocabulary (chip/rust/missing) DIFFER and must not be conflated. `notApplicableReason` free text is the honest home for a not-performed road test (INS-05).",
    "RESPONSE_SCHEMA": "201 `{receptionVisitId, kind, evidenceId}` per item.",
    "REQUIRED_PERMISSIONS": "`rec.reception.evidence.manage`.",
    "OWNER_REQUIREMENT_MAPPING": "Owner rows OR-15 and the OR-20/21 partial block; journey step S-9. Lift-inspection limits: no type column distinguishes lift from walk-around (RMC-18) and `inspection_status` is created `in_progress` with NO completing operation (RMC-12) — the UI must never render 'lift inspection complete'.",
    "PROTECTED_REPROOF": "PENDING CANDIDATE OBSERVATION — the authenticated browser tier must be observed at the phase's closing candidate; no candidate exists on day one.",
    "FINAL_VERDICT": "PARTIAL",
    "VERDICT_RATIONALE": "Writes READY — Wave E, buildable single-session. Truthful-labelling constraints (RMC-12/RMC-18) and the road-test exclusion (plan §7) bound what this screen may claim."
  },
  "FE-012": {
    "BACKEND_READ_CONTRACTS": "`shared.document-read` for the template document's metadata; per-reception document listing MISSING (G-MEDIA, R8, deferred behind P1-OD-025).",
    "BACKEND_WRITE_CONTRACTS": "`rec.reception-condition-evidence` with kinds `damage_map` and `damage_mark`.",
    "REQUEST_SCHEMA": "`damage_map` REQUIRES `documentId` + `documentVersionId` (the template); `damage_mark` coordinates are 0-1 FRACTIONS; mark vocabulary chip/rust/missing.",
    "RESPONSE_SCHEMA": "201 `{receptionVisitId, kind, evidenceId}`.",
    "REQUIRED_PERMISSIONS": "`rec.reception.evidence.manage`; `shared.document.manage` for the template chain.",
    "OWNER_REQUIREMENT_MAPPING": "Journey step S-7 damage documentation.",
    "PROTECTED_REPROOF": "PENDING CANDIDATE OBSERVATION — the authenticated browser tier must be observed at the phase's closing candidate; no candidate exists on day one.",
    "FINAL_VERDICT": "PARTIAL",
    "VERDICT_RATIONALE": "Wave H for the template half: a template document/version must exist and the media platform is dead-ended (no object store, no scanner — G-MEDIA), while the capture gates (`no-upload-path`) ban any upload UI while P1-OD-025 is open. The mark-capture write itself is READY, stranded behind its own prerequisite."
  },
  "FE-013": {
    "BACKEND_READ_CONTRACTS": "`veh.vehicle-odometer-history` — GET /vehicles/{vehicleId}/odometer-readings, cursor-paginated.",
    "BACKEND_WRITE_CONTRACTS": "`veh.vehicle-odometer-record` — POST same path; the reception then references `odometerReadingId` in `rec.reception-create`.",
    "REQUEST_SCHEMA": "Write takes `value` as a JSON NUMBER. TRAP INS-19: the read returns the value as a decimal STRING — no arithmetic across the divergence. No `road_test` capture method exists (INS-06).",
    "RESPONSE_SCHEMA": "201 with the reading id consumed by check-in; history is `Page<T>` of string-valued readings.",
    "REQUIRED_PERMISSIONS": "`veh.vehicle.odometer.record` for the write; `veh.vehicle.read` for history.",
    "OWNER_REQUIREMENT_MAPPING": "Owner rows OR-6..9 condition-capture block; journey step S-6.",
    "PROTECTED_REPROOF": "PENDING CANDIDATE OBSERVATION — the authenticated browser tier must be observed at the phase's closing candidate; no candidate exists on day one.",
    "FINAL_VERDICT": "PARTIAL",
    "VERDICT_RATIONALE": "Both contracts READY (Wave E) — the only fully unblocked capture pair in the reception wizard. The INS-19 number-in/string-out divergence is the QA-001 mirror-test obligation."
  },
  "FE-014": {
    "BACKEND_READ_CONTRACTS": "`rec.fuel-level-list [MISSING R3]` (G-CAT-REC): `rec.fuel_levels` exists with zero routes AND zero rows (`20260721095000:102`). A read alone does not fix emptiness — population needs a management contract or an Owner provisioning decision; the no-fake-data policy forbids inventing rows.",
    "BACKEND_WRITE_CONTRACTS": "Fields of `rec.reception-create`: `fuelLevelId?|null` and `evSocPercent? 0-100|null` (fractional accepted). No separate operation exists.",
    "REQUEST_SCHEMA": "Both fields nullable — check-in may proceed without them, which is the shipped degradation.",
    "RESPONSE_SCHEMA": "Carried in the create response's echo of the visit; no read-back until R1.",
    "REQUIRED_PERMISSIONS": "`rec.reception.manage` (the fields ride the create).",
    "OWNER_REQUIREMENT_MAPPING": "Owner rows within the OR-6..15 condition block; journey step S-6.",
    "PROTECTED_REPROOF": "PENDING CANDIDATE OBSERVATION — the authenticated browser tier must be observed at the phase's closing candidate; no candidate exists on day one.",
    "FINAL_VERDICT": "PARTIAL",
    "VERDICT_RATIONALE": "EV SOC capture is buildable now (a bounded number input); the fuel-level picker is blocked on G-CAT-REC (R3) plus a population decision. Both fields are nullable so Wave D/E ships without them, stating the gap."
  },
  "FE-015": {
    "BACKEND_READ_CONTRACTS": "`rec.warning-light-code-list [MISSING R3]` — and RMC-11: the catalogue table ships ZERO rows, no seed, no management route, so the kind is unusable in practice even after a read lands.",
    "BACKEND_WRITE_CONTRACTS": "`rec.reception-condition-evidence` with `kind:'warning_light'` — the write is accepted today; nothing legal can populate its catalogue reference.",
    "REQUEST_SCHEMA": "Per-kind schema referencing a warning-light code the platform cannot yet supply.",
    "RESPONSE_SCHEMA": "201 `{receptionVisitId, kind, evidenceId}`.",
    "REQUIRED_PERMISSIONS": "`rec.reception.evidence.manage`.",
    "OWNER_REQUIREMENT_MAPPING": "Owner rows within the OR-6..15 block; journey step S-7. The catalogue-population decision is plan §7's second open decision.",
    "PROTECTED_REPROOF": "PENDING CANDIDATE OBSERVATION — the authenticated browser tier must be observed at the phase's closing candidate; no candidate exists on day one.",
    "FINAL_VERDICT": "PARTIAL",
    "VERDICT_RATIONALE": "Excluded from Wave E deliberately: no picker ships while the catalogue is unreadable AND unpopulated (RMC-11 / G-CAT-REC). The absence is stated on-screen — a disabled control would imply a capability that exists, which is a false statement."
  },
  "FE-016": {
    "BACKEND_READ_CONTRACTS": "MISSING (G-REC-READ) for read-back until R1.",
    "BACKEND_WRITE_CONTRACTS": "`rec.reception-condition-evidence` with `kind:'contents'`.",
    "REQUEST_SCHEMA": "Contents descriptions are restricted narrative — the same sensitive-data class as complaint text.",
    "RESPONSE_SCHEMA": "201 `{receptionVisitId, kind:'contents', evidenceId}`.",
    "REQUIRED_PERMISSIONS": "`rec.reception.evidence.manage` PLUS `iam.sensitive.view` (WF-27 — same dual-permission trap as FE-010).",
    "OWNER_REQUIREMENT_MAPPING": "Owner rows within the OR-6..15 block; journey step S-7 (vehicle contents at custody).",
    "PROTECTED_REPROOF": "PENDING CANDIDATE OBSERVATION — the authenticated browser tier must be observed at the phase's closing candidate; no candidate exists on day one.",
    "FINAL_VERDICT": "PARTIAL",
    "VERDICT_RATIONALE": "Write READY — Wave E single-session. Shares FE-010's WF-27 edge and SEC-002's rendering gate; read-back blocked on G-REC-READ."
  },
  "FE-017": {
    "BACKEND_READ_CONTRACTS": "`shared.document-read` exists; per-reception document list and category enumeration MISSING (G-MEDIA: D2/D3, remediation R8 deferred behind P1-OD-025).",
    "BACKEND_WRITE_CONTRACTS": "`shared.attachment-upload-authorize`, `shared.attachment-version-register`, `shared.attachment-link-create` — contracts complete; the link allow-list already INCLUDES `apt.appointments` and `rec.reception_visits`, purposes include `inspection_media` (`attachment-policy.ts:42-62`).",
    "REQUEST_SCHEMA": "Chain: authorize upload, register version, link to the visit. DEAD-ENDED: `STORAGE_PROVIDER` defaults `unconfigured` and no scanner exists, so no version can reach `accepted` and downloads can NEVER be authorized (`catalog.ts:266-273`).",
    "RESPONSE_SCHEMA": "The honest best state of any registered media is 'registered, pending, never downloadable' (`reception-media-checklist.md:208-210`) — the UI must say exactly that, never 'uploaded'.",
    "REQUIRED_PERMISSIONS": "`shared.document.manage` — one code across the whole attachment chain (authorize, register version, link, read metadata).",
    "OWNER_REQUIREMENT_MAPPING": "Owner rows OR-12/13/14 — explicitly Blocked (INT-093/094/095, `owner-workflow-requirements.md:160-162`). Only the COUNT of seven exterior angles is fixed; the angle set is an unconfirmed proposal; whether missing media blocks approval is an unmade Owner decision (`reception-media-checklist.md:525-537`).",
    "PROTECTED_REPROOF": "PENDING CANDIDATE OBSERVATION — the authenticated browser tier must be observed at the phase's closing candidate; no candidate exists on day one.",
    "FINAL_VERDICT": "PARTIAL",
    "VERDICT_RATIONALE": "HARD BLOCKED (Wave H): P1-OD-025 open, Owner rows Blocked, no object store, no scanner, and the `no-upload-path` / `no-invented-media-limit` gates refuse capture code. What ships is the named-open-decision notice per `reception-media-checklist.md:508-517` — building nothing here is the compliant deliverable."
  },
  "FE-018": {
    "BACKEND_READ_CONTRACTS": "MISSING (G-REC-READ) for read-back of captured signatures until R1.",
    "BACKEND_WRITE_CONTRACTS": "`rec.reception-signature` — POST .../signatures.",
    "REQUEST_SCHEMA": "`{signerRole enum(7), signerPartnerId?|null, signatureDocumentId + signatureDocumentVersionId REQUIRED, captureMethod drawn/typed/uploaded/biometric, purpose enum(6), signatureHash? hex <=64B}` — NEVER the drawn bytes. A `pending` document version satisfies the guard (version-in-document only).",
    "RESPONSE_SCHEMA": "201 `{receptionVisitId, signatureId, purpose}`.",
    "REQUIRED_PERMISSIONS": "`rec.reception.signature.manage`.",
    "OWNER_REQUIREMENT_MAPPING": "Owner rows OR-22/23 (Contracted); journey steps S-10/S-11.",
    "PROTECTED_REPROOF": "PENDING CANDIDATE OBSERVATION — the authenticated browser tier must be observed at the phase's closing candidate; no candidate exists on day one.",
    "FINAL_VERDICT": "PARTIAL",
    "VERDICT_RATIONALE": "Write READY but it REQUIRES an attachment document, and the media platform is dead-ended (G-MEDIA): a pending version is legal yet its bytes are undeliverable, and capture UI is gate-banned while P1-OD-025 is open. Wave E may build the metadata capture; the signature-image half is Wave H."
  },
  "FE-019": {
    "BACKEND_READ_CONTRACTS": "`rec.refusal-reason-list [MISSING R3]` (G-CAT-REC — table `20260721095000:220`, zero routes, zero rows); read-back MISSING (G-REC-READ).",
    "BACKEND_WRITE_CONTRACTS": "`rec.reception-refusal` — POST .../refusals.",
    "REQUEST_SCHEMA": "`{refusalType enum(5), refusalReasonId? uuid, refusingPartnerId?, witnessEmployeeId?, evidenceDocumentId?}`. Type `authorization`: `refusingPartnerId` REQUIRED (422) and the partner must hold an ACTIVE authorizing role (409 `ERR-TRN-001`) — the refusal becomes a standing block on approve and convert.",
    "RESPONSE_SCHEMA": "201 `{receptionVisitId, refusalId, refusalType}`. A refusal NEVER changes `receptionStatus`.",
    "REQUIRED_PERMISSIONS": "`rec.reception.signature.manage` — deliberately the SAME code as FE-018's signatures; the permission gate must not invent a separate one.",
    "OWNER_REQUIREMENT_MAPPING": "Owner rows OR-22/23; journey steps S-10/S-11 (refusal is the negative half of the signature step).",
    "PROTECTED_REPROOF": "PENDING CANDIDATE OBSERVATION — the authenticated browser tier must be observed at the phase's closing candidate; no candidate exists on day one.",
    "FINAL_VERDICT": "PARTIAL",
    "VERDICT_RATIONALE": "Write READY — Wave E builds refusal capture minus the reason picker (G-CAT-REC). The standing-block consequence on approve/convert must be explained at capture time because it cannot be read back until R1."
  },
  "FE-020": {
    "BACKEND_READ_CONTRACTS": "MISSING (G-REC-READ / INT-015): `rec.reception-detail [MISSING R1]` and `rec.reception-condition-evidence-list [MISSING R1]` are what a summary reads. The item shape is already specified at `reception-read-surface-plan.md:439`.",
    "BACKEND_WRITE_CONTRACTS": "`rec.reception-approve` — POST .../approve, empty body `{}` or null, strict.",
    "REQUEST_SCHEMA": "`If-Match` REQUIRED (428 `ERR-CON-002` missing / 409 `ERR-CON-001` stale) and the ONLY RV source until R1 is the prior write chain in-session. Idempotency-Key mandatory.",
    "RESPONSE_SCHEMA": "200 `{receptionVisitId, receptionStatus:'authorized', appliedTransitions:['inspecting','authorized']|['authorized'], recordVersion}` + ETag. From `opened` it walks TWO edges in ONE transaction so RV = sent+2. Re-run on an authorized visit with a NEW key answers 409 `ERR-TRN-001` — NOT idempotent success, and re-reading does not fix `ERR-TRN-001`.",
    "REQUIRED_PERMISSIONS": "`rec.reception.approve` (high risk, approval audit class). A standing declined authorization or a standing authorization-refusal blocks this operation.",
    "OWNER_REQUIREMENT_MAPPING": "Owner row OR-24 — explicitly Blocked (INT-015); journey step S-11. The resumability mandate (`owner-workflow-requirements.md:182-197`) names this workflow's single-session limitation a P1-28 readiness blocker.",
    "PROTECTED_REPROOF": "PENDING CANDIDATE OBSERVATION — the authenticated browser tier must be observed at the phase's closing candidate; no candidate exists on day one.",
    "FINAL_VERDICT": "PARTIAL",
    "VERDICT_RATIONALE": "Operation READY / workflow BLOCKED: technically buildable single-session (Wave F) but must not close as done — plan §8 forbids presenting the single-session flow for acceptance without R1 or an explicit Owner waiver. The RV=sent+2 shape is a QA-004 pin."
  },
  "FE-021": {
    "BACKEND_READ_CONTRACTS": "`rec.reception-detail [MISSING R1]` — the acknowledgement document is a rendering of the visit record, so it is fully blocked on G-REC-READ.",
    "BACKEND_WRITE_CONTRACTS": "N/A with rationale: printing writes nothing; the export gate bans PDF/blob/download generation — PrintDocument frame with print styling only.",
    "REQUEST_SCHEMA": "N/A with rationale: no operation of its own; consumes R1's detail read when it exists.",
    "RESPONSE_SCHEMA": "The plan-specified detail item (`reception-read-surface-plan.md:439`): `id, displayNumber, receptionStatus, origin, vehicleId, vehicleDisplayNumber, custodyAcceptedAt, custodyReleasedAt, recordVersion`.",
    "REQUIRED_PERMISSIONS": "`rec.reception.read` — exists NOWHERE at `d64f259`; seeded by R1.",
    "OWNER_REQUIREMENT_MAPPING": "Journey step S-10 (the acknowledgement the customer takes away).",
    "PROTECTED_REPROOF": "PENDING CANDIDATE OBSERVATION — the authenticated browser tier must be observed at the phase's closing candidate; no candidate exists on day one.",
    "FINAL_VERDICT": "PARTIAL",
    "VERDICT_RATIONALE": "Wave G — fully blocked on G-REC-READ (R1). The export-gate constraint (print styling, never PDF bytes) is recorded now so the eventual build does not rediscover it."
  },
  "FE-022": {
    "BACKEND_READ_CONTRACTS": "`wo.work-order-detail` under `wo.work_order.read` for the result display; the If-Match source is `rec.reception-detail [MISSING R1]` until R1 (in-session RV chain before that).",
    "BACKEND_WRITE_CONTRACTS": "`rec.reception-convert-to-work-order` — POST .../convert-to-work-order. There is NO `POST /work-orders` anywhere in the platform; this command is the only way a work order comes to exist.",
    "REQUEST_SCHEMA": "Body `{}` or null, strict. `If-Match` REQUIRED; the visit must be `authorized` else 409 `ERR-TRN-001`; DB guard failure answers 409 'not ready to convert'; a concurrent race answers 409 `ERR-RES-002` (retryable).",
    "RESPONSE_SCHEMA": "200 `{receptionVisitId, workOrderId, displayNumber|null, state, alreadyConverted}` — `state` is an OPAQUE catalogue code, NOT an enum, and there is NO ETag (`convert-to-work-order/route.ts:73-83`). Replay on a converted visit answers 200 `alreadyConverted:true` = SUCCESS — the UI must treat it as distinct from approve's 409 replay.",
    "REQUIRED_PERMISSIONS": "`rec.reception.convert` (NOT a `wo.*` code — WF-12); result display needs `wo.work_order.read`.",
    "OWNER_REQUIREMENT_MAPPING": "Journey step S-12, the conversion action ONLY — everything past it is P1-29 (`owner-workflow-requirements.md:220-239`; plan §6 quotable boundary).",
    "PROTECTED_REPROOF": "PENDING CANDIDATE OBSERVATION — the authenticated browser tier must be observed at the phase's closing candidate; no candidate exists on day one.",
    "FINAL_VERDICT": "PARTIAL",
    "VERDICT_RATIONALE": "Operation READY, same If-Match blockage as FE-020 (Wave F). The three-replay-shapes distinction (create 200-no-ETag / approve 409 / convert 200 alreadyConverted) is the QA-002 obligation this task anchors."
  },
  "SEC-001": {
    "BACKEND_READ_CONTRACTS": "N/A with rationale: this task maps permissions, it does not read business data. The permission catalogue seed (`04_iam_permission_catalog.sql:135-168`) and `iam.auth-session`'s `permissions[]` array are its inputs.",
    "BACKEND_WRITE_CONTRACTS": "N/A with rationale: no mutation; role-to-grant mapping is seed work owned by P1-18 (`finding-phase-disposition.md:158`).",
    "REQUEST_SCHEMA": "N/A with rationale: no operation of its own.",
    "RESPONSE_SCHEMA": "N/A with rationale: no operation of its own.",
    "REQUIRED_PERMISSIONS": "The full seeded `apt.*`/`rec.*` set: `apt.appointment.manage`, `apt.appointment.lifecycle.manage`, `rec.reception.manage`, `rec.reception.party.manage`, `rec.reception.authorization.verify`, `rec.reception.evidence.manage`, `rec.reception.signature.manage`, `rec.reception.approve`, `rec.reception.convert`, plus `iam.sensitive.view` paired per WF-27, plus the two UNSEEDED read codes `apt.appointment.read` / `rec.reception.read` (G-PERM) once R1/R2 land.",
    "OWNER_REQUIREMENT_MAPPING": "Owner register question A block (`owner-workflow-requirements.md:199-216`): least-privilege mapping, an owner/reception account, a read-only negative control and a cross-tenant negative control. The `iam.user.read` staff-directory overload (any logged-in web user holds it via the session gate today) needs an explicit disposition.",
    "PROTECTED_REPROOF": "PENDING CANDIDATE OBSERVATION — the permission-denial branches are proven in the authenticated browser tier at the closing candidate; no candidate exists on day one.",
    "FINAL_VERDICT": "PARTIAL",
    "VERDICT_RATIONALE": "Nothing mapped yet — day-one row. The binding fact recorded now: cancel/no-show use `apt.appointment.lifecycle.manage`, a DIFFERENT code from create/reschedule, and complaint/contents need `iam.sensitive.view` granted together with `rec.reception.evidence.manage` or operators hit a database 403 mid-form."
  },
  "SEC-002": {
    "BACKEND_READ_CONTRACTS": "N/A with rationale: governs rendering of FE-010/FE-016 narratives; no read contract exists for them until R1, and when it lands the same `iam.sensitive.view` gate applies to display.",
    "BACKEND_WRITE_CONTRACTS": "`rec.reception-condition-evidence` kinds `complaint` and `contents` — the two writes whose WF-27 dual-permission refusal this task owns.",
    "REQUEST_SCHEMA": "Restricted narratives (complaint 1-4000; contents descriptions).",
    "RESPONSE_SCHEMA": "The refusal shape this task must surface honestly: an application-level pass followed by a DATABASE 403 without `iam.sensitive.view` — mapped to a permissions message naming the missing capability, never a crash and never a generic error.",
    "REQUIRED_PERMISSIONS": "`iam.sensitive.view` as the rendering gate; `rec.reception.evidence.manage` as the capture gate.",
    "OWNER_REQUIREMENT_MAPPING": "Owner rows OR-10/OR-26 (concern capture) and the sensitive-data handling expectations of `inspection-and-diagnostics.md` §7.3.",
    "PROTECTED_REPROOF": "PENDING CANDIDATE OBSERVATION — the denial rendering is proven in the authenticated browser tier at the closing candidate; no candidate exists on day one.",
    "FINAL_VERDICT": "PARTIAL",
    "VERDICT_RATIONALE": "Buildable with the writes alone (no read dependency) — rides Wave E. Day-one row; nothing built."
  },
  "SEC-003": {
    "BACKEND_READ_CONTRACTS": "Applies to the FUTURE R1/R2 list adapters: any `branchId` resource selector those reads need enters as a NAMED `query()` exemption with a pinning test — `read-operation.ts:89-133` throws on tenant/company/branch keys in query strings today, and that behaviour is the rule, not the obstacle.",
    "BACKEND_WRITE_CONTRACTS": "N/A with rationale: scope hygiene governs how adapters are written, not a contract of its own.",
    "REQUEST_SCHEMA": "N/A with rationale: no operation of its own.",
    "RESPONSE_SCHEMA": "N/A with rationale: no operation of its own.",
    "REQUIRED_PERMISSIONS": "N/A with rationale: enforces the ABSENCE of client-asserted scope (P1-27-SEC-001 precedent) — scope is server-resolved from the session.",
    "OWNER_REQUIREMENT_MAPPING": "N/A with rationale: no Owner table row binds this task; it polices the platform's isolation contract on the Owner's behalf.",
    "PROTECTED_REPROOF": "PENDING CANDIDATE OBSERVATION — isolation behaviour is proven in the authenticated browser tier at the closing candidate; no candidate exists on day one.",
    "FINAL_VERDICT": "PARTIAL",
    "VERDICT_RATIONALE": "Day-one row. Its concrete shape depends on the read-surface query design (G-APT-READ/G-REC-READ), which is exactly why it is registered before those reads exist — the exemption-with-pinning-test pattern is decided now, not improvised later."
  },
  "SEC-004": {
    "BACKEND_READ_CONTRACTS": "N/A with rationale: the gate walks the operation register and the web source tree; it reads no business data.",
    "BACKEND_WRITE_CONTRACTS": "All 12 `apt.*`/`rec.*` write operations are its subject: apt create/reschedule/cancel/no-show and rec create/party-role/authorization/condition-evidence/signature/refusal/approve/convert-to-work-order.",
    "REQUEST_SCHEMA": "N/A with rationale: no operation of its own.",
    "RESPONSE_SCHEMA": "N/A with rationale: no operation of its own.",
    "REQUIRED_PERMISSIONS": "N/A with rationale: a CI gate holds no permission.",
    "OWNER_REQUIREMENT_MAPPING": "N/A with rationale: no Owner table row binds this task; it exists because P1-27's dominant defect (INT-113 — ten canonical writes shipped with route, permission, audit class and register row, and NO screen from which anybody could invoke them) must not recur in P1-28.",
    "PROTECTED_REPROOF": "PENDING CANDIDATE OBSERVATION — the gate's own mutation proofs run in the root tier at the closing candidate; no candidate exists on day one.",
    "FINAL_VERDICT": "PARTIAL",
    "VERDICT_RATIONALE": "Day-one obligation (Wave A): the analogue of `check-p1-27-write-reachability.mjs`, which covers crm/veh ONLY — the 12 apt/rec writes have no reachability authority today. Every canonical mutation ends the phase reachable, deliberately absent, or decision-neutral, and the classification is derived, not maintained."
  },
  "QA-001": {
    "BACKEND_READ_CONTRACTS": "Every read adapter the phase acquires (C-table reads now; R1/R2 reads when they land) gets a contract-mirror suite: every Zod schema asserted field-for-field against the route source, absent-versus-null distinctions pinned.",
    "BACKEND_WRITE_CONTRACTS": "All 12 apt/rec writes, mirrored the same way — docblock operation tables read from ROUTE SOURCE, not from OpenAPI (P1-27-INT-004: the generator publishes 200 for 201 routes and never publishes 400/404).",
    "REQUEST_SCHEMA": "The mirror suites ARE the request-schema evidence: INS-19 (odometer number-in/string-out), the differing condition_item/damage_mark vocabularies, and the origin XOR union are the named pins.",
    "RESPONSE_SCHEMA": "The three replay shapes and the no-ETag convert response are response-side pins.",
    "REQUIRED_PERMISSIONS": "N/A with rationale: a test suite holds no permission.",
    "OWNER_REQUIREMENT_MAPPING": "N/A with rationale: no Owner table row binds this task; it is the WFP U1 exit criterion (contract modules proven before screens).",
    "PROTECTED_REPROOF": "PENDING CANDIDATE OBSERVATION — the suites execute in the web tier at the closing candidate; no candidate exists on day one.",
    "FINAL_VERDICT": "PARTIAL",
    "VERDICT_RATIONALE": "Wave A starts the harness; the suite grows with each wave. Day-one row — no adapter exists yet, therefore no mirror exists yet."
  },
  "QA-002": {
    "BACKEND_READ_CONTRACTS": "Error-path coverage for the reads as they land (R1/R2): 403 versus 404 isolation split per the read-surface plan's own case table.",
    "BACKEND_WRITE_CONTRACTS": "All 12 writes: the 428/409/422/403 branches per operation, and per-code 409 discrimination (`ERR-CON-001` stale version vs `ERR-TRN-001` state refusal vs `ERR-RES-002` resource conflict) — the P1-27 root-cause class where one message covered ten codes.",
    "REQUEST_SCHEMA": "422 `violations[{path,rule}]` rendering, including the FE-003 trap where the path names `body.confirmedFrom` for a `confirmedTo` violation.",
    "RESPONSE_SCHEMA": "THE THREE REPLAY SHAPES, asserted distinctly: idempotent replay of any command answers 200 with NO ETag even when the first answer was 201; approve re-run with a new key answers 409 `ERR-TRN-001`; convert re-run answers 200 `alreadyConverted:true` = success.",
    "REQUIRED_PERMISSIONS": "N/A with rationale: a test suite holds no permission.",
    "OWNER_REQUIREMENT_MAPPING": "N/A with rationale: no Owner table row binds this task; it protects every FE task's failure honesty.",
    "PROTECTED_REPROOF": "PENDING CANDIDATE OBSERVATION — executed in the web tier at the closing candidate; no candidate exists on day one.",
    "FINAL_VERDICT": "PARTIAL",
    "VERDICT_RATIONALE": "Day-one row. The replay/error matrix is enumerable NOW from the archaeology (it is §5 fact 3) — writing it before the first adapter is the cheapest moment."
  },
  "QA-003": {
    "BACKEND_READ_CONTRACTS": "Isolation semantics of every read consumed: `ERR-RES-001` 404 deliberately hides cross-tenant existence; scope keys in query strings throw client-side (`read-operation.ts:89-133`).",
    "BACKEND_WRITE_CONTRACTS": "Cross-tenant and cross-branch refusal for all 12 writes; the check-in coherence rule (422 `incoherent_reference` when body scope differs from the appointment's own).",
    "REQUEST_SCHEMA": "N/A with rationale: isolation is proven over the operations' own schemas.",
    "RESPONSE_SCHEMA": "N/A with rationale: isolation is proven over the operations' own responses.",
    "REQUIRED_PERMISSIONS": "The negative controls of SEC-001 (read-only principal, cross-tenant principal) are this task's fixtures.",
    "OWNER_REQUIREMENT_MAPPING": "N/A with rationale: no Owner table row binds this task; tenant isolation is a platform obligation.",
    "PROTECTED_REPROOF": "PENDING CANDIDATE OBSERVATION — proven in the authenticated browser and web tiers at the closing candidate; no candidate exists on day one.",
    "FINAL_VERDICT": "PARTIAL",
    "VERDICT_RATIONALE": "Day-one row; nothing proven yet. The P1-27 convention (tenant keys structurally unsendable, asserted by test) carries over unchanged."
  },
  "QA-004": {
    "BACKEND_READ_CONTRACTS": "Until R1/R2 land there is NO read to refresh a stale version from — which is this task's central fact, not an inconvenience: re-reading does not fix `ERR-TRN-001`, and nothing fixes a lost RV except the in-session cache.",
    "BACKEND_WRITE_CONTRACTS": "The five version-guarded writes (`apt.appointment-reschedule`, `apt.appointment-cancel`, `apt.appointment-no-show`, `rec.reception-approve`, `rec.reception-convert-to-work-order`) and the RV-sourcing discipline over the three unguarded creates.",
    "REQUEST_SCHEMA": "`If-Match` on all five; 428 `ERR-CON-002` when missing; approve's RV = sent+2 double-transition; convert's missing ETag.",
    "RESPONSE_SCHEMA": "Every command response's RV/ETag is CACHED — riskiest-fact 2: close the browser and the vehicle is in custody with no path forward until R1.",
    "REQUIRED_PERMISSIONS": "N/A with rationale: a test suite holds no permission.",
    "OWNER_REQUIREMENT_MAPPING": "The Owner resumability mandate (`owner-workflow-requirements.md:182-197`) is the requirement this task's evidence must speak to honestly: single-session concurrency discipline is provable now; resumable concurrency waits on R1.",
    "PROTECTED_REPROOF": "PENDING CANDIDATE OBSERVATION — executed at the closing candidate; no candidate exists on day one.",
    "FINAL_VERDICT": "PARTIAL",
    "VERDICT_RATIONALE": "Day-one row. The RV-sourcing rules are written into the plan and this register BEFORE any screen exists, because P1-27's QA-004 had to be settled by an Owner decision mid-phase — P1-28's version-token story is fixed by contract from the start."
  },
  "QA-005": {
    "BACKEND_READ_CONTRACTS": "N/A with rationale: evidence packaging reads the repository and the running application, not business contracts.",
    "BACKEND_WRITE_CONTRACTS": "N/A with rationale: no mutation.",
    "REQUEST_SCHEMA": "N/A with rationale: no operation of its own.",
    "RESPONSE_SCHEMA": "N/A with rationale: no operation of its own.",
    "REQUIRED_PERMISSIONS": "N/A with rationale: the Owner acceptance accounts are provisioned by the acceptance tooling, not by this row.",
    "OWNER_REQUIREMENT_MAPPING": "The permanent Frontend closure rule (plan §8): the phase closes ONLY on an explicit `OWNER ACCEPTANCE: PASS` against the running application; silence is never Pass. The resumability blocker (`owner-workflow-requirements.md:182-197`) must be either discharged by R1 or explicitly waived in the phase record before acceptance is requested.",
    "PROTECTED_REPROOF": "PENDING CANDIDATE OBSERVATION — QA-005 seals against the phase's final candidate by definition; no candidate exists on day one.",
    "FINAL_VERDICT": "PARTIAL",
    "VERDICT_RATIONALE": "End-of-phase by design — the P1-27 lifecycle convention (candidate assurance, not protected-merge proof) carries over: NO canonical task requires proof on protected develop; this row seals last."
  },
  "DO-001": {
    "BACKEND_READ_CONTRACTS": "N/A with rationale: CI gate wiring reads the repository, not business contracts.",
    "BACKEND_WRITE_CONTRACTS": "N/A with rationale: no mutation.",
    "REQUEST_SCHEMA": "N/A with rationale: no operation of its own.",
    "RESPONSE_SCHEMA": "N/A with rationale: no operation of its own.",
    "REQUIRED_PERMISSIONS": "N/A with rationale: a CI gate holds no permission.",
    "OWNER_REQUIREMENT_MAPPING": "N/A with rationale: no Owner table row binds this task; it keeps the phase's own record honest, which is what the Owner's acceptance ultimately reads.",
    "PROTECTED_REPROOF": "PENDING CANDIDATE OBSERVATION — the gates themselves are exercised by hosted CI at the closing candidate; no candidate exists on day one.",
    "FINAL_VERDICT": "PARTIAL",
    "VERDICT_RATIONALE": "Partially DISCHARGED already and deliberately still PARTIAL: the matrix gate (`validate:p1-28-matrix`), its builder, its test suite, and the derived-marker updates land in the SAME commit as this file (the DO-001 day-one rule). Outstanding: `MODULE_DISPOSITION` entries and SCAN_ROOTS decisions for the future feature trees, which cannot exist before the first dashboard file does."
  },
  "DO-002": {
    "BACKEND_READ_CONTRACTS": "N/A with rationale: pipeline wiring reads workflow and configuration files.",
    "BACKEND_WRITE_CONTRACTS": "N/A with rationale: no mutation. If the Backend remediations R1-R3 add operations, `idempotent-operations.ts` is REGENERATED, never hand-edited.",
    "REQUEST_SCHEMA": "N/A with rationale: no operation of its own.",
    "RESPONSE_SCHEMA": "N/A with rationale: no operation of its own.",
    "REQUIRED_PERMISSIONS": "N/A with rationale: a pipeline holds no permission.",
    "OWNER_REQUIREMENT_MAPPING": "N/A with rationale: no Owner table row binds this task.",
    "PROTECTED_REPROOF": "PENDING CANDIDATE OBSERVATION — pipeline behaviour is observed on hosted runs at the closing candidate; no candidate exists on day one.",
    "FINAL_VERDICT": "PARTIAL",
    "VERDICT_RATIONALE": "Day-one row. Standing obligations recorded: new vitest suites wire into `web-quality`; navigation `status` flips follow G-PERM (never point 'available' at a 404, never leave 'planned' on a live screen); no new toast/storage/fetch surfaces."
  },
  "DOC-001": {
    "BACKEND_READ_CONTRACTS": "N/A with rationale: this task IS the phase record — `canonical-plan.md`, `contract-archaeology.md`, this verdicts file and the generated matrix.",
    "BACKEND_WRITE_CONTRACTS": "N/A with rationale: no mutation.",
    "REQUEST_SCHEMA": "N/A with rationale: no operation of its own.",
    "RESPONSE_SCHEMA": "N/A with rationale: no operation of its own.",
    "REQUIRED_PERMISSIONS": "N/A with rationale: documentation holds no permission.",
    "OWNER_REQUIREMENT_MAPPING": "The Owner-row anchoring in every FE row of this file is DOC-001's product: each canonical task names the Owner requirement or journey step it answers, because P1-27's Owner acceptance failed against requirements no task row had claimed.",
    "PROTECTED_REPROOF": "NOT REQUIRED — documentation reconciliation is proven by gates that run on every pull request (`validate:p1-28-matrix` from `verify:policies`); no protected-branch-only or browser-tier job is part of this row's evidence.",
    "FINAL_VERDICT": "PARTIAL",
    "VERDICT_RATIONALE": "The phase's first act, substantially delivered by the day-one commit that introduces this file — and held PARTIAL deliberately: DOC-001 closes on record-versus-tree synchronization at the END of the phase (the P1-19/P1-20/P1-24 sibling convention), not on the day the record is born."
  },
  "DOC-002": {
    "BACKEND_READ_CONTRACTS": "N/A with rationale: guidance work.",
    "BACKEND_WRITE_CONTRACTS": "N/A with rationale: no mutation.",
    "REQUEST_SCHEMA": "N/A with rationale: no operation of its own.",
    "RESPONSE_SCHEMA": "N/A with rationale: no operation of its own.",
    "REQUIRED_PERMISSIONS": "N/A with rationale: documentation holds no permission.",
    "OWNER_REQUIREMENT_MAPPING": "N/A with rationale: no Owner table row binds this task directly; its truthful-labelling records serve the Owner's operators.",
    "PROTECTED_REPROOF": "NOT REQUIRED — guidance reconciliation is proven by the developer-guide gates on every pull request; no protected-branch-only or browser-tier job is part of this row's evidence.",
    "FINAL_VERDICT": "PARTIAL",
    "VERDICT_RATIONALE": "Day-one row. Standing obligations: one developer-guide sentence per new or extended gate rule id (the guidance-reconciliation test demands it), and the three truthful-labelling records — 'confirm = reschedule', media honesty ('registered, pending', never 'uploaded'), and the road-test absence. P1-27's DOC-002 lesson (a conjunction counted as resolved when only half was proven) applies: each half is tracked separately."
  }
}
