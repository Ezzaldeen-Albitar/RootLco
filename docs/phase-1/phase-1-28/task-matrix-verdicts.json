{
  "FE-001": {
    "IMPLEMENTATION_SURFACES": "`apps/web/src/features/appointments/components/AppointmentCalendarScreen.tsx` (462 lines: branch-target form, calendar table, day queue) behind `apps/web/src/app/[locale]/(dashboard)/appointments/page.tsx:38` (route gate) and `:64` (screen, handed the session's own resolved scope). The queue half is `apps/web/src/features/receptions/components/ReceptionQueueScreen.tsx` behind `apps/web/src/app/[locale]/(dashboard)/receptions/page.tsx:43`. Adapters: `apps/web/src/features/appointments/api.ts:102` (`listAppointments`), `apps/web/src/features/receptions/api.ts` (`listReceptions`).",
    "NORMAL_NAVIGATION_ENTRY": "Sidebar `appointments` → `/appointments` gated on `apt.appointment.read` (`apps/web/src/config/navigation.ts:158-166`) and `receptions` → `/receptions` gated on `rec.reception.read` (`:167-175`), both `status: 'available'`. The flip is pinned by `apps/web/tests/navigation.test.ts:52-89`, which asserts the whole available set as an equality, so a status claim without a screen fails.",
    "BACKEND_READ_CONTRACTS": "`apt.appointment-list` GET /api/v1/appointments and `apt.appointment-detail` GET /api/v1/appointments/{appointmentId} — both registered at this head (`docs/phase-1/phase-1-24/evidence/operation-register.json`; 29 apt/rec operations, 14 POST + 15 GET). G-APT-READ is CLOSED by R2 (PR #220). The queue half additionally reads `rec.reception-list`.",
    "BACKEND_WRITE_CONTRACTS": "N/A with rationale: a calendar and queue perform no mutation of their own. The affordances they render lead to FE-002 (booking) and to `rec.reception-create` (the day queue's Check in link), and each is gated on the permission of the operation it leads to rather than on an appointment code (`appointments/page.tsx:70-77`).",
    "REQUEST_SCHEMA": "Branch target as a resource selector plus offset-bearing local-day bounds and an optional lifecycle filter. Proved on the wire, not in prose: `apps/web/tests/appointments-calendar.dom.test.tsx:145-156` asserts the `{companyId, branchId}` pair travels as the target and that `from`/`to` carry `T00:00:00`/`T23:59:59` with an explicit UTC offset; `:159-170` pins the status filter through.",
    "RESPONSE_SCHEMA": "Cursor page `{items, nextCursor, hasMore}` with no total; the row carries id, display number, requester, lifecycle status and the requested/confirmed windows. `appointments-calendar.dom.test.tsx:243-253` refuses an invented total and offers Next only while the server says more exists; `:172-184` renders `appointments.window.notConfirmed` as a fact rather than a blank.",
    "REQUIRED_PERMISSIONS": "`apt.appointment.read` for the page (`appointments/page.tsx:38`), `apt.appointment.manage` for the booking offer and `rec.reception.manage` for the day queue's arrival affordance (`:69`, `:77`). `apps/web/tests/p1-28-appointment-routes.test.ts:121-157` grants each property for its own code and for nothing else.",
    "OWNER_REQUIREMENT_MAPPING": "Journey step S-1 (today's arrivals) and Owner appointment rows OR-1..5 (`docs/product/owner-workflow-requirements.md:147-180`). Decision WF-01 — whether arrival is a business record at all — is still undecided and is why the queue is a view over `confirmed` appointments rather than a separate arrival record.",
    "TENANT_ISOLATION_REQUIREMENT": "No tenant key ever leaves the browser and the branch pair travels as the operation's own required resource selector, not as a client claim: `apps/web/tests/p1-28-qa.test.ts:924-953` asserts no request carries a tenant and that the branch pair is the one the screen was handed. A cross-tenant appointment renders identically to a missing one (`apps/web/tests/p1-28-permission-route-binding.test.ts` \"renders a cross-tenant appointment identically to a missing one\").",
    "LOADING_STATE": "Shared table loading and skeleton rows (`apps/web/src/components/states/States.tsx:71`, `:87`) through the `useServerTable` status the screen renders; nothing is requested at all before the operator names a branch (`appointments-calendar.dom.test.tsx:102-109`).",
    "EMPTY_STATE": "`AppointmentCalendarScreen.tsx:250` renders the idle EmptyState before a target is named; the table's own generic empty state is suppressed (`:403`) so it cannot make a claim about the whole branch.",
    "ZERO_RESULT_STATE": "A zero-row answer is stated as being about the RANGE, not the branch — `appointments.calendar.noneInRange` — and the generic `state.empty.title` must not also render (`appointments-calendar.dom.test.tsx:233-241`).",
    "ERROR_STATE": "`BackendUnavailableState` with the correlation reference and a Retry (`States.tsx:192`), asserted at `appointments-calendar.dom.test.tsx:214-222`.",
    "DENIAL_STATE": "A denied read renders the denial instead of an empty calendar (`appointments-calendar.dom.test.tsx:206-212`); the route-level denial renders instead of the screen before any request (`p1-28-appointment-routes.test.ts:121-130`). An ended session is told to sign in with no useless retry (`:224-231`).",
    "CONFLICT_STATE": "N/A with rationale: this row performs no version-guarded write. An inverted range is refused locally, beside the field, with no request spent (`appointments-calendar.dom.test.tsx:119-131`).",
    "ARABIC_EVIDENCE": "Every `appointments.calendar.*` and `appointments.status.*` key is present in `apps/web/src/i18n/messages/ar.json`; the Arabic flow is driven end to end at `appointments-calendar.dom.test.tsx:338-361` (select company, select branch, submit, read the row).",
    "ENGLISH_EVIDENCE": "Same keys in `en.json`, used by every English case in that suite; the catalogue-parity gate holds the two files to the same key set.",
    "RTL_LTR_EVIDENCE": "`renderRtl` mounts the screen under `dir=\"rtl\"` (`appointments-calendar.dom.test.tsx:340`); identifiers and instants stay `dir=\"ltr\"` inside the RTL page (`AppointmentCalendarScreen.tsx:203`, `:211`, `:296`, `:346`).",
    "ACCESSIBILITY_EVIDENCE": "The results region is a labelled section (`AppointmentCalendarScreen.tsx:380`, `aria-labelledby=\"appointment-results-heading\"`); rows are reached by `getByRole('row')` and controls by accessible name throughout the suite, which is what a screen reader would use. NAMED GAP: no axe run covers this screen — only the media notice carries one (`apps/web/tests/p1-28-reception-media.dom.test.tsx` \"has no critical or serious accessibility violation, in both directions\") — and no tablet-viewport case covers it in the P1-28 suites.",
    "PRODUCTION_REACHABILITY": "Reachable from the sidebar for a holder of `apt.appointment.read`; the route is one of the eight pages the access gate DERIVES by what they load (`tests/ci/p1-28-access-gate.test.ts:603-611`), so it cannot fall out of the governed set silently.",
    "PRODUCTION_TEST_EVIDENCE": "`apps/web/tests/appointments-calendar.dom.test.tsx` (21 cases) and `apps/web/tests/p1-28-appointment-routes.test.ts:120-166` (4 cases) plus the queue's `apps/web/tests/reception-queue.dom.test.tsx` (18 cases). All re-run green at this head.",
    "NEGATIVE_OR_MUTATION_PROOF": "Check in is offered on a `confirmed` row and on NO other status — three rows are rendered and exactly one link is found (`appointments-calendar.dom.test.tsx:294-312`) — and it is withdrawn from an operator without `rec.reception.manage` while the Open control survives (`:314-323`). The truthful-labelling obligation is enforced negatively: no `appointments.*` string may equal 'Confirm' (`:325-334`).",
    "PROTECTED_REPROOF": "PENDING PROTECTED MERGE — RE-OBSERVED for this re-derivation at `a2318c36`: the web tier ran 2669 cases, 0 failed and 0 skipped (28 P1-28 suites, 760 cases), and the root CI tier ran 2475 cases, 0 failed and 0 skipped (5 P1-28 gate suites, 180 cases). THE BROWSER TIER NOW CARRIES THIS PHASE — `apps/web/tests/e2e/authenticated/appointments-and-receptions.spec.ts` (2478 lines, against the running application, the running API and the real database), which the previous derivation correctly recorded as absent. It covers this row directly: \"the P1-28 modules are reachable from the sidebar\", \"the appointment calendar reads only for a named branch\" (the idle state, no read until a target is submitted, an inverted range refused beside the field rather than relayed from the server) and \"the reception queue is a board for one named branch\" (the status vocabulary rendered as labels, an empty board offering no close). STILL NOT OBSERVED, and this is the whole outstanding debt on this row: that spec was NOT executed in this derivation — it needs the production launcher (`npm run acceptance:serve`), a live database and, for §18, a provisioned second workspace — and the run reported by the wave that landed it (319 expected, 0 unexpected, 0 flaky) is recorded in no phase document, so no browser result is bound to this head. THE TABLET VIEWPORT AND HOSTED CI WERE OBSERVED AT `38afa5c28e5b78d484a442cf6b8596fb2a5c34aa`, A HEAD THIS CANDIDATE SUPERSEDES. Two sentences stood here and both were false: that the `authenticated-tablet` project sees no P1-28 screen, and that no hosted-CI result exists for this head. `apps/web/playwright.config.ts:254-255` names that project and matches `(administration|appointments-and-receptions)`, so the phase surface is seen at 1024x768 as well as at 1440x900; run `31750364479` / job `94614564003` at head `38afa5c28e5b78d484a442cf6b8596fb2a5c34aa` executed `authenticated/appointments-and-receptions.spec.ts` 141 times — 47 in `authenticated-en`, 47 in `authenticated-ar`, 47 in `authenticated-tablet`, 0 failed and 0 skipped. Hosted CI at that head is run `31750364479`, 21 checks, 21 success, 0 failure, with `ci-gate` (`94617578352`) the single required check. The citation this cell used to give for the tablet claim pointed at lines 225 to 233 of the same config — the project DOCBLOCK, nine consecutive comment lines describing the state BEFORE the widening — so it read as confirmation of a sentence the code beneath it refutes; `validate:p1-28-evidence` now refuses a comment-only citation for exactly that reason. STILL OUTSTANDING ON THIS ROW: the protected-branch re-proof, because PR #226 has not merged, and OWNER ACCEPTANCE, which has not been requested or returned (`docs/phase-1/phase-1-28/evidence/change-log.md:5-11`). THAT HEAD IS NOT THE CURRENT CANDIDATE. Three fix waves changed 37 product files after `38afa5c28e5b78d484a442cf6b8596fb2a5c34aa`, so P1-28-QA-005 re-froze the candidate — at `6392ccb4321b004ed12e5d04ad583298da3303dd` then, and at `3c75f49a01e35b507461bf0929b0046e7140860a` now, the two being product-identical — and every hosted figure above is recorded in `docs/phase-1/phase-1-28/evidence/closure-candidate.json` as PENDING at that candidate — marked `describesSupersededHead`, citing the run it really came from, and listed in `pendingHostedBindings`. No hosted run has been taken at the current candidate yet, so no hosted result is bound to this head; `validate:p1-28-evidence` refuses a cell asserting an observation AT THIS CANDIDATE while the package records none.",
    "FINDING_IDS": "None open against this row. Its day-one blockers G-APT-READ and G-PERM are both CLOSED (`contract-archaeology.md:149`, `:158`).",
    "DOCUMENTATION_EVIDENCE": "`canonical-plan.md` §5.1; `operator-guide.md` 'The appointment list'; `evidence/change-log.md` Wave C/F-G entries.",
    "FINAL_VERDICT": "PASS",
    "VERDICT_RATIONALE": "The bound reads exist and the screen consumes them for real: the request shape is asserted on the wire (`appointments-calendar.dom.test.tsx:145-156`), all five honest states are separately asserted, and the day queue's arrival affordance is proved to appear on exactly one lifecycle status and to be withdrawn without `rec.reception.manage`. Delete the status filter, the offset composition, the Check-in gating or the no-Confirm rule and a named case fails. The DoD I applied is the §5.1 binding plus §10's state and both-directions list; the accessibility axis is met by labelled regions and role-based access rather than by an axe run, and that gap is named above rather than implied."
  },
  "FE-002": {
    "IMPLEMENTATION_SURFACES": "`apps/web/src/features/appointments/components/AppointmentBookingScreen.tsx` (511 lines) with the shared `BranchTargetFields.tsx` and `WindowFields.tsx`, behind `apps/web/src/app/[locale]/(dashboard)/appointments/new/page.tsx:53` (gate) and `:57` (both catalogue reads, on the server, past the gate). Adapters: `apps/web/src/features/appointments/api.ts:166` (`createAppointment`), `catalogue-api.ts:98`/`:102`.",
    "NORMAL_NAVIGATION_ENTRY": "From the calendar, offered only to a holder of `apt.appointment.manage` (`AppointmentCalendarScreen` booking link; `appointments-calendar.dom.test.tsx:194-202`). There is no sidebar entry of its own, deliberately — booking is an action taken from the calendar.",
    "BACKEND_READ_CONTRACTS": "`apt.catalogue-appointment-type-list` and `apt.catalogue-source-channel-list` (GET /api/v1/appointment-catalogue/*), both registered since R3 — G-CAT-APT CLOSED. The customer-first vehicle pick reads `crm.customer-vehicle-list` through the P1-27 shared library (`apps/web/src/features/appointments/components/AppointmentBookingScreen.tsx:19`, called at `:388`), which is R4 and closes G-CUST-VEH.",
    "BACKEND_WRITE_CONTRACTS": "`apt.appointment-create` — POST /api/v1/appointments, mandatory Idempotency-Key, 201 `{appointmentId, displayNumber, lifecycleStatus:'requested', recordVersion:1}` + `ETag:\"1\"`. Wired and classified REACHABLE by the SEC-004 gate (`write-reachability.json`, under `apt.appointment-create`).",
    "REQUEST_SCHEMA": "Strict: `companyId`, `branchId`, `vehicleId`, `requesterPartnerId`, `appointmentTypeId` all uuid required; `sourceChannelId` nullable; `requestedFrom`/`requestedTo` must parse with an explicit UTC offset, To strictly after From. `apps/web/tests/appointments-booking.dom.test.tsx:249-285` asserts the exact body the operator chose, with offset-bearing instants; `apps/web/tests/appointments-window-support.test.ts` (10 cases) holds the instant composition itself.",
    "RESPONSE_SCHEMA": "201 + `ETag:\"1\"`; an idempotent replay answers 200 with NO ETag and the stored body, which `apps/web/tests/p1-28-qa.test.ts:675-706` asserts as SUCCESS rather than as a failure. A backend window refusal renders once, under the pair rather than against one half (`appointments-booking.dom.test.tsx:286-303`).",
    "REQUIRED_PERMISSIONS": "`apt.appointment.manage`, checked before either catalogue read (`new/page.tsx:53`), plus `crm.customer.read`/`veh.vehicle.read` for the resolution reads. `apps/web/tests/p1-28-appointment-routes.test.ts:168-183` asserts the gate precedes the reads — a denied operator spends no request.",
    "OWNER_REQUIREMENT_MAPPING": "Owner rows OR-1..5 (`owner-workflow-requirements.md:147-180`); journey steps S-1/S-4. OR-3 (the customer's own vehicles) is no longer degraded: the customer-first pick reads `crm.customer-vehicle-list` and states plainly when the customer has no linked vehicle (`appointments-booking.dom.test.tsx:212-226`).",
    "TENANT_ISOLATION_REQUIREMENT": "Branch target from the session's resolved scope only, passed as the operation's required field; no tenant key on any request (`p1-28-qa.test.ts:924-939`). The gate's rule 5 refuses a scope key in any query string across the two feature trees plus components and lib (`tests/ci/p1-28-access-gate.test.ts:472-528`).",
    "LOADING_STATE": "Server-rendered catalogues mean the first paint already has its pickers; the customer-vehicle table renders the shared loading state through `useServerTable`.",
    "EMPTY_STATE": "An empty appointment-type catalogue is stated as NOT CONFIGURED and booking is blocked honestly rather than with a control that would 422 — the unit case \"says no types are configured, and blocks booking honestly\", and the same sentence asserted against the running application in both locales at the browser tier’s \"an empty appointment-type catalogue blocks booking and says why\". An empty source-channel catalogue is recorded as an absence WITHOUT blocking, because that field is nullable (\"records the absence of channels without blocking the booking\").",
    "ZERO_RESULT_STATE": "No vehicles are listed until a customer is chosen (`appointments-booking.dom.test.tsx:197-202`), and a customer with no linked vehicle is stated as such rather than rendered as an error (`:212-226`).",
    "ERROR_STATE": "A FAILED catalogue read is 'unavailable', with its reference, and is a different rendered fact from 'empty' (`appointments-booking.dom.test.tsx:176-183`); a truncated catalogue walk is admitted rather than presented as complete (`:190-195`).",
    "DENIAL_STATE": "Route-level denial renders instead of the form, before any read (`p1-28-appointment-routes.test.ts:168-183`).",
    "CONFLICT_STATE": "N/A with rationale: create is unguarded — no `If-Match` — so there is no version conflict to render. Its own 409 is idempotency-key reuse, which the shared client maps as a conflict kind (`p1-28-qa.test.ts:561-589`).",
    "ARABIC_EVIDENCE": "`appointments.book.*` in `ar.json`; the screen renders in Arabic at `appointments-booking.dom.test.tsx:305-320`.",
    "ENGLISH_EVIDENCE": "Same keys in `en.json`, used by every other case in that suite.",
    "RTL_LTR_EVIDENCE": "Arabic case mounts under `dir=\"rtl\"`; the window fields keep the composed instants `dir=\"ltr\"` (`WindowFields.tsx`), and `p1-28-shared-components.dom.test.tsx:574-664` holds the field pair in both directions.",
    "ACCESSIBILITY_EVIDENCE": "Every control is reached by label in the suite; local refusals land on the field they are about and the server's window complaint renders once under the pair (`p1-28-shared-components.dom.test.tsx:609-641`). NAMED GAP: no axe run and no tablet-viewport case covers this screen.",
    "PRODUCTION_REACHABILITY": "Reachable from the calendar for a manage holder; one of the eight derived route pages (`tests/ci/p1-28-access-gate.test.ts:603`). `apt.appointment-create` is REACHABLE in the SEC-004 manifest, and the gate fails a REACHABLE claim whose call site is deleted (`tests/ci/p1-28-write-reachability.test.ts:280-290`).",
    "PRODUCTION_TEST_EVIDENCE": "`apps/web/tests/appointments-booking.dom.test.tsx` (12 cases), `apps/web/tests/appointments-window-support.test.ts` (8), `apps/web/tests/p1-28-appointment-routes.test.ts` (\"gates on manage and reads the catalogues only past the gate\"). All re-run green at this head. PRODUCTION-INTEGRATION evidence, which none of those is — every one mocks the transport — is the browser tier’s §18 booking case.",
    "NEGATIVE_OR_MUTATION_PROOF": "An incomplete form is refused locally with ZERO requests sent (\"refuses an incomplete form locally, without a request\"), and a window that ends before it starts is refused against the end field, also without a request (\"refuses a window that ends before it starts\"). Remove the offset composition and \"sends exactly what the operator chose, with offset-bearing instants\" fails on the wire shape rather than on a message. In the browser, §18 re-proves the local refusal against the running server by counting POSTs to the web origin — zero for an empty submit — which is the half a mock cannot distinguish from a swallowed request.",
    "PROTECTED_REPROOF": "PENDING PROTECTED MERGE — RE-OBSERVED for this re-derivation at `a2318c36`: the web tier ran 2669 cases, 0 failed and 0 skipped (28 P1-28 suites, 760 cases), and the root CI tier ran 2475 cases, 0 failed and 0 skipped (5 P1-28 gate suites, 180 cases). THE BROWSER TIER NOW CARRIES THIS PHASE — `apps/web/tests/e2e/authenticated/appointments-and-receptions.spec.ts` (2478 lines, against the running application, the running API and the real database), which the previous derivation correctly recorded as absent. It covers this row in BOTH tenant states, in one run: unconfigured at \"the booking screen states the blocked truth rather than offering a path\" (both locales), and CONFIGURED at §18 — \"an appointment is booked, confirmed by rescheduling, and cancelled with a catalogued reason\", whose booking half chooses a customer by name, that customer’s own vehicle, the catalogued type and channel and a future window, lands on the detail page and reads the type back — plus \"ar: the configured pickers offer their rows in Arabic, on both screens\". STILL NOT OBSERVED, and this is the whole outstanding debt on this row: that spec was NOT executed in this derivation — it needs the production launcher (`npm run acceptance:serve`), a live database and, for §18, a provisioned second workspace — and the run reported by the wave that landed it (319 expected, 0 unexpected, 0 flaky) is recorded in no phase document, so no browser result is bound to this head. THE TABLET VIEWPORT AND HOSTED CI WERE OBSERVED AT `38afa5c28e5b78d484a442cf6b8596fb2a5c34aa`, A HEAD THIS CANDIDATE SUPERSEDES. Two sentences stood here and both were false: that the `authenticated-tablet` project sees no P1-28 screen, and that no hosted-CI result exists for this head. `apps/web/playwright.config.ts:254-255` names that project and matches `(administration|appointments-and-receptions)`, so the phase surface is seen at 1024x768 as well as at 1440x900; run `31750364479` / job `94614564003` at head `38afa5c28e5b78d484a442cf6b8596fb2a5c34aa` executed `authenticated/appointments-and-receptions.spec.ts` 141 times — 47 in `authenticated-en`, 47 in `authenticated-ar`, 47 in `authenticated-tablet`, 0 failed and 0 skipped. Hosted CI at that head is run `31750364479`, 21 checks, 21 success, 0 failure, with `ci-gate` (`94617578352`) the single required check. The citation this cell used to give for the tablet claim pointed at lines 225 to 233 of the same config — the project DOCBLOCK, nine consecutive comment lines describing the state BEFORE the widening — so it read as confirmation of a sentence the code beneath it refutes; `validate:p1-28-evidence` now refuses a comment-only citation for exactly that reason. STILL OUTSTANDING ON THIS ROW: the protected-branch re-proof, because PR #226 has not merged, and OWNER ACCEPTANCE, which has not been requested or returned (`docs/phase-1/phase-1-28/evidence/change-log.md:5-11`). THAT HEAD IS NOT THE CURRENT CANDIDATE. Three fix waves changed 37 product files after `38afa5c28e5b78d484a442cf6b8596fb2a5c34aa`, so P1-28-QA-005 re-froze the candidate — at `6392ccb4321b004ed12e5d04ad583298da3303dd` then, and at `3c75f49a01e35b507461bf0929b0046e7140860a` now, the two being product-identical — and every hosted figure above is recorded in `docs/phase-1/phase-1-28/evidence/closure-candidate.json` as PENDING at that candidate — marked `describesSupersededHead`, citing the run it really came from, and listed in `pendingHostedBindings`. No hosted run has been taken at the current candidate yet, so no hosted result is bound to this head; `validate:p1-28-evidence` refuses a cell asserting an observation AT THIS CANDIDATE while the package records none.",
    "FINDING_IDS": "`P1-28-OD-001` OPEN — and it does not block this row’s own capability, which is now evidenced rather than argued. `apt.appointment_types` ships zero rows and NO SCREEN in this product reaches the management contracts PR #227 registered (`apt.catalogue-appointment-type-create`/`-update`/`-status-set`, behind `apt.catalogue.manage`, granted to no role by any seed — `supabase/seeds/04_iam_permission_catalog.sql:307-311`). No canonical P1-28 task binds a catalogue-administration screen (`canonical-plan.md` §7), and all 21 catalogue writes are classified `DELIBERATELY_ABSENT` against that decision in `write-reachability.json`. The day-one consequence — a fresh tenant cannot book until somebody configures it — is real, is recorded there, and belongs to that decision rather than to this row.",
    "DOCUMENTATION_EVIDENCE": "`canonical-plan.md` §5.1; `contract-archaeology.md` Table A rows A1 and A7-A9; `operator-guide.md` 'Booking an appointment'.",
    "FINAL_VERDICT": "PASS",
    "VERDICT_RATIONALE": "RE-ADJUDICATED at this head, and the verdict moves because the EVIDENCE moved, not because the standard did. RULE APPLIED, identically on every row of this file: a task is graded against THE CAPABILITY IT CANONICALLY OWNS — its §5 binding in `canonical-plan.md` — and against nothing else. A row does not stay PARTIAL because a fresh tenant starts empty, PROVIDED both halves are evidenced: its own journey works against a CONFIGURED tenant, and the UNCONFIGURED state is truthful. Equally, a row does not close while its own canonical journey is impossible, and a capability no canonical P1-28 task binds is not this register’s to withhold a verdict over. This row binds `apt.appointment-create` plus the two catalogue reads, and all three now work end to end in the running application: §18 books an appointment through this unchanged screen against a workspace whose catalogues were populated AT RUN TIME through the published management contracts an administrator would use, choosing the customer by name, that customer’s own vehicle, the catalogued type and the nullable channel, and reads the type and channel back off the stored record. The UNCONFIGURED half is asserted in the same run and was not weakened to obtain the configured one: a tenant with no types is told booking is impossible and the control is disabled, in both locales. What remains open is `P1-28-OD-001` — who administers the intake catalogues and through which surface — which blocks a screen NO canonical P1-28 task binds; withholding this row’s verdict for it would grade the row against scope this phase was never given, which is the P1-27 error of treating an adjudicated item as a canonical one. The row-local wire evidence is unchanged and still holds: the create body is asserted with offset-bearing instants, and empty, failed and truncated are three separately rendered facts."
  },
  "FE-003": {
    "IMPLEMENTATION_SURFACES": "`apps/web/src/features/appointments/components/AppointmentDetailScreen.tsx` (724 lines; the confirm-or-reschedule panel) behind `apps/web/src/app/[locale]/(dashboard)/appointments/[appointmentId]/page.tsx:65` (read gate), `:71` (the detail read that supplies `If-Match`) and `:108` (the manage capability). Adapter `apps/web/src/features/appointments/api.ts:192` (`rescheduleAppointment`).",
    "NORMAL_NAVIGATION_ENTRY": "Opened from a calendar row (`appointments-calendar.dom.test.tsx:186-192` asserts the push to `/en/appointments/{id}`) or directly by URL, which is what R2 made possible.",
    "BACKEND_READ_CONTRACTS": "`apt.appointment-detail` — the record being confirmed and the ONLY `recordVersion` source for an operator who arrived by URL; `apt.appointment-list` is the calendar that leads here. Both registered; G-APT-READ CLOSED (R2, PR #220).",
    "BACKEND_WRITE_CONTRACTS": "`apt.appointment-reschedule` — POST .../{appointmentId}/reschedule. Confirmation and reschedule are ONE operation (WF-02) and `pending_confirmation` is reachable by none, which the screen states rather than offering a control that promises to reach it (`apps/web/tests/appointment-detail.dom.test.tsx:170-177`). REACHABLE in `write-reachability.json`, under `apt.appointment-reschedule`.",
    "REQUEST_SCHEMA": "Strict `{confirmedFrom, confirmedTo}` with the same explicit-offset and strict-ordering rules as create; `If-Match` REQUIRED (428 `ERR-CON-002` missing, 409 `ERR-CON-001` stale). `appointment-detail.dom.test.tsx:199-229` asserts the version the page READ travels, and that both instants carry explicit offsets.",
    "RESPONSE_SCHEMA": "200 `{appointmentId, changeKind:'rescheduled', lifecycleStatus:'confirmed', recordVersion+1}` + ETag. `appointment-detail.dom.test.tsx:257-302` proves the NEXT command's version comes from the previous command RESPONSE rather than from arithmetic.",
    "REQUIRED_PERMISSIONS": "`apt.appointment.manage` — arranging, deliberately a different code from ending. `appointment-detail.dom.test.tsx:157-162` withholds the arranging panel from a non-holder; `p1-28-appointment-routes.test.ts:202-212` grants each property for its own code and nothing else.",
    "OWNER_REQUIREMENT_MAPPING": "Journey step S-4; WFP-04 exit criterion — the UI must never label a control 'Confirm' unless it truthfully calls this operation and says so (`docs/product/workshop/frontend-implementation-program.md:313-314`).",
    "TENANT_ISOLATION_REQUIREMENT": "Path parameter only; every path parameter is encoded so an identifier cannot escape its segment (`p1-28-qa.test.ts:1048-1062`). A cross-tenant appointment renders identically to a missing one (`p1-28-permission-route-binding.test.ts` \"renders a cross-tenant appointment identically to a missing one\").",
    "LOADING_STATE": "Server-rendered detail: the first paint is the record. The command panels use the shared action-state pending rendering.",
    "EMPTY_STATE": "N/A with rationale: a detail page has one record or none; 'none' is `NotFoundState` (`[appointmentId]/page.tsx:83`), not an empty list.",
    "ZERO_RESULT_STATE": "N/A with rationale: no filtered list on this surface.",
    "ERROR_STATE": "`ErrorState` with the read's correlation reference (`[appointmentId]/page.tsx:92-96`); an expired session is told to sign in with no retry (`:86-90`).",
    "DENIAL_STATE": "A client-side gate denial carries NO reference because no request was made (`[appointmentId]/page.tsx:65-69`), a backend denial carries the one it got (`:73-80`); `p1-28-appointment-routes.test.ts:225-234` asserts exactly that split.",
    "CONFLICT_STATE": "A lost race surfaces as a conflict with a re-read affordance (`appointment-detail.dom.test.tsx:239-256`); the two 409s are told apart because the cure is opposite (`p1-28-shared-components.dom.test.tsx:413-448`, which also fails CLOSED on an unrecognised conflict key).",
    "ARABIC_EVIDENCE": "`appointments.detail.*` and the affordance labels in `ar.json`; Arabic render at `appointment-detail.dom.test.tsx:421-441`.",
    "ENGLISH_EVIDENCE": "Same keys in `en.json` across the suite's 20 other cases.",
    "RTL_LTR_EVIDENCE": "Arabic case mounts RTL; record identifiers and instants stay `dir=\"ltr\"`.",
    "ACCESSIBILITY_EVIDENCE": "Controls reached by accessible name; local refusals land on the field they are about (`appointment-detail.dom.test.tsx:230-238` puts an inverted window against the END field). NAMED GAP: no axe run and no tablet case for this screen.",
    "PRODUCTION_REACHABILITY": "Reachable by row or URL; the route is in the derived eight (`tests/ci/p1-28-access-gate.test.ts:603`), and the reschedule write is REACHABLE in the SEC-004 manifest with its call site named.",
    "PRODUCTION_TEST_EVIDENCE": "`apps/web/tests/appointment-detail.dom.test.tsx` (22 cases incl. the affordance matrix driven from the transition graph) and `apps/web/tests/appointments-contract.test.ts:117-213` (the lifecycle mirror against the frozen CHECK constraint). Green at this head.",
    "NEGATIVE_OR_MUTATION_PROOF": "The affordance matrix is the transition graph rendered, asserted per status and explicitly NOT vacuous — the graph must offer all three commands somewhere and refuse all three somewhere (`appointment-detail.dom.test.tsx:126-156`). No control may say 'Confirm' without saying rescheduling (`:178-197`). An inverted window is refused locally with no request (`:230-238`).",
    "PROTECTED_REPROOF": "PENDING PROTECTED MERGE — RE-OBSERVED for this re-derivation at `a2318c36`: the web tier ran 2669 cases, 0 failed and 0 skipped (28 P1-28 suites, 760 cases), and the root CI tier ran 2475 cases, 0 failed and 0 skipped (5 P1-28 gate suites, 180 cases). THE BROWSER TIER NOW CARRIES THIS PHASE — `apps/web/tests/e2e/authenticated/appointments-and-receptions.spec.ts` (2478 lines, against the running application, the running API and the real database), which the previous derivation correctly recorded as absent. It covers this row at \"reschedule, cancel and no-show are panels on a real appointment\" (the three lifecycle panels absent over a record that could not be read) and, against a real record, at §18 \"an appointment is booked, confirmed by rescheduling, and cancelled with a catalogued reason\", where the status moves to `confirmed` through this operation and no other. STILL NOT OBSERVED, and this is the whole outstanding debt on this row: that spec was NOT executed in this derivation — it needs the production launcher (`npm run acceptance:serve`), a live database and, for §18, a provisioned second workspace — and the run reported by the wave that landed it (319 expected, 0 unexpected, 0 flaky) is recorded in no phase document, so no browser result is bound to this head. THE TABLET VIEWPORT AND HOSTED CI WERE OBSERVED AT `38afa5c28e5b78d484a442cf6b8596fb2a5c34aa`, A HEAD THIS CANDIDATE SUPERSEDES. Two sentences stood here and both were false: that the `authenticated-tablet` project sees no P1-28 screen, and that no hosted-CI result exists for this head. `apps/web/playwright.config.ts:254-255` names that project and matches `(administration|appointments-and-receptions)`, so the phase surface is seen at 1024x768 as well as at 1440x900; run `31750364479` / job `94614564003` at head `38afa5c28e5b78d484a442cf6b8596fb2a5c34aa` executed `authenticated/appointments-and-receptions.spec.ts` 141 times — 47 in `authenticated-en`, 47 in `authenticated-ar`, 47 in `authenticated-tablet`, 0 failed and 0 skipped. Hosted CI at that head is run `31750364479`, 21 checks, 21 success, 0 failure, with `ci-gate` (`94617578352`) the single required check. The citation this cell used to give for the tablet claim pointed at lines 225 to 233 of the same config — the project DOCBLOCK, nine consecutive comment lines describing the state BEFORE the widening — so it read as confirmation of a sentence the code beneath it refutes; `validate:p1-28-evidence` now refuses a comment-only citation for exactly that reason. STILL OUTSTANDING ON THIS ROW: the protected-branch re-proof, because PR #226 has not merged, and OWNER ACCEPTANCE, which has not been requested or returned (`docs/phase-1/phase-1-28/evidence/change-log.md:5-11`). THAT HEAD IS NOT THE CURRENT CANDIDATE. Three fix waves changed 37 product files after `38afa5c28e5b78d484a442cf6b8596fb2a5c34aa`, so P1-28-QA-005 re-froze the candidate — at `6392ccb4321b004ed12e5d04ad583298da3303dd` then, and at `3c75f49a01e35b507461bf0929b0046e7140860a` now, the two being product-identical — and every hosted figure above is recorded in `docs/phase-1/phase-1-28/evidence/closure-candidate.json` as PENDING at that candidate — marked `describesSupersededHead`, citing the run it really came from, and listed in `pendingHostedBindings`. No hosted run has been taken at the current candidate yet, so no hosted result is bound to this head; `validate:p1-28-evidence` refuses a cell asserting an observation AT THIS CANDIDATE while the package records none.",
    "FINDING_IDS": "None open. G-APT-READ, the day-one blocker on this row's If-Match sourcing, is CLOSED.",
    "DOCUMENTATION_EVIDENCE": "`canonical-plan.md` §5.1 and §10 (truthful labelling); `operator-guide.md` 'Confirming an appointment'; `p1-28-guidance-reconciliation.test.ts` \"‘there is no Confirm button’ — the control really says what it calls\" pins that sentence to the control's own source.",
    "FINAL_VERDICT": "PASS",
    "VERDICT_RATIONALE": "The one operation this row binds is wired end to end with its version sourced from the read the page performed, asserted on the wire (`apps/web/tests/appointment-detail.dom.test.tsx:199-229`) and re-sourced from the command response afterwards (`:257-302`). The phase’s riskiest labelling rule is enforced negatively in two places — no control claims to confirm without naming rescheduling, and `pending_confirmation` is rendered as reachable by nothing. The affordance set is derived from the frozen transition graph with an anti-vacuity case, so a graph that offered everything or nothing would fail. RE-ADJUDICATED at this head and UNCHANGED at PASS, with one thing that DID change: the precondition this row used to state — that no appointment could be created in the product at all — is refuted by the browser tier, which books one and then confirms it by rescheduling through this very panel. RULE APPLIED, identically on every row of this file: a task is graded against THE CAPABILITY IT CANONICALLY OWNS — its §5 binding in `canonical-plan.md` — and against nothing else. A row does not stay PARTIAL because a fresh tenant starts empty, PROVIDED both halves are evidenced: its own journey works against a CONFIGURED tenant, and the UNCONFIGURED state is truthful. Equally, a row does not close while its own canonical journey is impossible, and a capability no canonical P1-28 task binds is not this register’s to withhold a verdict over."
  },
  "FE-004": {
    "IMPLEMENTATION_SURFACES": "The cancellation dialog of `apps/web/src/features/appointments/components/AppointmentDetailScreen.tsx`, fed by `apps/web/src/app/[locale]/(dashboard)/appointments/[appointmentId]/page.tsx:98-101`, which reads the reason catalogue ONLY for a holder of the lifecycle permission. Adapter `apps/web/src/features/appointments/api.ts:220` (`cancelAppointment`), catalogue `catalogue-api.ts:112`.",
    "NORMAL_NAVIGATION_ENTRY": "The appointment detail page, from a calendar row or by URL; the control appears only where the transition graph has the edge (`appointment-detail.dom.test.tsx:126-149`).",
    "BACKEND_READ_CONTRACTS": "`apt.catalogue-cancellation-reason-list` (GET /api/v1/appointment-catalogue/cancellation-reasons), registered since R3 — the READ gap is closed. `apt.appointment-detail` supplies the `recordVersion`.",
    "BACKEND_WRITE_CONTRACTS": "`apt.appointment-cancel` — POST .../cancel, `If-Match` required, Idempotency-Key mandatory. REACHABLE in `write-reachability.json`, under `apt.appointment-cancel` with the dialog named as its call site.",
    "REQUEST_SCHEMA": "Strict `{cancellationReasonId: uuid}` — mandatory and catalogued, with no free-text escape; an unknown id answers 422 `unknown_reference`. `appointment-detail.dom.test.tsx:304-333` asserts the chosen reason travels with the READ version; `:334-342` proves the form cannot be submitted without one.",
    "RESPONSE_SCHEMA": "200 `{appointmentId, changeKind:'cancelled', lifecycleStatus:'cancelled', recordVersion}` + ETag; the audit records the previous status and the reason.",
    "REQUIRED_PERMISSIONS": "`apt.appointment.lifecycle.manage` — ENDING, a DIFFERENT code from create/reschedule's `apt.appointment.manage`. `appointment-detail.dom.test.tsx:163-169` withholds ending from a non-holder; `p1-28-appointment-routes.test.ts:213-224` proves the catalogue is read only for a lifecycle holder; `p1-28-security.test.ts:149-180` keeps arranging and ending on separate codes.",
    "OWNER_REQUIREMENT_MAPPING": "Journey step S-4 lifecycle handling (`docs/product/workshop/end-to-end-workshop-workflow.md` §5).",
    "TENANT_ISOLATION_REQUIREMENT": "Path parameter, encoded; no scope key in any query string (gate rule 5). Cross-tenant renders as not-found.",
    "LOADING_STATE": "Catalogue read server-side before the dialog renders; the command uses the shared pending action state.",
    "EMPTY_STATE": "An EMPTY reason catalogue is stated as unpopulated, NOT as broken (\"says an EMPTY reason catalogue is unpopulated, not broken\"). A POPULATED one is proved against the running application at §18, where the reason is offered by name and chosen by the identifier behind it.",
    "ZERO_RESULT_STATE": "N/A with rationale: the picker is a catalogue, not a filtered search.",
    "ERROR_STATE": "A FAILED catalogue read is 'unavailable' with its reference — a different rendered fact from empty (`appointment-detail.dom.test.tsx:355-365`).",
    "DENIAL_STATE": "The ending panel is withdrawn with its reason rather than greyed out (`appointment-detail.dom.test.tsx:163-169`); route-level and backend denials are distinguished by whether a reference exists.",
    "CONFLICT_STATE": "Shared `CommandOutcome` tells `ERR-CON-001` (stale, cured by the re-read just performed) from every other 409 (blocked), failing closed on an unrecognised key (`p1-28-shared-components.dom.test.tsx:413-448`).",
    "ARABIC_EVIDENCE": "`appointments.cancel.*` in `ar.json`; the Arabic detail render exercises the panel (`appointment-detail.dom.test.tsx:421-441`).",
    "ENGLISH_EVIDENCE": "Same keys in `en.json`.",
    "RTL_LTR_EVIDENCE": "Rendered inside the RTL detail page; identifiers stay LTR.",
    "ACCESSIBILITY_EVIDENCE": "The reason select is reached by label and the refusal is announced through the shared alert region. NAMED GAP: no axe run, no tablet case.",
    "PRODUCTION_REACHABILITY": "Reachable from the detail page for a lifecycle holder; REACHABLE in the SEC-004 manifest.",
    "PRODUCTION_TEST_EVIDENCE": "`apps/web/tests/appointment-detail.dom.test.tsx` — \"sends the chosen reason with the read version\", \"refuses to submit without a reason — there is no free-text escape\", \"says an EMPTY reason catalogue is unpopulated, not broken\", \"says a FAILED catalogue read is unavailable, with the reference\" — plus the shared conflict cases and `apps/web/tests/appointments-contract.test.ts` \"splits arranging from ending across two permission codes\". Production-integration evidence, which none of those is: the browser tier’s §18 cancellation.",
    "NEGATIVE_OR_MUTATION_PROOF": "There is no free-text escape: submitting without a reason sends nothing (`appointment-detail.dom.test.tsx:334-342`). Remove the lifecycle gate and `p1-28-appointment-routes.test.ts:213-224` fails, because the catalogue read would happen for a non-holder.",
    "PROTECTED_REPROOF": "PENDING PROTECTED MERGE — RE-OBSERVED for this re-derivation at `a2318c36`: the web tier ran 2669 cases, 0 failed and 0 skipped (28 P1-28 suites, 760 cases), and the root CI tier ran 2475 cases, 0 failed and 0 skipped (5 P1-28 gate suites, 180 cases). THE BROWSER TIER NOW CARRIES THIS PHASE — `apps/web/tests/e2e/authenticated/appointments-and-receptions.spec.ts` (2478 lines, against the running application, the running API and the real database), which the previous derivation correctly recorded as absent. It covers this row at §18 \"an appointment is booked, confirmed by rescheduling, and cancelled with a catalogued reason\": the dialog refuses to submit with no reason and spends no request, the catalogued reason is offered BY NAME, the appointment reaches `cancelled` and the stored record names the reason back; a terminal appointment then offers no further lifecycle control. The unconfigured half stays asserted at \"reschedule, cancel and no-show are panels on a real appointment\". STILL NOT OBSERVED, and this is the whole outstanding debt on this row: that spec was NOT executed in this derivation — it needs the production launcher (`npm run acceptance:serve`), a live database and, for §18, a provisioned second workspace — and the run reported by the wave that landed it (319 expected, 0 unexpected, 0 flaky) is recorded in no phase document, so no browser result is bound to this head. THE TABLET VIEWPORT AND HOSTED CI WERE OBSERVED AT `38afa5c28e5b78d484a442cf6b8596fb2a5c34aa`, A HEAD THIS CANDIDATE SUPERSEDES. Two sentences stood here and both were false: that the `authenticated-tablet` project sees no P1-28 screen, and that no hosted-CI result exists for this head. `apps/web/playwright.config.ts:254-255` names that project and matches `(administration|appointments-and-receptions)`, so the phase surface is seen at 1024x768 as well as at 1440x900; run `31750364479` / job `94614564003` at head `38afa5c28e5b78d484a442cf6b8596fb2a5c34aa` executed `authenticated/appointments-and-receptions.spec.ts` 141 times — 47 in `authenticated-en`, 47 in `authenticated-ar`, 47 in `authenticated-tablet`, 0 failed and 0 skipped. Hosted CI at that head is run `31750364479`, 21 checks, 21 success, 0 failure, with `ci-gate` (`94617578352`) the single required check. The citation this cell used to give for the tablet claim pointed at lines 225 to 233 of the same config — the project DOCBLOCK, nine consecutive comment lines describing the state BEFORE the widening — so it read as confirmation of a sentence the code beneath it refutes; `validate:p1-28-evidence` now refuses a comment-only citation for exactly that reason. STILL OUTSTANDING ON THIS ROW: the protected-branch re-proof, because PR #226 has not merged, and OWNER ACCEPTANCE, which has not been requested or returned (`docs/phase-1/phase-1-28/evidence/change-log.md:5-11`). THAT HEAD IS NOT THE CURRENT CANDIDATE. Three fix waves changed 37 product files after `38afa5c28e5b78d484a442cf6b8596fb2a5c34aa`, so P1-28-QA-005 re-froze the candidate — at `6392ccb4321b004ed12e5d04ad583298da3303dd` then, and at `3c75f49a01e35b507461bf0929b0046e7140860a` now, the two being product-identical — and every hosted figure above is recorded in `docs/phase-1/phase-1-28/evidence/closure-candidate.json` as PENDING at that candidate — marked `describesSupersededHead`, citing the run it really came from, and listed in `pendingHostedBindings`. No hosted run has been taken at the current candidate yet, so no hosted result is bound to this head; `validate:p1-28-evidence` refuses a cell asserting an observation AT THIS CANDIDATE while the package records none.",
    "FINDING_IDS": "`P1-28-OD-001` OPEN, and it does not block this row’s own capability. `apt.cancellation_reasons` ships zero rows; the management contracts exist since PR #227 (`apt.catalogue-cancellation-reason-create`/`-update`/`-status-set` behind `apt.catalogue.manage`) and no screen in this product reaches them, because no canonical P1-28 task binds a catalogue-administration surface. The day-one consequence — a fresh tenant cannot cancel until somebody configures it — is recorded against that decision in `canonical-plan.md` §7, where the surface question lives.",
    "DOCUMENTATION_EVIDENCE": "`canonical-plan.md` §5.1; `contract-archaeology.md` Table A row A3; `operator-guide.md` 'Cancelling, and recording a no-show'.",
    "FINAL_VERDICT": "PASS",
    "VERDICT_RATIONALE": "RE-ADJUDICATED at this head on new evidence. RULE APPLIED, identically on every row of this file: a task is graded against THE CAPABILITY IT CANONICALLY OWNS — its §5 binding in `canonical-plan.md` — and against nothing else. A row does not stay PARTIAL because a fresh tenant starts empty, PROVIDED both halves are evidenced: its own journey works against a CONFIGURED tenant, and the UNCONFIGURED state is truthful. Equally, a row does not close while its own canonical journey is impossible, and a capability no canonical P1-28 task binds is not this register’s to withhold a verdict over. This row binds `apt.appointment-cancel` and its reason catalogue, and §18 exercises both against the running application: with the catalogue configured the dialog offers the reason BY NAME, refuses to submit without one while spending no request, sends the identifier behind the name with the read’s version, drives the appointment to `cancelled`, and the STORED record names the reason back; a terminal appointment then offers no further lifecycle control. The unconfigured statement is still asserted and still true, so nothing was deleted to obtain the configured proof. The contrast with FE-019 the previous derivation drew is sharper rather than weaker: there the catalogued field is OPTIONAL so an empty catalogue degrades the row, here it is MANDATORY so an empty catalogue stops a fresh tenant — but that is what `P1-28-OD-001` records, and that decision blocks an administration screen this register does not bind."
  },
  "FE-005": {
    "IMPLEMENTATION_SURFACES": "The no-show panel of `apps/web/src/features/appointments/components/AppointmentDetailScreen.tsx`; adapter `apps/web/src/features/appointments/api.ts:249` (`recordAppointmentNoShow`).",
    "NORMAL_NAVIGATION_ENTRY": "The appointment detail page; the control appears only from `confirmed`, which is where the frozen graph has the edge (`appointments-contract.test.ts:198-206`).",
    "BACKEND_READ_CONTRACTS": "`apt.appointment-detail` for the `If-Match` and for showing which appointment is being marked. Registered; G-APT-READ CLOSED.",
    "BACKEND_WRITE_CONTRACTS": "`apt.appointment-no-show` — POST .../no-show. REACHABLE in `write-reachability.json`, under `apt.appointment-no-show`.",
    "REQUEST_SCHEMA": "Body `{}` or null, strict; `If-Match` REQUIRED; legal from `confirmed` ONLY. `appointment-detail.dom.test.tsx:367-389` asserts the read version is sent with no body input, after an explicit confirmation step.",
    "RESPONSE_SCHEMA": "200 `{appointmentId, changeKind:'no_show_recorded', lifecycleStatus:'no_show', recordVersion}` + ETag.",
    "REQUIRED_PERMISSIONS": "`apt.appointment.lifecycle.manage` (the same ENDING authority as cancel, and not the arranging code) — withheld and asserted at `appointment-detail.dom.test.tsx:163-169` and `p1-28-appointment-routes.test.ts:202-212`.",
    "OWNER_REQUIREMENT_MAPPING": "Journey step S-4 (`end-to-end-workshop-workflow.md` §5).",
    "TENANT_ISOLATION_REQUIREMENT": "Path parameter, encoded; cross-tenant renders as not-found (`p1-28-permission-route-binding.test.ts` \"renders a cross-tenant appointment identically to a missing one\").",
    "LOADING_STATE": "Shared pending action state on the command button.",
    "EMPTY_STATE": "N/A with rationale: the command takes no list and no input.",
    "ZERO_RESULT_STATE": "N/A with rationale: no filtered list on this surface.",
    "ERROR_STATE": "Shared failure rendering with the correlation reference; a transport failure is never reported as success (`p1-28-qa.test.ts:452-486`).",
    "DENIAL_STATE": "Withdrawn with the reason for a non-holder of the lifecycle code; a backend 403 renders as a denial and never as invalid input (`p1-28-qa.test.ts:535-548`).",
    "CONFLICT_STATE": "The state-machine refusal is surfaced as a conflict that re-reading does NOT cure — 409 `ERR-TRN-001` — and is told apart from a stale version (`appointment-detail.dom.test.tsx:390-406`).",
    "ARABIC_EVIDENCE": "`appointments.noShow.*` in `ar.json`; exercised by the Arabic detail render.",
    "ENGLISH_EVIDENCE": "Same keys in `en.json`.",
    "RTL_LTR_EVIDENCE": "Rendered inside the RTL detail page.",
    "ACCESSIBILITY_EVIDENCE": "A destructive command behind an explicit confirmation, reached by accessible name. NAMED GAP: no axe run, no tablet case.",
    "PRODUCTION_REACHABILITY": "Reachable from the detail page on a confirmed appointment; REACHABLE in the SEC-004 manifest.",
    "PRODUCTION_TEST_EVIDENCE": "`apps/web/tests/appointment-detail.dom.test.tsx` — \"confirms before sending, then sends the read version with no body input\" and \"surfaces the state-machine refusal as a conflict that re-reading does not cure\" — plus `apps/web/tests/appointments-contract.test.ts` \"offers no-show from confirmed ONLY\". Production-integration evidence: the browser tier’s §18 no-show case.",
    "NEGATIVE_OR_MUTATION_PROOF": "`appointments-contract.test.ts:198-206` asserts the affordance exists from `confirmed` and from NO other status — widen it and the case fails. `appointment-detail.dom.test.tsx:390-406` asserts the 409 is presented as a refusal a re-read does not fix, which is the opposite treatment from a stale version.",
    "PROTECTED_REPROOF": "PENDING PROTECTED MERGE — RE-OBSERVED for this re-derivation at `a2318c36`: the web tier ran 2669 cases, 0 failed and 0 skipped (28 P1-28 suites, 760 cases), and the root CI tier ran 2475 cases, 0 failed and 0 skipped (5 P1-28 gate suites, 180 cases). THE BROWSER TIER NOW CARRIES THIS PHASE — `apps/web/tests/e2e/authenticated/appointments-and-receptions.spec.ts` (2478 lines, against the running application, the running API and the real database), which the previous derivation correctly recorded as absent. It covers this row at §18 \"a confirmed appointment is marked as a no-show\", which first proves a merely `requested` appointment offers NO no-show control, then confirms by rescheduling and reaches `no_show`. STILL NOT OBSERVED, and this is the whole outstanding debt on this row: that spec was NOT executed in this derivation — it needs the production launcher (`npm run acceptance:serve`), a live database and, for §18, a provisioned second workspace — and the run reported by the wave that landed it (319 expected, 0 unexpected, 0 flaky) is recorded in no phase document, so no browser result is bound to this head. THE TABLET VIEWPORT AND HOSTED CI WERE OBSERVED AT `38afa5c28e5b78d484a442cf6b8596fb2a5c34aa`, A HEAD THIS CANDIDATE SUPERSEDES. Two sentences stood here and both were false: that the `authenticated-tablet` project sees no P1-28 screen, and that no hosted-CI result exists for this head. `apps/web/playwright.config.ts:254-255` names that project and matches `(administration|appointments-and-receptions)`, so the phase surface is seen at 1024x768 as well as at 1440x900; run `31750364479` / job `94614564003` at head `38afa5c28e5b78d484a442cf6b8596fb2a5c34aa` executed `authenticated/appointments-and-receptions.spec.ts` 141 times — 47 in `authenticated-en`, 47 in `authenticated-ar`, 47 in `authenticated-tablet`, 0 failed and 0 skipped. Hosted CI at that head is run `31750364479`, 21 checks, 21 success, 0 failure, with `ci-gate` (`94617578352`) the single required check. The citation this cell used to give for the tablet claim pointed at lines 225 to 233 of the same config — the project DOCBLOCK, nine consecutive comment lines describing the state BEFORE the widening — so it read as confirmation of a sentence the code beneath it refutes; `validate:p1-28-evidence` now refuses a comment-only citation for exactly that reason. STILL OUTSTANDING ON THIS ROW: the protected-branch re-proof, because PR #226 has not merged, and OWNER ACCEPTANCE, which has not been requested or returned (`docs/phase-1/phase-1-28/evidence/change-log.md:5-11`). THAT HEAD IS NOT THE CURRENT CANDIDATE. Three fix waves changed 37 product files after `38afa5c28e5b78d484a442cf6b8596fb2a5c34aa`, so P1-28-QA-005 re-froze the candidate — at `6392ccb4321b004ed12e5d04ad583298da3303dd` then, and at `3c75f49a01e35b507461bf0929b0046e7140860a` now, the two being product-identical — and every hosted figure above is recorded in `docs/phase-1/phase-1-28/evidence/closure-candidate.json` as PENDING at that candidate — marked `describesSupersededHead`, citing the run it really came from, and listed in `pendingHostedBindings`. No hosted run has been taken at the current candidate yet, so no hosted result is bound to this head; `validate:p1-28-evidence` refuses a cell asserting an observation AT THIS CANDIDATE while the package records none.",
    "FINDING_IDS": "None open.",
    "DOCUMENTATION_EVIDENCE": "`canonical-plan.md` §5.1; `operator-guide.md` 'Cancelling, and recording a no-show'.",
    "FINAL_VERDICT": "PASS",
    "VERDICT_RATIONALE": "The only operation this row binds is wired, version-guarded from the page’s own read, offered from exactly the one status the frozen graph permits, and its 409 is rendered as the state refusal it is rather than as something a retry would cure. UNCHANGED at PASS, on stronger evidence than before: §18 marks a real appointment as a no-show against the running stack, and proves the affordance is ABSENT while the appointment is merely `requested` — the single-status rule observed in the product rather than only in a mock. RULE APPLIED, identically on every row of this file: a task is graded against THE CAPABILITY IT CANONICALLY OWNS — its §5 binding in `canonical-plan.md` — and against nothing else. A row does not stay PARTIAL because a fresh tenant starts empty, PROVIDED both halves are evidenced: its own journey works against a CONFIGURED tenant, and the UNCONFIGURED state is truthful. Equally, a row does not close while its own canonical journey is impossible, and a capability no canonical P1-28 task binds is not this register’s to withhold a verdict over."
  },
  "FE-006": {
    "IMPLEMENTATION_SURFACES": "`apps/web/src/features/receptions/intake/components/WalkInIntakeScreen.tsx` (568 lines) with `IntakeCustomerCreate.tsx` (353) and `IntakeVehicleStep.tsx` (1001), behind `apps/web/src/app/[locale]/(dashboard)/reception/walk-in/page.tsx:42` (gate) and `:68-84` (screen, capabilities resolved from the session). The handoff address is `apps/web/src/features/receptions/intake/intake-handoff.ts`.",
    "NORMAL_NAVIGATION_ENTRY": "Sidebar `walk-in` → `/reception/walk-in`, gated on `crm.customer.read` — the code the flow's FIRST operation requires (`apps/web/src/config/navigation.ts:129-137`), pinned available by `navigation.test.ts:85-88`.",
    "BACKEND_READ_CONTRACTS": "`crm.customer-search` (PARTIAL — no contact-point query, G-CRM-PHONE/R7 open), `crm.customer-read`, `crm.customer-vehicle-list` (R4, consumed at `IntakeVehicleStep.tsx:185`), `veh.vehicle-search` and the vehicle creation catalogues, all through the P1-27 public modules `@/components/party` and `@/lib/customers` — never `features/crm` internals.",
    "BACKEND_WRITE_CONTRACTS": "`crm.individual-create`, `crm.company-create`, `veh.vehicle-create` (lands `draft` always) and `crm.vehicle-link` — all four exercised through the P1-27 adapters, each bound to its OWN operation and never to the other (`p1-28-shared-components.dom.test.tsx:722-751`).",
    "REQUEST_SCHEMA": "Per the P1-27 creation contracts; the link takes `{vehicleId, relationshipRole}`. `reception-walkin.dom.test.tsx:374-395` asserts the chosen customer, vehicle and role travel on the link.",
    "RESPONSE_SCHEMA": "201 per P1-27; `possibleDuplicates` arrives on the CREATION response and the screen states the creation FIRST, then offers the duplicate decision (`p1-28-shared-components.dom.test.tsx:670-702`; `reception-walkin.dom.test.tsx:295-326` switches to the existing customer).",
    "REQUIRED_PERMISSIONS": "`crm.customer.read` (page), `crm.customer.create`, `veh.vehicle.read`, `veh.vehicle.manage`, `crm.customer.vehicle.manage` — each resolved at `walk-in/page.tsx:71-74` and each the code the operation behind its control requires. Every one is withdrawn WITH its reason rather than hidden (`reception-walkin.dom.test.tsx:258-268`, `:407-436`, `:484-491`).",
    "OWNER_REQUIREMENT_MAPPING": "Owner rows OR-1..5; journey step S-5. OR-3 is no longer blocked: the customer's own vehicles list is read directly (`reception-walkin.dom.test.tsx:328-345`) rather than degrading to a vehicle-first search.",
    "TENANT_ISOLATION_REQUIREMENT": "No client-asserted scope anywhere on the screen; the gate's rule 6 refuses a `holds()` call inside the feature trees at all, so the route owns every capability decision (`tests/ci/p1-28-access-gate.test.ts:552-579`).",
    "LOADING_STATE": "Shared table loading through `useServerTable` on both search surfaces.",
    "EMPTY_STATE": "A customer with no linked vehicle is stated as a fact (`reception-walkin.dom.test.tsx:328-345` covers the populated case; the empty case is the same panel's own statement), and the boundary where neither search nor create is available is stated rather than left blank (`:427-436`).",
    "ZERO_RESULT_STATE": "Search surfaces use the shared no-results state; the phone degradation is stated beside the controls so a zero-result search is not misread as 'this person is not a customer' (`:213-226`).",
    "ERROR_STATE": "Retry is offered on an unavailable read and re-reads on press (`reception-walkin.dom.test.tsx:587-602`).",
    "DENIAL_STATE": "A refused vehicle-list read renders the denied state with its reference, never an empty list (`reception-walkin.dom.test.tsx:575-586`).",
    "CONFLICT_STATE": "A vehicle-create 409 renders the honest copy and points at the search instead of guessing which unique index refused (`reception-walkin.dom.test.tsx:457-483`) — the `veh.vehicles` two-index trap.",
    "ARABIC_EVIDENCE": "The whole customer-first path is driven end to end in Arabic (`reception-walkin.dom.test.tsx:553-573`), and the phone degradation is stated in Arabic (`:220-226`).",
    "ENGLISH_EVIDENCE": "Same keys in `en.json` across the suite's other 22 cases.",
    "RTL_LTR_EVIDENCE": "Arabic cases mount RTL; `p1-28-shared-components.dom.test.tsx:752-772` renders the creation panel in Arabic with the document in RTL.",
    "ACCESSIBILITY_EVIDENCE": "Steps are reached by accessible name and the notice is a labelled `role`-bearing region (`WalkInIntakeScreen.tsx:301-320`). NAMED GAP: no axe run and no tablet case cover this screen.",
    "PRODUCTION_REACHABILITY": "Reachable from the sidebar; the singular `/reception/walk-in` page is one of the eight the access gate derives BY WHAT IT LOADS, which is how the odd singular segment stays governed (`tests/ci/p1-28-access-gate.test.ts:603-611`).",
    "PRODUCTION_TEST_EVIDENCE": "`apps/web/tests/reception-walkin.dom.test.tsx` (24 cases), `apps/web/tests/reception-walkin-handoff.test.ts` (12), `apps/web/tests/p1-28-shared-components.dom.test.tsx:666-857` (IntakeCustomerCreate and IntakeVehicleStep). Green at this head.",
    "NEGATIVE_OR_MUTATION_PROOF": "No phone box is offered that could fail silently (`reception-walkin.dom.test.tsx:227-233`), and a relationship row whose vehicle record is gone cannot be chosen (`:346-359`). The handoff constant is proved to resolve to a route directory that really exists and to the PLURAL segment (`reception-walkin-handoff.test.ts:38-58`) — while that constant was wrong nothing could tell, because the only code reading it never rendered.",
    "PROTECTED_REPROOF": "PENDING PROTECTED MERGE — RE-OBSERVED for this re-derivation at `a2318c36`: the web tier ran 2669 cases, 0 failed and 0 skipped (28 P1-28 suites, 760 cases), and the root CI tier ran 2475 cases, 0 failed and 0 skipped (5 P1-28 gate suites, 180 cases). THE BROWSER TIER NOW CARRIES THIS PHASE — `apps/web/tests/e2e/authenticated/appointments-and-receptions.spec.ts` (2478 lines, against the running application, the running API and the real database), which the previous derivation correctly recorded as absent. It covers this row at \"the walk-in intake hands off to check-in\" (the address the intake builds resolves to the check-in screen in the running application; a pair that does not resolve starts the screen empty rather than erroring) and at \"the walk-in intake confirms a customer before a vehicle\", in both locales. STILL NOT OBSERVED, and this is the whole outstanding debt on this row: that spec was NOT executed in this derivation — it needs the production launcher (`npm run acceptance:serve`), a live database and, for §18, a provisioned second workspace — and the run reported by the wave that landed it (319 expected, 0 unexpected, 0 flaky) is recorded in no phase document, so no browser result is bound to this head. THE TABLET VIEWPORT AND HOSTED CI WERE OBSERVED AT `38afa5c28e5b78d484a442cf6b8596fb2a5c34aa`, A HEAD THIS CANDIDATE SUPERSEDES. Two sentences stood here and both were false: that the `authenticated-tablet` project sees no P1-28 screen, and that no hosted-CI result exists for this head. `apps/web/playwright.config.ts:254-255` names that project and matches `(administration|appointments-and-receptions)`, so the phase surface is seen at 1024x768 as well as at 1440x900; run `31750364479` / job `94614564003` at head `38afa5c28e5b78d484a442cf6b8596fb2a5c34aa` executed `authenticated/appointments-and-receptions.spec.ts` 141 times — 47 in `authenticated-en`, 47 in `authenticated-ar`, 47 in `authenticated-tablet`, 0 failed and 0 skipped. Hosted CI at that head is run `31750364479`, 21 checks, 21 success, 0 failure, with `ci-gate` (`94617578352`) the single required check. The citation this cell used to give for the tablet claim pointed at lines 225 to 233 of the same config — the project DOCBLOCK, nine consecutive comment lines describing the state BEFORE the widening — so it read as confirmation of a sentence the code beneath it refutes; `validate:p1-28-evidence` now refuses a comment-only citation for exactly that reason. STILL OUTSTANDING ON THIS ROW: the protected-branch re-proof, because PR #226 has not merged, and OWNER ACCEPTANCE, which has not been requested or returned (`docs/phase-1/phase-1-28/evidence/change-log.md:5-11`). THAT HEAD IS NOT THE CURRENT CANDIDATE. Three fix waves changed 37 product files after `38afa5c28e5b78d484a442cf6b8596fb2a5c34aa`, so P1-28-QA-005 re-froze the candidate — at `6392ccb4321b004ed12e5d04ad583298da3303dd` then, and at `3c75f49a01e35b507461bf0929b0046e7140860a` now, the two being product-identical — and every hosted figure above is recorded in `docs/phase-1/phase-1-28/evidence/closure-candidate.json` as PENDING at that candidate — marked `describesSupersededHead`, citing the run it really came from, and listed in `pendingHostedBindings`. No hosted run has been taken at the current candidate yet, so no hosted result is bound to this head; `validate:p1-28-evidence` refuses a cell asserting an observation AT THIS CANDIDATE while the package records none.",
    "FINDING_IDS": "`G-CRM-PHONE` OPEN (R7, owned P1-16) — a walk-in cannot be found by telephone number. Stated on screen in both languages rather than papered over; it binds no operation in §5.2, so it degrades this row rather than blocking it. `G-CUST-VEH` CLOSED by R4.",
    "DOCUMENTATION_EVIDENCE": "`canonical-plan.md` §5.2; `operator-guide.md` 'Walk-in intake'; `p1-28-guidance-reconciliation.test.ts` \"‘you cannot search for a customer by telephone number’ — and the screen says so\" pins the telephone sentence to the screen that states it.",
    "FINAL_VERDICT": "PASS",
    "VERDICT_RATIONALE": "All six bound operations are reached through P1-27's public modules and each is proved to be bound to its own operation and not the other (`p1-28-shared-components.dom.test.tsx:722-751`); the customer-first vehicle pick that was write-only platform-wide now reads back (R4); creation, duplicate advisory, relationship recording and the truthful handoff to the wizard are each asserted with a request-shape or a navigation target. The one remaining degradation — no telephone search — is a missing contract with a named owner, stated on screen in both languages, and it binds no operation this task was given. I applied the §5.2 binding plus §10; the accessibility gap is named rather than implied."
  },
  "FE-007": {
    "IMPLEMENTATION_SURFACES": "`apps/web/src/features/receptions/components/CheckInStartScreen.tsx` (1087 lines: origin XOR, requester and vehicle pick, receiving employee, intake facts) behind `apps/web/src/app/[locale]/(dashboard)/receptions/check-in/page.tsx:70` (gate), `:95` (fuel catalogue) and `:96` (walk-in handoff); the resumed visit is `apps/web/src/features/receptions/components/CheckInWizardShell.tsx` (303 lines) behind `apps/web/src/app/[locale]/(dashboard)/receptions/check-in/[receptionId]/page.tsx:97` (gate), `:103` (the detail read) and `:145` (the shell). State machine: `apps/web/src/features/receptions/check-in/wizard.ts` (345). Adapter `apps/web/src/features/receptions/api.ts` (`createReception`). The receiving-employee directory is `apps/web/src/features/receptions/people/receiving-employee-directory.ts` (97 lines) — the permission the picker costs at `:58`, the operation it reads at `:61` and the branch scope it reaches at `:70`. The four-state name union this cell used to cite beside it moved with that read: it is `apps/web/src/features/receptions/people/person-name.ts` (`:53-57`, its resolver at `:87-104`) and it now serves the read-back ACTORS, because the receiving employee itself is printed from the visit’s own immutable `receivingEmployeeDisplayName` snapshot (`apps/web/src/features/receptions/components/CheckInWizardShell.tsx:189`, `apps/web/src/features/receptions/components/AcknowledgementDocument.tsx:156`).",
    "NORMAL_NAVIGATION_ENTRY": "Sidebar `receptions` → `/receptions` (the board), then Check in; the offer is granted by `rec.reception.manage` and by nothing else (`apps/web/tests/p1-28-reception-routes.test.ts` \"grants the check-in offer from the manage permission and nothing else\"). The walk-in intake links here with the handed-over pair (`reception-walkin.dom.test.tsx:521-541`).",
    "BACKEND_READ_CONTRACTS": "`rec.reception-list` and `rec.reception-detail` (the resume path, R1 — G-REC-READ CLOSED), `iam.user-list` for the receiving-employee picker (`apps/web/src/features/receptions/support-api.ts` (`listReceivingEmployeeCandidates`)), `iam.user-detail` to resolve the stored receiving-employee identifier to a NAME (`support-api.ts` (`readReceivingEmployeeIdentity`), under the same `iam.user.read` the picker already consumes), `apt.appointment-list` filtered to confirmed for the appointment origin, `crm.customer-vehicle-list` for the requester’s vehicles, and `rec.catalogue-fuel-level-list`.",
    "BACKEND_WRITE_CONTRACTS": "`rec.reception-create` — POST /api/v1/receptions, 201 + `ETag:\"1\"`, Idempotency-Key mandatory. REACHABLE in `write-reachability.json`, under `rec.reception-create`.",
    "REQUEST_SCHEMA": "Strict, with `origin` a discriminated union `{kind:'appointment'}` XOR `{kind:'walk_in'}`. `apps/web/tests/reception-checkin-wizard.test.ts` holds `buildCreateInput` directly: \"refuses a missing branch target, employee, requester and vehicle, each by name\", \"walk-in: sends the chosen vehicle and requester, and omits an empty note\", and — the coupling trap — \"appointment origin: vehicle and requester come from the APPOINTMENT, never the walk-in picks\".",
    "RESPONSE_SCHEMA": "201 `{receptionVisitId, displayNumber|null, receptionStatus:'opened', origin, recordVersion:1}`. One open visit per vehicle and one consumption per origin answer the SAME 409 `ERR-RES-002`, so the copy does NOT guess which rule refused (`reception-checkin.dom.test.tsx` \"a 409 on create renders the copy that does NOT guess which rule refused\").",
    "REQUIRED_PERMISSIONS": "`rec.reception.read` gates the page (the resume path is a read; `check-in/[receptionId]/page.tsx:97`), `rec.reception.manage` gates opening a visit and its absence is stated where the form would be, `iam.user.read` the employee picker AND the name resolution, `crm.customer.read` the requester search, `apt.appointment.read` the appointment picker — all resolved at the route (`check-in/[receptionId]/page.tsx:151-168`), with `apps/web/tests/p1-28-reception-routes.test.ts` granting each capability for its own permission and for nothing else.",
    "OWNER_REQUIREMENT_MAPPING": "Owner rows OR-6..9; journey steps S-5/S-6. The receiving-employee referent is Owner register question A (`owner-workflow-requirements.md:199-216`), still unanswered. The resumability mandate (`:182-197`) IS discharged: R1 makes a visit resumable from its id, and `reception-checkin.dom.test.tsx` \"choosing an appointment surfaces the open visit of ITS vehicle, with a resume link\" surfaces the open visit of a chosen appointment's vehicle with a resume link.",
    "TENANT_ISOLATION_REQUIREMENT": "Branch pair as the operation's required field, never a client claim; the employee search travels as an encoded parameter of a POST-backed Server Action and never reaches the address bar (`support-api.ts` (`listReceivingEmployeeCandidates`, a POST-backed Server Action)). No tenant on any request (`p1-28-qa.test.ts:924-939`).",
    "LOADING_STATE": "Server-rendered catalogue and handoff resolution mean the first paint is usable; the pickers use the shared table loading state.",
    "EMPTY_STATE": "An empty fuel catalogue is stated as not configured beside the nullable field rather than as an error (`CheckInStartScreen.tsx:627-636`); a handoff that resolves to no nameable customer starts EMPTY, the same state as arriving from the navigation (`check-in/page.tsx:130-168`; `reception-checkin.dom.test.tsx` \"starts empty when the page passes no handoff\").",
    "ZERO_RESULT_STATE": "The requester and appointment pickers use the shared no-results state; a handed-over vehicle that is not on the customer's list is STATED rather than silently dropped (`reception-checkin.dom.test.tsx` \"states it when the handed-over vehicle is not on that customer list\").",
    "ERROR_STATE": "Route-level failure branches one per outcome with the read's reference (`check-in/[receptionId]/page.tsx:103-127`); the shell's own reads use `EvidenceStates` with Retry.",
    "DENIAL_STATE": "A client-side gate denial carries no reference, a backend denial carries its own (`check-in/[receptionId]/page.tsx:97-110`); `p1-28-reception-routes.test.ts` \"the wizard route resolves every write capability\" grants each wizard capability for its own permission and for nothing else.",
    "CONFLICT_STATE": "The 409 copy names both readings of `ERR-RES-002` because the code does not distinguish them (`CheckInStartScreen.tsx:657-665`; asserted `reception-checkin.dom.test.tsx` \"a 409 on create renders the copy that does NOT guess which rule refused\").",
    "ARABIC_EVIDENCE": "`receptions.checkIn.*` and the three `receptions.wizard.receivingEmployee*` notice keys in `ar.json`; the screen and the handoff notice both render in Arabic (\"renders in Arabic, RTL, from the same catalogue\", \"renders the handoff notice in Arabic too\"). PRODUCTION: Measured across 18 combinations — desktop 1440, tablet 768 and 200% zoom (640 CSS px at deviceScaleFactor 2) x English/Arabic x the capture, damage-map and signature steps: 0 horizontal clipping on the document or any non-scrolling region, 0 controls without an accessible name, 0 raw translation keys, Arabic always dir=rtl with real Arabic script, and the approved file input keyboard-reachable in every viewport (28 focusable controls, 5 file inputs, 0 removed from the tab order).",
    "ENGLISH_EVIDENCE": "Same keys in `en.json` across the suite's other cases.",
    "RTL_LTR_EVIDENCE": "Arabic cases mount RTL; the EV charge field and identifiers stay `dir=\"ltr\"` (`CheckInStartScreen.tsx:645`). PRODUCTION: Measured across 18 combinations — desktop 1440, tablet 768 and 200% zoom (640 CSS px at deviceScaleFactor 2) x English/Arabic x the capture, damage-map and signature steps: 0 horizontal clipping on the document or any non-scrolling region, 0 controls without an accessible name, 0 raw translation keys, Arabic always dir=rtl with real Arabic script, and the approved file input keyboard-reachable in every viewport (28 focusable controls, 5 file inputs, 0 removed from the tab order).",
    "ACCESSIBILITY_EVIDENCE": "Grouped fieldsets with legends (`CheckInStartScreen.tsx:596`, `:608-611`), a `role=\"alert\"` for local refusals (`:652`), controls by label; the wizard header states the receiving employee as text rather than as a code element (`CheckInWizardShell.tsx:199-221`). NAMED GAP: no axe run covers this screen. The tablet half of that gap is CLOSED and the sentence that claimed it was false at this candidate: `apps/web/playwright.config.ts:255` matches `(administration|appointments-and-receptions)`, so `authenticated-tablet` executed 47 P1-28 cases at 1024x768 in run `31750364479` / job `94614564003`. PRODUCTION: Measured across 18 combinations — desktop 1440, tablet 768 and 200% zoom (640 CSS px at deviceScaleFactor 2) x English/Arabic x the capture, damage-map and signature steps: 0 horizontal clipping on the document or any non-scrolling region, 0 controls without an accessible name, 0 raw translation keys, Arabic always dir=rtl with real Arabic script, and the approved file input keyboard-reachable in every viewport (28 focusable controls, 5 file inputs, 0 removed from the tab order).",
    "PRODUCTION_REACHABILITY": "REACHABLE. rec.receiving-employee-list is the operation the picker consumes — answered 200 against the running stack, branch-scoped, returning {id, displayName} and no other field. The adapter manifest declares no pending entry for it or for anything else.",
    "PRODUCTION_TEST_EVIDENCE": "Driven against the production stack. rec.receiving-employee-list answered 200 for the operator's own branch with the eligible active account and nothing else. The historical read-back is a SNAPSHOT, not a lookup: receiving_employee_display_name is a NOT NULL column on rec.reception_visits beside the foreign-keyed receiving_employee_id (fk_reception_visits_receiving_employee -> iam.user_accounts(tenant_id, id) ON DELETE RESTRICT), so no change to an IAM account can rewrite who accepted custody — the value lives on a different table. Reading the visit returned receivingEmployeeDisplayName \"Configured Acceptance Operator\", and iam.user-detail was invoked ZERO times in the entire life of the stack, so nothing reconstructs the historical name from live identity. Measured across 18 combinations — desktop 1440, tablet 768 and 200% zoom (640 CSS px at deviceScaleFactor 2) x English/Arabic x the capture, damage-map and signature steps: 0 horizontal clipping on the document or any non-scrolling region, 0 controls without an accessible name, 0 raw translation keys, Arabic always dir=rtl with real Arabic script, and the approved file input keyboard-reachable in every viewport (28 focusable controls, 5 file inputs, 0 removed from the tab order).",
    "NEGATIVE_OR_MUTATION_PROOF": "A DENIAL IS NOT AN EMPTY LIST. The read-only principal, who holds no rec.reception.manage, was refused the picker read with 403 ERR-IAM-001 rather than being handed an empty catalogue — the distinction this row exists to protect, because \"nobody is eligible\" and \"you may not look\" are different facts about a branch. A forged receiving employee — a Tenant A account presented by a Tenant C operator on rec.reception-create — was refused 422 ERR-VAL-001 by the server.",
    "PROTECTED_REPROOF": "PENDING PROTECTED MERGE. Re-observed at this head against the PRODUCTION acceptance stack (next build + next start on both tiers, API 3000, web 3100, real S3-compatible store attached to the API process only): web 98/98 files and 2795/2795 cases, backend 88/88 and 2051/2051, DB/RLS 143/143 and 1711/1711, authenticated browser 365 passed / 0 failed.",
    "FINDING_IDS": "`G-EMP` OPEN (R6; Owner register question A) — HALF CLOSED at this head, and the halves must not be conflated. CLOSED: the plan §7 sentence \"The UI shows names, never UUIDs\" is now met. Both read-back surfaces print the visit’s own `receivingEmployeeDisplayName` — the snapshot `rec.stamp_receiving_employee_identity()` writes at insert and no later statement may edit — rather than resolving the identifier at read time, so neither has an identifier to print (`apps/web/src/features/receptions/components/CheckInWizardShell.tsx:189`, `apps/web/src/features/receptions/components/AcknowledgementDocument.tsx:156`). The four honest states this cell used to cite — named, denied, unresolved, unavailable — still exist and now serve the read-back ACTORS instead (`apps/web/src/features/receptions/people/person-name.ts:53-57`, `:87-104`). STILL OPEN: `rec.reception_visits.receiving_employee_id` is NOT NULL with no foreign key and no defined referent (`supabase/migrations/20260721097000_rec_reception_visits.sql:62`), the employee master never shipped, and nothing defines that an `iam.user` id is a legal value there.",
    "DOCUMENTATION_EVIDENCE": "`canonical-plan.md` §5.2 and §7 (receiving-employee referent); `operator-guide.md:99-102` (\"recorded as an identifier, not as an employee … it never shows you a raw identifier in place of a name\"), pinned to the two screens by `apps/web/tests/p1-28-guidance-reconciliation.test.ts` \"'recorded as an identifier, not as an employee' — G-EMP is stated on screen\" and \"'it never shows you a raw identifier in place of a name' — and neither surface does\".",
    "FINAL_VERDICT": "PASS",
    "VERDICT_RATIONALE": "PASS. The blocker was G-EMP / R6 — Owner register question A: receiving_employee_id \"has no foreign key and no defined referent, so the visit records a value whose meaning is undecided\", and the row correctly refused to close over an open Owner question about what a column MEANS. The Owner answered it and the Backend landed the answer: the column is now a real IAM reference under fk_reception_visits_receiving_employee with ON DELETE RESTRICT, guarded for eligibility by tg_reception_visits_receiving_employee, and accompanied by an immutable display-name snapshot on the visit. That is checked here against the live database rather than read from a record. The Frontend half follows: the picker consumes rec.receiving-employee-list, branch-scoped; a denied read is rendered as a denial and not as an empty branch; and the historical name is read from the snapshot with no IAM lookup anywhere in the stack."
  },
  "FE-008": {
    "IMPLEMENTATION_SURFACES": "`apps/web/src/features/receptions/components/steps/ConfirmationStep.tsx` (512 lines), registered as the wizard's first step (`check-in/steps.tsx:46-51`) and rendered by the shell. Reads through `apps/web/src/features/receptions/support-api.ts` (`readCustomerSummary`), (`readVehicleSummary`), (`listVehicleRelationshipEntries`) and `@/lib/customers/vehicles` (`ConfirmationStep.tsx:160`).",
    "NORMAL_NAVIGATION_ENTRY": "The check-in wizard at `/receptions/check-in/{receptionId}`; it is the step the operator lands on, and every registered step is rendered as step navigation (`reception-checkin.dom.test.tsx` \"renders the registry steps as a step navigation and switches on choice\").",
    "BACKEND_READ_CONTRACTS": "`crm.customer-read` (detail + ETag), `veh.vehicle-read` (detail incl. `recordVersion` and `mergedIntoId` — merged is NOT a 404), `veh.vehicle-relationship-list` (partner names included) and `crm.customer-vehicle-list` (R4, INT-012 closed). All four registered and all four consumed by this step.",
    "BACKEND_WRITE_CONTRACTS": "N/A with rationale: identity confirmation is a read-and-confirm step. It offers NO control labelled Confirm because nothing implements one, which is asserted rather than assumed (`reception-checkin.dom.test.tsx` \"offers NO control labelled Confirm — nothing implements one\"); a correction routes to FE-006's creation and link writes.",
    "REQUEST_SCHEMA": "Path uuid for the two details, cursor/limit for the two lists. Scope keys in a query string throw client-side at the one door (`p1-28-qa.test.ts:954-970`), so the tenant is resolved server-side and cannot be smuggled through a filter (`p1-28-security.test.ts:611-680`).",
    "RESPONSE_SCHEMA": "Customer detail `{id, displayNumber|null, displayName, partyType, lifecycleStatus, commercialStatus, recordVersion, …}`; vehicle detail with RV/ETag and `mergedIntoId`; relationship rows carrying `partnerName|null`, `partnerNumber|null`, `partnerType|null`, which is why the panel can render a name rather than a uuid.",
    "REQUIRED_PERMISSIONS": "`crm.customer.read` and `veh.vehicle.read`, resolved at the wizard route into `capabilities` so no component consults the session (`check-in/[receptionId]/page.tsx:154-155`); rule 6 of the access gate refuses a `holds()` call anywhere in the feature trees (`tests/ci/p1-28-access-gate.test.ts:552-562`).",
    "OWNER_REQUIREMENT_MAPPING": "Owner rows OR-1..5 (the confirmation half); journey steps S-5/S-6 — 'which of their vehicles is this'. OR-3's blocker INT-012 is closed by R4 and the link is stated in BOTH directions (`reception-checkin.dom.test.tsx` \"states the recorded link in both directions — and its absence as a fact, not an error\").",
    "TENANT_ISOLATION_REQUIREMENT": "A cross-tenant read renders the SAME refusal a missing record does (`p1-28-qa.test.ts:1022-1047`), and every path parameter is encoded (`:1048-1062`).",
    "LOADING_STATE": "Shared `EvidenceStates` skeleton while a panel's table is not idle (`EvidencePanels.tsx:60`; `p1-28-shared-components.dom.test.tsx:159-177`).",
    "EMPTY_STATE": "The recorded link's ABSENCE is stated as a fact rather than as an error (`reception-checkin.dom.test.tsx` \"states the recorded link in both directions — and its absence as a fact, not an error\").",
    "ZERO_RESULT_STATE": "A customer with no vehicles on the list renders the panel's own statement, not the generic empty state.",
    "ERROR_STATE": "`EvidenceStates` offers Retry with the correlation reference and actually calls back on press (`p1-28-shared-components.dom.test.tsx:120-140`).",
    "DENIAL_STATE": "A denied identity panel says so WITH the backend reference (`reception-checkin.dom.test.tsx` \"a denied identity panel says so, with the backend reference\"), and a denial is never rendered as an empty list (`p1-28-shared-components.dom.test.tsx:141-150`; `p1-28-qa.test.ts:549-560`).",
    "CONFLICT_STATE": "N/A with rationale: this step performs no write, so it has no version to lose.",
    "ARABIC_EVIDENCE": "`receptions.confirm.*` in `ar.json`; the wizard's Arabic render covers the step (`reception-checkin.dom.test.tsx` \"renders in Arabic, RTL, from the same catalogue\").",
    "ENGLISH_EVIDENCE": "Same keys in `en.json`.",
    "RTL_LTR_EVIDENCE": "Arabic cases mount RTL; identifiers and record numbers stay LTR through `PartyLabel` and the shared formatters.",
    "ACCESSIBILITY_EVIDENCE": "Each panel is an `EvidenceSection` labelled by its own heading so a screen reader can reach it (`p1-28-shared-components.dom.test.tsx:473-491`). NAMED GAP: no axe run and no tablet case for the wizard.",
    "PRODUCTION_REACHABILITY": "Reachable as the wizard's first step; the step registry is covered from the registry rather than by name (`p1-28-qa.test.ts:380-393`), so a step that stopped being rendered would fail.",
    "PRODUCTION_TEST_EVIDENCE": "`reception-checkin.dom.test.tsx` \"the confirmation step\" (5 cases) plus the shared-component suite for the panels it composes.",
    "NEGATIVE_OR_MUTATION_PROOF": "A MERGED vehicle renders honestly — a fact with a link, never a 404 (`reception-checkin.dom.test.tsx` \"renders a MERGED vehicle honestly — a fact with a link, never a 404\") — and the step offers no control labelled Confirm (\"offers NO control labelled Confirm — nothing implements one\"). Both cases fail if the panel is simplified into a plain detail render.",
    "PROTECTED_REPROOF": "PENDING PROTECTED MERGE — RE-OBSERVED for this re-derivation at `a2318c36`: the web tier ran 2669 cases, 0 failed and 0 skipped (28 P1-28 suites, 760 cases), and the root CI tier ran 2475 cases, 0 failed and 0 skipped (5 P1-28 gate suites, 180 cases). THE BROWSER TIER NOW CARRIES THIS PHASE — `apps/web/tests/e2e/authenticated/appointments-and-receptions.spec.ts` (2478 lines, against the running application, the running API and the real database), which the previous derivation correctly recorded as absent. It covers this row only as far as the data allows: \"the check-in wizard\" discovers a visit through the real API and asserts the thirteen-step registry when one exists and the honest not-found when none does, and §18 now OPENS a real visit, so the wizard shell is reached in the running application. This step’s own four reads are not separately asserted there — a gap this row states rather than implies. STILL NOT OBSERVED, and this is the whole outstanding debt on this row: that spec was NOT executed in this derivation — it needs the production launcher (`npm run acceptance:serve`), a live database and, for §18, a provisioned second workspace — and the run reported by the wave that landed it (319 expected, 0 unexpected, 0 flaky) is recorded in no phase document, so no browser result is bound to this head. THE TABLET VIEWPORT AND HOSTED CI WERE OBSERVED AT `38afa5c28e5b78d484a442cf6b8596fb2a5c34aa`, A HEAD THIS CANDIDATE SUPERSEDES. Two sentences stood here and both were false: that the `authenticated-tablet` project sees no P1-28 screen, and that no hosted-CI result exists for this head. `apps/web/playwright.config.ts:254-255` names that project and matches `(administration|appointments-and-receptions)`, so the phase surface is seen at 1024x768 as well as at 1440x900; run `31750364479` / job `94614564003` at head `38afa5c28e5b78d484a442cf6b8596fb2a5c34aa` executed `authenticated/appointments-and-receptions.spec.ts` 141 times — 47 in `authenticated-en`, 47 in `authenticated-ar`, 47 in `authenticated-tablet`, 0 failed and 0 skipped. Hosted CI at that head is run `31750364479`, 21 checks, 21 success, 0 failure, with `ci-gate` (`94617578352`) the single required check. The citation this cell used to give for the tablet claim pointed at lines 225 to 233 of the same config — the project DOCBLOCK, nine consecutive comment lines describing the state BEFORE the widening — so it read as confirmation of a sentence the code beneath it refutes; `validate:p1-28-evidence` now refuses a comment-only citation for exactly that reason. STILL OUTSTANDING ON THIS ROW: the protected-branch re-proof, because PR #226 has not merged, and OWNER ACCEPTANCE, which has not been requested or returned (`docs/phase-1/phase-1-28/evidence/change-log.md:5-11`). THAT HEAD IS NOT THE CURRENT CANDIDATE. Three fix waves changed 37 product files after `38afa5c28e5b78d484a442cf6b8596fb2a5c34aa`, so P1-28-QA-005 re-froze the candidate — at `6392ccb4321b004ed12e5d04ad583298da3303dd` then, and at `3c75f49a01e35b507461bf0929b0046e7140860a` now, the two being product-identical — and every hosted figure above is recorded in `docs/phase-1/phase-1-28/evidence/closure-candidate.json` as PENDING at that candidate — marked `describesSupersededHead`, citing the run it really came from, and listed in `pendingHostedBindings`. No hosted run has been taken at the current candidate yet, so no hosted result is bound to this head; `validate:p1-28-evidence` refuses a cell asserting an observation AT THIS CANDIDATE while the package records none.",
    "FINDING_IDS": "None open. `G-CUST-VEH` CLOSED by R4 (PR #221), adversarially verified.",
    "DOCUMENTATION_EVIDENCE": "`canonical-plan.md` §5.2; `contract-archaeology.md` Table C rows C2/C4/C6; `operator-guide.md` 'The check-in wizard'.",
    "FINAL_VERDICT": "PASS",
    "VERDICT_RATIONALE": "All four bound reads are consumed by this step, including the customer-first direction that was write-only platform-wide until R4 — the link is stated in both directions and its absence as a fact. The two failure modes that matter are proved rather than assumed: a merged vehicle is a fact with a link and not a 404, and a denied panel is a denial with its reference and not an empty list. There is no write, no catalogue and no open decision on this row."
  },
  "FE-009": {
    "IMPLEMENTATION_SURFACES": "`apps/web/src/features/receptions/components/steps/PartiesStep.tsx` (639 lines), registered at `check-in/steps.tsx:52-57`. Adapters `api.ts` (`assignPartyRole`) and `api.ts` (`recordAuthorization`); reads `api.ts` (`listPartyRoles`) and `api.ts` (`listAuthorizations`).",
    "NORMAL_NAVIGATION_ENTRY": "Second step of the check-in wizard; the step navigation is rendered from the registry and switches on choice (`reception-checkin.dom.test.tsx` \"renders the registry steps as a step navigation and switches on choice\").",
    "BACKEND_READ_CONTRACTS": "`rec.reception-party-role-list` and `rec.reception-authorization-list` — both R1, both consumed. The authorization list is a two-table UNION, so decisions AND refusals arrive together, each attributed and marked (`reception-checkin.dom.test.tsx` \"renders the authorization UNION: decisions AND refusals, each attributed and marked\").",
    "BACKEND_WRITE_CONTRACTS": "`rec.reception-party-role` (POST .../party-roles) and `rec.reception-authorization` (POST .../authorizations). Both REACHABLE with named call sites (`write-reachability.json`, under `rec.reception-party-role` and `rec.reception-authorization`).",
    "REQUEST_SCHEMA": "Party role `{partnerId, relationshipRole enum(7), assignmentSource?, supersede?}`; authorization `{authorizingRole enum(4) — `vehicle_user` and `payer` EXCLUDED, partnerId, decision, channel?, authorizedScope?, evidenceDocumentId?}`. The strict-subset rule is asserted against the contract in both directions (`apps/web/tests/receptions-contract.test.ts` \"authorizing roles are a strict subset excluding vehicle_user and payer\").",
    "RESPONSE_SCHEMA": "201 `{receptionVisitId, partyRoleId, relationshipRole, superseded}` / 201 `{receptionVisitId, authorizationId, decision}`. A role-not-held refusal is the same non-disclosing 409 `ERR-TRN-001` as the authority guard, and the copy does not guess (`reception-checkin.dom.test.tsx` \"a 409 verdict renders the non-guessing copy AND triggers the re-read\").",
    "REQUIRED_PERMISSIONS": "`rec.reception.party.manage` and `rec.reception.authorization.verify` — two separate codes, resolved at the route (`check-in/[receptionId]/page.tsx:152-153`) and each proved to grant its own form and nothing else (`p1-28-reception-routes.test.ts` \"grants ${field} for ${permission} and for nothing else\"). The authorization decision is in the approval audit class (`receptions-contract.test.ts` \"registers the authorization decision and the approval in the approval audit class\").",
    "OWNER_REQUIREMENT_MAPPING": "Journey step S-6 and the S-11 approval preconditions; the four authorizing roles are the Owner's narrow authorizing set, and a standing `declined` blocks approve AND convert.",
    "TENANT_ISOLATION_REQUIREMENT": "Visit-scoped paths, encoded; no scope key in any query (gate rule 5); the branch pair never travels on these calls at all (`p1-28-qa.test.ts:909-923` pins WHICH requests carry it).",
    "LOADING_STATE": "`EvidenceStates` skeleton on both tables while not idle.",
    "EMPTY_STATE": "`receptions.parties.rolesEmpty` is rendered as an emptiness rather than a failure; the authorization union states what it holds.",
    "ZERO_RESULT_STATE": "N/A with rationale: neither list is filtered by operator input.",
    "ERROR_STATE": "Shared `EvidenceStates` with reference and Retry.",
    "DENIAL_STATE": "Each write form is withdrawn WITH its reason when its own permission is absent (`reception-checkin.dom.test.tsx` \"withdraws the write forms without their permissions, saying why\"), and every write is locked on a terminal visit (\"locks every write on a terminal visit\").",
    "CONFLICT_STATE": "A 409 verdict renders the non-guessing copy AND triggers the re-read (`reception-checkin.dom.test.tsx` \"a 409 verdict renders the non-guessing copy AND triggers the re-read\"), so the next command's version comes from a read.",
    "ARABIC_EVIDENCE": "`receptions.parties.*` and `receptions.partyRole.*` in `ar.json`; Arabic render at `reception-checkin.dom.test.tsx` \"renders in Arabic, RTL\".",
    "ENGLISH_EVIDENCE": "Same keys in `en.json`.",
    "RTL_LTR_EVIDENCE": "Arabic case mounts RTL; party numbers stay LTR through `PartyLabel`.",
    "ACCESSIBILITY_EVIDENCE": "Labelled `EvidenceSection` regions; forms reached by label; the withdrawal is text, never a greyed control (`p1-28-shared-components.dom.test.tsx:367-379`). NAMED GAP: no axe run, no tablet case.",
    "PRODUCTION_REACHABILITY": "Registered step, rendered by the shell, covered from the registry (`p1-28-qa.test.ts:380-393`); both writes REACHABLE with consumption proved, not merely declared.",
    "PRODUCTION_TEST_EVIDENCE": "`reception-checkin.dom.test.tsx` \"the parties step\" (6 cases) and `receptions-contract.test.ts` \"the vocabularies keep the server distinctions\" (vocabularies and the refusing-partner rule).",
    "NEGATIVE_OR_MUTATION_PROOF": "The authorizing-role set is asserted as a STRICT SUBSET excluding `vehicle_user` and `payer` (`receptions-contract.test.ts` \"authorizing roles are a strict subset excluding vehicle_user and payer\") — widen it and the case fails. A successful party-role write is proved to re-read BOTH the list and the shell detail (`reception-checkin.dom.test.tsx` \"records a party role and re-reads: list, and the shell detail, after success\"), which is the QA-004 discipline made visible.",
    "PROTECTED_REPROOF": "PENDING PROTECTED MERGE — RE-OBSERVED for this re-derivation at `a2318c36`: the web tier ran 2669 cases, 0 failed and 0 skipped (28 P1-28 suites, 760 cases), and the root CI tier ran 2475 cases, 0 failed and 0 skipped (5 P1-28 gate suites, 180 cases). THE BROWSER TIER NOW CARRIES THIS PHASE — `apps/web/tests/e2e/authenticated/appointments-and-receptions.spec.ts` (2478 lines, against the running application, the running API and the real database), which the previous derivation correctly recorded as absent. It covers the wizard shell in the running application: \"the check-in wizard\" loads a visit’s thirteen-step registry through the real API, and §18 now opens a REAL visit and reaches that shell. This step’s own panels are exercised only as far as that goes — a limit this row states rather than implies. STILL NOT OBSERVED, and this is the whole outstanding debt on this row: that spec was NOT executed in this derivation — it needs the production launcher (`npm run acceptance:serve`), a live database and, for §18, a provisioned second workspace — and the run reported by the wave that landed it (319 expected, 0 unexpected, 0 flaky) is recorded in no phase document, so no browser result is bound to this head. THE TABLET VIEWPORT AND HOSTED CI WERE OBSERVED AT `38afa5c28e5b78d484a442cf6b8596fb2a5c34aa`, A HEAD THIS CANDIDATE SUPERSEDES. Two sentences stood here and both were false: that the `authenticated-tablet` project sees no P1-28 screen, and that no hosted-CI result exists for this head. `apps/web/playwright.config.ts:254-255` names that project and matches `(administration|appointments-and-receptions)`, so the phase surface is seen at 1024x768 as well as at 1440x900; run `31750364479` / job `94614564003` at head `38afa5c28e5b78d484a442cf6b8596fb2a5c34aa` executed `authenticated/appointments-and-receptions.spec.ts` 141 times — 47 in `authenticated-en`, 47 in `authenticated-ar`, 47 in `authenticated-tablet`, 0 failed and 0 skipped. Hosted CI at that head is run `31750364479`, 21 checks, 21 success, 0 failure, with `ci-gate` (`94617578352`) the single required check. The citation this cell used to give for the tablet claim pointed at lines 225 to 233 of the same config — the project DOCBLOCK, nine consecutive comment lines describing the state BEFORE the widening — so it read as confirmation of a sentence the code beneath it refutes; `validate:p1-28-evidence` now refuses a comment-only citation for exactly that reason. STILL OUTSTANDING ON THIS ROW: the protected-branch re-proof, because PR #226 has not merged, and OWNER ACCEPTANCE, which has not been requested or returned (`docs/phase-1/phase-1-28/evidence/change-log.md:5-11`). THAT HEAD IS NOT THE CURRENT CANDIDATE. Three fix waves changed 37 product files after `38afa5c28e5b78d484a442cf6b8596fb2a5c34aa`, so P1-28-QA-005 re-froze the candidate — at `6392ccb4321b004ed12e5d04ad583298da3303dd` then, and at `3c75f49a01e35b507461bf0929b0046e7140860a` now, the two being product-identical — and every hosted figure above is recorded in `docs/phase-1/phase-1-28/evidence/closure-candidate.json` as PENDING at that candidate — marked `describesSupersededHead`, citing the run it really came from, and listed in `pendingHostedBindings`. No hosted run has been taken at the current candidate yet, so no hosted result is bound to this head; `validate:p1-28-evidence` refuses a cell asserting an observation AT THIS CANDIDATE while the package records none.",
    "FINDING_IDS": "None open. The day-one blocker on reading standing decisions back is CLOSED by R1.",
    "DOCUMENTATION_EVIDENCE": "`canonical-plan.md` §5.2; `contract-archaeology.md` Table B rows B2/B3; `operator-guide.md` 'The check-in wizard'.",
    "FINAL_VERDICT": "PASS",
    "VERDICT_RATIONALE": "Both bound writes and both bound reads are wired, and the two facts most likely to be got wrong are pinned: the authorizing-role set is a strict subset of the relationship roles, and the union read returns refusals as well as decisions, each attributed and marked. The non-disclosing 409 is rendered without guessing which rule refused and triggers a re-read, so the next command is guarded by a version that came from a read. Both forms are withdrawn for their own permission and locked on a terminal visit."
  },
  "FE-010": {
    "IMPLEMENTATION_SURFACES": "`apps/web/src/features/receptions/components/steps/ComplaintsStep.tsx` (265 lines), registered at `check-in/steps.tsx:58-63`; write through `api.ts` (`recordConditionEvidence`); the WF-27 gate is `check-in/sensitive.ts:75` and the denial supplement `:98`.",
    "NORMAL_NAVIGATION_ENTRY": "Third step of the check-in wizard.",
    "BACKEND_READ_CONTRACTS": "`rec.reception-condition-evidence-list` filtered to `complaint` (R1). It deliberately EXCLUDES the narrative table, so the read returns category, severity and reporter and never the customer's words — proved as a field asymmetry, not asserted in prose (`apps/web/tests/p1-28-security.test.ts:397-431`).",
    "BACKEND_WRITE_CONTRACTS": "`rec.reception-condition-evidence` with `kind:'complaint'`. REACHABLE, with the five condition-evidence steps named as its call sites (`write-reachability.json`, under `rec.reception-condition-evidence`).",
    "REQUEST_SCHEMA": "Complaint text 1-4000, restricted narrative, appendable in any non-terminal state. `apps/web/tests/reception-condition-evidence.dom.test.tsx` \"records the complaint in the contract shape, omitting an untouched severity\" asserts the contract shape on the wire and that an untouched severity is OMITTED rather than blanked — a blanked optional would 422 against a strict route.",
    "RESPONSE_SCHEMA": "201 `{receptionVisitId, kind:'complaint', evidenceId}`; the visit is the audit entity. An idempotent REPLAY returns the stored row and must not be double-listed in the session capture (`reception-condition-evidence.dom.test.tsx` \"does not double-list an idempotent REPLAY of the same write\"; the rule itself at `apps/web/tests/reception-evidence.test.ts` \"refuses a duplicate id — an idempotent REPLAY returns the stored row\").",
    "REQUIRED_PERMISSIONS": "`rec.reception.evidence.manage` PLUS `iam.sensitive.view` — WF-27: the operation declares only the first and the DATABASE refuses without the second. The composition is recorded and VERIFIED against the migration policy text by the gate (`docs/phase-1/phase-1-28/composed-permissions.json:30-45`; `tests/ci/p1-28-access-gate.test.ts:384-471`).",
    "OWNER_REQUIREMENT_MAPPING": "Owner rows OR-10 and OR-26; journey step S-7 (reception half). Both mandatory labels — the customer's words, and not yet technically verified — render at capture time (`reception-condition-evidence.dom.test.tsx` \"labels the record as the customer’s words and as not yet technically verified\"); the label-REMOVAL condition is unimplementable (WF-05/INS-16) so the second label is permanent in P1-28.",
    "TENANT_ISOLATION_REQUIREMENT": "Visit-scoped path, encoded; a cross-tenant visit renders as not-found (`p1-28-qa.test.ts:1022-1047`).",
    "LOADING_STATE": "`EvidenceStates` skeleton on the read-back table.",
    "EMPTY_STATE": "An empty read-back still SAYS the narrative is not returned, so emptiness is not read as the whole truth (`p1-28-sensitive-narratives.dom.test.tsx:299-307`).",
    "ZERO_RESULT_STATE": "N/A with rationale: the list is filtered by kind, not by operator input.",
    "ERROR_STATE": "Shared `EvidenceStates` with reference and Retry; a failure is never reported as success (`p1-28-qa.test.ts:452-486`).",
    "DENIAL_STATE": "The capture form is WITHHELD without either permission, with the reason stated in both languages (`p1-28-sensitive-narratives.dom.test.tsx:137-178`); a database refusal renders the permissions message, a reference to quote and the pair — and KEEPS the typed words (`:200-259`), while a refusal that is NOT a permission problem is not blamed on one (`:260-297`).",
    "CONFLICT_STATE": "N/A with rationale: appending evidence is unguarded — no `If-Match` — so there is no version conflict. `StepOutcome` still refuses to guess which rule refused a 409 (`p1-28-shared-components.dom.test.tsx:391-405`).",
    "ARABIC_EVIDENCE": "`receptions.complaints.*` and the two mandatory labels in `ar.json`; both statements are published in both catalogues and neither is a copy of the other (`p1-28-security.test.ts:380-396`). Arabic render at `reception-condition-evidence.dom.test.tsx` \"renders in Arabic, RTL, from the same catalogue\".",
    "ENGLISH_EVIDENCE": "Same keys in `en.json`, used by the suite's other cases.",
    "RTL_LTR_EVIDENCE": "Arabic case mounts RTL; the free-text area follows the page direction while identifiers stay LTR.",
    "ACCESSIBILITY_EVIDENCE": "Labelled `EvidenceSection`; the text area reached by label; refusals announced through the shared alert. NAMED GAP: no axe run, no tablet case.",
    "PRODUCTION_REACHABILITY": "Registered step covered from the registry; `complaint` is classified `wired` in the coverage table of `apps/web/src/features/receptions/check-in/evidence.ts:89`, and `apps/web/tests/reception-evidence.test.ts` \"every reachable kind is genuinely constructed by its own step, and submitted\" proves it against the step’s own source rather than against the claim.",
    "PRODUCTION_TEST_EVIDENCE": "`apps/web/tests/reception-condition-evidence.dom.test.tsx` \"the complaints step (FE-010)\" (7 cases), `apps/web/tests/p1-28-sensitive-narratives.dom.test.tsx` (12 cases) and `apps/web/tests/reception-evidence.test.ts` (34). All green at this head.",
    "NEGATIVE_OR_MUTATION_PROOF": "The read-back is proved genuinely unable to return the narrative — a field asymmetry derived from the repository source, not a claim (`apps/web/tests/p1-28-security.test.ts:397-431`) — and the step keeps the typed words visible while SAYING the read cannot return them (\"keeps the typed words visible and says the read cannot return them\"). `reception-evidence.test.ts` \"rejects a malformed table, so the guard is not vacuous\" keeps the coverage table from vouching for nothing.",
    "PROTECTED_REPROOF": "PENDING PROTECTED MERGE — RE-OBSERVED for this re-derivation at `a2318c36`: the web tier ran 2669 cases, 0 failed and 0 skipped (28 P1-28 suites, 760 cases), and the root CI tier ran 2475 cases, 0 failed and 0 skipped (5 P1-28 gate suites, 180 cases). THE BROWSER TIER NOW CARRIES THIS PHASE — `apps/web/tests/e2e/authenticated/appointments-and-receptions.spec.ts` (2478 lines, against the running application, the running API and the real database), which the previous derivation correctly recorded as absent. It covers the wizard shell in the running application: \"the check-in wizard\" loads a visit’s thirteen-step registry through the real API, and §18 now opens a REAL visit and reaches that shell. This step’s own panels are exercised only as far as that goes — a limit this row states rather than implies. STILL NOT OBSERVED, and this is the whole outstanding debt on this row: that spec was NOT executed in this derivation — it needs the production launcher (`npm run acceptance:serve`), a live database and, for §18, a provisioned second workspace — and the run reported by the wave that landed it (319 expected, 0 unexpected, 0 flaky) is recorded in no phase document, so no browser result is bound to this head. THE TABLET VIEWPORT AND HOSTED CI WERE OBSERVED AT `38afa5c28e5b78d484a442cf6b8596fb2a5c34aa`, A HEAD THIS CANDIDATE SUPERSEDES. Two sentences stood here and both were false: that the `authenticated-tablet` project sees no P1-28 screen, and that no hosted-CI result exists for this head. `apps/web/playwright.config.ts:254-255` names that project and matches `(administration|appointments-and-receptions)`, so the phase surface is seen at 1024x768 as well as at 1440x900; run `31750364479` / job `94614564003` at head `38afa5c28e5b78d484a442cf6b8596fb2a5c34aa` executed `authenticated/appointments-and-receptions.spec.ts` 141 times — 47 in `authenticated-en`, 47 in `authenticated-ar`, 47 in `authenticated-tablet`, 0 failed and 0 skipped. Hosted CI at that head is run `31750364479`, 21 checks, 21 success, 0 failure, with `ci-gate` (`94617578352`) the single required check. The citation this cell used to give for the tablet claim pointed at lines 225 to 233 of the same config — the project DOCBLOCK, nine consecutive comment lines describing the state BEFORE the widening — so it read as confirmation of a sentence the code beneath it refutes; `validate:p1-28-evidence` now refuses a comment-only citation for exactly that reason. STILL OUTSTANDING ON THIS ROW: the protected-branch re-proof, because PR #226 has not merged, and OWNER ACCEPTANCE, which has not been requested or returned (`docs/phase-1/phase-1-28/evidence/change-log.md:5-11`). THAT HEAD IS NOT THE CURRENT CANDIDATE. Three fix waves changed 37 product files after `38afa5c28e5b78d484a442cf6b8596fb2a5c34aa`, so P1-28-QA-005 re-froze the candidate — at `6392ccb4321b004ed12e5d04ad583298da3303dd` then, and at `3c75f49a01e35b507461bf0929b0046e7140860a` now, the two being product-identical — and every hosted figure above is recorded in `docs/phase-1/phase-1-28/evidence/closure-candidate.json` as PENDING at that candidate — marked `describesSupersededHead`, citing the run it really came from, and listed in `pendingHostedBindings`. No hosted run has been taken at the current candidate yet, so no hosted result is bound to this head; `validate:p1-28-evidence` refuses a cell asserting an observation AT THIS CANDIDATE while the package records none.",
    "FINDING_IDS": "None open. WF-05/INS-16 (the unimplementable label-removal condition) is a platform limitation the UI honours rather than a defect on this row.",
    "DOCUMENTATION_EVIDENCE": "`canonical-plan.md` §5.2; `contract-archaeology.md` Table B row B4 and riskiest-fact 4; `operator-guide.md` 'The check-in wizard'.",
    "FINAL_VERDICT": "PASS",
    "VERDICT_RATIONALE": "The bound write is wired and its exact body asserted, including the omit-never-blank rule for an untouched optional. The phase's sharpest edge — WF-27 — is handled in both directions: the form is withheld before a refusal that could only fail, and an actual database refusal is rendered as a permissions message that names the pair, keeps the typed words, and does NOT diagnose which half failed, with a control proving a non-permission failure is not blamed on a permission. The read-back's inability to return the narrative is proved as a field asymmetry read from the repository source."
  },
  "FE-011": {
    "IMPLEMENTATION_SURFACES": "`apps/web/src/features/receptions/components/steps/InspectionStep.tsx` (618 lines), registered at `apps/web/src/features/receptions/check-in/steps.tsx:64-69`; write through `apps/web/src/features/receptions/api.ts` (`recordConditionEvidence`). The leak type is a translated SELECT over the database’s own vocabulary (`InspectionStep.tsx:566-578`).",
    "NORMAL_NAVIGATION_ENTRY": "Fourth step of the check-in wizard.",
    "BACKEND_READ_CONTRACTS": "`rec.reception-condition-evidence-list` filtered per kind (R1) — the open inspections it lists are what the finding form hangs off.",
    "BACKEND_WRITE_CONTRACTS": "`rec.reception-condition-evidence` with kinds `inspection`, `condition_item` and `leak`; all three constructed and submitted by this step, which `reception-evidence.test.ts` \"every reachable kind is genuinely constructed by its own step, and submitted\" verifies against the step's own source.",
    "REQUEST_SCHEMA": "Per-kind schemas. Two closed vocabularies are restated from the migrations rather than guessed, and the difference matters: the `condition_item` vocabulary (wear/missing_part/malfunction) and the `damage_mark` vocabulary (chip/rust/missing) DIFFER and must not be conflated (`apps/web/tests/receptions-contract.test.ts` \"the vocabularies keep the server distinctions\"), and `leak_type` is the SEVEN values `ck_leak_observations_type` admits (`supabase/migrations/20260721102000_rec_warning_lights_leaks.sql:170`), copied into `apps/web/src/features/receptions/receptions-contract.ts:682-691` with the constraint named in the comment above it. `notApplicableReason` free text is the honest home for a not-performed road test (INS-05).",
    "RESPONSE_SCHEMA": "201 `{receptionVisitId, kind, evidenceId}` per item; the session capture list shows what THIS session recorded and says it does not survive a reload (`p1-28-shared-components.dom.test.tsx:293-334`).",
    "REQUIRED_PERMISSIONS": "`rec.reception.evidence.manage`, resolved at the route (`check-in/[receptionId]/page.tsx:156`). Not a restricted narrative, so `iam.sensitive.view` is deliberately NOT required — the composition record is a fact about an operation, and this kind carries no personal narrative (`check-in/sensitive.ts:65-88`).",
    "OWNER_REQUIREMENT_MAPPING": "Owner row OR-15 and the OR-20/21 partial block; journey step S-9.",
    "TENANT_ISOLATION_REQUIREMENT": "Visit-scoped path, encoded; cross-tenant renders as not-found.",
    "LOADING_STATE": "`EvidenceStates` skeleton on each read-back table.",
    "EMPTY_STATE": "The finding form is WITHHELD until an OPEN inspection exists, and the reason is stated — the `data_gated` coverage row (`apps/web/src/features/receptions/check-in/evidence.ts:103`; asserted by \"withholds the finding form until an OPEN inspection exists, and says why\").",
    "ZERO_RESULT_STATE": "N/A with rationale: the lists are filtered by kind, not by operator input.",
    "ERROR_STATE": "Shared `EvidenceStates` with reference and Retry.",
    "DENIAL_STATE": "The write forms are withdrawn with their reason without the evidence permission and on a terminal visit (`check-in/sensitive.ts:80-83`).",
    "CONFLICT_STATE": "N/A with rationale: appending evidence is unguarded. `StepOutcome` does not guess which rule refused a 409.",
    "ARABIC_EVIDENCE": "`receptions.inspection.*`, `receptions.finding.*` and all seven `receptions.leakType.*` labels in `ar.json`; Arabic render at \"renders in Arabic, RTL\".",
    "ENGLISH_EVIDENCE": "Same keys in `en.json`.",
    "RTL_LTR_EVIDENCE": "Arabic case mounts RTL.",
    "ACCESSIBILITY_EVIDENCE": "Labelled sections; controls by label. NAMED GAP: no axe run, no tablet case.",
    "PRODUCTION_REACHABILITY": "Registered step covered from the registry; `inspection` and `leak` are `wired` and `condition_item` `data_gated` in the coverage table, each named against the step that owns it (`apps/web/src/features/receptions/check-in/evidence.ts:96`, `:103`, `:112`).",
    "PRODUCTION_TEST_EVIDENCE": "`apps/web/tests/reception-condition-evidence.dom.test.tsx` \"the inspection step (FE-011)\" (8 cases at this head, two of them added by the leak fix) and `apps/web/tests/reception-evidence.test.ts` for the coverage table in both directions.",
    "NEGATIVE_OR_MUTATION_PROOF": "Road test is STATED as absent and no control claims to be one (\"states that road test is absent, and offers no control claiming to be one\"), and staff findings are kept as different records from customer concerns (\"keeps staff findings and customer concerns as different records\"). The leak vocabulary carries its own anti-vacuity case: \"offers exactly the seven types, translated, and nothing the database refuses\" fails on one option, on eight, or on the stored tokens rendered as labels, and `apps/web/tests/receptions-contract.test.ts` \"takes the seven leak types from the database CHECK, never from a guess\" reads the constraint out of the migration rather than trusting the list. `reception-evidence.test.ts` \"names a REGISTERED step for every kind\" fails a coverage claim pointing at nothing.",
    "PROTECTED_REPROOF": "PENDING PROTECTED MERGE — RE-OBSERVED for this re-derivation at `a2318c36`: the web tier ran 2669 cases, 0 failed and 0 skipped (28 P1-28 suites, 760 cases), and the root CI tier ran 2475 cases, 0 failed and 0 skipped (5 P1-28 gate suites, 180 cases). THE BROWSER TIER NOW CARRIES THIS PHASE — `apps/web/tests/e2e/authenticated/appointments-and-receptions.spec.ts` (2478 lines, against the running application, the running API and the real database), which the previous derivation correctly recorded as absent. It covers the wizard shell in the running application: \"the check-in wizard\" loads a visit’s thirteen-step registry through the real API, and §18 now opens a REAL visit and reaches that shell. This step’s own panels are exercised only as far as that goes — a limit this row states rather than implies. STILL NOT OBSERVED, and this is the whole outstanding debt on this row: that spec was NOT executed in this derivation — it needs the production launcher (`npm run acceptance:serve`), a live database and, for §18, a provisioned second workspace — and the run reported by the wave that landed it (319 expected, 0 unexpected, 0 flaky) is recorded in no phase document, so no browser result is bound to this head. THE TABLET VIEWPORT AND HOSTED CI WERE OBSERVED AT `38afa5c28e5b78d484a442cf6b8596fb2a5c34aa`, A HEAD THIS CANDIDATE SUPERSEDES. Two sentences stood here and both were false: that the `authenticated-tablet` project sees no P1-28 screen, and that no hosted-CI result exists for this head. `apps/web/playwright.config.ts:254-255` names that project and matches `(administration|appointments-and-receptions)`, so the phase surface is seen at 1024x768 as well as at 1440x900; run `31750364479` / job `94614564003` at head `38afa5c28e5b78d484a442cf6b8596fb2a5c34aa` executed `authenticated/appointments-and-receptions.spec.ts` 141 times — 47 in `authenticated-en`, 47 in `authenticated-ar`, 47 in `authenticated-tablet`, 0 failed and 0 skipped. Hosted CI at that head is run `31750364479`, 21 checks, 21 success, 0 failure, with `ci-gate` (`94617578352`) the single required check. The citation this cell used to give for the tablet claim pointed at lines 225 to 233 of the same config — the project DOCBLOCK, nine consecutive comment lines describing the state BEFORE the widening — so it read as confirmation of a sentence the code beneath it refutes; `validate:p1-28-evidence` now refuses a comment-only citation for exactly that reason. STILL OUTSTANDING ON THIS ROW: the protected-branch re-proof, because PR #226 has not merged, and OWNER ACCEPTANCE, which has not been requested or returned (`docs/phase-1/phase-1-28/evidence/change-log.md:5-11`). THAT HEAD IS NOT THE CURRENT CANDIDATE. Three fix waves changed 37 product files after `38afa5c28e5b78d484a442cf6b8596fb2a5c34aa`, so P1-28-QA-005 re-froze the candidate — at `6392ccb4321b004ed12e5d04ad583298da3303dd` then, and at `3c75f49a01e35b507461bf0929b0046e7140860a` now, the two being product-identical — and every hosted figure above is recorded in `docs/phase-1/phase-1-28/evidence/closure-candidate.json` as PENDING at that candidate — marked `describesSupersededHead`, citing the run it really came from, and listed in `pendingHostedBindings`. No hosted run has been taken at the current candidate yet, so no hosted result is bound to this head; `validate:p1-28-evidence` refuses a cell asserting an observation AT THIS CANDIDATE while the package records none.",
    "FINDING_IDS": "DEFECT FOUND AND FIXED AT THIS HEAD, and it was a step no operator could complete: `leakType` was a REQUIRED free-text box whose own hint asked the operator for their own words, while `ck_leak_observations_type` admits exactly seven tokens — so \"engine oil\" or \"coolant dripping\" came back 422 `ERR-VAL-001` rendered as \"This value is not accepted here\", with no way to learn what would be accepted (`receptions-contract.ts:664-691` records the reasoning beside the list). It is now a translated select over the migration’s own vocabulary. STILL OPEN and not this row’s to close: RMC-12 (an inspection is created `in_progress` and no operation ever completes it) and RMC-18 (no column distinguishes a lift inspection from a walk-around), both platform limitations owned by P1-18. This row’s obligation is to claim neither, and it claims neither.",
    "DOCUMENTATION_EVIDENCE": "`canonical-plan.md` §5.2 and §7 (road test); `contract-archaeology.md` Table E rows E1/E2; `p1-28-guidance-reconciliation.test.ts` \"‘road test is not part of this release’ — nothing is labelled as one\" pins the road-test sentence to the absence of any control labelled as one.",
    "FINAL_VERDICT": "PASS",
    "VERDICT_RATIONALE": "All three bound kinds are constructed and submitted by this step, verified against the step’s own source rather than against a claim (\"every reachable kind is genuinely constructed by its own step, and submitted\"). The data gate on `condition_item` is honest and self-resolving: the same step opens the inspection a finding hangs off. The two truthful-labelling constraints this row inherits — never \"lift inspection complete\", never a control labelled road test — are enforced by cases that fail on the wording. The change since the last adjudication is a real product fix rather than a re-reading: the required leak type was free text the database refuses, so the step was uncompletable in the running application while every mocked tier passed; it is now the seven values `ck_leak_observations_type` admits, translated, with a case that fails on one option, on eight, or on raw tokens. RULE APPLIED, identically on every row of this file: a task is graded against THE CAPABILITY IT CANONICALLY OWNS — its §5 binding in `canonical-plan.md` — and against nothing else. A row does not stay PARTIAL because a fresh tenant starts empty, PROVIDED both halves are evidenced: its own journey works against a CONFIGURED tenant, and the UNCONFIGURED state is truthful. Equally, a row does not close while its own canonical journey is impossible, and a capability no canonical P1-28 task binds is not this register’s to withhold a verdict over."
  },
  "FE-012": {
    "IMPLEMENTATION_SURFACES": "`apps/web/src/features/receptions/components/steps/DamageMapStep.tsx` (530 lines), registered at `apps/web/src/features/receptions/check-in/steps.tsx:70-75`. The map section carries the block statement where the \"open a map\" control would have been (`DamageMapStep.tsx:149`); the mark form withholds itself with a stated reason when no map exists (`:174`) and submits through `recordConditionEvidence` at `:181-199`.",
    "NORMAL_NAVIGATION_ENTRY": "Fifth step of the check-in wizard.",
    "BACKEND_READ_CONTRACTS": "`rec.reception-condition-evidence-list` filtered to `damage_map` and to `damage_mark` (R1) — two tables read from one step, so the mark form’s choices are derived without a second call (`DamageMapStep.tsx:112-113`). `shared.document-read` exists; a per-reception document LIST does not (G-MEDIA / R8, deferred behind `P1-OD-025`).",
    "BACKEND_WRITE_CONTRACTS": "`rec.reception-condition-evidence` kinds `damage_map` and `damage_mark`. The mark write is genuinely wired (`DamageMapStep.tsx:181-199`); the map write is NOT constructed anywhere in the step, which `apps/web/tests/reception-evidence.test.ts` \"a BLOCKED kind is not constructed anywhere in its step\" asserts as a requirement of a `blocked` coverage row.",
    "REQUEST_SCHEMA": "`damage_map` REQUIRES `documentId` AND `documentVersionId` — both uuids naming a registered template document and the exact version drawn on. `damage_mark` takes `damageMapId`, a mark type from chip/rust/missing, a free-text zone and coordinates that are 0-1 FRACTIONS, submitted exactly as typed (`reception-condition-evidence.dom.test.tsx` \"submits the coordinate the operator TYPED, to the digit\" — `0.125` is not `0.13`).",
    "RESPONSE_SCHEMA": "201 `{receptionVisitId, kind, evidenceId}` per item; the session capture list shows what this session recorded.",
    "REQUIRED_PERMISSIONS": "`rec.reception.evidence.manage` for both kinds; `shared.document.manage` would be needed for the template chain that cannot exist. No sensitive-narrative composition applies — a damage mark carries no personal narrative, which is why the composition record is per-OPERATION and not per-screen (`composed-permissions.json:24-27`).",
    "OWNER_REQUIREMENT_MAPPING": "Journey step S-7, damage documentation. The Owner's media rows are Blocked (INT-093/094/095) and P1-OD-025 governs whether a document may exist at all.",
    "TENANT_ISOLATION_REQUIREMENT": "Visit-scoped path, encoded; cross-tenant renders as not-found.",
    "LOADING_STATE": "`EvidenceStates` skeleton on both read-back tables.",
    "EMPTY_STATE": "With no map, the mark form is withheld and the reason stated (`CoverageNotice` for `damage_mark`, `DamageMapStep.tsx:174`; asserted by \"withholds the mark form with no map, and says why\").",
    "ZERO_RESULT_STATE": "N/A with rationale: the lists are filtered by kind, not by operator input.",
    "ERROR_STATE": "Shared `EvidenceStates` with reference and Retry.",
    "DENIAL_STATE": "`WriteWithdrawn` states WHY the control is gone rather than greying one out (`DamageMapStep.tsx:166`), and it does so for the RIGHT reason — \"withdraws the mark form for a REASON, not as the missing-map gate, when writes are gone\".",
    "CONFLICT_STATE": "N/A with rationale: appending evidence is unguarded.",
    "ARABIC_EVIDENCE": "`receptions.damage.*` and `receptions.evidence.damageMapBlocked` / `damageMapRequired` in `ar.json`; every gated or blocked reason is required to exist in BOTH catalogues (`reception-evidence.test.ts` \"states every gated or blocked reason in BOTH catalogues\"). PRODUCTION: Measured across 18 combinations — desktop 1440, tablet 768 and 200% zoom (640 CSS px at deviceScaleFactor 2) x English/Arabic x the capture, damage-map and signature steps: 0 horizontal clipping on the document or any non-scrolling region, 0 controls without an accessible name, 0 raw translation keys, Arabic always dir=rtl with real Arabic script, and the approved file input keyboard-reachable in every viewport (28 focusable controls, 5 file inputs, 0 removed from the tab order).",
    "ENGLISH_EVIDENCE": "Same keys in `en.json`.",
    "RTL_LTR_EVIDENCE": "Rendered inside the wizard's Arabic/RTL case; coordinate fields stay LTR. PRODUCTION: Measured across 18 combinations — desktop 1440, tablet 768 and 200% zoom (640 CSS px at deviceScaleFactor 2) x English/Arabic x the capture, damage-map and signature steps: 0 horizontal clipping on the document or any non-scrolling region, 0 controls without an accessible name, 0 raw translation keys, Arabic always dir=rtl with real Arabic script, and the approved file input keyboard-reachable in every viewport (28 focusable controls, 5 file inputs, 0 removed from the tab order).",
    "ACCESSIBILITY_EVIDENCE": "The strongest keyboard evidence in the phase: the two number fields are the authoritative inputs and the schematic is a focusable button beside them, so a mark can be placed FROM THE KEYBOARD (\"places the mark from the KEYBOARD, so the diagram is not a pointer-only input\") and arrow keys clamp at the contract bounds instead of leaving the map (\"clamps a keyboard move at the contract bounds instead of leaving the map\"). NAMED GAP: no axe run. The tablet half is CLOSED and the sentence that claimed it was false at this candidate: `apps/web/playwright.config.ts:255` matches `(administration|appointments-and-receptions)`, so `authenticated-tablet` executed 47 P1-28 cases at 1024x768 in run `31750364479` / job `94614564003`. PRODUCTION: Measured across 18 combinations — desktop 1440, tablet 768 and 200% zoom (640 CSS px at deviceScaleFactor 2) x English/Arabic x the capture, damage-map and signature steps: 0 horizontal clipping on the document or any non-scrolling region, 0 controls without an accessible name, 0 raw translation keys, Arabic always dir=rtl with real Arabic script, and the approved file input keyboard-reachable in every viewport (28 focusable controls, 5 file inputs, 0 removed from the tab order).",
    "PRODUCTION_REACHABILITY": "REACHABLE. The reception reads its bindable revisions from rec.reception-evidence-binding-list behind rec.reception.read alone — no receptionist needs rec.catalogue.manage — and DamageMapStep binds the exact revision it was offered.",
    "PRODUCTION_TEST_EVIDENCE": "The whole template chain was published through the product contracts on the running stack: slot 7cf5035b-6959-4576-9f5a-fa6993b63ed0 created, its document authorized, a real object PUT, registered and scanned to accepted, linked to the slot, and revision 1 published. The visit's capture contract then offered exactly one bindable template behind rec.reception.read, the damage step rendered it with no administration control of any kind, and a map was opened from the UI: rec.damage_maps row defe7deb-9dcb-46a9-92d0-62b50427c245 carries document 8f857a6d-92f9-47a3-a881-795cc26fc8ca and version b98352d6-3608-472f-84e2-de8e4f6b7b84 — template revision 1, bound version accepted. Measured across 18 combinations — desktop 1440, tablet 768 and 200% zoom (640 CSS px at deviceScaleFactor 2) x English/Arabic x the capture, damage-map and signature steps: 0 horizontal clipping on the document or any non-scrolling region, 0 controls without an accessible name, 0 raw translation keys, Arabic always dir=rtl with real Arabic script, and the approved file input keyboard-reachable in every viewport (28 focusable controls, 5 file inputs, 0 removed from the tab order).",
    "NEGATIVE_OR_MUTATION_PROOF": "RETIREMENT WAS DRIVEN, and both halves hold. After rec.catalogue-damage-map-template-status-set retired the slot (200 with If-Match), the visit's capture contract returned bindableTemplates: [] — the diagram can no longer start a NEW map — while the map already drawn on it stayed readable through rec.reception-condition-evidence-list with its documentId and documentVersionId intact. A retired template is withdrawn from selection, never from history.",
    "PROTECTED_REPROOF": "PENDING PROTECTED MERGE. Re-observed at this head against the PRODUCTION acceptance stack (next build + next start on both tiers, API 3000, web 3100, real S3-compatible store attached to the API process only): web 98/98 files and 2795/2795 cases, backend 88/88 and 2051/2051, DB/RLS 143/143 and 1711/1711, authenticated browser 365 passed / 0 failed.",
    "FINDING_IDS": "`P1-OD-025` OPEN (Owner decision) and `G-MEDIA` OPEN (R8 deferred, P1-15/P1-18). The reason is now DERIVED rather than asserted, and it is stronger than the sentence it replaced: `apps/web/src/features/receptions/media/media-decision.ts:141-174` records three INDEPENDENT blockers, each re-checked against the repository by `apps/web/tests/p1-28-reception-media.test.ts` — `shared.document_categories` ships zero rows and no published operation creates, updates or lists a category, so the one operation that creates a document fails `ERR-RES-001` for every tenant; `buildStorageProvider()` returns `UnconfiguredStorageProvider` by default and the only alternative the composition root can build is the local fake (`apps/api/src/modules/shared-services/index.ts:222-227`); and a registered version can never leave `pending`. Closing any one leaves the chain dead. Two `apps/web` gate rules additionally ban the capture UI outright while the decision is open — `no-upload-path` and `no-invented-media-limit` (`scripts/ci/check-p1-27-frontend.mjs:910`, `:953`).",
    "DOCUMENTATION_EVIDENCE": "`canonical-plan.md` §5.2 and §7 (`P1-OD-025` blocks the template half); `contract-archaeology.md` Table D rows D1-D3; `media-capture-decision-record.md`.",
    "FINAL_VERDICT": "PASS",
    "VERDICT_RATIONALE": "PASS. The blocker was three facts, not one sentence: damage_map \"requires a registered documentId AND the exact documentVersionId, and neither value can be produced while the document-category table is empty, no storage provider is configured and no version can be accepted\". All three are spent — seven platform reception_* categories are seeded, the acceptance environment configures a real S3-compatible store, and a version reached accepted on this very chain. The map half is no longer unreachable by construction: a template was published through the published operations, offered to the reception behind rec.reception.read, and bound at its exact revision. The mark half was already well proved and is unchanged; what closes the row is that the map a mark hangs off can now exist."
  },
  "FE-013": {
    "IMPLEMENTATION_SURFACES": "The odometer panel of `apps/web/src/features/receptions/components/steps/ReadingsStep.tsx:103-260` (399-line step, registered at `check-in/steps.tsx:76-81`), calling the P1-27 vehicle adapters `apps/web/src/features/vehicles/history-api.ts` (`recordOdometerAction`, `listOdometerReadings`) rather than re-implementing the schema (`ReadingsStep.tsx:8-15`).",
    "NORMAL_NAVIGATION_ENTRY": "Sixth step of the check-in wizard.",
    "BACKEND_READ_CONTRACTS": "`veh.vehicle-odometer-history` — GET /vehicles/{vehicleId}/odometer-readings, cursor-paginated, read for the VISIT'S vehicle (`reception-readings-and-signoff.dom.test.tsx:169-180`).",
    "BACKEND_WRITE_CONTRACTS": "`veh.vehicle-odometer-record` — POST the same path; the reception then references `odometerReadingId` on create. A cross-domain write consumed by Reception, exactly as the walk-in intake consumes the vehicle creation write.",
    "REQUEST_SCHEMA": "`value` is a JSON NUMBER on the way in; the read returns a decimal STRING (INS-19), and no arithmetic crosses the divergence. `reception-readings-and-signoff.dom.test.tsx:190-219` asserts the recorded reading travels bound to the visit's vehicle.",
    "RESPONSE_SCHEMA": "201 with the reading id; history is a `Page<T>` of string-valued readings rendered exactly as received.",
    "REQUIRED_PERMISSIONS": "`veh.vehicle.odometer.record` for the write — its own code, held by neither `veh.vehicle.manage` nor `rec.reception.manage` — and `veh.vehicle.read` for the history. Both resolved at the wizard route (`check-in/[receptionId]/page.tsx:163`, `:155`) and each proved to grant its own control alone (`p1-28-reception-routes.test.ts` \"grants ${field} for ${permission} and for nothing else\").",
    "OWNER_REQUIREMENT_MAPPING": "Owner rows OR-6..9 (the condition-capture block); journey step S-6.",
    "TENANT_ISOLATION_REQUIREMENT": "Vehicle path parameter, encoded; no scope key in the query (gate rule 5).",
    "LOADING_STATE": "Shared table loading on the history through `useServerTable`.",
    "EMPTY_STATE": "The visit's own odometer reference is stated as NOT RECORDED when the visit carries none, rather than left blank (`reception-readings-and-signoff.dom.test.tsx:181-189`).",
    "ZERO_RESULT_STATE": "N/A with rationale: the history is not filtered by operator input.",
    "ERROR_STATE": "A failed history read surfaces with its reference and a retry (`reception-readings-and-signoff.dom.test.tsx:239-250`).",
    "DENIAL_STATE": "The form is withdrawn WITH its reason without `veh.vehicle.odometer.record` (`:220-229`), and a missing vehicle-read permission is STATED instead of rendering an empty history (`:230-238`).",
    "CONFLICT_STATE": "N/A with rationale: recording a reading is unguarded — no `If-Match`.",
    "ARABIC_EVIDENCE": "`receptions.readings.*` plus the reused `vehicles.odometer.*` keys in `ar.json`; Arabic render at `reception-readings-and-signoff.dom.test.tsx:314-324`.",
    "ENGLISH_EVIDENCE": "Same keys in `en.json`.",
    "RTL_LTR_EVIDENCE": "Arabic case mounts RTL; numeric readings stay LTR.",
    "ACCESSIBILITY_EVIDENCE": "Labelled `EvidenceSection`; the numeric field reached by label; the correction path is a link to where the history it corrects is read rather than a second control here. NAMED GAP: no axe run, no tablet case.",
    "PRODUCTION_REACHABILITY": "Registered step covered from the registry; the write is locked on a terminal visit (`reception-readings-and-signoff.dom.test.tsx:251-261`), which is the wizard's `writesLocked` rule over the frozen graph (`reception-checkin-wizard.test.ts` \"locks exactly the terminal statuses\").",
    "PRODUCTION_TEST_EVIDENCE": "`reception-readings-and-signoff.dom.test.tsx:168-261` (6 cases) plus the P1-27 odometer contract suites the adapters carry with them.",
    "NEGATIVE_OR_MUTATION_PROOF": "The step says WHERE a reading lands — on the vehicle, not on the visit — because no `rec.*` operation amends `odometer_reading_id` after create (`ReadingsStep.tsx:49-54`; asserted `:169-180`). Withdraw the permission and the form disappears with a reason rather than failing on submit.",
    "PROTECTED_REPROOF": "PENDING PROTECTED MERGE — RE-OBSERVED for this re-derivation at `a2318c36`: the web tier ran 2669 cases, 0 failed and 0 skipped (28 P1-28 suites, 760 cases), and the root CI tier ran 2475 cases, 0 failed and 0 skipped (5 P1-28 gate suites, 180 cases). THE BROWSER TIER NOW CARRIES THIS PHASE — `apps/web/tests/e2e/authenticated/appointments-and-receptions.spec.ts` (2478 lines, against the running application, the running API and the real database), which the previous derivation correctly recorded as absent. It covers this row at \"the opening intake facts\", where the odometer and state-of-charge reading is exercisable in the running application and never reaches the address bar, and §18 now opens a real visit so the readings step is reachable; the odometer WRITE itself is unobserved in a browser. STILL NOT OBSERVED, and this is the whole outstanding debt on this row: that spec was NOT executed in this derivation — it needs the production launcher (`npm run acceptance:serve`), a live database and, for §18, a provisioned second workspace — and the run reported by the wave that landed it (319 expected, 0 unexpected, 0 flaky) is recorded in no phase document, so no browser result is bound to this head. THE TABLET VIEWPORT AND HOSTED CI WERE OBSERVED AT `38afa5c28e5b78d484a442cf6b8596fb2a5c34aa`, A HEAD THIS CANDIDATE SUPERSEDES. Two sentences stood here and both were false: that the `authenticated-tablet` project sees no P1-28 screen, and that no hosted-CI result exists for this head. `apps/web/playwright.config.ts:254-255` names that project and matches `(administration|appointments-and-receptions)`, so the phase surface is seen at 1024x768 as well as at 1440x900; run `31750364479` / job `94614564003` at head `38afa5c28e5b78d484a442cf6b8596fb2a5c34aa` executed `authenticated/appointments-and-receptions.spec.ts` 141 times — 47 in `authenticated-en`, 47 in `authenticated-ar`, 47 in `authenticated-tablet`, 0 failed and 0 skipped. Hosted CI at that head is run `31750364479`, 21 checks, 21 success, 0 failure, with `ci-gate` (`94617578352`) the single required check. The citation this cell used to give for the tablet claim pointed at lines 225 to 233 of the same config — the project DOCBLOCK, nine consecutive comment lines describing the state BEFORE the widening — so it read as confirmation of a sentence the code beneath it refutes; `validate:p1-28-evidence` now refuses a comment-only citation for exactly that reason. STILL OUTSTANDING ON THIS ROW: the protected-branch re-proof, because PR #226 has not merged, and OWNER ACCEPTANCE, which has not been requested or returned (`docs/phase-1/phase-1-28/evidence/change-log.md:5-11`). THAT HEAD IS NOT THE CURRENT CANDIDATE. Three fix waves changed 37 product files after `38afa5c28e5b78d484a442cf6b8596fb2a5c34aa`, so P1-28-QA-005 re-froze the candidate — at `6392ccb4321b004ed12e5d04ad583298da3303dd` then, and at `3c75f49a01e35b507461bf0929b0046e7140860a` now, the two being product-identical — and every hosted figure above is recorded in `docs/phase-1/phase-1-28/evidence/closure-candidate.json` as PENDING at that candidate — marked `describesSupersededHead`, citing the run it really came from, and listed in `pendingHostedBindings`. No hosted run has been taken at the current candidate yet, so no hosted result is bound to this head; `validate:p1-28-evidence` refuses a cell asserting an observation AT THIS CANDIDATE while the package records none.",
    "FINDING_IDS": "None open. INS-19 (number in, string out) and INS-06 (no `road_test` capture method) are contract facts the step honours rather than defects on this row.",
    "DOCUMENTATION_EVIDENCE": "`canonical-plan.md` §5.2; `contract-archaeology.md` Table C row C8; `operator-guide.md` 'The check-in wizard'.",
    "FINAL_VERDICT": "PASS",
    "VERDICT_RATIONALE": "Both bound operations are wired through the existing P1-27 vehicle adapters rather than a second authority for the same schema, the write is bound to the visit's vehicle on the wire, its own permission code is separated from both neighbouring ones at the route, and the step states the one thing an operator would otherwise discover from a detail read: a reading recorded here joins the vehicle's history and does not amend the visit's reference, because no operation can. Every failure mode — denied read, denied write, failed history, terminal visit — is separately asserted."
  },
  "FE-014": {
    "IMPLEMENTATION_SURFACES": "Capture is at check-in: `apps/web/src/features/receptions/components/CheckInStartScreen.tsx:286-287` (draft state), `:305-306` (into the create input) and `:608-648` (the two controls). Display afterwards is the fuel panel of `ReadingsStep.tsx:94` / `FuelPanel`. The catalogue read is `apps/web/src/features/receptions/catalogue-api.ts:114`.",
    "NORMAL_NAVIGATION_ENTRY": "The check-in start screen's intake fieldset (`CheckInStartScreen.tsx:608-611`), then the readings step for read-back.",
    "BACKEND_READ_CONTRACTS": "`rec.catalogue-fuel-level-list` — registered since R3 and read on the server for a creating operator (`apps/web/src/app/[locale]/(dashboard)/receptions/check-in/page.tsx:95`), through `apps/web/src/features/receptions/catalogue-api.ts:114`. The READ gap is closed and, since PR #227, so is the WRITE gap (`rec.catalogue-fuel-level-create`/`-update`/`-status-set`, behind `rec.catalogue.manage`). What is neither is the SURFACE that would reach those writes, which is `P1-28-OD-001`.",
    "BACKEND_WRITE_CONTRACTS": "Fields of `rec.reception-create`: `fuelLevelId?|null` and `evSocPercent? 0-100|null` (fractional accepted). NO separate operation exists, and no published `rec.*` operation amends either field after the visit is created.",
    "REQUEST_SCHEMA": "Both fields nullable, so check-in proceeds without them; `reception-checkin-wizard.test.ts` \"optional intake facts: absent stays absent, present travels typed\" asserts that an absent optional stays ABSENT rather than being blanked, that a present one travels typed, and that a non-numeric charge level is refused by name.",
    "RESPONSE_SCHEMA": "Carried in the create response's echo of the visit and afterwards by `rec.reception-detail`; the readings panel renders what check-in recorded and says neither can be amended (`reception-readings-and-signoff.dom.test.tsx:263-280`).",
    "REQUIRED_PERMISSIONS": "`rec.reception.manage` — the fields ride the create. The catalogue is read only for an operator who can actually create, so a read-only visitor spends no request discovering a picker they cannot use (`receptions/check-in/page.tsx:95`).",
    "OWNER_REQUIREMENT_MAPPING": "Owner rows within the OR-6..15 condition block; journey step S-6.",
    "TENANT_ISOLATION_REQUIREMENT": "Catalogue read carries no scope key; the create carries the branch pair as its own required field.",
    "LOADING_STATE": "Server-rendered catalogue: the first paint already has the picker or the statement.",
    "EMPTY_STATE": "An empty fuel catalogue is stated as NOT CONFIGURED, never as an error, in both places it appears (`apps/web/src/features/receptions/components/CheckInStartScreen.tsx:627-636`; the readings panel case \"states an empty fuel catalogue as NOT CONFIGURED, never as an error\"), and the browser tier asserts the same sentence against the running application, in both locales, at \"an empty fuel catalogue disables its picker without disabling the form\" — the picker degrades, the check-in does not.",
    "ZERO_RESULT_STATE": "N/A with rationale: a catalogue, not a filtered search.",
    "ERROR_STATE": "A FAILED catalogue read is an ERROR with its reference — a different rendered fact from emptiness (`reception-readings-and-signoff.dom.test.tsx:290-301`).",
    "DENIAL_STATE": "Covered by the create form's own `rec.reception.manage` withdrawal (`reception-checkin.dom.test.tsx` \"without rec.reception.manage the create form is withdrawn, with the reason\").",
    "CONFLICT_STATE": "N/A with rationale: the fields ride an unguarded create.",
    "ARABIC_EVIDENCE": "`receptions.checkIn.fuelLevel`, `fuelEmpty`, `fuelUnavailable`, `evSoc`, `evSocHint` in `ar.json`; Arabic render at `reception-readings-and-signoff.dom.test.tsx:314-324`.",
    "ENGLISH_EVIDENCE": "Same keys in `en.json`.",
    "RTL_LTR_EVIDENCE": "The charge field is explicitly `dir=\"ltr\"` inside an RTL page (`CheckInStartScreen.tsx:645`).",
    "ACCESSIBILITY_EVIDENCE": "Both controls sit in a labelled fieldset with a legend (`CheckInStartScreen.tsx:608-611`) and are reached by label. NAMED GAP: no axe run, no tablet case.",
    "PRODUCTION_REACHABILITY": "Reachable on the check-in start screen for a manage holder; the configured case is proved at the unit tier (\"renders the configured levels when a tenant has any\") and, at this head, against the running application at §18 — the picker is ENABLED, offers the configured level by name, takes it, and the visit opens with it.",
    "PRODUCTION_TEST_EVIDENCE": "`apps/web/tests/reception-readings-and-signoff.dom.test.tsx` \"the readings step — fuel and charge (FE-014)\" (5 cases) and `apps/web/tests/reception-checkin-wizard.test.ts` \"optional intake facts: absent stays absent, present travels typed\" plus \"refuses a non-numeric charge level by name\". PRODUCTION-INTEGRATION evidence, which neither of those is: the browser tier’s §18 visit, where the level and the charge are recorded and READ BACK on the readings step from `rec.reception-detail` rather than echoed from the create response.",
    "NEGATIVE_OR_MUTATION_PROOF": "\"optional intake facts: absent stays absent, present travels typed\" fails if an untouched optional is sent as a blank — a strict route would 422 — and the pair \"states an empty fuel catalogue as NOT CONFIGURED, never as an error\" / \"a failed catalogue read is an ERROR with its reference, not emptiness\" fails if empty and failed are collapsed into one message. In the browser, §18 asserts the NOT-CONFIGURED sentence is ABSENT once the catalogue has a row, so a screen that always said it would fail.",
    "PROTECTED_REPROOF": "PENDING PROTECTED MERGE — RE-OBSERVED for this re-derivation at `a2318c36`: the web tier ran 2669 cases, 0 failed and 0 skipped (28 P1-28 suites, 760 cases), and the root CI tier ran 2475 cases, 0 failed and 0 skipped (5 P1-28 gate suites, 180 cases). THE BROWSER TIER NOW CARRIES THIS PHASE — `apps/web/tests/e2e/authenticated/appointments-and-receptions.spec.ts` (2478 lines, against the running application, the running API and the real database), which the previous derivation correctly recorded as absent. It covers this row in both tenant states: unconfigured at \"an empty fuel catalogue disables its picker without disabling the form\" (both locales) and \"state of charge is offered as an optional reading and accepts one\", and CONFIGURED at §18 \"a visit is opened with a catalogued fuel level and a charge reading, and a lamp is recorded\", which chooses the catalogued level by name, records 64.5% charge, and reads both back on the readings step — plus \"ar: the configured pickers offer their rows in Arabic, on both screens\". STILL NOT OBSERVED, and this is the whole outstanding debt on this row: that spec was NOT executed in this derivation — it needs the production launcher (`npm run acceptance:serve`), a live database and, for §18, a provisioned second workspace — and the run reported by the wave that landed it (319 expected, 0 unexpected, 0 flaky) is recorded in no phase document, so no browser result is bound to this head. THE TABLET VIEWPORT AND HOSTED CI WERE OBSERVED AT `38afa5c28e5b78d484a442cf6b8596fb2a5c34aa`, A HEAD THIS CANDIDATE SUPERSEDES. Two sentences stood here and both were false: that the `authenticated-tablet` project sees no P1-28 screen, and that no hosted-CI result exists for this head. `apps/web/playwright.config.ts:254-255` names that project and matches `(administration|appointments-and-receptions)`, so the phase surface is seen at 1024x768 as well as at 1440x900; run `31750364479` / job `94614564003` at head `38afa5c28e5b78d484a442cf6b8596fb2a5c34aa` executed `authenticated/appointments-and-receptions.spec.ts` 141 times — 47 in `authenticated-en`, 47 in `authenticated-ar`, 47 in `authenticated-tablet`, 0 failed and 0 skipped. Hosted CI at that head is run `31750364479`, 21 checks, 21 success, 0 failure, with `ci-gate` (`94617578352`) the single required check. The citation this cell used to give for the tablet claim pointed at lines 225 to 233 of the same config — the project DOCBLOCK, nine consecutive comment lines describing the state BEFORE the widening — so it read as confirmation of a sentence the code beneath it refutes; `validate:p1-28-evidence` now refuses a comment-only citation for exactly that reason. STILL OUTSTANDING ON THIS ROW: the protected-branch re-proof, because PR #226 has not merged, and OWNER ACCEPTANCE, which has not been requested or returned (`docs/phase-1/phase-1-28/evidence/change-log.md:5-11`). THAT HEAD IS NOT THE CURRENT CANDIDATE. Three fix waves changed 37 product files after `38afa5c28e5b78d484a442cf6b8596fb2a5c34aa`, so P1-28-QA-005 re-froze the candidate — at `6392ccb4321b004ed12e5d04ad583298da3303dd` then, and at `3c75f49a01e35b507461bf0929b0046e7140860a` now, the two being product-identical — and every hosted figure above is recorded in `docs/phase-1/phase-1-28/evidence/closure-candidate.json` as PENDING at that candidate — marked `describesSupersededHead`, citing the run it really came from, and listed in `pendingHostedBindings`. No hosted run has been taken at the current candidate yet, so no hosted result is bound to this head; `validate:p1-28-evidence` refuses a cell asserting an observation AT THIS CANDIDATE while the package records none.",
    "FINDING_IDS": "TWO findings, distinct, and only one of them is about emptiness. FIRST, `P1-28-OD-001` OPEN: `rec.fuel_levels` ships zero rows and no screen in this product reaches the management contract that would fill it. That blocks a catalogue-administration surface no canonical P1-28 task binds, and the browser tier now proves this row’s own journey works the moment a tenant is configured. SECOND, UNMOVED, and NOT this decision: no published `rec.*` operation amends `fuelLevelId` or `evSocPercent` after a visit is created, so an operator who records the wrong level has no correction path. That is a P1-18 contract gap outside this row’s §5.2 binding — which is `rec.reception-create [fields fuelLevelId/evSocPercent]`, the create and not an amend — and the readings panel states it on screen rather than hiding it (\"renders what check-in recorded, and says neither can be amended\"). OWNER: P1-18.",
    "DOCUMENTATION_EVIDENCE": "`canonical-plan.md` §5.2; `contract-archaeology.md` Table B rows B1/B12-B13; `operator-guide.md` 'The check-in wizard'.",
    "FINAL_VERDICT": "PASS",
    "VERDICT_RATIONALE": "RE-ADJUDICATED at this head on new evidence, and the two reasons this row was PARTIAL are answered differently. RULE APPLIED, identically on every row of this file: a task is graded against THE CAPABILITY IT CANONICALLY OWNS — its §5 binding in `canonical-plan.md` — and against nothing else. A row does not stay PARTIAL because a fresh tenant starts empty, PROVIDED both halves are evidenced: its own journey works against a CONFIGURED tenant, and the UNCONFIGURED state is truthful. Equally, a row does not close while its own canonical journey is impossible, and a capability no canonical P1-28 task binds is not this register’s to withhold a verdict over. On the FIRST — the empty catalogue — §18 records a real visit with a catalogued fuel level chosen by name and a state-of-charge reading, then READS BOTH BACK on the readings step from `rec.reception-detail`, so the round trip through storage is observed rather than the create response being replayed; the unconfigured sentence is asserted in the same run, in both locales, so nothing was deleted to get there. On the SECOND — that neither field can be amended after check-in — the honest answer is that it is outside this row’s canonical binding: §5.2 binds `rec.reception-create [fields fuelLevelId/evSocPercent]`, and the bracketed qualifier narrows the operation to the part of the contract this task consumes. No amend operation exists anywhere in the platform for any task to bind, the gap is P1-18’s, and the screen states it where an operator meets it. Grading this row down for a contract no canonical task owns would be the same category error as grading FE-002 down for an administration screen no canonical task owns; the gap is recorded in FINDING_IDS with its owner instead."
  },
  "FE-015": {
    "IMPLEMENTATION_SURFACES": "`apps/web/src/features/receptions/components/steps/WarningLightsStep.tsx` (315 lines), registered at `apps/web/src/features/receptions/check-in/steps.tsx:82-87`; catalogue read `apps/web/src/features/receptions/catalogue-api.ts:99`; write through `apps/web/src/features/receptions/api.ts` (`recordConditionEvidence`, `WarningLightsStep.tsx:179-193`). The unconfigured branch is `:141-162`; the capture form is `:258-314`, with the lamp select at `:267-281` and the observed-state select at `:282-295`.",
    "NORMAL_NAVIGATION_ENTRY": "Seventh step of the check-in wizard — the step exists so the absence is met where an operator would look for the control, not hidden by omitting the step.",
    "BACKEND_READ_CONTRACTS": "`rec.catalogue-warning-light-code-list` — registered since R3. R3 fixed reachability, not emptiness: a read does not populate a table. PR #227 has since added the management writes (`rec.catalogue-warning-light-code-create`/`-update`/`-status-set`, behind `rec.catalogue.manage`), and no screen in this product reaches them, which is `P1-28-OD-001`.",
    "BACKEND_WRITE_CONTRACTS": "`rec.reception-condition-evidence` with `kind:'warning_light'`. The step genuinely constructs and submits it — proved at the unit tier by \"records a lamp the moment the catalogue has one\" and, at this head, against the running application by §18, where a lamp is recorded from a configured catalogue and read back off the visit. This is a data gate, not dead code.",
    "REQUEST_SCHEMA": "`{warningLightCodeId, observedState?, note?}`. `observedState` is a CLOSED database vocabulary, not a bounded string: `ck_warning_light_observations_state` admits exactly `on`, `flashing`, `intermittent` (`supabase/migrations/20260721102000_rec_warning_lights_leaks.sql:60`), restated with its authority in `apps/web/src/features/receptions/receptions-contract.ts:643-662` and offered as a translated select. An untouched optional stays ABSENT rather than travelling blank, because the column has its own default and a strict route refuses an empty string (`WarningLightsStep.tsx:184-189`).",
    "RESPONSE_SCHEMA": "201 `{receptionVisitId, kind:'warning_light', evidenceId}`; the session capture list shows what this session recorded.",
    "REQUIRED_PERMISSIONS": "`rec.reception.evidence.manage`. No sensitive-narrative composition — a warning lamp is not a restricted narrative.",
    "OWNER_REQUIREMENT_MAPPING": "Owner rows within the OR-6..15 block; journey step S-7. The catalogue-population decision is plan §7's second open decision and it names this task as the only one it blocks.",
    "TENANT_ISOLATION_REQUIREMENT": "Visit-scoped path, encoded; the catalogue read carries no scope key.",
    "LOADING_STATE": "`EvidenceStates` skeleton on the read-back table.",
    "EMPTY_STATE": "The catalogue is stated as NOT CONFIGURED and no codes are invented (\"states that the catalogue is not configured, and invents no codes\"); no disabled control is offered, because a disabled control implies a capability that exists (`WarningLightsStep.tsx:141-162`).",
    "ZERO_RESULT_STATE": "N/A with rationale: a catalogue, not a filtered search.",
    "ERROR_STATE": "An unreadable catalogue is an ERROR with retry, NEVER 'not configured' (`reception-condition-evidence.dom.test.tsx` \"an unreadable catalogue is an ERROR with retry, never “not configured”\") — the two are different facts and the case fails if they are merged.",
    "DENIAL_STATE": "`WriteWithdrawn` with its reason without the evidence permission and on a terminal visit.",
    "CONFLICT_STATE": "N/A with rationale: appending evidence is unguarded.",
    "ARABIC_EVIDENCE": "`receptions.warning.*`, the three `receptions.warningState.*` labels and `receptions.evidence.warningCatalogueEmpty` in `ar.json` — the gated reason is required in BOTH catalogues (\"states every gated or blocked reason in BOTH catalogues\"); Arabic render at \"renders in Arabic, RTL\".",
    "ENGLISH_EVIDENCE": "Same keys in `en.json`.",
    "RTL_LTR_EVIDENCE": "Arabic case mounts RTL.",
    "ACCESSIBILITY_EVIDENCE": "Labelled `EvidenceSection` and a labelled `form`; both controls are selects reached by label, and while the catalogue is empty the statement is text rather than a greyed control, so nothing unreachable is presented to a screen reader as an option. NAMED GAP: no axe run. The tablet half is CLOSED and the sentence that claimed it was false at this candidate: `apps/web/playwright.config.ts:255` matches `(administration|appointments-and-receptions)`, so `authenticated-tablet` executed 47 P1-28 cases at 1024x768 in run `31750364479` / job `94614564003`.",
    "PRODUCTION_REACHABILITY": "The step is registered and rendered, and `warning_light` is classified `data_gated` with its on-screen statement named (`apps/web/src/features/receptions/check-in/evidence.ts:147`). `apps/web/tests/reception-evidence.test.ts` \"classifies the two kinds this release cannot fully deliver, and says why\" requires that classification to explain itself.",
    "PRODUCTION_TEST_EVIDENCE": "`apps/web/tests/reception-condition-evidence.dom.test.tsx` \"the warning-lights step (FE-015)\" (5 cases) and the coverage-table cases in `apps/web/tests/reception-evidence.test.ts`, including \"reads a CLOSED database vocabulary back translated, never as the stored token\". Production-integration evidence: the browser tier’s §18 lamp.",
    "NEGATIVE_OR_MUTATION_PROOF": "\"an unreadable catalogue is an ERROR with retry, never 'not configured'\" fails on the exact conflation that would hide an outage behind a policy statement; \"records a lamp the moment the catalogue has one\" fails if the write stops being constructed, which is what would turn the data gate into dead code; \"an untouched state is ABSENT from the body, not sent blank\" fails if the optional is blanked. In the browser, §18 asserts the observed state is read back TRANSLATED — a raw `flashing` reaching an operator is the same defect class as a raw message key — so a read-back that printed the stored token would fail.",
    "PROTECTED_REPROOF": "PENDING PROTECTED MERGE — RE-OBSERVED for this re-derivation at `a2318c36`: the web tier ran 2669 cases, 0 failed and 0 skipped (28 P1-28 suites, 760 cases), and the root CI tier ran 2475 cases, 0 failed and 0 skipped (5 P1-28 gate suites, 180 cases). THE BROWSER TIER NOW CARRIES THIS PHASE — `apps/web/tests/e2e/authenticated/appointments-and-receptions.spec.ts` (2478 lines, against the running application, the running API and the real database), which the previous derivation correctly recorded as absent. It covers this row in both tenant states: the unconfigured statement is asserted wherever an operator meets it, and CONFIGURED at §18 \"a visit is opened with a catalogued fuel level and a charge reading, and a lamp is recorded\", where the configured lamp is offered by name, `flashing` is chosen from the list, the record is listed for the session AND read back off the visit, and the observed state reads back translated rather than as the stored token. STILL NOT OBSERVED, and this is the whole outstanding debt on this row: that spec was NOT executed in this derivation — it needs the production launcher (`npm run acceptance:serve`), a live database and, for §18, a provisioned second workspace — and the run reported by the wave that landed it (319 expected, 0 unexpected, 0 flaky) is recorded in no phase document, so no browser result is bound to this head. THE TABLET VIEWPORT AND HOSTED CI WERE OBSERVED AT `38afa5c28e5b78d484a442cf6b8596fb2a5c34aa`, A HEAD THIS CANDIDATE SUPERSEDES. Two sentences stood here and both were false: that the `authenticated-tablet` project sees no P1-28 screen, and that no hosted-CI result exists for this head. `apps/web/playwright.config.ts:254-255` names that project and matches `(administration|appointments-and-receptions)`, so the phase surface is seen at 1024x768 as well as at 1440x900; run `31750364479` / job `94614564003` at head `38afa5c28e5b78d484a442cf6b8596fb2a5c34aa` executed `authenticated/appointments-and-receptions.spec.ts` 141 times — 47 in `authenticated-en`, 47 in `authenticated-ar`, 47 in `authenticated-tablet`, 0 failed and 0 skipped. Hosted CI at that head is run `31750364479`, 21 checks, 21 success, 0 failure, with `ci-gate` (`94617578352`) the single required check. The citation this cell used to give for the tablet claim pointed at lines 225 to 233 of the same config — the project DOCBLOCK, nine consecutive comment lines describing the state BEFORE the widening — so it read as confirmation of a sentence the code beneath it refutes; `validate:p1-28-evidence` now refuses a comment-only citation for exactly that reason. STILL OUTSTANDING ON THIS ROW: the protected-branch re-proof, because PR #226 has not merged, and OWNER ACCEPTANCE, which has not been requested or returned (`docs/phase-1/phase-1-28/evidence/change-log.md:5-11`). THAT HEAD IS NOT THE CURRENT CANDIDATE. Three fix waves changed 37 product files after `38afa5c28e5b78d484a442cf6b8596fb2a5c34aa`, so P1-28-QA-005 re-froze the candidate — at `6392ccb4321b004ed12e5d04ad583298da3303dd` then, and at `3c75f49a01e35b507461bf0929b0046e7140860a` now, the two being product-identical — and every hosted figure above is recorded in `docs/phase-1/phase-1-28/evidence/closure-candidate.json` as PENDING at that candidate — marked `describesSupersededHead`, citing the run it really came from, and listed in `pendingHostedBindings`. No hosted run has been taken at the current candidate yet, so no hosted result is bound to this head; `validate:p1-28-evidence` refuses a cell asserting an observation AT THIS CANDIDATE while the package records none.",
    "FINDING_IDS": "DEFECT FOUND AND FIXED AT THIS HEAD: `observedState` was a free-text box whose hint invited the operator’s own words, while the database admits exactly three tokens — measured against the running stack, `observedState: 'steady while running'` answered 422 `ERR-VAL-001 / incoherent_reference` and rendered as \"This value is not accepted here\" with no way to learn what would be accepted (`receptions-contract.ts:643-662`). It is now a translated select over the migration’s own vocabulary. RMC-11 — HALF CLOSED, HALF OPEN, and the halves must not be conflated: the contract half is closed (PR #227 registered create, update and status-set behind `rec.catalogue.manage`, and `canonical-plan.md` §7 has been corrected rather than left stating otherwise); the rows-still-zero half is `P1-28-OD-001`, a surface question no canonical P1-28 task binds. The no-fake-data policy still forbids a seed, and a seed would answer neither question.",
    "DOCUMENTATION_EVIDENCE": "`canonical-plan.md` §5.2 and §7 (warning-light catalogue population, OPEN); `contract-archaeology.md` Table B rows B4/B12-B13.",
    "FINAL_VERDICT": "PASS",
    "VERDICT_RATIONALE": "RE-ADJUDICATED at this head, and both halves of the change are real rather than a re-reading. RULE APPLIED, identically on every row of this file: a task is graded against THE CAPABILITY IT CANONICALLY OWNS — its §5 binding in `canonical-plan.md` — and against nothing else. A row does not stay PARTIAL because a fresh tenant starts empty, PROVIDED both halves are evidenced: its own journey works against a CONFIGURED tenant, and the UNCONFIGURED state is truthful. Equally, a row does not close while its own canonical journey is impossible, and a capability no canonical P1-28 task binds is not this register’s to withhold a verdict over. FIRST, a product defect on this row was found and fixed: the observed-state control was free text the database refuses, so an operator who used it met \"This value is not accepted here\" and could not learn what would be accepted; it is now the three tokens `ck_warning_light_observations_state` admits, translated, restated from the migration with its authority recorded beside the list. SECOND, the capture is proved end to end against the running application: §18 records a lamp from a configured catalogue, lists it for the session, and reads it back off the visit with the observed state TRANSLATED rather than as the stored token. The unconfigured statement is unchanged and still asserted, and the step still keeps NOT CONFIGURED and unreadable as two different facts. What remains open is `P1-28-OD-001` — who administers the intake catalogues and through which surface — which blocks a screen no canonical P1-28 task binds, and is recorded there rather than withheld here."
  },
  "FE-016": {
    "IMPLEMENTATION_SURFACES": "`apps/web/src/features/receptions/components/steps/ContentsStep.tsx` (282 lines), registered at `check-in/steps.tsx:88-93`; write through `api.ts`; the WF-27 gate and denial supplement from `check-in/sensitive.ts:75`/`:98`; the draft rules in `check-in/evidence.ts` (`contentsProblems`, `contentsOptionalFields`).",
    "NORMAL_NAVIGATION_ENTRY": "Eighth step of the check-in wizard.",
    "BACKEND_READ_CONTRACTS": "`rec.reception-condition-evidence-list` filtered to `contents` (R1). Like complaints, the union deliberately excludes the narrative table, so the read returns the quantity and location envelope and never the description, value or currency (`p1-28-sensitive-narratives.dom.test.tsx:334-361`).",
    "BACKEND_WRITE_CONTRACTS": "`rec.reception-condition-evidence` with `kind:'contents'`. REACHABLE as one of the five condition-evidence call sites.",
    "REQUEST_SCHEMA": "Item description, quantity (positive whole number), location, declared value and a three-letter currency; the currency CHECK is mirrored in both directions — a currency with no value beside it is refused BESIDE THE CURRENCY before a request is spent (`reception-evidence.test.ts` \"refuses a currency with no value beside it — ck_vehicle_content_details_currency\"; `reception-condition-evidence.dom.test.tsx` \"refuses a currency with no value BESIDE THE CURRENCY, before spending a request\").",
    "RESPONSE_SCHEMA": "201 `{receptionVisitId, kind:'contents', evidenceId}`; the read-back publishes only the quantity and the location, and the step SAYS the rest is restricted (`reception-condition-evidence.dom.test.tsx` \"reads back only the quantity and the location, and says the rest is restricted\").",
    "REQUIRED_PERMISSIONS": "`rec.reception.evidence.manage` PLUS `iam.sensitive.view` — the same WF-27 pair as FE-010, recorded once as a fact about the OPERATION and verified against `supabase/migrations/20260721103000_rec_vehicle_contents.sql` policy `ins_vehicle_content_details_gated` by the access gate (`composed-permissions.json:40-43`).",
    "OWNER_REQUIREMENT_MAPPING": "Owner rows within the OR-6..15 block; journey step S-7 (vehicle contents at custody).",
    "TENANT_ISOLATION_REQUIREMENT": "Visit-scoped path, encoded; cross-tenant renders as not-found.",
    "LOADING_STATE": "`EvidenceStates` skeleton on the read-back table.",
    "EMPTY_STATE": "An empty read-back still states what the read cannot return, so emptiness is not read as the whole truth (`p1-28-sensitive-narratives.dom.test.tsx:299-307`).",
    "ZERO_RESULT_STATE": "N/A with rationale: filtered by kind, not by operator input.",
    "ERROR_STATE": "Shared `EvidenceStates` with reference and Retry.",
    "DENIAL_STATE": "Capture is withheld without the sensitive-data capability with the requirement stated in both languages (`p1-28-sensitive-narratives.dom.test.tsx:137-178`); an ENDED visit says the visit ended, not the permission (`:179-198`) — the ordering rule of `narrativeGate`.",
    "CONFLICT_STATE": "N/A with rationale: appending evidence is unguarded.",
    "ARABIC_EVIDENCE": "`receptions.contents.*` in `ar.json`; every problem the draft validator can name exists in both catalogues (`reception-evidence.test.ts` \"states every problem it can name in both catalogues\"); Arabic render at `reception-condition-evidence.dom.test.tsx` \"renders in Arabic, RTL\".",
    "ENGLISH_EVIDENCE": "Same keys in `en.json`.",
    "RTL_LTR_EVIDENCE": "Arabic case mounts RTL; the currency and value fields stay LTR.",
    "ACCESSIBILITY_EVIDENCE": "Labelled `EvidenceSection`; each refusal lands on the control it is about rather than in a summary. NAMED GAP: no axe run, no tablet case.",
    "PRODUCTION_REACHABILITY": "Registered step covered from the registry; `contents` is `wired` in the coverage table and proved constructed by its own step.",
    "PRODUCTION_TEST_EVIDENCE": "`apps/web/tests/reception-condition-evidence.dom.test.tsx` \"the contents step (FE-016)\" (5 cases), `apps/web/tests/reception-evidence.test.ts` \"contentsProblems — the route schema and the currency CHECK, mirrored\" and \"contentsOptionalFields — omitted, never blanked\" (12 cases over the draft rules), `apps/web/tests/p1-28-sensitive-narratives.dom.test.tsx` (12).",
    "NEGATIVE_OR_MUTATION_PROOF": "\"leaves every untouched control off the body\" fails if an untouched control is sent as a blank instead of being omitted, and \"refuses a currency with no value beside it — ck_vehicle_content_details_currency\" fails if the pair rule is dropped. \"sends the signed-in operator as the witness only when asked\" proves the witness is never volunteered.",
    "PROTECTED_REPROOF": "PENDING PROTECTED MERGE — RE-OBSERVED for this re-derivation at `a2318c36`: the web tier ran 2669 cases, 0 failed and 0 skipped (28 P1-28 suites, 760 cases), and the root CI tier ran 2475 cases, 0 failed and 0 skipped (5 P1-28 gate suites, 180 cases). THE BROWSER TIER NOW CARRIES THIS PHASE — `apps/web/tests/e2e/authenticated/appointments-and-receptions.spec.ts` (2478 lines, against the running application, the running API and the real database), which the previous derivation correctly recorded as absent. It covers the wizard shell in the running application: \"the check-in wizard\" loads a visit’s thirteen-step registry through the real API, and §18 now opens a REAL visit and reaches that shell. This step’s own panels are exercised only as far as that goes — a limit this row states rather than implies. STILL NOT OBSERVED, and this is the whole outstanding debt on this row: that spec was NOT executed in this derivation — it needs the production launcher (`npm run acceptance:serve`), a live database and, for §18, a provisioned second workspace — and the run reported by the wave that landed it (319 expected, 0 unexpected, 0 flaky) is recorded in no phase document, so no browser result is bound to this head. THE TABLET VIEWPORT AND HOSTED CI WERE OBSERVED AT `38afa5c28e5b78d484a442cf6b8596fb2a5c34aa`, A HEAD THIS CANDIDATE SUPERSEDES. Two sentences stood here and both were false: that the `authenticated-tablet` project sees no P1-28 screen, and that no hosted-CI result exists for this head. `apps/web/playwright.config.ts:254-255` names that project and matches `(administration|appointments-and-receptions)`, so the phase surface is seen at 1024x768 as well as at 1440x900; run `31750364479` / job `94614564003` at head `38afa5c28e5b78d484a442cf6b8596fb2a5c34aa` executed `authenticated/appointments-and-receptions.spec.ts` 141 times — 47 in `authenticated-en`, 47 in `authenticated-ar`, 47 in `authenticated-tablet`, 0 failed and 0 skipped. Hosted CI at that head is run `31750364479`, 21 checks, 21 success, 0 failure, with `ci-gate` (`94617578352`) the single required check. The citation this cell used to give for the tablet claim pointed at lines 225 to 233 of the same config — the project DOCBLOCK, nine consecutive comment lines describing the state BEFORE the widening — so it read as confirmation of a sentence the code beneath it refutes; `validate:p1-28-evidence` now refuses a comment-only citation for exactly that reason. STILL OUTSTANDING ON THIS ROW: the protected-branch re-proof, because PR #226 has not merged, and OWNER ACCEPTANCE, which has not been requested or returned (`docs/phase-1/phase-1-28/evidence/change-log.md:5-11`). THAT HEAD IS NOT THE CURRENT CANDIDATE. Three fix waves changed 37 product files after `38afa5c28e5b78d484a442cf6b8596fb2a5c34aa`, so P1-28-QA-005 re-froze the candidate — at `6392ccb4321b004ed12e5d04ad583298da3303dd` then, and at `3c75f49a01e35b507461bf0929b0046e7140860a` now, the two being product-identical — and every hosted figure above is recorded in `docs/phase-1/phase-1-28/evidence/closure-candidate.json` as PENDING at that candidate — marked `describesSupersededHead`, citing the run it really came from, and listed in `pendingHostedBindings`. No hosted run has been taken at the current candidate yet, so no hosted result is bound to this head; `validate:p1-28-evidence` refuses a cell asserting an observation AT THIS CANDIDATE while the package records none.",
    "FINDING_IDS": "None open. The witness field shares FE-007's G-EMP referent problem and states the same disposition rather than presenting a resolved employee.",
    "DOCUMENTATION_EVIDENCE": "`canonical-plan.md` §5.2; `contract-archaeology.md` Table B row B4 and riskiest-fact 4; `operator-guide.md` 'The check-in wizard'.",
    "FINAL_VERDICT": "PASS",
    "VERDICT_RATIONALE": "The bound write is wired with its full envelope, the database CHECK on currency-without-value is mirrored client-side and refused beside the currency before a request is spent, and optional fields are omitted rather than blanked. The WF-27 pair is handled exactly as on FE-010, with the gate ordered by what is most true — an ended visit says the visit ended, not the permission — and the read-back states, on both an empty and a populated list, that the description, value and currency are not returned, so a thin row is never mistaken for data loss."
  },
  "FE-017": {
    "IMPLEMENTATION_SURFACES": "`apps/web/src/features/receptions/components/steps/MediaStep.tsx` (339 lines: the resolved capture policy read at `:90`, the per-requirement row at `:181`, the capture form at `:264` and the authorized waiver at `:280-284`), registered as a wizard step at `apps/web/src/features/receptions/check-in/steps.tsx:106`. Behind the form is the Server Action `apps/web/src/features/receptions/evidence-capture.ts` (`captureRequirementEvidence`, 207 lines: the authorize-and-PUT call at `:123`, the link at `:145`, the binding at `:154`, and a finalization attempted only against an accepted version at `:167-183`), and the ONE approved file input is `apps/web/src/features/receptions/components/CaptureFileField.tsx` (`:44`) — the single entry in `FILE_INPUT_ALLOW`, shared with the signature step so the allowance stays at one path as capture surfaces multiply. `apps/web/src/features/receptions/media/media-decision.ts` is 83 lines and records only that `P1-OD-025` is RESOLVED (`:56`, `:59`); the notice it used to render is deleted, and the states an operator meets are derived per capture from what the API answered rather than held in a constant (`MediaStep.tsx:174`).",
    "NORMAL_NAVIGATION_ENTRY": "Ninth step of the check-in wizard, placed between the contents and the signatures — where the operator would reach for the camera (`check-in/steps.tsx:95-102`; asserted `p1-28-reception-media.dom.test.tsx` \"puts the notice where the photographs would have gone, and nothing else\").",
    "BACKEND_READ_CONTRACTS": "N/A with rationale: nothing is read. `shared.document-read` exists but a per-reception document list and a category enumeration do not (G-MEDIA D2/D3, R8 deferred), and reading a document this product cannot create would render an empty panel that implies a capability.",
    "BACKEND_WRITE_CONTRACTS": "N/A with rationale, and it is the deliverable: `shared.attachment-upload-authorize`, `-version-register` and `-link-create` are complete contracts that this phase deliberately does not call. The chain is dead-ended anyway, for three INDEPENDENT reasons now derived from the repository rather than asserted (`media-decision.ts:141-174`): the document-category table ships zero rows and no published operation adds one, `buildStorageProvider()` returns `UnconfiguredStorageProvider` by default (`apps/api/src/modules/shared-services/index.ts:222-227`), and a registered version can never leave `pending`.",
    "REQUEST_SCHEMA": "N/A with rationale: no request is made. The gate rules `no-upload-path` (`scripts/ci/check-p1-27-frontend.mjs:910`) and `no-invented-media-limit` (`:953`) refuse capture code and invented ceilings while `P1-OD-025` is open, and `apps/web/tests/p1-28-reception-media.test.ts` \"applies rules that exist and that fire — a clean sweep of nothing proves nothing\" asserts the applied rules EXIST and FIRE.",
    "RESPONSE_SCHEMA": "N/A with rationale: no response. The wording rule stands in its place — the honest ceiling of any registered file is 'registered, pending', never 'uploaded', held across every `receptions.*` string in both catalogues (`p1-28-reception-media.test.ts` \"holds across every `receptions.*` string in both catalogues\") with the one surviving `uploaded` in the source proved to be a backend vocabulary member that is never rendered (\"leaves exactly one `uploaded` in the source, and it is a backend vocabulary member\").",
    "REQUIRED_PERMISSIONS": "N/A with rationale: the notice consults no permission and calls no operation, so it requires none — and it renders identically on a terminal visit, because the block is the release and not the state (`p1-28-reception-media.dom.test.tsx` \"shows the same notice on a terminal visit — the block is the release, not the state\").",
    "OWNER_REQUIREMENT_MAPPING": "Owner rows OR-12/13/14, explicitly Blocked (INT-093/094/095, `owner-workflow-requirements.md:160-162`). Only the COUNT of seven exterior angles is fixed; the angle set is an unconfirmed proposal; whether missing media blocks approval is an unmade Owner decision.",
    "TENANT_ISOLATION_REQUIREMENT": "N/A with rationale: no request leaves this surface, so there is no scope to assert or leak.",
    "LOADING_STATE": "N/A with rationale: nothing loads.",
    "EMPTY_STATE": "N/A with rationale: there is no list. What stands where a gallery would be is the notice, and the sweep proves the notice and NOTHING ELSE is there (`p1-28-reception-media.dom.test.tsx` \"puts the notice where the photographs would have gone, and nothing else\").",
    "ZERO_RESULT_STATE": "N/A with rationale: no search.",
    "ERROR_STATE": "N/A with rationale: no request can fail.",
    "DENIAL_STATE": "N/A with rationale: no permission is consulted; the notice is shown to every operator because the block is not a permission problem.",
    "CONFLICT_STATE": "N/A with rationale: no write.",
    "ARABIC_EVIDENCE": "All fifteen media strings are pinned EXACTLY, in both catalogues, so any edit is a deliberate one, and a sixteenth string the pin never saw fails (`p1-28-reception-media.test.ts` \"is EXACTLY these fifteen strings, in both catalogues — any edit is a deliberate one\"). The notice renders in Arabic right-to-left with the decision identifier still left-to-right (`p1-28-reception-media.dom.test.tsx` \"renders in Arabic, right-to-left, with the identifier still left-to-right\"). PRODUCTION: Measured across 18 combinations — desktop 1440, tablet 768 and 200% zoom (640 CSS px at deviceScaleFactor 2) x English/Arabic x the capture, damage-map and signature steps: 0 horizontal clipping on the document or any non-scrolling region, 0 controls without an accessible name, 0 raw translation keys, Arabic always dir=rtl with real Arabic script, and the approved file input keyboard-reachable in every viewport (28 focusable controls, 5 file inputs, 0 removed from the tab order).",
    "ENGLISH_EVIDENCE": "The same fifteen strings in `en.json`, with the English render asserting the block, the named decision and what the Owner must decide (`p1-28-reception-media.dom.test.tsx` \"states the block, names P1-OD-025, and says what the Owner must decide (English)\").",
    "RTL_LTR_EVIDENCE": "`p1-28-reception-media.dom.test.tsx` \"renders in Arabic, right-to-left, with the identifier still left-to-right\" — RTL page, LTR identifier. PRODUCTION: Measured across 18 combinations — desktop 1440, tablet 768 and 200% zoom (640 CSS px at deviceScaleFactor 2) x English/Arabic x the capture, damage-map and signature steps: 0 horizontal clipping on the document or any non-scrolling region, 0 controls without an accessible name, 0 raw translation keys, Arabic always dir=rtl with real Arabic script, and the approved file input keyboard-reachable in every viewport (28 focusable controls, 5 file inputs, 0 removed from the tab order).",
    "ACCESSIBILITY_EVIDENCE": "The only P1-28 surface with an axe run: no critical or serious violation in BOTH directions (`p1-28-reception-media.dom.test.tsx` \"has no critical or serious accessibility violation, in both directions\"). PRODUCTION: Measured across 18 combinations — desktop 1440, tablet 768 and 200% zoom (640 CSS px at deviceScaleFactor 2) x English/Arabic x the capture, damage-map and signature steps: 0 horizontal clipping on the document or any non-scrolling region, 0 controls without an accessible name, 0 raw translation keys, Arabic always dir=rtl with real Arabic script, and the approved file input keyboard-reachable in every viewport (28 focusable controls, 5 file inputs, 0 removed from the tab order).",
    "PRODUCTION_REACHABILITY": "REACHABLE, and derived rather than declared. All four operations this task owns — rec.reception-evidence-binding, -binding-finalize, rec.reception-capture-override and the attachment chain behind them — are classified REACHABLE in write-reachability.json, and check-p1-28-write-reachability.mjs confirms each against a consumed production call site (43 canonical writes: 18 REACHABLE, 25 DELIBERATELY_ABSENT, 0 NOT_YET_WIRED). The adapter manifest declares nothing at all: 71 published apt/rec operations, 0 PENDING_FRONTEND_ADAPTER.",
    "PRODUCTION_TEST_EVIDENCE": "REAL END-TO-END CAPTURE, driven from the operator UI against the production stack on visit b3942d34-cb25-41a9-b0f2-b83cb04dd8fd. Browser file selection -> the one approved CaptureFileField -> the same-origin Server Action -> shared.attachment-upload-authorize (signed capability issued, provider s3_compatible) -> a real object PUT -> shared.attachment-version-register, which read the object back, hashed it and completed the scan -> version accepted -> shared.attachment-link-create -> rec.reception-evidence-binding -> rec.reception-evidence-binding-finalize. Binding d137180b-1491-4623-9bfb-3bc4eced739b, requirement vin, document 9bfd86f9-eeea-4917-924f-3d57bbac478f, version db63a55f-f6ac-47e3-96f0-b345dc9d06bb number 1, status accepted, category reception_vin, link purpose identity_document, image/png 912 bytes, sha256 de6cecf4...6966a1, finalized. The UI moved 0/1 \"Nothing recorded yet.\" -> 1/1 \"Met.\". The OBJECT was then read back through the repository's own S3StorageProvider.readObject and its length, content type and SHA-256 all match the persisted record — a version row is metadata, and this phase has already recorded once what it costs to accept metadata as proof that bytes exist. Measured across 18 combinations — desktop 1440, tablet 768 and 200% zoom (640 CSS px at deviceScaleFactor 2) x English/Arabic x the capture, damage-map and signature steps: 0 horizontal clipping on the document or any non-scrolling region, 0 controls without an accessible name, 0 raw translation keys, Arabic always dir=rtl with real Arabic script, and the approved file input keyboard-reachable in every viewport (28 focusable controls, 5 file inputs, 0 removed from the tab order).",
    "NEGATIVE_OR_MUTATION_PROOF": "The rejection path is REAL, not simulated. An EICAR test signature declared image/png was captured through the same UI: the object stored and was read back (byte_validation true, size_matches true, hash_matches true, metadata_content_type_matches true), the scanner returned infected / eicar_test_signature, and shared.guard_document_version_transition refused acceptance — version 8ae6f03e-3c7a-407d-8949-1c1236af7655 is quarantined with accepted_at NULL and ZERO evidence bindings. The screen reported \"Nothing was recorded\", the count stayed 0/1 and the requirement never read as satisfied. THE OVERRIDE is separated by authority as the Owner decision requires: the authorized operator saw the control, supplied the mandatory reason, and the waiver was recorded with its actor, timestamp, requirement and branch scope plus the audit event rec.reception.capture_requirement_overridden; the read-only principal was refused by the SERVER with 403 ERR-IAM-001, not merely shown no button. Forged and cross-tenant attempts were all refused: a Tenant A receiving employee 422, a fabricated binding 404, a fabricated signature 404, Tenant B reading and Tenant B binding this visit 404, and no token at all 401.",
    "PROTECTED_REPROOF": "PENDING PROTECTED MERGE. Re-observed at this head against the PRODUCTION acceptance stack (next build + next start on both tiers, API 3000, web 3100, real S3-compatible store attached to the API process only): web 98/98 files and 2795/2795 cases, backend 88/88 and 2051/2051, DB/RLS 143/143 and 1711/1711, authenticated browser 365 passed / 0 failed.",
    "FINDING_IDS": "`P1-OD-025` OPEN (Owner decision); `G-MEDIA` OPEN (R8 deferred); Owner rows 12-14 Blocked (INT-093/094/095). All three are named on screen rather than left to be discovered.",
    "DOCUMENTATION_EVIDENCE": "`canonical-plan.md` §7 (`P1-OD-025` disposition: decision-neutral foundation) and §10 (media wording); `media-capture-decision-record.md` — the Owner-facing half of the same statement; `p1-28-guidance-reconciliation.test.ts` \"‘nothing takes, chooses or records a picture’ — and the screen says it\".",
    "FINAL_VERDICT": "PASS",
    "VERDICT_RATIONALE": "PASS, and the blocker that held it is quoted so the change is checkable. The row read: \"FE-017 canonically owns those four operations (canonical-plan.md §5) and none of them has a production adapter, so they stand as PENDING_FRONTEND_ADAPTER\". All four now have a production adapter AND a production consumer, the adapter manifest declares nothing, and the write gate derives REACHABLE for each against a real call site. What closes the row is not that the code exists but that a capture was DRIVEN: a file chosen in a browser became an object in the configured store, was read back and hashed by the scanner, reached accepted, was linked, bound and finalized, and the requirement then read Met — with the object itself verified through the repository's own provider afterwards. The half that matters more is the negative one: an EICAR capture travelled the identical path, was really stored, was scanned, and was quarantined — and satisfied nothing. A requirement in this product is met only by finalized accepted evidence, and that is now a measurement rather than a claim."
  },
  "FE-018": {
    "IMPLEMENTATION_SURFACES": "`apps/web/src/features/receptions/components/steps/SignatureStep.tsx` (516 lines), registered at `apps/web/src/features/receptions/check-in/steps.tsx:112`. It captures through the Server Action `apps/web/src/features/receptions/signature-capture.ts` (`captureSignatureEvidence`, 215 lines: the category read at `:137`, the authorize-and-PUT call at `:156`, the link at `:171` and `recordSignature` at `:180`), rendering the ONE approved file input `apps/web/src/features/receptions/components/CaptureFileField.tsx` at `SignatureStep.tsx:295` inside the capture form at `:226`. It reads the visit’s active parties from `rec.reception-party-role-list` (`:131`) and the signature ledger from `rec.reception-signature-list` (`:147`), and every row it renders (`:379`) offers finalize ONLY against an accepted version (`:398-399`, the form at `:428`) and repudiate ONLY against a finalized signature (`:400`, the form at `:462`) — both through `recordSignatureEvent` in `apps/web/src/features/receptions/api.ts`.",
    "NORMAL_NAVIGATION_ENTRY": "Tenth step of the check-in wizard.",
    "BACKEND_READ_CONTRACTS": "`rec.reception-party-role-list` (R1) for 'who would sign'. There is NO signature read anywhere: `rec.signatures` is INSERT/SELECT at the table with no route, and R1 deliberately left it outside arm E (`contract-archaeology.md:96`). The step SAYS so rather than rendering an empty list that would read as 'none exist' (`reception-readings-and-signoff.dom.test.tsx:349-353`).",
    "BACKEND_WRITE_CONTRACTS": "`rec.reception-signature` — POST .../signatures. NOT CALLED. `write-reachability.json`, under `rec.reception-signature` classifies it `NOT_YET_WIRED` with its reason, and the SEC-004 gate refuses an allow-listed operation that IS called, so this classification is checked in both directions (`tests/ci/p1-28-write-reachability.test.ts:306-320`).",
    "REQUEST_SCHEMA": "`{signerRole enum(7), signerPartnerId?, signatureDocumentId + signatureDocumentVersionId REQUIRED, captureMethod, purpose enum(6), signatureHash? hex <=64B}` — never the drawn bytes. Both document uuids are required and no operation in the published surface registers a document, so neither value can be produced.",
    "RESPONSE_SCHEMA": "201 `{receptionVisitId, signatureId, purpose}` — unobserved, because the operation is never invoked from this product.",
    "REQUIRED_PERMISSIONS": "`rec.reception.signature.manage` — the same code as FE-019's refusals, deliberately, and resolved once at the wizard route (`check-in/[receptionId]/page.tsx:162`). No separate code is invented for signatures.",
    "OWNER_REQUIREMENT_MAPPING": "Owner rows OR-22/23 (Contracted); journey steps S-10/S-11.",
    "TENANT_ISOLATION_REQUIREMENT": "The one read it performs is visit-scoped and encoded; no scope key travels.",
    "LOADING_STATE": "`EvidenceStates` skeleton on the parties table.",
    "EMPTY_STATE": "`receptions.parties.rolesEmpty` when the visit has no active roles; the signature list itself is absent rather than empty, and that is stated.",
    "ZERO_RESULT_STATE": "N/A with rationale: no filtered search on this step.",
    "ERROR_STATE": "Shared `EvidenceStates` with reference and Retry on the parties read.",
    "DENIAL_STATE": "N/A with rationale: there is no capture control to withdraw. The parties read renders a denial through `EvidenceStates` if refused.",
    "CONFLICT_STATE": "N/A with rationale: no write.",
    "ARABIC_EVIDENCE": "`receptions.signature.*` in `ar.json`, including the block, media-state, decision and no-read-back lines; the vocabulary list uses the Arabic comma rather than a Latin one (`SignatureStep.tsx:83`). Arabic render at `reception-readings-and-signoff.dom.test.tsx:378-388`. PRODUCTION: Measured across 18 combinations — desktop 1440, tablet 768 and 200% zoom (640 CSS px at deviceScaleFactor 2) x English/Arabic x the capture, damage-map and signature steps: 0 horizontal clipping on the document or any non-scrolling region, 0 controls without an accessible name, 0 raw translation keys, Arabic always dir=rtl with real Arabic script, and the approved file input keyboard-reachable in every viewport (28 focusable controls, 5 file inputs, 0 removed from the tab order).",
    "ENGLISH_EVIDENCE": "Same keys in `en.json`.",
    "RTL_LTR_EVIDENCE": "Arabic case mounts RTL; the two numeric facts are explicitly `dir=\"ltr\"` (`SignatureStep.tsx:155`, `:163`). PRODUCTION: Measured across 18 combinations — desktop 1440, tablet 768 and 200% zoom (640 CSS px at deviceScaleFactor 2) x English/Arabic x the capture, damage-map and signature steps: 0 horizontal clipping on the document or any non-scrolling region, 0 controls without an accessible name, 0 raw translation keys, Arabic always dir=rtl with real Arabic script, and the approved file input keyboard-reachable in every viewport (28 focusable controls, 5 file inputs, 0 removed from the tab order).",
    "ACCESSIBILITY_EVIDENCE": "The block is a `role=\"note\"` region inside a labelled `EvidenceSection` (`SignatureStep.tsx:99-110`); the parties list is a real list; the two numeric facts are explicitly `dir=\"ltr\"` (`:155`, `:163`). NAMED GAP: no axe run. The tablet half is CLOSED and the sentence that claimed it was false at this candidate: `apps/web/playwright.config.ts:255` matches `(administration|appointments-and-receptions)`, so `authenticated-tablet` executed 47 P1-28 cases at 1024x768 in run `31750364479` / job `94614564003`. PRODUCTION: Measured across 18 combinations — desktop 1440, tablet 768 and 200% zoom (640 CSS px at deviceScaleFactor 2) x English/Arabic x the capture, damage-map and signature steps: 0 horizontal clipping on the document or any non-scrolling region, 0 controls without an accessible name, 0 raw translation keys, Arabic always dir=rtl with real Arabic script, and the approved file input keyboard-reachable in every viewport (28 focusable controls, 5 file inputs, 0 removed from the tab order).",
    "PRODUCTION_REACHABILITY": "REACHABLE. features/receptions/signature-capture.ts registers the signature image and records the reference, and components/steps/SignatureStep.tsx consumes it and drives rec.reception-signature-event; both operations are REACHABLE in write-reachability.json and the gate confirms each against a consumed call site. The asymmetry this row used to record — a reachable step over an unreachable operation — is gone because the operation is reached.",
    "PRODUCTION_TEST_EVIDENCE": "A REAL signature, driven from the UI on visit b3942d34-cb25-41a9-b0f2-b83cb04dd8fd. The capture registered a reception_signature document, created the shared.document_links row at link purpose signature that rec.guard_signature_evidence() requires BEFORE the insert, and recorded signature 9d69ffce-fe9b-4bae-ae35-7b5a728026bf: signer_role vehicle_owner, purpose reception_acknowledgement, capture_method uploaded, document b90c4472-99ed-4b57-a507-0c0ef3a04366, exact immutable version 38ea7f70-a6e6-4e68-aec2-f1f788ceb100, signed_at and created_by recorded, correlation id carried. The ledger read it back as draft over an accepted version, and rec.reception-signature-event finalized it (201). Measured across 18 combinations — desktop 1440, tablet 768 and 200% zoom (640 CSS px at deviceScaleFactor 2) x English/Arabic x the capture, damage-map and signature steps: 0 horizontal clipping on the document or any non-scrolling region, 0 controls without an accessible name, 0 raw translation keys, Arabic always dir=rtl with real Arabic script, and the approved file input keyboard-reachable in every viewport (28 focusable controls, 5 file inputs, 0 removed from the tab order).",
    "NEGATIVE_OR_MUTATION_PROOF": "THE ACCEPTED-VERSION RULE IS ENFORCED BY THE DATABASE, and was driven rather than reasoned about. A second signature document was captured from an EICAR payload: the version was really stored, scanned, and quarantined, and rec.reception-signature then REFUSED the signature outright with 422 ERR-VAL-001 incoherent_reference — a non-accepted version cannot carry a signature at all, let alone a final one. HISTORY SURVIVES REPUDIATION: repudiating the finalized signature answered 201 and the ledger still returns the entry, now status repudiated, carrying its reason AND its original finalizedAt. Nothing was overwritten and nothing disappeared through a read filter, which is the overwrite the Owner decision forbids.",
    "PROTECTED_REPROOF": "PENDING PROTECTED MERGE. Re-observed at this head against the PRODUCTION acceptance stack (next build + next start on both tiers, API 3000, web 3100, real S3-compatible store attached to the API process only): web 98/98 files and 2795/2795 cases, backend 88/88 and 2051/2051, DB/RLS 143/143 and 1711/1711, authenticated browser 365 passed / 0 failed.",
    "FINDING_IDS": "`P1-OD-025` OPEN (Owner decision), with `G-MEDIA` behind it. The reason is derived rather than asserted: `rec.reception-signature` requires BOTH `signatureDocumentId` and `signatureDocumentVersionId`, and `apps/web/src/features/receptions/media/media-decision.ts:141-174` records three INDEPENDENT reasons the document chain cannot produce either value — the document-category table ships zero rows and no published operation adds one; `buildStorageProvider()` returns `UnconfiguredStorageProvider` by default (`apps/api/src/modules/shared-services/index.ts:222-227`) and the only alternative the composition root can build is the local fake; and a registered version can never leave `pending`. `apps/web/tests/p1-28-reception-media.test.ts` \"both document-bound reception writes require BOTH uuids, so neither degrades\" pins that neither write has a degraded form. Two `apps/web` gate rules additionally ban any capture UI while the decision is open — `no-upload-path` (`scripts/ci/check-p1-27-frontend.mjs:910`) and `no-invented-media-limit` (`:953`). SECOND, DISTINCT AND SEPARATELY OWNED: the signature READ-BACK does not exist and R1 deliberately did not add it (`contract-archaeology.md:96`), so even a signature captured by other means could not be shown.",
    "DOCUMENTATION_EVIDENCE": "`canonical-plan.md` §5.2 and §7 (the signature-image half is blocked by `P1-OD-025`); `contract-archaeology.md` Table B row B5; `operator-guide.md` 'Signatures'.",
    "FINAL_VERDICT": "PASS",
    "VERDICT_RATIONALE": "PASS. The recorded blocker was absolute: \"The task binds exactly one operation, rec.reception-signature, and the product calls it ZERO times — the manifest says so in writing and the SEC-004 gate would fail the opposite claim.\" That was true, and it was true because a third of this task had never been built: the adapters shipped with no consumer. They have one now, and the gate derives REACHABLE from the call site rather than being told. A signature was captured from a real image, bound to its exact immutable version, finalized against an accepted one, refused against a quarantined one, and repudiated without losing its history. Finalization is deliberately a SECOND act rather than something the capture slips in when the scan happens to finish first — the difference between signed and signed-and-verified must not depend on how fast the scanner was."
  },
  "FE-019": {
    "IMPLEMENTATION_SURFACES": "`apps/web/src/features/receptions/components/steps/RefusalStep.tsx` (392 lines), registered at `check-in/steps.tsx:114-119`; write through `api.ts` (`recordRefusal`); reads `api.ts` (`listAuthorizations`) and `catalogue-api.ts:103` (`listRefusalReasons`).",
    "NORMAL_NAVIGATION_ENTRY": "Eleventh step of the check-in wizard.",
    "BACKEND_READ_CONTRACTS": "`rec.catalogue-refusal-reason-list` (R3 — table still ships zero rows) and `rec.reception-authorization-list`, whose UNION returns authorization refusals; the step reads back only what the union returns and SAYS so rather than implying it lists every refusal (`reception-readings-and-signoff.dom.test.tsx:487-528`).",
    "BACKEND_WRITE_CONTRACTS": "`rec.reception-refusal` — POST .../refusals. REACHABLE (`write-reachability.json`, under `rec.reception-refusal`), and explicitly distinct from the `rec.reception-refuse` EXIT, which this step does not call and says so on screen.",
    "REQUEST_SCHEMA": "`{refusalType enum(5), refusalReasonId? uuid, refusingPartnerId?, witnessEmployeeId?, evidenceDocumentId?}`. Type `authorization` REQUIRES `refusingPartnerId` — refused beside the field BEFORE sending (`reception-readings-and-signoff.dom.test.tsx:427-445`) — and the requirement is pinned against the contract in both directions (`receptions-contract.test.ts` \"requires the refusing partner on an authorization refusal only\").",
    "RESPONSE_SCHEMA": "201 `{receptionVisitId, refusalId, refusalType}`. A refusal NEVER changes `receptionStatus`, and the step states that recording one is not the command that ends the visit (`reception-readings-and-signoff.dom.test.tsx:390-399`).",
    "REQUIRED_PERMISSIONS": "`rec.reception.signature.manage` — deliberately the SAME code as FE-018, not a separate invented one; `receptions-contract.test.ts` \"spreads the write surface across seven distinct permission codes\" holds the write surface to seven distinct permission codes, so a fabricated eighth fails.",
    "OWNER_REQUIREMENT_MAPPING": "Owner rows OR-22/23; journey steps S-10/S-11 — refusal is the negative half of the signature step.",
    "TENANT_ISOLATION_REQUIREMENT": "Visit-scoped path, encoded; the catalogue read carries no scope key.",
    "LOADING_STATE": "`EvidenceStates` skeleton on the union read-back.",
    "EMPTY_STATE": "An EMPTY reason catalogue is stated as NOT CONFIGURED and the refusal still records, because the reason is optional (`reception-readings-and-signoff.dom.test.tsx:446-466`) — the difference from FE-004, where the same emptiness is fatal because the field is mandatory.",
    "ZERO_RESULT_STATE": "N/A with rationale: a catalogue and a per-visit union, neither filtered by operator input.",
    "ERROR_STATE": "Shared `EvidenceStates` with reference and Retry.",
    "DENIAL_STATE": "The form is withdrawn with its reason without the signature permission and on a terminal visit (`reception-readings-and-signoff.dom.test.tsx:552-565`).",
    "CONFLICT_STATE": "The non-guessing conflict copy renders and the step RE-READS after it (`reception-readings-and-signoff.dom.test.tsx:529-551`) — a role-not-held refusal is the same non-disclosing 409 `ERR-TRN-001` as the authority guard.",
    "ARABIC_EVIDENCE": "`receptions.refusal.*` and `receptions.refusalType.*` in `ar.json`; Arabic render at `reception-readings-and-signoff.dom.test.tsx:566-572`.",
    "ENGLISH_EVIDENCE": "Same keys in `en.json`.",
    "RTL_LTR_EVIDENCE": "Arabic case mounts RTL; party numbers stay LTR.",
    "ACCESSIBILITY_EVIDENCE": "Labelled `EvidenceSection`; the party requirement is reported beside the field it is about rather than in a summary. NAMED GAP: no axe run, no tablet case.",
    "PRODUCTION_REACHABILITY": "Registered step covered from the registry; REACHABLE with a consumed call site, and the gate distinguishes it from the terminal `rec.reception-refuse` by path (`write-reachability.json` classifies `rec.reception-refusal` and the terminal `rec.reception-refuse` separately).",
    "PRODUCTION_TEST_EVIDENCE": "`reception-readings-and-signoff.dom.test.tsx:389-572` (9 cases) and `receptions-contract.test.ts` \"keeps refusal EVIDENCE and the refuse EXIT as two operations\" (the two operations kept apart).",
    "NEGATIVE_OR_MUTATION_PROOF": "`receptions-contract.test.ts` \"keeps refusal EVIDENCE and the refuse EXIT as two operations\" fails if refusal EVIDENCE and the refuse EXIT are ever collapsed into one operation — two different operations with the same English word. `reception-readings-and-signoff.dom.test.tsx:400-426` asserts only what the operator set travels, and `:467-486` proves the signed-in operator is sent as the witness ONLY when asked.",
    "PROTECTED_REPROOF": "PENDING PROTECTED MERGE — RE-OBSERVED for this re-derivation at `a2318c36`: the web tier ran 2669 cases, 0 failed and 0 skipped (28 P1-28 suites, 760 cases), and the root CI tier ran 2475 cases, 0 failed and 0 skipped (5 P1-28 gate suites, 180 cases). THE BROWSER TIER NOW CARRIES THIS PHASE — `apps/web/tests/e2e/authenticated/appointments-and-receptions.spec.ts` (2478 lines, against the running application, the running API and the real database), which the previous derivation correctly recorded as absent. It covers the wizard shell in the running application: \"the check-in wizard\" loads a visit’s thirteen-step registry through the real API, and §18 now opens a REAL visit and reaches that shell. This step’s own panels are exercised only as far as that goes — a limit this row states rather than implies. STILL NOT OBSERVED, and this is the whole outstanding debt on this row: that spec was NOT executed in this derivation — it needs the production launcher (`npm run acceptance:serve`), a live database and, for §18, a provisioned second workspace — and the run reported by the wave that landed it (319 expected, 0 unexpected, 0 flaky) is recorded in no phase document, so no browser result is bound to this head. THE TABLET VIEWPORT AND HOSTED CI WERE OBSERVED AT `38afa5c28e5b78d484a442cf6b8596fb2a5c34aa`, A HEAD THIS CANDIDATE SUPERSEDES. Two sentences stood here and both were false: that the `authenticated-tablet` project sees no P1-28 screen, and that no hosted-CI result exists for this head. `apps/web/playwright.config.ts:254-255` names that project and matches `(administration|appointments-and-receptions)`, so the phase surface is seen at 1024x768 as well as at 1440x900; run `31750364479` / job `94614564003` at head `38afa5c28e5b78d484a442cf6b8596fb2a5c34aa` executed `authenticated/appointments-and-receptions.spec.ts` 141 times — 47 in `authenticated-en`, 47 in `authenticated-ar`, 47 in `authenticated-tablet`, 0 failed and 0 skipped. Hosted CI at that head is run `31750364479`, 21 checks, 21 success, 0 failure, with `ci-gate` (`94617578352`) the single required check. The citation this cell used to give for the tablet claim pointed at lines 225 to 233 of the same config — the project DOCBLOCK, nine consecutive comment lines describing the state BEFORE the widening — so it read as confirmation of a sentence the code beneath it refutes; `validate:p1-28-evidence` now refuses a comment-only citation for exactly that reason. STILL OUTSTANDING ON THIS ROW: the protected-branch re-proof, because PR #226 has not merged, and OWNER ACCEPTANCE, which has not been requested or returned (`docs/phase-1/phase-1-28/evidence/change-log.md:5-11`). THAT HEAD IS NOT THE CURRENT CANDIDATE. Three fix waves changed 37 product files after `38afa5c28e5b78d484a442cf6b8596fb2a5c34aa`, so P1-28-QA-005 re-froze the candidate — at `6392ccb4321b004ed12e5d04ad583298da3303dd` then, and at `3c75f49a01e35b507461bf0929b0046e7140860a` now, the two being product-identical — and every hosted figure above is recorded in `docs/phase-1/phase-1-28/evidence/closure-candidate.json` as PENDING at that candidate — marked `describesSupersededHead`, citing the run it really came from, and listed in `pendingHostedBindings`. No hosted run has been taken at the current candidate yet, so no hosted result is bound to this head; `validate:p1-28-evidence` refuses a cell asserting an observation AT THIS CANDIDATE while the package records none.",
    "FINDING_IDS": "`P1-28-OD-001` applies to `rec.refusal_reasons` — its management contract landed with PR #227 and no screen reaches it — but the field is OPTIONAL by contract, so an empty catalogue DEGRADES this capability rather than stopping it, and the step records anyway while stating the absence. That is the distinction this row and FE-004 are the two sides of. The union read-back returns authorization refusals only; the step states that limit rather than implying completeness.",
    "DOCUMENTATION_EVIDENCE": "`canonical-plan.md` §5.2; `contract-archaeology.md` Table B row B6; `operator-guide.md` 'Refusals'; `p1-28-guidance-reconciliation.test.ts` \"‘a refusal does not end the visit’ — the exit is a different operation\" pins 'a refusal does not end the visit' to the two distinct operations.",
    "FINAL_VERDICT": "PASS",
    "VERDICT_RATIONALE": "The bound write records with only what the operator set, the one conditional the contract imposes is enforced beside the field before a request is spent, and the two operations that share the English word 'refuse' are kept apart by a contract test that would fail if they were merged. The empty reason catalogue degrades this row instead of blocking it — the field is optional and the step records anyway while stating the absence — which is exactly the distinction that makes FE-004 PARTIAL and this row not. The read-back states the limit of what the union returns rather than implying it lists every refusal."
  },
  "FE-020": {
    "IMPLEMENTATION_SURFACES": "`apps/web/src/features/receptions/components/steps/SummaryStep.tsx` (761 lines), registered at `check-in/steps.tsx:120-125`; commands `api.ts` (`approveReception`), `api.ts` (`closeReceptionWithoutWork`) and `api.ts` (`refuseReception`); the affordance and version rules are the pure module `check-in/closure.ts` (193).",
    "NORMAL_NAVIGATION_ENTRY": "Twelfth step of the check-in wizard; the queue at `/receptions` links every row to its visit (`reception-queue.dom.test.tsx:262-273`).",
    "BACKEND_READ_CONTRACTS": "`rec.reception-detail` (from the shell, and the `If-Match` source), `rec.reception-condition-evidence-list`, `rec.reception-party-role-list` and `rec.reception-authorization-list` — the three per-visit reads this step issues are asserted as exactly three (`reception-summary.dom.test.tsx:159-168`).",
    "BACKEND_WRITE_CONTRACTS": "`rec.reception-approve`, `rec.reception-close-without-work` and `rec.reception-refuse` — all three REACHABLE with named call sites (`write-reachability.json`, under `rec.reception-approve`, `rec.reception-close-without-work` and `rec.reception-refuse`). The plan §5.2 binding now names all three after DOC-001 corrected it.",
    "REQUEST_SCHEMA": "Approve: empty body, `If-Match` REQUIRED. The two exits take a mandatory trimmed reason bounded at the route's own limit, measured TRIMMED as the adapter does (`reception-closure.test.ts:113-137`), and refuse an empty reason beside the field while sending nothing (`reception-summary.dom.test.tsx:484-495`).",
    "RESPONSE_SCHEMA": "200 `{receptionStatus:'authorized', appliedTransitions, recordVersion}` + ETag; from `opened` it walks TWO edges in one transaction so RV = sent+2. The step presents the ANSWER'S version whether one edge or two was applied, from the SAME code path (`reception-summary.dom.test.tsx:363-406`), and never invents one the backend did not state (`reception-closure.test.ts:165-171`).",
    "REQUIRED_PERMISSIONS": "`rec.reception.approve` (approval audit class) and `rec.reception.close` for BOTH exits — one permission, as the backend does (`p1-28-reception-routes.test.ts` \"closes BOTH terminal exits with one permission, as the backend does\"). Each command is withdrawn for the permission it needs, and the screen says WHICH (`reception-summary.dom.test.tsx:312-339`).",
    "OWNER_REQUIREMENT_MAPPING": "Owner row OR-24 (was Blocked by INT-015); journey step S-11. The resumability mandate (`owner-workflow-requirements.md:182-197`), which plan §8 named a readiness blocker, is DISCHARGED by R1: a visit is resumable from its id and the approve version comes from a read.",
    "TENANT_ISOLATION_REQUIREMENT": "Visit-scoped paths, encoded; a cross-tenant reception renders identically to a missing one (`p1-28-permission-route-binding.test.ts` \"renders a cross-tenant reception identically to a missing one\"), and a REFUSED read is a denial rather than a not-found (\"discriminates: a REFUSED read is a denial, not a not-found\").",
    "LOADING_STATE": "Each section renders the shared loading state; the commands use the shared pending action state.",
    "EMPTY_STATE": "Each section is stated as EMPTY rather than as a failure (`reception-summary.dom.test.tsx:169-175`).",
    "ZERO_RESULT_STATE": "N/A with rationale: the sections are per-visit, not filtered by operator input. A truncated evidence page is stated as one page (`reception-summary.dom.test.tsx:204-224`).",
    "ERROR_STATE": "A failed section offers a retry with its reference (`reception-summary.dom.test.tsx:176-190`).",
    "DENIAL_STATE": "A denied section renders as a denial, NEVER as an empty list (`reception-summary.dom.test.tsx:191-203`); a terminal visit states why there is no decision to take (`:305-311`).",
    "CONFLICT_STATE": "The two 409s a guarded command can meet are told apart because the cure is opposite: `ERR-TRN-001` is a state refusal a re-read does not cure, `ERR-CON-001` is cured by the re-read just performed (`reception-summary.dom.test.tsx:428-464`), and the step re-reads after a conflict as well as after a success (`:465-482`). `reception-closure.test.ts:138-155` fails CLOSED: an unrecognised key is blocked, never stale.",
    "ARABIC_EVIDENCE": "`receptions.summary.*` and the closure keys in `ar.json`; the decision surface renders in Arabic right to left (`reception-summary.dom.test.tsx:792-803`).",
    "ENGLISH_EVIDENCE": "Same keys in `en.json`.",
    "RTL_LTR_EVIDENCE": "Arabic case mounts RTL; versions and identifiers stay LTR.",
    "ACCESSIBILITY_EVIDENCE": "Labelled sections per read; the reason field bounded and labelled; command outcomes announced through the shared alert region. NAMED GAP: no axe run and no tablet case.",
    "PRODUCTION_REACHABILITY": "Registered step covered from the registry; all three writes REACHABLE with consumption proved — the close/refuse pair only because the gate learned to enumerate a tail typed as a union of string literals (`tests/ci/p1-28-write-reachability.test.ts:580-625`).",
    "PRODUCTION_TEST_EVIDENCE": "`apps/web/tests/reception-summary.dom.test.tsx` (28 cases), `apps/web/tests/reception-closure.test.ts` (18), `p1-28-qa.test.ts:754-818` (the version behaviour at the adapter layer).",
    "NEGATIVE_OR_MUTATION_PROOF": "`reception-summary.dom.test.tsx:384-406` presents sent+1 and sent+2 FROM THE SAME CODE PATH, so a screen that hard-coded +2 fails; `reception-closure.test.ts:81-91` treats approval as REACHABILITY rather than as a single edge for the same reason. `:92-111` refuses every command from every terminal status and refuses to report a status as reaching itself without an edge. `reception-summary.dom.test.tsx:528-553` proves refuse is sent from its own form and only that one.",
    "PROTECTED_REPROOF": "PENDING PROTECTED MERGE — RE-OBSERVED for this re-derivation at `a2318c36`: the web tier ran 2669 cases, 0 failed and 0 skipped (28 P1-28 suites, 760 cases), and the root CI tier ran 2475 cases, 0 failed and 0 skipped (5 P1-28 gate suites, 180 cases). THE BROWSER TIER NOW CARRIES THIS PHASE — `apps/web/tests/e2e/authenticated/appointments-and-receptions.spec.ts` (2478 lines, against the running application, the running API and the real database), which the previous derivation correctly recorded as absent. It covers this row at \"the reception queue is a board for one named branch\" — the board an operator takes the terminal decision from, in the running application, where an empty board offers no close affordance at all — and §18 now opens a real visit, so the summary step is reachable. The approval and the two terminal exits as an operator takes them are NOT observed in a browser: §18 opens a visit and does not close one, and that is scope rather than a blocker. STILL NOT OBSERVED, and this is the whole outstanding debt on this row: that spec was NOT executed in this derivation — it needs the production launcher (`npm run acceptance:serve`), a live database and, for §18, a provisioned second workspace — and the run reported by the wave that landed it (319 expected, 0 unexpected, 0 flaky) is recorded in no phase document, so no browser result is bound to this head. THE TABLET VIEWPORT AND HOSTED CI WERE OBSERVED AT `38afa5c28e5b78d484a442cf6b8596fb2a5c34aa`, A HEAD THIS CANDIDATE SUPERSEDES. Two sentences stood here and both were false: that the `authenticated-tablet` project sees no P1-28 screen, and that no hosted-CI result exists for this head. `apps/web/playwright.config.ts:254-255` names that project and matches `(administration|appointments-and-receptions)`, so the phase surface is seen at 1024x768 as well as at 1440x900; run `31750364479` / job `94614564003` at head `38afa5c28e5b78d484a442cf6b8596fb2a5c34aa` executed `authenticated/appointments-and-receptions.spec.ts` 141 times — 47 in `authenticated-en`, 47 in `authenticated-ar`, 47 in `authenticated-tablet`, 0 failed and 0 skipped. Hosted CI at that head is run `31750364479`, 21 checks, 21 success, 0 failure, with `ci-gate` (`94617578352`) the single required check. The citation this cell used to give for the tablet claim pointed at lines 225 to 233 of the same config — the project DOCBLOCK, nine consecutive comment lines describing the state BEFORE the widening — so it read as confirmation of a sentence the code beneath it refutes; `validate:p1-28-evidence` now refuses a comment-only citation for exactly that reason. STILL OUTSTANDING ON THIS ROW: the protected-branch re-proof, because PR #226 has not merged, and OWNER ACCEPTANCE, which has not been requested or returned (`docs/phase-1/phase-1-28/evidence/change-log.md:5-11`). THAT HEAD IS NOT THE CURRENT CANDIDATE. Three fix waves changed 37 product files after `38afa5c28e5b78d484a442cf6b8596fb2a5c34aa`, so P1-28-QA-005 re-froze the candidate — at `6392ccb4321b004ed12e5d04ad583298da3303dd` then, and at `3c75f49a01e35b507461bf0929b0046e7140860a` now, the two being product-identical — and every hosted figure above is recorded in `docs/phase-1/phase-1-28/evidence/closure-candidate.json` as PENDING at that candidate — marked `describesSupersededHead`, citing the run it really came from, and listed in `pendingHostedBindings`. No hosted run has been taken at the current candidate yet, so no hosted result is bound to this head; `validate:p1-28-evidence` refuses a cell asserting an observation AT THIS CANDIDATE while the package records none.",
    "FINDING_IDS": "None open. INT-015 and the single-session limitation are closed by R1; INS-17 (a review RECORD has no home in the schema) stands, and this screen labels nothing as a stored review.",
    "DOCUMENTATION_EVIDENCE": "`canonical-plan.md` §5.2 and §8; `contract-archaeology.md` Table B rows B7/B11 and Table E row E3; `operator-guide.md` 'Summary, approval and ending a visit'.",
    "FINAL_VERDICT": "PASS",
    "VERDICT_RATIONALE": "All three bound commands are wired and version-guarded from the shell's own read, the decision is taken beside the three reads it is a decision about, and the two conflicts whose cures are opposite are told apart with a fail-closed default. The approve double-transition is handled by presenting the RESPONSE'S version from one code path proved against both one-edge and two-edge answers, which is the shape a screen would get wrong by computing sent+2. The terminal exits — the R5 commands that make an abandoned visit release its vehicle — are each sent from their own form with a trimmed, bounded, mandatory reason. Plan §8's condition for presenting this workflow is met: R1 landed, so approval no longer depends on one unbroken session."
  },
  "FE-021": {
    "IMPLEMENTATION_SURFACES": "`apps/web/src/features/receptions/components/AcknowledgementDocument.tsx` (348 lines) behind `apps/web/src/app/[locale]/(dashboard)/receptions/check-in/[receptionId]/acknowledgement/page.tsx:87` (gate), `:92` (detail) and `:115-129` (the three section reads and the receiving-employee resolution, issued together in one `Promise.all`).",
    "NORMAL_NAVIGATION_ENTRY": "From the wizard's breadcrumb (`acknowledgement/page.tsx:71-78`) and from every queue row (`reception-queue.dom.test.tsx:262-273`).",
    "BACKEND_READ_CONTRACTS": "`rec.reception-detail` plus `rec.reception-party-role-list`, `rec.reception-authorization-list` and `rec.reception-condition-evidence-list` — four reads, all R1 — and, at this head, `iam.user-detail` to resolve the receiving employee to a NAME (`acknowledgement/page.tsx:128`, `:158`). All are performed ON THE SERVER so the first paint is the finished sheet (\"reads the three sections on the server, so the first paint is the sheet\"), and the directory read is not spent on a visit that does not exist (\"does not spend the directory read on a visit that does not exist\").",
    "BACKEND_WRITE_CONTRACTS": "N/A with rationale: printing writes nothing. The export gate bans PDF/blob/download generation, and the suite proves the sheet generates no file at all (`reception-acknowledgement.dom.test.tsx` \"generates no file: no download, no blob, no PDF claim\").",
    "REQUEST_SCHEMA": "Path uuid plus the shared table request at page size 50; no operator input reaches the wire.",
    "RESPONSE_SCHEMA": "The detail item the read-surface plan specified — id, display number, status, origin, vehicle, custody timestamps, record version — plus each section's rows, `status`, `hasMore` and `correlationId`. Carrying the OUTCOME and not just the rows is the `F1` fix: a failed read arrives as `rows: []` and must not print as 'no records are recorded on this visit' (`acknowledgement/page.tsx:131-152`).",
    "REQUIRED_PERMISSIONS": "`rec.reception.read` alone for the sheet — the route is proved to be a read that resolves NO write capability (\"is a read: one permission, and no write capability is resolved\") — plus `iam.user.read` for the name, which the route decides for itself and, lacking it, spends no request (\"prints the receiving employee by name, and reads nothing when it may not\").",
    "OWNER_REQUIREMENT_MAPPING": "Journey step S-10 — the acknowledgement the customer takes away.",
    "TENANT_ISOLATION_REQUIREMENT": "Path parameter, encoded; a backend denial carries its correlation reference (`p1-28-reception-routes.test.ts` \"carries the backend correlation reference on a backend denial\") and a missing visit is stated without printing a half-empty sheet (\"states a missing visit without printing a half-empty sheet\").",
    "LOADING_STATE": "N/A with rationale: server-rendered in full — there is no client loading phase to render, which is the point of reading all four on the server.",
    "EMPTY_STATE": "An empty section is STATED rather than printed as a blank block (`reception-acknowledgement.dom.test.tsx` \"states an empty section rather than printing a blank block\").",
    "ZERO_RESULT_STATE": "N/A with rationale: nothing on the sheet is filtered by operator input.",
    "ERROR_STATE": "A FAILED read is printed as a failed read, never as an empty section (`reception-acknowledgement.dom.test.tsx` \"prints a FAILED read as a failed read, never as an empty section (`F1`)\"), and the truncation note is WITHHELD on a failed read because `hasMore: false` is not an answer (\"withholds the truncation note on a failed read — `hasMore: false` is not an answer\").",
    "DENIAL_STATE": "Route-level and backend denials distinguished by whether a reference exists (`acknowledgement/page.tsx:87-101`).",
    "CONFLICT_STATE": "N/A with rationale: no write.",
    "ARABIC_EVIDENCE": "All acknowledgement keys in `ar.json`; the sheet renders in Arabic right to left with NO English fallback (`reception-acknowledgement.dom.test.tsx` \"renders in Arabic, right to left, with no English fallback\").",
    "ENGLISH_EVIDENCE": "Same keys in `en.json`.",
    "RTL_LTR_EVIDENCE": "As above; the EV charge prints exactly as the wire carried it (\"prints the EV charge exactly as the wire carried it\") rather than being reformatted per locale.",
    "ACCESSIBILITY_EVIDENCE": "REAL table markup, so a header repeats across a page break (\"uses REAL table markup, so a header repeats across a page break\") and a screen reader gets a table rather than a grid of divs; the shared print document frame is reused rather than a second one invented (\"renders inside the shared print document frame\"). NAMED GAP: no axe run. The tablet half is CLOSED and the sentence that claimed it was false at this candidate: `apps/web/playwright.config.ts:255` matches `(administration|appointments-and-receptions)`, so `authenticated-tablet` executed 47 P1-28 cases at 1024x768 in run `31750364479` / job `94614564003`.",
    "PRODUCTION_REACHABILITY": "One of the eight derived route pages; reachable from the wizard and from every queue row.",
    "PRODUCTION_TEST_EVIDENCE": "`apps/web/tests/reception-acknowledgement.dom.test.tsx` (21 cases at this head, including \"prints the receiving employee by NAME, with the G-EMP note\" and the templated \"prints the reason rather than an identifier when the name came back ${status}\" over all three non-named outcomes) and `apps/web/tests/p1-28-reception-routes.test.ts` \"the acknowledgement route\" (7 cases, including the one that prints the sheet when ONE section read fails and hands the document its FAILURE).",
    "NEGATIVE_OR_MUTATION_PROOF": "The sheet prints no storage key and no document reference (\"prints no storage key and no document reference\"), prints \"registered, pending\" and never claims an upload, says nothing has been verified by a technician, distinguishes the customer’s report from what staff observed, and states that the customer’s wording is on the restricted record. Each is a sentence a customer signs, so each is asserted rather than assumed. Added at this head: the receiving employee is printed BY NAME, and the templated case fails if any of the three non-named outcomes prints the identifier instead of the reason.",
    "PROTECTED_REPROOF": "PENDING PROTECTED MERGE — RE-OBSERVED for this re-derivation at `a2318c36`: the web tier ran 2669 cases, 0 failed and 0 skipped (28 P1-28 suites, 760 cases), and the root CI tier ran 2475 cases, 0 failed and 0 skipped (5 P1-28 gate suites, 180 cases). THE BROWSER TIER NOW CARRIES THIS PHASE — `apps/web/tests/e2e/authenticated/appointments-and-receptions.spec.ts` (2478 lines, against the running application, the running API and the real database), which the previous derivation correctly recorded as absent. It covers this row at \"the reception acknowledgement\" — an unreadable visit prints a not-found in the running application, never a sheet with empty sections. The populated sheet is not observed in a browser: §18 opens a visit and does not print its acknowledgement, and the per-section \"could not be read\" notice is likewise unobserved there. STILL NOT OBSERVED, and this is the whole outstanding debt on this row: that spec was NOT executed in this derivation — it needs the production launcher (`npm run acceptance:serve`), a live database and, for §18, a provisioned second workspace — and the run reported by the wave that landed it (319 expected, 0 unexpected, 0 flaky) is recorded in no phase document, so no browser result is bound to this head. THE TABLET VIEWPORT AND HOSTED CI WERE OBSERVED AT `38afa5c28e5b78d484a442cf6b8596fb2a5c34aa`, A HEAD THIS CANDIDATE SUPERSEDES. Two sentences stood here and both were false: that the `authenticated-tablet` project sees no P1-28 screen, and that no hosted-CI result exists for this head. `apps/web/playwright.config.ts:254-255` names that project and matches `(administration|appointments-and-receptions)`, so the phase surface is seen at 1024x768 as well as at 1440x900; run `31750364479` / job `94614564003` at head `38afa5c28e5b78d484a442cf6b8596fb2a5c34aa` executed `authenticated/appointments-and-receptions.spec.ts` 141 times — 47 in `authenticated-en`, 47 in `authenticated-ar`, 47 in `authenticated-tablet`, 0 failed and 0 skipped. Hosted CI at that head is run `31750364479`, 21 checks, 21 success, 0 failure, with `ci-gate` (`94617578352`) the single required check. The citation this cell used to give for the tablet claim pointed at lines 225 to 233 of the same config — the project DOCBLOCK, nine consecutive comment lines describing the state BEFORE the widening — so it read as confirmation of a sentence the code beneath it refutes; `validate:p1-28-evidence` now refuses a comment-only citation for exactly that reason. STILL OUTSTANDING ON THIS ROW: the protected-branch re-proof, because PR #226 has not merged, and OWNER ACCEPTANCE, which has not been requested or returned (`docs/phase-1/phase-1-28/evidence/change-log.md:5-11`). THAT HEAD IS NOT THE CURRENT CANDIDATE. Three fix waves changed 37 product files after `38afa5c28e5b78d484a442cf6b8596fb2a5c34aa`, so P1-28-QA-005 re-froze the candidate — at `6392ccb4321b004ed12e5d04ad583298da3303dd` then, and at `3c75f49a01e35b507461bf0929b0046e7140860a` now, the two being product-identical — and every hosted figure above is recorded in `docs/phase-1/phase-1-28/evidence/closure-candidate.json` as PENDING at that candidate — marked `describesSupersededHead`, citing the run it really came from, and listed in `pendingHostedBindings`. No hosted run has been taken at the current candidate yet, so no hosted result is bound to this head; `validate:p1-28-evidence` refuses a cell asserting an observation AT THIS CANDIDATE while the package records none.",
    "FINDING_IDS": "None open on this row. `F1` — a failed section read printing as an observed absence on the copy a customer signs — was found and fixed within the phase and is now asserted in both directions. The receiving-employee UUID defect this sheet carried is also fixed at this head: it prints a NAME, or the reason there is none, and never the identifier. The referent itself is still undefined, which is `G-EMP` and is recorded on FE-007 where the field is written.",
    "DOCUMENTATION_EVIDENCE": "`canonical-plan.md` §5.2; `docs/phase-1/phase-1-27/reception-read-surface-plan.md:439` (the detail item shape); `operator-guide.md` 'The acknowledgement document'.",
    "FINAL_VERDICT": "PASS",
    "VERDICT_RATIONALE": "The document renders the visit record from four server-side reads, and the distinction that matters on a document a customer signs is proved in both directions: a failed section prints as a failed read and never as an observed absence, with the truncation note withheld because `hasMore: false` from a failure is not an answer. It generates no file — print styling only, real table markup so headers repeat — and every honesty sentence it carries (no upload claim, no storage reference, nothing verified by a technician, the customer’s wording on the restricted record) is separately asserted. CORRECTED AT THIS HEAD: the sheet used to print the receiving employee’s raw identifier inside a `<code>` element, with a DOM case holding the defect in place; it now prints the resolved NAME, or one of three stated reasons why no name could be learned, and never the identifier — which is what `canonical-plan.md` §7 said would ship. RULE APPLIED, identically on every row of this file: a task is graded against THE CAPABILITY IT CANONICALLY OWNS — its §5 binding in `canonical-plan.md` — and against nothing else. A row does not stay PARTIAL because a fresh tenant starts empty, PROVIDED both halves are evidenced: its own journey works against a CONFIGURED tenant, and the UNCONFIGURED state is truthful. Equally, a row does not close while its own canonical journey is impossible, and a capability no canonical P1-28 task binds is not this register’s to withhold a verdict over."
  },
  "FE-022": {
    "IMPLEMENTATION_SURFACES": "`apps/web/src/features/receptions/components/steps/ConversionStep.tsx` (334 lines), registered at `check-in/steps.tsx:126-131`; command `api.ts` (`convertReceptionToWorkOrder`); the result read is `work-order-api.ts:17` (`readConvertedWorkOrder`) over the typed `work-order-contract.ts` (71).",
    "NORMAL_NAVIGATION_ENTRY": "Last step of the check-in wizard, after the summary — it is legal only from `authorized`, so it can only follow it (`check-in/steps.tsx:41-43`).",
    "BACKEND_READ_CONTRACTS": "`wo.work-order-detail` under `wo.work_order.read`, read ON INTENT rather than eagerly, showing the created work order and its jobs (`reception-summary.dom.test.tsx:698-741`). The `If-Match` source is `rec.reception-detail` from the shell.",
    "BACKEND_WRITE_CONTRACTS": "`rec.reception-convert-to-work-order` — POST .../convert-to-work-order. REACHABLE (`write-reachability.json`, under `rec.reception-convert-to-work-order`). There is no `POST /work-orders` anywhere in the platform; this command is the only way a work order comes to exist, which `p1-28-guidance-reconciliation.test.ts` \"‘conversion is the only way a work order comes to exist’\" pins against the register.",
    "REQUEST_SCHEMA": "Body `{}` or null, strict; `If-Match` REQUIRED and sent verbatim from the read (`reception-summary.dom.test.tsx:595-618`). The visit must be `authorized` or the command answers 409 `ERR-TRN-001`.",
    "RESPONSE_SCHEMA": "200 `{receptionVisitId, workOrderId, displayNumber|null, state, alreadyConverted}` — `state` is an OPAQUE catalogue code, NOT an enum, and there is NO ETag. The step renders the code as a code without translating it (`reception-summary.dom.test.tsx:645-668`), and a REPLAY (`alreadyConverted: true`) as the success it is (`:619-644`).",
    "REQUIRED_PERMISSIONS": "`rec.reception.convert` — NOT a `wo.*` code (WF-12) — for the command, and `wo.work_order.read` for the result display. Both resolved at the wizard route (`check-in/[receptionId]/page.tsx:165`, `:167`); the command is withdrawn without the conversion permission (`reception-summary.dom.test.tsx:774-790`), and the work order is said to EXIST when the operator may not read work orders (`:742-773`).",
    "OWNER_REQUIREMENT_MAPPING": "Journey step S-12, the conversion action ONLY — everything past it is P1-29 (`owner-workflow-requirements.md:220-239`; plan §6's quotable boundary).",
    "TENANT_ISOLATION_REQUIREMENT": "Visit-scoped path, encoded; cross-tenant renders as not-found.",
    "LOADING_STATE": "Shared pending action state on the command; the work-order read renders its own loading state on intent.",
    "EMPTY_STATE": "A converted visit with no readable work order states that the work order exists rather than showing an empty panel (`reception-summary.dom.test.tsx:742-773`).",
    "ZERO_RESULT_STATE": "N/A with rationale: nothing here is filtered by operator input.",
    "ERROR_STATE": "Shared failure rendering with the correlation reference; a failure is never presented as success.",
    "DENIAL_STATE": "Command withdrawn for the missing conversion permission; the result panel degrades to a statement for a missing `wo.work_order.read`.",
    "CONFLICT_STATE": "A conflict is stated WITHOUT inviting a retry the state would refuse (`reception-summary.dom.test.tsx:680-697`) — the opposite treatment from a stale-version conflict, which the shared `CommandOutcome` cures by re-reading.",
    "ARABIC_EVIDENCE": "`receptions.convert.*` in `ar.json`; the conversion REPLAY renders in Arabic (`reception-summary.dom.test.tsx:804-823`), which is the path most likely to be left untranslated.",
    "ENGLISH_EVIDENCE": "Same keys in `en.json`.",
    "RTL_LTR_EVIDENCE": "Arabic case mounts RTL; the work-order number and the opaque state code stay LTR.",
    "ACCESSIBILITY_EVIDENCE": "Labelled section; the command reached by accessible name; the outcome announced through the shared alert region. NAMED GAP: no axe run, no tablet case.",
    "PRODUCTION_REACHABILITY": "Registered step covered from the registry; the command is offered only where the graph has the edge (`reception-summary.dom.test.tsx:669-679`), which is `authorized` alone (`receptions-contract.test.ts` \"offers convert from authorized only\").",
    "PRODUCTION_TEST_EVIDENCE": "`reception-summary.dom.test.tsx:594-823` (11 cases) and `p1-28-qa.test.ts:727-753` (the replay shape at the adapter layer).",
    "NEGATIVE_OR_MUTATION_PROOF": "The three replay shapes are asserted DISTINCTLY at the adapter layer — create replay 200-no-ETag is success, approve re-run 409 `ERR-TRN-001` is a refusal re-reading does not cure, convert re-run 200 `alreadyConverted: true` is success and names the work order (`p1-28-qa.test.ts:674-753`). Collapse any two and a named case fails. `receptions-contract.test.ts` \"offers convert from authorized only\" fails if conversion is ever offered from a status other than `authorized`.",
    "PROTECTED_REPROOF": "PENDING PROTECTED MERGE — RE-OBSERVED for this re-derivation at `a2318c36`: the web tier ran 2669 cases, 0 failed and 0 skipped (28 P1-28 suites, 760 cases), and the root CI tier ran 2475 cases, 0 failed and 0 skipped (5 P1-28 gate suites, 180 cases). THE BROWSER TIER NOW CARRIES THIS PHASE — `apps/web/tests/e2e/authenticated/appointments-and-receptions.spec.ts` (2478 lines, against the running application, the running API and the real database), which the previous derivation correctly recorded as absent. It covers this row only as far as the data allows: \"every P1-28 route is reachable and renders\" walks the eight screens in the running application, and §18 now opens a real visit so the wizard is reachable. The conversion command itself is NOT observed in a browser — a visit must be `authorized` first, and §18 opens one rather than approving it. That is scope, and it is stated rather than implied. STILL NOT OBSERVED, and this is the whole outstanding debt on this row: that spec was NOT executed in this derivation — it needs the production launcher (`npm run acceptance:serve`), a live database and, for §18, a provisioned second workspace — and the run reported by the wave that landed it (319 expected, 0 unexpected, 0 flaky) is recorded in no phase document, so no browser result is bound to this head. THE TABLET VIEWPORT AND HOSTED CI WERE OBSERVED AT `38afa5c28e5b78d484a442cf6b8596fb2a5c34aa`, A HEAD THIS CANDIDATE SUPERSEDES. Two sentences stood here and both were false: that the `authenticated-tablet` project sees no P1-28 screen, and that no hosted-CI result exists for this head. `apps/web/playwright.config.ts:254-255` names that project and matches `(administration|appointments-and-receptions)`, so the phase surface is seen at 1024x768 as well as at 1440x900; run `31750364479` / job `94614564003` at head `38afa5c28e5b78d484a442cf6b8596fb2a5c34aa` executed `authenticated/appointments-and-receptions.spec.ts` 141 times — 47 in `authenticated-en`, 47 in `authenticated-ar`, 47 in `authenticated-tablet`, 0 failed and 0 skipped. Hosted CI at that head is run `31750364479`, 21 checks, 21 success, 0 failure, with `ci-gate` (`94617578352`) the single required check. The citation this cell used to give for the tablet claim pointed at lines 225 to 233 of the same config — the project DOCBLOCK, nine consecutive comment lines describing the state BEFORE the widening — so it read as confirmation of a sentence the code beneath it refutes; `validate:p1-28-evidence` now refuses a comment-only citation for exactly that reason. STILL OUTSTANDING ON THIS ROW: the protected-branch re-proof, because PR #226 has not merged, and OWNER ACCEPTANCE, which has not been requested or returned (`docs/phase-1/phase-1-28/evidence/change-log.md:5-11`). THAT HEAD IS NOT THE CURRENT CANDIDATE. Three fix waves changed 37 product files after `38afa5c28e5b78d484a442cf6b8596fb2a5c34aa`, so P1-28-QA-005 re-froze the candidate — at `6392ccb4321b004ed12e5d04ad583298da3303dd` then, and at `3c75f49a01e35b507461bf0929b0046e7140860a` now, the two being product-identical — and every hosted figure above is recorded in `docs/phase-1/phase-1-28/evidence/closure-candidate.json` as PENDING at that candidate — marked `describesSupersededHead`, citing the run it really came from, and listed in `pendingHostedBindings`. No hosted run has been taken at the current candidate yet, so no hosted result is bound to this head; `validate:p1-28-evidence` refuses a cell asserting an observation AT THIS CANDIDATE while the package records none.",
    "FINDING_IDS": "None open. The P1-29 boundary is respected: nothing here edits, assigns, routes or diagnoses a work order.",
    "DOCUMENTATION_EVIDENCE": "`canonical-plan.md` §5.2 and §6 (the quotable boundary); `contract-archaeology.md` Table B row B8; `operator-guide.md` 'Turning a visit into a work order'.",
    "FINAL_VERDICT": "PASS",
    "VERDICT_RATIONALE": "The single conversion action is wired, version-guarded from the read, offered only from `authorized`, and its three distinguishing response facts are each asserted: a replay is SUCCESS and not an error, the state is an opaque catalogue code rendered untranslated, and there is no ETag to source a version from. The work-order result is read on intent and degrades to a plain statement for an operator without `wo.work_order.read`, so the boundary at plan §6 holds — the step displays a result and does nothing a P1-29 screen would do."
  },
  "SEC-001": {
    "IMPLEMENTATION_SURFACES": "`scripts/ci/check-p1-28-access.mjs` (six rules over the eight P1-28 route pages), `docs/phase-1/phase-1-28/composed-permissions.json`, the permission maps `features/appointments/appointments-contract.ts` and `features/receptions/receptions-contract.ts`, and the acceptance-environment grant in `scripts/dev/owner-acceptance/context.mjs` / `create-owner-account.mjs`.",
    "NORMAL_NAVIGATION_ENTRY": "N/A with rationale: a permission surface has no screen of its own. Its subject is every route page in the two trees plus the navigation entries that lead to them.",
    "BACKEND_READ_CONTRACTS": "N/A with rationale: this task maps permissions and reads no business data. Its inputs are the P1-24 operation register and the platform permission catalogue seed, both read at check time rather than transcribed.",
    "BACKEND_WRITE_CONTRACTS": "N/A with rationale: no mutation. Role-to-grant seeding is Backend-owned; what this row owns is that every screen consults exactly the codes its reachable operations require.",
    "REQUEST_SCHEMA": "N/A with rationale: no operation of its own. The gate's rule 1 does govern request ORDER — every route denies and returns on a permission BEFORE its first awaited read, found by SHAPE rather than by a prefix (`tests/ci/p1-28-access-gate.test.ts:125-281`).",
    "RESPONSE_SCHEMA": "N/A with rationale: no operation of its own.",
    "REQUIRED_PERMISSIONS": "The whole seeded apt/rec set — `apt.appointment.read`, `apt.appointment.manage`, `apt.appointment.lifecycle.manage`, `rec.reception.read`, `rec.reception.manage`, `rec.reception.party.manage`, `rec.reception.authorization.verify`, `rec.reception.evidence.manage`, `rec.reception.signature.manage`, `rec.reception.approve`, `rec.reception.convert`, `rec.reception.close` — mirrored against the published permission of every apt/rec operation in BOTH directions (`apps/web/tests/p1-28-security.test.ts:120-148`), plus `iam.sensitive.view` composed per WF-27.",
    "OWNER_REQUIREMENT_MAPPING": "The Owner register question A block (`owner-workflow-requirements.md:199-216`): least-privilege mapping, an owner/reception account, a read-only negative control and a cross-tenant negative control. The `iam.user.read` staff-directory overload has an explicit written disposition (`p1-28-security.test.ts:215-285`).",
    "TENANT_ISOLATION_REQUIREMENT": "Shared with SEC-003/QA-003: scope is resolved by the server and asserted by nobody. The cross-tenant negative control this tier can actually run renders a cross-tenant record identically to a missing one, and discriminates a REFUSED read as a denial (`p1-28-permission-route-binding.test.ts` \"the cross-tenant negative control this tier can actually run\").",
    "LOADING_STATE": "N/A with rationale: a gate and a permission map render nothing.",
    "EMPTY_STATE": "N/A with rationale: as above. The gate's anti-vacuity cases stand in its place — it fails if it derived an empty permission set for any route (`tests/ci/p1-28-access-gate.test.ts:112-124`).",
    "ZERO_RESULT_STATE": "N/A with rationale: no list.",
    "ERROR_STATE": "The gate fails CLOSED on a permission expression it cannot resolve (`tests/ci/p1-28-access-gate.test.ts:252-266`) rather than skipping the route.",
    "DENIAL_STATE": "Every route renders the denial INSTEAD of the screen without its own read code (`p1-28-permission-route-binding.test.ts` \"renders the denial INSTEAD of the screen without its own read code\"), and a reader-only principal reaches every READ screen and is refused every write-gated one (\"the reader-only negative control\").",
    "CONFLICT_STATE": "N/A with rationale: no write.",
    "ARABIC_EVIDENCE": "The `iam.user.read` disposition is stated ON SCREEN in both catalogues from the module that carries the decision (`p1-28-security.test.ts:255-270`); the shared denial states carry Arabic (`p1-28-shared-components.dom.test.tsx:456-472`).",
    "ENGLISH_EVIDENCE": "Same strings in `en.json`, asserted by the same cases.",
    "RTL_LTR_EVIDENCE": "Denials render in Arabic RTL through the shared state components.",
    "ACCESSIBILITY_EVIDENCE": "N/A with rationale: this row governs which controls exist, not how they read. Its output is a withdrawn control WITH a reason rather than a disabled one, which is the accessible form of a denial (`p1-28-shared-components.dom.test.tsx:367-379`).",
    "PRODUCTION_REACHABILITY": "The route set is DERIVED from what the pages load, not from a hand-written list of segments, and is proved to be a strict subset of the dashboard tree so the derivation discriminates (`tests/ci/p1-28-access-gate.test.ts:580-626`).",
    "PRODUCTION_TEST_EVIDENCE": "`tests/ci/p1-28-access-gate.test.ts` (56 cases; gate green on this tree with 'it really looked' anti-vacuity at `:79-124`), `apps/web/tests/p1-28-security.test.ts:98-285` (11 cases), `apps/web/tests/p1-28-permission-route-binding.test.ts` (the full route matrix, reader-only and cross-tenant controls).",
    "NEGATIVE_OR_MUTATION_PROOF": "Every rule has a firing case: a read inside `Promise.all` before a permission check (`:146-155`), a deny gate that follows the read even with a capability `holds()` above it (`:174-195`), an unpublished code (`:283-292`), a code no reachable operation requires (`:335-345`) and its removal-of-the-justifying-link twin (`:365-383`), a scope in a query string (`:473-480`), a `holds()` inside the feature trees (`:553-562`). The composed-permission record is refused if its migration does not declare the policy it names, if the policy no longer demands the permission, if a NEIGHBOURING policy vouches for the wrong statement, or if it explains nothing (`:414-451`).",
    "PROTECTED_REPROOF": "PENDING PROTECTED MERGE — RE-OBSERVED for this re-derivation at `a2318c36`: the web tier ran 2669 cases, 0 failed and 0 skipped (28 P1-28 suites, 760 cases), and the root CI tier ran 2475 cases, 0 failed and 0 skipped (5 P1-28 gate suites, 180 cases). THE BROWSER TIER NOW CARRIES THIS PHASE — `apps/web/tests/e2e/authenticated/appointments-and-receptions.spec.ts` (2478 lines, against the running application, the running API and the real database), which the previous derivation correctly recorded as absent. It covers this row with the negative control the whole task rests on: \"a read-only operator meets a denial, not an empty screen\" signs in as the reader identity provisioned by `scripts/dev/owner-acceptance/context.mjs`, reaches the calendar and the queue, is REFUSED the booking form, and finds every write affordance absent with the wizard saying why — in the running application, where \"empty\" and \"denied\" would otherwise look identical. \"the receiving-employee control states what it opens\" renders the `iam.user.read` disposition beside the control on a live screen. STILL NOT OBSERVED, and this is the whole outstanding debt on this row: that spec was NOT executed in this derivation — it needs the production launcher (`npm run acceptance:serve`), a live database and, for §18, a provisioned second workspace — and the run reported by the wave that landed it (319 expected, 0 unexpected, 0 flaky) is recorded in no phase document, so no browser result is bound to this head. THE TABLET VIEWPORT AND HOSTED CI WERE OBSERVED AT `38afa5c28e5b78d484a442cf6b8596fb2a5c34aa`, A HEAD THIS CANDIDATE SUPERSEDES. Two sentences stood here and both were false: that the `authenticated-tablet` project sees no P1-28 screen, and that no hosted-CI result exists for this head. `apps/web/playwright.config.ts:254-255` names that project and matches `(administration|appointments-and-receptions)`, so the phase surface is seen at 1024x768 as well as at 1440x900; run `31750364479` / job `94614564003` at head `38afa5c28e5b78d484a442cf6b8596fb2a5c34aa` executed `authenticated/appointments-and-receptions.spec.ts` 141 times — 47 in `authenticated-en`, 47 in `authenticated-ar`, 47 in `authenticated-tablet`, 0 failed and 0 skipped. Hosted CI at that head is run `31750364479`, 21 checks, 21 success, 0 failure, with `ci-gate` (`94617578352`) the single required check. The citation this cell used to give for the tablet claim pointed at lines 225 to 233 of the same config — the project DOCBLOCK, nine consecutive comment lines describing the state BEFORE the widening — so it read as confirmation of a sentence the code beneath it refutes; `validate:p1-28-evidence` now refuses a comment-only citation for exactly that reason. STILL OUTSTANDING ON THIS ROW: the protected-branch re-proof, because PR #226 has not merged, and OWNER ACCEPTANCE, which has not been requested or returned (`docs/phase-1/phase-1-28/evidence/change-log.md:5-11`). THAT HEAD IS NOT THE CURRENT CANDIDATE. Three fix waves changed 37 product files after `38afa5c28e5b78d484a442cf6b8596fb2a5c34aa`, so P1-28-QA-005 re-froze the candidate — at `6392ccb4321b004ed12e5d04ad583298da3303dd` then, and at `3c75f49a01e35b507461bf0929b0046e7140860a` now, the two being product-identical — and every hosted figure above is recorded in `docs/phase-1/phase-1-28/evidence/closure-candidate.json` as PENDING at that candidate — marked `describesSupersededHead`, citing the run it really came from, and listed in `pendingHostedBindings`. No hosted run has been taken at the current candidate yet, so no hosted result is bound to this head; `validate:p1-28-evidence` refuses a cell asserting an observation AT THIS CANDIDATE while the package records none.",
    "FINDING_IDS": "None open. The `iam.user.read` overload is disposed of in writing and kept NAMED as debt rather than presented as solved (`p1-28-security.test.ts:271-285`).",
    "DOCUMENTATION_EVIDENCE": "`canonical-plan.md` §5.3 (SEC-001 scope); `composed-permissions.json` header; `contract-archaeology.md` riskiest-fact 4.",
    "FINAL_VERDICT": "PASS",
    "VERDICT_RATIONALE": "All five obligations plan §5.3 gives this row are discharged with executable evidence: the grant mapping is DERIVED from the same screens the gate judges and the withholding still bites (`tests/ci/p1-28-access-gate.test.ts:747-798`), the read-only control is proved genuinely narrower and still usable (`:799-816`), the cross-tenant control renders a foreign record as a missing one, and the `iam.user.read` overload has a written disposition stated on screen in both catalogues with the debt kept named. Least privilege is enforced as a gate rather than as a test, with a firing case per rule and a fail-closed default — which is the answer to `WRITE_PERMISSIONS` having had exactly one reference while ten P1-27 write forms rendered for any reader."
  },
  "SEC-002": {
    "IMPLEMENTATION_SURFACES": "`apps/web/src/features/receptions/check-in/sensitive.ts` (102 lines: the ordered gate at `:75` and the denial supplement at `:98`), consumed by `ComplaintsStep.tsx` and `ContentsStep.tsx`; the capability is resolved once at the wizard route (`check-in/[receptionId]/page.tsx:161`); the composition is recorded and verified at `composed-permissions.json:30-45`.",
    "NORMAL_NAVIGATION_ENTRY": "N/A with rationale: this row governs two existing steps rather than adding a surface — the complaints and contents steps of the check-in wizard.",
    "BACKEND_READ_CONTRACTS": "`rec.reception-condition-evidence-list`, whose union deliberately never selects `rec.complaint_details` or `rec.vehicle_content_details`. That is proved as a FIELD ASYMMETRY derived from the repository source rather than described (`p1-28-security.test.ts:397-431`).",
    "BACKEND_WRITE_CONTRACTS": "`rec.reception-condition-evidence` kinds `complaint` and `contents` — the two writes whose WF-27 dual-permission refusal this row owns. `p1-28-security.test.ts:305-327` confirms the OPERATION declares only the other code, which is the trap.",
    "REQUEST_SCHEMA": "Restricted narratives: complaint text 1-4000; contents description, declared value and currency. The gate decides whether the FORM is offered at all, before any request is spent.",
    "RESPONSE_SCHEMA": "The refusal shape this row must surface honestly: an application-level pass followed by a DATABASE 42501 mapped to `ERR-IAM-001` — a 403 at write time on a form the operator was allowed to open. It renders as a permissions message naming the missing capability, never as a crash and never as a generic error.",
    "REQUIRED_PERMISSIONS": "`iam.sensitive.view` composed onto `rec.reception.evidence.manage`. The gate opens `supabase/migrations/20260721099000_rec_complaints.sql` policy `ins_complaint_details_gated` and `20260721103000_rec_vehicle_contents.sql` policy `ins_vehicle_content_details_gated` and requires each statement to contain `iam.has_permission('iam.sensitive.view')` (`tests/ci/p1-28-access-gate.test.ts:384-471`).",
    "OWNER_REQUIREMENT_MAPPING": "Owner rows OR-10/OR-26 (concern capture) and the sensitive-data handling expectations of `docs/product/workshop/inspection-and-diagnostics.md` §7.3.",
    "TENANT_ISOLATION_REQUIREMENT": "The database check `iam.has_permission` is scope-aware, so holding the code is NECESSARY and never sufficient — the module says so and deliberately does not rewrite a denial into a diagnosis (`check-in/sensitive.ts:33-48`).",
    "LOADING_STATE": "N/A with rationale: the gate is synchronous over already-resolved capabilities.",
    "EMPTY_STATE": "An empty restricted read-back still states what the read cannot return, so emptiness is not read as the whole truth (`p1-28-sensitive-narratives.dom.test.tsx:299-307`).",
    "ZERO_RESULT_STATE": "N/A with rationale: no filtered search.",
    "ERROR_STATE": "A refusal that is not a permission problem is NOT blamed on one (`p1-28-sensitive-narratives.dom.test.tsx:260-297`) — the supplement returns null for every non-denied outcome (`check-in/sensitive.ts:98-102`).",
    "DENIAL_STATE": "Capture is withheld before the attempt with the requirement stated (`p1-28-sensitive-narratives.dom.test.tsx:137-178`); a database refusal renders the denial, a reference to quote and the pair, and KEEPS the form and the typed words (`:200-259`).",
    "CONFLICT_STATE": "N/A with rationale: appending evidence is unguarded.",
    "ARABIC_EVIDENCE": "Both statements are published in both catalogues and NEITHER is a copy of the other (`p1-28-security.test.ts:380-396`) — a copied Arabic string is the usual way this obligation is faked; the requirement is stated in Arabic per step (`p1-28-sensitive-narratives.dom.test.tsx:164-178`).",
    "ENGLISH_EVIDENCE": "Same keys in `en.json`, asserted by the same cases.",
    "RTL_LTR_EVIDENCE": "The Arabic requirement cases render under RTL.",
    "ACCESSIBILITY_EVIDENCE": "The withheld form is replaced by text, not by a disabled control; the restriction is stated on EVERY restricted panel from ONE string, so the two steps cannot drift (`p1-28-security.test.ts:432-496`).",
    "PRODUCTION_REACHABILITY": "The composition is a fact about an OPERATION, so any screen that can reach it inherits the code — a new evidence screen needs no entry and a screen that cannot reach the operation gets no licence from it (`tests/ci/p1-28-access-gate.test.ts:452-471`).",
    "PRODUCTION_TEST_EVIDENCE": "`apps/web/tests/p1-28-security.test.ts:286-496` (8 cases), `apps/web/tests/p1-28-sensitive-narratives.dom.test.tsx` (12 cases), `tests/ci/p1-28-access-gate.test.ts:384-471` (7 cases over the composed record).",
    "NEGATIVE_OR_MUTATION_PROOF": "`p1-28-security.test.ts:328-346` fails if the gate covers a kind that is NOT restricted — a damage mark must not require a sensitive-data capability — and `:347-358` fixes the ordering by what is most true, so an ended visit cannot be reported as a permission problem. `:359-379` asserts the message names the pair and DIAGNOSES nothing, which is the non-disclosing posture the platform takes deliberately.",
    "PROTECTED_REPROOF": "PENDING PROTECTED MERGE — RE-OBSERVED for this re-derivation at `a2318c36`: the web tier ran 2669 cases, 0 failed and 0 skipped (28 P1-28 suites, 760 cases), and the root CI tier ran 2475 cases, 0 failed and 0 skipped (5 P1-28 gate suites, 180 cases). THE BROWSER TIER NOW CARRIES THIS PHASE — `apps/web/tests/e2e/authenticated/appointments-and-receptions.spec.ts` (2478 lines, against the running application, the running API and the real database), which the previous derivation correctly recorded as absent. It does NOT cover this row: the WF-27 denial needs a principal holding `rec.reception.evidence.manage` WITHOUT `iam.sensitive.view`, and the acceptance context provisions no such identity — `tests/ci/p1-28-access-gate.test.ts` \"keeps the WF-27 pair unsatisfiable for the reader — the SEC-002 control\" pins that the reader cannot satisfy the pair, which is a different claim from walking the refusal. So this row’s database-refusal branch remains proved only against mocked transport, and that is stated rather than implied. STILL NOT OBSERVED, and this is the whole outstanding debt on this row: that spec was NOT executed in this derivation — it needs the production launcher (`npm run acceptance:serve`), a live database and, for §18, a provisioned second workspace — and the run reported by the wave that landed it (319 expected, 0 unexpected, 0 flaky) is recorded in no phase document, so no browser result is bound to this head. THE TABLET VIEWPORT AND HOSTED CI WERE OBSERVED AT `38afa5c28e5b78d484a442cf6b8596fb2a5c34aa`, A HEAD THIS CANDIDATE SUPERSEDES. Two sentences stood here and both were false: that the `authenticated-tablet` project sees no P1-28 screen, and that no hosted-CI result exists for this head. `apps/web/playwright.config.ts:254-255` names that project and matches `(administration|appointments-and-receptions)`, so the phase surface is seen at 1024x768 as well as at 1440x900; run `31750364479` / job `94614564003` at head `38afa5c28e5b78d484a442cf6b8596fb2a5c34aa` executed `authenticated/appointments-and-receptions.spec.ts` 141 times — 47 in `authenticated-en`, 47 in `authenticated-ar`, 47 in `authenticated-tablet`, 0 failed and 0 skipped. Hosted CI at that head is run `31750364479`, 21 checks, 21 success, 0 failure, with `ci-gate` (`94617578352`) the single required check. The citation this cell used to give for the tablet claim pointed at lines 225 to 233 of the same config — the project DOCBLOCK, nine consecutive comment lines describing the state BEFORE the widening — so it read as confirmation of a sentence the code beneath it refutes; `validate:p1-28-evidence` now refuses a comment-only citation for exactly that reason. STILL OUTSTANDING ON THIS ROW: the protected-branch re-proof, because PR #226 has not merged, and OWNER ACCEPTANCE, which has not been requested or returned (`docs/phase-1/phase-1-28/evidence/change-log.md:5-11`). THAT HEAD IS NOT THE CURRENT CANDIDATE. Three fix waves changed 37 product files after `38afa5c28e5b78d484a442cf6b8596fb2a5c34aa`, so P1-28-QA-005 re-froze the candidate — at `6392ccb4321b004ed12e5d04ad583298da3303dd` then, and at `3c75f49a01e35b507461bf0929b0046e7140860a` now, the two being product-identical — and every hosted figure above is recorded in `docs/phase-1/phase-1-28/evidence/closure-candidate.json` as PENDING at that candidate — marked `describesSupersededHead`, citing the run it really came from, and listed in `pendingHostedBindings`. No hosted run has been taken at the current candidate yet, so no hosted result is bound to this head; `validate:p1-28-evidence` refuses a cell asserting an observation AT THIS CANDIDATE while the package records none.",
    "FINDING_IDS": "None open.",
    "DOCUMENTATION_EVIDENCE": "`canonical-plan.md` §5.3 (SEC-002 scope); `contract-archaeology.md` riskiest-fact 4; `composed-permissions.json` header.",
    "FINAL_VERDICT": "PASS",
    "VERDICT_RATIONALE": "The obligation is discharged in both halves and each half is executable. Capture: the form is withheld before a write that could only be refused, with the requirement stated in both languages, ordered so an ended visit is never reported as a permission problem, and scoped to exactly the two restricted kinds with a control proving a third kind is unaffected. Refusal: the database 403 renders as a permissions message that names the pair, carries the reference, keeps the typed words and diagnoses nothing — with a control proving a non-permission failure is not blamed on a permission. Rendering: the read genuinely cannot return the narrative, proved as a field asymmetry read from the repository source, and the restriction is stated on every restricted panel from one string."
  },
  "SEC-003": {
    "IMPLEMENTATION_SURFACES": "Rule 5 `no-scope-in-a-url` and rule 6 `route-owns-the-gate` of `scripts/ci/check-p1-28-access.mjs`, applied to `features/appointments`, `features/receptions`, `components/**` and `lib/**`; the single door is the shared read-operation guard, and the branch target travels as a typed VALUE through `features/appointments/components/BranchTargetFields.tsx` and the screens that use it.",
    "NORMAL_NAVIGATION_ENTRY": "N/A with rationale: scope hygiene governs how every adapter and screen is written rather than adding a surface.",
    "BACKEND_READ_CONTRACTS": "Every list read this phase performs. Four operations — and exactly four — legitimately take the company/branch pair as a REQUIRED resource selector, derived rather than listed (`apps/web/tests/p1-28-qa.test.ts:819-896`).",
    "BACKEND_WRITE_CONTRACTS": "N/A with rationale: scope hygiene is a rule about how requests are formed, not a contract of its own. It governs the writes' bodies too — the branch pair on `apt.appointment-create` and `rec.reception-create` is a required FIELD, not a client claim.",
    "REQUEST_SCHEMA": "No tenant, company or branch key may appear in a query string; the pair travels as a value where an operation requires it. `p1-28-qa.test.ts:909-923` pins WHICH requests carry the pair so the exemption cannot widen quietly, and `:924-939` asserts a tenant is never sent on any request at all.",
    "RESPONSE_SCHEMA": "N/A with rationale: no operation of its own. The cursor discipline is the response-side rule: the client forwards the server's token and mints none of its own (`apps/web/tests/p1-28-security.test.ts:681-713`).",
    "REQUIRED_PERMISSIONS": "N/A with rationale: this row enforces the ABSENCE of client-asserted scope. Scope is resolved from the session server-side, and no screen in either tree can resolve or widen its own (`p1-28-qa.test.ts:971-1021`).",
    "OWNER_REQUIREMENT_MAPPING": "N/A with rationale: no Owner table row binds this task; it polices the platform's isolation contract on the Owner's behalf.",
    "TENANT_ISOLATION_REQUIREMENT": "This row IS the requirement. The exemption is recorded honestly — the pair is a REQUIRED FIELD, not a client claim (`p1-28-security.test.ts:507-540`) — and narrowed in the gate by a rule-level distinction rather than by an exemption entry (`:541-561`).",
    "LOADING_STATE": "N/A with rationale: a gate renders nothing.",
    "EMPTY_STATE": "N/A with rationale: as above.",
    "ZERO_RESULT_STATE": "A refused cursor surfaces as an error with a reference, NEVER as 'empty' (`p1-28-security.test.ts:735-760`) — an empty page is a claim about data and a refused cursor is not one.",
    "ERROR_STATE": "As above; a target change RESTARTS the table instead of paging the old scope (`p1-28-security.test.ts:714-734`).",
    "DENIAL_STATE": "A forged branch request carries the named pair and NOTHING about the caller, so the server decides (`p1-28-security.test.ts:562-601`), with a control proving the same call reports ok when the server allows it (`:602-610`).",
    "CONFLICT_STATE": "N/A with rationale: no write of its own.",
    "ARABIC_EVIDENCE": "N/A with rationale: this row adds no copy. The branch-target fields it governs carry Arabic and are asserted in both directions (`p1-28-shared-components.dom.test.tsx:492-573`).",
    "ENGLISH_EVIDENCE": "As above.",
    "RTL_LTR_EVIDENCE": "As above — the field pair reports each half separately so an error lands on the control it is about, in either direction.",
    "ACCESSIBILITY_EVIDENCE": "N/A with rationale: no rendering of its own. The fields it governs offer the session's OWN resolved references and explain why there are no names (`p1-28-shared-components.dom.test.tsx:496-514`).",
    "PRODUCTION_REACHABILITY": "Rule 5 reaches `components/**` and `lib/**`, which no earlier root covered (`tests/ci/p1-28-access-gate.test.ts:511-528`), and does NOT extend the tenant clause outside the trees P1-28 owns (`:529-551`).",
    "PRODUCTION_TEST_EVIDENCE": "`apps/web/tests/p1-28-security.test.ts:497-760` (10 cases, three abuse cases with controls), `apps/web/tests/p1-28-qa.test.ts:819-1062` (10 cases), `tests/ci/p1-28-access-gate.test.ts:472-579` (9 cases).",
    "NEGATIVE_OR_MUTATION_PROOF": "Three abuse cases, each with a control: a forged branch, a scope key smuggled among ordinary filters — refused at the call site AND driven through the real adapter where the control is an ALLOW-LIST (`p1-28-security.test.ts:611-680`) — and a minted cursor. The gate fires on a tenant in ANY position and does NOT fire on a form-error key that merely happens to be named for a control (`tests/ci/p1-28-access-gate.test.ts:481-500`), which is the false-positive that would have made the rule unusable.",
    "PROTECTED_REPROOF": "PENDING PROTECTED MERGE — RE-OBSERVED for this re-derivation at `a2318c36`: the web tier ran 2669 cases, 0 failed and 0 skipped (28 P1-28 suites, 760 cases), and the root CI tier ran 2475 cases, 0 failed and 0 skipped (5 P1-28 gate suites, 180 cases). THE BROWSER TIER NOW CARRIES THIS PHASE — `apps/web/tests/e2e/authenticated/appointments-and-receptions.spec.ts` (2478 lines, against the running application, the running API and the real database), which the previous derivation correctly recorded as absent. It covers this row at \"nothing this phase reads reaches the address bar\" (no branch target and no identifier appears in a URL the browser navigates to; no request the browser issues carries a tenant parameter) and at \"the P1-28 surface discloses nothing of another workspace\", which additionally drives the API directly: a branch-scoped token is REFUSED both boards for another tenant, an unrestricted token gets an EMPTY board rather than a foreign row, and a control case proves the same token reads its OWN branch so the refusals mean something. STILL NOT OBSERVED, and this is the whole outstanding debt on this row: that spec was NOT executed in this derivation — it needs the production launcher (`npm run acceptance:serve`), a live database and, for §18, a provisioned second workspace — and the run reported by the wave that landed it (319 expected, 0 unexpected, 0 flaky) is recorded in no phase document, so no browser result is bound to this head. THE TABLET VIEWPORT AND HOSTED CI WERE OBSERVED AT `38afa5c28e5b78d484a442cf6b8596fb2a5c34aa`, A HEAD THIS CANDIDATE SUPERSEDES. Two sentences stood here and both were false: that the `authenticated-tablet` project sees no P1-28 screen, and that no hosted-CI result exists for this head. `apps/web/playwright.config.ts:254-255` names that project and matches `(administration|appointments-and-receptions)`, so the phase surface is seen at 1024x768 as well as at 1440x900; run `31750364479` / job `94614564003` at head `38afa5c28e5b78d484a442cf6b8596fb2a5c34aa` executed `authenticated/appointments-and-receptions.spec.ts` 141 times — 47 in `authenticated-en`, 47 in `authenticated-ar`, 47 in `authenticated-tablet`, 0 failed and 0 skipped. Hosted CI at that head is run `31750364479`, 21 checks, 21 success, 0 failure, with `ci-gate` (`94617578352`) the single required check. The citation this cell used to give for the tablet claim pointed at lines 225 to 233 of the same config — the project DOCBLOCK, nine consecutive comment lines describing the state BEFORE the widening — so it read as confirmation of a sentence the code beneath it refutes; `validate:p1-28-evidence` now refuses a comment-only citation for exactly that reason. STILL OUTSTANDING ON THIS ROW: the protected-branch re-proof, because PR #226 has not merged, and OWNER ACCEPTANCE, which has not been requested or returned (`docs/phase-1/phase-1-28/evidence/change-log.md:5-11`). THAT HEAD IS NOT THE CURRENT CANDIDATE. Three fix waves changed 37 product files after `38afa5c28e5b78d484a442cf6b8596fb2a5c34aa`, so P1-28-QA-005 re-froze the candidate — at `6392ccb4321b004ed12e5d04ad583298da3303dd` then, and at `3c75f49a01e35b507461bf0929b0046e7140860a` now, the two being product-identical — and every hosted figure above is recorded in `docs/phase-1/phase-1-28/evidence/closure-candidate.json` as PENDING at that candidate — marked `describesSupersededHead`, citing the run it really came from, and listed in `pendingHostedBindings`. No hosted run has been taken at the current candidate yet, so no hosted result is bound to this head; `validate:p1-28-evidence` refuses a cell asserting an observation AT THIS CANDIDATE while the package records none.",
    "FINDING_IDS": "None open. The named exemption the day-one row anticipated exists and is pinned: the branch pair is a required field on four operations, enforced at ONE door rather than trusted to every call site (`p1-28-qa.test.ts:954-970`).",
    "DOCUMENTATION_EVIDENCE": "`canonical-plan.md` §5.3 (SEC-003 scope); `contract-archaeology.md` §5 honourable mentions (scope keys in query strings throw client-side).",
    "FINAL_VERDICT": "PASS",
    "VERDICT_RATIONALE": "The rule the day-one row committed to — a named exemption with a pinning test rather than a workaround — is what shipped: the four operations that legitimately take a branch pair are DERIVED rather than listed, the requests that carry it are pinned so the exemption cannot widen quietly, no tenant is ever sent, and the guard sits at one door instead of being trusted to every call site. Three abuse cases try the walls and each has a control, so a green result cannot come from a check that refuses everything. The gate reaches two trees the earlier roots never covered and is proved not to fire on the obvious false positive."
  },
  "SEC-004": {
    "IMPLEMENTATION_SURFACES": "`scripts/ci/check-p1-28-write-reachability.mjs` (`validate:p1-28-write-reachability`) and its judged manifest `docs/phase-1/phase-1-28/write-reachability.json`. The subject set is DERIVED at check time and is 35 at this head — the 14 canonical apt/rec writes plus the 21 catalogue-management writes PR #227 registered — never a hand list.",
    "NORMAL_NAVIGATION_ENTRY": "N/A with rationale: a CI gate has no screen. Its subject is every mutation call site in `apps/web/src`.",
    "BACKEND_READ_CONTRACTS": "N/A with rationale: the gate walks the P1-24 operation register and the web source tree; it reads no business data.",
    "BACKEND_WRITE_CONTRACTS": "Every `apt.*`/`rec.*` operation whose method is not GET, DERIVED at check time (`tests/ci/p1-28-write-reachability.test.ts` \"derives the canonical writes from the register rather than a hand list\"). Thirteen are REACHABLE with named consumed call sites; one, `rec.reception-signature`, is honestly `NOT_YET_WIRED`; and 21 are `DELIBERATELY_ABSENT` against `P1-28-OD-001`, a count the gate pins exactly (\"pins the live DELIBERATELY_ABSENT count at exactly 21\") and whose every reference it resolves against `canonical-plan.md` §7 (\"resolves every live DELIBERATELY_ABSENT reference against the plan §7\").",
    "REQUEST_SCHEMA": "N/A with rationale: no operation of its own. What it inspects is request SHAPE in source — it recognises a write WRAPPER by shape so a wrapped POST is not read as a GET, and refuses to let a READ borrow a nearby write-method literal (`tests/ci/p1-28-write-reachability.test.ts:445-501`).",
    "RESPONSE_SCHEMA": "N/A with rationale: no operation of its own. Its own output is a classification per operation with the file that makes each REACHABLE one reachable (`:265-270`).",
    "REQUIRED_PERMISSIONS": "N/A with rationale: a CI gate holds no permission.",
    "OWNER_REQUIREMENT_MAPPING": "N/A with rationale: no Owner table row binds this task. It exists because `P1-27-INT-113` — ten canonical writes with a route, a permission, an audit class, an idempotency entry, an OpenAPI path and a register row, and no screen from which anybody could invoke them — must not recur.",
    "TENANT_ISOLATION_REQUIREMENT": "N/A with rationale: no runtime behaviour.",
    "LOADING_STATE": "N/A with rationale: a gate renders nothing.",
    "EMPTY_STATE": "N/A with rationale: as above. Its anti-vacuity cases stand in its place: it FAILS when zero files are scanned, when zero operations are derived and when no mutation call site exists anywhere (`tests/ci/p1-28-write-reachability.test.ts:395-413`).",
    "ZERO_RESULT_STATE": "N/A with rationale: no list.",
    "ERROR_STATE": "Malformed input is a violation, never a skip — a malformed classification and a manifest entry that is not canonical both fail (`tests/ci/p1-28-write-reachability.test.ts:379-394`).",
    "DENIAL_STATE": "N/A with rationale: no permission surface.",
    "CONFLICT_STATE": "N/A with rationale: no write of its own.",
    "ARABIC_EVIDENCE": "N/A with rationale: the gate emits developer diagnostics, not product copy.",
    "ENGLISH_EVIDENCE": "As above.",
    "RTL_LTR_EVIDENCE": "N/A with rationale: no rendering.",
    "ACCESSIBILITY_EVIDENCE": "N/A with rationale: no rendering.",
    "PRODUCTION_REACHABILITY": "The gate is `required` in the command register and is named DIRECTLY by a hosted job (`.github/workflows/_reusable-node-quality.yml:652`), not reached only through an aggregate.",
    "PRODUCTION_TEST_EVIDENCE": "`tests/ci/p1-28-write-reachability.test.ts` (50 cases at this head; green on the CURRENT tree, with the DELIBERATELY_ABSENT count pinned at exactly 21 and every decision reference resolved against the plan).",
    "NEGATIVE_OR_MUTATION_PROOF": "A fabricated unreached write fails as UNWIRED — INT-113 recurring (`:271-279`); a REACHABLE claim whose call site was deleted fails (`:280-290`); the ratchet fails a NOT_YET_WIRED id that is not on the frozen day-one list and fails an allow-listed operation that IS called (`:291-320`), while accepting the list SHRINKING with no test edit (`:321-338`). The finding that shaped the gate is itself proved: a REACHABLE claim backed only by the adapter that declares it FAILS, and a docblock naming the adapter is not consumption (`:667-717`).",
    "PROTECTED_REPROOF": "PENDING PROTECTED MERGE — RE-OBSERVED for this re-derivation at `a2318c36`: the root CI tier ran 2475 cases, 0 failed and 0 skipped, including this gate’s own 50, and `validate:p1-28-write-reachability` reports zero violations on the current tree. The authenticated-browser tier is not this row’s evidence — a source-reachability gate has nothing for a browser to observe. OBSERVED AT `38afa5c28e5b78d484a442cf6b8596fb2a5c34aa`, A HEAD THIS CANDIDATE SUPERSEDES. This cell used to deny that any hosted-CI result existed for this head, and that denial was false: run `31750364479` at `38afa5c28e5b78d484a442cf6b8596fb2a5c34aa` is 21 checks, 21 success, 0 failure, and `static-quality` (`94614564036`), the job that runs this gate, is green there. STILL OUTSTANDING: the protected-branch re-proof, because PR #226 has not merged, and OWNER ACCEPTANCE, which has not been requested or returned (`docs/phase-1/phase-1-28/evidence/change-log.md:5-11`). THAT HEAD IS NOT THE CURRENT CANDIDATE. Three fix waves changed 37 product files after `38afa5c28e5b78d484a442cf6b8596fb2a5c34aa`, so P1-28-QA-005 re-froze the candidate — at `6392ccb4321b004ed12e5d04ad583298da3303dd` then, and at `3c75f49a01e35b507461bf0929b0046e7140860a` now, the two being product-identical — and every hosted figure above is recorded in `docs/phase-1/phase-1-28/evidence/closure-candidate.json` as PENDING at that candidate — marked `describesSupersededHead`, citing the run it really came from, and listed in `pendingHostedBindings`. No hosted run has been taken at the current candidate yet, so no hosted result is bound to this head; `validate:p1-28-evidence` refuses a cell asserting an observation AT THIS CANDIDATE while the package records none.",
    "FINDING_IDS": "One honest allow-list entry remains: `rec.reception-signature` NOT_YET_WIRED with its reason (`write-reachability.json`, under `rec.reception-signature`), which is FE-018's blocker rather than a defect in this gate.",
    "DOCUMENTATION_EVIDENCE": "`canonical-plan.md` §5.3 (SEC-004 scope); `write-reachability.json` header; `evidence/change-log.md` 'Wave A'.",
    "FINAL_VERDICT": "PASS",
    "VERDICT_RATIONALE": "The gate exists, derives its universe from the register rather than from a list somebody maintains, is green on this tree with three anti-vacuity failures proving it examines something, and has a firing case for every way it could be fooled — a fabricated write, a deleted call site, a widened allow-list, a malformed entry, a comment, a read on the same path, a helper-built path it cannot vouch for. Its hardest rule is the one Wave A discovered and it is proved: an adapter nobody consumes is not reachability. At this head the subject set grew from 14 to 35 without a hand edit, because PR #227 registered 21 catalogue-management writes: all 21 are `DELIBERATELY_ABSENT` against a decision the gate RESOLVES against `canonical-plan.md` §7, so they cannot be quietly parked — and the count is pinned exactly, so a twenty-second could not join them in silence. Thirteen of the fourteen canonical writes are reachable and the fourteenth is allow-listed with a reason."
  },
  "QA-001": {
    "IMPLEMENTATION_SURFACES": "`apps/web/tests/p1-28-qa.test.ts` over the drive corpus `apps/web/tests/support/p1-28-drives.ts` (440 lines), plus the two contract mirrors `apps/web/tests/appointments-contract.test.ts` (332 lines) and `apps/web/tests/receptions-contract.test.ts` (403) against the generated operation manifest.",
    "NORMAL_NAVIGATION_ENTRY": "N/A with rationale: a test suite has no screen.",
    "BACKEND_READ_CONTRACTS": "Every read adapter the phase owns is driven by NAME and its request is inspected on the wire; the mirrors cover every published apt/rec read plus `crm.customer-vehicle-list` (`receptions-contract.test.ts` \"covers every published rec.* operation, and invents none\").",
    "BACKEND_WRITE_CONTRACTS": "All 14 apt/rec writes, mirrored from ROUTE SOURCE rather than from OpenAPI — the P1-27-INT-004 lesson, where the generator published 200 for 201 routes and never published 400/404.",
    "REQUEST_SCHEMA": "The mirrors ARE the request-schema evidence: the lifecycle vocabularies against the frozen CHECK constraints (`appointments-contract.test.ts:160-213`, `receptions-contract.test.ts` \"the lifecycle mirror matches the frozen CHECK constraint\"), the window rules against the module domain (`appointments-contract.test.ts:214-259`), the differing evidence vocabularies and the refusing-partner rule (`receptions-contract.test.ts` \"the vocabularies keep the server distinctions\"), and the bounds matched to the routes rather than to stricter guesses (\"bounds match the routes, not stricter guesses\").",
    "RESPONSE_SCHEMA": "The three replay shapes and the no-ETag convert response are held by QA-002; this row holds that every operation an adapter reaches has a contract-module row (`p1-28-qa.test.ts:303-325`) — the join neither contract suite can make alone.",
    "REQUIRED_PERMISSIONS": "N/A with rationale: a test suite holds no permission. It does assert that the two contract modules' permission maps mirror the published permission of every apt/rec operation in both directions.",
    "OWNER_REQUIREMENT_MAPPING": "N/A with rationale: no Owner table row binds this task; it is the WFP `U1` exit criterion — contract modules proven before screens.",
    "TENANT_ISOLATION_REQUIREMENT": "N/A with rationale: isolation is QA-003's subject.",
    "LOADING_STATE": "N/A with rationale: a suite renders nothing. Component rendering is proved by the `.dom` suites one component at a time.",
    "EMPTY_STATE": "N/A with rationale: as above.",
    "ZERO_RESULT_STATE": "N/A with rationale: as above.",
    "ERROR_STATE": "N/A with rationale: error paths are QA-002's subject.",
    "DENIAL_STATE": "N/A with rationale: denial is SEC-001's and QA-002's subject.",
    "CONFLICT_STATE": "N/A with rationale: conflicts are QA-002's subject.",
    "ARABIC_EVIDENCE": "N/A with rationale: this row measures contracts and coverage. Both catalogues are asserted per feature by the `.dom` suites and by the catalogue-parity gate.",
    "ENGLISH_EVIDENCE": "As above.",
    "RTL_LTR_EVIDENCE": "N/A with rationale: direction is asserted per screen, not here.",
    "ACCESSIBILITY_EVIDENCE": "N/A with rationale: as above.",
    "PRODUCTION_REACHABILITY": "The corpus DISCOVERS adapter modules by SHAPE and fails if a declaration form the tree lacks is read (`p1-28-qa.test.ts:226-240`); it excludes itself from the corpus it measures (`:374-379`).",
    "PRODUCTION_TEST_EVIDENCE": "`apps/web/tests/p1-28-qa.test.ts` (44 cases), `apps/web/tests/appointments-contract.test.ts` (61), `apps/web/tests/receptions-contract.test.ts` (57). All re-run green at this head.",
    "NEGATIVE_OR_MUTATION_PROOF": "Coverage is DERIVED, not asserted as a filename list: every component of the two feature trees must be named by a test (`p1-28-qa.test.ts:327-359`) with a positive control proving a component named only in PROSE does not satisfy it (`:360-373`); every wizard step is covered from the REGISTRY rather than by name (`:380-393`); every route page the two trees publish is covered (`:394-431`). No exclusion may name an adapter that no longer exists (`:216-225`), and each drive must issue exactly one request on the channel it declares (`:241-254`), so a hard-coded empty answer cannot pass the mapping cases.",
    "PROTECTED_REPROOF": "PENDING PROTECTED MERGE — RE-OBSERVED for this re-derivation at `a2318c36`: the web tier ran 2669 cases, 0 failed and 0 skipped (28 P1-28 suites, 760 cases), and the root CI tier ran 2475 cases, 0 failed and 0 skipped (5 P1-28 gate suites, 180 cases). THE BROWSER TIER NOW CARRIES THIS PHASE — `apps/web/tests/e2e/authenticated/appointments-and-receptions.spec.ts` (2478 lines, against the running application, the running API and the real database), which the previous derivation correctly recorded as absent. It bears on this row only indirectly: the corpus measures adapters and coverage, and a browser cannot observe a coverage derivation. What the browser tier now adds is the thing this row explicitly does NOT claim — that the components it proves are NAMED are also RENDERED against a real backend — and it adds it for the routes and screens §1, §6 and §18 walk. STILL NOT OBSERVED, and this is the whole outstanding debt on this row: that spec was NOT executed in this derivation — it needs the production launcher (`npm run acceptance:serve`), a live database and, for §18, a provisioned second workspace — and the run reported by the wave that landed it (319 expected, 0 unexpected, 0 flaky) is recorded in no phase document, so no browser result is bound to this head. THE TABLET VIEWPORT AND HOSTED CI WERE OBSERVED AT `38afa5c28e5b78d484a442cf6b8596fb2a5c34aa`, A HEAD THIS CANDIDATE SUPERSEDES. Two sentences stood here and both were false: that the `authenticated-tablet` project sees no P1-28 screen, and that no hosted-CI result exists for this head. `apps/web/playwright.config.ts:254-255` names that project and matches `(administration|appointments-and-receptions)`, so the phase surface is seen at 1024x768 as well as at 1440x900; run `31750364479` / job `94614564003` at head `38afa5c28e5b78d484a442cf6b8596fb2a5c34aa` executed `authenticated/appointments-and-receptions.spec.ts` 141 times — 47 in `authenticated-en`, 47 in `authenticated-ar`, 47 in `authenticated-tablet`, 0 failed and 0 skipped. Hosted CI at that head is run `31750364479`, 21 checks, 21 success, 0 failure, with `ci-gate` (`94617578352`) the single required check. The citation this cell used to give for the tablet claim pointed at lines 225 to 233 of the same config — the project DOCBLOCK, nine consecutive comment lines describing the state BEFORE the widening — so it read as confirmation of a sentence the code beneath it refutes; `validate:p1-28-evidence` now refuses a comment-only citation for exactly that reason. STILL OUTSTANDING ON THIS ROW: the protected-branch re-proof, because PR #226 has not merged, and OWNER ACCEPTANCE, which has not been requested or returned (`docs/phase-1/phase-1-28/evidence/change-log.md:5-11`). THAT HEAD IS NOT THE CURRENT CANDIDATE. Three fix waves changed 37 product files after `38afa5c28e5b78d484a442cf6b8596fb2a5c34aa`, so P1-28-QA-005 re-froze the candidate — at `6392ccb4321b004ed12e5d04ad583298da3303dd` then, and at `3c75f49a01e35b507461bf0929b0046e7140860a` now, the two being product-identical — and every hosted figure above is recorded in `docs/phase-1/phase-1-28/evidence/closure-candidate.json` as PENDING at that candidate — marked `describesSupersededHead`, citing the run it really came from, and listed in `pendingHostedBindings`. No hosted run has been taken at the current candidate yet, so no hosted result is bound to this head; `validate:p1-28-evidence` refuses a cell asserting an observation AT THIS CANDIDATE while the package records none.",
    "FINDING_IDS": "One limitation stated by the suite itself rather than by a reviewer: the component sweep proves NAMING, not rendering, and says so (`p1-28-qa.test.ts:334-347`). Rendering is proved where it is proved — by the `.dom` suites against real props.",
    "DOCUMENTATION_EVIDENCE": "`canonical-plan.md` §5.3; `evidence/change-log.md` 'Security, QA and DevOps riders'.",
    "FINAL_VERDICT": "PASS",
    "VERDICT_RATIONALE": "The mirror is executable in both directions at the layer that actually calls: every published apt/rec operation is REACHED by an adapter and no adapter calls a path the contract does not publish, and every operation reached has a contract row. Coverage is derived from the filesystem and from the step registry rather than from a list of suite filenames — the exact failure that let six P1-27 components ship with zero component coverage under a green QA-001 — and the sweep carries a positive control so a component named only in a comment cannot satisfy it. The suite also states its own limit rather than overclaiming: it proves naming, not rendering. That limit is now partly answered from outside this row, by the browser tier, which renders eight route pages and the wizard against the running application."
  },
  "QA-002": {
    "IMPLEMENTATION_SURFACES": "`apps/web/tests/p1-28-qa.test.ts:432-818` — eleven transport kinds, the four refusal branches, the three replay shapes and the approve version behaviour, all driven through the real adapters.",
    "NORMAL_NAVIGATION_ENTRY": "N/A with rationale: a test suite has no screen.",
    "BACKEND_READ_CONTRACTS": "Every read adapter is mapped from every transport kind to the state an operator should see (`p1-28-qa.test.ts:439-451`), and a 403 on a read renders a denial rather than an empty list (`:549-560`).",
    "BACKEND_WRITE_CONTRACTS": "All 14 writes: every kind maps to an action state and NEVER to success (`:452-486`), and every 403 reports a denial rather than invalid input (`:535-548`).",
    "REQUEST_SCHEMA": "422 `violations[{path,rule}]` marks the control the violation names, as a KEY and never as server prose (`p1-28-qa.test.ts:590-612`) — server prose on a control is untranslatable and this is the case that forbids it.",
    "RESPONSE_SCHEMA": "THE THREE REPLAY SHAPES, asserted distinctly (`p1-28-qa.test.ts:674-753`): create replay is 200 with no ETag and the stored body travels, and it is SUCCESS; approve re-run is 409 `ERR-TRN-001`, a refusal re-reading does NOT cure; convert re-run is 200 `alreadyConverted: true`, success, naming the work order.",
    "REQUIRED_PERMISSIONS": "N/A with rationale: a test suite holds no permission.",
    "OWNER_REQUIREMENT_MAPPING": "N/A with rationale: no Owner table row binds this task; it protects every FE task's failure honesty.",
    "TENANT_ISOLATION_REQUIREMENT": "N/A with rationale: isolation is QA-003's subject.",
    "LOADING_STATE": "N/A with rationale: the suite maps outcomes, it renders none.",
    "EMPTY_STATE": "N/A with rationale: as above — except that it forbids a denial being rendered AS an empty state, which is the rule the screens then implement.",
    "ZERO_RESULT_STATE": "N/A with rationale: as above.",
    "ERROR_STATE": "The correlation reference is carried out of EVERY failure (`p1-28-qa.test.ts:487-502`), and an expired session is reported WITHOUT issuing a request (`:503-517`).",
    "DENIAL_STATE": "403 covered in both directions for reads and for writes, as above.",
    "CONFLICT_STATE": "409 `ERR-CON-001` is STALE and every other 409 is BLOCKED (`p1-28-qa.test.ts:561-589`) — one message covering ten codes was the P1-27 root-cause class this replaces.",
    "ARABIC_EVIDENCE": "N/A with rationale: this row asserts that a violation is surfaced as a translation KEY, which is what makes the Arabic rendering possible; the rendering itself is asserted per screen.",
    "ENGLISH_EVIDENCE": "As above.",
    "RTL_LTR_EVIDENCE": "N/A with rationale: direction is asserted per screen.",
    "ACCESSIBILITY_EVIDENCE": "N/A with rationale: no rendering of its own.",
    "PRODUCTION_REACHABILITY": "Driven through the real adapters rather than against fixtures of their responses, so a change in an adapter's mapping fails here.",
    "PRODUCTION_TEST_EVIDENCE": "`apps/web/tests/p1-28-qa.test.ts` — \"P1-28-QA-002 — every adapter through every transport kind\", \"the four refusal branches the contract names\", \"the three replay shapes\" and \"approve answers with the version, and it can be sent + 2\" (14 cases) — plus the per-screen conflict and denial cases in the `.dom` suites and `apps/web/tests/reception-closure.test.ts` \"the two 409s a guarded command can meet\".",
    "NEGATIVE_OR_MUTATION_PROOF": "428 is proved STRUCTURALLY IMPOSSIBLE — every guarded adapter takes the version as a REQUIRED argument (`p1-28-qa.test.ts:613-635`) — and a 428 arriving anyway is never reported as success (`:636-673`). `reception-closure.test.ts:146-155` fails CLOSED: an unrecognised conflict key is blocked, never stale. Collapse any two replay shapes and a named case fails.",
    "PROTECTED_REPROOF": "PENDING PROTECTED MERGE — RE-OBSERVED for this re-derivation at `a2318c36`: the web tier ran 2669 cases, 0 failed and 0 skipped (28 P1-28 suites, 760 cases), and the root CI tier ran 2475 cases, 0 failed and 0 skipped (5 P1-28 gate suites, 180 cases). THE BROWSER TIER NOW CARRIES THIS PHASE — `apps/web/tests/e2e/authenticated/appointments-and-receptions.spec.ts` (2478 lines, against the running application, the running API and the real database), which the previous derivation correctly recorded as absent. It does not cover this row: an error-path matrix needs the backend to PRODUCE each transport kind on demand, and the browser tier drives a real server that answers what it answers. What §18 does establish is the one thing a mock structurally cannot — that the SUCCESS paths behind these mappings really are the shapes the adapters expect, since a booking, a reschedule, a cancellation, a no-show, a create and an evidence write all completed against the real API. The refusal branches remain proved against mocked transport, and that is stated rather than implied. STILL NOT OBSERVED, and this is the whole outstanding debt on this row: that spec was NOT executed in this derivation — it needs the production launcher (`npm run acceptance:serve`), a live database and, for §18, a provisioned second workspace — and the run reported by the wave that landed it (319 expected, 0 unexpected, 0 flaky) is recorded in no phase document, so no browser result is bound to this head. THE TABLET VIEWPORT AND HOSTED CI WERE OBSERVED AT `38afa5c28e5b78d484a442cf6b8596fb2a5c34aa`, A HEAD THIS CANDIDATE SUPERSEDES. Two sentences stood here and both were false: that the `authenticated-tablet` project sees no P1-28 screen, and that no hosted-CI result exists for this head. `apps/web/playwright.config.ts:254-255` names that project and matches `(administration|appointments-and-receptions)`, so the phase surface is seen at 1024x768 as well as at 1440x900; run `31750364479` / job `94614564003` at head `38afa5c28e5b78d484a442cf6b8596fb2a5c34aa` executed `authenticated/appointments-and-receptions.spec.ts` 141 times — 47 in `authenticated-en`, 47 in `authenticated-ar`, 47 in `authenticated-tablet`, 0 failed and 0 skipped. Hosted CI at that head is run `31750364479`, 21 checks, 21 success, 0 failure, with `ci-gate` (`94617578352`) the single required check. The citation this cell used to give for the tablet claim pointed at lines 225 to 233 of the same config — the project DOCBLOCK, nine consecutive comment lines describing the state BEFORE the widening — so it read as confirmation of a sentence the code beneath it refutes; `validate:p1-28-evidence` now refuses a comment-only citation for exactly that reason. STILL OUTSTANDING ON THIS ROW: the protected-branch re-proof, because PR #226 has not merged, and OWNER ACCEPTANCE, which has not been requested or returned (`docs/phase-1/phase-1-28/evidence/change-log.md:5-11`). THAT HEAD IS NOT THE CURRENT CANDIDATE. Three fix waves changed 37 product files after `38afa5c28e5b78d484a442cf6b8596fb2a5c34aa`, so P1-28-QA-005 re-froze the candidate — at `6392ccb4321b004ed12e5d04ad583298da3303dd` then, and at `3c75f49a01e35b507461bf0929b0046e7140860a` now, the two being product-identical — and every hosted figure above is recorded in `docs/phase-1/phase-1-28/evidence/closure-candidate.json` as PENDING at that candidate — marked `describesSupersededHead`, citing the run it really came from, and listed in `pendingHostedBindings`. No hosted run has been taken at the current candidate yet, so no hosted result is bound to this head; `validate:p1-28-evidence` refuses a cell asserting an observation AT THIS CANDIDATE while the package records none.",
    "FINDING_IDS": "None open.",
    "DOCUMENTATION_EVIDENCE": "`canonical-plan.md` §5.3 (QA-002 scope, which names the three replay shapes); `contract-archaeology.md` §5 riskiest-fact 3.",
    "FINAL_VERDICT": "PASS",
    "VERDICT_RATIONALE": "Every branch the plan named is covered by an executable case driven through the real adapters: eleven transport kinds mapped for reads and for writes with a write never mapping to success, the reference carried out of every failure, 403 kept distinct from invalid input on writes and from emptiness on reads, the three 409 codes discriminated with a fail-closed default, 422 surfaced as a key rather than as server prose, and the three replay shapes asserted separately. 428 is additionally shown to be structurally unreachable rather than merely handled, which is a stronger claim than the plan asked for."
  },
  "QA-003": {
    "IMPLEMENTATION_SURFACES": "`apps/web/tests/p1-28-qa.test.ts:819-1062` (the one-door scope rule and its abuse cases) and `apps/web/tests/p1-28-permission-route-binding.test.ts` \"the reader-only negative control\" and \"the cross-tenant negative control this tier can actually run\" (the reader-only and cross-tenant negative controls at the route layer).",
    "NORMAL_NAVIGATION_ENTRY": "N/A with rationale: a test suite has no screen.",
    "BACKEND_READ_CONTRACTS": "Isolation semantics of every read consumed: `ERR-RES-001` 404 deliberately hides cross-tenant existence, and a cross-tenant read renders the SAME refusal a missing record does (`p1-28-qa.test.ts:1022-1047`), while a REFUSED read is discriminated as a denial (`p1-28-permission-route-binding.test.ts` \"discriminates: a REFUSED read is a denial, not a not-found\").",
    "BACKEND_WRITE_CONTRACTS": "Cross-tenant and cross-branch refusal applies to all 14 writes; the check-in coherence rule (422 `incoherent_reference` when the body scope differs from the appointment's own) is enforced client-side at the input builder, which takes scope and vehicle FROM the appointment (`reception-checkin-wizard.test.ts` \"appointment origin: vehicle and requester come from the APPOINTMENT, never the walk-in picks\").",
    "REQUEST_SCHEMA": "No request may carry a scope its operation may not be addressed by (`p1-28-qa.test.ts:897-908`); the set of requests that legitimately carry the pair is pinned (`:909-923`); a tenant is never sent at all (`:924-939`); every path parameter is encoded so an identifier cannot escape its segment (`:1048-1062`).",
    "RESPONSE_SCHEMA": "N/A with rationale: isolation is proven over the operations' own responses — the identical 404 for missing and for cross-tenant is the response-side fact.",
    "REQUIRED_PERMISSIONS": "The negative controls of SEC-001 are this row's fixtures: a read-only principal reaches every READ screen and is refused every write-gated one (`p1-28-permission-route-binding.test.ts` \"reaches every READ screen, and is refused every write-gated one\"), and a cross-tenant principal sees a not-found.",
    "OWNER_REQUIREMENT_MAPPING": "N/A with rationale: no Owner table row binds this task; tenant isolation is a platform obligation.",
    "TENANT_ISOLATION_REQUIREMENT": "This row IS the requirement, proved at two layers: the adapter layer (what may be sent) and the route layer (what a foreign or under-privileged principal sees).",
    "LOADING_STATE": "N/A with rationale: a suite renders nothing.",
    "EMPTY_STATE": "N/A with rationale: as above; the rule it enforces is that a denial must never be rendered as an empty list.",
    "ZERO_RESULT_STATE": "N/A with rationale: as above.",
    "ERROR_STATE": "N/A with rationale: error mapping is QA-002's subject.",
    "DENIAL_STATE": "Covered above, at the route layer, per route.",
    "CONFLICT_STATE": "N/A with rationale: no write of its own.",
    "ARABIC_EVIDENCE": "N/A with rationale: no copy of its own.",
    "ENGLISH_EVIDENCE": "As above.",
    "RTL_LTR_EVIDENCE": "N/A with rationale: no rendering.",
    "ACCESSIBILITY_EVIDENCE": "N/A with rationale: no rendering.",
    "PRODUCTION_REACHABILITY": "The route matrix is derived per route rather than hand-listed, and the reader-only sweep asserts it REACHES every read screen — so a control that refused everything would fail (`p1-28-permission-route-binding.test.ts` \"reaches every READ screen, and is refused every write-gated one\").",
    "PRODUCTION_TEST_EVIDENCE": "`apps/web/tests/p1-28-qa.test.ts:819-1062` (10 cases), `apps/web/tests/p1-28-permission-route-binding.test.ts` (the route matrix plus 5 negative-control cases), `apps/web/tests/p1-28-security.test.ts:497-760`.",
    "NEGATIVE_OR_MUTATION_PROOF": "The scope rule DERIVED a real constraint and names exactly four operations in this phase (`p1-28-qa.test.ts:871-896`), so a derivation that matched everything or nothing fails; no screen in either tree can resolve or widen its own scope, read from source (`:971-1021`); and the exemption is enforced at the ONE door rather than trusted to every call site (`:954-970`).",
    "PROTECTED_REPROOF": "PENDING PROTECTED MERGE — RE-OBSERVED for this re-derivation at `a2318c36`: the web tier ran 2669 cases, 0 failed and 0 skipped (28 P1-28 suites, 760 cases), and the root CI tier ran 2475 cases, 0 failed and 0 skipped (5 P1-28 gate suites, 180 cases). THE BROWSER TIER NOW CARRIES THIS PHASE — `apps/web/tests/e2e/authenticated/appointments-and-receptions.spec.ts` (2478 lines, against the running application, the running API and the real database), which the previous derivation correctly recorded as absent. It covers this row with the control this tier could never run: \"the P1-28 surface discloses nothing of another workspace\" asserts, against a live database under RLS, that no Tenant B record appears on any P1-28 route, that a Tenant B branch target widens neither board, and — driving the API directly — that a branch-scoped token is REFUSED both boards while an unrestricted token gets an EMPTY board rather than a foreign row, with a control proving the same token reads its OWN branch and a second control proving a missing account marker is not tolerated. \"nothing this phase reads reaches the address bar\" adds the request-shape half in a real browser. STILL NOT OBSERVED, and this is the whole outstanding debt on this row: that spec was NOT executed in this derivation — it needs the production launcher (`npm run acceptance:serve`), a live database and, for §18, a provisioned second workspace — and the run reported by the wave that landed it (319 expected, 0 unexpected, 0 flaky) is recorded in no phase document, so no browser result is bound to this head. THE TABLET VIEWPORT AND HOSTED CI WERE OBSERVED AT `38afa5c28e5b78d484a442cf6b8596fb2a5c34aa`, A HEAD THIS CANDIDATE SUPERSEDES. Two sentences stood here and both were false: that the `authenticated-tablet` project sees no P1-28 screen, and that no hosted-CI result exists for this head. `apps/web/playwright.config.ts:254-255` names that project and matches `(administration|appointments-and-receptions)`, so the phase surface is seen at 1024x768 as well as at 1440x900; run `31750364479` / job `94614564003` at head `38afa5c28e5b78d484a442cf6b8596fb2a5c34aa` executed `authenticated/appointments-and-receptions.spec.ts` 141 times — 47 in `authenticated-en`, 47 in `authenticated-ar`, 47 in `authenticated-tablet`, 0 failed and 0 skipped. Hosted CI at that head is run `31750364479`, 21 checks, 21 success, 0 failure, with `ci-gate` (`94617578352`) the single required check. The citation this cell used to give for the tablet claim pointed at lines 225 to 233 of the same config — the project DOCBLOCK, nine consecutive comment lines describing the state BEFORE the widening — so it read as confirmation of a sentence the code beneath it refutes; `validate:p1-28-evidence` now refuses a comment-only citation for exactly that reason. STILL OUTSTANDING ON THIS ROW: the protected-branch re-proof, because PR #226 has not merged, and OWNER ACCEPTANCE, which has not been requested or returned (`docs/phase-1/phase-1-28/evidence/change-log.md:5-11`). THAT HEAD IS NOT THE CURRENT CANDIDATE. Three fix waves changed 37 product files after `38afa5c28e5b78d484a442cf6b8596fb2a5c34aa`, so P1-28-QA-005 re-froze the candidate — at `6392ccb4321b004ed12e5d04ad583298da3303dd` then, and at `3c75f49a01e35b507461bf0929b0046e7140860a` now, the two being product-identical — and every hosted figure above is recorded in `docs/phase-1/phase-1-28/evidence/closure-candidate.json` as PENDING at that candidate — marked `describesSupersededHead`, citing the run it really came from, and listed in `pendingHostedBindings`. No hosted run has been taken at the current candidate yet, so no hosted result is bound to this head; `validate:p1-28-evidence` refuses a cell asserting an observation AT THIS CANDIDATE while the package records none.",
    "FINDING_IDS": "None open. The suite names its own tier limit — this is the cross-tenant control the UNIT tier can actually run (\"the cross-tenant negative control this tier can actually run\") — rather than claiming a database-level proof it does not perform. At this head that limit is answered from outside the row: the browser tier runs the database-level control against a live tenant pair.",
    "DOCUMENTATION_EVIDENCE": "`canonical-plan.md` §5.3; `contract-archaeology.md` §5 honourable mentions.",
    "FINAL_VERDICT": "PASS",
    "VERDICT_RATIONALE": "Isolation is proved at both layers this suite can reach and the proof is derived rather than asserted: the rule names exactly four operations that may carry a branch pair, no screen can resolve or widen its own scope, no tenant is ever sent, every path parameter is encoded, and a cross-tenant record renders identically to a missing one while a refused read is discriminated as a denial. The reader-only control is proved to REACH every read screen as well as to be refused every write, so it cannot pass by refusing everything. What the previous derivation recorded as this row’s honest ceiling — that a unit tier cannot prove database-level isolation — is now covered by the browser tier against a live RLS-enforced database and a second real tenant, at both the browser and the API."
  },
  "QA-004": {
    "IMPLEMENTATION_SURFACES": "`scripts/ci/check-p1-28-version-sourcing.mjs` (`validate:p1-28-version-sourcing`) plus the version cases of `apps/web/tests/p1-28-qa.test.ts`; the runtime rules live in `apps/web/src/features/receptions/check-in/closure.ts` (193 lines, `nextVersionAfter`) and in the wizard shell’s `refresh()`.",
    "NORMAL_NAVIGATION_ENTRY": "N/A with rationale: a gate and a suite have no screen.",
    "BACKEND_READ_CONTRACTS": "`rec.reception-detail` and `apt.appointment-detail` are the two reads that publish a version, and since R1/R2 they are what makes a resumed visit approvable and a bookmarked appointment reschedulable. The gate traces every guarded call site to a read or to a command response (`tests/ci/p1-28-version-sourcing.test.ts:166-178`).",
    "BACKEND_WRITE_CONTRACTS": "The version-guarded writes — reschedule, cancel, no-show, approve, convert, close-without-work and refuse — derived from the published contract rather than listed (`tests/ci/p1-28-version-sourcing.test.ts:150-157`), each with exactly one adapter demanding a REQUIRED version (`:158-165`).",
    "REQUEST_SCHEMA": "`If-Match` on every guarded write and on no unguarded one (`p1-28-qa.test.ts:1100-1119`), sent VERBATIM — no adapter adds, subtracts or defaults one (`:1120-1155`) — and never derived by arithmetic (`:1156-1165`).",
    "RESPONSE_SCHEMA": "Approve returns the version the command applied, which is sent+2 when it walked two edges and sent+1 when it walked one; the screen presents the ANSWER from one code path (`reception-summary.dom.test.tsx:363-406`) and `reception-closure.test.ts:156-171` refuses to invent a version the backend did not state.",
    "REQUIRED_PERMISSIONS": "N/A with rationale: a gate and a suite hold no permission.",
    "OWNER_REQUIREMENT_MAPPING": "The Owner resumability mandate (`owner-workflow-requirements.md:182-197`). It is discharged rather than merely spoken to: R1/R2 publish the version, so an operator arriving by URL can act, and the discipline survives the change — cache what a command returns, re-read when you arrive by URL.",
    "TENANT_ISOLATION_REQUIREMENT": "N/A with rationale: isolation is QA-003's subject.",
    "LOADING_STATE": "N/A with rationale: no rendering of its own.",
    "EMPTY_STATE": "N/A with rationale: as above. The gate's anti-vacuity cases stand in its place — it FAILS when zero files are scanned, when the contract derives no guarded operation, when the adapter walk finds nothing and when no call site exists anywhere (`tests/ci/p1-28-version-sourcing.test.ts:354-386`).",
    "ZERO_RESULT_STATE": "N/A with rationale: no list.",
    "ERROR_STATE": "N/A with rationale: error mapping is QA-002's subject.",
    "DENIAL_STATE": "N/A with rationale: no permission surface.",
    "CONFLICT_STATE": "The two 409s are told apart by `conflictKindOf` with a fail-closed default (`reception-closure.test.ts:138-155`), and every screen re-reads after a conflict as well as after a success.",
    "ARABIC_EVIDENCE": "N/A with rationale: no copy of its own.",
    "ENGLISH_EVIDENCE": "As above.",
    "RTL_LTR_EVIDENCE": "N/A with rationale: no rendering.",
    "ACCESSIBILITY_EVIDENCE": "N/A with rationale: no rendering.",
    "PRODUCTION_REACHABILITY": "`validate:p1-28-version-sourcing` is `required` in the command register and named DIRECTLY by a hosted job (`.github/workflows/_reusable-node-quality.yml:624`).",
    "PRODUCTION_TEST_EVIDENCE": "`tests/ci/p1-28-version-sourcing.test.ts` (44 cases; green on the CURRENT tree), the version cases of `apps/web/tests/p1-28-qa.test.ts` (\"P1-28-QA-004 — idempotency is read off the contract, never off the verb\" and \"where the record version comes from\"), and `apps/web/tests/reception-closure.test.ts` \"the two 409s a guarded command can meet\" plus \"the version after approve\".",
    "NEGATIVE_OR_MUTATION_PROOF": "The gate refuses sent+1, a bare number, arithmetic hidden one hop away inside a const, and a version laundered through `Number()` (`tests/ci/p1-28-version-sourcing.test.ts:195-236`); it refuses a bare `useState` value — the cached guess itself — an identifier bound by nothing, and a member access that is not a `recordVersion` (`:237-277`); it refuses a component that commands and never renews, and does NOT accept writing the answer into local state as renewal (`:278-311`); and it refuses an optional or defaulted `ifMatch` and an adapter that takes a version and drops it (`:312-353`). `p1-28-qa.test.ts:1166-1180` asserts the discipline is enforced by a GATE and not only by these cases.",
    "PROTECTED_REPROOF": "PENDING PROTECTED MERGE — RE-OBSERVED for this re-derivation at `a2318c36`: the web tier ran 2669 cases, 0 failed and 0 skipped (28 P1-28 suites, 760 cases), and the root CI tier ran 2475 cases, 0 failed and 0 skipped (5 P1-28 gate suites, 180 cases). THE BROWSER TIER NOW CARRIES THIS PHASE — `apps/web/tests/e2e/authenticated/appointments-and-receptions.spec.ts` (2478 lines, against the running application, the running API and the real database), which the previous derivation correctly recorded as absent. It covers this row at §18, where a version-guarded command is taken against the real API for the first time in this phase: \"an appointment is booked, confirmed by rescheduling, and cancelled with a catalogued reason\" reschedules and then cancels the same record, so the `If-Match` the screen sent came from the read the page performed and the server accepted it — the sourcing discipline observed rather than only enforced. Approve, convert and the two terminal exits are NOT observed there, and that is stated rather than implied. STILL NOT OBSERVED, and this is the whole outstanding debt on this row: that spec was NOT executed in this derivation — it needs the production launcher (`npm run acceptance:serve`), a live database and, for §18, a provisioned second workspace — and the run reported by the wave that landed it (319 expected, 0 unexpected, 0 flaky) is recorded in no phase document, so no browser result is bound to this head. THE TABLET VIEWPORT AND HOSTED CI WERE OBSERVED AT `38afa5c28e5b78d484a442cf6b8596fb2a5c34aa`, A HEAD THIS CANDIDATE SUPERSEDES. Two sentences stood here and both were false: that the `authenticated-tablet` project sees no P1-28 screen, and that no hosted-CI result exists for this head. `apps/web/playwright.config.ts:254-255` names that project and matches `(administration|appointments-and-receptions)`, so the phase surface is seen at 1024x768 as well as at 1440x900; run `31750364479` / job `94614564003` at head `38afa5c28e5b78d484a442cf6b8596fb2a5c34aa` executed `authenticated/appointments-and-receptions.spec.ts` 141 times — 47 in `authenticated-en`, 47 in `authenticated-ar`, 47 in `authenticated-tablet`, 0 failed and 0 skipped. Hosted CI at that head is run `31750364479`, 21 checks, 21 success, 0 failure, with `ci-gate` (`94617578352`) the single required check. The citation this cell used to give for the tablet claim pointed at lines 225 to 233 of the same config — the project DOCBLOCK, nine consecutive comment lines describing the state BEFORE the widening — so it read as confirmation of a sentence the code beneath it refutes; `validate:p1-28-evidence` now refuses a comment-only citation for exactly that reason. STILL OUTSTANDING ON THIS ROW: the protected-branch re-proof, because PR #226 has not merged, and OWNER ACCEPTANCE, which has not been requested or returned (`docs/phase-1/phase-1-28/evidence/change-log.md:5-11`). THAT HEAD IS NOT THE CURRENT CANDIDATE. Three fix waves changed 37 product files after `38afa5c28e5b78d484a442cf6b8596fb2a5c34aa`, so P1-28-QA-005 re-froze the candidate — at `6392ccb4321b004ed12e5d04ad583298da3303dd` then, and at `3c75f49a01e35b507461bf0929b0046e7140860a` now, the two being product-identical — and every hosted figure above is recorded in `docs/phase-1/phase-1-28/evidence/closure-candidate.json` as PENDING at that candidate — marked `describesSupersededHead`, citing the run it really came from, and listed in `pendingHostedBindings`. No hosted run has been taken at the current candidate yet, so no hosted result is bound to this head; `validate:p1-28-evidence` refuses a cell asserting an observation AT THIS CANDIDATE while the package records none.",
    "FINDING_IDS": "None open. The day-one concern — that until R1/R2 landed there was no read to refresh a stale version from — is closed by those remediations.",
    "DOCUMENTATION_EVIDENCE": "`canonical-plan.md` §5.3 (QA-004 scope); `contract-archaeology.md` §5 riskiest-fact 2.",
    "FINAL_VERDICT": "PASS",
    "VERDICT_RATIONALE": "The sourcing discipline the plan asked for is not a convention here, it is a gate: guarded operations are derived from the published contract, each has one adapter demanding a required version, every call site is traced to a read or a command response, and the four ways a version can be wrong — computed, cached, untraceable, or weakened at the adapter — each have a refusing case. Four anti-vacuity cases prove the gate examines something. At the runtime layer, `If-Match` is present on every guarded write and absent from every unguarded one, the version travels verbatim, and 428 is structurally unreachable because the argument is required."
  },
  "QA-005": {
    "IMPLEMENTATION_SURFACES": "`scripts/ci/build-p1-28-evidence-manifest.mjs` (`evidence:p1-28` writes, `validate:p1-28-evidence` checks), sealing every file in the phase directory over its BYTES; `docs/phase-1/phase-1-28/evidence/evidence-manifest.json` (the seal, 14 documents); `docs/phase-1/phase-1-28/evidence/closure-candidate.json` (the machine-readable candidate, tiers, run/job ids, pending-binding list and blocked-task record); `docs/phase-1/phase-1-28/evidence/closure-evidence.md` (the prose half the Owner reads). Proved by `tests/ci/p1-28-evidence-manifest.test.ts` (61 cases).",
    "NORMAL_NAVIGATION_ENTRY": "N/A with rationale: evidence packaging has no screen.",
    "BACKEND_READ_CONTRACTS": "N/A with rationale: evidence packaging reads the repository and the running application, not business contracts.",
    "BACKEND_WRITE_CONTRACTS": "N/A with rationale: no mutation.",
    "REQUEST_SCHEMA": "N/A with rationale: no operation of its own.",
    "RESPONSE_SCHEMA": "N/A with rationale: no operation of its own.",
    "REQUIRED_PERMISSIONS": "N/A with rationale: the Owner-acceptance accounts are provisioned by the acceptance tooling (`scripts/dev/owner-acceptance/**`), not by this row.",
    "OWNER_REQUIREMENT_MAPPING": "The permanent Frontend closure rule (plan §8): the phase closes ONLY on an explicit `OWNER ACCEPTANCE: PASS` against the running application, and silence is never Pass. The resumability blocker (`owner-workflow-requirements.md:182-197`) IS discharged by R1, so acceptance can now be requested without a waiver — but it has not been.",
    "TENANT_ISOLATION_REQUIREMENT": "N/A with rationale: no runtime behaviour of its own.",
    "LOADING_STATE": "N/A with rationale: no rendering.",
    "EMPTY_STATE": "N/A with rationale: no rendering.",
    "ZERO_RESULT_STATE": "N/A with rationale: no rendering.",
    "ERROR_STATE": "N/A with rationale: no rendering.",
    "DENIAL_STATE": "N/A with rationale: no permission surface.",
    "CONFLICT_STATE": "N/A with rationale: no write.",
    "ARABIC_EVIDENCE": "N/A with rationale: the phase record is English by convention; the product copy it will cite is asserted per screen in both catalogues.",
    "ENGLISH_EVIDENCE": "As above.",
    "RTL_LTR_EVIDENCE": "N/A with rationale: no rendering.",
    "ACCESSIBILITY_EVIDENCE": "N/A with rationale: no rendering.",
    "PRODUCTION_REACHABILITY": "N/A with rationale: not a product surface.",
    "PRODUCTION_TEST_EVIDENCE": "THE CANDIDATE HAS BEEN RE-FROZEN TWICE AND IS NOW `3c75f49a01e35b507461bf0929b0046e7140860a` (tree `f1f92927f93e4fd55d47fcc475d9311b21cf79f0`), and this cell states which tiers were re-measured against it and which were not. The first move was forced: three fix waves changed 37 product files after `38afa5c2` and the gate refused the package. The second was deliberate and cost nothing: `git diff --name-only 6392ccb4..34b3fca5706ea037c46f4a1d16f5dfe2c4d194b1 -- apps supabase` is EMPTY, so only the seal machinery had moved, and re-freezing at the head stops each seal repair forcing another reseal. MEASURED AT THE CANDIDATE, locally, and COMPUTED against `docs/phase-1/phase-1-27/evidence/local-run-ledger.json`: root unit 2559/2559 over 98 files, 0 failed; web 2726/2726 over 98 files, 0 failed. Both were taken AT THE CANDIDATE ITSELF, so neither declares `measurementDrift` — and the gate refuses that field when nothing differs, so its absence is checked rather than assumed. The successor list is empty for the same reason: the candidate is the newest commit touching an executable path. NOT MEASURED AT THE CANDIDATE, and recorded PENDING rather than restated: backend integration, database and RLS, and the authenticated browser tier. Their figures in the package (2004, 1647, and 370 planned / 366 passed) come from run 31750364479 at `38afa5c28e5b78d484a442cf6b8596fb2a5c34aa`, a head this candidate supersedes; each is marked `describesSupersededHead`, cites the run and job it really came from, and is listed with eight other hosted bindings in `pendingHostedBindings`. The backend and database tiers need a running PostgreSQL this workstation does not host and the browser tier needs the full acceptance stack, so none of the three is takeable here at any candidate. Nine thousand-odd automated cases have been executed on this branch and the honest total AT THIS CANDIDATE is the two local tiers: 5285 cases, 0 failures. This row’s own gate is covered by `tests/ci/p1-28-evidence-manifest.test.ts` (78 cases), and by the gate’s own `selfCheck`, which drives 72 known-bad worlds on every invocation before it looks at the tree.",
    "NEGATIVE_OR_MUTATION_PROOF": "The seal is proved to be capable of failing, not asserted to be — and on this cycle it proved it against its own package. Every rule fires in exactly one function (`judge`) and the gate drives that function over 56 known-bad worlds on EVERY invocation, before reading the tree, so a stubbed rule turns its own known-bad case green, which is itself the failure. THE PROOF THAT MATTERS THIS TIME IS NOT SYNTHETIC: the gate COMPUTED `git diff --name-only 38afa5c2..HEAD -- apps supabase`, found 37 product files, and refused the package that had been assembled around the old candidate — five `tests/ci` cases red for that one reason, which is the whole purpose of the rule. Independent proofs in `tests/ci/p1-28-evidence-manifest.test.ts`: a single edited byte in a packaged document turns the check red (“the seal, proved”); a digest taken over the file PATH instead of its bytes is caught by an oracle that never calls `digest()`, because verifying a hash with the function that produced it is f(x) === f(x); deleting a cited document is caught by the dangling-citation half, which a count floor survives; an uncited document added to the tree is caught by the orphan half; a candidate the two halves of the package disagree about is refused; a fourth task flipped to PARTIAL with the package unchanged fails until both halves name it; a superseded head that names no commit in this repository is refused; a pending marker on a binding that is in fact bound to the candidate is refused; a tier claiming LOCAL_AND_HOSTED_AGREE while its hosted half is superseded is refused; and a fabricated `measurementDrift` list is refused against what `git diff` computes. Every mutation runs against a temporary-directory fixture or a synthetic repository, and each asserts the intact input is sound BEFORE mutating — otherwise the proof would be two empty sets agreeing.",
    "PROTECTED_REPROOF": "PENDING PROTECTED MERGE, AND NOW ALSO PENDING AN EXACT-HEAD RUN. The candidate observation this cell used to carry EXISTED and was bound — hosted CI at `38afa5c28e5b78d484a442cf6b8596fb2a5c34aa` is run `31750364479` (PR CI, `pull_request`, attempt 1) with 21 checks, 21 success, 0 failure, `ci-gate` (`94617578352`) the single required check; the authenticated browser job `94614564003` executed `authenticated/appointments-and-receptions.spec.ts` 141 times, 47 in each of `authenticated-en`, `authenticated-ar` and `authenticated-tablet`, 0 failed and 0 skipped; CodeQL `1616577501` (javascript-typescript, 201 rules) and `1616569830` (actions, 27 rules) both returned 0 results with 0 open alerts repository-wide, recorded as the DIFF-INFORMED pull-request result it is; dependency security 0 production and 0 development vulnerabilities across 830 dependencies; production build ok, 239 routes. EVERY ONE OF THOSE DESCRIBES `38afa5c28e5b78d484a442cf6b8596fb2a5c34aa`, WHICH IS NOT THIS CANDIDATE. Three fix waves changed 37 files under apps/** afterwards, so the package re-froze at `6392ccb4321b004ed12e5d04ad583298da3303dd`, and later at the product-identical `3c75f49a01e35b507461bf0929b0046e7140860a`, and recorded all eleven hosted bindings PENDING there rather than carrying the figures across. The tablet observation is the one thing that partly survives, and only in half: `apps/web/playwright.config.ts:254-260` still matches `(administration|appointments-and-receptions)` in the `authenticated-tablet` project, which is a fact about the CONFIG at this candidate; the 47 executed tablet cases are a fact about that RUN and are not. What genuinely cannot exist yet is therefore threefold: the protected-branch re-proof, because PR #226 has not merged; a hosted run at this candidate, which the phase coordinator takes; and OWNER ACCEPTANCE, which belongs to the Product Owner.",
    "FINDING_IDS": "This row’s own four gaps: (1) a packaged, sealed evidence set exists with immutability machinery proved able to fail — CLOSED; (2) a browser-tier result RECORDED and bound to a named commit, per project — recorded, and now bound to `38afa5c28e5b78d484a442cf6b8596fb2a5c34aa` rather than to the candidate, so it is PENDING again after the re-freeze and the package says so in `pendingHostedBindings` rather than restating it; (3) a hosted-CI result for this head — same state, same reason, same declaration; (4) OWNER ACCEPTANCE remains outstanding and is NOT a gap in this row’s deliverable — it is the phase-closure condition and belongs to the Product Owner. TWO CORRECTIONS MADE THIS CYCLE, both to records rather than to product code. Thirty-three `PROTECTED_REPROOF` cells asserted, in the present tense, that hosted CI and the tablet viewport had been observed at whatever the current candidate was; that was true when written and became an over-claim the moment the candidate moved, so each now names the head it really describes and states that no run has been taken at the current one. And `.github/ci-baselines/unrun-test-tiers.json` run (6) is re-stated as describing a superseded head, with the next run to be recorded there as (7). The gate was extended to refuse both classes: the anchored claims previously banned only sentences DENYING evidence that exists, which is one direction of a two-directional failure.",
    "DOCUMENTATION_EVIDENCE": "`docs/phase-1/phase-1-28/evidence/closure-evidence.md` (the package), `docs/phase-1/phase-1-28/evidence/closure-candidate.json` (its machine-readable half), `docs/phase-1/phase-1-28/evidence/evidence-manifest.json` (the seal), `canonical-plan.md` §5.3 and §8, and `evidence/change-log.md`, which continues to state that the phase is OPEN and that this is not a closure record.",
    "FINAL_VERDICT": "PASS",
    "VERDICT_RATIONALE": "DONE AS A DELIVERABLE, AND EXPLICITLY INCOMPLETE AS A MEASUREMENT — both stated here rather than one of them left to be inferred. RULE APPLIED, identically on every row of this file: a task is graded against THE CAPABILITY IT CANONICALLY OWNS — its §5 binding in `canonical-plan.md` — and against nothing else. This row owns “regression and immutable evidence packaging”. WHAT IS DELIVERED: a candidate is frozen (`3c75f49a01e35b507461bf0929b0046e7140860a`, tree `f1f92927f93e4fd55d47fcc475d9311b21cf79f0`) and BOUND TO THE REPOSITORY — the gate asks git whether it exists, whether that commit has that tree, whether it is an ancestor of HEAD, whether any file under apps/** or supabase/** differs from HEAD, and whether every executable successor is named; the two local tiers are re-measured against it and COMPUTED against a run ledger read out of git at a pinned commit; the evidence set is sealed by a gate PROVED able to fail rather than asserted to be, and it refused this very package until the candidate was re-frozen. WHAT IS NOT MEASURED, and is the reason this row does not claim a complete regression: no hosted observation of this candidate exists. Eleven hosted bindings — three tiers, hosted CI, CodeQL, the dependency audit, the production build, the database replay and the per-project browser record — are recorded PENDING, each naming the superseded head its figures really come from, each listed in `pendingHostedBindings`, and none of them takeable on this workstation. The phase coordinator takes that run; until it is bound, a reader of this package knows what two tiers did at this candidate and what five tiers did at a head it supersedes, and the package does not blur the two. THE OTHER THINGS THIS ROW DOES NOT CLAIM, stated because a package is judged on what it omits: the protected-branch re-proof cannot exist before the merge; the three PARTIAL rows (`FE-007` on `G-EMP`/R6, `FE-012` and `FE-018` on `P1-OD-025`) are blocked on OPEN OWNER DECISIONS this phase is forbidden to pre-empt and the package names each with its owner; and OWNER ACCEPTANCE has not been requested or returned. The seal’s blocker rule DERIVES the unclosed set from this very file on every run, so if any of those three rows changes, or a fourth stops closing, the gate fails until the package says so. OWNER: this phase’s coordinator for the packaging (delivered) and for the exact-head run (outstanding); the Product Owner for acceptance (outstanding)."
  },
  "DO-001": {
    "IMPLEMENTATION_SURFACES": "Five gates registered `required` — `validate:p1-28-matrix`, `validate:p1-28-write-reachability`, `validate:p1-28-access`, `validate:p1-28-version-sourcing` and, added at this head, `validate:p1-28-traceability` — all reachable from `verify:policies` and each named DIRECTLY by a hosted job in `.github/workflows/_reusable-node-quality.yml`. Gate-metadata co-maintenance lives in `scripts/ci/check-p1-27-frontend.mjs` (`ADOPTED_ROOTS`, `SCAN_ROOTS`, `MODULE_DISPOSITION`).",
    "NORMAL_NAVIGATION_ENTRY": "N/A with rationale: CI wiring has no screen.",
    "BACKEND_READ_CONTRACTS": "N/A with rationale: gate wiring reads the repository, the command register and the workflow files, not business contracts.",
    "BACKEND_WRITE_CONTRACTS": "N/A with rationale: no mutation.",
    "REQUEST_SCHEMA": "N/A with rationale: no operation of its own.",
    "RESPONSE_SCHEMA": "N/A with rationale: no operation of its own.",
    "REQUIRED_PERMISSIONS": "N/A with rationale: a CI gate holds no permission.",
    "OWNER_REQUIREMENT_MAPPING": "N/A with rationale: no Owner table row binds this task; it keeps the phase's own record honest, which is what the Owner's acceptance ultimately reads.",
    "TENANT_ISOLATION_REQUIREMENT": "N/A with rationale: no runtime behaviour.",
    "LOADING_STATE": "N/A with rationale: no rendering.",
    "EMPTY_STATE": "N/A with rationale: no rendering. The derivation's own floor stands in its place — the P1-28 gate set must derive at least four entries, and it must NOT sweep in the generator, which is registered `informational` (`tests/ci/p1-28-devops-gate.test.ts` \"derives the set from the register rather than from a list somebody keeps\").",
    "ZERO_RESULT_STATE": "N/A with rationale: no list.",
    "ERROR_STATE": "N/A with rationale: no rendering.",
    "DENIAL_STATE": "N/A with rationale: no permission surface.",
    "CONFLICT_STATE": "N/A with rationale: no write.",
    "ARABIC_EVIDENCE": "N/A with rationale: CI output is developer-facing.",
    "ENGLISH_EVIDENCE": "As above.",
    "RTL_LTR_EVIDENCE": "N/A with rationale: no rendering.",
    "ACCESSIBILITY_EVIDENCE": "N/A with rationale: no rendering.",
    "PRODUCTION_REACHABILITY": "The trace ends at the required-check list rather than at 'a workflow mentions it': exactly one required check is read from the ruleset document, `ci-gate` is made to wait for every job that runs a P1-28 gate, and a skip of any of them is refused (`tests/ci/p1-28-devops-gate.test.ts` \"the jobs that run them are required, traced to the required-check list\"), with the clean-room aggregate kept as a second independent path (\"keeps the clean-room aggregate as the second, independent path\").",
    "PRODUCTION_TEST_EVIDENCE": "`tests/ci/p1-28-devops-gate.test.ts` (13 cases). Green at this head; all five gates report clean locally (`validate:p1-28-matrix` in sync, `validate:p1-28-write-reachability` zero violations, `validate:p1-28-access` clean over the eight routes, `validate:p1-28-version-sourcing` zero violations, `validate:p1-28-traceability` 27 derived claims across 7 documents with 0 disagreements).",
    "NEGATIVE_OR_MUTATION_PROOF": "The workflow derivation is FALSIFIABLE — deleting the line makes the derivation say so (`tests/ci/p1-28-devops-gate.test.ts` \"is falsifiable — deleting the line makes the derivation say so\") — and the tier is read off the register rather than assumed from the name, because `informational` is how `validate:phase-ownership` spent two phases unenforced (\"registers each one `required`, so BOTH halves of coverage are enforced\"). The co-maintenance rule is enforced as an EQUALITY against plan §9, which is what caught `features/appointments` being adopted twice when two waves merged (\"adopts every P1-28 tree the plan declares and that no plan root already holds\"), and every module the adopted trees newly import must carry a `MODULE_DISPOSITION` decision (\"decides every module the adopted trees newly import\").",
    "PROTECTED_REPROOF": "PENDING PROTECTED MERGE — RE-OBSERVED for this re-derivation at `a2318c36`: the root CI tier ran 2475 cases, 0 failed and 0 skipped, and all five P1-28 gates report clean locally. The browser tier is not this row’s evidence — CI wiring has nothing for a browser to observe. OBSERVED AT `38afa5c28e5b78d484a442cf6b8596fb2a5c34aa`, A HEAD THIS CANDIDATE SUPERSEDES. This cell used to deny that any hosted-CI result existed for this head and to conclude that the jobs naming these gates had therefore not been seen to run at it; both halves were false: run `31750364479` at `38afa5c28e5b78d484a442cf6b8596fb2a5c34aa` is 21 checks, 21 success, 0 failure, and `static-quality` (`94614564036`) is among them. STILL OUTSTANDING: the protected-branch re-proof, because PR #226 has not merged, and OWNER ACCEPTANCE, which has not been requested or returned (`docs/phase-1/phase-1-28/evidence/change-log.md:5-11`). THAT HEAD IS NOT THE CURRENT CANDIDATE. Three fix waves changed 37 product files after `38afa5c28e5b78d484a442cf6b8596fb2a5c34aa`, so P1-28-QA-005 re-froze the candidate — at `6392ccb4321b004ed12e5d04ad583298da3303dd` then, and at `3c75f49a01e35b507461bf0929b0046e7140860a` now, the two being product-identical — and every hosted figure above is recorded in `docs/phase-1/phase-1-28/evidence/closure-candidate.json` as PENDING at that candidate — marked `describesSupersededHead`, citing the run it really came from, and listed in `pendingHostedBindings`. No hosted run has been taken at the current candidate yet, so no hosted result is bound to this head; `validate:p1-28-evidence` refuses a cell asserting an observation AT THIS CANDIDATE while the package records none.",
    "FINDING_IDS": "None open. The day-one outstanding item — `MODULE_DISPOSITION` entries and scan-root decisions for feature trees that did not yet exist — is discharged, and the co-maintenance rule proved itself again at this head: `validate:p1-28-traceability` was added as a fifth required gate and the DevOps derivation picked it up without a hand edit, because the set is read from the command register rather than kept in a list.",
    "DOCUMENTATION_EVIDENCE": "`canonical-plan.md` §5.3 (DO-001 same-pull-request rule) and §9; `evidence/change-log.md` 'Security, QA and DevOps riders'.",
    "FINAL_VERDICT": "PASS",
    "VERDICT_RATIONALE": "The gate set is DERIVED from the command register rather than from a list somebody keeps — which is what let two of the four be invisible while it was hand-written — every entry is registered `required` with a reason, every one is reachable from the aggregate a developer runs and is ALSO named directly by a hosted job, and the trace ends at `ci-gate`, the single required check, with skips refused. The co-maintenance obligation is enforced as an equality against plan §9 and by requiring a disposition for every newly imported module, and the day-one outstanding item is discharged rather than deferred. The workflow derivation carries a falsifiability case, so a green result cannot come from a search that matches nothing."
  },
  "DO-002": {
    "IMPLEMENTATION_SURFACES": "`apps/web/tests/p1-28-observability.test.ts` (547 lines) over both feature trees and all three P1-28 route trees; the states it measures are `apps/web/src/components/states/States.tsx:137` (`ErrorState`) and `:192` (`BackendUnavailableState`); the error boundary is the `(dashboard)` group’s own `error.tsx`.",
    "NORMAL_NAVIGATION_ENTRY": "N/A with rationale: observability wiring has no screen of its own; its subject is every P1-28 screen.",
    "BACKEND_READ_CONTRACTS": "N/A with rationale: pipeline and logging wiring reads workflow and configuration files and the web source, not business contracts.",
    "BACKEND_WRITE_CONTRACTS": "N/A with rationale: no mutation. If a remediation adds an operation, `lib/api/idempotent-operations.ts` is REGENERATED, never hand-edited — which is why the R1-R5 additions required no hand edit here.",
    "REQUEST_SCHEMA": "N/A with rationale: no operation of its own. What it governs is what LEAVES the browser: every record identifier is templated out of a P1-28 route before it is reported (`p1-28-observability.test.ts:494-508`), and the walk-in handoff parameters are kept out of the reported route (`:509-517`).",
    "RESPONSE_SCHEMA": "N/A with rationale: no operation of its own. The correlation reference from a failure response is the thing it tracks.",
    "REQUIRED_PERMISSIONS": "N/A with rationale: a pipeline holds no permission.",
    "OWNER_REQUIREMENT_MAPPING": "N/A with rationale: no Owner table row binds this task. Its user-visible product is that an operator holding a failure always has one identifier an administrator can chase.",
    "TENANT_ISOLATION_REQUIREMENT": "Nothing that leaves the browser names a reception or an appointment (`p1-28-observability.test.ts:479-526`), so a log line cannot become a cross-tenant disclosure channel.",
    "LOADING_STATE": "N/A with rationale: this row measures failure surfaces, not loading.",
    "EMPTY_STATE": "N/A with rationale: as above.",
    "ZERO_RESULT_STATE": "N/A with rationale: as above.",
    "ERROR_STATE": "The reference is carried on EVERY `ErrorState` and `BackendUnavailableState` across both feature trees and all three route trees (`p1-28-observability.test.ts:377-388`), and every governed state is found with each kind actually represented (`:366-376`).",
    "DENIAL_STATE": "The reference is carried on a backend denial and NEVER invented on a client-side gate (`p1-28-observability.test.ts:389-414`) — a fabricated reference is worse than none, because it sends an administrator to a request that never happened.",
    "CONFLICT_STATE": "Nothing is printed on an ended session or a missing record (`p1-28-observability.test.ts:415-429`), which is the same rule from the other side.",
    "ARABIC_EVIDENCE": "N/A with rationale: the reference is an identifier, rendered LTR in both directions by the shared state components, which carry Arabic (`p1-28-shared-components.dom.test.tsx:456-472`).",
    "ENGLISH_EVIDENCE": "As above.",
    "RTL_LTR_EVIDENCE": "As above.",
    "ACCESSIBILITY_EVIDENCE": "N/A with rationale: no rendering of its own; the shared states it measures are asserted for role and reference by `p1-28-shared-components.dom.test.tsx:119-190`.",
    "PRODUCTION_REACHABILITY": "All EIGHT P1-28 pages sit inside the `(dashboard)` group that owns `error.tsx`, so a render failure is reported rather than blank (`p1-28-observability.test.ts:527-546`). The navigation `status` flips followed G-PERM and are pinned as an equality in both directions (`apps/web/tests/navigation.test.ts:44-111`).",
    "PRODUCTION_TEST_EVIDENCE": "`apps/web/tests/p1-28-observability.test.ts` (12 cases). Green at this head.",
    "NEGATIVE_OR_MUTATION_PROOF": "The classifier is proved against sources built by hand — it tells the four verdicts apart and cannot be fooled by prose, and reads a conditional spread as passing the reference while an unrelated one is not (`p1-28-observability.test.ts:430-478`). The route templating is FALSIFIABLE: an untemplated route really does keep the identifier (`:518-526`). The sweep is proved to take its roots from the plan sentence and to read all three, and to reach the parts of the `(dashboard)` group the three segments excluded (`:321-365`).",
    "PROTECTED_REPROOF": "PENDING PROTECTED MERGE — RE-OBSERVED for this re-derivation at `a2318c36`: the web tier ran 2669 cases, 0 failed and 0 skipped (28 P1-28 suites, 760 cases), and the root CI tier ran 2475 cases, 0 failed and 0 skipped (5 P1-28 gate suites, 180 cases). THE BROWSER TIER NOW CARRIES THIS PHASE — `apps/web/tests/e2e/authenticated/appointments-and-receptions.spec.ts` (2478 lines, against the running application, the running API and the real database), which the previous derivation correctly recorded as absent. It covers this row at \"every P1-28 route is reachable and renders\" — the eight screens load in the running application without an error state — and at \"no P1-28 screen renders a raw message key\", which is the failure a structural sweep of source cannot see. \"nothing this phase reads reaches the address bar\" checks the identifier-templating rule against the URLs a real browser actually navigates to. STILL NOT OBSERVED, and this is the whole outstanding debt on this row: that spec was NOT executed in this derivation — it needs the production launcher (`npm run acceptance:serve`), a live database and, for §18, a provisioned second workspace — and the run reported by the wave that landed it (319 expected, 0 unexpected, 0 flaky) is recorded in no phase document, so no browser result is bound to this head. THE TABLET VIEWPORT AND HOSTED CI WERE OBSERVED AT `38afa5c28e5b78d484a442cf6b8596fb2a5c34aa`, A HEAD THIS CANDIDATE SUPERSEDES. Two sentences stood here and both were false: that the `authenticated-tablet` project sees no P1-28 screen, and that no hosted-CI result exists for this head. `apps/web/playwright.config.ts:254-255` names that project and matches `(administration|appointments-and-receptions)`, so the phase surface is seen at 1024x768 as well as at 1440x900; run `31750364479` / job `94614564003` at head `38afa5c28e5b78d484a442cf6b8596fb2a5c34aa` executed `authenticated/appointments-and-receptions.spec.ts` 141 times — 47 in `authenticated-en`, 47 in `authenticated-ar`, 47 in `authenticated-tablet`, 0 failed and 0 skipped. Hosted CI at that head is run `31750364479`, 21 checks, 21 success, 0 failure, with `ci-gate` (`94617578352`) the single required check. The citation this cell used to give for the tablet claim pointed at lines 225 to 233 of the same config — the project DOCBLOCK, nine consecutive comment lines describing the state BEFORE the widening — so it read as confirmation of a sentence the code beneath it refutes; `validate:p1-28-evidence` now refuses a comment-only citation for exactly that reason. STILL OUTSTANDING ON THIS ROW: the protected-branch re-proof, because PR #226 has not merged, and OWNER ACCEPTANCE, which has not been requested or returned (`docs/phase-1/phase-1-28/evidence/change-log.md:5-11`). THAT HEAD IS NOT THE CURRENT CANDIDATE. Three fix waves changed 37 product files after `38afa5c28e5b78d484a442cf6b8596fb2a5c34aa`, so P1-28-QA-005 re-froze the candidate — at `6392ccb4321b004ed12e5d04ad583298da3303dd` then, and at `3c75f49a01e35b507461bf0929b0046e7140860a` now, the two being product-identical — and every hosted figure above is recorded in `docs/phase-1/phase-1-28/evidence/closure-candidate.json` as PENDING at that candidate — marked `describesSupersededHead`, citing the run it really came from, and listed in `pendingHostedBindings`. No hosted run has been taken at the current candidate yet, so no hosted result is bound to this head; `validate:p1-28-evidence` refuses a cell asserting an observation AT THIS CANDIDATE while the package records none.",
    "FINDING_IDS": "None open. The day-one standing obligations are discharged: new vitest suites run through the web tier that `web-quality` executes, the navigation flips followed G-PERM and are pinned, and no new toast, storage or fetch surface was introduced (held by `validate:notification-authority` and `validate:web-boundary`).",
    "DOCUMENTATION_EVIDENCE": "`canonical-plan.md` §5.3 and §3 (the prohibition list); `evidence/change-log.md` 'P1-28-DO-002'.",
    "FINAL_VERDICT": "PASS",
    "VERDICT_RATIONALE": "The obligation was previously satisfied by convention and measured by nothing; it is measured now, structurally and in both directions, so a missing reference on a backend failure and an INVENTED one on a client-side gate both fail. The sweep derives its roots from the plan sentence rather than from a hand list and is proved to reach the parts of the dashboard tree the three segments excluded; the classifier is proved against hand-built sources and cannot be fooled by prose; the route templating carries a falsifiability case. All eight pages are inside the error boundary and both navigation flips are pinned as an equality, so an `available` entry without a screen fails."
  },
  "DOC-001": {
    "IMPLEMENTATION_SURFACES": "`docs/phase-1/phase-1-28/canonical-plan.md` (468 lines), `contract-archaeology.md` (283), this verdicts file, the generated `task-matrix.json`, the traceability record `evidence/traceability.json` + `evidence/traceability.md` (105), and the three executables that hold them to the tree: `scripts/ci/build-p1-28-task-matrix.mjs`, `tests/ci/p1-28-matrix.test.ts` and — new at this head — `scripts/ci/check-p1-28-traceability.mjs` (653 lines, `validate:p1-28-traceability`).",
    "NORMAL_NAVIGATION_ENTRY": "N/A with rationale: documentation has no screen.",
    "BACKEND_READ_CONTRACTS": "N/A with rationale: this task IS the phase record. Its inputs are the operation register, the migrations and the tree itself, each read rather than transcribed.",
    "BACKEND_WRITE_CONTRACTS": "N/A with rationale: no mutation.",
    "REQUEST_SCHEMA": "N/A with rationale: no operation of its own.",
    "RESPONSE_SCHEMA": "N/A with rationale: no operation of its own.",
    "REQUIRED_PERMISSIONS": "N/A with rationale: documentation holds no permission.",
    "OWNER_REQUIREMENT_MAPPING": "The Owner-row anchoring in every FE row of this file is DOC-001's product: each canonical task names the Owner requirement or journey step it answers, because P1-27's Owner acceptance failed against requirements no task row had claimed.",
    "TENANT_ISOLATION_REQUIREMENT": "N/A with rationale: no runtime behaviour.",
    "LOADING_STATE": "N/A with rationale: no rendering.",
    "EMPTY_STATE": "N/A with rationale: no rendering. The reconciler's anti-vacuity stands in its place — no evidence field may be blank, and `N/A` without a reason is a failure (`tests/ci/p1-28-matrix.test.ts` \"leaves no field silently blank\").",
    "ZERO_RESULT_STATE": "N/A with rationale: no list.",
    "ERROR_STATE": "N/A with rationale: no rendering. A `PROTECTED_REPROOF` cell opening with an unrecognised marker is a HARD ERROR rather than an unknown value (`scripts/ci/build-p1-28-task-matrix.mjs:90-102`).",
    "DENIAL_STATE": "N/A with rationale: no permission surface.",
    "CONFLICT_STATE": "N/A with rationale: no write.",
    "ARABIC_EVIDENCE": "N/A with rationale: the phase record is English by convention.",
    "ENGLISH_EVIDENCE": "As above.",
    "RTL_LTR_EVIDENCE": "N/A with rationale: no rendering.",
    "ACCESSIBILITY_EVIDENCE": "N/A with rationale: no rendering.",
    "PRODUCTION_REACHABILITY": "`validate:p1-28-matrix` and `validate:p1-28-traceability` are both `required`, both reachable from `verify:policies`, and both named directly by `static-quality` in `.github/workflows/_reusable-node-quality.yml`.",
    "PRODUCTION_TEST_EVIDENCE": "`tests/ci/p1-28-matrix.test.ts` (25 cases at this head) — the universe is 35, read FROM the plan rather than from a hand list, every id stated literally with no range shorthand, category sizes asserted, the generated matrix byte-identical to a fresh build, AND the synchronization gate exercised in both directions (\"catches a quoted case title that no test carries\", \"catches a citation of a file that does not exist\", \"refuses browser evidence that is not the browser tier\", \"refuses an id that names no proof and states no gap\", \"refuses a permission the register does not publish, and misses none it does\", \"catches a derived count that disagrees with the tree\", \"fails a marker it cannot derive rather than ignoring it\", \"refuses every claim it has already had to withdraw once\").",
    "NEGATIVE_OR_MUTATION_PROOF": "The day-one anti-vacuity floor refuses a token where a fact belongs on the seven binding fields, an unassessed task may not read as PASS, and the totals must be supported by the rows themselves. Beyond that, the method changed at this head rather than the sentences: three documents had been corrected by hand and went stale in four days when PR #227 published a surface they said could not exist, so `scripts/ci/check-p1-28-traceability.mjs` now substitutes the TREE’S answer for every figure a P1-28 document opts into with a `<!-- derived: -->` marker, resolves every §5 operation binding against the P1-24 register, binds each canonical `TC-P1-28-*` id to quoted case titles matched against COMMENT-STRIPPED source, refuses browser evidence that is not the browser tier, and refuses by name each claim this phase has already had to withdraw. A marker it cannot derive is a FAILURE, not a silent pass.",
    "PROTECTED_REPROOF": "NOT REQUIRED — record-to-tree synchronization is proven by gates that run on every pull request (`validate:p1-28-matrix` and `validate:p1-28-traceability`, both reachable from `verify:policies` and both named by `static-quality`, plus `tests/ci/p1-28-matrix.test.ts`); no protected-branch-only or browser-tier job is part of this row’s evidence. The browser tier is a SUBJECT of this record, not a proof of it.",
    "FINDING_IDS": "Four record defects found and fixed by THIS re-derivation, all of the class this row exists to catch — a true sentence that stopped being true. (1) Every row’s `PROTECTED_REPROOF` asserted that `apps/web/tests/e2e/**` contains zero occurrences of \"appointment\" or \"reception\"; a 2478-line browser spec has since landed. (2) `canonical-plan.md` §7 stated that what ships for the warning-light catalogue is \"no warning-light picker\", which the configured browser proof refutes. (3) §7 and the change log both stated that FE-002 and FE-004 are PARTIAL, which this re-derivation no longer records. (4) Citations into files the closure waves changed had drifted off their targets — the leak, warning-light, receiving-employee, damage and signature rows all cited line ranges that now point at unrelated code, and one pointed past the end of a 133-line file (`check-in/steps.tsx`, cited at lines 161-199). Test citations in the repaired rows are now QUOTED CASE TITLES, which is the form the traceability gate itself checks and the only form that cannot drift with a line number. ALSO CORRECTED, AND SINCE OVERTAKEN: the change log claimed the tier runs P1-28 in three projects including tablet, which was false when the log was written because the tablet project then matched `administration` alone. `apps/web/playwright.config.ts:255` was widened afterwards and run `31750364479` / job `94614564003` executed 47 P1-28 cases in `authenticated-tablet`, so the log original sentence describes this candidate; the correction is kept in the past tense it belongs to.",
    "DOCUMENTATION_EVIDENCE": "`canonical-plan.md` §0 and §4 (the re-derivation and the check that replaced it); `contract-archaeology.md:11-33`; `evidence/traceability.md`; `evidence/change-log.md` \"Documentation\".",
    "FINAL_VERDICT": "PASS",
    "VERDICT_RATIONALE": "RE-ADJUDICATED at this head. RULE APPLIED, identically on every row of this file: a task is graded against THE CAPABILITY IT CANONICALLY OWNS — its §5 binding in `canonical-plan.md` — and against nothing else. A row does not stay PARTIAL because a fresh tenant starts empty, PROVIDED both halves are evidenced: its own journey works against a CONFIGURED tenant, and the UNCONFIGURED state is truthful. Equally, a row does not close while its own canonical journey is impossible, and a capability no canonical P1-28 task binds is not this register’s to withhold a verdict over. The capability this row canonically owns is record-to-tree synchronization for the archaeology, the plan and the traceability between them — and at this head that is not a claim about diligence, it is an executable check that runs on every pull request and is green: `validate:p1-28-traceability` resolves every §5 operation binding against the P1-24 register, binds all 22 canonical test ids to quoted cases in comment-stripped source, refuses browser evidence that is not the browser tier, substitutes the tree’s answer for 27 derived figures across 7 documents, and refuses by name every claim this phase has already had to withdraw. This run exercised that method against itself: four stale statements were found and corrected, including three in documents this row owns, and the drifted line citations were replaced with quoted case titles — the form the gate checks. The previous derivation held this row PARTIAL on a coordination argument, that DOC rows close at the END of a phase; that obligation is QA-005’s freeze step, which remains PARTIAL and names it. Judged against the capability THIS row owns, the record describes this head and a required gate keeps it that way."
  },
  "DOC-002": {
    "IMPLEMENTATION_SURFACES": "`docs/phase-1/phase-1-28/operator-guide.md` (305 lines, both domains), `docs/phase-1/phase-1-28/developer-guide.md` (193 lines, new at this head) and `docs/phase-1/phase-1-28/evidence/change-log.md` (422 lines), plus `media-capture-decision-record.md` (170) as the Owner-facing half of the FE-017 statement.",
    "NORMAL_NAVIGATION_ENTRY": "N/A with rationale: guidance has no screen.",
    "BACKEND_READ_CONTRACTS": "N/A with rationale: guidance work. Its sentences are pinned to source and to the operation register rather than quoting either.",
    "BACKEND_WRITE_CONTRACTS": "N/A with rationale: no mutation.",
    "REQUEST_SCHEMA": "N/A with rationale: no operation of its own.",
    "RESPONSE_SCHEMA": "N/A with rationale: no operation of its own.",
    "REQUIRED_PERMISSIONS": "N/A with rationale: documentation holds no permission. The guide's permission claims ARE checked against the codes: 'a different permission from booking' is verified against the two constants, and 'a third permission again' against approve, close and evidence (`apps/web/tests/p1-28-guidance-reconciliation.test.ts` \"‘a different permission from booking’ — the two codes really differ\" and \"‘a third permission again’ — approve, close and evidence are three codes\").",
    "OWNER_REQUIREMENT_MAPPING": "N/A with rationale: no Owner table row binds this task directly; its truthful-labelling records serve the Owner's operators.",
    "TENANT_ISOLATION_REQUIREMENT": "N/A with rationale: no runtime behaviour.",
    "LOADING_STATE": "N/A with rationale: no rendering.",
    "EMPTY_STATE": "N/A with rationale: no rendering.",
    "ZERO_RESULT_STATE": "N/A with rationale: no list.",
    "ERROR_STATE": "The guide's failure-message table is held to the platform convention in both directions, and the code is asserted to agree (`p1-28-guidance-reconciliation.test.ts` \"states the reference rule in both directions, and the code agrees\").",
    "DENIAL_STATE": "N/A with rationale: no permission surface.",
    "CONFLICT_STATE": "N/A with rationale: no write.",
    "ARABIC_EVIDENCE": "N/A with rationale: the guide is English; the product strings it describes are asserted in BOTH catalogues by the suites it points at, and the media strings are pinned exactly in both (`p1-28-reception-media.test.ts` \"is EXACTLY these fifteen strings, in both catalogues — any edit is a deliberate one\").",
    "ENGLISH_EVIDENCE": "As above.",
    "RTL_LTR_EVIDENCE": "N/A with rationale: no rendering.",
    "ACCESSIBILITY_EVIDENCE": "N/A with rationale: no rendering.",
    "PRODUCTION_REACHABILITY": "The guidance suite runs in the web tier on every pull request, so a guide sentence that stops being true fails a named case rather than ageing quietly.",
    "PRODUCTION_TEST_EVIDENCE": "`apps/web/tests/p1-28-guidance-reconciliation.test.ts` (31 cases at this head): each guide sentence pinned to the executable fact it describes, with numbers RE-DERIVED from `EVIDENCE_KIND_COVERAGE` rather than read out of the sentence, and the developer half held to \"lists exactly the rules the access gate enforces — no more, no fewer\", \"names the three write classifications the reachability gate allows\", \"states the version-sourcing rule the classifier really implements\", \"carries the derived counts rather than hand-written ones\" and \"warns about the traps that cost this phase real time\".",
    "NEGATIVE_OR_MUTATION_PROOF": "The change-log half is separately enforced — it must follow the sibling convention rather than inventing a path, must name every canonical task category so no category goes unlogged, must record the Backend remediations, the decision and the browser tier by name, must record the integration fixes that changed the product rather than only the record, and must state the phase is OPEN because no Owner acceptance has been returned. The guides’ own claims are falsifiable: \"there is no edit screen\" is checked against the absence of an appointment edit route, \"no submit control on that step\" against the signature step’s source, and \"it never shows you a raw identifier in place of a name\" against BOTH surfaces that print the receiving employee. The developer guide is derived from the gates rather than copied, which is how writing it found that `check-p1-28-access.mjs` is headed \"The six rules\" and enforces seven.",
    "PROTECTED_REPROOF": "NOT REQUIRED — guidance reconciliation is proven by `apps/web/tests/p1-28-guidance-reconciliation.test.ts` on every pull request, re-run green at this head; no protected-branch-only or browser-tier job is part of this row’s evidence. The browser tier is a SUBJECT the change log records, not a proof of the change log.",
    "FINDING_IDS": "The conjunction defect this row recorded is CLOSED: the developer half now exists (`developer-guide.md`, 193 lines) and its own reconciliation cases require it to list exactly the rules each gate enforces, so it cannot age into a page that merely exists. TWO change-log corrections were made by this re-derivation, both of the class the row exists to prevent: the log stated the browser tier runs the P1-28 surface in three projects including tablet, which was false when the log was written because the tablet project then matched `administration` alone; THAT CORRECTION HAS ITSELF BEEN OVERTAKEN — `apps/web/playwright.config.ts:255` was widened to `(administration|appointments-and-receptions)` and run `31750364479` / job `94614564003` executed 47 P1-28 cases in that project, so the log original sentence is true of this candidate and the correction is recorded here in the past tense rather than left reading as a present claim — and it stated that FE-002 and FE-004 are PARTIAL, which this matrix no longer records. The closure waves that landed the acceptance fixtures, the configured browser proof, the receiving-employee name resolution and the two closed-vocabulary fixes had no entry at all; they have one now.",
    "DOCUMENTATION_EVIDENCE": "`canonical-plan.md` §5.3 and §10 (the two truthful-labelling obligations); `operator-guide.md`; `evidence/change-log.md`; `media-capture-decision-record.md`.",
    "FINAL_VERDICT": "PASS",
    "VERDICT_RATIONALE": "RE-ADJUDICATED at this head, and all three halves of the conjunction are now delivered and gate-pinned. RULE APPLIED, identically on every row of this file: a task is graded against THE CAPABILITY IT CANONICALLY OWNS — its §5 binding in `canonical-plan.md` — and against nothing else. A row does not stay PARTIAL because a fresh tenant starts empty, PROVIDED both halves are evidenced: its own journey works against a CONFIGURED tenant, and the UNCONFIGURED state is truthful. Equally, a row does not close while its own canonical journey is impossible, and a capability no canonical P1-28 task binds is not this register’s to withhold a verdict over. The canonical name is \"Operator / developer guidance and change-log update\", and the previous derivation held this row PARTIAL for the right reason — the DEVELOPER half was absent while P1-28 had added four gates and more than ten rule ids. That half now exists and is held to the P1-27 standard: it lists exactly the rules each gate enforces, derived FROM the gates rather than from their prose, which is how it found an access gate headed \"the six rules\" that enforces seven. The operator guide gained the three things a person at the desk was missing — what party roles and authority actually record, why every intake catalogue is empty and that the reason is a decision rather than a setting, and that acceptance runs on the production build. The change log was stale in two places and carried no entry for the closure waves; both are corrected here, and the sentences on all three pages are pinned to the executable things they describe, because a guide that only has to exist passes while describing a product nobody built."
  }
}
