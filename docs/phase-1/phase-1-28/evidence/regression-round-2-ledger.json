{
  "run": "wf_1f516788-f0a",
  "producedBy": "p1-28-final-bounded-regression",
  "note": "SECOND bounded regression round, run after the original 31 were closed. Kept SEPARATE from the frozen 31 and from FR-32 so no denominator is silently changed.",
  "counts": {
    "confirmed": 28,
    "refuted": 5,
    "closed": 28,
    "open": 0
  },
  "confirmed": [
    {
      "FINDING_ID": "R2-01",
      "CLASS": "PRODUCT_DEFECT",
      "SURFACE": "apps/web/src/features/receptions/components/CheckInStartScreen.tsx:490",
      "TITLE": "The FR-06 eligibility fix is defeated by one click of its own pager: page two of the branch directory withdraws the operator's custody default and states they are ineligible",
      "REPRODUCER": "Probe (created, run, deleted) at apps/web/tests/zzz-refuter-page2.dom.test.tsx: module-mock `@/features/receptions/support-api`, route `listReceivingEmployeeCandidates` by CURSOR — cursor `null` answers `{status:'ok', rows:[{id:'user-1',displayName:'Front Desk'}], nextCursor:'cur-2', hasMore:true}` (the operator IS on page one, more exists), cursor `'cur-2'` answers `{status:'ok', rows:[{id:'user-9',displayName:'Other Person'}], nextCursor:null, hasMore:false}` (the tail, last page). Render `<CheckInStartScreen canPickEmployee />`, read the state, click 'Choose someone else', then click Next in the `receptions.checkIn.employeePagerLabel` pager, and read the state again.\n\nCommand (from apps/web): `npx vitest run --project dom tests/zzz-refuter-page2.dom.test.tsx`\n\nStdout:\n  ### page1 chosen = You — Front Desk\n  ### page1 ineligible notice = ABSENT\n  ### page2 chosen = No receiving employe",
      "FAILURE_SCENARIO": "A branch with more than 25 accounts eligible to accept custody (the directory's `pageSize`) publishes `hasMore: true` on page one. The operator opens the picker to look at who else is available — the ordinary reason the picker exists — clicks Next once, and the screen tells them their own account is not eligible to accept custody in the branch they are standing in and empties the receiving-employee field. `submit` sends `receivingEmployeeId: ''`, so `buildCreateInput` refuses locally and the check-in cannot be opened at all until the operator guesses that going back a page restores them. The F",
      "REFUTER_WHY": "CONFIRMED — reproduced independently with my own probe and my own fixtures, not the finder's.\n\nProbe (created, run, DELETED): apps/web/tests/zzz-refuter-fr06-pagetwo.dom.test.tsx. It module-mocks `@/features/receptions/support-api` — the same boundary the shipped screen imports — and routes `listReceivingEmployeeCandidates` by CURSOR, logging every call it receives. Fixture is deliberately different from the claim's: page one is 25 REAL rows (24 `Aa Colleague NN` plus the operator `user-1` / `Front Desk`, i.e. the operator IS on page one under the server's display_name ASC), `hasMore: true`, `nextCursor: 'cur-2'`; page two is the tail (`Zz Colleague 90/91`), `hasMore: false`, `nextCursor: null` — a genuine LAST page, no operator on it.\n\nCommand (from apps/web): `npx vitest run --project dom tests/zzz-refuter-fr06-pagetwo.dom.test.tsx`\n\nStdout:\n  ### page1 chosen=[You — Front Desk] ineligible=[ABSENT] useMyAccountButton=ABSENT\n  ### pager on page one present, next disabled=false\n  ### page2 chosen=[No receiving employee chosen yet] ineligible=[Your account is not eligible to accept custody in this branch, so it is no longer selected. Choose someone who is.] useMyAccountButton=ABSENT\n  ### adapter calls = [{\"page\":1,\"cursor\":null},{\"page\":2,\"cursor\":\"cur-2\"}]\n  ### pager still present on page two = yes\n  ### page2 submit alerts = [\"Name the receiving employee.\"]\n  ### page2 creat",
      "STATUS": "CLOSED",
      "FIX_EVIDENCE": "`readCompleteness` re-derived from the real pagination contract: it now takes `request.page`, and only `page === 1 && hasMore === false` is COMPLETE. The eligibility default is no longer withdrawn on page two. Cases: `reception-checkin.dom.test.tsx` \"does not withdraw the operator’s own custody default on page two\". Mutation — restoring `!hasMore => complete` — turns it and two siblings RED."
    },
    {
      "FINDING_ID": "R2-02",
      "CLASS": "PRODUCT_DEFECT",
      "SURFACE": "apps/web/src/components/data-table/read-completeness.ts:45",
      "TITLE": "`readCompleteness`/`membershipVerdict` report a whole-set verdict from the last page, so `membershipVerdict` answers `absent` on page two — the handoff notice states the pre-selected vehicle is not in the customer's list and drops the selection",
      "REPRODUCER": "Probe (created, run, deleted) at apps/web/tests/zzz-refuter-handoff2.dom.test.tsx: module-mock `@/lib/customers/vehicles`, route `listCustomerVehicles` by CURSOR — cursor `null` answers the handed-over vehicle `veh-9` with `hasMore:true, nextCursor:'cur-2'`; cursor `'cur-2'` answers a different vehicle with `hasMore:false`. Render `<CheckInStartScreen walkInHandoff={{requester:{...}, vehicleId:'veh-9'}} />`, read the handoff notice and the `aria-pressed` state of the selected row, then click Next in the `receptions.checkIn.vehiclePagerLabel` pager.\n\nCommand (from apps/web): `npx vitest run --project dom tests/zzz-refuter-handoff2.dom.test.tsx`\n\nStdout:\n  ### page1 handoff notice = Continued from walk-in intake. The customer and vehicle recorded there are already selected — check them before opening the visit.\n  ### page1 V-9 pressed = true\n  ### page2 handoff notice = Continued from walk",
      "FAILURE_SCENARIO": "This is the module's own canonical example turned back on itself: `read-completeness.ts:8-13` justifies its existence with '\"No active link between this requester and this vehicle is recorded\" printed after reading twenty-five of a customer's twenty-six vehicles'. A fleet customer with more than 25 linked vehicles is exactly that case, and paging the vehicle picker — the control that exists so the operator can reach vehicle twenty-six — makes the screen assert the vehicle handed over from walk-in intake is not in the customer's list, un-selects it, stops the open-visit lookup (`chosenVehicleId",
      "REFUTER_WHY": "CONFIRMED — reproduced independently, on the finder's two surfaces plus a third they did not name.\n\n## What I did\n\nI did not run the finder's probe. I wrote two of my own from scratch (`apps/web/tests/zz-refuter-pagetwo.dom.test.tsx`, `apps/web/tests/zz-refuter-pagetwo2.dom.test.tsx`), module-mocking the adapters and rendering the shipped components. Both are now DELETED (`git status` shows no `zz-refuter-pagetwo*`).\n\nFirst I checked the measurement itself. The real adapter is `apps/web/src/lib/customers/vehicles.ts:23` — `listCustomerVehicles(customerId, request, cursor)`. My mock routes on the THIRD argument, so it is the same seam the component drives, not a positional coincidence.\n\n## Surface 1 — the walk-in handoff notice\n\n`npx vitest run --project dom tests/zz-refuter-pagetwo.dom.test.tsx` (from `apps/web`). Cursor `null` -> `{rows:[V-9], nextCursor:'cur-2', hasMore:true}`; any cursor -> `{rows:[V-77], nextCursor:null, hasMore:false}`.\n\n```\n### PAGE1 notice = Continued from walk-in intake. The customer and vehicle recorded there are already selected - check them before opening the visit.\n### PAGE1 pressed = [ 'V-9OwnerChosen' ]\n### PAGE1 truncation notice rendered = true\n### PAGE2 notice = Continued from walk-in intake, but that vehicle is not in this customer's list. Choose the vehicle below.\n### PAGE2 notice IS handoffVehicleMissing = true\n### PAGE2 pressed count = 0\n##",
      "STATUS": "CLOSED",
      "FIX_EVIDENCE": "`membershipVerdict` takes the page too, so a handoff row absent from page two is `unknown-truncated` rather than `absent`. Cases: \"does not withdraw the pre-selection after ONE click of its own pager\" and \"states truncation on page two, where hasMore is false and the set is unseen\". Both RED under the mutation."
    },
    {
      "FINDING_ID": "R2-03",
      "CLASS": "PRODUCT_DEFECT",
      "SURFACE": "apps/web/src/features/receptions/components/steps/InspectionStep.tsx:302",
      "TITLE": "The FR-05 four-value branch still prints \"this visit has none\" on page two, while the open inspection sits on page one and the finding form is withdrawn",
      "REPRODUCER": "Probe (created, run, deleted) at apps/web/tests/zzz-refuter-insp2.dom.test.tsx: module-mock `@/features/receptions/api`, route `listConditionEvidence` for kind `inspection` by CURSOR — cursor `null` answers one `in_progress` header with `hasMore:true, nextCursor:'cur-2'`; cursor `'cur-2'` answers one `completed` header with `hasMore:false`. Render `<InspectionStep {...stepProps()} />`, confirm the finding form is offered on page one, then click Next in the `receptions.inspection.pagerLabel` pager.\n\nCommand (from apps/web): `npx vitest run --project dom tests/zzz-refuter-insp2.dom.test.tsx`\n\nStdout:\n  ### page1 finding form = present\n  ### page1 coverage notice = ABSENT\n  ### page2 coverage notice = A finding is recorded against an open inspection, and this visit has none. Open an inspection above first.\n  ### page2 truncated/unknown notice = ABSENT\n  ### page2 finding form = WITHDRAWN\n  ",
      "FAILURE_SCENARIO": "A visit with more than 25 inspection-evidence rows (`useEvidenceTable` pageSize, EvidencePanels.tsx:256) truncates page one. The operator pages forward through the inspection read-back — the control the FR-05 remediation itself points at, since the truncated sentence says 'Use the pages above to look further' — and the step then states as established fact that the visit has no open inspection and instructs them to open a second one, while the open inspection is one page back and the finding form they need is gone. The direction of the error is the expensive one the DamageMapStep docblock (line",
      "REFUTER_WHY": "CONFIRMED — reproduced independently with my own probe, not by running the finder's file.\n\nWHAT I RAN. Probe written from scratch at C:\\Users\\Ezzaldeen\\OneDrive\\Desktop\\1millions\\RootLco\\apps\\web\\tests\\zzrefx-insp-page2.dom.test.tsx (now DELETED; `ls apps/web/tests | grep -i zzrefx` returns nothing, and no tracked file was modified). It module-mocks `@/features/receptions/api` and `@/features/receptions/support-api`, and routes `listConditionEvidence` for kind `inspection` BY THE CURSOR THE HOOK ACTUALLY SENDS: cursor `null` answers one `in_progress` header with `nextCursor:'CUR-2', hasMore:true`; any non-null cursor answers one `completed` header with `hasMore:false`. Renders `<InspectionStep {...stepProps()} />` and clicks Next inside the `receptions.inspection.pagerLabel` nav.\n\nCommand (from apps/web): `npx vitest run --project dom tests/zzrefx-insp-page2.dom.test.tsx`\n\nStdout:\n  ### page1 slot = FORM\n  ### cursors sent = [\"null\",\"CUR-2\"]\n  ### page2 slot = COVERAGE: A finding is recorded against an open inspection, and this visit has none. Open an inspection above first.\n  ### page2 finding form present = false\n  ### pager page number on screen = 2\n  ### read-back mentions the completed header = true\n  ### read-back rows on screen = 1\n  ### loading skeletons on screen = 0\n  ### Next disabled (last page) = true\n  ### read-back text = 13 Aug 2026, 11:00 Status Completed Inspe",
      "STATUS": "CLOSED",
      "FIX_EVIDENCE": "`InspectionStep` no longer prints the coverage notice once the walk has left page one. Case: \"does not say the visit has none once the walk has left page one\", plus the corner that ends the walk. RED under the mutation."
    },
    {
      "FINDING_ID": "R2-04",
      "CLASS": "TEST_DEFECT",
      "SURFACE": "apps/web/tests/reception-condition-evidence.dom.test.tsx:1703",
      "TITLE": "The FR-08 gate suite certifies \"an unread page is never this visit has none\" while every fixture in it is page one, and its single page-two case drives only the direction that passes",
      "REPRODUCER": "Read the block. `evidenceTruncated` (1705-1709), `evidenceUnreadable` (1711-1721) and `evidenceInFlight` (1724-1735) are all `listConditionEvidence.mockImplementation` bodies that ignore the `cursor` argument entirely, so page one and page two return the same payload and `hasMore` never changes across a page move. The four `readCompleteness` values are therefore only ever driven at `request.page === 1`, where `hasMore===false` really does mean the whole set. The one case that pages (1851, 'reaches the open inspection on the next page and offers the finding form') calls `evidenceByKind({ inspection: [OPEN_INSPECTION] })` immediately BEFORE clicking Next, i.e. it makes page two hold the open inspection — the positive direction, in which `openInspections.length === 0` is false and line 302 is never evaluated.\n\nThat the block cannot reach the state is shown by the probe in the finding above:",
      "FAILURE_SCENARIO": "Both suites are the named FIX_EVIDENCE for FR-08 and FR-14 ('The gate suite gained the fourth value it never drove'; 'Eight cases added'), and both are the evidence the P1-28 task-matrix rows were re-derived on. A reader of either block reasonably concludes the page-boundary claim is held under test, because the block is literally titled for it and contains a case that pages. It is not: `mockResolvedValue`/cursor-blind `mockImplementation` make every page identical, which is the one fixture shape under which `readCompleteness`'s missing page number cannot show. The three product defects above ",
      "REFUTER_WHY": "CONFIRMED as a TEST_DEFECT, with one decisive correction: the headline surface is INERT and the reachable failure is on the second surface the claim names. Details below, all reproduced by me.\n\nMECHANICS — TRUE, verified by reading and by running.\n`listConditionEvidence` is called as `(visitId, kind, request, cursor)` (apps/web/src/features/receptions/components/steps/EvidencePanels.tsx:252-255). Every fixture helper the F1 block uses drops args 3 and 4: `evidenceByKind` (:174-178, `(_visitId, kind)`), `evidenceTruncated` (:1705-1709), `evidenceUnreadable` (:1711-1721, no parameters at all), `evidenceInFlight` (:1724-1735), and the inline transition mock at :1768. So page one and page two return the same payload and `hasMore` cannot change across a page move. The one case that pages (:1851 'reaches the open inspection on the next page and offers the finding form') re-arms with `evidenceByKind({ inspection: [OPEN_INSPECTION] })` BEFORE clicking Next — page two holds the open inspection, the positive direction, in which `openInspections.length === 0` is false and the coverage branch is never evaluated. The block is literally titled `F1 — an unread page is never \"this visit has none\"` (:1703). Suite green: `cd apps/web && npx vitest run --project dom tests/reception-condition-evidence.dom.test.tsx` -> `Test Files 1 passed (1) / Tests 58 passed (58)`.\n\nCOMPONENT BEHAVIOUR — TRUE. M",
      "STATUS": "CLOSED",
      "FIX_EVIDENCE": "The F1 block’s fixtures take the CURSOR — `evidenceWalk(first, second, moreOnFirst, moreOnSecond)` — and issue a real `nextCursor`, so a page move is observable at all. All four corners are driven, including page 1 + hasMore false, which is the only one entitled to conclude. A new contract suite, `apps/web/tests/read-completeness.test.ts`, drives the primitive at every corner (15 cases)."
    },
    {
      "FINDING_ID": "R2-05",
      "CLASS": "TEST_DEFECT",
      "SURFACE": "tests/backend/outbox-worker.test.ts:738",
      "TITLE": "The queue-health test's only assertion on oldestAgeSeconds cannot fail for any database state, and the value it observes is now entirely another tenant's backlog",
      "REPRODUCER": "Line 738 is 'expect(pending.oldestAgeSeconds).toBeGreaterThanOrEqual(0);'. The value under test is built at apps/api/src/server/worker/outbox-worker.ts:93 as 'oldestAgeSeconds: Math.max(0, Math.round(Number(row?.oldest_age_seconds ?? 0)))'. node -e \"console.log(Math.max(0,Math.round(Number(null ?? 0))), Math.max(0,Math.round(Number('27852'))))\" -> '0 27852'; the column arrives as a numeric string or NULL, so the expression is >= 0 for every possible row the query can return. No database state fails this assertion. Meanwhile the number itself, measured against the live acceptance database with queueHealth's own SQL, is: SELECT round(EXTRACT(EPOCH FROM (now() - min(occurred_at) FILTER (WHERE status='pending')))) FROM shared.event_outbox -> 27852 (7h44m) - the age of an acceptance-tenant row, since TENANT_A owns no pending row at that moment. grep confirms this is the only assertion on the ",
      "FAILURE_SCENARIO": "The repair rewrote the surrounding assertions of this test to baseline deltas (line 737 depth, 739 and 753 dead letters) and left the third gauge with an assertion that is a tautology. queueHealth's oldestAgeSeconds is surfaced operationally on the worker readiness probe (apps/api/src/server/health/readiness.ts:102, 'oldest ${health.oldestAgeSeconds}s'). If that expression were wrong - FILTER changed to status='dead_letter', the FILTER dropped so a published row sets the floor, min() swapped for max(), or the whole field hard-wired to 0 - the suite would stay 9/9 green, because the only test t",
      "REFUTER_WHY": "CONFIRMED — reproduced independently, and with a stronger proof than the finder gave: the real suite stays green with the field hard-wired to 0.\n\n1. THE ASSERTION AND THE CLAMP (read, not assumed)\n`sed -n '736,740p' tests/backend/outbox-worker.test.ts` ->\n    const pending = await withWorkerTransaction((db) => queueHealth(db));\n    expect(pending.depth).toBe(baseline.depth + 3);\n    expect(pending.oldestAgeSeconds).toBeGreaterThanOrEqual(0);\n    expect(pending.deadLetterCount).toBe(baseline.deadLetterCount);\napps/api/src/server/worker/outbox-worker.ts:94 -> `oldestAgeSeconds: Math.max(0, Math.round(Number(row?.oldest_age_seconds ?? 0)))`.\n\n2. THE OPERAND DOMAIN CONTAINS NO FAILING VALUE (measured live, not reasoned)\nScratch probe against the acceptance DB (127.0.0.1:54322, rootlco_test_worker), running queueHealth's own SQL and four mutated age expressions, each through the same clamp:\n  SHIPPED raw row : {\"depth\":\"86\",\"oldest_age_seconds\":\"28187.582730\",\"dead_letters\":\"0\"}  typeof = string\n  M1 FILTER->dead_letter raw=null            clamped=0     >=0 true\n  M2 FILTER dropped      raw=28187.592199    clamped=28188 >=0 true\n  M3 min->max            raw=1208.848932     clamped=1209  >=0 true\n  M4 sign flipped        raw=-28187.597228   clamped=0     >=0 true\n  M5 hard-wired 0        clamped=0                          >=0 true\nnumeric arrives from node-postgres as a string and NU",
      "STATUS": "CLOSED",
      "FIX_EVIDENCE": "The vacuous `oldestAgeSeconds >= 0` is gone. The gauge is compared against the age the DATABASE computes for the oldest claimable row (one second of tolerance, one clock), and a second case plants a two-hour-old row and requires the reported age to jump, then to come back down — so a gauge reading the newest row, or a constant, fails."
    },
    {
      "FINDING_ID": "R2-06",
      "CLASS": "DOCUMENTATION_DEFECT",
      "SURFACE": "docs/phase-1/phase-1-28/task-matrix.json:1042",
      "TITLE": "FR-26's fix corrected only the gate docblock; the SEC-004 closure rationale the ledger's own correction named as the material instance still states 35 / 21 / fourteen / one-parked (documentation)",
      "REPRODUCER": "This is DOCUMENTATION, stated plainly. FR-26's FIX_EVIDENCE reads 'The stated derived-set figure now reads 43', and scripts/ci/check-p1-28-write-reachability.mjs:21 is indeed corrected. But FR-26's own REFUTER_CORRECTION in the frozen ledger says: '(b) The MATERIAL instance is one the claim mentions only in passing: the SEC-004 VERDICT_RATIONALE, present identically at task-matrix.json:1043 and task-matrix-verdicts.json:754. It has no correcting neighbour and no disclaimer … That is the sentence worth fixing first.' It was not fixed.\n\nLive run: `node scripts/ci/check-p1-28-write-reachability.mjs` -> 'P1-28 write reachability: 43 canonical write(s), 241 production file(s), 43 mutation call site(s), 42 of them consumed, 2 decision(s) recorded in the plan §7. REACHABLE = 18, DELIBERATELY_ABSENT = 25. OK'. NOT_YET_WIRED is absent, i.e. 0.\n\nStill live in the record (grep -n):\n  task-matrix.js",
      "FAILURE_SCENARIO": "A reader taking the phase's own PASS rationale for SEC-004 at face value carries away a fourteen-write subject set with one parked write, when the shipped gate governs forty-three with none parked — and, unlike the gate docblock, this sentence carries no 'the derivation is the authority' disclaimer and no pointer to the test that states the correct arithmetic. It is the closure assessment for the gate, written in the present tense, wrong on four separate figures, and it is the one instance FR-26's own adjudication singled out as needing to be fixed first.",
      "REFUTER_WHY": "CONFIRMED as a DOCUMENTATION_DEFECT, reproduced independently on feature/p1-28-owner-decisions-frontend (working tree, HEAD 366a956b).\n\n1. THE FIX DID LAND — BUT ONLY AT THE DOCBLOCK. `sed -n '14,24p' scripts/ci/check-p1-28-write-reachability.mjs` prints at line 21: \"The derived set is 43 today — counted from the register by this gate on every run, which is why the figure is stated here at all: a number in prose is a claim, and this one said 35 while the derivation answered 43 and the gate's own test pinned 43.\" FR-26's FIX_EVIDENCE is accurate for that one site.\n\n2. THE MATERIAL INSTANCE IS UNTOUCHED. `grep -n \"the subject set grew from 14 to 35\"` -> docs/phase-1/phase-1-28/task-matrix.json:1042 and docs/phase-1/phase-1-28/task-matrix-verdicts.json:754. The text is byte-identical to the ledger's quotation. `git diff docs/phase-1/phase-1-28/task-matrix-verdicts.json | grep \"subject set grew from 14 to 35\"` returns NOTHING — the remediation edited that file (it is ' M' in git status) for other findings and left this sentence alone. The claim's line number 1042 is exactly right (FR-26's own correction said 1043, off by one).\n\n3. ALL FOUR FIGURES WRONG AGAINST THE LIVE GATE. `node scripts/ci/check-p1-28-write-reachability.mjs` -> \"P1-28 write reachability: 43 canonical write(s), 241 production file(s), 43 mutation call site(s), 42 of them consumed, 2 decision(s) recorded in the pl",
      "STATUS": "CLOSED",
      "FIX_EVIDENCE": "The SEC-004 rationale is re-derived from the gate’s own output — 43 canonical writes, 18 REACHABLE, 25 DELIBERATELY_ABSENT, nothing parked, 66 cases — with the command that prints it and the superseded figures quoted as superseded rather than deleted."
    },
    {
      "FINDING_ID": "R2-07",
      "CLASS": "ASSURANCE_GATE_DEFECT",
      "SURFACE": "scripts/ci/check-p1-28-version-sourcing.mjs:625",
      "TITLE": "Version sourcing still attributes a guarded call site by PROXIMITY, so an unrelated component's refresh() satisfies the renewal rule for a component that never renews",
      "REPRODUCER": "`enclosingFunction` takes the last `function` declaration whose index precedes the call and ends its region at the NEXT declaration, or at text.length when there is none. It is the identical 'proximity wearing containment's clothes' defect FR-30 removed from the sibling gate in this same wave; it was left standing here. Arrow-function components are invisible to `allFunctions`, so a call inside one is credited to whatever declaration sits above it, and `renewsAfter(text, call.index, enclosing.end, enclosing.binds)` then scans forward past the component's own closing brace.\n\nMUTATION through the exported `run({document, sources, adapterRoots, manifest, decisions})` (scratchpad probe, `node vs-mutate.mjs`), one guarded operation rec.reception-approve, one adapter `approveReception(receptionId, ifMatch: number, input)`:\n  CONTROL — `export function StepHeading(...) {...}` followed by `expor",
      "FAILURE_SCENARIO": "A screen file holds two components. The first sends a version-guarded command — rec.reception-approve, which applies one edge from `inspecting` and two from `opened` — then keeps its own counsel: no callback, no re-read. The second, unrelated, component calls refresh(). The gate credits the second component's re-read to the first and reports the file clean, so the operator's next command carries a version the record has already moved past and the backend answers 409 ERR-CON-001 — the staleness this gate's 'renewal rule' section exists to forbid, certified green. The violation message also name",
      "REFUTER_WHY": "CONFIRMED — reproduced independently, four ways, including against the real shipped file.\n\nTHE CODE. scripts/ci/check-p1-28-version-sourcing.mjs:625 `enclosingFunction` returns `{ name: nearest.name, binds, end: next ? next.index : text.length }`, where `nearest` is the last `allFunctions` match whose index precedes the call. `allFunctions` (:338) matches only `function` declarations, and `end` is the index of the NEXT DECLARATION, not the end of the enclosing body. run() (:794) then calls `renewsAfter(text, call.index, enclosing.end, enclosing.binds)`. So both halves of the claim are in the source: attribution is proximity, and the renewal scan runs past the enclosing component's closing brace. The sibling gate fixed exactly this under FR-30 — check-p1-28-write-reachability.mjs:644 now requires `index <= declaration.end` with `end: bodyEnd(...)` and its docblock names the old rule \"proximity wearing containment's clothes\". version-sourcing does not import `bodyEnd` and was not given one; `git diff --stat HEAD` shows 465 lines changed in the sibling and 66 here (FR-27/FR-29 docblock and 'use server' work only).\n\nREPRODUCTION 1 — the finder's exact scenario, through the exported run(), `node scratchpad/vs-refute.mjs` (injected document with one guarded rec.reception-approve, one adapter `approveReception(receptionId, ifMatch: number, input)`, adapterRoots/manifest/decisions inje",
      "STATUS": "CLOSED",
      "FIX_EVIDENCE": "The version-sourcing gate no longer attributes by proximity. Containment comes from the TypeScript parser (`scripts/lib/typescript-source.mjs`), the renewal region is the enclosing function’s OWN body, and bindings are the lexical scope chain. Planted case: two arrow-const components, the hoarding one written first, judged separately."
    },
    {
      "FINDING_ID": "R2-08",
      "CLASS": "ASSURANCE_GATE_DEFECT",
      "SURFACE": "scripts/ci/check-p1-28-version-sourcing.mjs:123",
      "TITLE": "FR-29's derived adapter root set is still confined to features/*, so a defective guarded adapter under apps/web/src/lib — where two 'use server' modules already live — is never opened",
      "REPRODUCER": "`adapterRootsFromRepository()` enumerates only the immediate children of apps/web/src/features (7 roots at this head). Its docblock (:106-121) says the hand-written pair was replaced because 'a hand-listed root set makes that anti-vacuity check answer for the trees somebody remembered, so the sweep can be clean, non-empty, and still blind to a whole feature'. The derivation is still blind to a whole TREE.\n\nThat tree is not hypothetical: enumerating 'use server' modules with the sibling gate's exported `declaresUseServer` returns 35, and TWO are outside features/ — apps/web/src/lib/customers/directory.ts (`export async function searchCustomerDirectory`) and apps/web/src/lib/customers/vehicles.ts (`export async function listCustomerVehicles`), whose own docblock says 'why this module lives here rather than in a feature'.\n\nMUTATION through the exported `run`, with adapterRoots set to the RE",
      "FAILURE_SCENARIO": "A shared adapter is placed under apps/web/src/lib, following the convention two shipped 'use server' modules already establish there. Both defects this gate exists for — an ifMatch that is optional or defaulted, and a call site sending sent+1 — are then invisible: the adapter never enters the map, so its name never enters the call-site scan, and the adapter/operation equality still balances because the file was never opened. The gate prints '0 violations' and 'OK: every version-guarded command sources its If-Match from a read or a command response' over a module it did not read.",
      "REFUTER_WHY": "CONFIRMED — reproduced twice, once through the exported `run` with the REAL contract/manifest/plan/tree, and once ON DISK through the shipped CLI.\n\n1. THE MECHANISM. `scripts/ci/check-p1-28-version-sourcing.mjs:123` `adapterRootsFromRepository()` does `readdirSync(join(ROOT, 'apps/web/src/features'))` and returns only its immediate directory children. Measured:\n  `node -e \"import('./scripts/ci/check-p1-28-version-sourcing.mjs')…\"` -> ROOTS(7): apps\\web\\src\\features\\{administration,appointments,attachments,authentication,crm,receptions,vehicles}\nThe adapter walk is gated on `if (!adapterRoots.some((root) => path.startsWith(root))) continue;` (:751) BEFORE `declaresUseServer` (:752), so nothing under `apps/web/src/lib` is ever opened. The call-site scan then only searches for names in the `adapters` map (:775), so a hidden adapter's call sites are hidden with it.\n\n2. ON-DISK CONTROL/MUTANT, byte-identical files, only the directory changes. Baseline first: `node scripts/ci/check-p1-28-version-sourcing.mjs` -> \"…7 adapter(s), 7 guarded call site(s). OK: every version-guarded command sources its If-Match from a read or a command response.\" exit 0.\n  MUTANT — pair written to apps/web/src/lib/receptions/{approval.ts,SealPanel.tsx}; `approval.ts` is `'use server'` with `export async function sealApproval(receptionId: string, ifMatch = 0, input: unknown)`, `SealPanel.tsx` calls `sealApp",
      "STATUS": "CLOSED",
      "FIX_EVIDENCE": "The adapter roots are derived from `apps/web/src` rather than from `features/*`, and the root set is CHECKED: a `use server` module outside every root is a violation instead of a file that quietly never opened. Planted case: a defective adapter under `src/lib`, invisible to the old derivation and caught by this one."
    },
    {
      "FINDING_ID": "R2-09",
      "CLASS": "TEST_DEFECT",
      "SURFACE": "tests/backend/outbox-worker.test.ts:445",
      "TITLE": "The quarantine is a one-shot snapshot and the only foreign-state detector covers 2 rows, so the suite still claims, consumes and PUBLISHES another tenant's event while reporting 9/9 green",
      "REPRODUCER": "BASELINE (undisturbed run - the fix's own claim holds):\n1. node <scratch>/snap.mjs before.json -> 'rows 86 / error_records 0 / processed_events 0' (86 pending rows, tenant c0000000-0000-4000-8000-00000000000c; the ledger's 55 has grown to 86, newest occurred_at 2026-08-18T19:26:38Z).\n2. npx vitest run --config vitest.config.backend.ts tests/backend/outbox-worker.test.ts -> 'Test Files 1 passed (1) / Tests 9 passed (9)'.\n3. snapshot again + field-by-field diff -> 'total diffs 0 | before 86 | after 86'. So yes: with no concurrent writer the 86 foreign rows are byte-identical before and after.\n\nTHE ATTACK (the acceptance app publishing one event mid-test, which this environment demonstrably does - it produced 31 new outbox rows today, the newest four minutes before my run):\n4. Started a racer that polls shared.event_outbox for the TENANT_A count going 3 -> 0 (the queue-health test leaves ex",
      "FAILURE_SCENARIO": "The header at lines 63-64 states the rule 'Ownership and deltas in the assertions. Batch assertions name the ids the test itself published', and line 70 concludes 'Every test here therefore also proves the isolation'. Neither is what the code does: of the seven tests that issue a claim, only 'claiming is exclusive' (line 483) and the isolation test name ids; the graceful-shutdown test at lines 758-798 issues a whole OutboxWorker claim loop across every tenant and asserts nothing but its own event's outcome. Because quarantineForeignEvents() at line 445 is a SELECT ... FOR UPDATE taken once in ",
      "REFUTER_WHY": "CONFIRMED — reproduced independently three times against the live acceptance database, with the claimant captured by name.\n\nWHERE (verified by reading the source, not by trusting the claim)\n- tests/backend/outbox-worker.test.ts:436-446 — beforeEach deletes TENANT_A rows then calls quarantineForeignEvents().\n- :300-330 — quarantineForeignEvents() is a single `SELECT id FROM shared.event_outbox WHERE tenant_id <> $1 AND status IN ('pending','claimed') ORDER BY id FOR UPDATE` held open in one transaction. It locks the rows that EXIST (and are committed) at that instant. Nothing re-runs it.\n- :410-420 — foreignEventStates() is the only foreign-state detector and is scoped `WHERE tenant_id = $1` with FOREIGN_TENANT = 'f0000000-…-f'. Step-4 checks at :880-887 count error_records/processed_events for FOREIGN_TENANT only.\n- :758-798 — the graceful-shutdown test starts a full OutboxWorker (batchSize 5, claim spans every tenant) and asserts only its own event's outcome plus sideEffectCount(), which is `tenant_id = TENANT_A AND error_code = 'fx_p1_13_applied'`. No assertion bounds the batch.\n- apps/api/src/server/worker/outbox-worker.ts:190-206 — `consumersFor(event.eventType)`; if the list is EMPTY the for-loop body never runs and `shared.complete_outbox_event` is called anyway. So a foreign row of any type is marked published.\n- shared.claim_outbox_events (pg_get_functiondef) — no tenan",
      "STATUS": "CLOSED",
      "FIX_EVIDENCE": "The suite takes an exclusive advisory lease on the shared database (`scripts/lib/database-lease.mjs`) and REFUSES TO RUN without it; every claim is guarded by a check for foreign rows outside the quarantine snapshot and ABORTS before claiming. Reproducer: a producer writing mid-run aborts the batch and the row is left `pending`, unclaimed, unconsumed."
    },
    {
      "FINDING_ID": "R2-10",
      "CLASS": "TRACEABILITY_DEFECT",
      "SURFACE": "docs/phase-1/phase-1-28/task-matrix-verdicts.json:603",
      "TITLE": "FE-021 cites a templated acknowledgement test that does not exist, and a case count of 21 for a file that runs 18",
      "REPRODUCER": "PRODUCTION_TEST_EVIDENCE (SAME as HEAD) reads: \"`apps/web/tests/reception-acknowledgement.dom.test.tsx` (21 cases at this head, including \\\"prints the receiving employee by NAME, with the G-EMP note\\\" and the templated \\\"prints the reason rather than an identifier when the name came back ${status}\\\" over all three non-named outcomes)\", and NEGATIVE_OR_MUTATION_PROOF repeats \"the templated case fails if any of the three non-named outcomes prints the identifier instead of the reason\". Run it: `cd apps/web && npx vitest run tests/reception-acknowledgement.dom.test.tsx --project=dom` -> `Test Files 1 passed (1) / Tests 18 passed (18)`. `grep -n 'it(|it\\.each|describe(' tests/reception-acknowledgement.dom.test.tsx` lists 18 `it(` and zero `it.each`. `grep -n 'came back' tests/reception-acknowledgement.dom.test.tsx` -> no match. The named case that does exist is at `:190` ('prints the receivin",
      "FAILURE_SCENARIO": "Two of FE-021's evidence cells rest on a test that cannot be run because it is not there, and on a count that overstates the file by three. The mutation-proof cell in particular claims a falsifiable guard — \"fails if any of the three non-named outcomes prints the identifier\" — where nothing executes. Anyone auditing the row for the QA-005 seal, or re-running its evidence, quotes a case name that returns zero matches; the row's own claim that the identifier can never be printed then has no executable backing on the surface the customer signs.",
      "REFUTER_WHY": "CONFIRMED — reproduced independently, and it is an INCOMPLETE FIX of FR-32 rather than a fresh defect.\n\nREPRODUCTION (all from repo root, C:/Users/Ezzaldeen/OneDrive/Desktop/1millions/RootLco):\n\n1. The count. `cd apps/web && npx vitest run tests/reception-acknowledgement.dom.test.tsx --project=dom` -> \"Test Files 1 passed (1) / Tests 18 passed (18)\". `docs/phase-1/phase-1-28/task-matrix-verdicts.json:603` (and the identical `docs/phase-1/phase-1-28/task-matrix.json:852`) says \"21 cases at this head\". Overstates by three.\n\n2. The templated case. `grep -n \"came back\" apps/web/tests/reception-acknowledgement.dom.test.tsx` -> no match; `git show HEAD:...` -> no match either, and `git status --short` does not list the test file, so worktree == HEAD and this is not a working-tree artefact. `grep -n \"it(|it.each\"` lists exactly 18 `it(` and zero `it.each`. The case the cell pairs it with DOES exist (\"prints the receiving employee by NAME, with the G-EMP note\", :190); the three-outcome template does not.\n\n3. Nothing executes behind the mutation-proof cell. `AcknowledgementDocument.tsx:165-166` renders `{detail.receivingEmployeeDisplayName}` with no branch — there is no denied/unresolved/unavailable outcome on this component to template over, so \"the templated case fails if any of the three non-named outcomes prints the identifier instead of the reason\" describes a guard over states the",
      "STATUS": "CLOSED",
      "FIX_EVIDENCE": "FE-021’s evidence cells are re-derived from the files: 19 cases, not 21, and the templated case that does not exist is replaced by one that does — \"never prints the identifier, whatever the snapshot holds\", which drives an empty snapshot and asserts no uuid anywhere on the sheet."
    },
    {
      "FINDING_ID": "R2-11",
      "CLASS": "ASSURANCE_GATE_DEFECT",
      "SURFACE": "scripts/ci/check-p1-28-write-reachability.mjs:878",
      "TITLE": "FR-30/FR-31's fail-closed guarantee is defeated by a nested template literal — an unattributable write is re-labelled a top-level statement and passes REACHABLE",
      "REPRODUCER": "The repair's third outcome (docblock lines 144-171) says a site inside a construct the scanner cannot read is 'attributed to NOTHING and dropped, so the claim behind it turns RED', and it is separated from the legitimate top-level case by `braceDepth(text, index) === 0` at :878. That depth is computed over text already through `normaliseSourcePaths` (:325), whose `/\\$\\{[^}]*\\}/g` cannot cross a `}`, so an interpolation containing a nested template literal is mangled rather than collapsed — leaving a stray `}` (which decrements the depth) and a stray backtick (which makes `skipLiteral` at :522 swallow arbitrary code).\n\nThe construct is LIVE. From apps/web/src/features/receptions/components/steps/PartiesStep.tsx:349, run through the gate's own exported stripComments + normaliseSourcePaths:\n  RAW       : \"? `${translateDynamic(messages, `receptions.authorizingRole.${row.authorizingRole}`)} ",
      "FAILURE_SCENARIO": "A wave adds a label helper of the shape already used four times in this tree to an adapter module, then ships or later orphans a write in a construct the scanner cannot read — an arrow const, an object method, a class method. braceDepth reads 0 for that site, attributionsAt returns owner=null/attributable=true, and consumed(null) at :1023 returns true unconditionally, so the consumption question is never asked. The manifest's REACHABLE claim stays green for an operation no operator can invoke: INT-113 blessed by the gate built to catch it, the exact outcome FR-30's containment fix and FR-31's ",
      "REFUTER_WHY": "CONFIRMED — I reproduced it end to end, including a false GREEN on a REAL shipped adapter, from probes I wrote independently. All work is in the session scratchpad; I created nothing under apps/web/tests or tests/ and modified no tracked file (scripts/ci/check-p1-28-write-reachability.mjs mtime still 2026-08-18_17:42).\n\n1) THE MANGLING IS REAL AND LIVE. `normaliseSourcePaths` (:325) is `text.replace(/\\$\\{[^}]*\\}/g, ':p')`; `[^}]*` cannot cross a `}`, so an interpolation containing a NESTED template literal is truncated rather than collapsed. Driven through the gate's own exported `stripComments` + `normaliseSourcePaths` (scratchpad/ref-a-normalise.mjs), apps/web/src/features/receptions/components/steps/PartiesStep.tsx:\n  line 349 RAW \"? `${translateDynamic(messages, `receptions.authorizingRole.${row.authorizingRole}`)} · `\" -> NORMALISED \"? `:p`)} · `\"\n  line 352 the same, for `receptions.channel`.\nA sweep of all 243 production files with the gate's `braceDepth`/`skipLiteral` extracted VERBATIM from the source (ref-b-sweep.mjs) returns exactly four files ending at non-zero brace depth: Overlays.tsx +1, DamageMapStep.tsx +2, InspectionStep.tsx -1, PartiesStep.tsx -1. A precise count of truncated interpolations (ref-j-class2.mjs) gives 7, in those same four files. Identical to the finder's figures.\n\n2) THE FINDER'S EXACT MUTATION REPRODUCES (ref-c-mutate.mjs), through the gate's ",
      "STATUS": "CLOSED",
      "FIX_EVIDENCE": "The write-reachability gate no longer counts braces. Containment, top-level detection, path building and caller discovery all come from the TypeScript parser (`scripts/lib/typescript-source.mjs`), shared with the version-sourcing gate so the answer has ONE definition. A file the parser refuses yields no sites, so a claim over it turns red. Cases: a nested template literal is attributed correctly, an anonymous callback is refused, an unparseable file yields nothing, and a genuine top-level statement is still counted."
    },
    {
      "FINDING_ID": "R2-12",
      "CLASS": "TRACEABILITY_DEFECT",
      "SURFACE": "docs/phase-1/phase-1-28/task-matrix-verdicts.json:589",
      "TITLE": "The FR-32 fix left FE-021 requiring `iam.user.read` for a directory read the page cannot make",
      "REPRODUCER": "FR-32's FIX_EVIDENCE says \"FE-021 now records that no directory read is made and names the snapshot field instead\". A per-key diff of the row against HEAD (`node -e` comparing `git show HEAD:docs/phase-1/phase-1-28/task-matrix-verdicts.json` to the working tree) prints CHANGED for exactly two keys — BACKEND_READ_CONTRACTS and VERDICT_RATIONALE — and SAME for the other 25, including REQUIRED_PERMISSIONS. That cell still reads: \"`rec.reception.read` alone for the sheet … plus `iam.user.read` for the name, which the route decides for itself and, lacking it, spends no request\". Against the code: `grep -n \"iam\\.user|readUserIdentity|user-directory|USER_DIRECTORY\" 'apps/web/src/app/[locale]/(dashboard)/receptions/check-in/[receptionId]/acknowledgement/page.tsx'` -> GREP_EXIT=1, no match; the file's whole import list (lines 1-24) contains nothing from `features/receptions/people`. The repositor",
      "FAILURE_SCENARIO": "REQUIRED_PERMISSIONS is the cell a least-privilege reviewer and a role-granting administrator read. It tells them the acknowledgement sheet needs `iam.user.read` — a tenant-wide staff-directory capability — when the shipped route consults only `rec.reception.read`. Granting from this cell hands every operator who may print an acknowledgement a directory read the product does not use, which is the same over-grant class as P1-28-SEC-001. And the row now contradicts itself: BACKEND_READ_CONTRACTS (corrected) says \"NO directory read for the receiving employee\" while REQUIRED_PERMISSIONS (uncorrect",
      "REFUTER_WHY": "REPRODUCED, every step independently, from the repo root on `feature/p1-28-owner-decisions-frontend` (HEAD `366a956b`).\n\n1. The FR-32 fix did not touch the cell. I extracted the HEAD blob and diffed the FE-021 row key by key:\n   `git show HEAD:docs/phase-1/phase-1-28/task-matrix-verdicts.json > $SCRATCH/head-verdicts.json`, then a node per-key comparison of `['FE-021']` HEAD vs working tree.\n   Output: CHANGED for exactly two keys — BACKEND_READ_CONTRACTS and VERDICT_RATIONALE — and SAME for the other 25, REQUIRED_PERMISSIONS and IMPLEMENTATION_SURFACES among them. Matches the claim exactly.\n   The HEAD text of BACKEND_READ_CONTRACTS confirms what was repaired: \"...and, at this head, `iam.user-detail` to resolve the receiving employee to a NAME (`acknowledgement/page.tsx:128`, `:158`)\". The HEAD text of REQUIRED_PERMISSIONS is byte-identical to the working tree's.\n\n2. The shipped page makes no directory read, and it is the committed surface.\n   `git status --porcelain -- 'apps/web/src/app/[locale]/(dashboard)/receptions/check-in/[receptionId]/acknowledgement/page.tsx'` => empty (unmodified).\n   `grep -nE \"iam\\.user|readUserIdentity|user-directory|USER_DIRECTORY|iam/users|user-detail\" <that file>` => GREP_EXIT=1, no match. The file is 157 lines; I read all of it. Its import list (lines 1-24) contains nothing from `features/receptions/people`. Its `Promise.all` (112-120) has thre",
      "STATUS": "CLOSED",
      "FIX_EVIDENCE": "FE-021 requires `rec.reception.read` ALONE. `iam.user.read` is removed from the cell, with the reason: the page prints the `receivingEmployeeDisplayName` snapshot and holds no call to `readUserIdentity`."
    },
    {
      "FINDING_ID": "R2-13",
      "CLASS": "TRACEABILITY_DEFECT",
      "SURFACE": "docs/phase-1/phase-1-28/task-matrix-verdicts.json:583",
      "TITLE": "FE-021 still records the receiving-employee resolution inside the acknowledgement page's Promise.all, at line numbers that are not the code",
      "REPRODUCER": "IMPLEMENTATION_SURFACES (SAME as HEAD per the per-key diff) reads: \"`AcknowledgementDocument.tsx` (348 lines) behind `acknowledgement/page.tsx:87` (gate), `:92` (detail) and `:115-129` (the three section reads AND THE RECEIVING-EMPLOYEE RESOLUTION, issued together in one `Promise.all`)\". Read the file: `grep -n 'holds(session.permissions|await readReception|Promise.all|The receiving employee is NOT read here' \"apps/web/src/app/[locale]/(dashboard)/receptions/check-in/[receptionId]/acknowledgement/page.tsx\"` -> `84: if (!holds(...))`, `89: const detail = await readReception(receptionId)`, `112: const [parties, authorizations, evidence] = await Promise.all([`, `116: // The receiving employee is NOT read here.` The Promise.all is :112-120 and holds exactly three calls; `:115` is `listConditionEvidence(...)`, `:126-129` is the middle of the OUTCOME docblock; `:92` is `return frame(` inside t",
      "FAILURE_SCENARIO": "This is the cell a reader opens to learn what FE-021 is built out of. It sends them to a Promise.all it says issues four reads including a directory lookup, at a line range that is mostly a comment block; the code at those lines states in plain words that no such read is made. The next agent re-deriving FE-021 either believes a fourth read exists and preserves a permission and a failure mode for it, or concludes the matrix is unusable. Both `:87/:92/:115-129` and `:156` are the coordinates the closure review would check first.",
      "REFUTER_WHY": "Reproduced independently against the working tree, HEAD, and commit 42e0ae29. The FE-021 IMPLEMENTATION_SURFACES cell is confirmed stale in both matrix files, with provenance traced to the pre-9fc2da7d page, and both `validate:p1-28-matrix` and `validate:p1-28-traceability` are green over it. The `:156`/`:166` portion of the stated scenario is refuted: those citations belong to FE-007, and the finder's direction is reversed by an uncommitted +10-line edit in AcknowledgementDocument.tsx.",
      "STATUS": "CLOSED",
      "FIX_EVIDENCE": "FE-021’s implementation surfaces re-derived: 351 lines, gate `:84`, detail `:89`, THREE section reads at `:112`. The \"receiving-employee resolution in the Promise.all\" is removed — the code at `:116-119` says in plain words that it is not read there."
    },
    {
      "FINDING_ID": "R2-14",
      "CLASS": "TRACEABILITY_DEFECT",
      "SURFACE": "docs/phase-1/phase-1-28/task-matrix-verdicts.json:185",
      "TITLE": "FE-007 still describes an \"employee search\" travelling as a parameter, on an operation whose strict schema admits no search term",
      "REPRODUCER": "FR-13's own REFUTER_CORRECTION (B) named this cell explicitly: \"SCOPE: three cells are stale, not one. TENANT_ISOLATION_REQUIREMENT also describes 'the employee search travels as an encoded parameter of a POST-backed Server Action' when there is no search at all\". The per-key diff against HEAD shows FE-007 changed BACKEND_READ_CONTRACTS, REQUIRED_PERMISSIONS and VERDICT_RATIONALE, and prints SAME for TENANT_ISOLATION_REQUIREMENT, which still reads: \"Branch pair as the operation's required field, never a client claim; the employee search travels as an encoded parameter of a POST-backed Server Action and never reaches the address bar\". Against the code: `apps/api/src/app/api/v1/reception-catalogue/receiving-employees/route.ts:47-54` is `const Query = z.object({ companyId: schemas.uuid, branchId: schemas.uuid, cursor: schemas.cursor.optional(), limit: schemas.limit.optional() }).strict();` ",
      "FAILURE_SCENARIO": "The isolation cell is the record of what operator input crosses the wire on this surface. It describes a search parameter that the server refuses by `.strict()` and that the client never builds — a leftover of the `iam.user-list` picker FE-007 was rewritten to remove. A reviewer assessing the disclosure surface of the receiving-employee picker reasons about a free-text staff query that does not exist, and the wave's own refuter flagged this cell by name before the fix was applied, so the row is now two-thirds corrected and reads as though the third were checked.",
      "REFUTER_WHY": "CONFIRMED, and it is a DOCUMENTATION defect — no shipped runtime behaviour is wrong. It is an INCOMPLETE FIX of FR-13, whose own REFUTER_CORRECTION (B) named this cell by name and whose FIX_EVIDENCE names only two of the three stale cells.\n\n(1) The cell is present at HEAD and in the working tree. `docs/phase-1/phase-1-28/task-matrix-verdicts.json:185`, key FE-007, TENANT_ISOLATION_REQUIREMENT reads: \"Branch pair as the operation's required field, never a client claim; the employee search travels as an encoded parameter of a POST-backed Server Action and never reaches the address bar (`support-api.ts` (`listReceivingEmployeeCandidates`, a POST-backed Server Action)). No tenant on any request (`p1-28-qa.test.ts:924-939`).\" `git show HEAD:docs/phase-1/phase-1-28/task-matrix-verdicts.json` prints the identical string, so it is not a working-tree artefact; the GENERATED `docs/phase-1/phase-1-28/task-matrix.json` carries the same string verbatim, so `npm run matrix:p1-28` will not correct it — the editable source is the verdicts file.\n\n(2) Per-key diff of FE-007 (node, HEAD vs working tree) reproduces the finder's exactly: CHANGED BACKEND_READ_CONTRACTS, CHANGED REQUIRED_PERMISSIONS, CHANGED VERDICT_RATIONALE, SAME TENANT_ISOLATION_REQUIREMENT (24 other keys SAME).\n\n(3) The server admits no search term. `apps/api/src/app/api/v1/reception-catalogue/receiving-employees/route.ts:47-54` ",
      "STATUS": "CLOSED",
      "FIX_EVIDENCE": "FE-007’s isolation cell no longer describes an employee search. `rec.receiving-employee-list` takes the branch pair and a cursor, its schema is `.strict()`, and no operator text crosses the wire on that surface."
    },
    {
      "FINDING_ID": "R2-15",
      "CLASS": "TEST_DEFECT",
      "SURFACE": "apps/web/tests/p1-28-reception-routes.test.ts:299",
      "TITLE": "The corrected FE-021 read cell cites a test whose title asserts the directory read the same sentence says does not exist, and the assertion cannot fail",
      "REPRODUCER": "FE-021 BACKEND_READ_CONTRACTS — the one cell FR-32 rewrote — ends: \"…and NO directory read for the receiving employee … and the directory read is not spent on a visit that does not exist (\\\"does not spend the directory read on a visit that does not exist\\\")\". The cited case is `apps/web/tests/p1-28-reception-routes.test.ts:299-304`: it grants `[RECEPTION_PERMISSIONS.read, USER_DIRECTORY_PERMISSION]`, forces `readReception` to `not-found`, awaits `AcknowledgementPage`, and asserts `expect(readUserIdentity).not.toHaveBeenCalled()`. The page cannot call it under ANY input: `grep -c readUserIdentity` on the acknowledgement page source = 0, and its full import list (lines 1-24) imports nothing from `@/features/receptions/people`. So the not-found fixture is decorative — the assertion holds identically when the visit IS found, which the file already asserts unconditionally at `:259-263` (`cons",
      "FAILURE_SCENARIO": "The corrected cell contradicts itself in one sentence — no directory read exists, and here is the test proving the directory read is not spent — and the test it names cannot distinguish the two worlds it appears to separate. A reviewer checking that FR-32 was really discharged reads a case title that asserts a conditional efficiency property of a read that was removed, finds it green, and concludes the read is present but well-behaved. The genuine regression guard is the unconditional pair at :253-266; this case adds a false one beside it, and it is the one the matrix quotes.",
      "REFUTER_WHY": "CONFIRMED — reproduced independently, and the defect is larger than the finder stated.\n\n1) The read really is gone from the page.\n`grep -c \"readUserIdentity\" 'apps/web/src/app/[locale]/(dashboard)/receptions/check-in/[receptionId]/acknowledgement/page.tsx'` -> `0` (rc=1). Its full import list (:1-24) pulls `readReception`, `listPartyRoles`, `listAuthorizations`, `listConditionEvidence` from `@/features/receptions/api` and nothing from `@/features/receptions/people` or `support-api`. `grep -rn \"readUserIdentity\" apps/web/src` returns exactly three lines, all in the WIZARD chain (`components/steps/EvidencePanels.tsx:33,188` and the definition at `support-api.ts:180`). `USER_DIRECTORY_PERMISSION` is resolved only at `check-in/[receptionId]/page.tsx:17,161`. The page states the absence in a comment inside the `Promise.all` at :113-119.\n\n2) The cited case's fixture is decorative — proved by running it, not by reading it.\nI wrote a throwaway probe (`apps/web/tests/zz-refuter-probe-fr32.test.ts`, since DELETED) that reuses the same session/api/support-api mocks and the same granted pair `[RECEPTION_PERMISSIONS.read, USER_DIRECTORY_PERMISSION]`, then drives `AcknowledgementPage` once per reachable `readReception` outcome — `ok` (visit IS found), `not-found`, `denied`, `expired`, `error` — asserting `expect(readUserIdentity).not.toHaveBeenCalled()` each time. `npx vitest run tests/zz-re",
      "STATUS": "CLOSED",
      "FIX_EVIDENCE": "The route case is rewritten from \"does not spend the directory read on a visit that does not exist\" — which could not fail — to \"makes NO directory read at all, on any input\", driving four permission/detail combinations AND reading the route source. Renaming the snapshot field turns it RED."
    },
    {
      "FINDING_ID": "R2-16",
      "CLASS": "DOCUMENTATION_DEFECT",
      "SURFACE": "docs/phase-1/phase-1-27/deliverable-manifest.md:396",
      "TITLE": "The FR-20 repair changed line 300 of the deliverable manifest to RESOLVED and left line 396 of the same file calling P1-OD-025 an OPEN decision with MEDIA_STATUS live",
      "REPRODUCER": "FR-20's REPRODUCER named both lines: \"docs/phase-1/phase-1-27/deliverable-manifest.md:300 … and :396\". `git diff -U0` on the file shows exactly one MEDIA_STATUS hunk, at :300, rewritten to \"Held `MEDIA_STATUS = 'blocked-on-p1-od-025'` while **`P1-OD-025`** was open; the decision is RESOLVED and both constants are now a tombstone in that file\". `sed -n '396p'` on the same file still prints: \"**`P1-OD-025` — vehicle document and media file policy — is an OPEN Owner decision.** There is no vehicle media operation in the platform at all. `MEDIA_STATUS` is `'blocked-on-p1-od-025'`, not a feature flag: a flag implies something to switch on.\" Both constants are gone from the code — `git diff apps/web/src/features/vehicles/documents-contract.ts` removes `export const MEDIA_STATUS` and `export const MEDIA_BLOCKING_DECISION`, and `apps/web/tests/p1-27-security.test.ts:899` now asserts their ABSENC",
      "FAILURE_SCENARIO": "One document now states both readings 96 lines apart: line 300 says the decision is RESOLVED and the constants are a tombstone, line 396 says the decision is OPEN and the constant is live and explains why it is not a feature flag. FIX_EVIDENCE for FR-20 asserts \"the two P1-27 records this wave edited no longer present MEDIA_STATUS as a live constant\" — which is false of the very file it names. A reader is told to grep a constant that no longer exists, in the manifest that enumerates the phase's deliverables.",
      "REFUTER_WHY": "CONFIRMED, reproduced independently. This is a DOCUMENTATION defect, and I say that plainly — but it clears the pass's bar because the prose states a rule the code does not implement, in three separate ways, and one of the three was made false by this wave's own code change.\n\nWHAT I RAN (repo root, branch feature/p1-28-owner-decisions-frontend, HEAD 366a956b, uncommitted working tree).\n\n1. THE CONTRADICTION IS EXACTLY WHERE CLAIMED. `sed -n '300p' docs/phase-1/phase-1-27/deliverable-manifest.md` prints \"| `documents-contract.ts` | The document reference shape … Held `MEDIA_STATUS = 'blocked-on-p1-od-025'` while **`P1-OD-025`** was open; the decision is RESOLVED and both constants are now a tombstone in that file |\". `sed -n '396p'` on the same file prints \"| `no-upload-path` | Seven constructs: `new FormData(` …, `multipart/form-data`, a file input in any spelling, `FileReader`, an `input.files` list, an `onDrop=`/`onDragOver=` target, a `DataTransfer` | **`P1-OD-025` — vehicle document and media file policy — is an OPEN Owner decision.** There is no vehicle media operation in the platform at all. `MEDIA_STATUS` is `'blocked-on-p1-od-025'`, not a feature flag: a flag implies something to switch on. …|\". Both readings, 96 lines apart, in one document.\n\n2. THE REPAIR TOUCHED ONE OF THE TWO LINES FR-20 NAMED. `git diff -U0 -- docs/phase-1/phase-1-27/deliverable-manifest.md` yields",
      "STATUS": "CLOSED",
      "FIX_EVIDENCE": "The deliverable manifest’s three remaining OPEN claims are brought level with line 300. Each now states the SCOPE: `P1-OD-025` is resolved for reception evidence and open for the trees these rules scan, which is why the rules are unchanged."
    },
    {
      "FINDING_ID": "R2-17",
      "CLASS": "DOCUMENTATION_DEFECT",
      "SURFACE": "docs/product/workshop/frontend-implementation-program.md:156",
      "TITLE": "Marking P1-OD-025 RESOLVED at programme line 267 left lines 62 and 156 of the same programme declaring it OPEN — and 156 is the entry-criteria table the P1-28 matrix generator cites",
      "REPRODUCER": "`git diff docs/product/workshop/frontend-implementation-program.md` shows one hunk, at :264-279, replacing \"`P1-OD-017` and `P1-OD-025` both bind … `MEDIA_STATUS` is `'blocked-on-p1-od-025'`\" with \"**`P1-OD-017` binds; `P1-OD-025` is RESOLVED.**\". `grep -n P1-OD-025` on the same file then prints, unchanged: `:62` \"**`P1-OD-017`** … and **`P1-OD-025`** (document and media policy) are **OPEN** Owner decisions\"; `:156` universal entry criterion U5 — \"Every open Owner decision the package touches is named in the package's own plan … `P1-OD-017` and `P1-OD-025` are open.\"; and `:527`, `:530`, `:599`, `:871`, `:912` in the same present tense. The U5 row matters mechanically: `scripts/ci/build-p1-28-task-matrix.mjs:12-14` names \"docs/product/workshop/frontend-implementation-program.md:152\" — U1 of that same table — as the rule P1-28 was built to, and the generator derives all 35 rows from it. `",
      "FAILURE_SCENARIO": "The programme is the live product authority — the document the phase's own CI generator cites by line number as the rule it implements. It now answers \"is P1-OD-025 open?\" both ways within 200 lines, and the OPEN half sits in the universal entry-criteria table that every future package is graded against. A package author reading U5 names a decision as open in their plan and withholds the capability; the same document 111 lines later tells them the decision is taken and private versioned evidence ships. The edit that created the split is this wave's.",
      "REFUTER_WHY": "CONFIRMED as a documentation defect — reproduced independently at `feature/p1-28-owner-decisions-frontend`, HEAD `366a956b`, wave uncommitted. It qualifies under this pass's own bar because the surviving prose states normative rules and checkable facts that the shipped code and the live acceptance database contradict. But the finder's central mechanical argument is FALSE, and I set out the correct one below.\n\nWHAT I RAN.\n\n1. THE SPLIT IS REAL AND IT IS ONE HUNK. `git diff -U0 docs/product/workshop/frontend-implementation-program.md | grep '^@@'` → exactly one hunk, `@@ -267,5 +267,14 @@`, replacing \"`P1-OD-017` and `P1-OD-025` both bind … `MEDIA_STATUS` is `'blocked-on-p1-od-025'`\" with \"**`P1-OD-017` binds; `P1-OD-025` is RESOLVED.**\" plus nine lines of narration. `git diff --stat` → 14 insertions, 5 deletions, 1 file. `git show HEAD:…program.md | awk 'NR==62||NR==156'` returns the two rows byte-identical to the working tree, so neither was touched:\n  `:62` \"| **`P1-OD-017`** (duplicate and merge rules) and **`P1-OD-025`** (document and media policy) are **OPEN** Owner decisions.\"\n  `:156` \"| U5 | **Every open Owner decision the package touches is named in the package's own plan**, with the capability it blocks stated as pending. `P1-OD-017` and `P1-OD-025` are open.\"\n`grep -n P1-OD-025` on the file returns, in the same present tense, `:62 :156 :199 :338 :527 :530 :599 :871 :9",
      "STATUS": "CLOSED",
      "FIX_EVIDENCE": "Programme rows 62 and 156 carry the scope that row 267 already had. The WFP-16 entry criteria are qualified rather than reversed — that package is vehicle and general documents, for which no category exists."
    },
    {
      "FINDING_ID": "R2-18",
      "CLASS": "TRACEABILITY_DEFECT",
      "SURFACE": "docs/phase-1/phase-1-28/task-matrix-verdicts.json:200",
      "TITLE": "FE-007 still records that receiving_employee_id has no foreign key and no referent, contradicting the same wave's canonical-plan edit and the live database",
      "REPRODUCER": "FE-007 FINDING_IDS (SAME as HEAD) reads: \"`G-EMP` OPEN (R6; Owner register question A) … STILL OPEN: `rec.reception_visits.receiving_employee_id` is NOT NULL with no foreign key and no defined referent (`supabase/migrations/20260721097000_rec_reception_visits.sql:62`), the employee master never shipped, and nothing defines that an `iam.user` id is a legal value there.\" Three sources at this head say the opposite. (1) The migration series: `grep -rn fk_reception_visits_receiving_employee supabase/migrations/` -> `20260815093000_rec_receiving_employee_identity.sql:82  ADD CONSTRAINT fk_reception_visits_receiving_employee`, with `tg_reception_visits_receiving_employee` at `:201`. (2) The LIVE acceptance database: `docker exec supabase_db_RootLco psql -U postgres -d postgres -tAc \"select conname, confrelid::regclass from pg_constraint where conname='fk_reception_visits_receiving_employee';\"`",
      "FAILURE_SCENARIO": "FE-007's verdict is PASS, and the cell that records what is still open says the phase's headline gap — a NOT NULL column with no referent — is unresolved. The wave closed that gap in the canonical plan, in the rationale and in production evidence, and left the finding register saying it is live. At the seal, a reader reconciling the matrix against the plan finds the plan says G-EMP is CLOSED and the matrix says G-EMP is OPEN, on the one task both are about; whichever they trust, one of the two artefacts the closure decision reads is wrong about the database.",
      "REFUTER_WHY": "CONFIRMED — reproduced independently at HEAD 366a956b, working tree, against the live acceptance database. (1) THE TEXT IS THERE AND IS UNCHANGED. `sed -n '190,215p' docs/phase-1/phase-1-28/task-matrix-verdicts.json` shows line 200 = FE-007 FINDING_IDS ending \"STILL OPEN: `rec.reception_visits.receiving_employee_id` is NOT NULL with no foreign key and no defined referent … nothing defines that an `iam.user` id is a legal value there\", with FINAL_VERDICT \"PASS\" on the same row. (2) THE MIGRATION SERIES SAYS OTHERWISE. `grep -rn fk_reception_visits_receiving_employee supabase/migrations/` -> 20260815093000_rec_receiving_employee_identity.sql:82; `git cat-file -e HEAD:supabase/migrations/20260815093000_rec_receiving_employee_identity.sql` succeeds, so it is committed at HEAD (fca7d42a), not a working-tree artefact. (3) THE LIVE DATABASE SAYS OTHERWISE. `docker exec supabase_db_RootLco psql -U postgres -d postgres -tAc \"select conname, convalidated, confrelid::regclass, pg_get_constraintdef(oid) from pg_constraint where conname='fk_reception_visits_receiving_employee';\"` -> `fk_reception_visits_receiving_employee|t|iam.user_accounts|FOREIGN KEY (tenant_id, receiving_employee_id) REFERENCES iam.user_accounts(tenant_id, id) ON DELETE RESTRICT` — present and VALIDATED. `select version from supabase_migrations.schema_migrations` lists 20260815093000, so it is the shipped series and not",
      "STATUS": "CLOSED",
      "FIX_EVIDENCE": "FE-007’s finding register records `G-EMP` CLOSED on both halves, naming the foreign key, the eligibility trigger and the canonical operation, and no longer contradicts the plan."
    },
    {
      "FINDING_ID": "R2-19",
      "CLASS": "DOCUMENTATION_DEFECT",
      "SURFACE": "apps/web/tests/p1-28-reception-media.test.ts:1031",
      "TITLE": "A live test still states MAX_FILE_NAME mirrors shared.documents.file_name, three days after the source docblock was corrected to say that column does not exist",
      "REPRODUCER": "FR-15's REPRODUCER named two locations — the constant's docblock and \"apps/web/tests/p1-28-reception-media.test.ts:1031\". Only the first was fixed. `apps/web/src/features/attachments/attachments-contract.ts:161-173` now reads \"This cited `shared.documents.file_name` as the authority. That column does not exist: a search of the whole `supabase/` tree for `file_name` returns nothing, and `CREATE TABLE shared.documents` has no such field\". The test comment at `:1031-1032`, inside the executing case `it('and the one thing the rule WOULD flag there is a file-NAME bound, not a media limit', …)`, still reads \"`MAX_FILE_NAME` matches it — a mirror of `shared.documents.file_name`, the column bound the upload-authorization route itself parses with.\" Both are true of the tree right now: repo-wide `shared.documents.file_name` appears in exactly three live places — that test comment, the correction i",
      "FAILURE_SCENARIO": "The tree now carries the correction and the error it corrects, in files edited by the same wave, and the error is the copy inside a running test — the more authoritative-looking of the two, because a green assertion sits three lines below it. Anyone re-deriving the 400 bound follows the test comment to a database column, searches `supabase/` for `file_name`, gets nothing, and lands one hop away on `MAX_FILE_NAME_LENGTH = 200` in `storage-key.ts:125` — the truncation the stored title really gets. 200 is half the request bound and the more attractive wrong answer.",
      "REFUTER_WHY": "REPRODUCED — and it is an INCOMPLETE CLOSURE of ledger row FR-15, not a fresh wart. Stated plainly: this is DOCUMENTATION. It qualifies under the bar because the prose states a derivation the code does not and cannot implement, it sits inside a green executing test, and the same tree three files away now carries the opposite statement.\n\nFR-15's own TITLE in the frozen ledger reads \"MAX_FILE_NAME is documented as mirroring shared.documents.file_name, a column that exists in no migration — AND A TEST REPEATS THE CLAIM\", and its REPRODUCER names the second location by file and line (\"The claim is repeated at apps/web/tests/p1-28-reception-media.test.ts:1031\"). The row's FIX_EVIDENCE addresses only the constant: \"MAX_FILE_NAME no longer cites shared.documents.file_name … It names the request schema the upload-authorization route parses, which attachments-contract.test.ts reads it against.\" The scope the row itself declared was two locations; one was fixed.\n\nEVERY STEP RUN INDEPENDENTLY, from the repo root unless noted:\n\n1. The false copy is live and unedited.\n   grep -n \"shared.documents.file_name\" apps/web/tests/p1-28-reception-media.test.ts\n     -> 1031:     * and `MAX_FILE_NAME` matches it — a mirror of `shared.documents.file_name`,\n   sed -n '1028,1034p' shows it is a block comment INSIDE the executing case `it('and the one thing the rule WOULD flag there is a file-NAME bound, ",
      "STATUS": "CLOSED",
      "FIX_EVIDENCE": "The live test comment no longer cites `shared.documents.file_name` — a column that does not exist. It names the request schema the upload-authorization route really parses."
    },
    {
      "FINDING_ID": "R2-20",
      "CLASS": "TRACEABILITY_DEFECT",
      "SURFACE": "docs/phase-1/phase-1-27/task-matrix-verdicts.json:645",
      "TITLE": "A citation repair moved FE-025's mutation proof from the case it correctly named to a different case",
      "REPRODUCER": "The per-key diff of the P1-27 verdicts against HEAD shows exactly two changed cells, one of them FE-025.NEGATIVE_OR_MUTATION_PROOF: `form-reset-class.test.ts:419-448` -> `:649-678`. At HEAD the OLD citation was RIGHT: `git show HEAD:apps/web/tests/form-reset-class.test.ts | awk 'NR>=419&&NR<=448'` prints `419:  it('requires every in-form CustomerSelector to be told the attempt', () => {` through `448: expect(sites, 'a CustomerSelector inside a form with no `attempt` prop').toEqual([]);` — precisely the scan the cell describes. The wave grew that file and moved the case: `grep -n \"requires every in-form CustomerSelector\" apps/web/tests/form-reset-class.test.ts` -> `844`, and the assertion runs to `:873`. The NEW citation `:649-678` lands elsewhere entirely — `awk 'NR>=649&&NR<=660'` shows the tail of one coverage assertion and the opening of `it('reaches the files the first version of thi",
      "FAILURE_SCENARIO": "This is the only P1-27 line-number repair the wave made outside SEC-002, and it broke a citation that was correct before the wave touched it. FE-025's mutation proof is the record that F-03 — three CustomerSelector call sites silently losing their reset key — is enforced by a whole-tree scan; the cell now points a verifier at a case that scans directories and says nothing about `attempt`. A verifier reading :649-678 concludes the enforcement claim is unsupported and either reopens F-03 or, worse, accepts the wrong case as the proof.",
      "REFUTER_WHY": "CONFIRMED as a RECORD/TRACEABILITY defect (I say plainly: this is documentation — the product behaviour it describes is genuinely enforced; the pointer to the enforcement is wrong). Reproduced independently, nothing taken on the finder's word. No file created or deleted; two existing suites run read-only.\n\n1. THE DIFF IS EXACTLY WHAT THE FINDER SAYS. `git diff -U0 docs/phase-1/phase-1-27/task-matrix-verdicts.json` -> two changed lines only: `@@ -645 +645 @@` and `@@ -785 +785 @@`. Mapping each to its enclosing top-level key (node scan for `^  \"([A-Z]+-[0-9]+)\": {`) gives `line645 -> FE-025` and `line785 -> SEC-002`. The FE-025 change is exactly `form-reset-class.test.ts:419-448` -> `:649-678`, inside NEGATIVE_OR_MUTATION_PROOF, in the sentence \"`F-03` FIXED: all three `CustomerSelector` call sites of this phase pass `attempt`, enforced by ... — a scan of every call site in the tree\".\n\n2. THE OLD CITATION WAS THE RIGHT CASE AT HEAD. `git show HEAD:apps/web/tests/form-reset-class.test.ts | awk 'NR>=419&&NR<=448'` prints `419:   it('requires every in-form CustomerSelector to be told the attempt', () => {` ... `448:     expect(sites, 'a CustomerSelector inside a form with no `attempt` prop').toEqual([]);` — the call-site scan, verbatim, exactly the scan the cell describes.\n\n3. THE NEW CITATION IS A DIFFERENT CASE. Case boundaries in the working tree (`grep -n \"^  it(\" apps/web/test",
      "STATUS": "CLOSED",
      "FIX_EVIDENCE": "FE-025’s mutation proof cites the case by NAME and by its current line, and records that the citation has now been wrong in both directions — right, \"repaired\" to a directory scan, and re-derived here."
    },
    {
      "FINDING_ID": "R2-21",
      "CLASS": "DOCUMENTATION_DEFECT",
      "SURFACE": "apps/web/src/features/vehicles/components/VehicleAttributeHistorySection.tsx:68",
      "TITLE": "The new ledger docblock says the product does not authorise this screen to make catalogue reads; the five catalogue reads require the permission that already gates the page",
      "REPRODUCER": "The FR-18 repair added this docblock (`git diff` on the file shows lines 63-73 as new). It reads: \"there is no name to show. Resolving one would mean this ledger issuing catalogue reads THE PRODUCT DOES NOT AUTHORISE IT TO MAKE, on behalf of a screen that is only reporting history.\" The five reads exist and cost exactly what the page already holds: `apps/api/src/app/api/v1/vehicle-catalogue/makes/route.ts` declares `id: 'veh.catalogue-make-list'`, `permissions: ['veh.vehicle.read']`; `apps/web/src/features/vehicles/catalogue-api.ts:13-23` tabulates all five and states \"All `veh.vehicle.read`\". The page that mounts this section gates on precisely that code — `apps/web/src/app/[locale]/(dashboard)/vehicles/[vehicleId]/page.tsx:68` `if (!holds(session.permissions, VEHICLE_PERMISSIONS.vehicleRead))`. Two shipped pages already call them: `vehicles/new/page.tsx:59` and `vehicles/page.tsx:73` (",
      "FAILURE_SCENARIO": "The docblock is the record of WHY the ledger prints \"Changed to a different catalogue entry\" instead of a make name, and its stated reason is an authorisation limit that does not exist. The next agent asked to improve the ledger reads that resolving a name is forbidden, when the capability is the one every reader of the page already holds and two sibling pages already spend. The behaviour shipped is defensible on other grounds — no display name travels with the row, and a per-row catalogue walk on a history table is expensive — but the reason recorded is false, so the decision cannot be re-exa",
      "REFUTER_WHY": "CONFIRMED — reproduced independently against the running production stack and against the shipped source. This is a DOCUMENTATION defect and I say so plainly: no rendered pixel changes. But it counts under the brief's rule, because the docblock states a PROHIBITION the code does not implement, and it is the record of why the ledger prints \"Changed to a different catalogue entry\".\n\nTHE TEXT, AND THAT IT IS NEW\n`grep -n` on apps/web/src/features/vehicles/components/VehicleAttributeHistorySection.tsx, lines 63-73:\n  63:/**\n  64: * The five columns holding a uuid.\n  66: * The history read model publishes `fieldCode`, `oldValue` and `newValue` and\n  67: * nothing else - no display name travels with the row - so there is no name to\n  68: * show. Resolving one would mean this ledger issuing catalogue reads the\n  69: * product does not authorise it to make, on behalf of a screen that is only\n  70: * reporting history. ...\n`git diff -- apps/web/src/features/vehicles/components/VehicleAttributeHistorySection.tsx` shows every one of those lines with a leading `+`, inside the same hunk that adds `CATALOGUE_REFERENCE`. So it is the FR-18 repair's own record, written in this uncommitted tree.\n\nRUNTIME REPRODUCTION (the part the finder never did)\nBoth tiers are up. I signed in through the shipped operation `iam.auth-login` at http://localhost:3000 with the local acceptance account (`.local/ow",
      "STATUS": "CLOSED",
      "FIX_EVIDENCE": "The ledger docblock no longer claims an authorisation limit that does not exist. All five catalogue reads cost `veh.vehicle.read`, which already gates the page; the real reason is that a catalogue read answers what an identifier means NOW and a history row is about the past."
    },
    {
      "FINDING_ID": "R2-22",
      "CLASS": "TRACEABILITY_DEFECT",
      "SURFACE": "docs/phase-1/phase-1-27/task-matrix-verdicts.json:783",
      "TITLE": "SEC-002's read cell was re-derived to the right lines and its three sibling citations left pointing at unrelated cases",
      "REPRODUCER": "The per-key diff shows the wave re-derived SEC-002.BACKEND_READ_CONTRACTS from `p1-27-security.test.ts:505-512,536-545` to `:789-797,826-836`, and the new numbers are correct — `awk 'NR>=789&&NR<=797'` prints `it('confirms the export operations exist, so their absence here is a decision', …)` and `awk 'NR>=826&&NR<=836'` prints the download-authorize anti-vacuity case. Three siblings in the same row were not re-derived and none of them lands: IMPLEMENTATION_SURFACES cites `:673-693` for \"Five conjuncts named as data\" — that range is `isForbiddenUrlKey`/`carriableSearchParams` assertions and the start of 'calls no export operation anywhere in this phase'; the conjunct data is `const SEC_002_PARTS = [...]` at `:719`, under the docblock at `:708-730` and the describe at `:731`. REQUIRED_PERMISSIONS cites `:524-527` for \"asserted not to start with `veh.`\"; that range is `expect(() => query({",
      "FAILURE_SCENARIO": "The wave demonstrably knew the coordinates in this row had moved — it recomputed one of the four — and stopped. SEC-002 is a conjunction graded one conjunct at a time, and the cell that names where the five conjuncts are declared as data (the whole mechanism that stops a conjunct being dropped by deleting a `describe`) points at a URL-key deny-list. A verifier opening `:673-693` to confirm the conjunction is still complete sees no conjunction at all, and the permission cell's `veh.` guard — the assertion that stops the documents tab being reopened to every reader by a copy-paste — is 289 lines",
      "REFUTER_WHY": "CONFIRMED — reproduced independently, and the finding is larger than claimed.\n\nLEG 1 (the wave re-derived one of four). `git diff HEAD -- docs/phase-1/phase-1-27/task-matrix-verdicts.json` is 22 lines for the whole file, two changed cells. In SEC-002 the sole change is BACKEND_READ_CONTRACTS `p1-27-security.test.ts:505-512,536-545` -> `:789-797,826-836`. The new numbers land: `awk 'NR>=789&&NR<=797' apps/web/tests/p1-27-security.test.ts` prints `it('confirms the export operations exist, so their absence here is a decision', ...)`; `awk 'NR>=826&&NR<=836'` prints `it('constructs no download URL and prefetches no download authorization', ...)`. The old ones did not land even at HEAD (`:505-512` = tail of a call-site toEqual list; `:536-545` = branchTargetQuery).\n\nLEG 2 (the three siblings miss). IMPLEMENTATION_SURFACES `:673-693` prints `expect(isForbiddenUrlKey('search')).toBe(true)` plus the openings of 'calls no export operation anywhere in this phase' and 'offers no upload path of any kind'; the conjunct data is `const SEC_002_PARTS = ['sensitive-data','export','document','media','file-access'] as const;` at :719, docblock :711-718, describe :731. REQUIRED_PERMISSIONS `:524-527` prints `expect(() => query({ tenantId: 't1', name: 'ali' })).toThrow(/tenantId/)` and siblings; the guard `expect(DOCUMENT_LIST_PERMISSION.startsWith('veh.')).toBe(false)` is at :816. BACKEND_WRITE_CO",
      "STATUS": "CLOSED",
      "FIX_EVIDENCE": "SEC-002’s three sibling citations are re-derived: the conjuncts are declared as data at `:719-739` (with the case that pins them), and the case counts are the measured ones."
    },
    {
      "FINDING_ID": "R2-23",
      "CLASS": "DOCUMENTATION_DEFECT",
      "SURFACE": "apps/web/src/features/attachments/attachments-contract.ts:5",
      "TITLE": "The attachments contract header still claims every value is Backend-derived and fails when it moves, while the FR-17 fix 145 lines below says MAX_CAPTURED_AT mirrors no route",
      "REPRODUCER": "FR-17's REFUTER_WHY point 5 named this exact residue. The header at `:4-7` still reads \"Every value here is a copy of something the Backend governs, and copies drift. `apps/web/tests/attachments-contract.test.ts` derives each one and fails when it moves\", and its enumeration \"What governs each value is:\" (`:14-23`) lists three bullets for five exported values, omitting `MAX_CAPTURED_AT`. The FR-17 fix at `:146-159` now states the opposite of the header: \"It mirrors NO route, and the sentence here said it did.\" The test does not fail when the value moves: `tests/attachments-contract.test.ts:155-166` asserts only `expect(MAX_CAPTURED_AT).toBeGreaterThanOrEqual(new Date().toISOString().length)` and `toBeLessThanOrEqual(MAX_FILE_NAME)`. Replicated in the scratchpad: `new Date().toISOString()` = `2026-08-18T19:43:52.170Z`, length 24; the two assertions admit 377 distinct values, 24..400; movi",
      "FAILURE_SCENARIO": "The module now asserts a universal derivation rule in its header and refutes it in the docblock of one of the five values it governs, with the test agreeing with the docblock and not the header. The wave rewrote this header specifically to purge a false derivation citation, so the surviving \"Every value … fails when it moves\" reads as freshly audited. A reader trusting it treats `MAX_CAPTURED_AT` as pinned by CI and moves it — anywhere in 24..400 — and CI does not notice; and the property the fixed docblock says is checked, that an offset-bearing instant fits, is checked nowhere.",
      "REFUTER_WHY": "CONFIRMED as a DOCUMENTATION defect, and I say plainly that it is documentation: no runtime behaviour is wrong. It qualifies under the stated rule because the module header asserts a rule the code does not implement, and I proved the non-implementation by MUTATION of the shipped constant against the real, unmodified test file — not by re-reading prose. All commands from repo root unless noted; branch `feature/p1-28-owner-decisions-frontend`, HEAD `366a956b`, working tree uncommitted.\n\n1. THE TEXT (reproduced verbatim, not paraphrased).\n`grep -n` on `apps/web/src/features/attachments/attachments-contract.ts`:\n  :4-5  ` * Every value here is a copy of something the Backend governs, and copies` / ` * drift. \\`apps/web/tests/attachments-contract.test.ts\\` derives each one and`\n  :6    ` * fails when it moves, ...`\n  :14-23 the enumeration `What governs each value is:` — exactly three bullets (version vocabulary / scan vocabulary / file-name bound).\n  :149  ` * It mirrors NO route, and the sentence here said it did.`\n  :155-156 ` * ISO instant with milliseconds and an offset fits. \\`attachments-contract.test.ts\\`` / ` * checks it as that property ...`\n  :159  `export const MAX_CAPTURED_AT = 64;`\nLine 149 is 145 lines below line 4 — the finder's \"145 lines below\" is arithmetically right. `git diff apps/web/src/features/attachments/attachments-contract.ts` shows ONE hunk (`@@ -1,11 +1",
      "STATUS": "CLOSED",
      "FIX_EVIDENCE": "The attachments-contract header no longer claims every value is Backend-derived. `MAX_CAPTURED_AT` mirrors no route and the header says so, and the enumeration lists every value rather than three of five."
    },
    {
      "FINDING_ID": "R2-24",
      "CLASS": "TEST_DEFECT",
      "SURFACE": "apps/web/tests/p1-28-reception-media.dom.test.tsx:1019",
      "TITLE": "The DOM outcome table is still FOUR after the wave took the outcome line to eight: it spends a fixture on `boundPending`, which the chain provably cannot return, and has none for `capturedNotBound`, which has zero assertions anywhere in the repository",
      "REPRODUCER": "(1) COVERAGE — `Grep 'capturedNotBound|boundNotCounted|overrideRecorded|overrideFailed'` over the whole repository returns, outside `src/` and the ledger: `boundNotCounted` at dom:808, `overrideRecorded` at dom:839/866, `overrideFailed` at dom:891, and `capturedNotBound` NOWHERE. The key added by this wave for stage `captured` is asserted by no test in the tree.\n\n(2) THE TABLE — `OUTCOME_BRANCHES` at :1019-1041 holds four rows (`finalized`, `boundPending`, `boundNoScanner`, `failed`); the guard at :1043 is titled 'has four sentences to choose between, and they are four' and asserts `expect(new Set(sentences).size).toBe(4)` over its own four-element list; the docblock at :1012 says \"The four outcomes, and the sentence each one earns\". `outcomeKey` now selects between EIGHT keys.\n\n(3) THE `boundPending` FIXTURE IS A SHAPE THE ACTION CANNOT RETURN — probe `apps/web/tests/zzlens-chain-enumer",
      "FAILURE_SCENARIO": "The suite whose declared job is \"the four outcomes, and the sentence each one earns\" certifies a sentence no operator can ever be shown and does not certify the one two operators were shown. Delete `if (outcome.stage === 'captured') return 'receptions.capture.capturedNotBound';` from MediaStep.tsx:266 and every capture that meets a refused scan or a failed bind silently reverts to \"Nothing was recorded.\" — the exact sentence FR-07 was raised to remove — with the whole web tier green, because nothing anywhere names that key. A reviewer reading `.size).toBe(4)` beside a title saying \"they are fo",
      "REFUTER_WHY": "CONFIRMED — reproduced independently with my own probes (all written under apps/web/tests, run, and DELETED; `git status --porcelain | grep zzrf` is empty and MediaStep.tsx:266 is untouched).\n\n1) THE MAPPING IS EIGHT, THE TABLE IS FOUR — read from the shipped source, not inferred. `outcomeKey` (apps/web/src/features/receptions/components/steps/MediaStep.tsx:251-268) selects between overrideRecorded, overrideFailed, finalized, boundNotCounted, boundNoScanner, boundPending, capturedNotBound, failed = 8 keys. `OUTCOME_BRANCHES` (apps/web/tests/p1-28-reception-media.dom.test.tsx:1019-1041) holds 4 rows; the docblock at :1012 reads \"The four outcomes, and the sentence each one earns\"; the guard at :1043 is titled \"has four sentences to choose between, and they are four\" and its assertion at :1052 is `expect(new Set(sentences).size).toBe(4)` computed from `OUTCOME_BRANCHES.map(...)` at :1048 — i.e. over its own four-element list, so it can only prove those four strings differ, never that the mapping is covered. `git diff -- apps/web/tests/p1-28-reception-media.dom.test.tsx` shows no +/- line inside OUTCOME_BRANCHES: the wave added `capturedNotBound` (a `+` line in `git diff -- .../MediaStep.tsx`) and left the table exactly as it was.\n\n2) COVERAGE — `grep -rn \"capturedNotBound|boundNotCounted|overrideRecorded|overrideFailed\" --include=*.ts --include=*.tsx --include=*.json --include=*.",
      "STATUS": "CLOSED",
      "FIX_EVIDENCE": "The DOM outcome table is rebuilt from the states the chain can REACH — six branches plus the two finalized outcomes — with the fixture for `boundPending` gone and `capturedTerminal`, `capturedStored` and `capturedNotBound` each driven."
    },
    {
      "FINDING_ID": "R2-25",
      "CLASS": "ASSURANCE_GATE_DEFECT",
      "SURFACE": "apps/web/tests/form-reset-class.test.ts:206",
      "TITLE": "safeShape() checks that a `key` attribute EXISTS, not that it changes on the attempt — the one half of the three-part shape a select actually depends on",
      "REPRODUCER": "The predicate is presence-only: `const keyed = /\\bkey=/.test(c.head);` (form-reset-class.test.ts:206). Nothing in the file inspects the key EXPRESSION, and the two wiring cases that name an attempt (lines 867 and 900) are also presence-only, `/\\battempt=/`.\n\nI measured what the accepted shape actually does. Probe (deleted; `git status --short | grep zz-probe` now returns nothing) at apps/web/tests/zz-probe-constant-key.dom.test.tsx mounted a real `<form action={…}>` holding the shipped shared controls, three of them carrying a CONSTANT `key` plus `defaultValue`/`defaultChecked` plus `onChange` — the exact conjunction `safeShape()` returns true for — beside one control keyed on `state.attempt`. Values read from FormData and `option.selected` AFTER the settle, never from a cached list.\n  cd apps/web && npx vitest run tests/zz-probe-constant-key.dom.test.tsx --project dom\n  BEFORE {\"varying",
      "FAILURE_SCENARIO": "All 15 guarded selects in the tree currently key on `state.attempt`, so nothing is broken today — the gate is what is broken. Any future edit that spells the key as a constant (`key=\"amount\"`, `key={field.name}`, `attempt={0}`) leaves this suite at 11/11 green while the operator's choice in a `<form action={…}>` is silently replaced by the select's first option on every refused write, with component state still holding the old value and sending it. That is `NEW-FE-01` verbatim — the defect found five separate times by hand, whose recurrence this file exists to make impossible — and the invento",
      "REFUTER_WHY": "CONFIRMED — reproduced independently in four parts, with my own probe, and the mechanism measured rather than cited.\n\n(1) THE PREDICATE IS PRESENCE-ONLY (read). apps/web/tests/form-reset-class.test.ts:205-215:\n  205 function safeShape(c: Control): boolean {\n  206   const keyed = /\\bkey=/.test(c.head);\n  207   const defaulted = /\\bdefault(Value|Checked)=/.test(c.head);\n  209   const changes = /\\bonChange=/.test(c.head) || /\\{\\.\\.\\.\\w+\\}/.test(c.head);\nNothing in the file inspects the key EXPRESSION. The two wiring cases at :867 and :900 are `/\\battempt=/`, also presence-only. `safeShape` has exactly one consumer, the case `protects every one with the safe shape for its kind` (:697), which is `guarded.filter((c) => !safeShape(c))` — so this predicate IS the gate's whole decision.\n\n(2) THE GATE'S OWN DECISION FUNCTION, EXECUTED VERBATIM. I sliced `function safeShape` out of the test file at run time (no retyping, no paraphrase) and evaluated it:\n  cd apps/web && node --input-type=module -e \"<read tests/form-reset-class.test.ts, slice safeShape, new Function(...)>\"\n  VARYING  (shipped CustomerSelector:287)  => safeShape: true\n  CONSTANT string literal key              => safeShape: true\n  CONSTANT key={field.name}                => safeShape: true\n  CONSTANT key={0}                         => safeShape: true\n  /\\battempt=/ on \"attempt={state.attempt}\" true | \"attempt={0}\" true | \"a",
      "STATUS": "CLOSED",
      "FIX_EVIDENCE": "`safeShape` judges the key EXPRESSION against the settlement signals of its own file. Static keys are refused in every spelling; the `attempt` wiring is judged by value, not presence; and `form-reset-runtime.dom.test.tsx` measures the consequence on the shipped controls against a real refusal, with a canary proving the reset ran."
    },
    {
      "FINDING_ID": "R2-26",
      "CLASS": "PRODUCT_DEFECT",
      "SURFACE": "apps/web/src/features/receptions/components/steps/MediaStep.tsx:266",
      "TITLE": "`capturedNotBound` — the ONLY new branch the live acceptance database actually exercised — reports a TERMINAL scan refusal as a provisional \"not yet\", and `outcomeKey` throws away the `versionStatus` that would tell them apart",
      "REPRODUCER": "(1) LIVE DATABASE — the acceptance environment (127.0.0.1:54322, untouched, not reset). `select v.id, v.status, c.category_code, l.entity_type, (select count(*) from rec.reception_evidence_bindings b where b.document_version_id=v.id) as bindings from shared.document_versions v join shared.documents d on d.id=v.document_id join shared.document_categories c on c.id=d.category_id left join shared.document_links l on l.document_id=d.id` returns 8 versions: 6 `accepted`, and TWO `quarantined` in category `reception_dashboard`, linked to `rec.reception_visits`, each with `bindings = 0`. `select scan_status, count(*) from shared.file_scan_results` -> clean 6, infected 2. There are ZERO `pending` versions. So two real captures made against the running app went down this branch, and neither left a binding.\n\n(2) WHY THEY LEFT NO BINDING — `rec.guard_reception_evidence_binding()` (supabase/migratio",
      "FAILURE_SCENARIO": "An operator photographs the dashboard. The scanner reads the object back and refuses it — the version is `quarantined`, which is terminal — so the binding is refused and nothing at all appears in the requirement's list. The screen prints \"The file was stored but not attached to this requirement, so it counts towards **nothing yet**\", the requirement row still reads \"Nothing recorded yet.\", and the capture form is still there. \"yet\" is a promise that the state is provisional: it is not, and re-submitting the same photograph produces the same terminal quarantine for ever. The identical sentence ",
      "REFUTER_WHY": "CONFIRMED — reproduced independently, end to end, including against the running production bundle. Nothing here rests on a docblock.\n\n(1) LIVE DATABASE, untouched, not reset. `docker exec supabase_db_RootLco psql -U postgres -d postgres -c \"select v.id, v.status, c.category_code, l.entity_type, (select count(*) from rec.reception_evidence_bindings b where b.document_version_id=v.id) as bindings from shared.document_versions v join shared.documents d on d.id=v.document_id join shared.document_categories c on c.id=d.category_id left join shared.document_links l on l.document_id=d.id\"` -> 8 rows: 6 `accepted`, 2 `quarantined` in `reception_dashboard` linked to `rec.reception_visits`, both `bindings = 0`. `select scan_status, count(*) from shared.file_scan_results group by 1` -> clean 6, infected 2, zero `pending`. `select count(*) from rec.reception_evidence_bindings` -> 2, both `vin`, both finalized. The claim's DB figures are exact.\n\n(2) THE REFUSAL WAS THE STATUS CLAUSE AND NOTHING ELSE — this is what the finder asserted but did not drive. Both quarantined documents ARE correctly linked: `link_purpose='inspection_media'` equals the category's `business_link_purpose`, `deleted_at IS NULL`, category matches the requirement. So every other clause of `rec.guard_reception_evidence_binding()` passes and only the status clause can fire. Driven for real inside a rolled-back transaction",
      "STATUS": "CLOSED",
      "FIX_EVIDENCE": "`outcomeKey` reads `versionStatus`. A quarantined or rejected version is TERMINAL and says so (\"Record a different file\"); an accepted-but-unattached version and a stored-unchecked one each get their own sentence, and both say \"again\"."
    },
    {
      "FINDING_ID": "R2-27",
      "CLASS": "DEAD_CODE",
      "SURFACE": "apps/web/src/features/receptions/components/steps/MediaStep.tsx:264",
      "TITLE": "`receptions.capture.boundPending` has no producer at all — the branch and its EN+AR strings are dead, and the docblock this wave added names it as one of \"the two ways a bound capture can fail to count\"",
      "REPRODUCER": "Probe `apps/web/tests/zzlens-chain-enumeration.test.ts` (deleted) drove the real `captureRequirementEvidence` over every registration shape the API can emit and asserted `expect(keys.has('boundPending')).toBe(false)` — green. Command: `cd apps/web && npx vitest run --project logic tests/zzlens-chain-enumeration.test.ts`. Output: `[E] reachable keys: boundNoScanner, boundNotCounted, capturedNotBound, finalized`.\nThe derivation, all of it checkable: `registerVersionAndScan` (apps/api/.../attachment-service.ts:590-668) returns `status:'pending'` only from the `!storageProviderCanRead(provider)` early return at :596, which hard-codes `scannerAvailable:false`; every other path calls `completeScan`, and `shared.complete_document_scan` (pg_get_functiondef, read from the live database) returns only `'accepted'` or `'quarantined'`. `rec.guard_reception_evidence_binding()` refuses a bind whose ver",
      "FAILURE_SCENARIO": "This is the sentence FR-02/FR-22 were raised over — \"Recorded. It does not count yet — the file is still being checked.\" The fix correctly removed the waiver's route to it, and left a branch nothing can reach, two catalogue entries (EN and AR) nothing can print, a DOM fixture at :1028 spending one of four slots certifying it, and a static pin at :1155 requiring the literal `return 'receptions.capture.boundPending'` to stay in the source. Stated plainly as documentation: the docblock this wave wrote at MediaStep.tsx:246-249 says \"the two ways a bound capture can fail to count — the scan has not",
      "REFUTER_WHY": "CONFIRMED. I did not take the finder's derivation on trust — I re-derived the emittable set from the API and the live database, then drove the shipped chain and the shipped component myself. `receptions.capture.boundPending` (MediaStep.tsx:264, verified by `grep -n`) has no producer.\n\nLEG 1 — WHAT THE REPORTER ACTUALLY REACHES, BY EXECUTION.\nProbe A (`apps/web/tests/zzrefA-chain.test.ts`, deleted) imported the REAL `captureRequirementEvidence` and mocked only the transport (`@/features/attachments/api`, `@/features/receptions/api`). Command: `cd apps/web && npx vitest run --project logic tests/zzrefA-chain.test.ts` → 7 passed. Output (abridged):\n  [A] accepted+finalizeOK      {\"status\":\"success\",\"stage\":\"finalized\",\"versionStatus\":\"accepted\",\"scannerAvailable\":true}\n  [B] accepted+finalizeDENIED  {\"status\":\"denied\",\"stage\":\"bound\",\"versionStatus\":\"accepted\",\"scannerAvailable\":true}\n  [C] quarantined+bindREFUSED  {\"status\":\"error\",\"stage\":\"captured\",\"versionStatus\":\"quarantined\",\"scannerAvailable\":true}\n  [D] pending+scannerFALSE     {\"status\":\"success\",\"stage\":\"bound\",\"versionStatus\":\"pending\",\"scannerAvailable\":false}\n  [E] pending+scannerTRUE      {\"status\":\"success\",\"stage\":\"bound\",\"versionStatus\":\"pending\",\"scannerAvailable\":true}   (control)\nProbe B (`apps/web/tests/zzrefB-boundpending.dom.test.tsx`, deleted) rendered the REAL `MediaStep` and fed those tuples back through ",
      "STATUS": "CLOSED",
      "FIX_EVIDENCE": "`boundPending` is removed — the branch, both catalogue strings and the docblock claim. The static twin derives the reachable key set from the reader itself and asserts the EXACT set, so the ladder cannot grow or shrink in silence."
    },
    {
      "FINDING_ID": "R2-28",
      "CLASS": "TEST_DEFECT",
      "SURFACE": "apps/web/tests/p1-28-reception-media.test.ts:1142",
      "TITLE": "FR-03's fix took the state pin from four to five and left the sibling outcome pin at four: the DOM-independent twin names none of the wave's four new keys, and BOTH new outcome branches can be deleted with the file green",
      "REPRODUCER": "Probe `apps/web/tests/zzlens-static-blindness.test.ts` (deleted) read the real `MediaStep.tsx`, applied the suite's own `body()`/`GATE.stripComments()` pipeline, and replayed every literal the file pins against a mutant. Command: `cd apps/web && npx vitest run --project logic tests/zzlens-static-blindness.test.ts`. Output:\n  [D] \"boundNotCounted\" occurrences in p1-28-reception-media.test.ts: 0\n  [D] \"capturedNotBound\" occurrences in p1-28-reception-media.test.ts: 0\n  [D] \"overrideRecorded\" occurrences in p1-28-reception-media.test.ts: 0\n  [D] \"overrideFailed\" occurrences in p1-28-reception-media.test.ts: 0\n  [D] \"StepOutcome\" occurrences in p1-28-reception-media.test.ts: 0\n  [D] pinned literals the mutant no longer satisfies: []\n  [D] requirement.recordedCount occurrences in mutant: 1\nThe mutant is the shipped source with BOTH `    if (outcome.status !== 'success') return 'receptions.cap",
      "FAILURE_SCENARIO": "This file's declared purpose is the DOM-independent pin of the capture state model, and FR-03 was confirmed and closed for precisely this shape one case earlier (four states pinned over a five-state ladder). The remediation fixed the ladder case at :1099 and did not touch the outcome case at :1142, so the two product fixes the wave shipped — FR-07's `boundNotCounted` and FR-02's stage-`captured` sentence — are both revertible with this file green. Its comment is now false in both halves: the way it names first (\"the scan has not concluded\", `boundPending`) is unreachable, and the two ways that",
      "REFUTER_WHY": "CONFIRMED as a TEST_DEFECT, reproduced independently with a real vitest run against a real mutated file — not an in-memory replay of the assertions.\n\nINSTRUMENT (no repo file was modified at any point). I mirrored the web workspace into the scratchpad: copied apps/web/{src,tests,package.json,vitest.config.ts,tsconfig.json,next.config.ts,tailwind/postcss/eslint/stylelint/prettier configs,public,scripts}, then created Windows directory JUNCTIONS for node_modules, apps/web/node_modules, scripts, supabase, apps/api, .github.\n  Control, unmutated mirror: `cd <mirror>/apps/web && npx vitest run --project logic tests/p1-28-reception-media.test.ts` -> Test Files 1 passed (1) / Tests 50 passed (50).\n  Repo baseline, same command in the repo -> 50 passed (50). The mirror is a faithful instrument.\n\nREPRODUCTION 1 — the static inventory. `grep -c` over apps/web/tests/p1-28-reception-media.test.ts:\n  boundNotCounted 0 | capturedNotBound 0 | overrideRecorded 0 | overrideFailed 0 | StepOutcome 0 | \"step.kind\" 0.\nThe case at :1142 asserts exactly three source literals — the `stage === 'finalized'` line (:1149-1151), the `scannerAvailable === false` line (:1152-1154) and `return 'receptions.capture.boundPending'` (:1155) — and lists four keys at :1157-1160 (finalized, boundNoScanner, boundPending, failed). `grep -n stage` over the file returns 7 hits, of which exactly one reads the MediaStep bo",
      "STATUS": "CLOSED",
      "FIX_EVIDENCE": "The DOM-independent pin is derived rather than listed: every `receptions.capture.*` key the reader returns is read out of the step and compared with the catalogues. Deleting a branch turns it RED."
    }
  ],
  "refuted": [
    {
      "FINDING_ID": null,
      "CLASS": "DOCUMENTATION_DEFECT",
      "SURFACE": "docs/phase-1/phase-1-28/evidence/final-refuter-ledger.json:855",
      "TITLE": "Ledger proof A asserts the acceptance rows are 'measured before and after every run'; no code in the suite measures them, so the leak above leaves no trace",
      "REPRODUCER": "The frozen ledger's outboxIntegrationBlocker.PROOFS reads: A_foreign_pending_unchanged = '55 acceptance rows measured before and after every run - unchanged.' Grep for what the suite actually measures:\n  grep -n \"foreignEventStates\" tests/backend/outbox-worker.test.ts -> 410 (definition), 821, 841, 880.\n  sed -n '405,418p' tests/backend/outbox-worker.test.ts -> the query is 'SELECT ... FROM shared.event_outbox WHERE tenant_id = $1 ORDER BY id' called with [FOREIGN_TENANT] = 'f0000000-0000-4000-8000-00000000000f'.\nThat tenant holds exactly the two rows plantForeignEvents() inserted (line 821 asserts toHaveLength(2)). There is no query anywhere in the file over tenant c0000000-0000-4000-8000-00000000000c or over 'all other tenants' state', and no before/after comparison of anything except those two rows. I performed the measurement the proof describes myself, externally (86 rows, field-by-",
      "FAILURE_SCENARIO": "A reader of the seal evidence concludes that every run of tests/backend/outbox-worker.test.ts verifies the acceptance tenant's rows are untouched. It does not: it verifies two planted rows under a synthetic tenant. Worse, claim ordering makes those two rows the least likely thing a leak would ever reach - shared.claim_outbox_events orders candidates 'ORDER BY candidate.available_at, candidate.occurred_at, candidate.id' (supabase/migrations/20260718106000_shared_event_outbox.sql:247) and the plant is inserted in beforeAll, so it sorts behind all 86 older acceptance rows, while every batch this ",
      "REFUTER_WHY": "REFUTED. The ledger text is quoted correctly, but both load-bearing halves of the claim fail when driven.\n\nQUOTE VERIFIED. `grep -n A_foreign_pending_unchanged docs/phase-1/phase-1-28/evidence/final-refuter-ledger.json` -> `855:      \"A_foreign_pending_unchanged\": \"55 acceptance rows measured before and after every run - unchanged.\"`\n\n(1) \"No code in the suite measures them\" is FALSE as stated. Three queries in tests/backend/outbox-worker.test.ts span exactly the acceptance tenant's rows, and one of them IS a before/after comparison:\n  - :320-328 `quarantineForeignEvents()` — `SELECT id FROM shared.event_outbox WHERE tenant_id <> $1 AND status IN ('pending','claimed') ... FOR UPDATE`, run in every `beforeEach`; today that enumerates the 86 acceptance rows plus the 2 planted canaries.\n  - :374-383 `claimableElsewhere()` — `count(*) ... WHERE tenant_id <> $1 AND status IN ('pending','claimed')`, i.e. a direct count of the acceptance tenant's state.\n  - :732 `const baseline = await withWorkerTransaction((db) => queueHealth(db))` vs :752-753 `expect(drained.depth).toBe(baseline.depth)`. `queueHealth` (apps/api/src/server/worker/outbox-worker.ts:78-90) is `count(*) FILTER (WHERE status IN ('pending','claimed')) ... FROM shared.event_outbox` with NO tenant filter. Ran that verbatim SQL against the live acceptance DB: `depth=86, acceptance_in_depth=86, dead_letters=0`. Every acceptanc",
      "STATUS": "REFUTED"
    },
    {
      "FINDING_ID": null,
      "CLASS": "TEST_DEFECT",
      "SURFACE": "apps/web/tests/zzr-final-ledger-probe.dom.test.tsx:77",
      "TITLE": "Eight untracked probe files left in apps/web/tests by the remediation's own agents — three of them FAIL, so the web tier is red at this head",
      "REPRODUCER": "`git status --porcelain | grep '^??'` at HEAD 366a956b lists eight probe files under apps/web/tests: zz-probe-formreset-b.dom.test.tsx, zz-probe-formreset-lens.dom.test.tsx, zz-refuter-pagetwo.dom.test.tsx, zzlens-media-outcome.dom.test.tsx, zzr-final-ledger-probe.dom.test.tsx, zzrefx-insp-page2.dom.test.tsx, zzz-refuter-fr06-pagetwo.dom.test.tsx, zzz-refuter-fr08-page2.dom.test.tsx. All eight match the web `dom` project include (apps/web/vitest.config.ts:143, 'tests/**/*.dom.test.tsx'), so all eight execute and count.\n\n`cd apps/web && npx vitest run tests/zz-probe-formreset-b.dom.test.tsx tests/zz-probe-formreset-lens.dom.test.tsx tests/zz-refuter-pagetwo.dom.test.tsx tests/zzlens-media-outcome.dom.test.tsx tests/zzr-final-ledger-probe.dom.test.tsx tests/zzrefx-insp-page2.dom.test.tsx tests/zzz-refuter-fr06-pagetwo.dom.test.tsx tests/zzz-refuter-fr08-page2.dom.test.tsx` ->\n  'Test Files",
      "FAILURE_SCENARIO": "Any web-tier figure recorded for the seal is taken over a corpus of 13 extra tests, and a full apps/web run at this head reports three failures belonging to no deliverable — so either the seal records a red tier and blames the product, or it records a green tier by excluding files nobody has enumerated. A `git add -A` before the commit ships all eight probe files into the branch, where they run in CI and fail there too.",
      "REFUTER_WHY": "REFUTED. Same HEAD as the finder (git rev-parse HEAD -> 366a956be5a1659c905be2e54333d6623dac661f), and not one particular of the claim survives.\n\n1. THE EIGHT FILES DO NOT EXIST. Loop over the finder's exact list -> ABSENT x8 (zz-probe-formreset-b, zz-probe-formreset-lens, zz-refuter-pagetwo, zzlens-media-outcome, zzr-final-ledger-probe, zzrefx-insp-page2, zzz-refuter-fr06-pagetwo, zzz-refuter-fr08-page2). `ls apps/web/tests/zz* apps/web/tests/*refut*` -> \"No such file or directory\". The finding's anchor, apps/web/tests/zzr-final-ledger-probe.dom.test.tsx:77, is a nonexistent line in a nonexistent file, so its \"AssertionError: stored token rendered\" cannot be re-run by me or by anyone sealing this branch.\n\n2. THE THREE FAILURES ARE NOT REPRODUCIBLE AND THE CAUSAL STORY IS WRONG. The claim is \"three of them FAIL, so the web tier is red\". Exactly one stray probe is present now (apps/web/tests/zz-probe-radio-exemption.dom.test.tsx, which appeared mid-session while I worked). I ran it: `cd apps/web && npx vitest run tests/zz-probe-radio-exemption.dom.test.tsx` -> \"Test Files 1 passed (1) / Tests 1 passed (1)\". It PASSES, and the tier is red anyway. A passing probe turns this tier red exactly as surely as a failing one, so \"three of them FAIL\" is not the mechanism.\n\n3. THE RESIDUE IS TRANSIENT CONTAMINATION FROM THE AUDIT ITSELF, NOT A DEFECT IN THE REPAIRS. Untracked files are work",
      "STATUS": "REFUTED"
    },
    {
      "FINDING_ID": null,
      "CLASS": "ASSURANCE_GATE_DEFECT",
      "SURFACE": "scripts/ci/check-p1-28-version-sourcing.mjs:440",
      "TITLE": "The `sent + 1` defect this gate exists for is hidden by naming the computed constant after an earlier, well-sourced const anywhere in the same file",
      "REPRODUCER": "`declarations(text)` builds ONE file-global name->initializer map and keeps the FIRST binding ('First declaration wins', :469-471). `classifyVersionExpression` consults that map (:589) before it consults parameters, so an inner `const` in a different function scope is never classified — the outer one answers for it.\n\nMUTATION through the exported `run` (scratchpad probe, `node vs-mutate2.mjs`). Both files carry the same head, modelled on the real apps/web/src/features/appointments/components/AppointmentDetailScreen.tsx:102 (`const version = fresher ? lastChanged.recordVersion : detail.recordVersion;`) inside `VisitSummary`:\n  CONTROL — the panel computes with a name of its own: `const nextVersion = version + 1; await approveReception(receptionId, nextVersion, {});`\n    site: arg=\"nextVersion\" ok=FALSE\n    RED — 'approveReception is sent an If-Match this gate refuses — \"version + 1\" compu",
      "FAILURE_SCENARIO": "An implementer on a screen that already declares a well-sourced `const version = …recordVersion` at page level writes, inside one of its section components, `const version = sentVersion + 1` and sends that. The gate resolves the identifier to the page-level conditional, classifies it kind:'response', and reports the file clean. rec.reception-approve applies one edge from `inspecting` and two from `opened`, so the invented version is wrong on roughly half of all approvals and the command answers 409 ERR-CON-001 — the single defect the gate's headline docblock (:16-21) says it was built to catch",
      "REFUTER_WHY": "MECHANISM REPRODUCED EXACTLY AS CLAIMED. CLAIM REFUTED AS A FINDING: the stated scenario is wrong in two material ways, the product defect is unreachable at all seven shipped call sites, and the code and its test are untouched by this wave.\n\nWHAT I REPRODUCED (my own probes, scratchpad only, all deleted; every run drives the REAL `run()` over the REAL apps/web/src tree with the real contract, manifest, decisions and derived adapter roots — only ONE file's text is substituted in memory).\n\n1. The resolution is file-global and first-wins, and it is mis-resolving on the shipped tree TODAY. `node scripts/ci/check-p1-28-version-sourcing.mjs --json` reports the three AppointmentDetailScreen.tsx sites as `{\"argument\":\"version\",\"ok\":true,\"kind\":\"response\"}`. But at those call sites `version` is a destructured PROP of `RescheduleSection` (:380), `CancelSection` (:481) and `NoShowSection` (:659) — the correct classification is `kind:\"supplied\"`, which is what SummaryStep.tsx/ConversionStep.tsx get for their parameter `recordVersion`. The gate says `\"response\"` because `declarations()` handed back the page-level `const version = fresher ? lastChanged.recordVersion : detail.recordVersion` at :102. Directly: on the comment-stripped real file, `declarations().get('version')` === `\"fresher ? lastChanged.recordVersion : detail.recordVersion\"` while `parameterNames().has('version')` === true — t",
      "STATUS": "REFUTED"
    },
    {
      "FINDING_ID": null,
      "CLASS": "TEST_DEFECT",
      "SURFACE": "apps/web/tests/p1-28-reception-media.dom.test.tsx:778",
      "TITLE": "FR-07's own DOM proof prints \"Use 'Count this evidence' on the entry below to finish it\" over a row that renders zero such controls — the fixture it re-reads contradicts the sentence it asserts",
      "REPRODUCER": "Probe `apps/web/tests/zzlens-media-fixture.dom.test.tsx` (deleted) replayed the shipped case byte-for-byte — same `CONTRACT` fixture, same `captureRequirementEvidence` return (`{status:'denied', stage:'bound', bindingId:'bind-9', versionStatus:'accepted', scannerAvailable:true}`), same upload-and-submit on the `vin` row — and then read the row back. Command: `cd apps/web && npx vitest run --project dom tests/zzlens-media-fixture.dom.test.tsx`. Output:\n  [C] outcome line   : \"Recorded, but it has not been counted yet — the last step did not answer. Use “Count this evidence” on the entry below to finish it.\"\n  [C] vin rows below : [\"Accepted · counted\",\"Uploaded, not yet checked\",\"Being checked\"]\n  [C] \"Count this evidence\" controls in that row: 0\n  [C] readCaptureContract calls (the re-read happened): 2\nThe re-read really fires (2 calls) and returns `CONTRACT`, whose `vin` bindings are `v",
      "FAILURE_SCENARIO": "The new `boundNotCounted` sentence is two claims — what happened, and what to do about it — and only the first is proved. In the DOM the shipped case actually renders, the second claim is false on screen: the operator is directed to a control that is not in the row beneath. A change that broke the derivation of the finalize control (its `canCapture` gate, its `ACCEPTED_VERSION_STATUS` comparison, or the `finalizedAt === null` test) would leave this case green while the recovery path FR-07 exists to open is shut, because the case's own fixture already renders that shut state and passes.",
      "REFUTER_WHY": "REFUTED as framed. Every mechanical fact reproduces; the failure they are claimed to imply does not.\\n\\nREPRODUCTION. Probe apps/web/tests/zzrefx07-fixture.dom.test.tsx (created, run, DELETED) replayed the shipped case at apps/web/tests/p1-28-reception-media.dom.test.tsx:778 byte-for-byte — same CONTRACT fixture (vin bindings vin-1 accepted+finalized, vin-2 pending, vin-3 scanning), same captureRequirementEvidence return {status:'denied', stage:'bound', bindingId:'bind-9', versionStatus:'accepted', scannerAvailable:true}, same upload-and-submit on the vin row. Command: cd apps/web && npx vitest run --project dom tests/zzrefx07-fixture.dom.test.tsx -> 1 passed, printing:\\n  outcomeText: \"Recorded, but it has not been counted yet — the last step did not answer. Use “Count this evidence” on the entry below to finish it.\"\\n  vinRowFinalizeControls: 0 | pageFinalizeControls: 1 -> [\"capture-finalize-lamp-2\"] (the warning_lamp row) | lampRowFinalizeControls: 1\\n  vinEntries: [\"Accepted · counted\",\"Uploaded, not yet checked\",\"Being checked\"]\\n  readCaptureContractCalls: 2 | outcomeIsLastChildOfSection: true | domOrder: vin row is BEFORE (above) the outcome line\\nSo yes: the sentence renders over a row carrying zero \"Count this evidence\" controls, and the re-read really fires.\\n\\nWHY THE FINDING IS STILL REFUTED — three independent reasons.\\n\\n1. THE STATED FAILURE IS FALSE, BY MUTATION",
      "STATUS": "REFUTED"
    },
    {
      "FINDING_ID": null,
      "CLASS": "ASSURANCE_GATE_DEFECT",
      "SURFACE": "apps/web/tests/form-reset-class.test.ts:292",
      "TITLE": "The RadioGroupField exemption states that the CALL SITE carries the key and the default; the component's props accept neither, so `radio` in RESET_STRANDS is a rule no call site can satisfy",
      "REPRODUCER": "The exemption at form-reset-class.test.ts:289-293 reads `match: '<input id={id} type=\"radio\"'`, `why: 'The same primitive argument. `RadioGroupField` renders one input per option from the caller’s props.'` — i.e. it inherits the argument stated for the other five Field.tsx primitives, that the key and the default belong to the call site and every call site is inventoried here.\n\nThat is documentation: the component does not implement it. `RadioGroupField` (apps/web/src/components/forms/Field.tsx:430-503) types its props as `BaseFieldProps & { readonly name: string; readonly value: string; readonly onChange: (value: string) => void; readonly options: readonly RadioOption[] }` and hard-codes `checked={value === option.value}` on each input (Field.tsx:482). It neither accepts nor forwards `defaultChecked`/`defaultValue`, and passes no `key` to the inputs.\n\nProved with the compiler. Probe (de",
      "FAILURE_SCENARIO": "The first radio group placed in any of the 21 `<form action={…}>` files turns this suite red at `protects every one with the safe shape for its kind` with no legal fix available at the call site — `defaultChecked` is a TS2322. The three exits are deleting the control, changing the shared component, or adding a second FALSE `OUTSIDE_A_FORM` exemption; and the existing exemption's own text steers the reader away from the second by asserting the call site already carries what it provably cannot. An exemption list whose stated reason is untrue for the control it exempts is precisely the failure mo",
      "REFUTER_WHY": "MECHANISM REPRODUCED, CLAIM REFUTED AS STATED — unreachable in the shipped product, untouched by this wave, and the finder's \"and unnecessary\" leg is measurably false.\n\nWHAT REPRODUCES (I ran all of it myself; no fact below is taken from the finder)\n\n1. The compiler leg reproduces, and is stronger than stated. Probe apps/web/tests/zz-refuter-radio-ts.tsx (created, run, DELETED) declaring three call sites of the shipped RadioGroupField:\n   cd apps/web && npx tsc --noEmit\n   tests/zz-refuter-radio-ts.tsx(16,7): error TS2322: ... Property 'defaultChecked' does not exist on type 'IntrinsicAttributes & BaseFieldProps & { readonly name: string; readonly value: string; readonly onChange: (value: string) => void; readonly options: readonly RadioOption[]; }'.\n   tests/zz-refuter-radio-ts.tsx(30,7): error TS2322: ... Property 'defaultValue' does not exist on type '...'.\n   The third probe — key + label + name + value + onChange + options, NO default — typechecks clean. So the finder tested only defaultChecked; defaultValue is refused too, and `key` alone is legal (it is an IntrinsicAttributes member, never a prop).\n\n2. The asymmetry is real. apps/web/src/components/forms/Field.tsx: SelectField (:339-352) takes `...select` over `Omit<SelectHTMLAttributes<...>>` and spreads `{...select}` onto the element; CheckboxField (:383-389) does the same with `{...input}`; TextField/TextAreaField/Pas",
      "STATUS": "REFUTED"
    }
  ]
}
