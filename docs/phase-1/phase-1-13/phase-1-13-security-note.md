# Phase 1-13 — Security Note and Abuse Review

**Company:** RootLco — Root Link Company · **Product:** [PRODUCT NAME — Pending Final Approval] ·
**Classification:** Confidential · **Phase:** P1-13 · **Date:** 2026-07-21 ·
**Reviewer:** Eng. Ezzaldeen Al-Bitar — owner-authorized technical **self-review** under the
[Standing Technical Authorization Policy](../../governance/standing-technical-authorization-policy.md)
and the [Solo Developer Review Policy](../../governance/solo-developer-review-policy.md).
**Never an independent third-party audit, and never a penetration test.** ·
**Task IDs:** P1-13-SEC-005, P1-13-SEC-002, P1-13-SEC-003, P1-13-SEC-006

---

## 1. Method

Each abuse case below was walked against the implemented code, and — where the control is
testable — against an executed test. A control with no test is recorded as _reviewed_, not as
_verified_: the distinction is the point of the exercise.

## 2. Abuse cases and controls

| #   | Abuse case                                             | Control                                                                                                                                                                                                                                                                                                                                                             | Evidence                                                                                      |
| --- | ------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------- |
| 1   | Missing authorization declaration                      | `defineOperation()` refuses a non-public operation with no permission codes, at import time; `scripts/check-authorization-coverage.mjs` reconciles routes against declarations in CI                                                                                                                                                                                | `tests/foundation/operation-registry.test.ts`; CI step                                        |
| 2   | Scope spoofing (client sends `tenantId` / `companyId`) | Scope is resolved from `iam.user_accounts` + `iam.role_grants`; a requested narrowing is intersected by `narrowScope()` and **rejected**, never silently widened; the claimed tenant is a lookup key that must contain the session subject                                                                                                                          | `tests/backend/context-spoofing.test.ts` (11 tests)                                           |
| 3   | Context omission                                       | `DbHandle` cannot be constructed without a `RequestContext`; `Repository.assertContext()` throws `ERR-CTX-001` before any statement                                                                                                                                                                                                                                 | `tests/backend/context-and-rls.test.ts`                                                       |
| 4   | Runtime role with unintended RLS bypass                | Preflight asserts `rolbypassrls = false`; isolation is only ever asserted on a least-privilege login role                                                                                                                                                                                                                                                           | `tests/backend/capabilities.test.ts`, `context-and-rls.test.ts`                               |
| 5   | Context leaking between pooled requests                | Context is applied with `set_config(..., true)` — transaction-local, evaporating at COMMIT/ROLLBACK                                                                                                                                                                                                                                                                 | `tests/backend/context-and-rls.test.ts`                                                       |
| 6   | Correlation-ID injection / log forging                 | An inbound value is accepted only if it is a canonical UUID; anything else is replaced and the rejection is logged. Control characters in any logged string are escaped                                                                                                                                                                                             | `tests/foundation/correlation.test.ts`, `redaction.test.ts`, `tests/backend/api-ping.test.ts` |
| 7   | Sensitive-data logging                                 | Two independent layers: key-name redaction and value-shape scrubbing (JWT, `Bearer`, `Basic`, PEM, postgres URL with inline password, AWS key, GitHub token)                                                                                                                                                                                                        | `tests/foundation/redaction.test.ts`, `logger.test.ts`                                        |
| 8   | Error-detail leakage                                   | The problem document is assembled only from the catalog entry and `safeDetails`; `message`, `cause`, and stacks are unreachable from the renderer                                                                                                                                                                                                                   | `tests/foundation/error-model.test.ts`                                                        |
| 9   | Resource-existence leakage                             | Uniform `ERR-IAM-001` for denial; `ERR-CON-001` does not distinguish "stale version" from "out of scope"; `ERR-RES-001` is scope-relative                                                                                                                                                                                                                           | `tests/foundation/error-model.test.ts`, `tests/backend/authorization.test.ts`                 |
| 10  | Cache cross-tenant leakage                             | Keys are built centrally; a tenant-owned key is not expressible without the tenant; segments are length-prefixed so contents cannot forge another key                                                                                                                                                                                                               | `tests/foundation/cache.test.ts`                                                              |
| 11  | Cache poisoning / stale authorization                  | `authorization`, `entitlement`, `financial-command`, and `restricted-data` are permanently non-cacheable; `assertCacheable()` refuses them and refuses a TTL above the category ceiling                                                                                                                                                                             | `tests/foundation/cache.test.ts`                                                              |
| 12  | Cache stampede                                         | Concurrent misses share one in-flight loader                                                                                                                                                                                                                                                                                                                        | `tests/foundation/cache.test.ts`                                                              |
| 13  | Caching an error                                       | Only a fulfilled value is stored; a rejected loader leaves the key empty                                                                                                                                                                                                                                                                                            | `tests/foundation/cache.test.ts`                                                              |
| 14  | Rate-limit bypass via forged forwarding headers        | Forwarded headers are read **only** when the peer is in an explicitly configured trusted-proxy allow-list, which is **empty by default**; the right-most untrusted entry is chosen                                                                                                                                                                                  | `tests/foundation/trusted-proxy.test.ts`                                                      |
| 15  | Rate-limit key collision                               | Length-prefixed segments; tenant is always a discrete segment                                                                                                                                                                                                                                                                                                       | `tests/foundation/rate-limit.test.ts`                                                         |
| 16  | Rate limiting that silently does nothing at scale      | **Contract, not yet an operating control.** `assertStoreSuitableForMultiInstance()` throws for a process-local store, but it has **no production call site** — nothing in `src/` calls it, so today it is enforced only by its own test. ADR-015 makes calling it a condition of introducing a load balancer; wiring it into deployment composition is P1-14+ work. | `tests/foundation/rate-limit.test.ts` (definition + assertions only)                          |
| 17  | Idempotency replay abuse (same key, altered request)   | SHA-256 fingerprint over method, path, and canonicalised body; a mismatch is `ERR-INT-001` and neither version executes                                                                                                                                                                                                                                             | `tests/backend/idempotency.test.ts`                                                           |
| 18  | Idempotency race (two concurrent first uses)           | The unique index arbitrates; the loser re-reads the winner's stored response                                                                                                                                                                                                                                                                                        | `tests/backend/idempotency.test.ts`                                                           |
| 19  | Worker double claim                                    | `FOR UPDATE SKIP LOCKED` in `shared.claim_outbox_events`; no in-process lock is used or would suffice                                                                                                                                                                                                                                                               | `tests/backend/outbox-worker.test.ts`                                                         |
| 20  | Event loss on worker crash                             | The claim lease expires and the row becomes claimable again                                                                                                                                                                                                                                                                                                         | `tests/backend/outbox-worker.test.ts`                                                         |
| 21  | Poison event / schema incompatibility                  | An unsupported schema version is reported, not silently skipped, and drives the event toward dead letter                                                                                                                                                                                                                                                            | `tests/backend/outbox-worker.test.ts`                                                         |
| 22  | Retry storm                                            | Bounded exponential backoff with **full** jitter, capped                                                                                                                                                                                                                                                                                                            | `tests/foundation/backoff.test.ts`                                                            |
| 23  | Dead-letter accumulation unnoticed                     | Dead letter writes `shared.error_records` with a stable code and emits an error-level log record; `queueHealth` exposes depth, oldest age, and dead-letter count                                                                                                                                                                                                    | `tests/backend/outbox-worker.test.ts`                                                         |
| 24  | Duplicate consumer side effects on redelivery          | `shared.processed_events` PK `(consumer_code, event_id)` written in the same transaction as the effect                                                                                                                                                                                                                                                              | `tests/backend/outbox-worker.test.ts`                                                         |
| 25  | Unbounded worker concurrency                           | Explicit semaphore; bounded batch size                                                                                                                                                                                                                                                                                                                              | `tests/backend/outbox-worker.test.ts`                                                         |
| 26  | Graceful-shutdown event loss                           | Claiming stops first, the in-flight batch drains within a bounded grace period, and anything still claimed returns via the lease                                                                                                                                                                                                                                    | `tests/backend/outbox-worker.test.ts`                                                         |
| 27  | Feature-flag probing                                   | `ERR-TEN-001` does not name the flag; the flag code is logged, never returned                                                                                                                                                                                                                                                                                       | `src/server/auth/entitlement.ts`; reviewed                                                    |
| 28  | Unauthorized event payload content                     | The envelope is validated against the catalog; payload construction is the publishing module's responsibility and is reviewed per phase                                                                                                                                                                                                                             | Reviewed — no domain events are published in P1-13                                            |
| 29  | OpenAPI information leakage                            | The document is generated from the registry and carries no example data, no internal host, and no product name                                                                                                                                                                                                                                                      | `scripts/check-openapi.mjs`, `tests/openapi-contract.test.ts`                                 |
| 30  | Unbounded pagination                                   | Default 50, hard maximum 100, deterministic total ordering, cursor bound to its ordering contract                                                                                                                                                                                                                                                                   | `tests/foundation/pagination.test.ts`                                                         |
| 31  | N+1 access patterns                                    | Repository convention: bounded, single-statement reads; the reference repository reads one row by key                                                                                                                                                                                                                                                               | Reviewed — enforced by convention and review, not by tooling                                  |
| 32  | Secret exposure in tracked files                       | `npm run security:all` over the whole tracked tree, backend packages included                                                                                                                                                                                                                                                                                       | Executed — see the validation record                                                          |
| 32b | Dependency vulnerability exposure                      | **Pending — no control exists.** No dependency-vulnerability scan (`npm audit`, SCA, or equivalent) runs in CI or locally, so no evidence supports this row. Previously folded into row 32, whose control covers secrets only.                                                                                                                                      | None — to be decided with the P1-14 security tooling                                          |
| 33  | Authentication bypass                                  | The default authenticator returns no claims, so every authenticated operation answers `ERR-IAM-002`. There is deliberately **no** development header-based authenticator                                                                                                                                                                                            | `tests/backend/api-ping.test.ts`                                                              |

## 3. Findings

Five observations. **None is Critical**, none is left unaddressed, and the two High items were
fixed and covered by regression tests. P1-13-F-004 and P1-13-F-005 were found later than the rest:
they were latent in the merged feature work and unreachable until DBCR-P1-13-001 made the audit
path executable, at which point exercising it for the first time exposed both.

| Ref             | Severity         | Finding                                                                                                                                                                                                                                                                                        | Exploit / failure path                                                                                                                                                                                                                                                                           | Disposition                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| --------------- | ---------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **P1-13-F-001** | **High — FIXED** | `withTransaction` issued `SET LOCAL statement_timeout = $1`. `SET` is a utility statement and cannot take a bind parameter, so **every** transaction threw SQLSTATE `42601` before the caller's first statement.                                                                               | Not a security hole — it fails closed — but the entire controlled data-access path was non-functional against a real database, and the unit tier could not see it because it opens no connection.                                                                                                | **Fixed** in `src/server/db/transaction.ts`: `SELECT set_config('statement_timeout', $1, true)`, which is parameterisable (no interpolation, no injection surface) and equally transaction-local. Independently reproduced against PostgreSQL 17 before and after. **Regression test:** the whole of `tests/backend/**` (58 tests at the time of the fix; the suite is 69 tests at the P1-13 post-gate corrective SHA) exercises the wrapper; all were red before the fix and green after. |
| **P1-13-F-002** | Low — accepted   | `resolveClientAddress()` returns `viaTrustedProxy: true` when the peer is trusted and every forwarded entry is _also_ a trusted proxy, even though the returned IP is the peer address rather than a header value.                                                                             | None. The IP is correct and the rate-limit key is correct; only the provenance flag is imprecise, and the flag is informational.                                                                                                                                                                 | **Accepted.** Changing it would alter behaviour a passing test pins, for no security gain. Recorded so the next reader is not surprised. Regression test: `tests/foundation/trusted-proxy.test.ts`.                                                                                                                                                                                                                                                                                        |
| **P1-13-F-003** | Low — accepted   | `parseIfMatch()` strips quotes with a permissive pattern, so an unbalanced `"7` parses as version 7.                                                                                                                                                                                           | None. The value is still constrained to a positive integer and is used only as an equality predicate in a version-guarded UPDATE.                                                                                                                                                                | **Accepted** as client leniency. Regression test: `tests/foundation/concurrency-helpers.test.ts`.                                                                                                                                                                                                                                                                                                                                                                                          |
| **P1-13-F-004** | Medium — FIXED   | `AuditDetail.classification` offered `'confidential'`, which `ck_audit_record_details_class` rejects, and omitted `'secret'`, which it accepts.                                                                                                                                                | A caller classifying a value `confidential` aborts the whole command on a CHECK violation — and because `iam.audit_mask` masks only `restricted`/`secret`, the raw values appear unmasked in the PostgreSQL error DETAIL. The strongest masking class was meanwhile unreachable from TypeScript. | **Fixed** in `src/server/audit/audit.ts`: the union is derived from `AUDIT_CLASSIFICATIONS`. **Regression test:** `tests/backend/transaction.test.ts` reconciles that constant against the live CHECK constraint, so the two cannot drift apart again.                                                                                                                                                                                                                                     |
| **P1-13-F-005** | **High — FIXED** | `appendAudit` passed its own `{field, classification, value}` objects straight through, while `iam.audit_append` reads `field`, `old`, `new`, and `class`. Every field-level audit entry therefore stored a field name with NULL before/after values at the default `internal` classification. | Audit evidence was silently content-free, and a caller's `restricted` classification was downgraded to `internal` — so a value that did arrive would not have been masked. Invisible to the existing tests, which counted audit rows rather than reading them.                                   | **Fixed** in `src/server/audit/audit.ts` by translating each entry through `toDetailEnvelope()`. Confirmed by execution against PostgreSQL 17 before the fix. **Regression test:** `tests/backend/transaction.test.ts` asserts the stored `old`/`new`/`class` for an internal, a restricted, and a secret field.                                                                                                                                                                           |

One further observation — `problemFor()` gating optional fields on truthiness rather than
`!== undefined` — was **corrected during review** rather than logged, because it was unreachable
today and the fix carried no behavioural risk.

## 4. Change request raised, then implemented

**DBCR-P1-13-001.** The `app_runtime` archetype held SELECT only across `shared` and `iam`, so
audit append, outbox publication, idempotency storage, and security-event recording had no path to
the database. The gap was raised as a change request rather than patched inside the feature work,
and was then implemented on its own branch by migration
`20260725090000_iam_shared_runtime_write_capabilities.sql`. Executing the proposed grant set proved
that `iam.audit_append` also needs EXECUTE on three helper functions and writer-scoped SELECT
policies on the three audit tables — none of which a reading of the migration would have revealed.

Two things the draft grant set asked for were **not** built, because execution showed what they
would cost:

- A tenant-wide writer SELECT policy on `iam.audit_records` would have sat beside the shipped
  `iam.audit.view` policy and, because PostgreSQL ORs permissive policies, silently repealed that
  read gate for every session of a tenant. The shipped policies match only a record with no chain
  link yet — the row `iam.audit_append` is building, never one that `audit_append` has already linked into the chain.
  This is narrower than "never a committed row": ADV-03 records the residual case where an actor
  with arbitrary SQL as `app_runtime` inserts an audit record **outside** `audit_append`, leaving a
  committed row that is permanently unlinked and therefore permanently readable by that tenant.
  That case is accepted and tracked, not excluded by these policies. Reading audit history
  still requires `iam.audit.view`.
- `GRANT USAGE ON SCHEMA extensions` would have exposed `extensions.pg_stat_statements`, which the
  extension grants to PUBLIC and which therefore cannot be revoked for one role.
  `iam.audit_hash` uses `pg_catalog.sha256` instead, so no schema grant is made.

One accepted Low widening remains and is recorded in §7 of the change request: any session of a
tenant may read that tenant's `iam.audit_integrity_links` — a counter, an opaque record id, and two
SHA-256 digests, with no action, actor, entity, or field value — because extending a hash chain
requires its previous link.

**Security posture:** the foundation still **fails closed**. There is no branch that skips an audit
record and proceeds; the capability is measured on the connection in hand rather than assumed from
the migration history. The one deliberate asymmetry is `recordSecurityEvent()`, which logs and
continues rather than failing the request — because the denial it describes has _already been
enforced_, and converting a clean 403 into a 500 would degrade security, not improve it.

## 5. Statements this review does not make

- No penetration test was performed. No claim is made about resistance to a determined attacker.
- No production environment exists (ADR-012), so no production security posture is assessed.
- No independent review took place. Nothing reached protected `develop` outside the approved
  pull-request and hosted-CI flow. The work was reviewed under the Standing Technical
  Authorization and Solo Developer Review policies.

## 6. Conclusion

**Zero unresolved Critical findings. Zero unresolved High findings** — both Highs (P1-13-F-001 and
P1-13-F-005) and the one Medium (P1-13-F-004) were fixed and are covered by regression tests. Two
Low findings are accepted and recorded with their reasons. One database change request was raised
with executed evidence, deliberately kept out of the feature work, and implemented separately as
DBCR-P1-13-001; its own accepted residual is one Low, recorded in §7 of that document.

Worth saying plainly, because it is the useful lesson here: two of these defects existed in code
that had already passed a full review and green CI, and stayed invisible because the tests counted
rows instead of reading them. They surfaced the moment the capability became real. A test that
asserts _that_ something was written is not evidence of _what_ was written.

## 7. Gate review addendum (2026-07-21)

A further refute-focused review was run against the **merged** implementation — feature work and
database remediation together — as part of closing the gate. It is recorded in full in
[`phase-1-13-adversarial-review.md`](./phase-1-13-adversarial-review.md) and does not change the
conclusion above: **zero unresolved Critical, zero unresolved High**.

It did add four Medium findings, every one of them latent in P1-13 and becoming live in P1-14, plus
five Low and five informational observations. Two deserve naming here, because they touch controls
this note relies on:

- **ADV-01** — the layering gate matches alias imports only, so a relative import of the data layer
  from a handler passes CI. Confirmed by execution against a throwaway fixture. Nothing in the
  merged tree does this, but it is the guardrail behind abuse cases 1 and 3, and it should be closed
  before P1-14.
- **ADV-04** — an idempotency replay is matched on `(tenant, operation, key)` only, so a second user
  of the same tenant can receive the first user's stored response. Abuse case 17 covered "same key,
  altered request"; it did not cover the cross-principal case. Latent, because no idempotent
  business command exists yet.

One suspected finding was **refuted by measurement** rather than argued away: the writer-scoped
audit read predicate does not make appends scale with chain length — 50 appends took 28.5 ms on a
20,000-link chain against 35.7 ms on an empty one.
