# Phase 1-7 — Vehicle RLS Policy Matrix

<!-- GENERATED from live veh introspection; do not hand-edit count tables. -->

Generated from the live `veh` schema. Totals: **23 tables, 320 columns, 29 functions, 57 triggers, 62 policies, 91 indexes, 54 foreign keys, 104 check constraints, 7 EXCLUDE constraints.**

Every veh table is `ENABLE ROW LEVEL SECURITY` **and** `FORCE ROW LEVEL SECURITY`
(verified by `tests/db/veh-security.test.ts`). Default-deny: a command with no
matching policy is denied. No table has a DELETE policy (soft delete only).

| Table                       | Policy                                 | Cmd    | Roles                    | USING                                                                                                                                 | WITH CHECK                                                                                                                            |
| --------------------------- | -------------------------------------- | ------ | ------------------------ | ------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------- |
| `battery_masters`           | `ins_battery_masters_tenant`           | INSERT | app_runtime              | `—`                                                                                                                                   | `(tenant_id = iam.current_tenant_id())`                                                                                               |
| `battery_masters`           | `sel_battery_masters_tenant`           | SELECT | app_runtime,app_readonly | `(tenant_id = iam.current_tenant_id())`                                                                                               | `—`                                                                                                                                   |
| `battery_masters`           | `upd_battery_masters_tenant`           | UPDATE | app_runtime              | `(tenant_id = iam.current_tenant_id())`                                                                                               | `(tenant_id = iam.current_tenant_id())`                                                                                               |
| `battery_readings`          | `ins_battery_readings_tenant`          | INSERT | app_runtime              | `—`                                                                                                                                   | `(tenant_id = iam.current_tenant_id())`                                                                                               |
| `battery_readings`          | `sel_battery_readings_tenant`          | SELECT | app_runtime,app_readonly | `(tenant_id = iam.current_tenant_id())`                                                                                               | `—`                                                                                                                                   |
| `body_types`                | `ins_body_types_tenant`                | INSERT | app_runtime              | `—`                                                                                                                                   | `((scope = 'tenant'::text) AND (tenant_id = iam.current_tenant_id()))`                                                                |
| `body_types`                | `sel_body_types_visible`               | SELECT | app_runtime,app_readonly | `((scope = 'platform'::text) OR (tenant_id = iam.current_tenant_id()))`                                                               | `—`                                                                                                                                   |
| `body_types`                | `upd_body_types_tenant`                | UPDATE | app_runtime              | `((scope = 'tenant'::text) AND (tenant_id = iam.current_tenant_id()))`                                                                | `((scope = 'tenant'::text) AND (tenant_id = iam.current_tenant_id()))`                                                                |
| `duplicate_candidates`      | `ins_duplicate_candidates_tenant`      | INSERT | app_runtime              | `—`                                                                                                                                   | `(tenant_id = iam.current_tenant_id())`                                                                                               |
| `duplicate_candidates`      | `sel_duplicate_candidates_tenant`      | SELECT | app_runtime,app_readonly | `(tenant_id = iam.current_tenant_id())`                                                                                               | `—`                                                                                                                                   |
| `duplicate_candidates`      | `upd_duplicate_candidates_tenant`      | UPDATE | app_runtime              | `(tenant_id = iam.current_tenant_id())`                                                                                               | `(tenant_id = iam.current_tenant_id())`                                                                                               |
| `engine_history`            | `ins_engine_history_tenant`            | INSERT | app_runtime              | `—`                                                                                                                                   | `(tenant_id = iam.current_tenant_id())`                                                                                               |
| `engine_history`            | `sel_engine_history_tenant`            | SELECT | app_runtime,app_readonly | `(tenant_id = iam.current_tenant_id())`                                                                                               | `—`                                                                                                                                   |
| `engine_history`            | `upd_engine_history_tenant`            | UPDATE | app_runtime              | `(tenant_id = iam.current_tenant_id())`                                                                                               | `(tenant_id = iam.current_tenant_id())`                                                                                               |
| `makes`                     | `ins_makes_tenant`                     | INSERT | app_runtime              | `—`                                                                                                                                   | `((scope = 'tenant'::text) AND (tenant_id = iam.current_tenant_id()))`                                                                |
| `makes`                     | `sel_makes_visible`                    | SELECT | app_runtime,app_readonly | `((scope = 'platform'::text) OR (tenant_id = iam.current_tenant_id()))`                                                               | `—`                                                                                                                                   |
| `makes`                     | `upd_makes_tenant`                     | UPDATE | app_runtime              | `((scope = 'tenant'::text) AND (tenant_id = iam.current_tenant_id()))`                                                                | `((scope = 'tenant'::text) AND (tenant_id = iam.current_tenant_id()))`                                                                |
| `models`                    | `ins_models_tenant`                    | INSERT | app_runtime              | `—`                                                                                                                                   | `((scope = 'tenant'::text) AND (tenant_id = iam.current_tenant_id()))`                                                                |
| `models`                    | `sel_models_visible`                   | SELECT | app_runtime,app_readonly | `((scope = 'platform'::text) OR (tenant_id = iam.current_tenant_id()))`                                                               | `—`                                                                                                                                   |
| `models`                    | `upd_models_tenant`                    | UPDATE | app_runtime              | `((scope = 'tenant'::text) AND (tenant_id = iam.current_tenant_id()))`                                                                | `((scope = 'tenant'::text) AND (tenant_id = iam.current_tenant_id()))`                                                                |
| `odometer_readings`         | `ins_odometer_readings_tenant`         | INSERT | app_runtime              | `—`                                                                                                                                   | `(tenant_id = iam.current_tenant_id())`                                                                                               |
| `odometer_readings`         | `sel_odometer_readings_tenant`         | SELECT | app_runtime,app_readonly | `(tenant_id = iam.current_tenant_id())`                                                                                               | `—`                                                                                                                                   |
| `ownership_history`         | `ins_ownership_history_tenant`         | INSERT | app_runtime              | `—`                                                                                                                                   | `(tenant_id = iam.current_tenant_id())`                                                                                               |
| `ownership_history`         | `sel_ownership_history_tenant`         | SELECT | app_runtime,app_readonly | `(tenant_id = iam.current_tenant_id())`                                                                                               | `—`                                                                                                                                   |
| `ownership_history`         | `upd_ownership_history_tenant`         | UPDATE | app_runtime              | `(tenant_id = iam.current_tenant_id())`                                                                                               | `(tenant_id = iam.current_tenant_id())`                                                                                               |
| `plate_history`             | `ins_plate_history_tenant`             | INSERT | app_runtime              | `—`                                                                                                                                   | `(tenant_id = iam.current_tenant_id())`                                                                                               |
| `plate_history`             | `sel_plate_history_tenant`             | SELECT | app_runtime,app_readonly | `(tenant_id = iam.current_tenant_id())`                                                                                               | `—`                                                                                                                                   |
| `plate_history`             | `upd_plate_history_tenant`             | UPDATE | app_runtime              | `(tenant_id = iam.current_tenant_id())`                                                                                               | `(tenant_id = iam.current_tenant_id())`                                                                                               |
| `powertrain_types`          | `ins_powertrain_types_tenant`          | INSERT | app_runtime              | `—`                                                                                                                                   | `((scope = 'tenant'::text) AND (tenant_id = iam.current_tenant_id()))`                                                                |
| `powertrain_types`          | `sel_powertrain_types_visible`         | SELECT | app_runtime,app_readonly | `((scope = 'platform'::text) OR (tenant_id = iam.current_tenant_id()))`                                                               | `—`                                                                                                                                   |
| `powertrain_types`          | `upd_powertrain_types_tenant`          | UPDATE | app_runtime              | `((scope = 'tenant'::text) AND (tenant_id = iam.current_tenant_id()))`                                                                | `((scope = 'tenant'::text) AND (tenant_id = iam.current_tenant_id()))`                                                                |
| `relationship_evidence`     | `ins_relationship_evidence_tenant`     | INSERT | app_runtime              | `—`                                                                                                                                   | `(tenant_id = iam.current_tenant_id())`                                                                                               |
| `relationship_evidence`     | `sel_relationship_evidence_tenant`     | SELECT | app_runtime,app_readonly | `(tenant_id = iam.current_tenant_id())`                                                                                               | `—`                                                                                                                                   |
| `transmission_history`      | `ins_transmission_history_tenant`      | INSERT | app_runtime              | `—`                                                                                                                                   | `(tenant_id = iam.current_tenant_id())`                                                                                               |
| `transmission_history`      | `sel_transmission_history_tenant`      | SELECT | app_runtime,app_readonly | `(tenant_id = iam.current_tenant_id())`                                                                                               | `—`                                                                                                                                   |
| `transmission_history`      | `upd_transmission_history_tenant`      | UPDATE | app_runtime              | `(tenant_id = iam.current_tenant_id())`                                                                                               | `(tenant_id = iam.current_tenant_id())`                                                                                               |
| `trims`                     | `ins_trims_tenant`                     | INSERT | app_runtime              | `—`                                                                                                                                   | `((scope = 'tenant'::text) AND (tenant_id = iam.current_tenant_id()))`                                                                |
| `trims`                     | `sel_trims_visible`                    | SELECT | app_runtime,app_readonly | `((scope = 'platform'::text) OR (tenant_id = iam.current_tenant_id()))`                                                               | `—`                                                                                                                                   |
| `trims`                     | `upd_trims_tenant`                     | UPDATE | app_runtime              | `((scope = 'tenant'::text) AND (tenant_id = iam.current_tenant_id()))`                                                                | `((scope = 'tenant'::text) AND (tenant_id = iam.current_tenant_id()))`                                                                |
| `vehicle_alerts`            | `ins_vehicle_alerts_tenant`            | INSERT | app_runtime              | `—`                                                                                                                                   | `(tenant_id = iam.current_tenant_id())`                                                                                               |
| `vehicle_alerts`            | `sel_vehicle_alerts_tenant`            | SELECT | app_runtime,app_readonly | `(tenant_id = iam.current_tenant_id())`                                                                                               | `—`                                                                                                                                   |
| `vehicle_alerts`            | `upd_vehicle_alerts_tenant`            | UPDATE | app_runtime              | `(tenant_id = iam.current_tenant_id())`                                                                                               | `(tenant_id = iam.current_tenant_id())`                                                                                               |
| `vehicle_attribute_history` | `ins_vehicle_attribute_history_tenant` | INSERT | app_runtime              | `—`                                                                                                                                   | `(tenant_id = iam.current_tenant_id())`                                                                                               |
| `vehicle_attribute_history` | `sel_vehicle_attribute_history_tenant` | SELECT | app_runtime,app_readonly | `(tenant_id = iam.current_tenant_id())`                                                                                               | `—`                                                                                                                                   |
| `vehicle_ev_profiles`       | `ins_vehicle_ev_profiles_tenant`       | INSERT | app_runtime              | `—`                                                                                                                                   | `(tenant_id = iam.current_tenant_id())`                                                                                               |
| `vehicle_ev_profiles`       | `sel_vehicle_ev_profiles_tenant`       | SELECT | app_runtime,app_readonly | `(tenant_id = iam.current_tenant_id())`                                                                                               | `—`                                                                                                                                   |
| `vehicle_ev_profiles`       | `upd_vehicle_ev_profiles_tenant`       | UPDATE | app_runtime              | `(tenant_id = iam.current_tenant_id())`                                                                                               | `(tenant_id = iam.current_tenant_id())`                                                                                               |
| `vehicle_identifiers`       | `ins_vehicle_identifiers_tenant`       | INSERT | app_runtime              | `—`                                                                                                                                   | `((tenant_id = iam.current_tenant_id()) AND ((classification = 'internal'::text) OR iam.has_permission('iam.sensitive.view'::text)))` |
| `vehicle_identifiers`       | `sel_vehicle_identifiers_tenant`       | SELECT | app_runtime,app_readonly | `((tenant_id = iam.current_tenant_id()) AND ((classification = 'internal'::text) OR iam.has_permission('iam.sensitive.view'::text)))` | `—`                                                                                                                                   |
| `vehicle_identifiers`       | `upd_vehicle_identifiers_tenant`       | UPDATE | app_runtime              | `((tenant_id = iam.current_tenant_id()) AND ((classification = 'internal'::text) OR iam.has_permission('iam.sensitive.view'::text)))` | `((tenant_id = iam.current_tenant_id()) AND ((classification = 'internal'::text) OR iam.has_permission('iam.sensitive.view'::text)))` |
| `vehicle_merges`            | `ins_vehicle_merges_tenant`            | INSERT | app_runtime              | `—`                                                                                                                                   | `(tenant_id = iam.current_tenant_id())`                                                                                               |
| `vehicle_merges`            | `sel_vehicle_merges_tenant`            | SELECT | app_runtime,app_readonly | `(tenant_id = iam.current_tenant_id())`                                                                                               | `—`                                                                                                                                   |
| `vehicle_relationships`     | `ins_vehicle_relationships_tenant`     | INSERT | app_runtime              | `—`                                                                                                                                   | `(tenant_id = iam.current_tenant_id())`                                                                                               |
| `vehicle_relationships`     | `sel_vehicle_relationships_tenant`     | SELECT | app_runtime,app_readonly | `(tenant_id = iam.current_tenant_id())`                                                                                               | `—`                                                                                                                                   |
| `vehicle_relationships`     | `upd_vehicle_relationships_tenant`     | UPDATE | app_runtime              | `(tenant_id = iam.current_tenant_id())`                                                                                               | `(tenant_id = iam.current_tenant_id())`                                                                                               |
| `vehicle_status_history`    | `ins_vehicle_status_history_tenant`    | INSERT | app_runtime              | `—`                                                                                                                                   | `(tenant_id = iam.current_tenant_id())`                                                                                               |
| `vehicle_status_history`    | `sel_vehicle_status_history_tenant`    | SELECT | app_runtime,app_readonly | `(tenant_id = iam.current_tenant_id())`                                                                                               | `—`                                                                                                                                   |
| `vehicles`                  | `ins_vehicles_tenant`                  | INSERT | app_runtime              | `—`                                                                                                                                   | `(tenant_id = iam.current_tenant_id())`                                                                                               |
| `vehicles`                  | `sel_vehicles_tenant`                  | SELECT | app_runtime,app_readonly | `(tenant_id = iam.current_tenant_id())`                                                                                               | `—`                                                                                                                                   |
| `vehicles`                  | `upd_vehicles_tenant`                  | UPDATE | app_runtime              | `(tenant_id = iam.current_tenant_id())`                                                                                               | `(tenant_id = iam.current_tenant_id())`                                                                                               |
| `vin_verifications`         | `ins_vin_verifications_tenant`         | INSERT | app_runtime              | `—`                                                                                                                                   | `(tenant_id = iam.current_tenant_id())`                                                                                               |
| `vin_verifications`         | `sel_vin_verifications_tenant`         | SELECT | app_runtime,app_readonly | `(tenant_id = iam.current_tenant_id())`                                                                                               | `—`                                                                                                                                   |
