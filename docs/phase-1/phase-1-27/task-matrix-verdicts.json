{
  "FE-007": {
    "NORMAL_NAVIGATION_ENTRY": "Sidebar `config/navigation.ts:174` -> /crm/customers -> row action `CustomerSearchScreen.tsx:214` -> `/[locale]/crm/customers/[customerId]` -> the section tab. Mounted via SECTIONS/BUILT `CustomerProfileScreen.tsx:101-142`.",
    "REQUIRED_PERMISSIONS": "Page read `crm.customer.read` (`[customerId]/page.tsx:62`); the write form is gated by `ComponentSection` `CustomerProfileScreen.tsx:883` on `table.response && canWrite`, with `writes={permittedWrites(session.permissions)}` at `page.tsx:130`.",
    "TENANT_ISOLATION_REQUIREMENT": "Client sends cursor and limit only (`profile-api.ts:67-69`); `query()` throws on any tenant/company/branch key (`read-operation.ts:89-100`), asserted `p1-27-security.test.ts:270-276`.",
    "LOADING_STATE": "`DataTable.tsx:267-279`.",
    "EMPTY_STATE": "`DataTable.tsx:302-321` — code present; no positive test for these sections.",
    "ZERO_RESULT_STATE": "N/A with rationale: the section defines no filters, so `isNarrowed()` is always false and the narrowed-empty branch is structurally unreachable. Correct rather than missing.",
    "ERROR_STATE": "`DataTable.tsx:165-167`, correlation id proved `crm-customer-profile.dom.test.tsx:334`.",
    "DENIAL_STATE": "`DataTable.tsx:131-136`; write form withheld, both directions proved `write-permission-gating.dom.test.tsx:203-221`.",
    "RTL_LTR_EVIDENCE": "Catalogue parity enforced `apps/web/tests/i18n.test.ts:72`; RTL render helper used by the DOM suites.",
    "ACCESSIBILITY_EVIDENCE": "Semantic only — sr-only h2, table caption, `role=\"region\"`, labelled fields. NO axe scan: the customer-profile route is absent from `apps/web/tests/e2e/authenticated/accessibility.spec.ts:103-112`. Recorded as finding `H-01`.",
    "IMPLEMENTATION_SURFACES": "`CustomerProfileScreen.tsx:549-652`; `profile-api.ts:85-91`; `profile-actions.ts:38-76`; contract `profile-contract.ts:73-84,233,255-257`.",
    "BACKEND_READ_CONTRACTS": "`crm.contact-list` GET /api/v1/customers/{id}/contacts (`profile-api.ts:58-83`).",
    "BACKEND_WRITE_CONTRACTS": "`crm.contact-add` POST same path (`profile-actions.ts:72-74`).",
    "CONFLICT_STATE": "Generic write-failure copy via `RecordForm.tsx:339-348`; no conflict-specific copy and no conflict test. Recorded as finding `H-02`.",
    "ARABIC_EVIDENCE": "`crm.channel.*` and `crm.contactStatus.*` resolve in ar.json and match migration `20260719097000:68-69`.",
    "ENGLISH_EVIDENCE": "Same keys resolve in en.json.",
    "PRODUCTION_REACHABILITY": "Reachable and mounted; write form rendered only for `crm.customer.profile.write`.",
    "PRODUCTION_TEST_EVIDENCE": "`crm-customer-profile.dom.test.tsx:242,265,287,298`; `crm-profile-api.test.ts:125,137,147`; `governance-write-validation.test.ts:216-230,260`.",
    "NEGATIVE_OR_MUTATION_PROOF": "`write-permission-gating.dom.test.tsx:203-221` proves the form is withheld AND offered — both directions.",
    "ROUND5_FINDING_IDS": "None mapped.",
    "DOCUMENTATION_EVIDENCE": "`docs/phase-1/phase-1-27/evidence/task-traceability.md` row; canonical-plan §5.1.",
    "FINAL_VERDICT": "PASS",
    "VERDICT_RATIONALE": "Real read and real write against the published operations, correctly gated, exercised at both component and adapter level with a two-directional permission proof.",
    "PROTECTED_REPROOF": "OUTSTANDING — the AUTHENTICATED browser tier. It is now wired and run: the `authenticated-browser` job (`protected-develop-verification.yml:176-419`) sets `ROOTLCO_E2E_AUTH: '1'` at `:319`, bootstraps a real operator and a second tenant, and fails unless every authenticated spec contributed an executed test. It runs on pushes to `develop` and `main` ONLY, and `.github/ci-baselines/unrun-test-tiers.json` (`notYetObservedOnARunner`) records that it has never executed on a hosted runner even once. Collection is proved locally at 229 tests across 7 spec files. The FEATURE is complete at this head; the REPROOF is unpaid."
  },
  "FE-008": {
    "NORMAL_NAVIGATION_ENTRY": "Sidebar `config/navigation.ts:174` -> /crm/customers -> row action `CustomerSearchScreen.tsx:214` -> `/[locale]/crm/customers/[customerId]` -> the section tab. Mounted via SECTIONS/BUILT `CustomerProfileScreen.tsx:101-142`.",
    "REQUIRED_PERMISSIONS": "Page read `crm.customer.read` (`[customerId]/page.tsx:62`); the write form is gated by `ComponentSection` `CustomerProfileScreen.tsx:883` on `table.response && canWrite`, with `writes={permittedWrites(session.permissions)}` at `page.tsx:130`.",
    "TENANT_ISOLATION_REQUIREMENT": "Client sends cursor and limit only (`profile-api.ts:67-69`); `query()` throws on any tenant/company/branch key (`read-operation.ts:89-100`), asserted `p1-27-security.test.ts:270-276`.",
    "LOADING_STATE": "`DataTable.tsx:267-279`.",
    "EMPTY_STATE": "`DataTable.tsx:302-321` — code present; no positive test for these sections.",
    "ZERO_RESULT_STATE": "N/A with rationale: the section defines no filters, so `isNarrowed()` is always false and the narrowed-empty branch is structurally unreachable. Correct rather than missing.",
    "ERROR_STATE": "`DataTable.tsx:165-167`, correlation id proved `crm-customer-profile.dom.test.tsx:334`.",
    "DENIAL_STATE": "`DataTable.tsx:131-136`; write form withheld, both directions proved `write-permission-gating.dom.test.tsx:203-221`.",
    "RTL_LTR_EVIDENCE": "Catalogue parity enforced `apps/web/tests/i18n.test.ts:72`; RTL render helper used by the DOM suites.",
    "ACCESSIBILITY_EVIDENCE": "Semantic only — sr-only h2, table caption, `role=\"region\"`, labelled fields. NO axe scan: the customer-profile route is absent from `apps/web/tests/e2e/authenticated/accessibility.spec.ts:103-112`. Recorded as finding `H-01`.",
    "IMPLEMENTATION_SURFACES": "`CustomerProfileScreen.tsx:654-780`; `profile-api.ts:93-99`; `profile-actions.ts:78-133`; `addressLines` `profile-contract.ts:271-280`.",
    "BACKEND_READ_CONTRACTS": "`crm.address-list` GET /api/v1/customers/{id}/addresses.",
    "BACKEND_WRITE_CONTRACTS": "`crm.address-add` POST same path (`profile-actions.ts:129-131`).",
    "CONFLICT_STATE": "Generic write-failure copy only; no conflict test. Finding `H-02`.",
    "ARABIC_EVIDENCE": "`ADDRESS_TYPES` verified against the real CHECK constraint `tests/server-vocabularies.test.ts:79-86`, with an orphan-label check `:163-174`; ar.json resolves every key.",
    "ENGLISH_EVIDENCE": "en.json resolves every key.",
    "PRODUCTION_REACHABILITY": "Reachable and mounted.",
    "PRODUCTION_TEST_EVIDENCE": "`crm-customer-profile.dom.test.tsx:109-151,308,334`; `governance-write-validation.test.ts:222-230,261`.",
    "NEGATIVE_OR_MUTATION_PROOF": "`write-permission-gating.dom.test.tsx:155` withholding proof.",
    "ROUND5_FINDING_IDS": "None mapped.",
    "DOCUMENTATION_EVIDENCE": "traceability row; canonical-plan §5.1.",
    "FINAL_VERDICT": "PASS",
    "VERDICT_RATIONALE": "The only CRM vocabulary in the phase verified against its own migration CHECK constraint, with the read, the write and the gate all exercised.",
    "PROTECTED_REPROOF": "OUTSTANDING — the AUTHENTICATED browser tier. It is now wired and run: the `authenticated-browser` job (`protected-develop-verification.yml:176-419`) sets `ROOTLCO_E2E_AUTH: '1'` at `:319`, bootstraps a real operator and a second tenant, and fails unless every authenticated spec contributed an executed test. It runs on pushes to `develop` and `main` ONLY, and `.github/ci-baselines/unrun-test-tiers.json` (`notYetObservedOnARunner`) records that it has never executed on a hosted runner even once. Collection is proved locally at 229 tests across 7 spec files. The FEATURE is complete at this head; the REPROOF is unpaid."
  },
  "FE-009": {
    "NORMAL_NAVIGATION_ENTRY": "Sidebar `config/navigation.ts:174` -> /crm/customers -> row action `CustomerSearchScreen.tsx:214` -> `/[locale]/crm/customers/[customerId]` -> the section tab. Mounted via SECTIONS/BUILT `CustomerProfileScreen.tsx:101-142`.",
    "REQUIRED_PERMISSIONS": "Page read `crm.customer.read` (`[customerId]/page.tsx:62`); the write form is gated by `ComponentSection` `CustomerProfileScreen.tsx:883` on `table.response && canWrite`, with `writes={permittedWrites(session.permissions)}` at `page.tsx:130`.",
    "TENANT_ISOLATION_REQUIREMENT": "Client sends cursor and limit only (`profile-api.ts:67-69`); `query()` throws on any tenant/company/branch key (`read-operation.ts:89-100`), asserted `p1-27-security.test.ts:270-276`.",
    "LOADING_STATE": "`DataTable.tsx:267-279`.",
    "EMPTY_STATE": "`DataTable.tsx:302-321` — code present; no positive test for these sections.",
    "ZERO_RESULT_STATE": "N/A with rationale: the section defines no filters, so `isNarrowed()` is always false and the narrowed-empty branch is structurally unreachable. Correct rather than missing.",
    "ERROR_STATE": "`DataTable.tsx:165-167`, correlation id proved `crm-customer-profile.dom.test.tsx:334`.",
    "DENIAL_STATE": "`DataTable.tsx:131-136`; write form withheld, both directions proved `write-permission-gating.dom.test.tsx:203-221`.",
    "RTL_LTR_EVIDENCE": "Catalogue parity enforced `apps/web/tests/i18n.test.ts:72`; RTL render helper used by the DOM suites.",
    "ACCESSIBILITY_EVIDENCE": "Semantic only — sr-only h2, table caption, `role=\"region\"`, labelled fields. NO axe scan: the customer-profile route is absent from `apps/web/tests/e2e/authenticated/accessibility.spec.ts:103-112`. Recorded as finding `H-01`.",
    "IMPLEMENTATION_SURFACES": "`CustomerProfileScreen.tsx:888-998`; `profile-api.ts:101-107`; `governance-actions.ts:245-294`.",
    "BACKEND_READ_CONTRACTS": "`crm.preference-list` GET /api/v1/customers/{id}/preferences.",
    "BACKEND_WRITE_CONTRACTS": "`crm.preference-set` PUT same path; idempotency key required, proved `crm-governance-writes.test.ts:34,45-54`.",
    "CONFLICT_STATE": "DEFECT. `profile-contract.ts:115` states `recordVersion` is \"What a client puts in `If-Match` on the preferences PUT\". `action-support.ts:73` calls `client.send(method, path, body)` with NO `ifMatch`, although the option exists (`client.ts:218,275`) and is used by `administration/organization/actions.ts:62`. The backend route never reads it either (`preferences/route.ts:97-123`). The documented stale-version path exists on neither side. Recorded as finding `H-03`.",
    "ARABIC_EVIDENCE": "`crm.purpose.*` resolve in ar.json, covered `crm-customer-components.dom.test.tsx:434`.",
    "ENGLISH_EVIDENCE": "en.json resolves every key.",
    "PRODUCTION_REACHABILITY": "Reachable, mounted, gated `write-permission-gating.dom.test.tsx:156`.",
    "PRODUCTION_TEST_EVIDENCE": "`crm-customer-components.dom.test.tsx:302,324`; `crm-governance-writes.test.ts:34,45-54`; `governance-write-validation.test.ts:253-255`.",
    "NEGATIVE_OR_MUTATION_PROOF": "Permission withholding proved; NO negative proof for the documented concurrency control, because it is not implemented.",
    "ROUND5_FINDING_IDS": "`H-03` FIXED at `ab4e6ec` — the retraction landed at `crm/customers/profile-contract.ts:139-143`, which now states last-write-wins in the product's own terms instead of documenting an `If-Match` rule neither side implements.",
    "DOCUMENTATION_EVIDENCE": "traceability row; canonical-plan §5.1.",
    "FINAL_VERDICT": "PASS",
    "VERDICT_RATIONALE": "Screen, read and gated write were already real. The single reason this was PARTIAL was `H-03`: the contract documented an `If-Match`/`recordVersion` concurrency control implemented on neither the client nor the server — a docblock stating a rule the code does not have. Re-assessed at `26eab7e`; the previous cell was written at `9eff8bd` and the tree moved. `profile-contract.ts:139-143` now says the preferences PUT is last-write-wins, names that `crm.preference-set` is not registered `versionGuarded`, and records why the earlier sentence was withdrawn. The concurrency GAP itself is real and is carried by `QA-004`, whose name is the conjunction it belongs to; it is not this row's defect and recording it twice is what made the register unable to count.",
    "PROTECTED_REPROOF": "OUTSTANDING — the AUTHENTICATED browser tier. It is now wired and run: the `authenticated-browser` job (`protected-develop-verification.yml:176-419`) sets `ROOTLCO_E2E_AUTH: '1'` at `:319`, bootstraps a real operator and a second tenant, and fails unless every authenticated spec contributed an executed test. It runs on pushes to `develop` and `main` ONLY, and `.github/ci-baselines/unrun-test-tiers.json` (`notYetObservedOnARunner`) records that it has never executed on a hosted runner even once. Collection is proved locally at 229 tests across 7 spec files. The FEATURE is complete at this head; the REPROOF is unpaid."
  },
  "FE-010": {
    "NORMAL_NAVIGATION_ENTRY": "Sidebar `config/navigation.ts:174` -> /crm/customers -> row action `CustomerSearchScreen.tsx:214` -> `/[locale]/crm/customers/[customerId]` -> the section tab. Mounted via SECTIONS/BUILT `CustomerProfileScreen.tsx:101-142`.",
    "REQUIRED_PERMISSIONS": "Page read `crm.customer.read` (`[customerId]/page.tsx:62`); the write form is gated by `ComponentSection` `CustomerProfileScreen.tsx:883` on `table.response && canWrite`, with `writes={permittedWrites(session.permissions)}` at `page.tsx:130`.",
    "TENANT_ISOLATION_REQUIREMENT": "Client sends cursor and limit only (`profile-api.ts:67-69`); `query()` throws on any tenant/company/branch key (`read-operation.ts:89-100`), asserted `p1-27-security.test.ts:270-276`.",
    "LOADING_STATE": "`DataTable.tsx:267-279`.",
    "EMPTY_STATE": "`DataTable.tsx:302-321` — code present; no positive test for these sections.",
    "ZERO_RESULT_STATE": "N/A with rationale: the section defines no filters, so `isNarrowed()` is always false and the narrowed-empty branch is structurally unreachable. Correct rather than missing.",
    "ERROR_STATE": "`DataTable.tsx:165-167`, correlation id proved `crm-customer-profile.dom.test.tsx:334`.",
    "DENIAL_STATE": "`DataTable.tsx:131-136`; write form withheld, both directions proved `write-permission-gating.dom.test.tsx:203-221`.",
    "RTL_LTR_EVIDENCE": "Catalogue parity enforced `apps/web/tests/i18n.test.ts:72`; RTL render helper used by the DOM suites.",
    "ACCESSIBILITY_EVIDENCE": "Semantic only — sr-only h2, table caption, `role=\"region\"`, labelled fields. NO axe scan: the customer-profile route is absent from `apps/web/tests/e2e/authenticated/accessibility.spec.ts:103-112`. Recorded as finding `H-01`.",
    "IMPLEMENTATION_SURFACES": "`CustomerProfileScreen.tsx:1000-1126`; `profile-api.ts:109-115`; `governance-actions.ts:296-337`.",
    "BACKEND_READ_CONTRACTS": "`crm.consent-list` GET /api/v1/customers/{id}/consents.",
    "BACKEND_WRITE_CONTRACTS": "`crm.consent-record` POST same path; permission `crm.customer.consent.write` (`governance-contract.ts:168`).",
    "CONFLICT_STATE": "Generic only; append-only semantics stated on screen `:1068`. Finding `H-02`.",
    "ARABIC_EVIDENCE": "`crm.consentKind.*` / `crm.consentStatus.*` enumerated from the CHECK constraint and resolved in ar.json (`crm-customer-components.dom.test.tsx:435-436`).",
    "ENGLISH_EVIDENCE": "en.json resolves every key.",
    "PRODUCTION_REACHABILITY": "Reachable, mounted, gated `write-permission-gating.dom.test.tsx:157`.",
    "PRODUCTION_TEST_EVIDENCE": "`crm-customer-components.dom.test.tsx:350,360,370`; `governance-write-validation.test.ts:179-203` drives the real action and rejects `expired`; `record-form-consumers.dom.test.tsx:210-304` proves the real consent form survives a failed write and clears on success.",
    "NEGATIVE_OR_MUTATION_PROOF": "Form-reset behaviour proved in both directions on the real consumer.",
    "ROUND5_FINDING_IDS": "None mapped.",
    "DOCUMENTATION_EVIDENCE": "traceability row; canonical-plan §5.1.",
    "FINAL_VERDICT": "PASS",
    "VERDICT_RATIONALE": "Read, write, gate and the failed-write retention path are all exercised against production code; the action accepts an unused optional `contactPointId`, which is inert rather than wrong.",
    "PROTECTED_REPROOF": "OUTSTANDING — the AUTHENTICATED browser tier. It is now wired and run: the `authenticated-browser` job (`protected-develop-verification.yml:176-419`) sets `ROOTLCO_E2E_AUTH: '1'` at `:319`, bootstraps a real operator and a second tenant, and fails unless every authenticated spec contributed an executed test. It runs on pushes to `develop` and `main` ONLY, and `.github/ci-baselines/unrun-test-tiers.json` (`notYetObservedOnARunner`) records that it has never executed on a hosted runner even once. Collection is proved locally at 229 tests across 7 spec files. The FEATURE is complete at this head; the REPROOF is unpaid."
  },
  "FE-011": {
    "NORMAL_NAVIGATION_ENTRY": "Sidebar `config/navigation.ts:174` -> /crm/customers -> row action `CustomerSearchScreen.tsx:214` -> `/[locale]/crm/customers/[customerId]` -> the section tab. Mounted via SECTIONS/BUILT `CustomerProfileScreen.tsx:101-142`.",
    "REQUIRED_PERMISSIONS": "Page read `crm.customer.read` (`[customerId]/page.tsx:62`); the write form is gated by `ComponentSection` `CustomerProfileScreen.tsx:883` on `table.response && canWrite`, with `writes={permittedWrites(session.permissions)}` at `page.tsx:130`.",
    "TENANT_ISOLATION_REQUIREMENT": "Client sends cursor and limit only (`profile-api.ts:67-69`); `query()` throws on any tenant/company/branch key (`read-operation.ts:89-100`), asserted `p1-27-security.test.ts:270-276`.",
    "LOADING_STATE": "`DataTable.tsx:267-279`.",
    "EMPTY_STATE": "`DataTable.tsx:302-321`, plus a fourth completeness state this section alone carries: `includesRestricted` true/false/null (`:1143,1201-1210`), which states whether restricted notes are hidden from this caller.",
    "ZERO_RESULT_STATE": "N/A with rationale: the section defines no filters, so `isNarrowed()` is always false and the narrowed-empty branch is structurally unreachable. Correct rather than missing.",
    "ERROR_STATE": "`DataTable.tsx:165-167`, correlation id proved `crm-customer-profile.dom.test.tsx:334`.",
    "DENIAL_STATE": "`DataTable.tsx:131-136`; write form withheld, both directions proved `write-permission-gating.dom.test.tsx:203-221`.",
    "RTL_LTR_EVIDENCE": "Catalogue parity enforced `apps/web/tests/i18n.test.ts:72`; RTL render helper used by the DOM suites.",
    "ACCESSIBILITY_EVIDENCE": "Semantic only — sr-only h2, table caption, `role=\"region\"`, labelled fields. NO axe scan: the customer-profile route is absent from `apps/web/tests/e2e/authenticated/accessibility.spec.ts:103-112`. Recorded as finding `H-01`.",
    "IMPLEMENTATION_SURFACES": "`CustomerProfileScreen.tsx:1128-1250`; dedicated adapter `profile-api.ts:127-162`; `governance-actions.ts:67-102`.",
    "BACKEND_READ_CONTRACTS": "`crm.note-list` GET /api/v1/customers/{id}/notes.",
    "BACKEND_WRITE_CONTRACTS": "`crm.note-add` POST same path; permission `crm.customer.note.write`.",
    "CONFLICT_STATE": "Generic only. Finding `H-02`.",
    "ARABIC_EVIDENCE": "`crm.noteClassification.*` and `crm.noteVisibility.*` resolve in ar.json.",
    "ENGLISH_EVIDENCE": "en.json resolves every key.",
    "PRODUCTION_REACHABILITY": "Reachable, mounted, gated `write-permission-gating.dom.test.tsx:158`, plus a cross-capability case `:223-239`.",
    "PRODUCTION_TEST_EVIDENCE": "`crm-customer-components.dom.test.tsx:160,170,178`; `crm-profile-api.test.ts:52,62,70,80,91,115` — the adapter suite exists because the DOM suite mocks the adapter wholesale, stated at `:6-15`.",
    "NEGATIVE_OR_MUTATION_PROOF": "Cross-capability proof: one write capability does not unlock another.",
    "ROUND5_FINDING_IDS": "None mapped.",
    "DOCUMENTATION_EVIDENCE": "traceability row; canonical-plan §5.1.",
    "FINAL_VERDICT": "PASS",
    "VERDICT_RATIONALE": "The best-evidenced section of the six; `Note.editedAt` renders as an em dash because the register publishes no note-edit operation, which is a contract limit rather than a defect.",
    "PROTECTED_REPROOF": "OUTSTANDING — the AUTHENTICATED browser tier. It is now wired and run: the `authenticated-browser` job (`protected-develop-verification.yml:176-419`) sets `ROOTLCO_E2E_AUTH: '1'` at `:319`, bootstraps a real operator and a second tenant, and fails unless every authenticated spec contributed an executed test. It runs on pushes to `develop` and `main` ONLY, and `.github/ci-baselines/unrun-test-tiers.json` (`notYetObservedOnARunner`) records that it has never executed on a hosted runner even once. Collection is proved locally at 229 tests across 7 spec files. The FEATURE is complete at this head; the REPROOF is unpaid."
  },
  "FE-012": {
    "NORMAL_NAVIGATION_ENTRY": "Sidebar `config/navigation.ts:174` -> /crm/customers -> row action `CustomerSearchScreen.tsx:214` -> `/[locale]/crm/customers/[customerId]` -> the section tab. Mounted via SECTIONS/BUILT `CustomerProfileScreen.tsx:101-142`.",
    "REQUIRED_PERMISSIONS": "Page read `crm.customer.read` (`[customerId]/page.tsx:62`); the write form is gated by `ComponentSection` `CustomerProfileScreen.tsx:883` on `table.response && canWrite`, with `writes={permittedWrites(session.permissions)}` at `page.tsx:130`.",
    "TENANT_ISOLATION_REQUIREMENT": "Client sends cursor and limit only (`profile-api.ts:67-69`); `query()` throws on any tenant/company/branch key (`read-operation.ts:89-100`), asserted `p1-27-security.test.ts:270-276`.",
    "LOADING_STATE": "`DataTable.tsx:267-279`.",
    "EMPTY_STATE": "`DataTable.tsx:302-321` — code present; no positive test for these sections.",
    "ZERO_RESULT_STATE": "N/A with rationale: the section defines no filters, so `isNarrowed()` is always false and the narrowed-empty branch is structurally unreachable. Correct rather than missing.",
    "ERROR_STATE": "`DataTable.tsx:165-167`, correlation id proved `crm-customer-profile.dom.test.tsx:334`.",
    "DENIAL_STATE": "`DataTable.tsx:131-136`; write form withheld, both directions proved `write-permission-gating.dom.test.tsx:203-221`.",
    "RTL_LTR_EVIDENCE": "Catalogue parity enforced `apps/web/tests/i18n.test.ts:72`; RTL render helper used by the DOM suites.",
    "ACCESSIBILITY_EVIDENCE": "Semantic only — sr-only h2, table caption, `role=\"region\"`, labelled fields. NO axe scan: the customer-profile route is absent from `apps/web/tests/e2e/authenticated/accessibility.spec.ts:103-112`. Recorded as finding `H-01`.",
    "IMPLEMENTATION_SURFACES": "`CustomerProfileScreen.tsx:1252-1383`; `profile-api.ts:164-170`; `governance-actions.ts:104-134`.",
    "BACKEND_READ_CONTRACTS": "`crm.alert-list` GET /api/v1/customers/{id}/alerts.",
    "BACKEND_WRITE_CONTRACTS": "`crm.alert-raise` POST same path; permission `crm.customer.governance.manage`.",
    "CONFLICT_STATE": "Generic only; active-only scope stated on screen `:1341`. Finding `H-02`.",
    "ARABIC_EVIDENCE": "`crm.alertType.*` and `crm.severity.*` resolve in ar.json (`crm-customer-components.dom.test.tsx:438-439`).",
    "ENGLISH_EVIDENCE": "en.json resolves every key.",
    "PRODUCTION_REACHABILITY": "Reachable, mounted, gated `write-permission-gating.dom.test.tsx:159`.",
    "PRODUCTION_TEST_EVIDENCE": "`crm-customer-components.dom.test.tsx:118,126,135,141,250,274,294`; `governance-write-validation.test.ts:161-169,211-214`.",
    "NEGATIVE_OR_MUTATION_PROOF": "Permission withholding proved.",
    "ROUND5_FINDING_IDS": "None mapped.",
    "DOCUMENTATION_EVIDENCE": "traceability row; canonical-plan §5.1.",
    "FINAL_VERDICT": "PASS",
    "VERDICT_RATIONALE": "`severityRank` drives emphasis and option order only, never a claim; `acknowledgedBy`/`acknowledgedAt` are contract fields with no registered acknowledge operation and are rendered nowhere, so nothing is promised that cannot be delivered.",
    "PROTECTED_REPROOF": "OUTSTANDING — the AUTHENTICATED browser tier. It is now wired and run: the `authenticated-browser` job (`protected-develop-verification.yml:176-419`) sets `ROOTLCO_E2E_AUTH: '1'` at `:319`, bootstraps a real operator and a second tenant, and fails unless every authenticated spec contributed an executed test. It runs on pushes to `develop` and `main` ONLY, and `.github/ci-baselines/unrun-test-tiers.json` (`notYetObservedOnARunner`) records that it has never executed on a hosted runner even once. Collection is proved locally at 229 tests across 7 spec files. The FEATURE is complete at this head; the REPROOF is unpaid."
  },
  "FE-001": {
    "IMPLEMENTATION_SURFACES": "`app/[locale]/(dashboard)/crm/customers/page.tsx:25-89`; `features/crm/customers/components/CustomerSearchScreen.tsx:67-114` and `:252-400`.",
    "NORMAL_NAVIGATION_ENTRY": "Sidebar entry `config/navigation.ts:172-187` (`status:available`, permission `crm.customer.read`), rendered through `AppShell.tsx:8` -> `Sidebar.tsx`; pinned by `tests/navigation.test.ts:66,89`.",
    "BACKEND_READ_CONTRACTS": "`searchCustomers` (`features/crm/customers/api.ts:20`) -> `searchCustomerDirectory` (`lib/customers/directory.ts:54`) -> GET /api/v1/customers, `retries: 0` at `:85`.",
    "BACKEND_WRITE_CONTRACTS": "N/A with rationale: a search performs no write and the plan registers none.",
    "REQUIRED_PERMISSIONS": "`crm.customer.read` (`page.tsx:36`); `crm.customer.create` gates the create links (`:52`).",
    "TENANT_ISOLATION_REQUIREMENT": "Client sends no tenant/company/branch key; `query()` throws on any scope key (`lib/api/read-operation.ts:92-100,126-133`), asserted `apps/web/tests/p1-27-security.test.ts:270-276`.",
    "LOADING_STATE": "`DataTable.tsx:267-279`.",
    "EMPTY_STATE": "Not-yet-searched idle state `CustomerSearchScreen.tsx:95-102`, distinct from a zero-result.",
    "ZERO_RESULT_STATE": "`CustomerSearchScreen.tsx:235-247`; the table empty state is suppressed at `:210` so the two cannot be confused.",
    "ERROR_STATE": "`DataTable.tsx:165-167` with retry.",
    "DENIAL_STATE": "`DataTable.tsx:131-136`; a denial is rendered instead of an empty list (`crm-customer-search.dom.test.tsx:139`).",
    "CONFLICT_STATE": "N/A with rationale: a read publishes no conflict.",
    "ARABIC_EVIDENCE": "Keys resolve in ar.json; RTL render asserted `crm-customer-search.dom.test.tsx:210`.",
    "ENGLISH_EVIDENCE": "en.json:299-317,209-219,335.",
    "RTL_LTR_EVIDENCE": "Catalogue parity `tests/i18n.test.ts:72`; RTL case as above.",
    "ACCESSIBILITY_EVIDENCE": "`/crm/customers` IS axe-scanned (`accessibility.spec.ts:108`).",
    "PRODUCTION_REACHABILITY": "Reachable from the sidebar; no request issued before an explicit submit (`crm-customer-search.dom.test.tsx:74`).",
    "PRODUCTION_TEST_EVIDENCE": "`crm-customer-search.dom.test.tsx` (13 cases on the real component); `crm-customer-search.test.ts` (41); `p1-27-qa.test.ts:82-85` drives the real adapter through all 11 failure kinds.",
    "NEGATIVE_OR_MUTATION_PROOF": "Stale-response case `:178` proves the second search wins; denial-not-empty `:139`.",
    "ROUND5_FINDING_IDS": "None mapped.",
    "DOCUMENTATION_EVIDENCE": "canonical-plan §5.1; traceability row.",
    "FINAL_VERDICT": "PASS",
    "VERDICT_RATIONALE": "Reachable, permission-gated, server-scoped, every state distinct, and exercised at both component and adapter level; the request URL is additionally asserted by source scan, which is weaker than execution but not the only evidence.",
    "PROTECTED_REPROOF": "OUTSTANDING — the AUTHENTICATED browser tier. It is now wired and run: the `authenticated-browser` job (`protected-develop-verification.yml:176-419`) sets `ROOTLCO_E2E_AUTH: '1'` at `:319`, bootstraps a real operator and a second tenant, and fails unless every authenticated spec contributed an executed test. It runs on pushes to `develop` and `main` ONLY, and `.github/ci-baselines/unrun-test-tiers.json` (`notYetObservedOnARunner`) records that it has never executed on a hosted runner even once. Collection is proved locally at 229 tests across 7 spec files. The FEATURE is complete at this head; the REPROOF is unpaid."
  },
  "FE-002": {
    "IMPLEMENTATION_SURFACES": "`CustomerSearchScreen.tsx:124-250` (`CustomerSearchResults`), columns `:144-178`, row action `:211-219`.",
    "NORMAL_NAVIGATION_ENTRY": "Same route; results mount only after a submission (`:95-111`, keyed on criteria `:105`).",
    "BACKEND_READ_CONTRACTS": "Same adapter as FE-001; cursor paging via `useServerTable`.",
    "BACKEND_WRITE_CONTRACTS": "N/A with rationale: results perform no write.",
    "REQUIRED_PERMISSIONS": "Inherits `crm.customer.read`.",
    "TENANT_ISOLATION_REQUIREMENT": "Client sends no tenant/company/branch key; `query()` throws on any scope key (`lib/api/read-operation.ts:92-100,126-133`), asserted `apps/web/tests/p1-27-security.test.ts:270-276`. Criteria are deliberately kept OUT of `TableRequest` so no customer name reaches the address bar (`:196-209`).",
    "LOADING_STATE": "`DataTable.tsx:267-279`.",
    "EMPTY_STATE": "Idle state as FE-001.",
    "ZERO_RESULT_STATE": "`:235-247`, with the create actions offered; proved not to claim the tenant is empty (`search-empty-states.dom.test.tsx:90`).",
    "ERROR_STATE": "`DataTable.tsx:165-167`.",
    "DENIAL_STATE": "`DataTable.tsx:131-136`.",
    "CONFLICT_STATE": "N/A with rationale: read only.",
    "ARABIC_EVIDENCE": "`crm.customers.column.*`, `crm.partyType.*`, `crm.lifecycle.*` resolve in ar.json.",
    "ENGLISH_EVIDENCE": "en.json:209-219,373-385.",
    "RTL_LTR_EVIDENCE": "Parity gate; RTL case in the DOM suite.",
    "ACCESSIBILITY_EVIDENCE": "Covered by the `/crm/customers` axe scan; the scroll region is focusable and named (`DataTable.tsx:240-245`).",
    "PRODUCTION_REACHABILITY": "Row action navigates to the profile by id.",
    "PRODUCTION_TEST_EVIDENCE": "`crm-customer-search.dom.test.tsx:102,178`; `search-empty-states.dom.test.tsx:90,104,113,122`.",
    "NEGATIVE_OR_MUTATION_PROOF": "No `total` is published (`directory.ts:100-103`), so the pager offers Previous/Next rather than an invented range.",
    "ROUND5_FINDING_IDS": "None mapped.",
    "DOCUMENTATION_EVIDENCE": "canonical-plan §5.1; traceability row.",
    "FINAL_VERDICT": "PASS",
    "VERDICT_RATIONALE": "Zero-result is distinguished from empty by construction rather than by copy, and the search terms are structurally prevented from reaching the URL.",
    "PROTECTED_REPROOF": "NOT REQUIRED. Every proof this row cites is executed by the `web` tier or by `tests/ci`, both of which run on every pull request (`web-quality` and `static-quality` in `_reusable-node-quality.yml`). Nothing here waits on a job that only a protected push starts."
  },
  "FE-003": {
    "IMPLEMENTATION_SURFACES": "`CustomerCreateScreen.tsx:267-353`, duplicate block `:299-335`; shape `creation-contract.ts:69-89`.",
    "NORMAL_NAVIGATION_ENTRY": "Reachable only as the OUTCOME of the create form at `/crm/customers/new/{kind}`.",
    "BACKEND_READ_CONTRACTS": "NEITHER canonical operation is called. The warning reads `possibleDuplicates` off the creation response (`creation-actions.ts:120,136,155,167`). `crm.duplicate-list` serves FE-016; `crm.duplicate-scan` has ZERO callers in `apps/web`.",
    "BACKEND_WRITE_CONTRACTS": "N/A with rationale: the warning writes nothing; it reports what the create returned.",
    "REQUIRED_PERMISSIONS": "None of its own; inherits `crm.customer.create` (`new/[kind]/page.tsx:46`).",
    "TENANT_ISOLATION_REQUIREMENT": "Client sends no tenant/company/branch key; `query()` throws on any scope key (`lib/api/read-operation.ts:92-100,126-133`), asserted `apps/web/tests/p1-27-security.test.ts:270-276`.",
    "LOADING_STATE": "Belongs to the creation action (`CustomerCreateScreen.tsx:247`).",
    "EMPTY_STATE": "No duplicates -> the section is absent (`:335`). Correct: an empty \"no look-alikes\" panel would be noise.",
    "ZERO_RESULT_STATE": "N/A with rationale: the section renders only when the response carries candidates.",
    "ERROR_STATE": "Belongs to the creation action `role=\"alert\"` `:100-114`.",
    "DENIAL_STATE": "Route-level on the create page.",
    "CONFLICT_STATE": "Inherited from the create action.",
    "ARABIC_EVIDENCE": "`crm.customers.create.duplicatesTitle/duplicatesBody` in ar.json:239-240.",
    "ENGLISH_EVIDENCE": "Same keys in en.json.",
    "RTL_LTR_EVIDENCE": "Parity gate; the create screen has an RTL case `:282`.",
    "ACCESSIBILITY_EVIDENCE": "NO axe coverage: `/crm/customers/new/*` is absent from `accessibility.spec.ts:95-115`. Finding `H-01`.",
    "PRODUCTION_REACHABILITY": "Reachable, and deliberately post-hoc: no pre-check request is issued while typing (`crm-customer-create.dom.test.tsx:128`).",
    "PRODUCTION_TEST_EVIDENCE": "`crm-customer-create.dom.test.tsx:128,139,163`; absence of a pre-check asserted `:288-307`.",
    "NEGATIVE_OR_MUTATION_PROOF": "Absence-of-pre-check proved by source scan and by a no-request behavioural case.",
    "ROUND5_FINDING_IDS": "`A42-07` FIXED at `3bb03b5` — `canonical-plan.md:144` now assigns this task \"none of its own — see §7.1\" instead of two operations it never called.",
    "DOCUMENTATION_EVIDENCE": "Divergence documented in code `creation-contract.ts:25-59`; the canonical plan was NOT updated (`canonical-plan.md:144,268`).",
    "FINAL_VERDICT": "PASS",
    "VERDICT_RATIONALE": "A real and honest post-hoc warning ships, and the specification now says what the task does. The single reason this was PARTIAL was `A42-07`: the plan assigned `FE-003` two Backend operations it does not use, so the task and its own specification disagreed. Re-assessed at `26eab7e`; the previous cell was written at `9eff8bd` and the tree moved. `canonical-plan.md:144` reads `none of its own — see §7.1`, and the matrix generator derives `CANONICAL_BACKEND_OPERATIONS` from that line rather than remembering it, so the two cannot come apart again without `validate:p1-27-matrix` failing. Nothing else was outstanding on this row.",
    "PROTECTED_REPROOF": "OUTSTANDING — the AUTHENTICATED browser tier. It is now wired and run: the `authenticated-browser` job (`protected-develop-verification.yml:176-419`) sets `ROOTLCO_E2E_AUTH: '1'` at `:319`, bootstraps a real operator and a second tenant, and fails unless every authenticated spec contributed an executed test. It runs on pushes to `develop` and `main` ONLY, and `.github/ci-baselines/unrun-test-tiers.json` (`notYetObservedOnARunner`) records that it has never executed on a hosted runner even once. Collection is proved locally at 229 tests across 7 spec files. The FEATURE is complete at this head; the REPROOF is unpaid."
  },
  "FE-004": {
    "IMPLEMENTATION_SURFACES": "Route `new/[kind]/page.tsx:28-82`; `CustomerCreateScreen.tsx:67-258`, individual fields `:116-148`; action `creation-actions.ts:104-138`.",
    "NORMAL_NAVIGATION_ENTRY": "`/[locale]/crm/customers/new/individual`, linked from `CustomerCreateActions.tsx:70-76`, mounted in the search header and again under a zero result.",
    "BACKEND_WRITE_CONTRACTS": "`crm.individual-create` POST /api/v1/customers/individuals (`creation-actions.ts:120`); the idempotency key comes from the operation registry, never from the HTTP method.",
    "PRODUCTION_TEST_EVIDENCE": "`crm-customer-create.dom.test.tsx` — 17 cases on the real screen (`:92,99,105,202,220,239`); `field-error-translation.test.ts:147-159`.",
    "VERDICT_RATIONALE": "Screen behaviour was already proven, including that the chosen initial status survives a failed submit. The single reason this was PARTIAL was that `createIndividualAction` was never EXECUTED — its path, body and Zod bounds were asserted by source scan only. Re-assessed at `26eab7e`; the previous cell was written at `9eff8bd` and the tree moved. It is now one of the twenty-three adapters `write-adapters-driven.test.ts` executes (66 cases, measured at this head), and the driven set is compared against a filesystem walk in BOTH directions, so a twenty-fourth adapter fails the suite rather than joining it unnoticed.",
    "BACKEND_READ_CONTRACTS": "N/A with rationale: the create screen reads nothing; the duplicate warning consumes the create response (FE-003).",
    "REQUIRED_PERMISSIONS": "`crm.customer.create` at `new/[kind]/page.tsx:46` for visibility; no permission check inside the action, deliberately (`creation-actions.ts:26-29`) — the server is the authority.",
    "TENANT_ISOLATION_REQUIREMENT": "Client sends no tenant/company/branch key; `query()` throws on any scope key (`lib/api/read-operation.ts:92-100,126-133`), asserted `apps/web/tests/p1-27-security.test.ts:270-276`. No scope key in the request body.",
    "LOADING_STATE": "`CustomerCreateScreen.tsx:247` (`form.saving`).",
    "EMPTY_STATE": "N/A with rationale: a create form has no collection to be empty.",
    "ZERO_RESULT_STATE": "N/A with rationale: as above.",
    "ERROR_STATE": "One `role=\"alert\"` keyed on `attempt` `:100-114`; per-field errors `:407-411` with `aria-invalid`/`aria-describedby` `:398-399`.",
    "DENIAL_STATE": "Route-level at `new/[kind]/page.tsx:46`.",
    "CONFLICT_STATE": "Resolves to `state.conflict.title` (`client.ts:369`, en.json:706); no bespoke copy. Finding `H-02`.",
    "ARABIC_EVIDENCE": "`crm.customers.create.*` complete in ar.json:235-252.",
    "ENGLISH_EVIDENCE": "en.json:235-252 plus `field.*`/`form.*` en.json:436-467.",
    "RTL_LTR_EVIDENCE": "RTL case `crm-customer-create.dom.test.tsx:282`.",
    "ACCESSIBILITY_EVIDENCE": "NO axe coverage: `/crm/customers/new/*` is absent from `accessibility.spec.ts:95-115`. Finding `H-01`.",
    "PRODUCTION_REACHABILITY": "Two real link mount points each.",
    "NEGATIVE_OR_MUTATION_PROOF": "Failed-submit retention proved on the real screen; NO execution-level proof of the write adapter. Finding `H-05`.",
    "ROUND5_FINDING_IDS": "`H-05` FIXED at `c283354` — `createIndividualAction` was a `vi.fn()` in every file that named it. `tests/support/write-drives.ts:78` now drives it and `write-adapters-driven.test.ts` executes it against the real Zod schema and the real body assembly, with only `@/lib/api/server-client` mocked.",
    "DOCUMENTATION_EVIDENCE": "canonical-plan §5.1; traceability row.",
    "FINAL_VERDICT": "PASS",
    "PROTECTED_REPROOF": "OUTSTANDING — the AUTHENTICATED browser tier. It is now wired and run: the `authenticated-browser` job (`protected-develop-verification.yml:176-419`) sets `ROOTLCO_E2E_AUTH: '1'` at `:319`, bootstraps a real operator and a second tenant, and fails unless every authenticated spec contributed an executed test. It runs on pushes to `develop` and `main` ONLY, and `.github/ci-baselines/unrun-test-tiers.json` (`notYetObservedOnARunner`) records that it has never executed on a hosted runner even once. Collection is proved locally at 229 tests across 7 spec files. The FEATURE is complete at this head; the REPROOF is unpaid."
  },
  "FE-005": {
    "IMPLEMENTATION_SURFACES": "Same route and screen; company fields `CustomerCreateScreen.tsx:150-172`; action `creation-actions.ts:140-169`.",
    "NORMAL_NAVIGATION_ENTRY": "`/[locale]/crm/customers/new/company`, linked from `CustomerCreateActions.tsx:77-83`.",
    "BACKEND_WRITE_CONTRACTS": "`crm.company-create` POST /api/v1/customers/companies (`:155`); `tradeName` optional and OMITTED rather than nulled (`:157`).",
    "PRODUCTION_TEST_EVIDENCE": "`crm-customer-create.dom.test.tsx:115,121,282`.",
    "VERDICT_RATIONALE": "Same shape as `FE-004` and closed by the same work. Both reasons this was PARTIAL are gone: `A42-10` (a miscounted evidence cell) is FIXED, and `createCompanyAction` is no longer mocked everywhere and executed nowhere. Re-assessed at `26eab7e`; the previous cell was written at `9eff8bd` and the tree moved. The company branch of the creation form is driven through the real adapter with the real schema, and the two-directional set comparison is what keeps that true.",
    "BACKEND_READ_CONTRACTS": "N/A with rationale: the create screen reads nothing; the duplicate warning consumes the create response (FE-003).",
    "REQUIRED_PERMISSIONS": "`crm.customer.create` at `new/[kind]/page.tsx:46` for visibility; no permission check inside the action, deliberately (`creation-actions.ts:26-29`) — the server is the authority.",
    "TENANT_ISOLATION_REQUIREMENT": "Client sends no tenant/company/branch key; `query()` throws on any scope key (`lib/api/read-operation.ts:92-100,126-133`), asserted `apps/web/tests/p1-27-security.test.ts:270-276`. No scope key in the request body.",
    "LOADING_STATE": "`CustomerCreateScreen.tsx:247` (`form.saving`).",
    "EMPTY_STATE": "N/A with rationale: a create form has no collection to be empty.",
    "ZERO_RESULT_STATE": "N/A with rationale: as above.",
    "ERROR_STATE": "One `role=\"alert\"` keyed on `attempt` `:100-114`; per-field errors `:407-411` with `aria-invalid`/`aria-describedby` `:398-399`.",
    "DENIAL_STATE": "Route-level at `new/[kind]/page.tsx:46`.",
    "CONFLICT_STATE": "Resolves to `state.conflict.title` (`client.ts:369`, en.json:706); no bespoke copy. Finding `H-02`.",
    "ARABIC_EVIDENCE": "`crm.customers.create.*` complete in ar.json:235-252.",
    "ENGLISH_EVIDENCE": "en.json:235-252 plus `field.*`/`form.*` en.json:436-467.",
    "RTL_LTR_EVIDENCE": "RTL case `crm-customer-create.dom.test.tsx:282`.",
    "ACCESSIBILITY_EVIDENCE": "NO axe coverage: `/crm/customers/new/*` is absent from `accessibility.spec.ts:95-115`. Finding `H-01`.",
    "PRODUCTION_REACHABILITY": "Two real link mount points each.",
    "NEGATIVE_OR_MUTATION_PROOF": "Failed-submit retention proved on the real screen; NO execution-level proof of the write adapter. Finding `H-05`.",
    "ROUND5_FINDING_IDS": "`A42-10` FIXED — the evidence cell named four cases where the file holds three. `H-05` FIXED at `c283354`: `createCompanyAction` is driven at `write-drives.ts:86` and executed by `write-adapters-driven.test.ts`.",
    "DOCUMENTATION_EVIDENCE": "canonical-plan §5.1; traceability row.",
    "FINAL_VERDICT": "PASS",
    "PROTECTED_REPROOF": "OUTSTANDING — the AUTHENTICATED browser tier. It is now wired and run: the `authenticated-browser` job (`protected-develop-verification.yml:176-419`) sets `ROOTLCO_E2E_AUTH: '1'` at `:319`, bootstraps a real operator and a second tenant, and fails unless every authenticated spec contributed an executed test. It runs on pushes to `develop` and `main` ONLY, and `.github/ci-baselines/unrun-test-tiers.json` (`notYetObservedOnARunner`) records that it has never executed on a hosted runner even once. Collection is proved locally at 229 tests across 7 spec files. The FEATURE is complete at this head; the REPROOF is unpaid."
  },
  "FE-006": {
    "IMPLEMENTATION_SURFACES": "`[customerId]/page.tsx:36-137`; `CustomerProfileScreen.tsx:161-284`, header `:439-489`, overview `:491-530`, section shell `:790-886`.",
    "NORMAL_NAVIGATION_ENTRY": "From a search result row (`CustomerSearchScreen.tsx:211-219`) and from the creation outcome (`CustomerCreateScreen.tsx:338-343`). No sidebar entry, correctly — it needs a record id.",
    "BACKEND_READ_CONTRACTS": "`readCustomer` (`profile-api.ts:47-49`) -> GET /api/v1/customers/{id}, plus nine component lists `:85-186` and `listTimeline`.",
    "BACKEND_WRITE_CONTRACTS": "Nine section actions, each gated by `permittedWrites(session.permissions)` passed at `page.tsx:130`.",
    "REQUIRED_PERMISSIONS": "`crm.customer.read` at `page.tsx:62`; per-section write codes via `governance-contract.permittedWrites`, defaulting to `NO_WRITES` (`CustomerProfileScreen.tsx:161`).",
    "TENANT_ISOLATION_REQUIREMENT": "Client sends no tenant/company/branch key; `query()` throws on any scope key (`lib/api/read-operation.ts:92-100,126-133`), asserted `apps/web/tests/p1-27-security.test.ts:270-276`. Paths are fixed segments plus `encodeURIComponent` (`profile-api.ts:58-69`).",
    "LOADING_STATE": "Route-level `app/[locale]/(dashboard)/loading.tsx`, plus per-section `DataTable` loading.",
    "EMPTY_STATE": "Per section via `DataTable`; the unbuilt `vehicles` tab says so honestly (`:277-281`).",
    "ZERO_RESULT_STATE": "N/A with rationale: the profile is a record view, not a filtered list.",
    "ERROR_STATE": "`page.tsx:82-103` with a correlation id — a defect this branch fixed.",
    "DENIAL_STATE": "`page.tsx:62-64` and `:68-75` with a correlation id.",
    "CONFLICT_STATE": "On the status write, via `fromFailure`. Generic write-failure copy via `RecordForm.tsx:339-348` / `action-result.ts`; no conflict-specific copy and no conflict test. Finding `H-02`.",
    "ARABIC_EVIDENCE": "`crm.customers.profile.*` complete in ar.json:273-290.",
    "ENGLISH_EVIDENCE": "Same keys in en.json.",
    "RTL_LTR_EVIDENCE": "Parity gate plus RTL cases in the profile DOM suite.",
    "ACCESSIBILITY_EVIDENCE": "NO axe coverage: the customer-profile route is absent from `apps/web/tests/e2e/authenticated/accessibility.spec.ts:103-112`, and the spec records the gap in its own words at `:84-93`. Finding `H-01`.",
    "PRODUCTION_REACHABILITY": "Reachable by row action and by the creation outcome.",
    "PRODUCTION_TEST_EVIDENCE": "`crm-customer-profile.dom.test.tsx` (21); `crm-customer-components.dom.test.tsx` (~28); `crm-profile-api.test.ts` drives the component adapters; `route-correlation-binding.test.ts:116,185` executes the real route.",
    "NEGATIVE_OR_MUTATION_PROOF": "`write-permission-gating.dom.test.tsx:223,241,252` — one capability does not unlock another.",
    "ROUND5_FINDING_IDS": "`H-01` FIXED — the profile had zero accessibility coverage; `profile-accessibility.dom.test.tsx` (26 cases, measured) now scans `CustomerProfileScreen` section by section in both directions. `H-06` OPEN — `readCustomer` is executed by nothing.",
    "DOCUMENTATION_EVIDENCE": "canonical-plan §5.1; traceability row.",
    "FINAL_VERDICT": "PARTIAL",
    "VERDICT_RATIONALE": "Route, permission split and every failure state are proven, and the accessibility half — one of the two reasons this was PARTIAL — is closed. Re-assessed at `26eab7e`; the previous cell was written at `9eff8bd` and the tree moved. ONE BINDING REASON REMAINS (`H-06`): `readCustomer` is this task's canonical operation (`crm.customer-read`) and the read that decides whether the page renders at all, and it is a `vi.fn()` in all six files that name it — `crm-customer-profile.dom.test.tsx:36`, `crm-customer-components.dom.test.tsx:36`, `record-form-consumers.dom.test.tsx:133`, `timeline-actor-identity.dom.test.tsx:59`, `route-correlation-binding.test.ts:57` and `p1-27-permission-route-binding.dom.test.tsx:121`. Nothing executes the real adapter, so its path, its `retries` setting and its failure mapping are asserted by nothing. CLOSABLE TODAY: give the READ side the treatment `write-drives.ts` gave the writes — one accepted call per read adapter, executed with only `server-client` mocked, with the driven set compared against a filesystem walk in both directions. This is not a defect in the screen; it is an untested adapter behind a screen that works.",
    "PROTECTED_REPROOF": "OUTSTANDING — the AUTHENTICATED browser tier. It is now wired and run: the `authenticated-browser` job (`protected-develop-verification.yml:176-419`) sets `ROOTLCO_E2E_AUTH: '1'` at `:319`, bootstraps a real operator and a second tenant, and fails unless every authenticated spec contributed an executed test. It runs on pushes to `develop` and `main` ONLY, and `.github/ci-baselines/unrun-test-tiers.json` (`notYetObservedOnARunner`) records that it has never executed on a hosted runner even once. Collection is proved locally at 229 tests across 7 spec files. The FEATURE is complete at this head; the REPROOF is unpaid."
  },
  "FE-013": {
    "TENANT_ISOLATION_REQUIREMENT": "Client sends no tenant/company/branch key; `query()` throws on any scope key (`lib/api/read-operation.ts:92-100,126-133`), asserted `apps/web/tests/p1-27-security.test.ts:270-276`.",
    "RTL_LTR_EVIDENCE": "Catalogue parity `tests/i18n.test.ts:72`; RTL cases in the owning DOM suite.",
    "IMPLEMENTATION_SURFACES": "`CustomerProfileScreen.tsx:1386-1480`; `profile-api.ts:172-178`; `governance-actions.ts:215-243`.",
    "NORMAL_NAVIGATION_ENTRY": "Profile \"Tags\" tab `CustomerProfileScreen.tsx:258-265`.",
    "BACKEND_READ_CONTRACTS": "`crm.tag-list` GET /api/v1/customers/{id}/tags.",
    "BACKEND_WRITE_CONTRACTS": "`crm.tag-assign` POST same path, `idempotent: true` (`idempotent-operations.ts:313-323`).",
    "REQUIRED_PERMISSIONS": "`crm.customer.governance.manage` (`governance-contract.ts:171`); gated at `CustomerProfileScreen.tsx:883`.",
    "LOADING_STATE": "`DataTable.tsx:267-279`.",
    "EMPTY_STATE": "`DataTable.tsx:302-310`.",
    "ZERO_RESULT_STATE": "N/A with rationale: the section has no filters.",
    "ERROR_STATE": "`DataTable.tsx:165`.",
    "DENIAL_STATE": "`DataTable.tsx:131`; form withheld.",
    "CONFLICT_STATE": "`action-result.ts:65`. Generic write-failure copy via `RecordForm.tsx:339-348` / `action-result.ts`; no conflict-specific copy and no conflict test. Finding `H-02`.",
    "ARABIC_EVIDENCE": "All tag keys resolve in ar.json (1090/1090 parity, zero divergence).",
    "ENGLISH_EVIDENCE": "Same in en.json.",
    "ACCESSIBILITY_EVIDENCE": "NO axe coverage: the customer-profile route is absent from `apps/web/tests/e2e/authenticated/accessibility.spec.ts:103-112`, and the spec records the gap in its own words at `:84-93`. Finding `H-01`.",
    "PRODUCTION_REACHABILITY": "Reachable and gated.",
    "PRODUCTION_TEST_EVIDENCE": "`crm-customer-components.dom.test.tsx:382-403`; `governance-write-validation.test.ts:141-157` drives the REAL action; `p1-27-qa.test.ts:60` drives `listTags` through 11 failure kinds.",
    "NEGATIVE_OR_MUTATION_PROOF": "`write-permission-gating.dom.test.tsx:203-221`, both directions.",
    "ROUND5_FINDING_IDS": "None mapped.",
    "DOCUMENTATION_EVIDENCE": "traceability row; canonical-plan §5.1.",
    "FINAL_VERDICT": "PASS",
    "VERDICT_RATIONALE": "The prior `validateTag` defect is genuinely fixed: the six mirrors are deleted (`governance-contract.ts:197-228`), a repository-wide grep returns zero occurrences, and the replacement suite drives the real action.",
    "PROTECTED_REPROOF": "OUTSTANDING — the AUTHENTICATED browser tier. It is now wired and run: the `authenticated-browser` job (`protected-develop-verification.yml:176-419`) sets `ROOTLCO_E2E_AUTH: '1'` at `:319`, bootstraps a real operator and a second tenant, and fails unless every authenticated spec contributed an executed test. It runs on pushes to `develop` and `main` ONLY, and `.github/ci-baselines/unrun-test-tiers.json` (`notYetObservedOnARunner`) records that it has never executed on a hosted runner even once. Collection is proved locally at 229 tests across 7 spec files. The FEATURE is complete at this head; the REPROOF is unpaid."
  },
  "FE-014": {
    "TENANT_ISOLATION_REQUIREMENT": "Client sends no tenant/company/branch key; `query()` throws on any scope key (`lib/api/read-operation.ts:92-100,126-133`), asserted `apps/web/tests/p1-27-security.test.ts:270-276`.",
    "RTL_LTR_EVIDENCE": "Catalogue parity `tests/i18n.test.ts:72`; RTL cases in the owning DOM suite.",
    "IMPLEMENTATION_SURFACES": "`CustomerProfileScreen.tsx:1493-1603`; `profile-api.ts:180-186`; `governance-actions.ts:136-168`.",
    "NORMAL_NAVIGATION_ENTRY": "Profile \"Restrictions\" tab `:266-273`.",
    "BACKEND_READ_CONTRACTS": "`crm.restriction-list` GET /api/v1/customers/{id}/restrictions.",
    "BACKEND_WRITE_CONTRACTS": "`crm.restriction-impose` POST same path, idempotent (`idempotent-operations.ts:295-305`).",
    "REQUIRED_PERMISSIONS": "Its own code `crm.customer.restriction.manage` (`permissions.ts:32`), deliberately not `governanceManage`.",
    "LOADING_STATE": "`DataTable.tsx:267-279`.",
    "EMPTY_STATE": "`DataTable.tsx:302-310`.",
    "ZERO_RESULT_STATE": "N/A with rationale: no filters.",
    "ERROR_STATE": "`DataTable.tsx:165`.",
    "DENIAL_STATE": "Fails CLOSED — no count, no reason, no vocabulary disclosed (`crm-customer-components.dom.test.tsx:190-246`).",
    "CONFLICT_STATE": "Generic write-failure copy via `RecordForm.tsx:339-348` / `action-result.ts`; no conflict-specific copy and no conflict test. Finding `H-02`.",
    "ARABIC_EVIDENCE": "All five `crm.restrictionType.*` resolve in ar.json.",
    "ENGLISH_EVIDENCE": "Same in en.json.",
    "ACCESSIBILITY_EVIDENCE": "NO axe coverage: the customer-profile route is absent from `apps/web/tests/e2e/authenticated/accessibility.spec.ts:103-112`, and the spec records the gap in its own words at `:84-93`. Finding `H-01`.",
    "PRODUCTION_REACHABILITY": "Reachable and gated.",
    "PRODUCTION_TEST_EVIDENCE": "`crm-customer-components.dom.test.tsx:190-246`; `write-permission-gating.dom.test.tsx:223-250`; `governance-write-validation.test.ts:88-139,171-177` drives the real action.",
    "NEGATIVE_OR_MUTATION_PROOF": "One capability does not unlock another, and the vocabulary is not disclosed to a denied caller.",
    "ROUND5_FINDING_IDS": "None mapped.",
    "DOCUMENTATION_EVIDENCE": "traceability row; canonical-plan §5.1.",
    "FINAL_VERDICT": "PASS",
    "VERDICT_RATIONALE": "The best-covered CRM section: a denied caller learns nothing, including the restriction vocabulary itself.",
    "PROTECTED_REPROOF": "OUTSTANDING — the AUTHENTICATED browser tier. It is now wired and run: the `authenticated-browser` job (`protected-develop-verification.yml:176-419`) sets `ROOTLCO_E2E_AUTH: '1'` at `:319`, bootstraps a real operator and a second tenant, and fails unless every authenticated spec contributed an executed test. It runs on pushes to `develop` and `main` ONLY, and `.github/ci-baselines/unrun-test-tiers.json` (`notYetObservedOnARunner`) records that it has never executed on a hosted runner even once. Collection is proved locally at 229 tests across 7 spec files. The FEATURE is complete at this head; the REPROOF is unpaid."
  },
  "FE-015": {
    "TENANT_ISOLATION_REQUIREMENT": "Client sends no tenant/company/branch key; `query()` throws on any scope key (`lib/api/read-operation.ts:92-100,126-133`), asserted `apps/web/tests/p1-27-security.test.ts:270-276`.",
    "RTL_LTR_EVIDENCE": "Catalogue parity `tests/i18n.test.ts:72`; RTL cases in the owning DOM suite.",
    "IMPLEMENTATION_SURFACES": "`CustomerProfileScreen.tsx:1613-1707`; `identity-api.ts:28-51`; `identity-contract.ts:41-105`.",
    "NORMAL_NAVIGATION_ENTRY": "Profile \"Timeline\" tab `:274-276`; no longer marked planned (`BUILT` at `:141`).",
    "BACKEND_READ_CONTRACTS": "`crm.customer-timeline` GET /api/v1/customers/{id}/timeline.",
    "BACKEND_WRITE_CONTRACTS": "N/A with rationale: no write exists and none should; documented `:1606-1611`. `canWrite={false}` is passed explicitly at `:1695` so the section cannot skip the question.",
    "REQUIRED_PERMISSIONS": "`crm.customer.read`.",
    "LOADING_STATE": "`DataTable.tsx:267-279`.",
    "EMPTY_STATE": "`DataTable.tsx:302-321`.",
    "ZERO_RESULT_STATE": "N/A with rationale: no filters.",
    "ERROR_STATE": "`DataTable.tsx:165`.",
    "DENIAL_STATE": "`DataTable.tsx:131`.",
    "CONFLICT_STATE": "N/A with rationale: read only.",
    "ARABIC_EVIDENCE": "Nine `crm.customers.timeline.*` and eight `crm.timelineEvent.*` resolve in ar.json.",
    "ENGLISH_EVIDENCE": "Same in en.json.",
    "ACCESSIBILITY_EVIDENCE": "NO axe coverage: the customer-profile route is absent from `apps/web/tests/e2e/authenticated/accessibility.spec.ts:103-112`, and the spec records the gap in its own words at `:84-93`. Finding `H-01`.",
    "PRODUCTION_REACHABILITY": "Reachable and mounted.",
    "PRODUCTION_TEST_EVIDENCE": "`timeline-actor-identity.dom.test.tsx:130-240` — 8 cases: named actor, uuid never printed, system vs unavailable, Arabic, non-vacuity; `p1-27-qa.test.ts:62`.",
    "NEGATIVE_OR_MUTATION_PROOF": "The uuid is asserted absent from `textContent` in every actor case.",
    "ROUND5_FINDING_IDS": "None mapped.",
    "DOCUMENTATION_EVIDENCE": "Limitation stated in `identity-contract.ts:81-102`, not hidden.",
    "FINAL_VERDICT": "PASS",
    "VERDICT_RATIONALE": "One honest limitation, documented rather than concealed: `crm.customer-timeline` never publishes `actorName`, so every human row reads the safe sentence. That is a Backend gap correctly stated, and no uuid can reach the screen.",
    "PROTECTED_REPROOF": "OUTSTANDING — the AUTHENTICATED browser tier. It is now wired and run: the `authenticated-browser` job (`protected-develop-verification.yml:176-419`) sets `ROOTLCO_E2E_AUTH: '1'` at `:319`, bootstraps a real operator and a second tenant, and fails unless every authenticated spec contributed an executed test. It runs on pushes to `develop` and `main` ONLY, and `.github/ci-baselines/unrun-test-tiers.json` (`notYetObservedOnARunner`) records that it has never executed on a hosted runner even once. Collection is proved locally at 229 tests across 7 spec files. The FEATURE is complete at this head; the REPROOF is unpaid."
  },
  "FE-016": {
    "TENANT_ISOLATION_REQUIREMENT": "Client sends no tenant/company/branch key; `query()` throws on any scope key (`lib/api/read-operation.ts:92-100,126-133`), asserted `apps/web/tests/p1-27-security.test.ts:270-276`.",
    "RTL_LTR_EVIDENCE": "Catalogue parity `tests/i18n.test.ts:72`; RTL cases in the owning DOM suite.",
    "IMPLEMENTATION_SURFACES": "`DuplicateReviewScreen.tsx:46-232`; `DuplicateDecisionPanel.tsx:56-247`; `identity-api.ts:74-145`.",
    "NORMAL_NAVIGATION_ENTRY": "`/[locale]/crm/customer-duplicates`, own sidebar entry `navigation.ts:188-199`.",
    "BACKEND_READ_CONTRACTS": "`crm.duplicate-list` GET /api/v1/customer-duplicates.",
    "BACKEND_WRITE_CONTRACTS": "`crm.duplicate-review` POST /customer-duplicates/{id}/review, idempotent, privileged audit. `crm.customer-merge` is NEVER called.",
    "REQUIRED_PERMISSIONS": "`crm.customer.duplicate.review` at `page.tsx:34`; `crm.customer.merge` is checked nowhere.",
    "LOADING_STATE": "`DataTable.tsx:267-279`.",
    "EMPTY_STATE": "`DataTable.tsx:302-321`.",
    "ZERO_RESULT_STATE": "Per status filter, the queue re-reads on `loadKey`.",
    "ERROR_STATE": "`DataTable.tsx:165`.",
    "DENIAL_STATE": "Route-level before the first read.",
    "CONFLICT_STATE": "Dismissal carries invalid/denied/conflict/expired via `fromFailure`; settled candidates offer no control (`:144-159`).",
    "ARABIC_EVIDENCE": "Keys resolve in ar.json, including `crm.duplicates.mergePendingDecision`.",
    "ENGLISH_EVIDENCE": "Same in en.json.",
    "ACCESSIBILITY_EVIDENCE": "Route IS axe-scanned (`accessibility.spec.ts:109`), but the decision panel mounts only after a click and is therefore never scanned. Finding `H-12` also records two identical `h1`s on this route.",
    "PRODUCTION_REACHABILITY": "Reachable; the merge affordance is ABSENT, not disabled, as `P1-OD-017` requires.",
    "PRODUCTION_TEST_EVIDENCE": "`crm-duplicate-review.dom.test.tsx` (24 cases, incl. `:148` \"offers dismissal and no merge, because P1-OD-017 is open\"); `crm-duplicate-review.test.ts:170-192`; e2e `crm-and-vehicles.spec.ts:167-185` asserts absence in the running app.",
    "NEGATIVE_OR_MUTATION_PROOF": "Absence of a merge validator and of an exported merge action, asserted directly.",
    "ROUND5_FINDING_IDS": "None mapped; raises `H-04`.",
    "DOCUMENTATION_EVIDENCE": "`P1-OD-017` OPEN (`docs/product/README.md:371`); canonical-plan §7 names `crm.duplicate-review`.",
    "FINAL_VERDICT": "PASS",
    "VERDICT_RATIONALE": "Merge is deliberately uncalled and the affordance absent with the decision id printed on screen, exactly as the open decision requires; the residual is a stale docblock at `crm/customer-duplicates/page.tsx:18-21` claiming `crm.customer.merge` gates a control that does not exist (`H-04`).",
    "PROTECTED_REPROOF": "OUTSTANDING — the AUTHENTICATED browser tier. It is now wired and run: the `authenticated-browser` job (`protected-develop-verification.yml:176-419`) sets `ROOTLCO_E2E_AUTH: '1'` at `:319`, bootstraps a real operator and a second tenant, and fails unless every authenticated spec contributed an executed test. It runs on pushes to `develop` and `main` ONLY, and `.github/ci-baselines/unrun-test-tiers.json` (`notYetObservedOnARunner`) records that it has never executed on a hosted runner even once. Collection is proved locally at 229 tests across 7 spec files. The FEATURE is complete at this head; the REPROOF is unpaid."
  },
  "FE-017": {
    "TENANT_ISOLATION_REQUIREMENT": "Client sends no tenant/company/branch key; `query()` throws on any scope key (`lib/api/read-operation.ts:92-100,126-133`), asserted `apps/web/tests/p1-27-security.test.ts:270-276`.",
    "RTL_LTR_EVIDENCE": "Catalogue parity `tests/i18n.test.ts:72`; RTL cases in the owning DOM suite.",
    "IMPLEMENTATION_SURFACES": "`VehicleSearchScreen.tsx:61-204` (form) and `:206-461` (results, separately mounted); `api.ts:40-69`.",
    "NORMAL_NAVIGATION_ENTRY": "`/[locale]/vehicles`, sidebar `navigation.ts:200-210`.",
    "BACKEND_READ_CONTRACTS": "`veh.vehicle-search` GET /api/v1/vehicles, `retries: 0`, `expensive-read`; empty criteria refused client-side (`api.ts:47-49`).",
    "BACKEND_WRITE_CONTRACTS": "N/A with rationale: the search performs no write; `canCreate` only gates links.",
    "REQUIRED_PERMISSIONS": "`veh.vehicle.read` at `page.tsx:37`; `veh.vehicle.manage` gates both create links `:88`.",
    "LOADING_STATE": "`DataTable.tsx:267`.",
    "EMPTY_STATE": "Pre-search idle `:184-189`.",
    "ZERO_RESULT_STATE": "Distinguished from empty at `:439-454` with `suppressEmptyState` `:407`.",
    "ERROR_STATE": "`DataTable` error with retry; 429 wording asserted.",
    "DENIAL_STATE": "`DataTable.tsx:131`; denial is not an empty list.",
    "CONFLICT_STATE": "N/A with rationale: read only.",
    "ARABIC_EVIDENCE": "Every `vehicles.search.*`, `vehicles.column.*` and lifecycle/powertrain/workshop enum resolves in ar.json.",
    "ENGLISH_EVIDENCE": "Same in en.json.",
    "ACCESSIBILITY_EVIDENCE": "Route axe-scanned (`accessibility.spec.ts:110`); sr-only results heading `:384`. The results table itself needs a submit and is not scanned.",
    "PRODUCTION_REACHABILITY": "Reachable; no read on mount; row action navigates by id.",
    "PRODUCTION_TEST_EVIDENCE": "`vehicle-screens.dom.test.tsx:149-334`; `search-empty-states.dom.test.tsx:131-181`; `vehicle-api.test.ts:49-155`; `p1-27-qa.test.ts:87-95` asserts the actual wire call.",
    "NEGATIVE_OR_MUTATION_PROOF": "e2e `crm-and-vehicles.spec.ts:254` asserts no search term reaches the URL.",
    "ROUND5_FINDING_IDS": "None mapped.",
    "DOCUMENTATION_EVIDENCE": "canonical-plan §5.2; traceability row.",
    "FINAL_VERDICT": "PASS",
    "VERDICT_RATIONALE": "Catalogue-failure and unrecognised-id are distinguished per row, and zero-result is structurally separated from empty rather than reworded.",
    "PROTECTED_REPROOF": "OUTSTANDING — the AUTHENTICATED browser tier. It is now wired and run: the `authenticated-browser` job (`protected-develop-verification.yml:176-419`) sets `ROOTLCO_E2E_AUTH: '1'` at `:319`, bootstraps a real operator and a second tenant, and fails unless every authenticated spec contributed an executed test. It runs on pushes to `develop` and `main` ONLY, and `.github/ci-baselines/unrun-test-tiers.json` (`notYetObservedOnARunner`) records that it has never executed on a hosted runner even once. Collection is proved locally at 229 tests across 7 spec files. The FEATURE is complete at this head; the REPROOF is unpaid."
  },
  "FE-018": {
    "TENANT_ISOLATION_REQUIREMENT": "Client sends no tenant/company/branch key; `query()` throws on any scope key (`lib/api/read-operation.ts:92-100,126-133`), asserted `apps/web/tests/p1-27-security.test.ts:270-276`.",
    "RTL_LTR_EVIDENCE": "Catalogue parity `tests/i18n.test.ts:72`; RTL cases in the owning DOM suite.",
    "IMPLEMENTATION_SURFACES": "`VehicleCreateScreen.tsx:75-404`, `CatalogueSelect:417-507`, `CreationOutcome:516-610`; `api.ts:119-192`.",
    "NORMAL_NAVIGATION_ENTRY": "`/[locale]/vehicles/new`, from the search header `:167-174` and the zero-result block `:444-452`.",
    "BACKEND_READ_CONTRACTS": "Five catalogue operations plus `checkVinAvailability` (`profile-api.ts:179-200`).",
    "BACKEND_WRITE_CONTRACTS": "`veh.vehicle-create` POST /api/v1/vehicles, idempotent, key from the shared authority.",
    "REQUIRED_PERMISSIONS": "`veh.vehicle.manage` at `new/page.tsx:36` — correctly not `veh.vehicle.read`.",
    "LOADING_STATE": "Submitting `:384`; catalogue loading `:484-497`.",
    "EMPTY_STATE": "Catalogue empty/truncated states `:484-497`.",
    "ZERO_RESULT_STATE": "N/A with rationale: a create form has no result set.",
    "ERROR_STATE": "Error plus correlation id `:389-396`.",
    "DENIAL_STATE": "Route-level `:36-50`.",
    "CONFLICT_STATE": "`api.ts:179-181` -> `vehicles.create.conflict`, deliberately NOT blamed on the VIN because `veh.vehicles` carries two unique indexes (`api.ts:155-178`).",
    "ARABIC_EVIDENCE": "Every `vehicles.create.*` key used resolves in ar.json.",
    "ENGLISH_EVIDENCE": "Same in en.json.",
    "ACCESSIBILITY_EVIDENCE": "Route axe-scanned (`accessibility.spec.ts:111`); every field has `htmlFor`/`id`, `aria-invalid`, `aria-describedby`, `role=\"alert\"`.",
    "PRODUCTION_REACHABILITY": "Two real link mount points.",
    "PRODUCTION_TEST_EVIDENCE": "`vehicle-create.dom.test.tsx` (16 cases); `vehicle-api.test.ts:253-360`; `vin-create-validation.dom.test.tsx`.",
    "NEGATIVE_OR_MUTATION_PROOF": "The client-validation branch advances `attempt` (`VehicleCreateScreen.tsx:118-123`) and every catalogue select is keyed on it; the retry is proved to SUBMIT the chosen ids by reading the actual `FormData` (`vehicle-create.dom.test.tsx:238-241`), not merely to display them.",
    "ROUND5_FINDING_IDS": "`F-01` — FIXED.",
    "DOCUMENTATION_EVIDENCE": "canonical-plan §5.2; round-five register.",
    "FINAL_VERDICT": "PASS",
    "VERDICT_RATIONALE": "Both halves of the round-five defect hold: the selects survive a client rejection AND the retry submits the chosen catalogue ids, proved against the real FormData.",
    "PROTECTED_REPROOF": "OUTSTANDING — the AUTHENTICATED browser tier. It is now wired and run: the `authenticated-browser` job (`protected-develop-verification.yml:176-419`) sets `ROOTLCO_E2E_AUTH: '1'` at `:319`, bootstraps a real operator and a second tenant, and fails unless every authenticated spec contributed an executed test. It runs on pushes to `develop` and `main` ONLY, and `.github/ci-baselines/unrun-test-tiers.json` (`notYetObservedOnARunner`) records that it has never executed on a hosted runner even once. Collection is proved locally at 229 tests across 7 spec files. The FEATURE is complete at this head; the REPROOF is unpaid."
  },
  "FE-019": {
    "IMPLEMENTATION_SURFACES": "`VehicleProfileScreen.tsx:103-853`; `profile-contract.ts:58-268`; `profile-api.ts:39-162`; page `vehicles/[vehicleId]/page.tsx:33-137`.",
    "NORMAL_NAVIGATION_ENTRY": "Sidebar `navigation.ts:202-204` -> search -> ROW ACTION `VehicleSearchScreen.tsx:423-431`, proved `vehicle-screens.dom.test.tsx:257-276`. The former \"reachable only from the duplicate queue\" defect is fixed.",
    "BACKEND_READ_CONTRACTS": "`readVehicle` GET /api/v1/vehicles/{id} (`profile-api.ts:39-41`).",
    "BACKEND_WRITE_CONTRACTS": "PATCH /vehicles/{id} (`:95-99`); PATCH /vehicles/{id}/status (`:149-153`).",
    "REQUIRED_PERMISSIONS": "`veh.vehicle.read` (`page.tsx:53`); `veh.vehicle.manage` -> EditPanel; `veh.vehicle.status.manage` -> StatusPanel. Both gated.",
    "TENANT_ISOLATION_REQUIREMENT": "No scope key anywhere in `features/vehicles`; swept `p1-27-qa.test.ts:509-514,566-573`.",
    "LOADING_STATE": "Route `loading.tsx`.",
    "EMPTY_STATE": "N/A with rationale: a record view has no collection of its own; each tab owns its empty state.",
    "ZERO_RESULT_STATE": "N/A with rationale: no filters on the profile itself.",
    "ERROR_STATE": "`page.tsx:85` with correlation id.",
    "DENIAL_STATE": "`page.tsx:53-62,82-84`.",
    "CONFLICT_STATE": "Via `Outcome` `:770-790`. DEFECT: the 409 on these two PATCHes renders `state.conflict.title` \"Someone else changed this\", but the real causes are a VIN collision and a merged/frozen vehicle. Finding `H-09`.",
    "ARABIC_EVIDENCE": "All 135 literal keys and every dynamic key resolve in ar.json; 273 `vehicles.*` keys, zero divergence.",
    "ENGLISH_EVIDENCE": "Same in en.json.",
    "RTL_LTR_EVIDENCE": "Parity gate plus RTL cases in `vehicle-profile-lifecycle.dom.test.tsx`.",
    "ACCESSIBILITY_EVIDENCE": "`profile-accessibility.dom.test.tsx` — 26 cases, measured — scans `VehicleProfileScreen` section by section in both directions against the four WCAG tags the browser gate claims, and pins that SC 2.5.3 comes back `incomplete` under jsdom rather than assuming either way. Route-level axe remains the authenticated tier `e2e/authenticated/accessibility.spec.ts`.",
    "PRODUCTION_REACHABILITY": "Reachable by row action; both panels gated.",
    "PRODUCTION_TEST_EVIDENCE": "`vehicle-profile-lifecycle.dom.test.tsx` (29 cases) on the real component; `vehicle-profile.test.ts` (29) on the real adapter.",
    "NEGATIVE_OR_MUTATION_PROOF": "Cross-axis rule verified against the server: `TERMINAL_LIFECYCLE` (`profile-contract.ts:198`) equals `vehicle-lifecycle.ts:78`, `pairRefused` (`:222-224`) equals server `:275`, and BOTH `allowedLifecycleTargets` and `allowedWorkshopTargets` judge the RESULTING pair. Status change re-reads via `router.refresh()` `:676`, tested `:587`.",
    "ROUND5_FINDING_IDS": "`H-07` FIXED at `ab4e6ec` — `VehicleProfileScreen.tsx:581` calls `router.refresh()` on success only. `H-08` FIXED at `ab4e6ec` — the panel renders `state.fieldErrors` at `:608`, `:745-751`, `:795-801`, `:816-822`, `:836-842` and passes `error` to `VinField` at `:624`. `H-09` FIXED at `fd51140`. `H-11` names nothing and is registered as an id that resolves to no finding; it was raised in this cell and its author must state it or withdraw it.",
    "DOCUMENTATION_EVIDENCE": "canonical-plan §5.2; traceability row.",
    "FINAL_VERDICT": "PASS",
    "VERDICT_RATIONALE": "The lifecycle cross-axis rule matches the server exactly and the status change re-reads. Both reasons this was PARTIAL named the DETAILS edit panel, and both are now false. Re-assessed at `26eab7e`; the previous cell was written at `9eff8bd` and the tree moved. FIRST, it re-reads: `VehicleProfileScreen.tsx:577-582` calls `router.refresh()` when and only when the action returned `success`, so the header no longer prints pre-edit values and the next edit is computed against fresh `original` values (`H-07`). SECOND, it renders field errors: `errorFor()` at `:608` reads `state.fieldErrors`, and the lifecycle select (`:745-751`, `:795-801`), the workshop select (`:816-822`, `:836-842`), `VinField` (`:624`) and the text controls all carry it with `aria-invalid` and `aria-errormessage` (`H-08`). `vehicle-profile-lifecycle.dom.test.tsx` is 41 cases, measured and passing at this head, and drives a real 422 with `violations` through to the rendered field. `H-11` remains an id nobody described; that is a register defect, not a defect in this screen.",
    "PROTECTED_REPROOF": "OUTSTANDING — the AUTHENTICATED browser tier. It is now wired and run: the `authenticated-browser` job (`protected-develop-verification.yml:176-419`) sets `ROOTLCO_E2E_AUTH: '1'` at `:319`, bootstraps a real operator and a second tenant, and fails unless every authenticated spec contributed an executed test. It runs on pushes to `develop` and `main` ONLY, and `.github/ci-baselines/unrun-test-tiers.json` (`notYetObservedOnARunner`) records that it has never executed on a hosted runner even once. Collection is proved locally at 229 tests across 7 spec files. The FEATURE is complete at this head; the REPROOF is unpaid."
  },
  "FE-020": {
    "IMPLEMENTATION_SURFACES": "`VinField.tsx:84-228`; `profile-contract.ts:281-333`; `profile-api.ts:179-200`.",
    "NORMAL_NAVIGATION_ENTRY": "Two mount points: create `VehicleCreateScreen.tsx:224-232`, update `VehicleProfileScreen.tsx:617-625`.",
    "BACKEND_READ_CONTRACTS": "`checkVinAvailability` GET /api/v1/vehicles?vin=…&limit=2, `retries: 0`.",
    "BACKEND_WRITE_CONTRACTS": "Conflict paths of `veh.vehicle-create` and `veh.vehicle-update`.",
    "REQUIRED_PERMISSIONS": "N/A with rationale: the check rides on `veh.vehicle.read`; the field sits inside panels already gated by create or `veh.vehicle.manage`, and no separate code exists.",
    "TENANT_ISOLATION_REQUIREMENT": "Query carries `vin` and `limit` only.",
    "LOADING_STATE": "Checking state in the field.",
    "EMPTY_STATE": "N/A with rationale: a single field has no collection.",
    "ZERO_RESULT_STATE": "N/A with rationale: as above.",
    "ERROR_STATE": "Transport failure resolves to `unavailable`, never `available` (`profile-api.ts:187,193`).",
    "DENIAL_STATE": "Inherited from the enclosing panel.",
    "CONFLICT_STATE": "Attributed on BOTH mount points now. `VinField` takes `error` on create (`VehicleCreateScreen.tsx:231`) and on update (`VehicleProfileScreen.tsx:624`, `error={errorFor('vin')}`), so a 422 naming `body.vin` is rendered on the field in either panel. A 409 is deliberately NOT attributed to the VIN, and that is correct rather than missing: `veh.vehicles` carries TWO partial unique indexes — `uq_vehicles_active_vin` and `uq_vehicles_active_display_number` (`supabase/migrations/20260720092000_veh_vehicles.sql:128,131`) — and the 409 carries a catalog code with no field detail, so naming the VIN would be an invention. `H-09` fixed the copy to say what the code means instead.",
    "ARABIC_EVIDENCE": "All eight `vehicles.vin.*` keys in ar.json.",
    "ENGLISH_EVIDENCE": "Same in en.json.",
    "RTL_LTR_EVIDENCE": "Parity gate.",
    "ACCESSIBILITY_EVIDENCE": "`aria-describedby`/`aria-invalid`/`aria-errormessage` `:134-136`; `role=\"alert\"` on danger, `role=\"status\"` otherwise `:224`.",
    "PRODUCTION_REACHABILITY": "Both mount points real.",
    "PRODUCTION_TEST_EVIDENCE": "`vin-create-validation.dom.test.tsx:275-330` renders the real field; `vehicle-profile.test.ts:165-247` drives the real adapter (format bounds, unavailable-not-available, self-exclusion).",
    "NEGATIVE_OR_MUTATION_PROOF": "The search/unique-index divergence is one-directional and documented `VinField.tsx:28-40`: `available` can never be wrong in the dangerous direction.",
    "ROUND5_FINDING_IDS": "`H-09` FIXED at `fd51140` — the 409 said \"Someone else changed this\" for four distinct server refusals; `client.ts:726-727` now chooses by catalog code and the choice is proved negatively over ten codes.",
    "DOCUMENTATION_EVIDENCE": "canonical-plan §5.2.",
    "FINAL_VERDICT": "PASS",
    "VERDICT_RATIONALE": "The format and uniqueness preview is sound and fails safe — a transport failure resolves to `unavailable`, never `available`, so the dangerous direction is unreachable. The single reason this was PARTIAL was that the `veh.vehicle-update` conflict path surfaced no VIN-attributed error. Re-assessed at `26eab7e`; the previous cell was written at `9eff8bd` and the tree moved. That had two halves and both are answered. The 422 half was a wiring gap and is closed: `VinField`'s `error` prop is passed on the update panel at `VehicleProfileScreen.tsx:624`. The 409 half is NOT a gap — it is the correct behaviour, because two unique indexes can raise the same 409 and the response carries no field detail, so attributing it to the VIN would tell an operator something the server did not say. Demanding it would be asking for an invention, which is why this row no longer does.",
    "PROTECTED_REPROOF": "NOT REQUIRED. Every proof this row cites is executed by the `web` tier or by `tests/ci`, both of which run on every pull request (`web-quality` and `static-quality` in `_reusable-node-quality.yml`). Nothing here waits on a job that only a protected push starts."
  },
  "FE-021": {
    "IMPLEMENTATION_SURFACES": "`VehicleHistorySections.tsx:102-253`; `history-api.ts:80-86,160-215`; `history-contract.ts:97-105,135-187`.",
    "NORMAL_NAVIGATION_ENTRY": "Profile `ownership` tab (`VehicleProfileScreen.tsx:88-96,173-185`).",
    "BACKEND_READ_CONTRACTS": "`veh.vehicle-ownership-history` GET /vehicles/{id}/ownerships.",
    "BACKEND_WRITE_CONTRACTS": "`veh.vehicle-ownership-transfer` POST same path (`:188-215`) — BOTH halves of the pair are wired.",
    "REQUIRED_PERMISSIONS": "`veh.vehicle.relationship.manage` AND `!terminal`; the form renders only when `table.response && canWrite`.",
    "TENANT_ISOLATION_REQUIREMENT": "Path plus `cursor`/`limit` only.",
    "LOADING_STATE": "`DataTable.tsx:267`.",
    "EMPTY_STATE": "`DataTable.tsx:302`.",
    "ZERO_RESULT_STATE": "N/A with rationale: the section has no filters.",
    "ERROR_STATE": "`DataTable.tsx:165` with retry.",
    "DENIAL_STATE": "`DataTable.tsx:131`; write withheld.",
    "CONFLICT_STATE": "Via `writeVehicle` -> `fromFailure` rendered `RecordForm.tsx:339-348`; the guard blocks an unchosen customer before a slot is spent.",
    "ARABIC_EVIDENCE": "`vehicles.ownership.*`, `ownershipKind.*`, `interval.*` complete in ar.json.",
    "ENGLISH_EVIDENCE": "Same in en.json.",
    "RTL_LTR_EVIDENCE": "Arabic/RTL case `vehicle-relationship-writes.dom.test.tsx:526-571`.",
    "ACCESSIBILITY_EVIDENCE": "`aria-labelledby` plus sr-only h2 `:552-555`; per-field label/`aria-invalid`/`role=\"alert\"`. No axe on the profile route — `H-01`.",
    "PRODUCTION_REACHABILITY": "Reachable and gated.",
    "PRODUCTION_TEST_EVIDENCE": "`vehicle-relationship-writes.dom.test.tsx:198-291` renders the real section: reachability, gating, exact-day send with no timezone round-trip, no free-text id field, denial.",
    "NEGATIVE_OR_MUTATION_PROOF": "Form survival proved `:273-291`; the four-state interval badge means a future-dated open row reads `scheduled` rather than `active`.",
    "ROUND5_FINDING_IDS": "`F-03` FIXED; `A42-06`, `A42-11` OPEN (record defects).",
    "DOCUMENTATION_EVIDENCE": "canonical-plan §5.2.",
    "FINAL_VERDICT": "PASS",
    "VERDICT_RATIONALE": "Both contract halves wired, correctly gated on permission AND lifecycle, with the date sent as the exact chosen day and the customer preserved across a failed write.",
    "PROTECTED_REPROOF": "NOT REQUIRED. Every proof this row cites is executed by the `web` tier or by `tests/ci`, both of which run on every pull request (`web-quality` and `static-quality` in `_reusable-node-quality.yml`). Nothing here waits on a job that only a protected push starts."
  },
  "FE-022": {
    "IMPLEMENTATION_SURFACES": "`VehicleHistorySections.tsx:256-354`; `history-api.ts:88-94,118-158`.",
    "NORMAL_NAVIGATION_ENTRY": "Profile `plates` tab (`VehicleProfileScreen.tsx:186-197`).",
    "BACKEND_READ_CONTRACTS": "`veh.vehicle-plate-history` GET /vehicles/{id}/plates.",
    "BACKEND_WRITE_CONTRACTS": "`veh.vehicle-plate-assign` POST same path — both halves wired; fields match `plates/route.ts:31-33`.",
    "REQUIRED_PERMISSIONS": "`veh.vehicle.manage` AND `!terminal`; `canWrite` is a required prop.",
    "TENANT_ISOLATION_REQUIREMENT": "Clean.",
    "LOADING_STATE": "`DataTable.tsx:267`.",
    "EMPTY_STATE": "`DataTable.tsx:302`.",
    "ZERO_RESULT_STATE": "N/A with rationale: no filters.",
    "ERROR_STATE": "`DataTable.tsx:165`.",
    "DENIAL_STATE": "`DataTable.tsx:131`; write withheld, proved `write-permission-gating.dom.test.tsx:265-296`.",
    "CONFLICT_STATE": "Via `fromFailure`. ",
    "ARABIC_EVIDENCE": "All twelve `vehicles.plate.*` keys in ar.json.",
    "ENGLISH_EVIDENCE": "Same in en.json.",
    "RTL_LTR_EVIDENCE": "Parity gate; no Arabic render case for this section.",
    "ACCESSIBILITY_EVIDENCE": "Shared section semantics; no axe — `H-01`.",
    "PRODUCTION_REACHABILITY": "Reachable, proved indirectly through the profile (`vehicle-profile-lifecycle.dom.test.tsx:209-223`).",
    "PRODUCTION_TEST_EVIDENCE": "Only `write-permission-gating.dom.test.tsx:265-296` (visible/hidden/default-closed). No test renders plate rows or the interval badge.",
    "NEGATIVE_OR_MUTATION_PROOF": "Gating proved both directions; the write schema has NO test — the 2-3 character country rule, the `toUpperCase()` transform and the date length are exercised nowhere. Finding `H-10`.",
    "ROUND5_FINDING_IDS": "`H-10` FIXED at `c283354` — the plate-assignment write schema was exercised nowhere; `write-adapters-driven.test.ts:598-679` now drives the two-to-three character country rule, the `toUpperCase()` transform and the ten-character effective-date rule, and `vehicle-plate-section.dom.test.tsx` (14 cases, measured) renders the real section. `A42-06` and `A42-11` remain OPEN and are DOCUMENTATION findings owned by `DOC-001`: the first is a Summary/section contradiction about roots and commits, the second is a traceability row listing only the GET of a canonical operation pair.",
    "DOCUMENTATION_EVIDENCE": "canonical-plan §5.2.",
    "FINAL_VERDICT": "PASS",
    "VERDICT_RATIONALE": "Both contract halves are wired and correctly gated. The single reason this was PARTIAL was that the plate write validation and every rendered state of the section were exercised by no test. Re-assessed at `26eab7e`; the previous cell was written at `9eff8bd` and the tree moved. `write-adapters-driven.test.ts:598-679` executes the write adapter against its real schema — one and four characters refused, three accepted, a date that is not exactly ten characters refused — and `vehicle-plate-section.dom.test.tsx` renders the section itself. `A42-06` and `A42-11` are still open and are recorded on `DOC-001`, which owns document synchronization; neither is a claim about this screen's behaviour.",
    "PROTECTED_REPROOF": "NOT REQUIRED. Every proof this row cites is executed by the `web` tier or by `tests/ci`, both of which run on every pull request (`web-quality` and `static-quality` in `_reusable-node-quality.yml`). Nothing here waits on a job that only a protected push starts."
  },
  "FE-023": {
    "IMPLEMENTATION_SURFACES": "`VehicleHistorySections.tsx:376-517`; `history-api.ts:96-102,217-306`.",
    "NORMAL_NAVIGATION_ENTRY": "Profile `odometer` tab (`VehicleProfileScreen.tsx:198-219`).",
    "BACKEND_READ_CONTRACTS": "`veh.vehicle-odometer-history` GET /vehicles/{id}/odometer-readings.",
    "BACKEND_WRITE_CONTRACTS": "`veh.vehicle-odometer-record` POST same path — both halves wired.",
    "REQUIRED_PERMISSIONS": "Its own code `veh.vehicle.odometer.record` (`permissions.ts:45`) AND `!frozen` — deliberately not `!terminal`, stated as Owner policy `:199-218`.",
    "TENANT_ISOLATION_REQUIREMENT": "Clean.",
    "LOADING_STATE": "`DataTable.tsx:267`.",
    "EMPTY_STATE": "`DataTable.tsx:302`.",
    "ZERO_RESULT_STATE": "N/A with rationale: no filters.",
    "ERROR_STATE": "`DataTable.tsx:165`.",
    "DENIAL_STATE": "`DataTable.tsx:131`; withheld and default-closed.",
    "CONFLICT_STATE": "Via `fromFailure`.",
    "ARABIC_EVIDENCE": "`vehicles.odometer.*`, `odometerUnit.*`, `captureMethod.*`, `anomalyReason.*` all in ar.json.",
    "ENGLISH_EVIDENCE": "Same in en.json.",
    "RTL_LTR_EVIDENCE": "Parity gate.",
    "ACCESSIBILITY_EVIDENCE": "Shared section semantics; no axe — `H-01`.",
    "PRODUCTION_REACHABILITY": "Reachable; present on a scrapped vehicle and withdrawn on a merged one (`vehicle-profile-lifecycle.dom.test.tsx:384-421`).",
    "PRODUCTION_TEST_EVIDENCE": "`vehicle-history.test.ts` (20 cases incl. vocabulary/CHECK match); adapter driven `p1-27-qa.test.ts:576-683`.",
    "NEGATIVE_OR_MUTATION_PROOF": "The client mirrors the domain rule EXACTLY: `history-api.ts:245-270` copies `ISO_DATETIME` and the second half `!Number.isNaN(Date.parse(...))` from `vehicle-odometer.ts:46,93`. Proved three ways — a space-separated datetime refused with ZERO requests sent, a valid ISO sent as control, and `2026-02-30` deliberately NOT refused because `Date.parse` rolls it over.",
    "ROUND5_FINDING_IDS": "`A42-11` OPEN (record defect).",
    "DOCUMENTATION_EVIDENCE": "canonical-plan §5.2.",
    "FINAL_VERDICT": "PASS",
    "VERDICT_RATIONALE": "The named edge case is correct in both halves and proved with a zero-request control; the number input carries `step: 1` against a `numeric(12,1)` column, which is a client stricter than the server and is documented rather than silent.",
    "PROTECTED_REPROOF": "NOT REQUIRED. Every proof this row cites is executed by the `web` tier or by `tests/ci`, both of which run on every pull request (`web-quality` and `static-quality` in `_reusable-node-quality.yml`). Nothing here waits on a job that only a protected push starts."
  },
  "FE-024": {
    "IMPLEMENTATION_SURFACES": "`VehicleRelationsSections.tsx:55-268`; `relations-api.ts:42-64,91-176`; `relations-contract.ts:198-232`.",
    "NORMAL_NAVIGATION_ENTRY": "Profile `ev` tab (`VehicleProfileScreen.tsx:220-239`).",
    "BACKEND_READ_CONTRACTS": "`veh.vehicle-ev-profile-read` GET /vehicles/{id}/ev-profile, read server-side (`page.tsx:95`).",
    "BACKEND_WRITE_CONTRACTS": "`veh.vehicle-ev-profile-set` POST same path.",
    "REQUIRED_PERMISSIONS": "`veh.vehicle.manage` AND `!terminal`, verified against `vehicle-lifecycle-service.ts:68-74` which refuses merged AND scrapped.",
    "TENANT_ISOLATION_REQUIREMENT": "Clean.",
    "LOADING_STATE": "Route `loading.tsx`.",
    "EMPTY_STATE": "`none` splits again on `canHaveEvProfile` — \"not recorded yet\" vs \"not applicable\" (`:140-171`).",
    "ZERO_RESULT_STATE": "N/A with rationale: a single profile, not a list.",
    "ERROR_STATE": "Failed read `role=\"alert\"` plus correlation id `:173-188`.",
    "DENIAL_STATE": "Conflated with a transport error (`relations-api.ts:59-63`); low impact because the page already required `veh.vehicle.read`.",
    "CONFLICT_STATE": "Via `fromFailure`.",
    "ARABIC_EVIDENCE": "`vehicles.ev.*` and `vehicles.evKind.*` in ar.json.",
    "ENGLISH_EVIDENCE": "Same in en.json.",
    "RTL_LTR_EVIDENCE": "Arabic/RTL case in `ev-profile.dom.test.tsx`.",
    "ACCESSIBILITY_EVIDENCE": "`aria-labelledby`, `aria-live=\"polite\"`, `role=\"status\"`/`\"alert\"`. No axe — `H-01`.",
    "PRODUCTION_REACHABILITY": "Reachable and gated.",
    "PRODUCTION_TEST_EVIDENCE": "`ev-profile.dom.test.tsx` (12 cases) on the real section; `vehicle-relations.test.ts:53-150` on the real adapter.",
    "NEGATIVE_OR_MUTATION_PROOF": "REPLACE semantics: `clearOnSuccess={false}` and `initialValues` seeded from the current profile, so saving one field cannot silently clear the rest. The high-voltage checkbox uses key + `defaultChecked` + `onChange` — the one field kind whose default is frozen at mount — proved `record-form-consumers.dom.test.tsx:308-471`.",
    "ROUND5_FINDING_IDS": "None mapped.",
    "DOCUMENTATION_EVIDENCE": "canonical-plan §5.2.",
    "FINAL_VERDICT": "PASS",
    "VERDICT_RATIONALE": "Bounds mirror the route, the three read states are kept apart including a 404 that means \"none\" rather than an error, and the replace-vs-append distinction is proved on the real form.",
    "PROTECTED_REPROOF": "NOT REQUIRED. Every proof this row cites is executed by the `web` tier or by `tests/ci`, both of which run on every pull request (`web-quality` and `static-quality` in `_reusable-node-quality.yml`). Nothing here waits on a job that only a protected push starts."
  },
  "FE-025": {
    "TENANT_ISOLATION_REQUIREMENT": "No scope key anywhere in `features/vehicles`; `query()` throws, asserted `p1-27-security.test.ts:270-276`.",
    "LOADING_STATE": "`DataTable.tsx:267`.",
    "ZERO_RESULT_STATE": "N/A with rationale: the section has no filters of its own.",
    "ERROR_STATE": "`DataTable.tsx:165` with retry.",
    "DENIAL_STATE": "`DataTable.tsx:131`; writes withheld.",
    "RTL_LTR_EVIDENCE": "Catalogue parity (1090 = 1090 keys, zero one-sided); RTL cases in the owning suite.",
    "IMPLEMENTATION_SURFACES": "`VehicleRelationsSections.tsx:283,422,495,557`; `relations-contract.ts:74-190`; `relations-api.ts:66,206,262,301`.",
    "NORMAL_NAVIGATION_ENTRY": "Profile `relationships` tab `VehicleProfileScreen.tsx:240-262`.",
    "BACKEND_READ_CONTRACTS": "`veh.vehicle-relationship-list` GET /vehicles/{id}/relationships.",
    "BACKEND_WRITE_CONTRACTS": "`crm.vehicle-link` POST /customers/{partnerId}/vehicles; `veh.vehicle-authorized-party-add` POST /vehicles/{id}/authorized-parties; `…-retire` POST …/{relId}/retirement.",
    "REQUIRED_PERMISSIONS": "`veh.vehicle.relationship.manage` and `crm.customer.vehicle.manage`, resolved server-side (`page.tsx:115-122`). `canRetire` deliberately not `!terminal` — an authorised driver can be removed from a written-off car.",
    "EMPTY_STATE": "`DataTable.tsx:302`.",
    "CONFLICT_STATE": "Per write via `fromFailure` rendered `:336,387`.",
    "ARABIC_EVIDENCE": "All 20 `vehicles.relationships.*`, 7 `role.*`, 6 `action.*`, 4 `interval.*` resolve in ar.json.",
    "ENGLISH_EVIDENCE": "Same in en.json.",
    "ACCESSIBILITY_EVIDENCE": "`fieldset`/`legend` for the scope `:343-349`, `role=\"alert\"` field errors. No axe on the profile route — `H-01`.",
    "PRODUCTION_REACHABILITY": "Reachable and gated.",
    "PRODUCTION_TEST_EVIDENCE": "`vehicle-relationship-writes.dom.test.tsx:140` — 18 cases on the real section incl. the FormData payload, six-actions-no-seventh, retire-only-on-an-open-scoped-row.",
    "NEGATIVE_OR_MUTATION_PROOF": "`F-02` FIXED: `defaultChecked` + `key={action-attempt}` `:373-379`, and `RecordForm` increments `attempt` even on a client guard rejection so the remount fires. `F-03` FIXED: all three `CustomerSelector` call sites pass `attempt`, enforced by `form-reset-class.test.ts:414-444`.",
    "ROUND5_FINDING_IDS": "`F-02` FIXED, `F-03` FIXED; raises `H-13`.",
    "DOCUMENTATION_EVIDENCE": "canonical-plan §5.2; round-five register.",
    "FINAL_VERDICT": "PASS",
    "VERDICT_RATIONALE": "Every surface, gate, state and locale is real and both round-five defect classes are fixed and gate-enforced; the residual is that the three write ADAPTERS are mocked everywhere, so request paths and `.strict()` bodies lack execution evidence (`H-13`) — evidence depth, not behaviour.",
    "PROTECTED_REPROOF": "NOT REQUIRED. Every proof this row cites is executed by the `web` tier or by `tests/ci`, both of which run on every pull request (`web-quality` and `static-quality` in `_reusable-node-quality.yml`). Nothing here waits on a job that only a protected push starts."
  },
  "FE-026": {
    "TENANT_ISOLATION_REQUIREMENT": "No scope key anywhere in `features/vehicles`; `query()` throws, asserted `p1-27-security.test.ts:270-276`.",
    "LOADING_STATE": "`DataTable.tsx:267`.",
    "ZERO_RESULT_STATE": "N/A with rationale: the section has no filters of its own.",
    "ERROR_STATE": "`DataTable.tsx:165` with retry.",
    "DENIAL_STATE": "`DataTable.tsx:131`; writes withheld.",
    "RTL_LTR_EVIDENCE": "Catalogue parity (1090 = 1090 keys, zero one-sided); RTL cases in the owning suite.",
    "IMPLEMENTATION_SURFACES": "`VehicleDocumentsSection.tsx:16-100`; `documents-contract.ts:60-67`; `documents-api.ts:27-46`.",
    "NORMAL_NAVIGATION_ENTRY": "Profile `documents` tab `VehicleProfileScreen.tsx:263-270`.",
    "BACKEND_READ_CONTRACTS": "`veh.vehicle-document-list` GET /vehicles/{id}/documents, no cursor/limit.",
    "BACKEND_WRITE_CONTRACTS": "N/A with rationale: `veh` publishes no document write; inventing one would be a Backend change inside a Frontend phase.",
    "REQUIRED_PERMISSIONS": "`shared.document.manage`, held INDEPENDENTLY of `veh.vehicle.read`; the read is SKIPPED when not held (`page.tsx:101-104`) so a denied caller spends no rate-limit slot.",
    "EMPTY_STATE": "Zero-result `:50-53`, kept distinct from not-found `:80-83`.",
    "CONFLICT_STATE": "N/A with rationale: read only.",
    "ARABIC_EVIDENCE": "Eight `vehicles.documents.*` keys in ar.json; tests additionally assert en != ar, so a copied catalogue would fail.",
    "ENGLISH_EVIDENCE": "Same in en.json.",
    "ACCESSIBILITY_EVIDENCE": "`aria-labelledby` per section, `role=\"alert\"` on failure; ids rendered as inert `<code dir=\"ltr\">`.",
    "PRODUCTION_REACHABILITY": "Reachable; denial names the missing capability rather than showing an empty panel.",
    "PRODUCTION_TEST_EVIDENCE": "`vehicle-documents.dom.test.tsx` (18 cases, all five states); `vehicle-documents.test.ts:42` drives the real adapter (path encoding, no cursor/limit, no retry, expired-before-network).",
    "NEGATIVE_OR_MUTATION_PROOF": "Asserts no link, no storage host, no download authorization in the markup.",
    "ROUND5_FINDING_IDS": "None mapped.",
    "DOCUMENTATION_EVIDENCE": "canonical-plan §5.2.",
    "FINAL_VERDICT": "PASS",
    "VERDICT_RATIONALE": "Ships exactly the read the contract publishes, with the inverted permission named on screen instead of shown as an empty panel.",
    "PROTECTED_REPROOF": "NOT REQUIRED. Every proof this row cites is executed by the `web` tier or by `tests/ci`, both of which run on every pull request (`web-quality` and `static-quality` in `_reusable-node-quality.yml`). Nothing here waits on a job that only a protected push starts."
  },
  "FE-027": {
    "TENANT_ISOLATION_REQUIREMENT": "No scope key anywhere in `features/vehicles`; `query()` throws, asserted `p1-27-security.test.ts:270-276`.",
    "LOADING_STATE": "N/A with rationale: a stated status has no load.",
    "ZERO_RESULT_STATE": "N/A with rationale: the section has no filters of its own.",
    "ERROR_STATE": "N/A with rationale: as above.",
    "DENIAL_STATE": "N/A with rationale: as above.",
    "RTL_LTR_EVIDENCE": "Catalogue parity (1090 = 1090 keys, zero one-sided); RTL cases in the owning suite.",
    "IMPLEMENTATION_SURFACES": "`VehicleDocumentsSection.tsx:102-119` — a stated-status section, no controls.",
    "NORMAL_NAVIGATION_ENTRY": "Rendered inside the `documents` tab; mounted.",
    "BACKEND_READ_CONTRACTS": "N/A with rationale: no vehicle media operation exists in the registry.",
    "BACKEND_WRITE_CONTRACTS": "N/A with rationale: deliberately absent under `P1-OD-025`, which is OPEN.",
    "REQUIRED_PERMISSIONS": "N/A with rationale: there is no control to gate.",
    "EMPTY_STATE": "N/A with rationale: as above; no fabricated empty state.",
    "CONFLICT_STATE": "N/A with rationale: as above.",
    "ARABIC_EVIDENCE": "`vehicles.media.heading` and `vehicles.media.blocked` in ar.json:940-941.",
    "ENGLISH_EVIDENCE": "en.json:940-941.",
    "ACCESSIBILITY_EVIDENCE": "Section semantics only; nothing interactive to scan.",
    "PRODUCTION_REACHABILITY": "The pending policy is stated and the decision id printed on screen `:114-118`.",
    "PRODUCTION_TEST_EVIDENCE": "`vehicle-documents.dom.test.tsx:53-78` asserts no file input, no form, no upload button, no URL and no storage host, and that the decision id is visible.",
    "NEGATIVE_OR_MUTATION_PROOF": "TWO independent mechanisms: CI rule `no-upload-path` (`check-p1-27-frontend.mjs:111-113`) and `vehicle-duplicates.test.ts:210` asserting the adapter exports nothing matching upload/media/attach.",
    "ROUND5_FINDING_IDS": "None mapped.",
    "DOCUMENTATION_EVIDENCE": "`P1-OD-025` OPEN (`open-decisions.md:69,243-245`); disposition `canonical-plan.md:245-257` — \"keep upload acceptance blocked, do not invent limits\".",
    "FINAL_VERDICT": "PASS",
    "VERDICT_RATIONALE": "Deliberately absent under an approved disposition of an open Owner decision, with the affordance absent rather than disabled, enforced by a gate AND a test rather than merely asserted.",
    "PROTECTED_REPROOF": "NOT REQUIRED. Every proof this row cites is executed by the `web` tier or by `tests/ci`, both of which run on every pull request (`web-quality` and `static-quality` in `_reusable-node-quality.yml`). Nothing here waits on a job that only a protected push starts."
  },
  "FE-028": {
    "TENANT_ISOLATION_REQUIREMENT": "No scope key anywhere in `features/vehicles`; `query()` throws, asserted `p1-27-security.test.ts:270-276`.",
    "LOADING_STATE": "`DataTable.tsx:267`.",
    "ZERO_RESULT_STATE": "Per status filter, re-reading with `loadKey: status` (`:102-105`).",
    "ERROR_STATE": "`DataTable.tsx:165` with retry.",
    "DENIAL_STATE": "`DataTable.tsx:131`; writes withheld.",
    "RTL_LTR_EVIDENCE": "Catalogue parity (1090 = 1090 keys, zero one-sided); RTL cases in the owning suite.",
    "IMPLEMENTATION_SURFACES": "`VehicleDuplicateReviewScreen.tsx:70,293`; `duplicates-contract.ts:47-212`; `duplicates-api.ts:34,105`.",
    "NORMAL_NAVIGATION_ENTRY": "`/vehicles/duplicates`, own sidebar entry `navigation.ts:212-219`.",
    "BACKEND_READ_CONTRACTS": "`veh.vehicle-duplicate-list` GET /api/v1/vehicle-duplicates.",
    "BACKEND_WRITE_CONTRACTS": "`veh.vehicle-duplicate-review` POST /vehicle-duplicates/{id}/review; the decision vocabulary is `[dismissed]` only.",
    "REQUIRED_PERMISSIONS": "`veh.vehicle.duplicate.review` gates the page BEFORE the first read (`page.tsx:42-51`). `veh.vehicle.merge` deliberately unchecked and stated so.",
    "EMPTY_STATE": "`DataTable.tsx:302`.",
    "CONFLICT_STATE": "Invalid reason `role=\"alert\"` `:430-438`; conflict/denied/success `:440-452`.",
    "ARABIC_EVIDENCE": "12 `vehicles.duplicates.*` and 3 `duplicateStatus.*` resolve in ar.json.",
    "ENGLISH_EVIDENCE": "Same in en.json.",
    "ACCESSIBILITY_EVIDENCE": "Route IS axe-scanned (`accessibility.spec.ts:112`) and `label-content-name-mismatch` is force-enabled AND proved to fire on a planted violation (`:64,165-183,221-230`) — the exact hole that let the old WCAG 2.5.3 defect through. Finding `H-12`: two identical `h1`s on this route.",
    "PRODUCTION_REACHABILITY": "Reachable; merge and rescan absent, not disabled.",
    "PRODUCTION_TEST_EVIDENCE": "`vehicle-duplicate-identity.dom.test.tsx` (13 cases); `vehicle-screens.dom.test.tsx:335-440`; `duplicate-review-writes.test.ts` on the real write adapter.",
    "NEGATIVE_OR_MUTATION_PROOF": "Repository-wide guard `p1-27-security.test.ts:372-387` forbids merge and duplicate-scan in every phase file. The ordinal is an `sr-only` SUFFIX inside the link with the reference first, so the visible text is contained in the accessible name — WCAG 2.5.3 satisfied and now provably scanned.",
    "ROUND5_FINDING_IDS": "Raises `H-12`.",
    "DOCUMENTATION_EVIDENCE": "canonical-plan §7; `P1-OD-017`.",
    "FINAL_VERDICT": "PASS",
    "VERDICT_RATIONALE": "Merge and scan are genuinely uncalled, and the 2.5.3 fix is correct and now covered by a rule proved to fire rather than one silently dropped from a tag-scoped run.",
    "PROTECTED_REPROOF": "OUTSTANDING — the AUTHENTICATED browser tier. It is now wired and run: the `authenticated-browser` job (`protected-develop-verification.yml:176-419`) sets `ROOTLCO_E2E_AUTH: '1'` at `:319`, bootstraps a real operator and a second tenant, and fails unless every authenticated spec contributed an executed test. It runs on pushes to `develop` and `main` ONLY, and `.github/ci-baselines/unrun-test-tiers.json` (`notYetObservedOnARunner`) records that it has never executed on a hosted runner even once. Collection is proved locally at 229 tests across 7 spec files. The FEATURE is complete at this head; the REPROOF is unpaid."
  },
  "FE-029": {
    "TENANT_ISOLATION_REQUIREMENT": "No scope key anywhere in `features/vehicles`; `query()` throws, asserted `p1-27-security.test.ts:270-276`.",
    "LOADING_STATE": "`DataTable.tsx:267`.",
    "ZERO_RESULT_STATE": "N/A with rationale: the section has no filters of its own.",
    "ERROR_STATE": "`DataTable.tsx:165` with retry.",
    "DENIAL_STATE": "`DataTable.tsx:131`; writes withheld.",
    "RTL_LTR_EVIDENCE": "Catalogue parity (1090 = 1090 keys, zero one-sided); RTL cases in the owning suite.",
    "IMPLEMENTATION_SURFACES": "`VehicleAttributeHistorySection.tsx:48-186`; `duplicates-contract.ts:87-142`; `duplicates-api.ts:69-92`.",
    "NORMAL_NAVIGATION_ENTRY": "Profile `history` tab `VehicleProfileScreen.tsx:271-277`; the naming difference from \"timeline\" is justified at `:78-97`.",
    "BACKEND_READ_CONTRACTS": "`veh.vehicle-history` GET /api/v1/vehicles/{id}/history.",
    "BACKEND_WRITE_CONTRACTS": "N/A with rationale: an append-only ledger; the phase registers no write.",
    "REQUIRED_PERMISSIONS": "`veh.vehicle.read`, the same code that gates the page; API declares it at `history/route.ts:30`.",
    "EMPTY_STATE": "`DataTable.tsx:302`.",
    "CONFLICT_STATE": "N/A with rationale: read only.",
    "ARABIC_EVIDENCE": "11 `vehicles.history.*` keys in ar.json; unknown field codes fall back to the key rather than to an empty cell.",
    "ENGLISH_EVIDENCE": "Same in en.json.",
    "ACCESSIBILITY_EVIDENCE": "`aria-labelledby`, `<time dateTime>`, the arrow is `aria-hidden` with an sr-only \"becomes\". No axe (needs a real vehicle id) — `H-01`.",
    "PRODUCTION_REACHABILITY": "Reachable and mounted.",
    "PRODUCTION_TEST_EVIDENCE": "`vehicle-screens.dom.test.tsx:587-678` — never prints \"null\", creation reads as \"set to\", and three actor cases each assert the uuid is absent from `textContent`.",
    "NEGATIVE_OR_MUTATION_PROOF": "`actorName` holds on BOTH halves: the API resolves it through the IAM directory (`actor-identity.ts:71-90`, wired `vehicle-history-service.ts:48-57`), and the UI renders `undefined` and `null` identically while `actorId` is carried in the type but read by NO cell — so no merge order can put a uuid back on screen.",
    "ROUND5_FINDING_IDS": "None mapped.",
    "DOCUMENTATION_EVIDENCE": "canonical-plan §5.2 and the capability-gap note at `:267`.",
    "FINAL_VERDICT": "PASS",
    "VERDICT_RATIONALE": "PR #212 landed the Backend half and the UI renders a human identity with no uuid path back, rather than a fabricated unified stream.",
    "PROTECTED_REPROOF": "NOT REQUIRED. Every proof this row cites is executed by the `web` tier or by `tests/ci`, both of which run on every pull request (`web-quality` and `static-quality` in `_reusable-node-quality.yml`). Nothing here waits on a job that only a protected push starts."
  },
  "SEC-001": {
    "IMPLEMENTATION_SURFACES": "`features/crm/permissions.ts` (`CRM_PERMISSIONS`, `VEHICLE_PERMISSIONS`, `holds`); `features/crm/customers/governance-contract.ts` (`permittedWrites`, `WRITE_PERMISSIONS`, `NO_WRITES`); the eight dashboard routes that call `requireSession()` and gate before they read.",
    "NORMAL_NAVIGATION_ENTRY": "N/A with rationale: a permission rule has no navigation entry of its own. It is reached on every route entry — `crm/customers/[customerId]/page.tsx:62` and `vehicles/[vehicleId]/page.tsx:53` are the two the route-binding suite drives.",
    "BACKEND_READ_CONTRACTS": "Every P1-27 read. The obligation here is that the route evaluates the permission BEFORE issuing the read, proved for the vehicle documents list at `p1-27-security.test.ts:531-540` (`canListDocuments ? await listVehicleDocuments`) so a denied operator never spends an `expensive-read` slot.",
    "BACKEND_WRITE_CONTRACTS": "All 23 write adapters. `permittedWrites(session.permissions)` is the single expression the nine customer write surfaces hang off (`crm/customers/[customerId]/page.tsx:130`).",
    "REQUIRED_PERMISSIONS": "Nine customer surfaces behind five distinct codes and eight vehicle surfaces behind six, enumerated as data and asserted to be distinct at `p1-27-permission-route-binding.dom.test.tsx:381-394,539-544` — if the codes ever collapsed to one string every denial case below would be testing an empty set.",
    "TENANT_ISOLATION_REQUIREMENT": "Client sends no tenant, company or branch key on any request: `query()` throws on all six scope names (`lib/api/read-operation.ts:92-100,126-133`), the gate rule `no-client-asserted-scope` scans all three canonical trees with `allow: []`, and `p1-27-qa.test.ts:503-556` sweeps every list adapter and all 23 write BODIES for the three words after asserting the request was actually issued.",
    "LOADING_STATE": "N/A with rationale: permission and resolved-scope enforcement is a cross-cutting obligation with no screen of its own; the states belong to the Frontend rows this task governs.",
    "EMPTY_STATE": "N/A with rationale: permission and resolved-scope enforcement is a cross-cutting obligation with no screen of its own; the states belong to the Frontend rows this task governs.",
    "ZERO_RESULT_STATE": "N/A with rationale: permission and resolved-scope enforcement is a cross-cutting obligation with no screen of its own; the states belong to the Frontend rows this task governs.",
    "ERROR_STATE": "A backend refusal renders the failure state INSTEAD of the screen and carries the correlation reference out of it, driven through the real route at `p1-27-permission-route-binding.dom.test.tsx:634-651`.",
    "DENIAL_STATE": "Proved on the real route in both directions for all seventeen surfaces: granted (`:397-407`, `:548-559`) and denied while holding EVERY OTHER code in the domain (`:409-421`, `:561-570`). The denied direction is the one that fails against a hard-coded `true`, a map of all-`true` and a copy-pasted sibling constant — the three shapes this phase has shipped.",
    "CONFLICT_STATE": "N/A with rationale: an authorization decision publishes no conflict; a 409 is a resource outcome and belongs to `QA-004`.",
    "ARABIC_EVIDENCE": "`state.denied.title` and `state.notFound.title` resolve in ar.json; catalogue parity is enforced by `tests/i18n.test.ts:72`.",
    "ENGLISH_EVIDENCE": "Same keys in en.json; the route-binding suite asserts against `en.json` directly.",
    "RTL_LTR_EVIDENCE": "N/A with rationale: a permission decision has no direction. The screens it gates carry the RTL evidence on their own rows.",
    "ACCESSIBILITY_EVIDENCE": "N/A with rationale: enforcement is the absence of a control, and an absent control has no accessible name. `profile-accessibility.dom.test.tsx` scans the profile sections WITH every write offered (26 cases), which is the harder direction.",
    "PRODUCTION_REACHABILITY": "The real route modules are invoked — `CustomerProfilePage` and `VehicleProfilePage`, not a re-implementation — and the element tree they return is RENDERED, so every assertion is about a control an operator can or cannot see (`p1-27-permission-route-binding.dom.test.tsx:8-58`).",
    "PRODUCTION_TEST_EVIDENCE": "`p1-27-permission-route-binding.dom.test.tsx` — 43 cases, measured, all passing at this head. Supported by `p1-27-security.test.ts` (41) and `route-permission-binding.test.ts` (the vehicle route at the prop level).",
    "NEGATIVE_OR_MUTATION_PROOF": "The negative session is the proof: a session holding the read code and every write code EXCEPT one, asserted non-trivial at `:414` (`toBe(4)`) and `:561` (`toBe(5)`) so it can never degrade to an empty set. Before this file existed, replacing `permittedWrites(session.permissions)` with a map of nine `true`s broke no test in the suite.",
    "ROUND5_FINDING_IDS": "None mapped. The wire this task is about is what `H-01`…`H-11` were raised alongside; none of them names `SEC-001`.",
    "DOCUMENTATION_EVIDENCE": "canonical-plan §5.3; `evidence/task-traceability.md` §4, whose case count for `p1-27-security.test.ts` is now carried by a `<!-- derived: cases … -->` marker that `validate:p1-27-doc-counts` holds against the tree (measured 49 at this head), rather than by the prose figure of 20 that had aged there.",
    "FINAL_VERDICT": "PASS",
    "VERDICT_RATIONALE": "The PERMISSION half was already proved end to end and in both directions on the real route modules. The single reason this was PARTIAL was the RESOLVED-SCOPE half of the conjunction: no artefact in P1-27 established that a session resolving to one branch is refused a record in another, and the one that would was executed by nothing. Re-assessed at `26eab7e`; the previous cell was written at `9eff8bd` and the tree moved. It is now wired and run: `apps/web/tests/e2e/authenticated/isolation.spec.ts` asks for Tenant B things with a Tenant A bearer token across six cases, INCLUDING two positive controls — \"the same token reads Tenant A, so the denials above mean something\" and \"the control itself can fail\" — and the `authenticated-browser` job runs it against a live Supabase, a production API build logged in as a role holding neither SUPERUSER nor BYPASSRLS, and a real second tenant. The web tier is also honest about its own limit: `p1-27-security.test.ts:1255-1298` pins that `CustomerSearchHit` carries no scope field, so a client-side comparison is structurally impossible, and fails the day one appears. What is left is not a feature gap but the reproof — see `PROTECTED_REPROOF`.",
    "PROTECTED_REPROOF": "OUTSTANDING — the AUTHENTICATED browser tier. It is now wired and run: the `authenticated-browser` job (`protected-develop-verification.yml:176-419`) sets `ROOTLCO_E2E_AUTH: '1'` at `:319`, bootstraps a real operator and a second tenant, and fails unless every authenticated spec contributed an executed test. It runs on pushes to `develop` and `main` ONLY, and `.github/ci-baselines/unrun-test-tiers.json` (`notYetObservedOnARunner`) records that it has never executed on a hosted runner even once. Collection is proved locally at 229 tests across 7 spec files. The FEATURE is complete at this head; the REPROOF is unpaid."
  },
  "SEC-002": {
    "IMPLEMENTATION_SURFACES": "Five conjuncts named as data at `p1-27-security.test.ts:433-443`, with the disposition of each — `sensitive-data` surface, `export` absence, `document` surface, `media` absence, `file-access` absence — so a conjunct cannot be dropped by deleting a `describe`.",
    "NORMAL_NAVIGATION_ENTRY": "The two conjuncts with a surface are reached from the customer profile notes section and the vehicle profile documents tab; the media statement renders in `VehicleDocumentsSection.tsx` beside it.",
    "BACKEND_READ_CONTRACTS": "`crm.note-list` (which `sel_notes_tenant` silently shortens for a caller without `iam.sensitive.view`) and `veh.vehicle-document-list` (ids only). Both anti-vacuity controls read the published contract rather than a regex: `p1-27-security.test.ts:505-512,536-545` assert the export and download operations EXIST, so their absence here is a decision and not a sweep for nothing.",
    "BACKEND_WRITE_CONTRACTS": "None, deliberately. No export, no upload, no download authorization is called anywhere on the phase surface (`:511-518`, `:546-551`).",
    "REQUIRED_PERMISSIONS": "`shared.document.manage` gates the vehicle documents list — a MANAGE code from a different module, asserted not to start with `veh.` at `:524-527` because a copy-paste to `veh.vehicle.read` would open the tab to every reader.",
    "TENANT_ISOLATION_REQUIREMENT": "Client sends no tenant, company or branch key on any request: `query()` throws on all six scope names (`lib/api/read-operation.ts:92-100,126-133`), the gate rule `no-client-asserted-scope` scans all three canonical trees with `allow: []`, and `p1-27-qa.test.ts:503-556` sweeps every list adapter and all 23 write BODIES for the three words after asserting the request was actually issued.",
    "LOADING_STATE": "N/A with rationale: sensitive-data and file-access control is a cross-cutting obligation with no screen of its own; the states belong to the Frontend rows this task governs.",
    "EMPTY_STATE": "`vehicles.documents.none` for an empty document list, distinguished from `vehicles.documents.needsOtherPermission` in both directions at `p1-27-permission-route-binding.dom.test.tsx:572-596`.",
    "ZERO_RESULT_STATE": "N/A with rationale: sensitive-data and file-access control is a cross-cutting obligation with no screen of its own; the states belong to the Frontend rows this task governs.",
    "ERROR_STATE": "N/A with rationale: sensitive-data and file-access control is a cross-cutting obligation with no screen of its own; the states belong to the Frontend rows this task governs.",
    "DENIAL_STATE": "The documents tab renders `needsOtherPermission` rather than an empty list, so a denied reader is never told the vehicle has no documents.",
    "CONFLICT_STATE": "N/A with rationale: none of the five conjuncts performs a write that can collide.",
    "ARABIC_EVIDENCE": "Both note-completeness sentences and both media sentences resolve in ar.json AND are asserted to differ from the English (`:484-497`, `:576-585`) — a caveat present only in English is a caveat an Arabic operator does not get, on the one screen where not getting it means believing a short list.",
    "ENGLISH_EVIDENCE": "Same four keys in en.json; `vehicles.media.blocked` additionally asserted to contain \"pending\" and \"decision\".",
    "RTL_LTR_EVIDENCE": "Catalogue parity `tests/i18n.test.ts:72`; the profile RTL cases carry the render.",
    "ACCESSIBILITY_EVIDENCE": "The media section is asserted to contain NO control at all — not `<button`, `<input`, `<form`, `<a `, `onClick` or `disabled` (`:606-624`), read out of the `<section aria-labelledby=\"vehicle-media-heading\">` itself rather than the whole file so the documents section above it cannot satisfy it by being control-free.",
    "PRODUCTION_REACHABILITY": "Both surfaces reachable from the vehicle and customer profiles; the three absences are enforced over `PHASE_SURFACE`, which is wider than the gate tree and asserted so at `:967`.",
    "PRODUCTION_TEST_EVIDENCE": "`p1-27-security.test.ts:445-667` — one `describe` per conjunct, 15 cases, measured and passing. `no-upload-path` is additionally exercised through the gate's own `evaluate()` at `:651-666`.",
    "NEGATIVE_OR_MUTATION_PROOF": "The file-access conjunct plants `new FormData()` into a synthetic file and requires exactly one `no-upload-path` failure (`:658-668`), so the rule is shown to fire rather than assumed to. The other four conjuncts have no planted control.",
    "ROUND5_FINDING_IDS": "`A42-14` FIXED — the five-part conjunction was adjudicated on one part, on a commit-less row; it is now proved one conjunct at a time.",
    "DOCUMENTATION_EVIDENCE": "canonical-plan §5.3 and §7 (`P1-OD-025`); `evidence/task-traceability.md` §4.",
    "FINAL_VERDICT": "PARTIAL",
    "VERDICT_RATIONALE": "All five conjuncts have a case and the three absences are proved non-vacuously — each forbidden thing is shown to EXIST in the published contract before its absence is asserted. Re-assessed at `26eab7e`; the previous cell was written at `9eff8bd` and the tree moved. The second weakness recorded here has been re-examined and is downgraded: `PHASE_SURFACE` is built by walking the tree, and the gate's own suite holds the root set against `canonical-plan.md`, so a file leaving the walked roots is caught there rather than nowhere. ONE BINDING REASON REMAINS: of the five conjuncts, only `file-access` has a GATE rule behind it. `scripts/ci/check-p1-27-frontend.mjs` still declares exactly six rules — `no-merge-caller`, `no-duplicate-scan-on-a-queue`, `no-client-asserted-scope`, `no-invented-total`, `no-upload-path`, `no-console-output` (`:308`, `:314`, `:337`, `:355`, `:361`, `:367`) — so `export`, `document` and `media` are held by `p1-27-security.test.ts` alone, and a suite can be deleted in the same commit as the code it guards. CLOSABLE TODAY: add `no-export-caller` and `no-invented-media-limit` to `RULES` with a planted violation each in `tests/ci/p1-27-frontend-gate.test.ts`, which already runs an `it.each` over planted violations for all six existing rules.",
    "PROTECTED_REPROOF": "NOT REQUIRED. Every proof this row cites is executed by the `web` tier or by `tests/ci`, both of which run on every pull request (`web-quality` and `static-quality` in `_reusable-node-quality.yml`). Nothing here waits on a job that only a protected push starts."
  },
  "SEC-003": {
    "IMPLEMENTATION_SURFACES": "The abuse surface is what is ABSENT: no merge caller in either domain, no duplicate-scan caller on any review screen, no client fetch anywhere behind a write. Enforced by the gate rules `no-merge-caller` and `no-duplicate-scan-on-a-queue`, both with `allow: []` (`scripts/ci/check-p1-27-frontend.mjs:249-277`).",
    "NORMAL_NAVIGATION_ENTRY": "The two duplicate queues — `/crm/customer-duplicates` and `/vehicles/duplicates` — are the screens where a privileged audited operation would most plausibly be wired to a refresh button, and are where the absence is asserted.",
    "BACKEND_READ_CONTRACTS": "Every operation this phase calls resolves against the published contract, so none was invented (`p1-27-security.test.ts:712-724` over five method/path pairs).",
    "BACKEND_WRITE_CONTRACTS": "`crm.customer-merge`, `veh.vehicle-merge`, `crm.duplicate-scan` and `veh.vehicle-duplicate-scan` are the four that must NOT be called; all four are `DELIBERATELY_ABSENT` in `canonical-write-reachability.json` and `validate:p1-27-reachability` refuses any operation it cannot classify.",
    "REQUIRED_PERMISSIONS": "Escalation is prevented by holding each surface to its own code rather than to a domain code — the seventeen pairings under `SEC-001`. `crm.customer.merge` is checked nowhere, which is correct while `P1-OD-017` is open and is the subject of finding `H-04`.",
    "TENANT_ISOLATION_REQUIREMENT": "Client sends no tenant, company or branch key on any request: `query()` throws on all six scope names (`lib/api/read-operation.ts:92-100,126-133`), the gate rule `no-client-asserted-scope` scans all three canonical trees with `allow: []`, and `p1-27-qa.test.ts:503-556` sweeps every list adapter and all 23 write BODIES for the three words after asserting the request was actually issued.",
    "LOADING_STATE": "N/A with rationale: abuse-case and privilege-escalation control is a cross-cutting obligation with no screen of its own; the states belong to the Frontend rows this task governs.",
    "EMPTY_STATE": "N/A with rationale: abuse-case and privilege-escalation control is a cross-cutting obligation with no screen of its own; the states belong to the Frontend rows this task governs.",
    "ZERO_RESULT_STATE": "N/A with rationale: abuse-case and privilege-escalation control is a cross-cutting obligation with no screen of its own; the states belong to the Frontend rows this task governs.",
    "ERROR_STATE": "Every write stays behind a server action rather than a client fetch (`:966-974`), so a failure never reaches page script and the session token is never in its reach.",
    "DENIAL_STATE": "A denied read renders the denial INSTEAD of the screen and no write surface survives it, asserted over all nine customer surfaces at `p1-27-permission-route-binding.dom.test.tsx:634-651`.",
    "CONFLICT_STATE": "N/A with rationale: an abuse case is refused, not resolved; there is no conflicting write to reconcile.",
    "ARABIC_EVIDENCE": "N/A with rationale: an absent operation publishes no copy in either language.",
    "ENGLISH_EVIDENCE": "N/A with rationale: as above.",
    "RTL_LTR_EVIDENCE": "N/A with rationale: as above.",
    "ACCESSIBILITY_EVIDENCE": "The merge affordance is ABSENT rather than disabled — `DuplicateReviewScreen.tsx:23-32` states why — so there is no control to name. A disabled control would advertise a capability the product does not have and is the single most likely thing for a later commit to enable.",
    "PRODUCTION_REACHABILITY": "The absences are enforced over `PHASE_FILES` and by two gate rules that run in hosted CI through `validate:p1-27-frontend` (`_reusable-node-quality.yml:573`), so a suite deletion does not disarm them.",
    "PRODUCTION_TEST_EVIDENCE": "`p1-27-security.test.ts:669-725` — 4 cases: no merge caller, no duplicate-scan caller on a review screen, an idempotency key on every write and on no read, and every called operation resolving.",
    "NEGATIVE_OR_MUTATION_PROOF": "The idempotency case is two-sided — five writes must require a key and four reads must not — so a `requiresIdempotencyKey` that returned `true` for everything fails it. The gate's `evaluate()` fails any rule that inspected zero files, so a rule cannot pass by matching nothing.",
    "ROUND5_FINDING_IDS": "None mapped. `H-04` names `FE-016` and is the docblock residue of this task's own refusal.",
    "DOCUMENTATION_EVIDENCE": "canonical-plan §5.3 and §7 (`P1-OD-017`); `evidence/task-traceability.md` §4.",
    "FINAL_VERDICT": "PARTIAL",
    "VERDICT_RATIONALE": "The absence half is genuinely discharged and non-vacuously, and two gate rules carry it independently of the suite. Re-assessed at `26eab7e`; the previous cell was written at `9eff8bd` and the tree moved. ONE BINDING REASON REMAINS, and it is the second word of the task name: PRIVILEGE ESCALATION is cited from nowhere in this phase. Every write adapter is exported and directly callable, and `write-adapters-driven.test.ts` calls all twenty-three of them with no session permission model at all — correctly, because the adapters evaluate none; authorization is the Backend's. So the honest statement is that the web tier hides the control and the Backend refuses the call, and this record cites nothing for the second clause. CLOSABLE TODAY without new product code: cite, from this row, the API authorization case that refuses one named P1-27 write to a session lacking its capability — `validate:authorization-coverage` already runs in the clean room and the coverage exists to be cited. A citation is what is missing, not the refusal.",
    "PROTECTED_REPROOF": "NOT REQUIRED. Every proof this row cites is executed by the `web` tier or by `tests/ci`, both of which run on every pull request (`web-quality` and `static-quality` in `_reusable-node-quality.yml`). Nothing here waits on a job that only a protected push starts."
  },
  "SEC-004": {
    "IMPLEMENTATION_SURFACES": "`scripts/ci/generate-idempotent-operations.mjs` reads the published `x-audit-class` extension (`:87`, `:111-119`) and writes `auditClass` onto every `PublishedOperation` in `apps/web/src/lib/api/idempotent-operations.ts` (`:49` declares it; 243 operations carry it, with six distinct values); `lib/api/client.ts` for the correlation reference; the `no-console-output` rule in `scripts/ci/check-p1-27-frontend.mjs:367-383`.",
    "NORMAL_NAVIGATION_ENTRY": "N/A with rationale: an audit obligation has no entry point. The reference an operator quotes appears on the two route failure states named below.",
    "BACKEND_READ_CONTRACTS": "Every P1-27 read, asserted to declare `auditClass: none` — not from a written-down list but from the calls the shipped adapters actually make: `p1-27-qa.test.ts:844` resolves the operation each list adapter asked the client for and asserts its published class.",
    "BACKEND_WRITE_CONTRACTS": "Every write is declared `auditClass: privileged` in the published contract. That declaration is quoted in nine docblocks across `features/` and is asserted by NOTHING — see the rationale.",
    "REQUIRED_PERMISSIONS": "N/A with rationale: audit coverage is not permission-gated. The one audited READ this phase touches — `iam.audit.viewed` — belongs to the administration module, not to P1-27.",
    "TENANT_ISOLATION_REQUIREMENT": "Client sends no tenant, company or branch key on any request: `query()` throws on all six scope names (`lib/api/read-operation.ts:92-100,126-133`), the gate rule `no-client-asserted-scope` scans all three canonical trees with `allow: []`, and `p1-27-qa.test.ts:503-556` sweeps every list adapter and all 23 write BODIES for the three words after asserting the request was actually issued.",
    "LOADING_STATE": "N/A with rationale: audit-event coverage is a cross-cutting obligation with no screen of its own; the states belong to the Frontend rows this task governs.",
    "EMPTY_STATE": "N/A with rationale: audit-event coverage is a cross-cutting obligation with no screen of its own; the states belong to the Frontend rows this task governs.",
    "ZERO_RESULT_STATE": "N/A with rationale: audit-event coverage is a cross-cutting obligation with no screen of its own; the states belong to the Frontend rows this task governs.",
    "ERROR_STATE": "`<BackendUnavailableState>` and `<ErrorState>` are the only two failure tags on the phase routes, and BOTH are asserted to carry `correlationId` by walking the rendered TAGS rather than the file text (`:782-830`). The inspected SET is named, not counted: a file-level `includes('correlationId')` passed on the customer profile route while its `BackendUnavailableState` had none.",
    "DENIAL_STATE": "A denial carries the reference out with it, driven through the real route at `p1-27-permission-route-binding.dom.test.tsx:645` (`expect(text).toContain('cid-denied')`).",
    "CONFLICT_STATE": "N/A with rationale: a conflict is an outcome, not an audit event; its copy belongs to `QA-004`.",
    "ARABIC_EVIDENCE": "The correlation reference is an opaque identifier and is not translated; the surrounding failure copy resolves in both catalogues under `state.*`.",
    "ENGLISH_EVIDENCE": "Same keys in en.json.",
    "RTL_LTR_EVIDENCE": "N/A with rationale: an identifier has no direction; the states that carry it are covered by the screen rows.",
    "ACCESSIBILITY_EVIDENCE": "The reference is rendered as text inside the failure state rather than as a title attribute, so it is available to a screen reader. Not separately asserted.",
    "PRODUCTION_REACHABILITY": "The console rule runs in hosted CI over three trees via `validate:p1-27-frontend`; the correlation sweeps run in the web tier via `test:web-ci`.",
    "PRODUCTION_TEST_EVIDENCE": "`p1-27-security.test.ts:745-993` — 49 cases in the file, measured and passing at this head — for the correlation and console halves. The audit half is `p1-27-qa.test.ts:788-845` and `operation-contract.test.ts:210-350` (34 cases, measured), which assert the class is non-blank on all 243 operations, that more than one distinct value exists (so a generator emitting `none` everywhere fails), and that every value is one of the declared classes.",
    "NEGATIVE_OR_MUTATION_PROOF": "The console rule is read out of the gate rather than restated, and is shown to match every console method and NOT to match innocent text, so the case cannot be satisfied by `/./` (`p1-27-security.test.ts:870-966`). On the audit half the anti-vacuity is explicit: `operation-contract.test.ts:215` fails on any blank class and `:227` fails a generator that emitted one value for all 243 rows.",
    "ROUND5_FINDING_IDS": "`G-06` FIXED at `cd8d942` — the gate was widened to any `console.*` while the cited test stayed at five names; `p1-27-security.test.ts:868-870` now reads the rule out of the gate that enforces it. This register recorded `G-06` as OPEN at `9eff8bd`, by which time `cd8d942` was already an ancestor.",
    "DOCUMENTATION_EVIDENCE": "canonical-plan §5.3; `evidence/task-traceability.md` §4, whose `SEC-004` cell states \"every write is `auditClass: privileged`; every read is `none`\" — a sentence that was prose when it was written and is now the assertion at `p1-27-qa.test.ts:811` and `:844`.",
    "FINAL_VERDICT": "PASS",
    "VERDICT_RATIONALE": "The console half was already strong and derived from the gate's own `RULES`. The single reason this was PARTIAL was that the AUDIT-EVENT half — the words in the task name — had no executable assertion anywhere: `auditClass` appeared in `apps/web` only inside docblocks, and nothing could derive one. Re-assessed at `26eab7e`; the previous cell was written at `9eff8bd` and the tree moved. The remedy recorded here as \"buildable today\" was built at `6ba21e4`, exactly as specified: the generator carries the published `x-audit-class` onto `PublishedOperation`, and the assertion is made against the calls the application MAKES rather than against a list — `p1-27-qa.test.ts:811` resolves the operation behind each of the twenty-three executed write adapters and requires `privileged`, `:844` does the same for each list adapter and requires `none`. That is stronger than the sentence it replaces, because it would fail if the app called an operation the contract does not publish at all.",
    "PROTECTED_REPROOF": "NOT REQUIRED. Every proof this row cites is executed by the `web` tier or by `tests/ci`, both of which run on every pull request (`web-quality` and `static-quality` in `_reusable-node-quality.yml`). Nothing here waits on a job that only a protected push starts."
  },
  "QA-001": {
    "IMPLEMENTATION_SURFACES": "`tests/support/write-drives.ts` — one accepted call per write adapter, shared by two suites so they cannot come to disagree about the set; `write-adapters-driven.test.ts` executes them; `profile-accessibility.dom.test.tsx` renders the two profile screens section by section.",
    "NORMAL_NAVIGATION_ENTRY": "N/A with rationale: coverage is a property of the suite, not a screen. The suites run under `npm run test:web-ci` (`_reusable-node-quality.yml:579-586`).",
    "BACKEND_READ_CONTRACTS": "Eighteen list adapters are driven and each is asserted to have issued exactly one request before anything is claimed about it (`p1-27-qa.test.ts:484-499`), so an adapter that silently stopped calling the client fails rather than passes.",
    "BACKEND_WRITE_CONTRACTS": "All 23. `write-adapters-driven.test.ts:120-155` asserts the driven set equals the exported set as a two-directional comparison against a filesystem walk, so a twenty-fourth adapter fails the suite rather than joining it unnoticed.",
    "REQUIRED_PERMISSIONS": "N/A with rationale: the adapters evaluate no permission — the routes do, and `SEC-001` carries that evidence.",
    "TENANT_ISOLATION_REQUIREMENT": "Client sends no tenant, company or branch key on any request: `query()` throws on all six scope names (`lib/api/read-operation.ts:92-100,126-133`), the gate rule `no-client-asserted-scope` scans all three canonical trees with `allow: []`, and `p1-27-qa.test.ts:503-556` sweeps every list adapter and all 23 write BODIES for the three words after asserting the request was actually issued.",
    "LOADING_STATE": "`DataTable.tsx:267-279` is exercised by the component suites; `loading-boundary.dom.test.tsx` covers the route-level boundary.",
    "EMPTY_STATE": "`p1-27-qa.test.ts:484-499` — every list adapter returns `status: 'ok'` with `rows: []` for an empty page, never an error.",
    "ZERO_RESULT_STATE": "`search-empty-states.dom.test.tsx` separates a not-yet-searched idle state from a zero-result.",
    "ERROR_STATE": "All eleven transport failure kinds are driven through every list adapter (`p1-27-qa.test.ts`), and none throws.",
    "DENIAL_STATE": "Covered by `write-permission-gating.dom.test.tsx` and, on the real routes, by `p1-27-permission-route-binding.dom.test.tsx`.",
    "CONFLICT_STATE": "Covered by `QA-004`; the conflict-copy mapping is proved in `api-client.test.ts:726-800`.",
    "ARABIC_EVIDENCE": "`write-adapters-driven.test.ts:1043` — no rejected write returns a message key that is missing from either catalogue, which is the case that caught unkeyed Zod prose rendering under an Arabic label.",
    "ENGLISH_EVIDENCE": "Same case; both catalogues are read, not one.",
    "RTL_LTR_EVIDENCE": "The DOM suites render in both directions through `renderLtr`/`renderRtl` (`tests/render.tsx`).",
    "ACCESSIBILITY_EVIDENCE": "`profile-accessibility.dom.test.tsx` — 26 cases, measured — scans both profile screens section by section in both directions with the same four WCAG tags the browser gate claims, and pins that SC 2.5.3 is returned `incomplete` under jsdom rather than assuming either way.",
    "PRODUCTION_REACHABILITY": "Only `@/lib/api/server-client` is mocked in `write-adapters-driven.test.ts`; the adapters, their Zod schemas and their body assembly are the real ones and the assertions are on the request that came out.",
    "PRODUCTION_TEST_EVIDENCE": "`write-adapters-driven.test.ts` — 63 cases, measured. `p1-27-qa.test.ts` — 26. `profile-accessibility.dom.test.tsx` — 26. All passing at this head.",
    "NEGATIVE_OR_MUTATION_PROOF": "Two-directional completeness: `DRIVE_IDS`/`exportedWriteAdapters()` compares the driven set against a filesystem walk in both directions, so an adapter added without a drive fails and a drive naming an adapter that no longer exists fails.",
    "ROUND5_FINDING_IDS": "`H-05` FIXED — the two creation adapters were mocked everywhere and executed nowhere. `H-06` OPEN — `readCustomer` still is.",
    "DOCUMENTATION_EVIDENCE": "canonical-plan §5.3; `evidence/task-traceability.md` §5; `.github/ci-baselines/coverage-baseline.web.json`, which records the establishment run, the two known gaps and the critical-module floors.",
    "FINAL_VERDICT": "PASS",
    "VERDICT_RATIONALE": "The write half is execution rather than a source-text scan, with completeness asserted as a two-directional set comparison. Re-assessed at `26eab7e`; the previous cell was written at `9eff8bd` and the tree moved. All three gaps recorded here are closed. FIRST and largest — \"there is NO coverage measurement for `apps/web` at all\" — is now false: `apps/web/vitest.config.ts` declares a root-level `coverage` block with `provider: v8`, `all: true` and an explicit four-root `include`, and `_reusable-node-quality.yml:608-617` runs `coverage-gate.mjs` against `.github/ci-baselines/coverage-baseline.web.json` (79.45% lines / 86.92% functions / 87.00% branches measured, floors set below with stated headroom, a 60% touched-file minimum and two critical-module floors). The configuration is honest in the way that matters: the exclusion list is empty and says so, the dashboard routes at 7.62% and two zero-coverage library modules are recorded as `knownGaps` rather than excluded, and the `[locale]` / `(dashboard)` glob escaping is asserted against the real tree because unescaped it matches zero files while the percentage goes UP. SECOND: the completeness walk now matches three declaration forms and reads `.tsx` as well as `.ts` (`write-drives.ts:311-318`, `:361-388`), with `writeAdapterNamesIn` exported so a form can be put in front of the scanner directly rather than only against a tree that does not contain it yet. THIRD: the read-side gap (`H-06`) is real, is still open, and is recorded on `FE-006`, whose canonical operation it is; it is one adapter, it is inside the measured denominator, and it does not make \"unit and component coverage\" unmet at this head — 1533 cases across 70 files executed here with 0 failed and 0 skipped.",
    "PROTECTED_REPROOF": "OUTSTANDING — the web coverage establishment figure. `coverage-gate.mjs` runs against `.github/ci-baselines/coverage-baseline.web.json` on every pull request (`_reusable-node-quality.yml:608-617`), so the GATE needs no protected push. What does is the baseline's own provenance: `establishedBy` records a LOCAL measurement and says so in those words, and `establishmentProvenance` states that the first hosted run is the authority and replaces it. That is provenance for a number, not a defect in the feature."
  },
  "QA-002": {
    "IMPLEMENTATION_SURFACES": "`lib/api/client.ts:40-110` — `Violation` and `ProblemDetails`, rewritten to the shape the API actually publishes; `violationKeysOf`/`fieldErrorsOf` (`:591-665`) which read `violations`.",
    "NORMAL_NAVIGATION_ENTRY": "N/A with rationale: an error contract has no entry point; every failing request reaches it.",
    "BACKEND_READ_CONTRACTS": "Every read failure. All eleven transport kinds are driven through every list adapter and none throws (`p1-27-qa.test.ts`).",
    "BACKEND_WRITE_CONTRACTS": "Every write failure, including the 422 that had never once produced a field error: `fieldErrorsOf` read `problem.errors` while the API emits `violations`, so it returned `{}` for every real validation failure and no form in CRM or Vehicle could show a field-level error.",
    "REQUIRED_PERMISSIONS": "N/A with rationale: the error contract is evaluated after authorization and does not depend on it.",
    "TENANT_ISOLATION_REQUIREMENT": "Client sends no tenant, company or branch key on any request: `query()` throws on all six scope names (`lib/api/read-operation.ts:92-100,126-133`), the gate rule `no-client-asserted-scope` scans all three canonical trees with `allow: []`, and `p1-27-qa.test.ts:503-556` sweeps every list adapter and all 23 write BODIES for the three words after asserting the request was actually issued.",
    "LOADING_STATE": "N/A with rationale: API contract and error-path coverage is a cross-cutting obligation with no screen of its own; the states belong to the Frontend rows this task governs.",
    "EMPTY_STATE": "An empty page is `ok` with no rows, never a failure (`p1-27-qa.test.ts:484-499`).",
    "ZERO_RESULT_STATE": "N/A with rationale: API contract and error-path coverage is a cross-cutting obligation with no screen of its own; the states belong to the Frontend rows this task governs.",
    "ERROR_STATE": "`FAILURE_MESSAGE_KEY` is asserted to map every kind to a key beginning `state.` or `form.` (`api-client.test.ts:420-423`), so a new kind cannot arrive with no copy.",
    "DENIAL_STATE": "403 carries `requiredPermissions`, a field the old declaration did not have and the API has always sent.",
    "CONFLICT_STATE": "Belongs to `QA-004`: `client.ts:726-727` chooses the sentence from the catalog code rather than from the kind.",
    "ARABIC_EVIDENCE": "Every violation maps to a translation KEY, never to prose — the API deliberately sends no message — so both catalogues carry the copy and `field-error-translation.test.ts` holds the mapping.",
    "ENGLISH_EVIDENCE": "Same keys in en.json.",
    "RTL_LTR_EVIDENCE": "N/A with rationale: a problem document has no direction; the screens that render its keys carry the RTL evidence.",
    "ACCESSIBILITY_EVIDENCE": "Field errors are rendered with `role=\"alert\"` and `aria-invalid` by the components that consume them; `VehicleProfileScreen` renders none at all, which is finding `H-08`.",
    "PRODUCTION_REACHABILITY": "The `QA-002` cases drive a REAL request through the client with a stubbed `fetch`, so the mapping is exercised on the path a screen actually takes rather than by calling the mapper directly.",
    "PRODUCTION_TEST_EVIDENCE": "`api-client.test.ts` — 88 cases, measured, passing. `p1-27-qa.test.ts` — 26.",
    "NEGATIVE_OR_MUTATION_PROOF": "`REAL_RULES` (`api-client.test.ts:450-464`) is fourteen rule tokens that appear in `apps/api/src`, and the paths are forms `toViolations` actually produces, so a fixture cannot be satisfied by inventing a shape. Stronger still, the declaration READER is checked before it is believed (`:930-976`): it is shown to read a member whose type is an inline object, to ignore a field only a comment mentions, to fail loudly when asked for an interface that is not there, and to be reading the real files rather than a path that resolved to nothing — because an empty field list would otherwise compare equal to another empty field list and the whole comparison would pass over two files it never opened.",
    "ROUND5_FINDING_IDS": "None mapped. The contract correction landed in `af6c6fc` before the round-five register was rebuilt, and the derivation check that closed the remaining gap landed at `5048c8f`.",
    "DOCUMENTATION_EVIDENCE": "canonical-plan §5.3; `evidence/task-traceability.md` §5.",
    "FINAL_VERDICT": "PASS",
    "VERDICT_RATIONALE": "The contract is the real one and the fixtures are real, and the defect it fixed was total: no form in either domain could show a field error, because `fieldErrorsOf` read `problem.errors` while the API emits `violations`. The single reason this was PARTIAL was that nothing DERIVED the client's interface from `apps/api/src/server/errors/problem.ts` — renaming `violations` there changed nothing on the web side. Re-assessed at `26eab7e`; the previous cell was written at `9eff8bd` and the tree moved. That check now exists and is exactly the one specified: `api-client.test.ts:977-1050` reads `ProblemDocument` off disk from `apps/api/src/server/errors/problem.ts` and `ProblemDetails` off disk from `lib/api/client.ts`, and asserts the SAME field names on both sides, every client field optional because the body is untrusted, the violation MEMBER type derived too rather than only the envelope, and that the running code reads only fields the API sends. The reader itself is proved before it is trusted, which is what stops the comparison passing over two files it failed to parse.",
    "PROTECTED_REPROOF": "NOT REQUIRED. Every proof this row cites is executed by the `web` tier or by `tests/ci`, both of which run on every pull request (`web-quality` and `static-quality` in `_reusable-node-quality.yml`). Nothing here waits on a job that only a protected push starts."
  },
  "QA-003": {
    "IMPLEMENTATION_SURFACES": "`lib/api/read-operation.ts:92-100,126-133` — `query()` throws on any of the six scope key names; `companyFilterQuery` is the one sanctioned exception and is asserted to have exactly one call site.",
    "NORMAL_NAVIGATION_ENTRY": "N/A with rationale: isolation is a property of every request, not of a route.",
    "BACKEND_READ_CONTRACTS": "Every list read: 18 adapters driven, each asserted to have issued exactly one request before its arguments are inspected.",
    "BACKEND_WRITE_CONTRACTS": "All 23 write bodies, swept by `JSON.stringify` over the whole `send` call — not argument zero — so a scope smuggled into an options object could not hide in a path comparison (`p1-27-qa.test.ts:523-553`).",
    "REQUIRED_PERMISSIONS": "N/A with rationale: scope resolution is server-side and carries no client-held permission code.",
    "TENANT_ISOLATION_REQUIREMENT": "Client sends no tenant, company or branch key on any request: `query()` throws on all six scope names (`lib/api/read-operation.ts:92-100,126-133`), the gate rule `no-client-asserted-scope` scans all three canonical trees with `allow: []`, and `p1-27-qa.test.ts:503-556` sweeps every list adapter and all 23 write BODIES for the three words after asserting the request was actually issued.",
    "LOADING_STATE": "N/A with rationale: tenant, company and branch isolation is a cross-cutting obligation with no screen of its own; the states belong to the Frontend rows this task governs.",
    "EMPTY_STATE": "An empty result is rendered as empty and never as \"the tenant has no records\" — the distinction `FE-002` was reopened for, held by `search-empty-states.dom.test.tsx`.",
    "ZERO_RESULT_STATE": "As above.",
    "ERROR_STATE": "A cross-scope record answers `ERR-RES-001`, the same 404 as absent, soft-deleted and merged-away, so the endpoint is not an existence oracle.",
    "DENIAL_STATE": "`p1-27-permission-route-binding.dom.test.tsx:653-670` asserts the not-found state hints at none of \"tenant\", \"deleted\", \"merged\" or \"another\" — if the copy ever started speculating, the existence oracle is back.",
    "CONFLICT_STATE": "N/A with rationale: isolation refuses, it does not collide.",
    "ARABIC_EVIDENCE": "The not-found and denied copy resolves in both catalogues; the leak sweep runs on the English text, which is the wider of the two.",
    "ENGLISH_EVIDENCE": "Same keys in en.json.",
    "RTL_LTR_EVIDENCE": "N/A with rationale: a scope key has no direction.",
    "ACCESSIBILITY_EVIDENCE": "N/A with rationale: isolation renders nothing of its own.",
    "PRODUCTION_REACHABILITY": "Three independent mechanisms: `query()` throwing at runtime, the gate rule `no-client-asserted-scope` over three trees in hosted CI, and the runtime body sweep. The gate rule is POSITIONAL (`assertedScopes()`), so a profile screen DISPLAYING the tenant the server resolved is not a violation while sending one is.",
    "PRODUCTION_TEST_EVIDENCE": "`p1-27-qa.test.ts:502-556` and `p1-27-security.test.ts:186-335` for the client half, plus `p1-27-security.test.ts:1137-1325` — \"what the client tier can prove about isolation\" — which drives the real read path through a stubbed transport, proves the instrument fires on a request that DOES carry a scope, and then states in executable form what this tier cannot decide. The end-to-end claim is `e2e/authenticated/isolation.spec.ts`, six cases with two positive controls, executed by the `authenticated-browser` job.",
    "NEGATIVE_OR_MUTATION_PROOF": "The positive control is the load-bearing half: `expect(get).toHaveBeenCalledTimes(1)` before the sweep, because `?? ''` made \"no request at all\" pass as \"no scope\" and an adapter that silently stopped calling the client satisfied the loop perfectly.",
    "ROUND5_FINDING_IDS": "None mapped.",
    "DOCUMENTATION_EVIDENCE": "canonical-plan §5.3; `evidence/task-traceability.md` §5; `evidence/test-catalogue-traceability.json`, whose \"scope denial\" path is recorded `PARTIAL` with the reason, and `.github/ci-baselines/unrun-test-tiers.json`, which now records the tier as WIRED AND RUN and states plainly that no hosted runner has executed it yet.",
    "FINAL_VERDICT": "PASS",
    "VERDICT_RATIONALE": "What runs is good, is proved three ways, and its positive controls are real — the `toHaveBeenCalledTimes(1)` before each sweep is load-bearing, because `?? ''` made \"no request at all\" pass as \"no scope\". The single reason this was PARTIAL was that the client half proves the client SENDS no scope while ISOLATION is a different claim, and the one artefact that decides it end to end was gated behind `ROOTLCO_E2E_AUTH=1`, \"which is set in no workflow\". Re-assessed at `26eab7e`; the previous cell was written at `9eff8bd` and the tree moved. That is now false: `protected-develop-verification.yml:319` sets it, and the `authenticated-browser` job starts Supabase, bootstraps a real second tenant, runs the API as a login holding neither SUPERUSER nor BYPASSRLS, and fails unless every authenticated spec contributed an executed test. The web tier also stopped hand-waving about its own limit: `p1-27-security.test.ts:1255-1298` pins that `CustomerSearchHit` carries no scope field while `SessionSummary` carries all three, so the comparison is structurally impossible here and the day that changes the case fails. The feature and its proof exist at this head; the outstanding item is the reproof, recorded in its own column rather than as a defect in the task.",
    "PROTECTED_REPROOF": "OUTSTANDING — the AUTHENTICATED browser tier. It is now wired and run: the `authenticated-browser` job (`protected-develop-verification.yml:176-419`) sets `ROOTLCO_E2E_AUTH: '1'` at `:319`, bootstraps a real operator and a second tenant, and fails unless every authenticated spec contributed an executed test. It runs on pushes to `develop` and `main` ONLY, and `.github/ci-baselines/unrun-test-tiers.json` (`notYetObservedOnARunner`) records that it has never executed on a hosted runner even once. Collection is proved locally at 229 tests across 7 spec files. The FEATURE is complete at this head; the REPROOF is unpaid."
  },
  "QA-004": {
    "IMPLEMENTATION_SURFACES": "`lib/api/operation-contract.ts` (`requiresIdempotencyKey`, `resolveOperation`) over the generated `lib/api/idempotent-operations.ts`; `lib/api/client.ts:679-727` for conflict copy.",
    "NORMAL_NAVIGATION_ENTRY": "N/A with rationale: neither concurrency nor idempotency has a screen; both are properties of a request.",
    "BACKEND_READ_CONTRACTS": "Seven reads asserted to require NO key, so a GET never answers `400 ERR-INT-002` before authorization (`p1-27-qa.test.ts:731-743`).",
    "BACKEND_WRITE_CONTRACTS": "Eleven write paths asserted to require one (`:712-729`). The key is read off the OPERATION, not the HTTP verb — `crm.preference-set` is a PUT and is idempotent, and deriving from the method is what produced `P1-27-INT-003`.",
    "REQUIRED_PERMISSIONS": "N/A with rationale: the idempotency header is evaluated before authorization, which is exactly why getting it wrong answered 400 to everything.",
    "TENANT_ISOLATION_REQUIREMENT": "Client sends no tenant, company or branch key on any request: `query()` throws on all six scope names (`lib/api/read-operation.ts:92-100,126-133`), the gate rule `no-client-asserted-scope` scans all three canonical trees with `allow: []`, and `p1-27-qa.test.ts:503-556` sweeps every list adapter and all 23 write BODIES for the three words after asserting the request was actually issued.",
    "LOADING_STATE": "N/A with rationale: concurrency and idempotency is a cross-cutting obligation with no screen of its own; the states belong to the Frontend rows this task governs.",
    "EMPTY_STATE": "N/A with rationale: concurrency and idempotency is a cross-cutting obligation with no screen of its own; the states belong to the Frontend rows this task governs.",
    "ZERO_RESULT_STATE": "N/A with rationale: concurrency and idempotency is a cross-cutting obligation with no screen of its own; the states belong to the Frontend rows this task governs.",
    "ERROR_STATE": "A conflict maps to an error state rather than to a silent success (`p1-27-qa.test.ts:755-763`); reporting it as success would tell an operator their edit landed when it did not.",
    "DENIAL_STATE": "N/A with rationale: a conflict is not a denial and the two must not share copy — which is what this task fixed.",
    "CONFLICT_STATE": "The strongest evidence on this row. `client.ts:726-727` picks `state.conflict.title` for `ERR-CON-001` alone and `state.conflict.blocked.title` for every other 409, driven through a real request at `api-client.test.ts:726-800`, with a bare 409 failing safe to the sentence that claims nothing.",
    "ARABIC_EVIDENCE": "Both conflict sentences and both descriptions resolve in ar.json (`state.conflict.*`, `state.conflict.blocked.*`).",
    "ENGLISH_EVIDENCE": "en.json:720-723.",
    "RTL_LTR_EVIDENCE": "Catalogue parity `tests/i18n.test.ts:72`.",
    "ACCESSIBILITY_EVIDENCE": "Conflict copy renders through the same `Outcome`/`RecordForm` regions the other failure states use; not separately scanned.",
    "PRODUCTION_REACHABILITY": "`idempotent-operations.ts` is generated from `docs/api/openapi.v1.json` and reconciled by `validate:idempotent-operations`, so the table is the contract rather than a guess.",
    "PRODUCTION_TEST_EVIDENCE": "`p1-27-qa.test.ts:711-764` (4 cases); `api-client.test.ts:726-800` (4 cases, driving real 409 responses); `operation-contract.test.ts`.",
    "NEGATIVE_OR_MUTATION_PROOF": "Negative and exhaustive on the conflict half: \"reaches the concurrency sentence from no other catalog code\" walks ten 409 codes and requires every one of them to produce the blocked sentence, so widening the concurrency branch by one code fails.",
    "ROUND5_FINDING_IDS": "`A42-13` PARTIAL — the concurrency conjunct is contradicted by `P1-27-INT-009`. `H-09` FIXED at `fd51140` — the vehicle 409 said \"Someone else changed this\" for a VIN collision.",
    "DOCUMENTATION_EVIDENCE": "canonical-plan §5.3; `evidence/task-traceability.md` §5; `evidence/test-catalogue-traceability.md` §4 records \"stale version\" and \"concurrent update\" as `ABSENT` and \"idempotent replay\" as `PARTIAL`.",
    "FINAL_VERDICT": "PARTIAL",
    "VERDICT_RATIONALE": "The conflict-copy work is real, negatively proved over ten catalog codes, and removed a sentence that accused an absent third party for four distinct server refusals. Re-assessed at `26eab7e`; the previous cell was written at `9eff8bd` and the tree moved. ONE BINDING REASON REMAINS: the task name is a conjunction and the CONCURRENCY conjunct is unbuilt. No P1-27 write consumes `recordVersion` or sends an `If-Match` header — `ifMatch` occurs in `apps/web/src` only under `features/administration` and `features/authentication` — and `crm.preference-set` is not registered `versionGuarded`, so a preferences PUT is last-write-wins and two operators editing the same channel do not collide. Both contracts now SAY so rather than claiming otherwise (`crm/customers/profile-contract.ts:34-40`, `:66-71`, `:139-143`), which is the honest half; stating a gap is not closing it. WHAT CLOSES IT: a Backend decision on which P1-27 writes become version-guarded, then the header — a client-only version check is a guard the server does not enforce, so this cannot be closed from the web tier alone. Recorded separately because it is closable from here: the idempotency sample at `p1-27-qa.test.ts:712-729` is still eleven write paths listed by hand against twenty-three executed adapters, and it checks only that a key is REQUIRED — no case issues the same request twice and counts the side effects as a delta.",
    "PROTECTED_REPROOF": "NOT REQUIRED. Every proof this row cites is executed by the `web` tier or by `tests/ci`, both of which run on every pull request (`web-quality` and `static-quality` in `_reusable-node-quality.yml`). Nothing here waits on a job that only a protected push starts."
  },
  "QA-005": {
    "IMPLEMENTATION_SURFACES": "`scripts/ci/build-p1-27-evidence-manifest.mjs` (`evidence:p1-27` / `validate:p1-27-evidence`); `evidence/evidence-manifest.json`; `clean-room-evidence.md` and `ci-evidence.md`.",
    "NORMAL_NAVIGATION_ENTRY": "N/A with rationale: evidence packaging has no screen.",
    "BACKEND_READ_CONTRACTS": "N/A with rationale: this task reads the repository, not the API.",
    "BACKEND_WRITE_CONTRACTS": "N/A with rationale: as above.",
    "REQUIRED_PERMISSIONS": "N/A with rationale: as above.",
    "TENANT_ISOLATION_REQUIREMENT": "N/A with rationale: as above.",
    "LOADING_STATE": "N/A with rationale: regression and immutable evidence packaging is a cross-cutting obligation with no screen of its own; the states belong to the Frontend rows this task governs.",
    "EMPTY_STATE": "N/A with rationale: regression and immutable evidence packaging is a cross-cutting obligation with no screen of its own; the states belong to the Frontend rows this task governs.",
    "ZERO_RESULT_STATE": "N/A with rationale: regression and immutable evidence packaging is a cross-cutting obligation with no screen of its own; the states belong to the Frontend rows this task governs.",
    "ERROR_STATE": "`validate:p1-27-evidence` fails and NAMES the file whose digest moved; it does not merely report a mismatch.",
    "DENIAL_STATE": "N/A with rationale: as above.",
    "CONFLICT_STATE": "N/A with rationale: as above.",
    "ARABIC_EVIDENCE": "N/A with rationale: the evidence record is an internal English document set; no product copy is involved.",
    "ENGLISH_EVIDENCE": "N/A with rationale: as above.",
    "RTL_LTR_EVIDENCE": "N/A with rationale: as above.",
    "ACCESSIBILITY_EVIDENCE": "N/A with rationale: as above.",
    "PRODUCTION_REACHABILITY": "FACTUAL AND KNOWABLE TODAY: `validate:p1-27-evidence` reaches hosted CI through `verify:policies` -> `verify:workspaces`, invoked by the clean-room job (`_reusable-clean-room.yml:203`), which both `pr-ci.yml:203` and `protected-develop-verification.yml:119` call. Traced, not assumed. Measured at this head: `evidence manifest in sync — 36 documents`.",
    "PRODUCTION_TEST_EVIDENCE": "FACTUAL AND KNOWABLE TODAY: `tests/ci/p1-27-evidence-manifest.test.ts` recomputes every digest from disk, requires the manifest to cover the whole phase tree and to exclude exactly one path (itself), and requires each of the 13 non-Frontend canonical ids — derived from `canonical-plan.md`, not listed — to carry a traceability row and a register row.",
    "NEGATIVE_OR_MUTATION_PROOF": "NOT ASSESSED BY DESIGN, and the twelve open `QA005-*` findings say why it would be premature: `QA005-03` (a 40-hex regex that `deadbeef` five times satisfies), `QA005-06` (the digest recomputed with the function under test), `QA005-08` (eight unreferenced documents deletable with every case green) and `QA005-09` (most numbers on both evidence pages decorative) are all open at this head.",
    "ROUND5_FINDING_IDS": "`QA005-01` … `QA005-12` — ten OPEN, two FIXED (`QA005-10`, `QA005-11`), derived from `adversarial-round-five.md` rather than counted here. These twelve were NOT re-verified against this head; they belong to the evidence pages this task seals last, and asserting anything about them now is the exact defect the task exists to report.",
    "DOCUMENTATION_EVIDENCE": "canonical-plan §5.3; `evidence/task-traceability.md` §5, whose `QA-005` cell now carries a `<!-- derived: manifest fileCount -->` marker instead of the prose figure \"29 documents\" it had aged to; the manifest holds 36 and `validate:p1-27-doc-counts` fails if the two disagree.",
    "FINAL_VERDICT": "PARTIAL",
    "VERDICT_RATIONALE": "DELIBERATELY NOT ASSESSED, and PARTIAL here is a SEQUENCING HOLD rather than a finding against the work. `QA-005` seals the record and is closed LAST, against the final candidate head; any assessment made now measures a head that is not the one the phase closes on, which is precisely the defect this task exists to report. The factual fields above are filled where they are knowable today and are marked as such; the judgement fields are not. Re-assessed at `26eab7e`; the previous cell was written at `9eff8bd` and the tree moved. — and the re-assessment changed nothing here on purpose, because it is not this row's turn. ONE BINDING REASON, unchanged: the closing candidate does not exist yet, and the twelve `QA005-*` findings are undispositioned — ten of them open, four about assertions that cannot fail.",
    "PROTECTED_REPROOF": "NOT REQUIRED. Every proof this row cites is executed by the `web` tier or by `tests/ci`, both of which run on every pull request (`web-quality` and `static-quality` in `_reusable-node-quality.yml`). Nothing here waits on a job that only a protected push starts."
  },
  "DO-001": {
    "IMPLEMENTATION_SURFACES": "`scripts/ci/check-p1-27-frontend.mjs` — six rules over three canonical trees, every `allow` empty; `scripts/ci/check-p1-27-write-reachability.mjs`; `scripts/ci/summarise-vitest.mjs` and the per-tier floor; `.github/ci-baselines/phase-ownership-profiles.json` and `unrun-test-tiers.json`.",
    "NORMAL_NAVIGATION_ENTRY": "N/A with rationale: a CI gate has no navigation entry; it is reached by every push and every pull request.",
    "BACKEND_READ_CONTRACTS": "N/A with rationale: the gate reads the tree, not the API.",
    "BACKEND_WRITE_CONTRACTS": "`validate:p1-27-reachability` derives the canonical mutation list from the P1-24 register at check time and refuses any operation it cannot classify — 27 canonical / 23 REACHABLE / 4 DELIBERATELY_ABSENT / 0 BLOCKED / 0 UNCLASSIFIED.",
    "REQUIRED_PERMISSIONS": "N/A with rationale: the gate runs as CI and holds no product permission.",
    "TENANT_ISOLATION_REQUIREMENT": "The gate carries one of the three mechanisms that hold it: rule `no-client-asserted-scope`, positional rather than name-matching, so displaying the tenant the server resolved is permitted while sending one is not.",
    "LOADING_STATE": "N/A with rationale: the continuous-integration quality gate is a cross-cutting obligation with no screen of its own; the states belong to the Frontend rows this task governs.",
    "EMPTY_STATE": "`evaluate()` fails when zero files were inspected overall AND fails any individual rule that inspected zero files — so a rule cannot pass by matching nothing, which is how a scan that examined nothing reported clean.",
    "ZERO_RESULT_STATE": "As above; a zero-file scan is a failure, not a pass.",
    "ERROR_STATE": "The gate fails CLOSED on a symlink inside a scanned tree (`assertNotSymlink`), naming the path rather than silently omitting it, and on an unresolvable baseline path (`WTF-04`).",
    "DENIAL_STATE": "N/A with rationale: a gate has no denial state.",
    "CONFLICT_STATE": "N/A with rationale: as above.",
    "ARABIC_EVIDENCE": "N/A with rationale: gate output is developer-facing and is not product copy.",
    "ENGLISH_EVIDENCE": "N/A with rationale: as above.",
    "RTL_LTR_EVIDENCE": "N/A with rationale: as above.",
    "ACCESSIBILITY_EVIDENCE": "N/A with rationale: as above.",
    "PRODUCTION_REACHABILITY": "Traced rather than assumed. `validate:p1-27-frontend` and `validate:p1-27-reachability` run at `_reusable-node-quality.yml:587` and `:591` (`web-quality`). `validate:p1-27-matrix`, `validate:p1-27-doc-counts` and `validate:p1-27-evidence` reach CI through `verify:policies` -> `verify:workspaces` at `_reusable-clean-room.yml:203`, which `pr-ci.yml:203` and `protected-develop-verification.yml:119` both call. `validate:phase-ownership` is wired at `:299-320` and now decides its own context: a pull request resolves the profile from the head branch, an ordinary push or dispatch from the pushed ref, a protected push is a DECLARED skip written to the summary and the artefact, and anything else exits 1.",
    "PRODUCTION_TEST_EVIDENCE": "`tests/ci/p1-27-frontend-gate.test.ts` re-derives the scan roots from `canonical-plan.md` and plants a violation for each of the six rules; `tests/ci/job-summary-rendering.test.ts` derives the required summary list from the workflow's own `--markdown` arguments; `tests/ci/e2e-tier-coverage.test.ts` (26 cases) holds `unrun-test-tiers.json` against the actual spec files AND asserts that only `protected-develop-verification.yml` sets `ROOTLCO_E2E_AUTH` (`:170-171`, `:217-218`); `tests/ci/phase-ownership.test.ts` covers the context decision, including \"contains no bare `exit 0` — the shape the defect had\"; `tests/ci/web-test-floor.test.ts` anchors the floor to the tree. All 38 `tests/ci` files and 833 cases executed at this head, 0 failed.",
    "NEGATIVE_OR_MUTATION_PROOF": "Every rule has a planted violation; the floor compares EXECUTED tests after `WTF-03` showed `it.skip` across the tier satisfied it with `numTotalTests` unchanged; `WTF-02` showed the baseline lookup lived in `main()`, which nothing imports. MUTATION PROVED AT THIS HEAD for the anchor `WTF-08` was about: setting `tiers.web.minTests` and `tiers.web.measured` both to 12 fails four cases in `tests/ci/web-test-floor.test.ts`, led by \"WTF-08: the floor is at least the number of cases the tree declares\" — the comparison now terminates in the suite on disk, not in a hand-written number.",
    "ROUND5_FINDING_IDS": "`WTF-01` … `WTF-11` all FIXED at this head. Four of them — `WTF-08`, `WTF-09` (`6aa84b5`) and `WTF-11` (`776fb50`) — were recorded OPEN in a register published at `9eff8bd`, by which time their closing commits were already ancestors; `WTF-10` was genuinely open then and closed at `5d28569`. `F-04` FIXED. `A42-12` FIXED at `0a3f713`, also already an ancestor when it was recorded open: the 42-task check is a set comparison against an enumerated specification plus a re-derivation from the plan (`tests/ci/p1-27-task-matrix.test.ts:76-105`), not a substring existence check.",
    "DOCUMENTATION_EVIDENCE": "canonical-plan §5.3; `evidence/task-traceability.md` §6; `ci-evidence.md` (the phase directory, not `evidence/` — the previous citation named a path that does not exist).",
    "FINAL_VERDICT": "PASS",
    "VERDICT_RATIONALE": "The gate spans three trees, every rule is planted-violation proved, phase ownership is wired to a job and refuses a context it cannot judge, and the floor measures execution rather than collection. The single reason this was PARTIAL was a TIER, not a rule: the authenticated browser tier — which holds the only end-to-end tenant-isolation proof and the only route-level accessibility proof — was \"executed by NOTHING\", and the four open `WTF-*` findings, led by `WTF-08`. Re-assessed at `26eab7e`; the previous cell was written at `9eff8bd` and the tree moved. Both are answered. The tier is now run by the `authenticated-browser` job (`protected-develop-verification.yml:176-419`), which sets the variable, stands up Supabase and the API, bootstraps a real operator and a second tenant, and then FAILS unless every authenticated spec contributed an executed test — and `tests/ci/e2e-tier-coverage.test.ts` fails if any other workflow starts setting the variable or if that job stops. The claim recorded in `unrun-test-tiers.json` that a hosted runner could not be given a live Supabase was withdrawn there in plain words. All four `WTF-*` are closed, `WTF-08` mutation-proved above. What remains is not a defect: the job runs on protected pushes only and has never executed on a runner, which is recorded in `PROTECTED_REPROOF` and in the baseline's own `notYetObservedOnARunner` field, and a green pull request still does not include it.",
    "PROTECTED_REPROOF": "OUTSTANDING — the AUTHENTICATED browser tier. It is now wired and run: the `authenticated-browser` job (`protected-develop-verification.yml:176-419`) sets `ROOTLCO_E2E_AUTH: '1'` at `:319`, bootstraps a real operator and a second tenant, and fails unless every authenticated spec contributed an executed test. It runs on pushes to `develop` and `main` ONLY, and `.github/ci-baselines/unrun-test-tiers.json` (`notYetObservedOnARunner`) records that it has never executed on a hosted runner even once. Collection is proved locally at 229 tests across 7 spec files. The FEATURE is complete at this head; the REPROOF is unpaid."
  },
  "DO-002": {
    "IMPLEMENTATION_SURFACES": "`lib/observability/client-log.ts` — `redact`, `looksLikeCredential`, `safeRoute`, `templateRoute`, `deliveringAdapter`, `installConfiguredMonitoringAdapter` (`:298-305`); `lib/env.ts:41-70` (`NEXT_PUBLIC_CLIENT_MONITORING_URL`, optional, no default); `lib/security/csp.ts` (`monitoringUrl` option and `connectSourceOrigin`); `src/proxy.ts:43-46`, which feeds the same variable to the policy builder; `app/[locale]/(dashboard)/error.tsx:8` is the render-error caller.",
    "NORMAL_NAVIGATION_ENTRY": "The adapter installs at MODULE LOAD, not on a component mount — every path that can report anything imports the module, so load is the one moment guaranteed to happen once and BEFORE the first event.",
    "BACKEND_READ_CONTRACTS": "N/A with rationale: diagnostics are delivered to a sink, not read from the API.",
    "BACKEND_WRITE_CONTRACTS": "`navigator.sendBeacon` to `NEXT_PUBLIC_CLIENT_MONITORING_URL`, or nothing. No vendor is named, no SDK is imported and no key is reached for; with the variable unset `currentAdapter()` stays `null` and `observability.test.ts` asserts that default rather than assuming it.",
    "REQUIRED_PERMISSIONS": "N/A with rationale: the browser beacon carries no session permission.",
    "TENANT_ISOLATION_REQUIREMENT": "Enforced by redaction rather than by scoping: `redact` drops the query string — \"which is where secrets hide\" — and `looksLikeCredential` removes long opaque tokens whatever the key is called.",
    "LOADING_STATE": "N/A with rationale: structured logging, monitoring and alert routing is a cross-cutting obligation with no screen of its own; the states belong to the Frontend rows this task governs.",
    "EMPTY_STATE": "N/A with rationale: structured logging, monitoring and alert routing is a cross-cutting obligation with no screen of its own; the states belong to the Frontend rows this task governs.",
    "ZERO_RESULT_STATE": "N/A with rationale: structured logging, monitoring and alert routing is a cross-cutting obligation with no screen of its own; the states belong to the Frontend rows this task governs.",
    "ERROR_STATE": "`report()` is not silent when no adapter is attached: it writes the REDACTED line, so a deployment with no sink still has something to read.",
    "DENIAL_STATE": "N/A with rationale: the logger makes no authorization decision.",
    "CONFLICT_STATE": "N/A with rationale: as above.",
    "ARABIC_EVIDENCE": "N/A with rationale: a diagnostic event is machine-facing and is never rendered to an operator in either language.",
    "ENGLISH_EVIDENCE": "N/A with rationale: as above.",
    "RTL_LTR_EVIDENCE": "N/A with rationale: as above.",
    "ACCESSIBILITY_EVIDENCE": "The operator-visible half is the correlation reference on the failure states, which `SEC-004` carries; the logger renders nothing itself.",
    "PRODUCTION_REACHABILITY": "The caller exists and runs (`installConfiguredMonitoringAdapter`, module load), and the DELIVERY path now works end to end: `src/proxy.ts:43-46` reads `NEXT_PUBLIC_CLIENT_MONITORING_URL` and passes it to `contentSecurityPolicy`, which adds its ORIGIN to `connect-src` — path discarded, non-`http(s)` values dropped rather than interpolated, and the header byte identical when the variable is unset. The console rule that keeps server state out of stdout runs in hosted CI over three trees.",
    "PRODUCTION_TEST_EVIDENCE": "`observability.test.ts` — 25 cases, measured, passing: \"redacts a JWT whatever it is called\", \"redacts a long opaque token whatever it is called\", \"drops the query string, which is where secrets hide\", \"is null until a deployment attaches one\", \"receives the REDACTED event, never the raw one\".",
    "NEGATIVE_OR_MUTATION_PROOF": "The redaction cases assert the sink receives the redacted event and NEVER the raw one, which fails against an adapter that delivers both; and `installConfiguredMonitoringAdapter` returns whether it installed, so \"configured and installed\" is distinguishable from \"not configured\" rather than inferred.",
    "ROUND5_FINDING_IDS": "`A42-02` PARTIAL — `DO-002` counted as delivered while its own record concedes alert routing is unattached. `A42-03` and `A42-05` FIXED.",
    "DOCUMENTATION_EVIDENCE": "canonical-plan §5.3; `evidence/task-traceability.md` §6, whose `DO-002` cell used to read \"`setMonitoringAdapter` has no production caller **by design**\" — a sentence this wave's own work falsified, corrected in place and bound to the code by a `<!-- checked: … -->` claim rather than left as prose.",
    "FINAL_VERDICT": "PARTIAL",
    "VERDICT_RATIONALE": "The production caller exists, and the PII discipline is the strongest part of the task: redaction was built before any delivery, deliberately, and is proved by name rather than by key. Re-assessed at `26eab7e`; the previous cell was written at `9eff8bd` and the tree moved. The first of the two recorded gaps is CLOSED. \"The enablement path does not work\" was true and is not: `contentSecurityPolicy` now takes `monitoringUrl`, `src/proxy.ts:43-46` passes the same variable the beacon uses, only the ORIGIN is emitted because a path matches nothing as a source, anything that is not an `http(s)` URL is dropped rather than interpolated, and with the variable unset the header is byte identical — all three asserted in `tests/security.test.ts` (43 cases, measured). The two docblocks that instructed the reader to widen the policy in `src/proxy.ts` by hand were corrected rather than left to be rediscovered. ONE BINDING REASON REMAINS: ALERT ROUTING IS THE THIRD WORD IN THE TASK NAME AND IS ABSENT. Nothing in `apps/web` defines a severity threshold, a route or a recipient, and no document claims one exists — `client-log.ts:289-293` states only that the sink must be an endpoint the deployment operates. CLOSABLE TODAY only as a decision, not as code: either record an accepted-limitation entry for P1-27 in the form P1-19, P1-20 and P1-24 use, and narrow the task name in `canonical-plan.md` to match it, or attach a route. What is not acceptable is the present state, where the name promises three things and the record delivers two.",
    "PROTECTED_REPROOF": "NOT REQUIRED. Every proof this row cites is executed by the `web` tier or by `tests/ci`, both of which run on every pull request (`web-quality` and `static-quality` in `_reusable-node-quality.yml`). Nothing here waits on a job that only a protected push starts."
  },
  "DOC-001": {
    "IMPLEMENTATION_SURFACES": "`evidence/test-catalogue-traceability.json` — 29 records, one per declared `TC-P1-27-###`, naming 20 files and 138 quoted case titles, with an 18-row `pathMatrix` and an explicit `weakCitations` block; `evidence/task-traceability.md`; `scripts/ci/check-p1-27-doc-counts.mjs` (`checkCatalogue`), which requires every named file to exist and every quoted title to be present in a COLLECTED test.",
    "NORMAL_NAVIGATION_ENTRY": "N/A with rationale: a documentation record has no navigation entry.",
    "BACKEND_READ_CONTRACTS": "The catalogue names no operations; the contract half of the task name is discharged elsewhere — `validate:p1-27-reachability` against the P1-24 register, and `validate:idempotent-operations` against `docs/api/openapi.v1.json`.",
    "BACKEND_WRITE_CONTRACTS": "As above.",
    "REQUIRED_PERMISSIONS": "N/A with rationale: a record holds no permission.",
    "TENANT_ISOLATION_REQUIREMENT": "N/A with rationale: as above.",
    "LOADING_STATE": "N/A with rationale: contract, catalogue and traceability synchronization is a cross-cutting obligation with no screen of its own; the states belong to the Frontend rows this task governs.",
    "EMPTY_STATE": "A catalogue id with no proof must state a gap, or the gate fails with \"names no proof and states no gap — an unstated gap is a lie\". At this head no id is in that state.",
    "ZERO_RESULT_STATE": "The gate fails when the plan declares zero ids, so the whole check cannot become vacuous by the plan being renamed.",
    "ERROR_STATE": "Each failure NAMES the id, the file and the quoted title, so a reader is sent to the row rather than to a count.",
    "DENIAL_STATE": "N/A with rationale: as above.",
    "CONFLICT_STATE": "N/A with rationale: as above.",
    "ARABIC_EVIDENCE": "N/A with rationale: the record is internal English; the product copy it points at is checked by `tests/i18n.test.ts` and by the guidance suite in both catalogues.",
    "ENGLISH_EVIDENCE": "N/A with rationale: as above.",
    "RTL_LTR_EVIDENCE": "N/A with rationale: as above.",
    "ACCESSIBILITY_EVIDENCE": "N/A with rationale: as above.",
    "PRODUCTION_REACHABILITY": "`validate:p1-27-doc-counts` reaches hosted CI through `verify:policies` -> `verify:workspaces` -> `_reusable-clean-room.yml:203`. Measured at this head before the rebinding: `93 derived claim(s) across 30 document(s), 0 disagreement(s)`.",
    "PRODUCTION_TEST_EVIDENCE": "`tests/ci/p1-27-doc-counts.test.ts` and `apps/web/tests/p1-27-doc-reconciliation.test.ts`; the per-FILE title check is the part that bites — the older web reconciliation concatenated every test file, so a title attributed to the WRONG file had always passed it (`G-11`).",
    "NEGATIVE_OR_MUTATION_PROOF": "The plan-to-record reconciliation is two-directional: an id the plan declares with no record fails, and a recorded id the plan does not declare fails. `matrixDischarged` is refused any value but `false`. An unrecognised `derived:` kind fails rather than passing silently, which the previous revision did not do.",
    "ROUND5_FINDING_IDS": "`G-07`, `G-08`, `G-11` FIXED; `E-03` OPEN by decision; `A42-06` and `A42-11` OPEN and owned here; `B-05` and `B-06` OPEN. `B-01` … `B-06`, `A-05` and the ten open `QA005-*` were NOT re-verified at this head — they are attacks on the evidence-page guards, which `QA-005` seals last, and this row does not claim otherwise.",
    "DOCUMENTATION_EVIDENCE": "canonical-plan §5.3 and §6; `evidence/task-traceability.md` §7; `evidence/test-catalogue-traceability.md` in full.",
    "FINAL_VERDICT": "PARTIAL",
    "VERDICT_RATIONALE": "All 29 catalogue ids resolve to executable cases, checked per FILE; the record says plainly that \"resolves\" is not \"proves\" and keeps two weak citations rather than deleting them; and the path matrix is recorded honestly. Re-assessed at `26eab7e`; the previous cell was written at `9eff8bd` and the tree moved. Two of the three recorded gaps are closed. The CONTRACT conjunct is no longer unclaimed — it is discharged by `validate:p1-27-reachability` (27 canonical mutations, 23 REACHABLE, 4 DELIBERATELY_ABSENT, 0 UNCLASSIFIED) and `validate:idempotent-operations`, both cited here now. And the stale prose in `evidence/task-traceability.md` §4-§6 — the security suite at 20 cases against a measured 49, the manifest at 29 documents against 36, and `DO-002` described as having no production caller by design — is corrected AND converted to `derived:` / `checked:` markers, so the next drift fails `validate:p1-27-doc-counts` instead of ageing. ONE BINDING REASON REMAINS: `canonical-plan.md` §6 requires each catalogue id to \"expand into the required path matrix\" of eighteen paths, and it has not — `matrixDischarged` is `false` and the gate refuses any other value. Eleven paths are PROVEN, four PARTIAL, three ABSENT. CLOSABLE TODAY, and the only one of the three that is: `recovery` — no case takes a CRM or vehicle screen from a rendered failure through Retry to a rendered success, and every ingredient for that case already exists in the dom tier. The other two, `stale version` and `concurrent update`, are the same Backend decision `A42-13` records against `QA-004` and cannot be closed from here.",
    "PROTECTED_REPROOF": "NOT REQUIRED. Every proof this row cites is executed by the `web` tier or by `tests/ci`, both of which run on every pull request (`web-quality` and `static-quality` in `_reusable-node-quality.yml`). Nothing here waits on a job that only a protected push starts."
  },
  "DOC-002": {
    "IMPLEMENTATION_SURFACES": "`operator-guide.md` and `developer-guide.md`, both bound to the code by `<!-- checked: … -->` claims that `scripts/ci/check-p1-27-doc-counts.mjs` proves against the executable thing each sentence describes; `evidence/change-log.md`, which now runs through the round-five, partial-closure and re-assessment waves at this head.",
    "NORMAL_NAVIGATION_ENTRY": "N/A with rationale: a guide has no navigation entry.",
    "BACKEND_READ_CONTRACTS": "The operator guide's search-budget sentence is proved against the API's own `expensive-read` policy read out of `apps/api/src/server/http/rate-limit.ts` — a Backend change to the limit fails the guide by name.",
    "BACKEND_WRITE_CONTRACTS": "The developer guide's \"where things live\" partition is proved against the real `'use server'` files, over the OWNERSHIP GATE's scan roots rather than the guide's narrower ones (`D-02`).",
    "REQUIRED_PERMISSIONS": "N/A with rationale: a guide holds no permission.",
    "TENANT_ISOLATION_REQUIREMENT": "N/A with rationale: as above.",
    "LOADING_STATE": "N/A with rationale: operator and developer guidance is a cross-cutting obligation with no screen of its own; the states belong to the Frontend rows this task governs.",
    "EMPTY_STATE": "N/A with rationale: operator and developer guidance is a cross-cutting obligation with no screen of its own; the states belong to the Frontend rows this task governs.",
    "ZERO_RESULT_STATE": "N/A with rationale: operator and developer guidance is a cross-cutting obligation with no screen of its own; the states belong to the Frontend rows this task governs.",
    "ERROR_STATE": "An unknown `checked:` claim name FAILS rather than passing silently, so a guide cannot opt a sentence in, misspell the name, and be unchecked indistinguishably from being checked.",
    "DENIAL_STATE": "N/A with rationale: as above.",
    "CONFLICT_STATE": "N/A with rationale: as above.",
    "ARABIC_EVIDENCE": "Every failure message quoted in the operator guide resolves in `ar.json` as well as `en.json` — the fix for `D-01`, where the guidance suite opened `en.json` only and \"quoted from the product\" was proved for one locale of two.",
    "ENGLISH_EVIDENCE": "Same, in en.json; the guidance suite reads the catalogue rather than a fixture.",
    "RTL_LTR_EVIDENCE": "N/A with rationale: the guides are internal English documents.",
    "ACCESSIBILITY_EVIDENCE": "N/A with rationale: as above.",
    "PRODUCTION_REACHABILITY": "`p1-27-guidance-reconciliation.test.ts` runs under `verify:web` and in the web tier job; the two `GUIDE_CLAIMS` run under `validate:p1-27-doc-counts` in the clean room. Two independent paths, deliberately — the claims that needed the Arabic catalogue and the ownership gate's roots could not live in the web workspace.",
    "PRODUCTION_TEST_EVIDENCE": "`apps/web/tests/p1-27-guidance-reconciliation.test.ts` (10 cases) — each pins a guide sentence to the executable thing it describes rather than to its own existence; `apps/web/tests/p1-27-doc-reconciliation.test.ts` holds the change-log half.",
    "NEGATIVE_OR_MUTATION_PROOF": "Writing the check found two defects in the guide it was written to prove, and the second was introduced by the fix for the first — the `vehicles/*actions.ts` row that named a file which has never existed. An existence check was considered and rejected: it passes against a guide describing a product nobody built.",
    "ROUND5_FINDING_IDS": "`D-01` and `D-02` FIXED; `A42-03` and `A42-05` FIXED; `E-05` FIXED. This row raises no new finding.",
    "DOCUMENTATION_EVIDENCE": "canonical-plan §5.3; `evidence/task-traceability.md` §7; `evidence/change-log.md`.",
    "FINAL_VERDICT": "PASS",
    "VERDICT_RATIONALE": "The GUIDANCE half was already gate-verified in the strong sense — a sentence held against the executable thing it describes, in both catalogues and over the wider scan roots. The single reason this was PARTIAL was the CHANGE-LOG half of the conjunction: `evidence/change-log.md` recorded nothing from round five, nothing from the wave that closed `DOC-001`, and nothing from the wave after it — a change log that ended two rounds before the head it shipped with. Re-assessed at `26eab7e`; the previous cell was written at `9eff8bd` and the tree moved. The missing entries are written: the round-five register and its `F-*` / `WTF-*` / `A42-*` / `G-*` / `H-*` findings, the four-branch partial-closure wave (`6d33739`, `bff8c71`, `673fadb`, `821239d`), the capability commits `5d28569`, `00d187b` and `26eab7e`, and this re-assessment. Two lines that asserted fixed defects in the present tense were superseded in place with the commit that closed each, and the terminal table — which read \"40/42 proven, 2 partial\" against a matrix whose own totals were 21/21 — no longer states a number of its own: it points at `task-matrix.json` `totals`, which `validate:p1-27-matrix` regenerates and compares byte-for-byte. A change log that carries a number nothing derives is how this one went wrong the first time.",
    "PROTECTED_REPROOF": "NOT REQUIRED. Every proof this row cites is executed by the `web` tier or by `tests/ci`, both of which run on every pull request (`web-quality` and `static-quality` in `_reusable-node-quality.yml`). Nothing here waits on a job that only a protected push starts."
  }
}
