node_modules
.next
.next-dev
# The repository's local-only directory: dev state, acceptance credentials, the
# dedicated Chrome profile. Git-ignored, but Prettier and ESLint do not read
# `.gitignore` and will happily walk a browser profile.
.local
out
build
dist
coverage
package-lock.json
supabase/.branches
supabase/.temp
*.docx
*.pdf

# Machine-generated by scripts/check-operation-test-coverage.mjs on every run of
# `npm run validate:operation-coverage` (which CI runs before `format:check`).
# Both are regenerated deterministically from the registry + test files, so
# gating them on prettier would fail CI whenever the generator's spacing differs.
# The P1-15 file is the same generator's P1-15-only projection.
docs/phase-1/phase-1-14/evidence/operation-test-matrix.json
docs/phase-1/phase-1-15/evidence/operation-test-matrix.json

# Machine-generated by scripts/ci/build-p1-27-task-matrix.mjs. The universe is
# derived from canonical-plan.md and alidate:p1-27-matrix compares the file
# byte-for-byte with a fresh build, so a formatter rewriting it would put two
# tools in permanent disagreement over one artefact.
docs/phase-1/phase-1-27/task-matrix.json

# The P1-28 sibling, generated by scripts/ci/build-p1-28-task-matrix.mjs and
# compared byte-for-byte by validate:p1-28-matrix, for the same reason.
docs/phase-1/phase-1-28/task-matrix.json

# Workspaces format themselves — each apps/* package owns its own prettier
# config. The root formatting the same files with a different config is a
# permanent disagreement between two formatters, not a style preference.
# Root orchestration runs each workspace check via format:check:all.
apps/

# Evidence the clean-room job writes into the workspace root while it runs.
# Machine-generated, never committed, and not ours to format — checking them
# made a passing clean room fail on the shape of its own output.
clean-room-*.json
clean-room-*.md
workflow-security.json
route-parity.json
test-honesty.json
