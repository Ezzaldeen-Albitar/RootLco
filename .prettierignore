node_modules
.next
out
build
dist
coverage
package-lock.json
supabase/.branches
supabase/.temp
*.docx
*.pdf

# Machine-generated by scripts/check-operation-test-coverage.mjs on every run of
# `npm run validate:operation-coverage` (which CI runs before `format:check`).
# It is regenerated deterministically from the manifest + test files, so gating
# it on prettier would fail CI whenever the generator's spacing differs.
docs/phase-1/phase-1-14/evidence/operation-test-matrix.json
