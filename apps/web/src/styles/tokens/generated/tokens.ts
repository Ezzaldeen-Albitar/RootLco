/*
 * GENERATED by apps/web/scripts/generate-design-tokens.mjs from src/styles/tokens/*.scss.
 * Do not edit: change the Sass token, then run `node scripts/generate-design-tokens.mjs`
 * from apps/web. validate:web-tokens and tests/design-tokens-generated.test.ts fail
 * when this file drifts. Colours are not emitted: reference var(--color-*).
 */

/** Spacing scale (`--space-*`) in pixels. */
export const SPACE_PX = {
  '0': 0,
  '1': 4,
  '2': 8,
  '3': 12,
  '4': 16,
  '5': 20,
  '6': 24,
  '8': 32,
  '10': 40,
  '12': 48,
  '16': 64,
  '20': 80,
  '24': 96,
} as const;

/** Radii (`--radius-*`) in pixels. */
export const RADIUS_PX = {
  none: 0,
  sm: 4,
  md: 6,
  lg: 8,
  xl: 12,
  '2xl': 16,
  full: 9999,
} as const;

/** Type sizes (`--font-size-*`) in pixels. */
export const FONT_SIZE_PX = {
  display: 32,
  'page-title': 24,
  'section-title': 18,
  body: 14,
  'body-large': 16,
  supporting: 13,
  label: 13,
  caption: 12,
  'table-header': 12,
  'table-cell': 14,
  button: 14,
} as const;

/** Line heights (`--line-height-*`) in pixels. */
export const LINE_HEIGHT_PX = {
  display: 40,
  'page-title': 32,
  'section-title': 26,
  body: 22,
  'body-large': 24,
  supporting: 20,
  label: 18,
  caption: 16,
  'table-header': 16,
  'table-cell': 20,
  button: 20,
} as const;

/** Font weights (`--font-weight-*`). */
export const FONT_WEIGHT = {
  regular: 400,
  medium: 500,
  semibold: 600,
  bold: 700,
} as const;

/** Layout measures (`--layout-*`) in pixels. */
export const LAYOUT_PX = {
  'sidebar-expanded': 264,
  'sidebar-collapsed': 72,
  'header-height': 60,
  'page-gutter': 24,
  'page-gutter-compact': 16,
  'content-max-width': 1440,
  'control-height-sm': 32,
  'control-height-md': 38,
  'control-height-lg': 44,
  'table-row-height': 48,
  'table-row-height-compact': 40,
  'chart-height': 240,
} as const;

/** Stacking layers (`--z-*`). */
export const Z_INDEX = {
  base: 0,
  raised: 10,
  sticky: 100,
  sidebar: 200,
  header: 300,
  dropdown: 400,
  overlay: 500,
  dialog: 600,
  toast: 700,
  tooltip: 800,
} as const;

/** Durations (`--duration-*`) in milliseconds. */
export const DURATION_MS = {
  instant: 80,
  fast: 120,
  base: 180,
  slow: 240,
  overlay: 200,
} as const;

/** Easing curves (`--ease-*`). */
export const EASING = {
  standard: 'cubic-bezier(0.2, 0, 0.2, 1)',
  enter: 'cubic-bezier(0, 0, 0.2, 1)',
  exit: 'cubic-bezier(0.4, 0, 1, 1)',
} as const;

/** Breakpoints in pixels. */
export const BREAKPOINT_PX = {
  sm: 640,
  md: 768,
  lg: 1024,
  xl: 1280,
  '2xl': 1440,
} as const;

/** Font stacks (`--font-family-*`). */
export const FONT_FAMILY = {
  sans: 'system-ui, -apple-system, Segoe UI, Noto Sans Arabic, Segoe UI Arabic, Roboto, Helvetica Neue, Arial, sans-serif',
  mono: 'ui-monospace, SFMono-Regular, SF Mono, Consolas, Liberation Mono, monospace',
} as const;
