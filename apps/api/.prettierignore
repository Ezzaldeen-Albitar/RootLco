# Build output and installed packages only.
#
# There is deliberately NO .prettierrc.json in this workspace: prettier resolves
# formatting options by walking UP from each file, so these files keep the exact
# rules the repository root already applied to them before the move. A second
# copy of the same six options would be a drift surface for no benefit.
.next
node_modules
coverage

# Regenerated by `next build`, gitignored, and not ours to format.
next-env.d.ts
